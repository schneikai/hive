import { Effect } from 'effect'
import { describe, expect, it } from 'vitest'
import {
  PET_JUMP_TO_WORKTREE_CHANNEL,
  PET_SETTINGS_UPDATED_CHANNEL,
  PET_STATUS_CHANNEL
} from '@shared/pet-events'
import { GIT_STATUS_CHANGED_CHANNEL } from '@shared/git-events'
import type { FileTreeNode, FlatFile } from '@shared/types/file-tree'
import type { GitBranchInfo, GitFileStatus } from '@shared/types/git'
import type { PetSettings } from '@shared/types/pet'
import { makeEventBus } from '../events/event-bus'
import type { GitOpsRpcService } from '../rpc/domains/git-ops'
import { parseKanbanBoardImportFile } from '../rpc/domains/kanban'
import { makeRpcRouter } from '../rpc/router'

describe('rpc router', () => {
  const makeRouter = () => makeRpcRouter({ eventBus: makeEventBus() })
  const project = {
    id: 'project-1',
    name: 'Hive',
    path: '/tmp/hive',
    description: null,
    tags: null,
    language: 'typescript',
    custom_icon: null,
    detected_icon: null,
    setup_script: null,
    run_script: null,
    archive_script: null,
    auto_assign_port: true,
    sort_order: 0,
    created_at: '2026-05-26T00:00:00.000Z',
    last_accessed_at: '2026-05-26T00:00:00.000Z'
  }
  const worktree = {
    id: 'worktree-1',
    project_id: project.id,
    name: 'main',
    branch_name: 'main',
    path: '/tmp/hive',
    status: 'active' as const,
    is_default: false,
    branch_renamed: 0,
    last_message_at: null,
    session_titles: '[]',
    last_model_provider_id: null,
    last_model_id: null,
    last_model_variant: null,
    attachments: '[]',
    pinned: 0,
    context: null,
    github_pr_number: null,
    github_pr_url: null,
    base_branch: null,
    created_at: '2026-05-26T00:00:00.000Z',
    last_accessed_at: '2026-05-26T00:00:00.000Z'
  }
  const session = {
    id: 'session-1',
    worktree_id: worktree.id,
    project_id: project.id,
    connection_id: null,
    name: 'Build session',
    status: 'active' as const,
    opencode_session_id: null,
    claude_session_id: null,
    agent_sdk: 'opencode' as const,
    mode: 'build' as const,
    session_type: 'default' as const,
    model_provider_id: null,
    model_id: null,
    model_variant: null,
    remote_launch: null,
    created_at: '2026-05-26T00:00:00.000Z',
    updated_at: '2026-05-26T00:00:00.000Z',
    column_changed_at: null,
    completed_at: null,
    pinned_to_board: false
  }
  const sessionMessage = {
    id: 'message-1',
    session_id: session.id,
    role: 'assistant' as const,
    content: 'Done',
    opencode_message_id: null,
    opencode_message_json: null,
    opencode_parts_json: null,
    opencode_timeline_json: null,
    created_at: '2026-05-26T00:01:00.000Z'
  }
  const sessionActivity = {
    id: 'activity-1',
    session_id: session.id,
    agent_session_id: null,
    thread_id: null,
    turn_id: null,
    item_id: null,
    request_id: null,
    kind: 'session.info' as const,
    tone: 'info' as const,
    summary: 'Session started',
    payload_json: null,
    sequence: 1,
    created_at: '2026-05-26T00:02:00.000Z'
  }
  const space = {
    id: 'space-1',
    name: 'Backlog',
    icon_type: 'emoji',
    icon_value: 'H',
    sort_order: 0,
    created_at: '2026-05-26T00:03:00.000Z'
  }
  const diffComment = {
    id: 'diff-comment-1',
    worktree_id: worktree.id,
    file_path: 'src/App.tsx',
    line_start: 42,
    line_end: null,
    anchor_text: null,
    anchor_context_before: null,
    anchor_context_after: null,
    body: 'Check this branch',
    is_outdated: false,
    created_at: '2026-05-26T00:04:00.000Z',
    updated_at: '2026-05-26T00:04:00.000Z',
    column_changed_at: null
  }

  it('handles system.ping', async () => {
    const response = await Effect.runPromise(
      makeRouter().handle({ id: '1', method: 'system.ping', params: {} })
    )

    expect(response).toEqual({ id: '1', ok: true, value: { ok: true } })
  })

  it('handles kanban.ticket.create through the kanban RPC domain', async () => {
    const ticket = {
      id: 'ticket-1',
      project_id: project.id,
      title: 'Ship HTTP migration',
      description: null,
      attachments: [],
      column: 'todo' as const,
      sort_order: 0,
      current_session_id: null,
      worktree_id: null,
      mode: null,
      plan_ready: false,
      created_at: '2026-05-26T00:05:00.000Z',
      updated_at: '2026-05-26T00:05:00.000Z',
      column_changed_at: null,
      archived_at: null,
      external_provider: null,
      external_id: null,
      external_url: null,
      github_pr_number: null,
      github_pr_url: null,
      mark: null,
      total_tokens: 0,
      pending_launch_config: null,
      goal_mode: false,
      goal_success_criteria: null,
      note: null,
      created_from_session: false,
      auto_approve_plan: false,
      model_provider_id: null,
      model_id: null,
      model_variant: null,
      variant_group_id: null
    }
    const calls: unknown[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: (projectId, data) =>
          Effect.sync(() => {
            calls.push({ projectId, data })
            return ticket
          }),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run'))
      }
    })

    const params = {
      project_id: project.id,
      title: 'Ship HTTP migration',
      description: null,
      attachments: [],
      column: 'todo' as const,
      plan_ready: false
    }
    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-create-1',
        method: 'kanban.ticket.create',
        params
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-create-1',
      ok: true,
      value: ticket
    })
    expect(calls).toEqual([{ projectId: project.id, data: params }])
  })

  it('validates kanban.ticket.create params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run for invalid params')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-create-2',
        method: 'kanban.ticket.create',
        params: { project_id: project.id }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-create-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('round-trips model fields and note through kanban.ticket.create', async () => {
    const ticket = {
      id: 'ticket-model-1',
      project_id: project.id,
      title: 'Ship HTTP migration',
      description: null,
      attachments: [],
      column: 'todo' as const,
      sort_order: 0,
      current_session_id: null,
      worktree_id: null,
      mode: null,
      plan_ready: false,
      created_at: '2026-05-26T00:05:00.000Z',
      updated_at: '2026-05-26T00:05:00.000Z',
      column_changed_at: null,
      archived_at: null,
      external_provider: null,
      external_id: null,
      external_url: null,
      github_pr_number: null,
      github_pr_url: null,
      mark: null,
      total_tokens: 0,
      pending_launch_config: null,
      goal_mode: false,
      goal_success_criteria: null,
      note: 'Launched via multi-model',
      created_from_session: false,
      auto_approve_plan: false,
      model_provider_id: 'anthropic',
      model_id: 'claude-opus-4-6',
      model_variant: 'thinking',
      variant_group_id: 'group-1'
    }
    const calls: unknown[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: (projectId, data) =>
          Effect.sync(() => {
            calls.push({ projectId, data })
            return ticket
          }),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run'))
      }
    })

    const params = {
      project_id: project.id,
      title: 'Ship HTTP migration',
      description: null,
      attachments: [],
      column: 'todo' as const,
      plan_ready: false,
      note: 'Launched via multi-model',
      model_provider_id: 'anthropic',
      model_id: 'claude-opus-4-6',
      model_variant: 'thinking',
      variant_group_id: 'group-1'
    }
    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-create-model-1',
        method: 'kanban.ticket.create',
        params
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-create-model-1',
      ok: true,
      value: ticket
    })
    expect(calls).toEqual([{ projectId: project.id, data: params }])
  })

  it('handles kanban.ticket.duplicate through the kanban RPC domain', async () => {
    const ticket = {
      id: 'ticket-2',
      project_id: project.id,
      title: 'Ship HTTP migration',
      description: null,
      attachments: [],
      column: 'todo' as const,
      sort_order: 0,
      current_session_id: null,
      worktree_id: null,
      mode: null,
      plan_ready: false,
      created_at: '2026-05-26T00:05:00.000Z',
      updated_at: '2026-05-26T00:05:00.000Z',
      column_changed_at: null,
      archived_at: null,
      external_provider: null,
      external_id: null,
      external_url: null,
      github_pr_number: null,
      github_pr_url: null,
      mark: null,
      total_tokens: 0,
      pending_launch_config: null,
      goal_mode: false,
      goal_success_criteria: null,
      note: null,
      created_from_session: false,
      auto_approve_plan: false,
      model_provider_id: 'anthropic',
      model_id: 'claude-opus-4-6',
      model_variant: 'thinking',
      variant_group_id: 'group-1'
    }
    const calls: unknown[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        duplicateTicket: (projectId, id, overrides) =>
          Effect.sync(() => {
            calls.push({ projectId, id, overrides })
            return ticket
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-duplicate-1',
        method: 'kanban.ticket.duplicate',
        params: {
          projectId: project.id,
          id: 'ticket-1',
          overrides: {
            column: 'in_progress',
            model_provider_id: 'anthropic',
            model_id: 'claude-opus-4-6',
            model_variant: 'thinking',
            variant_group_id: 'group-1'
          }
        }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-duplicate-1',
      ok: true,
      value: ticket
    })
    expect(calls).toEqual([
      {
        projectId: project.id,
        id: 'ticket-1',
        overrides: {
          column: 'in_progress',
          model_provider_id: 'anthropic',
          model_id: 'claude-opus-4-6',
          model_variant: 'thinking',
          variant_group_id: 'group-1'
        }
      }
    ])
  })

  it('validates kanban.ticket.duplicate params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        duplicateTicket: () =>
          Effect.die(new Error('duplicateTicket should not run for invalid params'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-duplicate-2',
        method: 'kanban.ticket.duplicate',
        params: { projectId: project.id, id: 'ticket-1', overrides: { unknownField: true } }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-duplicate-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.createBatch through the kanban RPC domain', async () => {
    const firstTicket = {
      id: 'ticket-1',
      project_id: project.id,
      title: 'Plan migration',
      description: null,
      attachments: [],
      column: 'todo' as const,
      sort_order: 0,
      current_session_id: null,
      worktree_id: null,
      mode: null,
      plan_ready: false,
      created_at: '2026-05-26T00:05:00.000Z',
      updated_at: '2026-05-26T00:05:00.000Z',
      column_changed_at: null,
      archived_at: null,
      external_provider: null,
      external_id: null,
      external_url: null,
      github_pr_number: null,
      github_pr_url: null,
      mark: null,
      total_tokens: 0,
      pending_launch_config: null,
      goal_mode: false,
      goal_success_criteria: null,
      note: null,
      created_from_session: false,
      auto_approve_plan: false,
      model_provider_id: null,
      model_id: null,
      model_variant: null,
      variant_group_id: null
    }
    const secondTicket = {
      ...firstTicket,
      id: 'ticket-2',
      title: 'Ship migration',
      sort_order: 1
    }
    const result = {
      tickets: [firstTicket, secondTicket],
      dependencies: [
        {
          dependent_id: 'ticket-2',
          blocker_id: 'ticket-1',
          created_at: '2026-05-26T00:06:00.000Z'
        }
      ]
    }
    const calls: unknown[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        createTicketBatch: (projectId, data) =>
          Effect.sync(() => {
            calls.push({ projectId, data })
            return result
          })
      }
    })

    const params = {
      drafts: [
        {
          draft_key: 'draft-1',
          project_id: project.id,
          title: 'Plan migration',
          depends_on: []
        },
        {
          draft_key: 'draft-2',
          project_id: project.id,
          title: 'Ship migration',
          depends_on: ['draft-1']
        }
      ]
    }
    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-create-batch-1',
        method: 'kanban.ticket.createBatch',
        params: { projectId: project.id, data: params }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-create-batch-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual([{ projectId: project.id, data: params }])
  })

  it('validates kanban.ticket.createBatch params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        createTicketBatch: () =>
          Effect.die(new Error('createTicketBatch should not run for invalid params'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-create-batch-2',
        method: 'kanban.ticket.createBatch',
        params: { drafts: [{ draft_key: 'draft-1', project_id: project.id }] }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-create-batch-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.get through the kanban RPC domain', async () => {
    const ticket = {
      id: 'ticket-1',
      project_id: project.id,
      title: 'Plan migration',
      description: null,
      attachments: [],
      column: 'todo' as const,
      sort_order: 0,
      current_session_id: null,
      worktree_id: null,
      mode: null,
      plan_ready: false,
      created_at: '2026-05-26T00:05:00.000Z',
      updated_at: '2026-05-26T00:05:00.000Z',
      column_changed_at: null,
      archived_at: null,
      external_provider: null,
      external_id: null,
      external_url: null,
      github_pr_number: null,
      github_pr_url: null,
      mark: null,
      total_tokens: 0,
      pending_launch_config: null,
      goal_mode: false,
      goal_success_criteria: null,
      note: null,
      created_from_session: false,
      auto_approve_plan: false,
      model_provider_id: null,
      model_id: null,
      model_variant: null,
      variant_group_id: null
    }
    const calls: Array<{ projectId: string; id: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        getTicket: (projectId, id) =>
          Effect.sync(() => {
            calls.push({ projectId, id })
            return ticket
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-get-1',
        method: 'kanban.ticket.get',
        params: { projectId: project.id, id: 'ticket-1' }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-get-1',
      ok: true,
      value: ticket
    })
    expect(calls).toEqual([{ projectId: project.id, id: 'ticket-1' }])
  })

  it('validates kanban.ticket.get params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run for invalid params')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-get-2',
        method: 'kanban.ticket.get',
        params: { id: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-get-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.getByProject through the kanban RPC domain', async () => {
    const ticket = {
      id: 'ticket-1',
      project_id: project.id,
      title: 'Plan migration',
      description: null,
      attachments: [],
      column: 'todo' as const,
      sort_order: 0,
      current_session_id: null,
      worktree_id: null,
      mode: null,
      plan_ready: false,
      created_at: '2026-05-26T00:05:00.000Z',
      updated_at: '2026-05-26T00:05:00.000Z',
      column_changed_at: null,
      archived_at: null,
      external_provider: null,
      external_id: null,
      external_url: null,
      github_pr_number: null,
      github_pr_url: null,
      mark: null,
      total_tokens: 0,
      pending_launch_config: null,
      goal_mode: false,
      goal_success_criteria: null,
      note: null,
      created_from_session: false,
      auto_approve_plan: false,
      model_provider_id: null,
      model_id: null,
      model_variant: null,
      variant_group_id: null
    }
    const calls: Array<{ projectId: string; includeArchived: boolean | undefined }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        getTicketsByProject: (projectId, includeArchived) =>
          Effect.sync(() => {
            calls.push({ projectId, includeArchived })
            return [ticket]
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-get-by-project-1',
        method: 'kanban.ticket.getByProject',
        params: { projectId: project.id, includeArchived: true }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-get-by-project-1',
      ok: true,
      value: [ticket]
    })
    expect(calls).toEqual([{ projectId: project.id, includeArchived: true }])
  })

  it('validates kanban.ticket.getByProject params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () =>
          Effect.die(new Error('getTicketsByProject should not run for invalid params')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-get-by-project-2',
        method: 'kanban.ticket.getByProject',
        params: { projectId: project.id, includeArchived: 'yes' }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-get-by-project-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.update through the kanban RPC domain', async () => {
    const ticket = {
      id: 'ticket-1',
      project_id: project.id,
      title: 'Updated migration',
      description: 'Updated description',
      attachments: [],
      column: 'in_progress' as const,
      sort_order: 0,
      current_session_id: null,
      worktree_id: null,
      mode: 'plan' as const,
      plan_ready: true,
      created_at: '2026-05-26T00:05:00.000Z',
      updated_at: '2026-05-26T00:06:00.000Z',
      column_changed_at: null,
      archived_at: null,
      external_provider: null,
      external_id: null,
      external_url: null,
      github_pr_number: 42,
      github_pr_url: 'https://github.com/acme/hive/pull/42',
      mark: 'rare' as const,
      total_tokens: 0,
      pending_launch_config: null,
      goal_mode: true,
      goal_success_criteria: 'Build passes',
      note: 'Keep private',
      created_from_session: false,
      auto_approve_plan: false,
      model_provider_id: null,
      model_id: null,
      model_variant: null,
      variant_group_id: null
    }
    const calls: Array<{ projectId: string; id: string; data: unknown }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        updateTicket: (projectId, id, data) =>
          Effect.sync(() => {
            calls.push({ projectId, id, data })
            return ticket
          })
      }
    })

    const data = {
      title: 'Updated migration',
      description: 'Updated description',
      column: 'in_progress' as const,
      mode: 'plan' as const,
      plan_ready: true,
      github_pr_number: 42,
      github_pr_url: 'https://github.com/acme/hive/pull/42',
      mark: 'rare' as const,
      goal_mode: true,
      goal_success_criteria: 'Build passes',
      note: 'Keep private',
      auto_approve_plan: false
    }
    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-update-1',
        method: 'kanban.ticket.update',
        params: { projectId: project.id, id: 'ticket-1', data }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-update-1',
      ok: true,
      value: ticket
    })
    expect(calls).toEqual([{ projectId: project.id, id: 'ticket-1', data }])
  })

  it('validates kanban.ticket.update params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run for invalid params')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-update-2',
        method: 'kanban.ticket.update',
        params: { id: 'ticket-1', data: { column: 'doing' } }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-update-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('round-trips model fields through kanban.ticket.update, then nulls them out', async () => {
    const ticketWithModel = {
      id: 'ticket-1',
      project_id: project.id,
      title: 'Ship HTTP migration',
      description: null,
      attachments: [],
      column: 'in_progress' as const,
      sort_order: 0,
      current_session_id: null,
      worktree_id: null,
      mode: null,
      plan_ready: false,
      created_at: '2026-05-26T00:05:00.000Z',
      updated_at: '2026-05-26T00:06:00.000Z',
      column_changed_at: null,
      archived_at: null,
      external_provider: null,
      external_id: null,
      external_url: null,
      github_pr_number: null,
      github_pr_url: null,
      mark: null,
      total_tokens: 0,
      pending_launch_config: null,
      goal_mode: false,
      goal_success_criteria: null,
      note: null,
      created_from_session: false,
      auto_approve_plan: false,
      model_provider_id: 'anthropic',
      model_id: 'claude-opus-4-6',
      model_variant: 'thinking',
      variant_group_id: 'group-1'
    }
    const ticketWithoutModel = {
      ...ticketWithModel,
      updated_at: '2026-05-26T00:07:00.000Z',
      column_changed_at: null,
      model_provider_id: null,
      model_id: null,
      model_variant: null,
      variant_group_id: null
    }
    const calls: Array<{ projectId: string; id: string; data: unknown }> = []
    const responses = [ticketWithModel, ticketWithoutModel]
    let callIndex = 0
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        updateTicket: (projectId, id, data) =>
          Effect.sync(() => {
            calls.push({ projectId, id, data })
            return responses[callIndex++]
          })
      }
    })

    const setData = {
      model_provider_id: 'anthropic',
      model_id: 'claude-opus-4-6',
      model_variant: 'thinking',
      variant_group_id: 'group-1'
    }
    const setResponse = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-update-model-1',
        method: 'kanban.ticket.update',
        params: { projectId: project.id, id: 'ticket-1', data: setData }
      })
    )

    expect(setResponse).toEqual({
      id: 'kanban-ticket-update-model-1',
      ok: true,
      value: ticketWithModel
    })

    const nullData = {
      model_provider_id: null,
      model_id: null,
      model_variant: null,
      variant_group_id: null
    }
    const nullResponse = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-update-model-2',
        method: 'kanban.ticket.update',
        params: { projectId: project.id, id: 'ticket-1', data: nullData }
      })
    )

    expect(nullResponse).toEqual({
      id: 'kanban-ticket-update-model-2',
      ok: true,
      value: ticketWithoutModel
    })
    expect(calls).toEqual([
      { projectId: project.id, id: 'ticket-1', data: setData },
      { projectId: project.id, id: 'ticket-1', data: nullData }
    ])
  })

  it('handles kanban.ticket.delete through the kanban RPC domain', async () => {
    const calls: Array<{ projectId: string; id: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: (projectId, id) =>
          Effect.sync(() => {
            calls.push({ projectId, id })
            return true
          }),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-delete-1',
        method: 'kanban.ticket.delete',
        params: { projectId: project.id, id: 'ticket-1' }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-delete-1',
      ok: true,
      value: true
    })
    expect(calls).toEqual([{ projectId: project.id, id: 'ticket-1' }])
  })

  it('validates kanban.ticket.delete params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run for invalid params')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-delete-2',
        method: 'kanban.ticket.delete',
        params: { id: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-delete-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.archive through the kanban RPC domain', async () => {
    const ticket = {
      id: 'ticket-1',
      project_id: project.id,
      title: 'Archived migration',
      description: null,
      attachments: [],
      column: 'done' as const,
      sort_order: 0,
      current_session_id: null,
      worktree_id: null,
      mode: null,
      plan_ready: false,
      created_at: '2026-05-26T00:05:00.000Z',
      updated_at: '2026-05-26T00:06:00.000Z',
      column_changed_at: null,
      archived_at: '2026-05-26T00:06:00.000Z',
      external_provider: null,
      external_id: null,
      external_url: null,
      github_pr_number: null,
      github_pr_url: null,
      mark: null,
      total_tokens: 0,
      pending_launch_config: null,
      goal_mode: false,
      goal_success_criteria: null,
      note: null,
      created_from_session: false,
      auto_approve_plan: false,
      model_provider_id: null,
      model_id: null,
      model_variant: null,
      variant_group_id: null
    }
    const calls: Array<{ projectId: string; id: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: (projectId, id) =>
          Effect.sync(() => {
            calls.push({ projectId, id })
            return ticket
          }),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-archive-1',
        method: 'kanban.ticket.archive',
        params: { projectId: project.id, id: 'ticket-1' }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-archive-1',
      ok: true,
      value: ticket
    })
    expect(calls).toEqual([{ projectId: project.id, id: 'ticket-1' }])
  })

  it('validates kanban.ticket.archive params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () =>
          Effect.die(new Error('archiveTicket should not run for invalid params')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-archive-2',
        method: 'kanban.ticket.archive',
        params: { id: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-archive-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.archiveAllDone through the kanban RPC domain', async () => {
    const calls: Array<{ projectId: string; id: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: (projectId) =>
          Effect.sync(() => {
            calls.push(projectId)
            return 3
          }),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-archive-all-done-1',
        method: 'kanban.ticket.archiveAllDone',
        params: { projectId: project.id }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-archive-all-done-1',
      ok: true,
      value: 3
    })
    expect(calls).toEqual([project.id])
  })

  it('validates kanban.ticket.archiveAllDone params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () =>
          Effect.die(new Error('archiveAllDoneTickets should not run for invalid params')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-archive-all-done-2',
        method: 'kanban.ticket.archiveAllDone',
        params: { projectId: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-archive-all-done-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.unarchive through the kanban RPC domain', async () => {
    const ticket = {
      id: 'ticket-1',
      project_id: project.id,
      title: 'Unarchived migration',
      description: null,
      attachments: [],
      column: 'done' as const,
      sort_order: 0,
      current_session_id: null,
      worktree_id: null,
      mode: null,
      plan_ready: false,
      created_at: '2026-05-26T00:05:00.000Z',
      updated_at: '2026-05-26T00:07:00.000Z',
      column_changed_at: null,
      archived_at: null,
      external_provider: null,
      external_id: null,
      external_url: null,
      github_pr_number: null,
      github_pr_url: null,
      mark: null,
      total_tokens: 0,
      pending_launch_config: null,
      goal_mode: false,
      goal_success_criteria: null,
      note: null,
      created_from_session: false,
      auto_approve_plan: false,
      model_provider_id: null,
      model_id: null,
      model_variant: null,
      variant_group_id: null
    }
    const calls: Array<{ projectId: string; ticketId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: (projectId, id) =>
          Effect.sync(() => {
            calls.push({ projectId, id })
            return ticket
          }),
        moveTicket: () => Effect.die(new Error('moveTicket should not run'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-unarchive-1',
        method: 'kanban.ticket.unarchive',
        params: { projectId: project.id, id: 'ticket-1' }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-unarchive-1',
      ok: true,
      value: ticket
    })
    expect(calls).toEqual([{ projectId: project.id, id: 'ticket-1' }])
  })

  it('validates kanban.ticket.unarchive params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () =>
          Effect.die(new Error('unarchiveTicket should not run for invalid params')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-unarchive-2',
        method: 'kanban.ticket.unarchive',
        params: { id: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-unarchive-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.move through the kanban RPC domain', async () => {
    const ticket = {
      id: 'ticket-1',
      project_id: project.id,
      title: 'Moved migration',
      description: null,
      attachments: [],
      column: 'review' as const,
      sort_order: 42,
      current_session_id: null,
      worktree_id: null,
      mode: null,
      plan_ready: false,
      created_at: '2026-05-26T00:05:00.000Z',
      updated_at: '2026-05-26T00:08:00.000Z',
      column_changed_at: null,
      archived_at: null,
      external_provider: null,
      external_id: null,
      external_url: null,
      github_pr_number: null,
      github_pr_url: null,
      mark: null,
      total_tokens: 0,
      pending_launch_config: null,
      goal_mode: false,
      goal_success_criteria: null,
      note: null,
      created_from_session: false,
      auto_approve_plan: false,
      model_provider_id: null,
      model_id: null,
      model_variant: null,
      variant_group_id: null
    }
    const calls: Array<{ projectId: string; id: string; column: string; sortOrder: number }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: (projectId, id, column, sortOrder) =>
          Effect.sync(() => {
            calls.push({ projectId, id, column, sortOrder })
            return ticket
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-move-1',
        method: 'kanban.ticket.move',
        params: { projectId: project.id, id: 'ticket-1', column: 'review', sortOrder: 42 }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-move-1',
      ok: true,
      value: ticket
    })
    expect(calls).toEqual([
      { projectId: project.id, id: 'ticket-1', column: 'review', sortOrder: 42 }
    ])
  })

  it('validates kanban.ticket.move params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run for invalid params'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-move-2',
        method: 'kanban.ticket.move',
        params: { id: 'ticket-1', column: 'doing', sortOrder: 42 }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-move-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.moveToProject through the kanban RPC domain', async () => {
    const ticket = {
      id: 'ticket-1',
      project_id: 'project-2',
      title: 'Relocated migration',
      description: null,
      attachments: [],
      column: 'todo' as const,
      sort_order: 0,
      current_session_id: null,
      worktree_id: null,
      mode: null,
      plan_ready: false,
      created_at: '2026-05-26T00:05:00.000Z',
      updated_at: '2026-05-26T00:08:00.000Z',
      column_changed_at: null,
      archived_at: null,
      external_provider: null,
      external_id: null,
      external_url: null,
      github_pr_number: null,
      github_pr_url: null,
      mark: null,
      total_tokens: 0,
      pending_launch_config: null,
      goal_mode: false,
      goal_success_criteria: null,
      note: null
    }
    const calls: Array<{ projectId: string; id: string; targetProjectId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        moveTicketToProject: (projectId, id, targetProjectId) =>
          Effect.sync(() => {
            calls.push({ projectId, id, targetProjectId })
            return ticket
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-move-to-project-1',
        method: 'kanban.ticket.moveToProject',
        params: { projectId: 'project-1', id: 'ticket-1', targetProjectId: 'project-2' }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-move-to-project-1',
      ok: true,
      value: ticket
    })
    expect(calls).toEqual([
      { projectId: 'project-1', id: 'ticket-1', targetProjectId: 'project-2' }
    ])
  })

  it('validates kanban.ticket.moveToProject params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        moveTicketToProject: () =>
          Effect.die(new Error('moveTicketToProject should not run for invalid params'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-move-to-project-2',
        method: 'kanban.ticket.moveToProject',
        params: { id: 'ticket-1' }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-move-to-project-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.reorder through the kanban RPC domain', async () => {
    const calls: Array<{ projectId: string; id: string; sortOrder: number }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        reorderTicket: (projectId, id, sortOrder) =>
          Effect.sync(() => {
            calls.push({ projectId, id, sortOrder })
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-reorder-1',
        method: 'kanban.ticket.reorder',
        params: { projectId: project.id, id: 'ticket-1', sortOrder: 42 }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-reorder-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ projectId: project.id, id: 'ticket-1', sortOrder: 42 }])
  })

  it('validates kanban.ticket.reorder params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        reorderTicket: () =>
          Effect.die(new Error('reorderTicket should not run for invalid params'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-reorder-2',
        method: 'kanban.ticket.reorder',
        params: { id: 'ticket-1', sortOrder: 'last' }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-reorder-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.getBySession through the kanban RPC domain', async () => {
    const ticket = {
      id: 'ticket-1',
      project_id: project.id,
      title: 'Session migration',
      description: null,
      attachments: [],
      column: 'todo' as const,
      sort_order: 0,
      current_session_id: session.id,
      worktree_id: null,
      mode: null,
      plan_ready: false,
      created_at: '2026-05-26T00:05:00.000Z',
      updated_at: '2026-05-26T00:05:00.000Z',
      column_changed_at: null,
      archived_at: null,
      external_provider: null,
      external_id: null,
      external_url: null,
      github_pr_number: null,
      github_pr_url: null,
      mark: null,
      total_tokens: 0,
      pending_launch_config: null,
      goal_mode: false,
      goal_success_criteria: null,
      note: null,
      created_from_session: false,
      auto_approve_plan: false,
      model_provider_id: null,
      model_id: null,
      model_variant: null,
      variant_group_id: null
    }
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        getTicketsBySession: (sessionId) =>
          Effect.sync(() => {
            calls.push(sessionId)
            return [ticket]
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-get-by-session-1',
        method: 'kanban.ticket.getBySession',
        params: { sessionId: session.id }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-get-by-session-1',
      ok: true,
      value: [ticket]
    })
    expect(calls).toEqual([session.id])
  })

  it('validates kanban.ticket.getBySession params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        getTicketsBySession: () =>
          Effect.die(new Error('getTicketsBySession should not run for invalid params'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-get-by-session-2',
        method: 'kanban.ticket.getBySession',
        params: { sessionId: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-get-by-session-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.addTokens through the kanban RPC domain', async () => {
    const ticket = {
      id: 'ticket-1',
      project_id: project.id,
      title: 'Token accounting',
      description: null,
      attachments: [],
      column: 'todo' as const,
      sort_order: 0,
      current_session_id: null,
      worktree_id: null,
      mode: null,
      plan_ready: false,
      created_at: '2026-05-26T00:05:00.000Z',
      updated_at: '2026-05-26T00:06:00.000Z',
      column_changed_at: null,
      archived_at: null,
      external_provider: null,
      external_id: null,
      external_url: null,
      github_pr_number: null,
      github_pr_url: null,
      mark: null,
      total_tokens: 128,
      pending_launch_config: null,
      goal_mode: false,
      goal_success_criteria: null,
      note: null,
      created_from_session: false,
      auto_approve_plan: false,
      model_provider_id: null,
      model_id: null,
      model_variant: null,
      variant_group_id: null
    }
    const calls: Array<{ projectId: string; id: string; tokens: number }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        addTicketTokens: (projectId, id, tokens) =>
          Effect.sync(() => {
            calls.push({ projectId, id, tokens })
            return ticket
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-add-tokens-1',
        method: 'kanban.ticket.addTokens',
        params: { projectId: project.id, id: 'ticket-1', tokens: 128 }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-add-tokens-1',
      ok: true,
      value: ticket
    })
    expect(calls).toEqual([{ projectId: project.id, id: 'ticket-1', tokens: 128 }])
  })

  it('validates kanban.ticket.addTokens params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        addTicketTokens: () =>
          Effect.die(new Error('addTicketTokens should not run for invalid params'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-add-tokens-2',
        method: 'kanban.ticket.addTokens',
        params: { id: 'ticket-1', tokens: '128' }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-add-tokens-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.syncPR through the kanban RPC domain', async () => {
    const calls: Array<{ worktreeId: string; prNumber: number; prUrl: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        syncPrToTickets: (worktreeId, prNumber, prUrl) =>
          Effect.sync(() => {
            calls.push({ worktreeId, prNumber, prUrl })
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-sync-pr-1',
        method: 'kanban.ticket.syncPR',
        params: {
          worktreeId: worktree.id,
          prNumber: 123,
          prUrl: 'https://github.com/example/hive/pull/123'
        }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-sync-pr-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([
      {
        worktreeId: worktree.id,
        prNumber: 123,
        prUrl: 'https://github.com/example/hive/pull/123'
      }
    ])
  })

  it('validates kanban.ticket.syncPR params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        syncPrToTickets: () =>
          Effect.die(new Error('syncPrToTickets should not run for invalid params'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-sync-pr-2',
        method: 'kanban.ticket.syncPR',
        params: {
          worktreeId: worktree.id,
          prNumber: '123',
          prUrl: 'https://github.com/example/hive/pull/123'
        }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-sync-pr-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.clearPR through the kanban RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        clearPrFromTickets: (worktreeId) =>
          Effect.sync(() => {
            calls.push(worktreeId)
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-clear-pr-1',
        method: 'kanban.ticket.clearPR',
        params: { worktreeId: worktree.id }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-clear-pr-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([worktree.id])
  })

  it('validates kanban.ticket.clearPR params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        clearPrFromTickets: () =>
          Effect.die(new Error('clearPrFromTickets should not run for invalid params'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-clear-pr-2',
        method: 'kanban.ticket.clearPR',
        params: { worktreeId: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-clear-pr-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.attachPR through the kanban RPC domain', async () => {
    const calls: Array<{
      ticketId: string
      projectId: string
      prNumber: number
      prUrl: string
    }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        attachPrToTicket: (ticketId, projectId, prNumber, prUrl) =>
          Effect.sync(() => {
            calls.push({ ticketId, projectId, prNumber, prUrl })
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-attach-pr-1',
        method: 'kanban.ticket.attachPR',
        params: {
          ticketId: 'ticket-1',
          projectId: project.id,
          prNumber: 123,
          prUrl: 'https://github.com/example/hive/pull/123'
        }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-attach-pr-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([
      {
        ticketId: 'ticket-1',
        projectId: project.id,
        prNumber: 123,
        prUrl: 'https://github.com/example/hive/pull/123'
      }
    ])
  })

  it('validates kanban.ticket.attachPR params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        attachPrToTicket: () =>
          Effect.die(new Error('attachPrToTicket should not run for invalid params'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-attach-pr-2',
        method: 'kanban.ticket.attachPR',
        params: {
          ticketId: 'ticket-1',
          projectId: project.id,
          prNumber: '123',
          prUrl: 'https://github.com/example/hive/pull/123'
        }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-attach-pr-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.detachPR through the kanban RPC domain', async () => {
    const calls: Array<{ ticketId: string; projectId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        detachPrFromTicket: (ticketId, projectId) =>
          Effect.sync(() => {
            calls.push({ ticketId, projectId })
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-detach-pr-1',
        method: 'kanban.ticket.detachPR',
        params: {
          ticketId: 'ticket-1',
          projectId: project.id
        }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-detach-pr-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ ticketId: 'ticket-1', projectId: project.id }])
  })

  it('validates kanban.ticket.detachPR params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        detachPrFromTicket: () =>
          Effect.die(new Error('detachPrFromTicket should not run for invalid params'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-detach-pr-2',
        method: 'kanban.ticket.detachPR',
        params: {
          ticketId: 'ticket-1',
          projectId: 123
        }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-detach-pr-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.ticket.detachWorktree through the kanban RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        detachWorktreeFromTickets: (worktreeId) =>
          Effect.sync(() => {
            calls.push(worktreeId)
            return 2
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-detach-worktree-1',
        method: 'kanban.ticket.detachWorktree',
        params: {
          worktreeId: worktree.id
        }
      })
    )

    expect(response).toEqual({
      id: 'kanban-ticket-detach-worktree-1',
      ok: true,
      value: 2
    })
    expect(calls).toEqual([worktree.id])
  })

  it('validates kanban.ticket.detachWorktree params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        detachWorktreeFromTickets: () =>
          Effect.die(new Error('detachWorktreeFromTickets should not run for invalid params'))
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-ticket-detach-worktree-2',
        method: 'kanban.ticket.detachWorktree',
        params: {
          worktreeId: 123
        }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-ticket-detach-worktree-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles kanban.simpleMode.toggle through the kanban RPC domain', async () => {
    const calls: Array<{ projectId: string; enabled: boolean }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        updateProjectSimpleMode: (projectId, enabled) =>
          Effect.sync(() => {
            calls.push({ projectId, enabled })
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-simple-mode-toggle-1',
        method: 'kanban.simpleMode.toggle',
        params: {
          projectId: project.id,
          enabled: true
        }
      })
    )

    expect(response).toEqual({
      id: 'kanban-simple-mode-toggle-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ projectId: project.id, enabled: true }])
  })

  it('validates kanban.simpleMode.toggle params', async () => {
    const calls: Array<{ projectId: string; enabled: boolean }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        updateProjectSimpleMode: (projectId, enabled) =>
          Effect.sync(() => {
            calls.push({ projectId, enabled })
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-simple-mode-toggle-2',
        method: 'kanban.simpleMode.toggle',
        params: {
          projectId: project.id,
          enabled: 'true'
        }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-simple-mode-toggle-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles kanban.dependency.add through the kanban RPC domain', async () => {
    const calls: Array<{ projectId: string; dependentId: string; blockerId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        addTicketDependency: (projectId, dependentId, blockerId) =>
          Effect.sync(() => {
            calls.push({ projectId, dependentId, blockerId })
            return { success: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-dependency-add-1',
        method: 'kanban.dependency.add',
        params: {
          projectId: project.id,
          dependentId: 'ticket-1',
          blockerId: 'ticket-2'
        }
      })
    )

    expect(response).toEqual({
      id: 'kanban-dependency-add-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([
      { projectId: project.id, dependentId: 'ticket-1', blockerId: 'ticket-2' }
    ])
  })

  it('validates kanban.dependency.add params', async () => {
    const calls: Array<{ projectId: string; dependentId: string; blockerId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        addTicketDependency: (projectId, dependentId, blockerId) =>
          Effect.sync(() => {
            calls.push({ projectId, dependentId, blockerId })
            return { success: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-dependency-add-2',
        method: 'kanban.dependency.add',
        params: {
          projectId: project.id,
          dependentId: 'ticket-1',
          blockerId: 123
        }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-dependency-add-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles kanban.dependency.remove through the kanban RPC domain', async () => {
    const calls: Array<{ projectId: string; dependentId: string; blockerId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        removeTicketDependency: (projectId, dependentId, blockerId) =>
          Effect.sync(() => {
            calls.push({ projectId, dependentId, blockerId })
            return true
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-dependency-remove-1',
        method: 'kanban.dependency.remove',
        params: {
          projectId: project.id,
          dependentId: 'ticket-1',
          blockerId: 'ticket-2'
        }
      })
    )

    expect(response).toEqual({
      id: 'kanban-dependency-remove-1',
      ok: true,
      value: true
    })
    expect(calls).toEqual([
      { projectId: project.id, dependentId: 'ticket-1', blockerId: 'ticket-2' }
    ])
  })

  it('validates kanban.dependency.remove params', async () => {
    const calls: Array<{ projectId: string; dependentId: string; blockerId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        removeTicketDependency: (projectId, dependentId, blockerId) =>
          Effect.sync(() => {
            calls.push({ projectId, dependentId, blockerId })
            return true
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-dependency-remove-2',
        method: 'kanban.dependency.remove',
        params: {
          projectId: project.id,
          dependentId: 123,
          blockerId: 'ticket-2'
        }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-dependency-remove-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles kanban.dependency.getBlockers through the kanban RPC domain', async () => {
    const blockerTicket = {
      id: 'ticket-2',
      project_id: project.id,
      title: 'Blocking ticket',
      description: null,
      attachments: [],
      column: 'todo' as const,
      sort_order: 0,
      current_session_id: null,
      worktree_id: null,
      mode: null,
      plan_ready: false,
      created_at: '2026-05-26T00:05:00.000Z',
      updated_at: '2026-05-26T00:07:00.000Z',
      column_changed_at: null,
      archived_at: null,
      external_provider: null,
      external_id: null,
      external_url: null,
      github_pr_number: null,
      github_pr_url: null,
      mark: null,
      total_tokens: 0,
      pending_launch_config: null,
      goal_mode: false,
      goal_success_criteria: null,
      note: null,
      created_from_session: false,
      auto_approve_plan: false,
      model_provider_id: null,
      model_id: null,
      model_variant: null,
      variant_group_id: null
    }
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        getBlockersForTicket: (projectId, ticketId) =>
          Effect.sync(() => {
            calls.push({ projectId, ticketId })
            return [blockerTicket]
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-dependency-get-blockers-1',
        method: 'kanban.dependency.getBlockers',
        params: {
          projectId: project.id,
          id: 'ticket-1'
        }
      })
    )

    expect(response).toEqual({
      id: 'kanban-dependency-get-blockers-1',
      ok: true,
      value: [blockerTicket]
    })
    expect(calls).toEqual([{ projectId: project.id, ticketId: 'ticket-1' }])
  })

  it('validates kanban.dependency.getBlockers params', async () => {
    const calls: Array<{ projectId: string; ticketId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        getBlockersForTicket: (projectId, ticketId) =>
          Effect.sync(() => {
            calls.push({ projectId, ticketId })
            return []
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-dependency-get-blockers-2',
        method: 'kanban.dependency.getBlockers',
        params: {
          projectId: project.id,
          id: 123
        }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-dependency-get-blockers-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles kanban.dependency.getDependents through the kanban RPC domain', async () => {
    const dependentTicket = {
      id: 'ticket-1',
      project_id: project.id,
      title: 'Dependent ticket',
      description: null,
      attachments: [],
      column: 'review' as const,
      sort_order: 1,
      current_session_id: null,
      worktree_id: null,
      mode: null,
      plan_ready: false,
      created_at: '2026-05-26T00:05:00.000Z',
      updated_at: '2026-05-26T00:07:00.000Z',
      column_changed_at: null,
      archived_at: null,
      external_provider: null,
      external_id: null,
      external_url: null,
      github_pr_number: null,
      github_pr_url: null,
      mark: null,
      total_tokens: 0,
      pending_launch_config: null,
      goal_mode: false,
      goal_success_criteria: null,
      note: null,
      created_from_session: false,
      auto_approve_plan: false,
      model_provider_id: null,
      model_id: null,
      model_variant: null,
      variant_group_id: null
    }
    const calls: Array<{ projectId: string; ticketId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        getDependentsOfTicket: (projectId, ticketId) =>
          Effect.sync(() => {
            calls.push({ projectId, ticketId })
            return [dependentTicket]
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-dependency-get-dependents-1',
        method: 'kanban.dependency.getDependents',
        params: {
          projectId: project.id,
          id: 'ticket-2'
        }
      })
    )

    expect(response).toEqual({
      id: 'kanban-dependency-get-dependents-1',
      ok: true,
      value: [dependentTicket]
    })
    expect(calls).toEqual([{ projectId: project.id, ticketId: 'ticket-2' }])
  })

  it('validates kanban.dependency.getDependents params', async () => {
    const calls: Array<{ projectId: string; ticketId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        getDependentsOfTicket: (projectId, ticketId) =>
          Effect.sync(() => {
            calls.push({ projectId, ticketId })
            return []
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-dependency-get-dependents-2',
        method: 'kanban.dependency.getDependents',
        params: {
          projectId: project.id,
          id: 123
        }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-dependency-get-dependents-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles kanban.dependency.getForProject through the kanban RPC domain', async () => {
    const dependency = {
      dependent_id: 'ticket-1',
      blocker_id: 'ticket-2',
      created_at: '2026-05-26T00:09:00.000Z'
    }
    const calls: Array<{ projectId: string; ticketId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        getDependenciesForProject: (projectId) =>
          Effect.sync(() => {
            calls.push(projectId)
            return [dependency]
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-dependency-get-for-project-1',
        method: 'kanban.dependency.getForProject',
        params: {
          projectId: project.id
        }
      })
    )

    expect(response).toEqual({
      id: 'kanban-dependency-get-for-project-1',
      ok: true,
      value: [dependency]
    })
    expect(calls).toEqual([project.id])
  })

  it('validates kanban.dependency.getForProject params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        getDependenciesForProject: (projectId) =>
          Effect.sync(() => {
            calls.push(projectId)
            return []
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-dependency-get-for-project-2',
        method: 'kanban.dependency.getForProject',
        params: {
          projectId: 123
        }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-dependency-get-for-project-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles kanban.dependency.removeAll through the kanban RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        removeAllDependenciesForTicket: (projectId, ticketId) =>
          Effect.sync(() => {
            calls.push({ projectId, ticketId })
            return 2
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-dependency-remove-all-1',
        method: 'kanban.dependency.removeAll',
        params: {
          projectId: project.id,
          id: 'ticket-1'
        }
      })
    )

    expect(response).toEqual({
      id: 'kanban-dependency-remove-all-1',
      ok: true,
      value: 2
    })
    expect(calls).toEqual([{ projectId: project.id, ticketId: 'ticket-1' }])
  })

  it('validates kanban.dependency.removeAll params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        removeAllDependenciesForTicket: (ticketId) =>
          Effect.sync(() => {
            calls.push(ticketId)
            return 0
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-dependency-remove-all-2',
        method: 'kanban.dependency.removeAll',
        params: {
          id: 123
        }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-dependency-remove-all-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles kanban.board.export through the kanban RPC domain', async () => {
    const calls: Array<{ projectId: string; projectName: string }> = []
    const result = {
      success: true,
      ticketCount: 2,
      path: '/tmp/board-Hive.hive.json'
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        exportBoard: (projectId, projectName) =>
          Effect.sync(() => {
            calls.push({ projectId, projectName })
            return result
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-board-export-1',
        method: 'kanban.board.export',
        params: {
          projectId: project.id,
          projectName: project.name
        }
      })
    )

    expect(response).toEqual({
      id: 'kanban-board-export-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual([{ projectId: project.id, projectName: project.name }])
  })

  it('validates kanban.board.export params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        exportBoard: (projectId) =>
          Effect.sync(() => {
            calls.push(projectId)
            return { success: false, ticketCount: 0 }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-board-export-2',
        method: 'kanban.board.export',
        params: {
          projectId: project.id,
          projectName: 123
        }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-board-export-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles kanban.board.openImportFile through the kanban RPC domain', async () => {
    const calls: string[] = []
    const result = {
      tickets: [
        {
          id: 'ticket-1',
          title: 'Imported ticket',
          description: null,
          attachments: [],
          column: 'todo'
        }
      ],
      dependencies: [{ dependentId: 'ticket-1', blockerId: 'ticket-2' }],
      projectName: null
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        openBoardImportFile: () =>
          Effect.sync(() => {
            calls.push('open')
            return result
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-board-open-import-file-1',
        method: 'kanban.board.openImportFile',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'kanban-board-open-import-file-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual(['open'])
  })

  it('validates kanban.board.openImportFile params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        openBoardImportFile: () =>
          Effect.sync(() => {
            calls.push('open')
            return null
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-board-open-import-file-2',
        method: 'kanban.board.openImportFile',
        params: { path: '/tmp/board.json' }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-board-open-import-file-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('parses Hive board import files while filtering malformed dependencies', () => {
    const parsed = parseKanbanBoardImportFile(
      JSON.stringify({
        tickets: [{ id: 'ticket-1', title: 'Imported ticket' }],
        dependencies: [
          { dependentId: 'ticket-1', blockerId: 'ticket-2' },
          { dependentId: 'ticket-1' },
          null
        ]
      })
    )

    expect(parsed).toEqual({
      tickets: [{ id: 'ticket-1', title: 'Imported ticket' }],
      dependencies: [{ dependentId: 'ticket-1', blockerId: 'ticket-2' }],
      projectName: null
    })
  })

  it('handles kanban.board.importTickets through the kanban RPC domain', async () => {
    const calls: Array<{
      projectId: string
      tickets: Array<{
        id: string
        title: string
        description?: string | null
        attachments?: unknown[] | null
        column?: string
      }>
      dependencies?: Array<{ dependentId: string; blockerId: string }>
    }> = []
    const tickets = [
      {
        id: 'ticket-1',
        title: 'Imported ticket',
        description: null,
        attachments: [{ name: 'note.txt' }],
        column: 'todo'
      }
    ]
    const dependencies = [{ dependentId: 'ticket-1', blockerId: 'ticket-2' }]
    const result = {
      created: 1,
      updated: 0,
      dependencyCount: 0,
      ignoredDependencyCount: 1
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        importBoardTickets: (projectId, tickets, dependencies) =>
          Effect.sync(() => {
            calls.push({ projectId, tickets, dependencies })
            return result
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-board-import-tickets-1',
        method: 'kanban.board.importTickets',
        params: {
          projectId: project.id,
          tickets,
          dependencies
        }
      })
    )

    expect(response).toEqual({
      id: 'kanban-board-import-tickets-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual([{ projectId: project.id, tickets, dependencies }])
  })

  it('validates kanban.board.importTickets params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      kanban: {
        createTicket: () => Effect.die(new Error('createTicket should not run')),
        createTicketBatch: () => Effect.die(new Error('createTicketBatch should not run')),
        getTicket: () => Effect.die(new Error('getTicket should not run')),
        getTicketsByProject: () => Effect.die(new Error('getTicketsByProject should not run')),
        updateTicket: () => Effect.die(new Error('updateTicket should not run')),
        deleteTicket: () => Effect.die(new Error('deleteTicket should not run')),
        archiveTicket: () => Effect.die(new Error('archiveTicket should not run')),
        archiveAllDoneTickets: () => Effect.die(new Error('archiveAllDoneTickets should not run')),
        unarchiveTicket: () => Effect.die(new Error('unarchiveTicket should not run')),
        moveTicket: () => Effect.die(new Error('moveTicket should not run')),
        importBoardTickets: (projectId) =>
          Effect.sync(() => {
            calls.push(projectId)
            return {
              created: 0,
              updated: 0,
              dependencyCount: 0,
              ignoredDependencyCount: 0
            }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'kanban-board-import-tickets-2',
        method: 'kanban.board.importTickets',
        params: {
          projectId: project.id,
          tickets: [{ id: 'ticket-1', title: 123 }]
        }
      })
    )

    expect(response).toMatchObject({
      id: 'kanban-board-import-tickets-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles loggingOps.createResponseLog through the logging ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      loggingOps: {
        createResponseLog: (sessionId) =>
          Effect.sync(() => {
            calls.push(sessionId)
            return `/tmp/hive/${sessionId}.jsonl`
          }),
        appendResponseLog: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'logging-ops-create-response-log-1',
        method: 'loggingOps.createResponseLog',
        params: { sessionId: 'session-1' }
      })
    )

    expect(response).toEqual({
      id: 'logging-ops-create-response-log-1',
      ok: true,
      value: '/tmp/hive/session-1.jsonl'
    })
    expect(calls).toEqual(['session-1'])
  })

  it('validates loggingOps.createResponseLog params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      loggingOps: {
        createResponseLog: () => Effect.succeed('/tmp/hive/session-1.jsonl'),
        appendResponseLog: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'logging-ops-create-response-log-2',
        method: 'loggingOps.createResponseLog',
        params: { sessionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'logging-ops-create-response-log-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles loggingOps.appendResponseLog through the logging ops RPC domain', async () => {
    const calls: Array<{ filePath: string; data: unknown }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      loggingOps: {
        createResponseLog: () => Effect.succeed('/tmp/hive/session-1.jsonl'),
        appendResponseLog: (filePath, data) =>
          Effect.sync(() => {
            calls.push({ filePath, data })
          })
      }
    })

    const data = { type: 'assistant_delta', text: 'hello' }
    const response = await Effect.runPromise(
      router.handle({
        id: 'logging-ops-append-response-log-1',
        method: 'loggingOps.appendResponseLog',
        params: { filePath: '/tmp/hive/session-1.jsonl', data }
      })
    )

    expect(response).toEqual({
      id: 'logging-ops-append-response-log-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ filePath: '/tmp/hive/session-1.jsonl', data }])
  })

  it('validates loggingOps.appendResponseLog params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      loggingOps: {
        createResponseLog: () => Effect.succeed('/tmp/hive/session-1.jsonl'),
        appendResponseLog: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'logging-ops-append-response-log-2',
        method: 'loggingOps.appendResponseLog',
        params: { filePath: '', data: { type: 'assistant_delta' } }
      })
    )

    expect(response).toMatchObject({
      id: 'logging-ops-append-response-log-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles fileOps.readFile through the file ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileOps: {
        readFile: (filePath) =>
          Effect.sync(() => {
            calls.push(filePath)
            return { success: true, content: 'hello from http' }
          }),
        writeFile: () => Effect.succeed(null),
        createFile: () => Effect.succeed(null),
        readImageAsBase64: () => Effect.succeed({ data: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-ops-read-file-1',
        method: 'fileOps.readFile',
        params: { filePath: '/tmp/hive/README.md' }
      })
    )

    expect(response).toEqual({
      id: 'file-ops-read-file-1',
      ok: true,
      value: { success: true, content: 'hello from http' }
    })
    expect(calls).toEqual(['/tmp/hive/README.md'])
  })

  it('validates fileOps.readFile params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileOps: {
        readFile: () => Effect.succeed({ success: false, error: 'should not run' }),
        writeFile: () => Effect.succeed(null),
        createFile: () => Effect.succeed(null),
        readImageAsBase64: () => Effect.succeed({ data: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-ops-read-file-2',
        method: 'fileOps.readFile',
        params: { filePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'file-ops-read-file-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles fileOps.writeFile through the file ops RPC domain', async () => {
    const calls: Array<{ filePath: string; content: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileOps: {
        readFile: () => Effect.succeed({ success: false, error: 'unused' }),
        writeFile: (filePath, content) =>
          Effect.sync(() => {
            calls.push({ filePath, content })
            return null
          }),
        createFile: () => Effect.succeed(null),
        readImageAsBase64: () => Effect.succeed({ data: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-ops-write-file-1',
        method: 'fileOps.writeFile',
        params: { filePath: '/tmp/hive/README.md', content: 'hello from http' }
      })
    )

    expect(response).toEqual({
      id: 'file-ops-write-file-1',
      ok: true,
      value: null
    })
    expect(calls).toEqual([{ filePath: '/tmp/hive/README.md', content: 'hello from http' }])
  })

  it('validates fileOps.writeFile params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileOps: {
        readFile: () => Effect.succeed({ success: false, error: 'unused' }),
        writeFile: () => Effect.succeed(null),
        createFile: () => Effect.succeed(null),
        readImageAsBase64: () => Effect.succeed({ data: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-ops-write-file-2',
        method: 'fileOps.writeFile',
        params: { filePath: '/tmp/hive/README.md', content: 42 }
      })
    )

    expect(response).toMatchObject({
      id: 'file-ops-write-file-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles fileOps.createFile through the file ops RPC domain', async () => {
    const calls: Array<{
      directoryPath: string
      fileName: string
      content: string
      overwrite: boolean
    }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileOps: {
        readFile: () => Effect.succeed({ success: false, error: 'unused' }),
        writeFile: () => Effect.succeed(null),
        createFile: (directoryPath, fileName, content, overwrite) =>
          Effect.sync(() => {
            calls.push({ directoryPath, fileName, content, overwrite })
            return null
          }),
        readImageAsBase64: () => Effect.succeed({ data: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-ops-create-file-1',
        method: 'fileOps.createFile',
        params: {
          directoryPath: '/tmp/hive',
          fileName: 'PLAN_feature.md',
          content: '# Plan',
          overwrite: false
        }
      })
    )

    expect(response).toEqual({
      id: 'file-ops-create-file-1',
      ok: true,
      value: null
    })
    expect(calls).toEqual([
      {
        directoryPath: '/tmp/hive',
        fileName: 'PLAN_feature.md',
        content: '# Plan',
        overwrite: false
      }
    ])
  })

  it('validates fileOps.createFile params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileOps: {
        readFile: () => Effect.succeed({ success: false, error: 'unused' }),
        writeFile: () => Effect.succeed(null),
        createFile: () => Effect.succeed(null),
        readImageAsBase64: () => Effect.succeed({ data: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-ops-create-file-2',
        method: 'fileOps.createFile',
        params: { directoryPath: '/tmp/hive', fileName: 'PLAN_feature.md', content: '# Plan' }
      })
    )

    expect(response).toMatchObject({
      id: 'file-ops-create-file-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles fileOps.readImageAsBase64 through the file ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileOps: {
        readFile: () => Effect.succeed({ success: false, error: 'unused' }),
        writeFile: () => Effect.succeed(null),
        createFile: () => Effect.succeed(null),
        readImageAsBase64: (filePath) =>
          Effect.sync(() => {
            calls.push(filePath)
            return { data: 'aGVsbG8=', mimeType: 'image/png' }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-ops-read-image-1',
        method: 'fileOps.readImageAsBase64',
        params: { filePath: '/tmp/hive/icon.png' }
      })
    )

    expect(response).toEqual({
      id: 'file-ops-read-image-1',
      ok: true,
      value: { data: 'aGVsbG8=', mimeType: 'image/png' }
    })
    expect(calls).toEqual(['/tmp/hive/icon.png'])
  })

  it('validates fileOps.readImageAsBase64 params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileOps: {
        readFile: () => Effect.succeed({ success: false, error: 'unused' }),
        writeFile: () => Effect.succeed(null),
        createFile: () => Effect.succeed(null),
        readImageAsBase64: () => Effect.succeed({ data: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-ops-read-image-2',
        method: 'fileOps.readImageAsBase64',
        params: { filePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'file-ops-read-image-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles attachmentOps.deleteImage through the attachment ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      attachmentOps: {
        saveImage: () => Effect.succeed({ success: false, error: 'unused' }),
        deleteImage: (filePath) =>
          Effect.sync(() => {
            calls.push(filePath)
            return { success: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'attachment-ops-delete-image-1',
        method: 'attachmentOps.deleteImage',
        params: { filePath: '/tmp/hive/.hive/attachments/image.png' }
      })
    )

    expect(response).toEqual({
      id: 'attachment-ops-delete-image-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['/tmp/hive/.hive/attachments/image.png'])
  })

  it('handles attachmentOps.saveImage through the attachment ops RPC domain', async () => {
    const calls: Array<{ dataBase64: string; originalName: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      attachmentOps: {
        saveImage: (dataBase64, originalName) =>
          Effect.sync(() => {
            calls.push({ dataBase64, originalName })
            return { success: true, filePath: '/tmp/hive/.hive/attachments/image.png' }
          }),
        deleteImage: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'attachment-ops-save-image-1',
        method: 'attachmentOps.saveImage',
        params: { dataBase64: 'aGVsbG8=', originalName: 'image.png' }
      })
    )

    expect(response).toEqual({
      id: 'attachment-ops-save-image-1',
      ok: true,
      value: { success: true, filePath: '/tmp/hive/.hive/attachments/image.png' }
    })
    expect(calls).toEqual([{ dataBase64: 'aGVsbG8=', originalName: 'image.png' }])
  })

  it('validates attachmentOps.saveImage params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      attachmentOps: {
        saveImage: () => Effect.succeed({ success: false, error: 'should not run' }),
        deleteImage: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'attachment-ops-save-image-2',
        method: 'attachmentOps.saveImage',
        params: { dataBase64: '', originalName: 'image.png' }
      })
    )

    expect(response).toMatchObject({
      id: 'attachment-ops-save-image-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('validates attachmentOps.deleteImage params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      attachmentOps: {
        saveImage: () => Effect.succeed({ success: false, error: 'unused' }),
        deleteImage: () => Effect.succeed({ success: false, error: 'should not run' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'attachment-ops-delete-image-2',
        method: 'attachmentOps.deleteImage',
        params: { filePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'attachment-ops-delete-image-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles connectionOps.create through the connection ops RPC domain', async () => {
    const connection = {
      id: 'connection-1',
      name: 'Hive',
      custom_name: null,
      status: 'active' as const,
      path: '/tmp/hive-connection',
      color: null,
      pinned: 0,
      created_at: '2026-05-26T00:00:00.000Z',
      updated_at: '2026-05-26T00:00:00.000Z',
      members: [
        {
          id: 'member-1',
          connection_id: 'connection-1',
          worktree_id: 'worktree-1',
          project_id: 'project-1',
          symlink_name: 'hive',
          added_at: '2026-05-26T00:00:00.000Z',
          worktree_name: 'main',
          worktree_branch: 'main',
          worktree_path: '/tmp/hive',
          project_name: 'Hive'
        }
      ]
    }
    const calls: string[][] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: (worktreeIds) =>
          Effect.sync(() => {
            calls.push(worktreeIds)
            return { success: true, connection }
          }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-create-1',
        method: 'connectionOps.create',
        params: { worktreeIds: ['worktree-1', 'worktree-2'] }
      })
    )

    expect(response).toEqual({
      id: 'connection-ops-create-1',
      ok: true,
      value: { success: true, connection }
    })
    expect(calls).toEqual([['worktree-1', 'worktree-2']])
  })

  it('validates connectionOps.create params', async () => {
    const calls: string[][] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: (worktreeIds) =>
          Effect.sync(() => {
            calls.push(worktreeIds)
            return { success: true }
          }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-create-2',
        method: 'connectionOps.create',
        params: { worktreeIds: [''] }
      })
    )

    expect(response).toMatchObject({
      id: 'connection-ops-create-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles connectionOps.addMember through the connection ops RPC domain', async () => {
    const member = {
      id: 'member-1',
      connection_id: 'connection-1',
      worktree_id: 'worktree-1',
      project_id: 'project-1',
      symlink_name: 'hive',
      added_at: '2026-05-26T00:00:00.000Z',
      worktree_name: 'main',
      worktree_branch: 'main',
      worktree_path: '/tmp/hive',
      project_name: 'Hive'
    }
    const calls: Array<{ connectionId: string; worktreeId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: (connectionId, worktreeId) =>
          Effect.sync(() => {
            calls.push({ connectionId, worktreeId })
            return { success: true, member }
          }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-add-member-1',
        method: 'connectionOps.addMember',
        params: { connectionId: 'connection-1', worktreeId: 'worktree-1' }
      })
    )

    expect(response).toEqual({
      id: 'connection-ops-add-member-1',
      ok: true,
      value: { success: true, member }
    })
    expect(calls).toEqual([{ connectionId: 'connection-1', worktreeId: 'worktree-1' }])
  })

  it('validates connectionOps.addMember params', async () => {
    const calls: Array<{ connectionId: string; worktreeId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: (connectionId, worktreeId) =>
          Effect.sync(() => {
            calls.push({ connectionId, worktreeId })
            return { success: true }
          }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-add-member-2',
        method: 'connectionOps.addMember',
        params: { connectionId: 'connection-1', worktreeId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'connection-ops-add-member-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles connectionOps.removeMember through the connection ops RPC domain', async () => {
    const calls: Array<{ connectionId: string; worktreeId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: (connectionId, worktreeId) =>
          Effect.sync(() => {
            calls.push({ connectionId, worktreeId })
            return { success: true, connectionDeleted: false }
          }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-remove-member-1',
        method: 'connectionOps.removeMember',
        params: { connectionId: 'connection-1', worktreeId: 'worktree-1' }
      })
    )

    expect(response).toEqual({
      id: 'connection-ops-remove-member-1',
      ok: true,
      value: { success: true, connectionDeleted: false }
    })
    expect(calls).toEqual([{ connectionId: 'connection-1', worktreeId: 'worktree-1' }])
  })

  it('validates connectionOps.removeMember params', async () => {
    const calls: Array<{ connectionId: string; worktreeId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: (connectionId, worktreeId) =>
          Effect.sync(() => {
            calls.push({ connectionId, worktreeId })
            return { success: true }
          }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-remove-member-2',
        method: 'connectionOps.removeMember',
        params: { connectionId: '', worktreeId: 'worktree-1' }
      })
    )

    expect(response).toMatchObject({
      id: 'connection-ops-remove-member-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles connectionOps.openInTerminal through the connection ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: (connectionPath) =>
          Effect.sync(() => {
            calls.push(connectionPath)
            return { success: true }
          }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-open-in-terminal-1',
        method: 'connectionOps.openInTerminal',
        params: { connectionPath: '/tmp/hive-connection' }
      })
    )

    expect(response).toEqual({
      id: 'connection-ops-open-in-terminal-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['/tmp/hive-connection'])
  })

  it('validates connectionOps.openInTerminal params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: (connectionPath) =>
          Effect.sync(() => {
            calls.push(connectionPath)
            return { success: true }
          }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-open-in-terminal-2',
        method: 'connectionOps.openInTerminal',
        params: { connectionPath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'connection-ops-open-in-terminal-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles connectionOps.openInEditor through the connection ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: (connectionPath) =>
          Effect.sync(() => {
            calls.push(connectionPath)
            return { success: true }
          }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-open-in-editor-1',
        method: 'connectionOps.openInEditor',
        params: { connectionPath: '/tmp/hive-connection' }
      })
    )

    expect(response).toEqual({
      id: 'connection-ops-open-in-editor-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['/tmp/hive-connection'])
  })

  it('validates connectionOps.openInEditor params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: (connectionPath) =>
          Effect.sync(() => {
            calls.push(connectionPath)
            return { success: true }
          }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-open-in-editor-2',
        method: 'connectionOps.openInEditor',
        params: { connectionPath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'connection-ops-open-in-editor-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles connectionOps.removeWorktreeFromAll through the connection ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: (worktreeId) =>
          Effect.sync(() => {
            calls.push(worktreeId)
            return { success: true }
          }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-remove-worktree-from-all-1',
        method: 'connectionOps.removeWorktreeFromAll',
        params: { worktreeId: 'worktree-1' }
      })
    )

    expect(response).toEqual({
      id: 'connection-ops-remove-worktree-from-all-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['worktree-1'])
  })

  it('validates connectionOps.removeWorktreeFromAll params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: (worktreeId) =>
          Effect.sync(() => {
            calls.push(worktreeId)
            return { success: true }
          }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-remove-worktree-from-all-2',
        method: 'connectionOps.removeWorktreeFromAll',
        params: { worktreeId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'connection-ops-remove-worktree-from-all-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles connectionOps.rename through the connection ops RPC domain', async () => {
    const connection = {
      id: 'connection-1',
      name: 'Hive',
      custom_name: 'Renamed',
      status: 'active' as const,
      path: '/tmp/hive-connection',
      color: null,
      pinned: 0,
      created_at: '2026-05-26T00:00:00.000Z',
      updated_at: '2026-05-26T00:00:00.000Z',
      members: []
    }
    const calls: Array<{ connectionId: string; customName: string | null }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: (connectionId, customName) =>
          Effect.sync(() => {
            calls.push({ connectionId, customName })
            return { success: true, connection }
          }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-rename-1',
        method: 'connectionOps.rename',
        params: { connectionId: 'connection-1', customName: 'Renamed' }
      })
    )

    expect(response).toEqual({
      id: 'connection-ops-rename-1',
      ok: true,
      value: { success: true, connection }
    })
    expect(calls).toEqual([{ connectionId: 'connection-1', customName: 'Renamed' }])
  })

  it('validates connectionOps.rename params', async () => {
    const calls: Array<{ connectionId: string; customName: string | null }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: (connectionId, customName) =>
          Effect.sync(() => {
            calls.push({ connectionId, customName })
            return { success: true }
          }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-rename-2',
        method: 'connectionOps.rename',
        params: { connectionId: '', customName: 'Renamed' }
      })
    )

    expect(response).toMatchObject({
      id: 'connection-ops-rename-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles connectionOps.setPinned through the connection ops RPC domain', async () => {
    const calls: Array<{ connectionId: string; pinned: boolean }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: (connectionId, pinned) =>
          Effect.sync(() => {
            calls.push({ connectionId, pinned })
            return { success: true }
          }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-set-pinned-1',
        method: 'connectionOps.setPinned',
        params: { connectionId: 'connection-1', pinned: true }
      })
    )

    expect(response).toEqual({
      id: 'connection-ops-set-pinned-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([{ connectionId: 'connection-1', pinned: true }])
  })

  it('validates connectionOps.setPinned params', async () => {
    const calls: Array<{ connectionId: string; pinned: boolean }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: (connectionId, pinned) =>
          Effect.sync(() => {
            calls.push({ connectionId, pinned })
            return { success: true }
          }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-set-pinned-2',
        method: 'connectionOps.setPinned',
        params: { connectionId: 'connection-1', pinned: 'true' }
      })
    )

    expect(response).toMatchObject({
      id: 'connection-ops-set-pinned-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles connectionOps.getPinned through the connection ops RPC domain', async () => {
    const connections = [
      {
        id: 'connection-1',
        name: 'Hive',
        custom_name: null,
        status: 'active' as const,
        path: '/tmp/hive-connection',
        color: null,
        pinned: 1,
        created_at: '2026-05-26T00:00:00.000Z',
        updated_at: '2026-05-26T00:00:00.000Z',
        members: [
          {
            id: 'member-1',
            connection_id: 'connection-1',
            worktree_id: 'worktree-1',
            project_id: 'project-1',
            symlink_name: 'hive',
            added_at: '2026-05-26T00:00:00.000Z',
            worktree_name: 'main',
            worktree_branch: 'main',
            worktree_path: '/tmp/hive',
            project_name: 'Hive'
          }
        ]
      }
    ]
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () =>
          Effect.sync(() => {
            calls.push('getPinned')
            return connections
          }),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-get-pinned-1',
        method: 'connectionOps.getPinned',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'connection-ops-get-pinned-1',
      ok: true,
      value: connections
    })
    expect(calls).toEqual(['getPinned'])
  })

  it('validates connectionOps.getPinned params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () =>
          Effect.sync(() => {
            calls.push('getPinned')
            return []
          }),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-get-pinned-2',
        method: 'connectionOps.getPinned',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'connection-ops-get-pinned-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles connectionOps.getAll through the connection ops RPC domain', async () => {
    const connections = [
      {
        id: 'connection-1',
        name: 'Hive',
        custom_name: null,
        status: 'active' as const,
        path: '/tmp/hive-connection',
        color: null,
        pinned: 0,
        created_at: '2026-05-26T00:00:00.000Z',
        updated_at: '2026-05-26T00:00:00.000Z',
        members: [
          {
            id: 'member-1',
            connection_id: 'connection-1',
            worktree_id: 'worktree-1',
            project_id: 'project-1',
            symlink_name: 'hive',
            added_at: '2026-05-26T00:00:00.000Z',
            worktree_name: 'main',
            worktree_branch: 'main',
            worktree_path: '/tmp/hive',
            project_name: 'Hive'
          }
        ]
      }
    ]
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () =>
          Effect.sync(() => {
            calls.push('getAll')
            return { success: true, connections }
          }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-get-all-1',
        method: 'connectionOps.getAll',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'connection-ops-get-all-1',
      ok: true,
      value: { success: true, connections }
    })
    expect(calls).toEqual(['getAll'])
  })

  it('validates connectionOps.getAll params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-get-all-2',
        method: 'connectionOps.getAll',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'connection-ops-get-all-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles connectionOps.get through the connection ops RPC domain', async () => {
    const connection = {
      id: 'connection-1',
      name: 'Hive',
      custom_name: null,
      status: 'active' as const,
      path: '/tmp/hive-connection',
      color: null,
      pinned: 0,
      created_at: '2026-05-26T00:00:00.000Z',
      updated_at: '2026-05-26T00:00:00.000Z',
      members: [
        {
          id: 'member-1',
          connection_id: 'connection-1',
          worktree_id: 'worktree-1',
          project_id: 'project-1',
          symlink_name: 'hive',
          added_at: '2026-05-26T00:00:00.000Z',
          worktree_name: 'main',
          worktree_branch: 'main',
          worktree_path: '/tmp/hive',
          project_name: 'Hive'
        }
      ]
    }
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: (connectionId) =>
          Effect.sync(() => {
            calls.push(connectionId)
            return { success: true, connection }
          }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-get-1',
        method: 'connectionOps.get',
        params: { connectionId: 'connection-1' }
      })
    )

    expect(response).toEqual({
      id: 'connection-ops-get-1',
      ok: true,
      value: { success: true, connection }
    })
    expect(calls).toEqual(['connection-1'])
  })

  it('validates connectionOps.get params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'should not run' }),
        delete: () => Effect.succeed({ success: false, error: 'unused' }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-get-2',
        method: 'connectionOps.get',
        params: { connectionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'connection-ops-get-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles connectionOps.delete through the connection ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: (connectionId) =>
          Effect.sync(() => {
            calls.push(connectionId)
            return { success: true }
          }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-delete-1',
        method: 'connectionOps.delete',
        params: { connectionId: 'connection-1' }
      })
    )

    expect(response).toEqual({
      id: 'connection-ops-delete-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['connection-1'])
  })

  it('validates connectionOps.delete params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      connectionOps: {
        create: () => Effect.succeed({ success: false, error: 'unused' }),
        addMember: () => Effect.succeed({ success: false, error: 'unused' }),
        removeMember: () => Effect.succeed({ success: false, error: 'unused' }),
        openInTerminal: () => Effect.succeed({ success: false, error: 'unused' }),
        openInEditor: () => Effect.succeed({ success: false, error: 'unused' }),
        removeWorktreeFromAll: () => Effect.succeed({ success: false, error: 'unused' }),
        rename: () => Effect.succeed({ success: false, error: 'unused' }),
        setPinned: () => Effect.succeed({ success: false, error: 'unused' }),
        getPinned: () => Effect.succeed([]),
        getAll: () => Effect.succeed({ success: true, connections: [] }),
        get: () => Effect.succeed({ success: false, error: 'unused' }),
        delete: (connectionId) =>
          Effect.sync(() => {
            calls.push(connectionId)
            return { success: true }
          }),
        updateMembers: () => Effect.succeed({ success: false, error: 'unused' }),
        getRecentConnections: () => Effect.succeed({ success: false, error: 'unused' }),
        setRecentConnectionNote: () => Effect.succeed({ success: false, error: 'unused' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'connection-ops-delete-2',
        method: 'connectionOps.delete',
        params: { connectionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'connection-ops-delete-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles usageOps.fetch through the usage ops RPC domain', async () => {
    const result = {
      success: true,
      data: {
        five_hour: { utilization: 42, resets_at: '2026-05-26T05:00:00.000Z' },
        seven_day: { utilization: 17, resets_at: '2026-06-02T00:00:00.000Z' },
        extra_usage: {
          is_enabled: true,
          utilization: 5,
          used_credits: 1,
          monthly_limit: 20
        }
      }
    }
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      usageOps: {
        fetchOpenai: () => Effect.succeed({ success: false, error: 'unused' }),
        fetchForAccount: () => Effect.succeed({ success: false, status: 'error' }),
        refreshAllForProvider: () => Effect.succeed([]),
        fetch: () =>
          Effect.sync(() => {
            calls.push('fetch')
            return result
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'usage-ops-fetch-1',
        method: 'usageOps.fetch',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'usage-ops-fetch-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual(['fetch'])
  })

  it('validates usageOps.fetch params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      usageOps: {
        fetchOpenai: () => Effect.succeed({ success: false, error: 'unused' }),
        fetchForAccount: () => Effect.succeed({ success: false, status: 'error' }),
        refreshAllForProvider: () => Effect.succeed([]),
        fetch: () =>
          Effect.sync(() => {
            calls.push('fetch')
            return { success: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'usage-ops-fetch-2',
        method: 'usageOps.fetch',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'usage-ops-fetch-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles usageOps.fetchOpenai through the usage ops RPC domain', async () => {
    const result = {
      success: true,
      data: {
        plan_type: 'pro',
        rate_limit: {
          primary_window: {
            used_percent: 25,
            limit_window_seconds: 3600,
            reset_after_seconds: 600,
            reset_at: 1770000000
          },
          secondary_window: null
        },
        credits: {
          has_credits: true,
          unlimited: false,
          balance: '12.34'
        }
      }
    }
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      usageOps: {
        fetch: () => Effect.succeed({ success: false, error: 'unused' }),
        fetchForAccount: () => Effect.succeed({ success: false, status: 'error' }),
        refreshAllForProvider: () => Effect.succeed([]),
        fetchOpenai: () =>
          Effect.sync(() => {
            calls.push('fetchOpenai')
            return result
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'usage-ops-fetch-openai-1',
        method: 'usageOps.fetchOpenai',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'usage-ops-fetch-openai-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual(['fetchOpenai'])
  })

  it('validates usageOps.fetchOpenai params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      usageOps: {
        fetch: () => Effect.succeed({ success: false, error: 'unused' }),
        fetchForAccount: () => Effect.succeed({ success: false, status: 'error' }),
        refreshAllForProvider: () => Effect.succeed([]),
        fetchOpenai: () =>
          Effect.sync(() => {
            calls.push('fetchOpenai')
            return { success: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'usage-ops-fetch-openai-2',
        method: 'usageOps.fetchOpenai',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'usage-ops-fetch-openai-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles usageOps.fetchForAccount through the usage ops RPC domain', async () => {
    const result = {
      success: true,
      data: {
        five_hour: { utilization: 42, resets_at: '2026-05-26T05:00:00.000Z' },
        seven_day: { utilization: 17, resets_at: '2026-06-02T00:00:00.000Z' }
      },
      status: 'ok' as const
    }
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      usageOps: {
        fetch: () => Effect.succeed({ success: false, error: 'unused' }),
        fetchOpenai: () => Effect.succeed({ success: false, error: 'unused' }),
        fetchForAccount: (accountId) =>
          Effect.sync(() => {
            calls.push(accountId)
            return result
          }),
        refreshAllForProvider: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'usage-ops-fetch-for-account-1',
        method: 'usageOps.fetchForAccount',
        params: { accountId: 'account-1' }
      })
    )

    expect(response).toEqual({
      id: 'usage-ops-fetch-for-account-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual(['account-1'])
  })

  it('validates usageOps.fetchForAccount params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      usageOps: {
        fetch: () => Effect.succeed({ success: false, error: 'unused' }),
        fetchOpenai: () => Effect.succeed({ success: false, error: 'unused' }),
        fetchForAccount: (accountId) =>
          Effect.sync(() => {
            calls.push(accountId)
            return { success: true, status: 'ok' as const }
          }),
        refreshAllForProvider: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'usage-ops-fetch-for-account-2',
        method: 'usageOps.fetchForAccount',
        params: { accountId: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'usage-ops-fetch-for-account-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('passes usageOps.fetchForAccount userInitiated through to the usage ops RPC domain', async () => {
    const calls: Array<{ accountId: string; userInitiated: boolean | undefined }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      usageOps: {
        fetch: () => Effect.succeed({ success: false, error: 'unused' }),
        fetchOpenai: () => Effect.succeed({ success: false, error: 'unused' }),
        fetchForAccount: (accountId, userInitiated) =>
          Effect.sync(() => {
            calls.push({ accountId, userInitiated })
            return { success: true, status: 'ok' as const }
          }),
        refreshAllForProvider: () => Effect.succeed([])
      }
    })

    await Effect.runPromise(
      router.handle({
        id: 'usage-ops-fetch-for-account-3',
        method: 'usageOps.fetchForAccount',
        params: { accountId: 'account-1', userInitiated: true }
      })
    )

    expect(calls).toEqual([{ accountId: 'account-1', userInitiated: true }])
  })

  it('handles usageOps.refreshAllForProvider through the usage ops RPC domain', async () => {
    const result = [
      { accountId: 'account-1', success: true },
      { accountId: 'account-2', success: false, error: 'stale token', retryAfter: 60 }
    ]
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      usageOps: {
        fetch: () => Effect.succeed({ success: false, error: 'unused' }),
        fetchOpenai: () => Effect.succeed({ success: false, error: 'unused' }),
        fetchForAccount: () => Effect.succeed({ success: false, status: 'error' }),
        refreshAllForProvider: (provider) =>
          Effect.sync(() => {
            calls.push(provider)
            return result
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'usage-ops-refresh-all-for-provider-1',
        method: 'usageOps.refreshAllForProvider',
        params: { provider: 'openai' }
      })
    )

    expect(response).toEqual({
      id: 'usage-ops-refresh-all-for-provider-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual(['openai'])
  })

  it('validates usageOps.refreshAllForProvider params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      usageOps: {
        fetch: () => Effect.succeed({ success: false, error: 'unused' }),
        fetchOpenai: () => Effect.succeed({ success: false, error: 'unused' }),
        fetchForAccount: () => Effect.succeed({ success: false, status: 'error' }),
        refreshAllForProvider: (provider) =>
          Effect.sync(() => {
            calls.push(provider)
            return []
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'usage-ops-refresh-all-for-provider-2',
        method: 'usageOps.refreshAllForProvider',
        params: { provider: 'other' }
      })
    )

    expect(response).toMatchObject({
      id: 'usage-ops-refresh-all-for-provider-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles accountOps.getClaudeEmail through the account ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      accountOps: {
        getClaudeEmail: () =>
          Effect.sync(() => {
            calls.push('getClaudeEmail')
            return 'user@example.com'
          }),
        getOpenAIEmail: () => Effect.succeed(null),
        listSaved: () => Effect.succeed([]),
        removeSaved: () => Effect.succeed(false),
        switchAccount: () => Effect.succeed({ success: false }),
        loginStart: () => Effect.succeed({ loginId: 'login-1' }),
        loginStatus: () =>
          Effect.succeed({
            loginId: 'login-1',
            provider: 'anthropic' as const,
            state: 'waiting' as const,
            email: null,
            error: null
          }),
        loginCancel: () => Effect.succeed(false)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'account-ops-get-claude-email-1',
        method: 'accountOps.getClaudeEmail',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'account-ops-get-claude-email-1',
      ok: true,
      value: 'user@example.com'
    })
    expect(calls).toEqual(['getClaudeEmail'])
  })

  it('validates accountOps.getClaudeEmail params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      accountOps: {
        getClaudeEmail: () =>
          Effect.sync(() => {
            calls.push('getClaudeEmail')
            return null
          }),
        getOpenAIEmail: () => Effect.succeed(null),
        listSaved: () => Effect.succeed([]),
        removeSaved: () => Effect.succeed(false),
        switchAccount: () => Effect.succeed({ success: false }),
        loginStart: () => Effect.succeed({ loginId: 'login-1' }),
        loginStatus: () =>
          Effect.succeed({
            loginId: 'login-1',
            provider: 'anthropic' as const,
            state: 'waiting' as const,
            email: null,
            error: null
          }),
        loginCancel: () => Effect.succeed(false)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'account-ops-get-claude-email-2',
        method: 'accountOps.getClaudeEmail',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'account-ops-get-claude-email-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles accountOps.getOpenAIEmail through the account ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      accountOps: {
        getClaudeEmail: () => Effect.succeed(null),
        getOpenAIEmail: () =>
          Effect.sync(() => {
            calls.push('getOpenAIEmail')
            return 'openai-user@example.com'
          }),
        listSaved: () => Effect.succeed([]),
        removeSaved: () => Effect.succeed(false),
        switchAccount: () => Effect.succeed({ success: false }),
        loginStart: () => Effect.succeed({ loginId: 'login-1' }),
        loginStatus: () =>
          Effect.succeed({
            loginId: 'login-1',
            provider: 'anthropic' as const,
            state: 'waiting' as const,
            email: null,
            error: null
          }),
        loginCancel: () => Effect.succeed(false)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'account-ops-get-openai-email-1',
        method: 'accountOps.getOpenAIEmail',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'account-ops-get-openai-email-1',
      ok: true,
      value: 'openai-user@example.com'
    })
    expect(calls).toEqual(['getOpenAIEmail'])
  })

  it('validates accountOps.getOpenAIEmail params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      accountOps: {
        getClaudeEmail: () => Effect.succeed(null),
        getOpenAIEmail: () =>
          Effect.sync(() => {
            calls.push('getOpenAIEmail')
            return null
          }),
        listSaved: () => Effect.succeed([]),
        removeSaved: () => Effect.succeed(false),
        switchAccount: () => Effect.succeed({ success: false }),
        loginStart: () => Effect.succeed({ loginId: 'login-1' }),
        loginStatus: () =>
          Effect.succeed({
            loginId: 'login-1',
            provider: 'anthropic' as const,
            state: 'waiting' as const,
            email: null,
            error: null
          }),
        loginCancel: () => Effect.succeed(false)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'account-ops-get-openai-email-2',
        method: 'accountOps.getOpenAIEmail',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'account-ops-get-openai-email-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles accountOps.listSaved through the account ops RPC domain', async () => {
    const accounts = [
      {
        id: 'account-1',
        provider: 'openai' as const,
        email: 'openai-user@example.com',
        last_usage: null,
        last_fetched_at: '2026-05-26T00:00:00.000Z',
        status: 'ok' as const,
        last_error: null,
        created_at: '2026-05-25T00:00:00.000Z',
        plan: null
      }
    ]
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      accountOps: {
        getClaudeEmail: () => Effect.succeed(null),
        getOpenAIEmail: () => Effect.succeed(null),
        listSaved: (provider) =>
          Effect.sync(() => {
            calls.push(provider ?? 'all')
            return accounts
          }),
        removeSaved: () => Effect.succeed(false),
        switchAccount: () => Effect.succeed({ success: false }),
        loginStart: () => Effect.succeed({ loginId: 'login-1' }),
        loginStatus: () =>
          Effect.succeed({
            loginId: 'login-1',
            provider: 'anthropic' as const,
            state: 'waiting' as const,
            email: null,
            error: null
          }),
        loginCancel: () => Effect.succeed(false)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'account-ops-list-saved-1',
        method: 'accountOps.listSaved',
        params: { provider: 'openai' }
      })
    )

    expect(response).toEqual({
      id: 'account-ops-list-saved-1',
      ok: true,
      value: accounts
    })
    expect(calls).toEqual(['openai'])
  })

  it('validates accountOps.listSaved params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      accountOps: {
        getClaudeEmail: () => Effect.succeed(null),
        getOpenAIEmail: () => Effect.succeed(null),
        listSaved: (provider) =>
          Effect.sync(() => {
            calls.push(provider ?? 'all')
            return []
          }),
        removeSaved: () => Effect.succeed(false),
        switchAccount: () => Effect.succeed({ success: false }),
        loginStart: () => Effect.succeed({ loginId: 'login-1' }),
        loginStatus: () =>
          Effect.succeed({
            loginId: 'login-1',
            provider: 'anthropic' as const,
            state: 'waiting' as const,
            email: null,
            error: null
          }),
        loginCancel: () => Effect.succeed(false)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'account-ops-list-saved-2',
        method: 'accountOps.listSaved',
        params: { provider: 'other' }
      })
    )

    expect(response).toMatchObject({
      id: 'account-ops-list-saved-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles accountOps.removeSaved through the account ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      accountOps: {
        getClaudeEmail: () => Effect.succeed(null),
        getOpenAIEmail: () => Effect.succeed(null),
        listSaved: () => Effect.succeed([]),
        removeSaved: (accountId) =>
          Effect.sync(() => {
            calls.push(accountId)
            return true
          }),
        switchAccount: () => Effect.succeed({ success: false }),
        loginStart: () => Effect.succeed({ loginId: 'login-1' }),
        loginStatus: () =>
          Effect.succeed({
            loginId: 'login-1',
            provider: 'anthropic' as const,
            state: 'waiting' as const,
            email: null,
            error: null
          }),
        loginCancel: () => Effect.succeed(false)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'account-ops-remove-saved-1',
        method: 'accountOps.removeSaved',
        params: { accountId: 'account-1' }
      })
    )

    expect(response).toEqual({
      id: 'account-ops-remove-saved-1',
      ok: true,
      value: true
    })
    expect(calls).toEqual(['account-1'])
  })

  it('validates accountOps.removeSaved params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      accountOps: {
        getClaudeEmail: () => Effect.succeed(null),
        getOpenAIEmail: () => Effect.succeed(null),
        listSaved: () => Effect.succeed([]),
        removeSaved: (accountId) =>
          Effect.sync(() => {
            calls.push(accountId)
            return false
          }),
        switchAccount: () => Effect.succeed({ success: false }),
        loginStart: () => Effect.succeed({ loginId: 'login-1' }),
        loginStatus: () =>
          Effect.succeed({
            loginId: 'login-1',
            provider: 'anthropic' as const,
            state: 'waiting' as const,
            email: null,
            error: null
          }),
        loginCancel: () => Effect.succeed(false)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'account-ops-remove-saved-2',
        method: 'accountOps.removeSaved',
        params: { accountId: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'account-ops-remove-saved-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles accountOps.switchAccount through the account ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      accountOps: {
        getClaudeEmail: () => Effect.succeed(null),
        getOpenAIEmail: () => Effect.succeed(null),
        listSaved: () => Effect.succeed([]),
        removeSaved: () => Effect.succeed(false),
        switchAccount: (accountId) =>
          Effect.sync(() => {
            calls.push(accountId)
            return { success: true }
          }),
        loginStart: () => Effect.succeed({ loginId: 'login-1' }),
        loginStatus: () =>
          Effect.succeed({
            loginId: 'login-1',
            provider: 'anthropic' as const,
            state: 'waiting' as const,
            email: null,
            error: null
          }),
        loginCancel: () => Effect.succeed(false)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'account-ops-switch-account-1',
        method: 'accountOps.switchAccount',
        params: { accountId: 'account-1' }
      })
    )

    expect(response).toEqual({
      id: 'account-ops-switch-account-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['account-1'])
  })

  it('validates accountOps.switchAccount params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      accountOps: {
        getClaudeEmail: () => Effect.succeed(null),
        getOpenAIEmail: () => Effect.succeed(null),
        listSaved: () => Effect.succeed([]),
        removeSaved: () => Effect.succeed(false),
        switchAccount: (accountId) =>
          Effect.sync(() => {
            calls.push(accountId)
            return { success: true }
          }),
        loginStart: () => Effect.succeed({ loginId: 'login-1' }),
        loginStatus: () =>
          Effect.succeed({
            loginId: 'login-1',
            provider: 'anthropic' as const,
            state: 'waiting' as const,
            email: null,
            error: null
          }),
        loginCancel: () => Effect.succeed(false)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'account-ops-switch-account-2',
        method: 'accountOps.switchAccount',
        params: { accountId: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'account-ops-switch-account-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles perfDiagnosticsOps.enable through the perf diagnostics RPC domain', async () => {
    const calls: boolean[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      perfDiagnosticsOps: {
        enable: (enabled) =>
          Effect.sync(() => {
            calls.push(enabled)
          }),
        getSnapshot: () =>
          Effect.succeed({
            perfVersion: 'v6',
            timestamp: '2026-05-26T00:00:00.000Z',
            uptimeMs: 1234,
            cpu: { userMs: 0, systemMs: 0, percentSinceLastSample: 0 },
            memory: {
              rss: 0,
              heapUsed: 0,
              heapTotal: 0,
              external: 0,
              arrayBuffers: 0,
              nativeEstimate: 0
            },
            heap: {
              sizeLimit: 0,
              totalPhysical: 0,
              mallocedMemory: 0,
              numberOfGcContexts: 0
            },
            processes: {
              ptyActive: 0,
              scriptsActive: 0,
              scriptsTotalOpened: 0,
              scriptsTotalClosed: 0
            },
            watchers: { fileTree: 0, worktree: 0, branch: 0 },
            sessions: { active: 0 },
            handles: { active: 0, requests: 0, byType: {} },
            electron: { windows: 0, webContents: 0 },
            eventLoopLagMs: 0
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'perf-diagnostics-ops-enable-1',
        method: 'perfDiagnosticsOps.enable',
        params: { enabled: true }
      })
    )

    expect(response).toEqual({
      id: 'perf-diagnostics-ops-enable-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([true])
  })

  it('validates perfDiagnosticsOps.enable params', async () => {
    const calls: boolean[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      perfDiagnosticsOps: {
        enable: (enabled) =>
          Effect.sync(() => {
            calls.push(enabled)
          }),
        getSnapshot: () =>
          Effect.succeed({
            perfVersion: 'v6',
            timestamp: '2026-05-26T00:00:00.000Z',
            uptimeMs: 1234,
            cpu: { userMs: 0, systemMs: 0, percentSinceLastSample: 0 },
            memory: {
              rss: 0,
              heapUsed: 0,
              heapTotal: 0,
              external: 0,
              arrayBuffers: 0,
              nativeEstimate: 0
            },
            heap: {
              sizeLimit: 0,
              totalPhysical: 0,
              mallocedMemory: 0,
              numberOfGcContexts: 0
            },
            processes: {
              ptyActive: 0,
              scriptsActive: 0,
              scriptsTotalOpened: 0,
              scriptsTotalClosed: 0
            },
            watchers: { fileTree: 0, worktree: 0, branch: 0 },
            sessions: { active: 0 },
            handles: { active: 0, requests: 0, byType: {} },
            electron: { windows: 0, webContents: 0 },
            eventLoopLagMs: 0
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'perf-diagnostics-ops-enable-2',
        method: 'perfDiagnosticsOps.enable',
        params: { enabled: 'yes' }
      })
    )

    expect(response).toMatchObject({
      id: 'perf-diagnostics-ops-enable-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles perfDiagnosticsOps.getSnapshot through the perf diagnostics RPC domain', async () => {
    const snapshot = {
      perfVersion: 'v6',
      timestamp: '2026-05-26T00:00:00.000Z',
      uptimeMs: 1234,
      cpu: { userMs: 1, systemMs: 2, percentSinceLastSample: 3 },
      memory: {
        rss: 4,
        heapUsed: 5,
        heapTotal: 6,
        external: 7,
        arrayBuffers: 8,
        nativeEstimate: 9
      },
      heap: {
        sizeLimit: 10,
        totalPhysical: 11,
        mallocedMemory: 12,
        numberOfGcContexts: 13
      },
      processes: {
        ptyActive: 14,
        scriptsActive: 15,
        scriptsTotalOpened: 16,
        scriptsTotalClosed: 17
      },
      watchers: { fileTree: 18, worktree: 19, branch: 20 },
      sessions: { active: 21 },
      handles: { active: 22, requests: 23, byType: { Socket: 2 } },
      electron: { windows: 24, webContents: 25 },
      eventLoopLagMs: 26
    }
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      perfDiagnosticsOps: {
        enable: () => Effect.void,
        getSnapshot: () =>
          Effect.sync(() => {
            calls.push('getSnapshot')
            return snapshot
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'perf-diagnostics-ops-get-snapshot-1',
        method: 'perfDiagnosticsOps.getSnapshot',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'perf-diagnostics-ops-get-snapshot-1',
      ok: true,
      value: snapshot
    })
    expect(calls).toEqual(['getSnapshot'])
  })

  it('validates perfDiagnosticsOps.getSnapshot params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      perfDiagnosticsOps: {
        enable: () => Effect.void,
        getSnapshot: () =>
          Effect.sync(() => {
            calls.push('getSnapshot')
            return {
              perfVersion: 'v6',
              timestamp: '2026-05-26T00:00:00.000Z',
              uptimeMs: 1234,
              cpu: { userMs: 0, systemMs: 0, percentSinceLastSample: 0 },
              memory: {
                rss: 0,
                heapUsed: 0,
                heapTotal: 0,
                external: 0,
                arrayBuffers: 0,
                nativeEstimate: 0
              },
              heap: {
                sizeLimit: 0,
                totalPhysical: 0,
                mallocedMemory: 0,
                numberOfGcContexts: 0
              },
              processes: {
                ptyActive: 0,
                scriptsActive: 0,
                scriptsTotalOpened: 0,
                scriptsTotalClosed: 0
              },
              watchers: { fileTree: 0, worktree: 0, branch: 0 },
              sessions: { active: 0 },
              handles: { active: 0, requests: 0, byType: {} },
              electron: { windows: 0, webContents: 0 },
              eventLoopLagMs: 0
            }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'perf-diagnostics-ops-get-snapshot-2',
        method: 'perfDiagnosticsOps.getSnapshot',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'perf-diagnostics-ops-get-snapshot-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles codexDebugLoggerOps.configure through the codex debug logger RPC domain', async () => {
    const calls: Array<{ enabled: boolean; resetPerSession: boolean }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      codexDebugLoggerOps: {
        configure: (enabled, resetPerSession) =>
          Effect.sync(() => {
            calls.push({ enabled, resetPerSession })
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'codex-debug-logger-ops-configure-1',
        method: 'codexDebugLoggerOps.configure',
        params: { enabled: true, resetPerSession: false }
      })
    )

    expect(response).toEqual({
      id: 'codex-debug-logger-ops-configure-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ enabled: true, resetPerSession: false }])
  })

  it('validates codexDebugLoggerOps.configure params', async () => {
    const calls: Array<{ enabled: boolean; resetPerSession: boolean }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      codexDebugLoggerOps: {
        configure: (enabled, resetPerSession) =>
          Effect.sync(() => {
            calls.push({ enabled, resetPerSession })
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'codex-debug-logger-ops-configure-2',
        method: 'codexDebugLoggerOps.configure',
        params: { enabled: true, resetPerSession: 'no' }
      })
    )

    expect(response).toMatchObject({
      id: 'codex-debug-logger-ops-configure-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles analyticsOps.track through the analytics RPC domain', async () => {
    const calls: Array<{ event: string; properties?: Record<string, unknown> }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      analyticsOps: {
        track: (event, properties) =>
          Effect.sync(() => {
            calls.push({ event, properties })
          }),
        setEnabled: () => Effect.void,
        isEnabled: () => Effect.succeed(true)
      }
    })

    const properties = { source: 'settings', enabled: true }
    const response = await Effect.runPromise(
      router.handle({
        id: 'analytics-ops-track-1',
        method: 'analyticsOps.track',
        params: { event: 'setting_changed', properties }
      })
    )

    expect(response).toEqual({
      id: 'analytics-ops-track-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ event: 'setting_changed', properties }])
  })

  it('validates analyticsOps.track params', async () => {
    const calls: Array<{ event: string; properties?: Record<string, unknown> }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      analyticsOps: {
        track: (event, properties) =>
          Effect.sync(() => {
            calls.push({ event, properties })
          }),
        setEnabled: () => Effect.void,
        isEnabled: () => Effect.succeed(true)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'analytics-ops-track-2',
        method: 'analyticsOps.track',
        params: { event: 'setting_changed', properties: 'bad' }
      })
    )

    expect(response).toMatchObject({
      id: 'analytics-ops-track-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles analyticsOps.setEnabled through the analytics RPC domain', async () => {
    const calls: boolean[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      analyticsOps: {
        track: () => Effect.void,
        setEnabled: (enabled) =>
          Effect.sync(() => {
            calls.push(enabled)
          }),
        isEnabled: () => Effect.succeed(true)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'analytics-ops-set-enabled-1',
        method: 'analyticsOps.setEnabled',
        params: { enabled: false }
      })
    )

    expect(response).toEqual({
      id: 'analytics-ops-set-enabled-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([false])
  })

  it('validates analyticsOps.setEnabled params', async () => {
    const calls: boolean[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      analyticsOps: {
        track: () => Effect.void,
        setEnabled: (enabled) =>
          Effect.sync(() => {
            calls.push(enabled)
          }),
        isEnabled: () => Effect.succeed(true)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'analytics-ops-set-enabled-2',
        method: 'analyticsOps.setEnabled',
        params: { enabled: 'no' }
      })
    )

    expect(response).toMatchObject({
      id: 'analytics-ops-set-enabled-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles analyticsOps.isEnabled through the analytics RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      analyticsOps: {
        track: () => Effect.void,
        setEnabled: () => Effect.void,
        isEnabled: () =>
          Effect.sync(() => {
            calls.push('isEnabled')
            return false
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'analytics-ops-is-enabled-1',
        method: 'analyticsOps.isEnabled',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'analytics-ops-is-enabled-1',
      ok: true,
      value: false
    })
    expect(calls).toEqual(['isEnabled'])
  })

  it('validates analyticsOps.isEnabled params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      analyticsOps: {
        track: () => Effect.void,
        setEnabled: () => Effect.void,
        isEnabled: () =>
          Effect.sync(() => {
            calls.push('isEnabled')
            return true
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'analytics-ops-is-enabled-2',
        method: 'analyticsOps.isEnabled',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'analytics-ops-is-enabled-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles ticketImport.listProviders through the ticket import RPC domain', async () => {
    const calls: string[] = []
    const providers = [
      {
        id: 'github' as const,
        name: 'GitHub',
        icon: 'github'
      },
      {
        id: 'jira' as const,
        name: 'Jira',
        icon: 'jira'
      }
    ]
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      ticketImport: {
        listProviders: () =>
          Effect.sync(() => {
            calls.push('listProviders')
            return providers
          }),
        getSettingsSchema: () => Effect.succeed([]),
        authenticate: () => Effect.succeed({ success: true, error: null }),
        detectRepo: () => Effect.succeed({ repo: null }),
        listIssues: () => Effect.succeed({ issues: [], hasNextPage: false, totalCount: 0 }),
        importIssues: () => Effect.succeed({ imported: [], skipped: [] }),
        getAvailableStatuses: () => Effect.succeed([]),
        updateRemoteStatus: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'ticket-import-list-providers-1',
        method: 'ticketImport.listProviders',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'ticket-import-list-providers-1',
      ok: true,
      value: providers
    })
    expect(calls).toEqual(['listProviders'])
  })

  it('validates ticketImport.listProviders params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      ticketImport: {
        listProviders: () =>
          Effect.sync(() => {
            calls.push('listProviders')
            return []
          }),
        getSettingsSchema: () => Effect.succeed([]),
        authenticate: () => Effect.succeed({ success: true, error: null }),
        detectRepo: () => Effect.succeed({ repo: null }),
        listIssues: () => Effect.succeed({ issues: [], hasNextPage: false, totalCount: 0 }),
        importIssues: () => Effect.succeed({ imported: [], skipped: [] }),
        getAvailableStatuses: () => Effect.succeed([]),
        updateRemoteStatus: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'ticket-import-list-providers-2',
        method: 'ticketImport.listProviders',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'ticket-import-list-providers-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles ticketImport.getSettingsSchema through the ticket import RPC domain', async () => {
    const calls: string[] = []
    const schema = [
      {
        key: 'token',
        label: 'Token',
        type: 'password' as const,
        required: true,
        placeholder: 'ghp_...'
      }
    ]
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      ticketImport: {
        listProviders: () => Effect.succeed([]),
        getSettingsSchema: (providerId) =>
          Effect.sync(() => {
            calls.push(providerId)
            return schema
          }),
        authenticate: () => Effect.succeed({ success: true, error: null }),
        detectRepo: () => Effect.succeed({ repo: null }),
        listIssues: () => Effect.succeed({ issues: [], hasNextPage: false, totalCount: 0 }),
        importIssues: () => Effect.succeed({ imported: [], skipped: [] }),
        getAvailableStatuses: () => Effect.succeed([]),
        updateRemoteStatus: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'ticket-import-get-settings-schema-1',
        method: 'ticketImport.getSettingsSchema',
        params: { providerId: 'github' }
      })
    )

    expect(response).toEqual({
      id: 'ticket-import-get-settings-schema-1',
      ok: true,
      value: schema
    })
    expect(calls).toEqual(['github'])
  })

  it('validates ticketImport.getSettingsSchema params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      ticketImport: {
        listProviders: () => Effect.succeed([]),
        getSettingsSchema: (providerId) =>
          Effect.sync(() => {
            calls.push(providerId)
            return []
          }),
        authenticate: () => Effect.succeed({ success: true, error: null }),
        detectRepo: () => Effect.succeed({ repo: null }),
        listIssues: () => Effect.succeed({ issues: [], hasNextPage: false, totalCount: 0 }),
        importIssues: () => Effect.succeed({ imported: [], skipped: [] }),
        getAvailableStatuses: () => Effect.succeed([]),
        updateRemoteStatus: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'ticket-import-get-settings-schema-2',
        method: 'ticketImport.getSettingsSchema',
        params: { providerId: 'linear' }
      })
    )

    expect(response).toMatchObject({
      id: 'ticket-import-get-settings-schema-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles ticketImport.authenticate through the ticket import RPC domain', async () => {
    const calls: Array<{ providerId: string; settings: Record<string, string> }> = []
    const result = { success: false, error: 'Invalid token' }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      ticketImport: {
        listProviders: () => Effect.succeed([]),
        getSettingsSchema: () => Effect.succeed([]),
        authenticate: (providerId, settings) =>
          Effect.sync(() => {
            calls.push({ providerId, settings })
            return result
          }),
        detectRepo: () => Effect.succeed({ repo: null }),
        listIssues: () => Effect.succeed({ issues: [], hasNextPage: false, totalCount: 0 }),
        importIssues: () => Effect.succeed({ imported: [], skipped: [] }),
        getAvailableStatuses: () => Effect.succeed([]),
        updateRemoteStatus: () => Effect.succeed({ success: true })
      }
    })

    const settings = { token: 'ghp_test' }
    const response = await Effect.runPromise(
      router.handle({
        id: 'ticket-import-authenticate-1',
        method: 'ticketImport.authenticate',
        params: { providerId: 'github', settings }
      })
    )

    expect(response).toEqual({
      id: 'ticket-import-authenticate-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual([{ providerId: 'github', settings }])
  })

  it('validates ticketImport.authenticate params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      ticketImport: {
        listProviders: () => Effect.succeed([]),
        getSettingsSchema: () => Effect.succeed([]),
        authenticate: (providerId) =>
          Effect.sync(() => {
            calls.push(providerId)
            return { success: true, error: null }
          }),
        detectRepo: () => Effect.succeed({ repo: null }),
        listIssues: () => Effect.succeed({ issues: [], hasNextPage: false, totalCount: 0 }),
        importIssues: () => Effect.succeed({ imported: [], skipped: [] }),
        getAvailableStatuses: () => Effect.succeed([]),
        updateRemoteStatus: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'ticket-import-authenticate-2',
        method: 'ticketImport.authenticate',
        params: { providerId: 'github', settings: { token: 123 } }
      })
    )

    expect(response).toMatchObject({
      id: 'ticket-import-authenticate-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles ticketImport.detectRepo through the ticket import RPC domain', async () => {
    const calls: Array<{ providerId: string; projectPath: string }> = []
    const result = { repo: 'owner/repo' }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      ticketImport: {
        listProviders: () => Effect.succeed([]),
        getSettingsSchema: () => Effect.succeed([]),
        authenticate: () => Effect.succeed({ success: true, error: null }),
        detectRepo: (providerId, projectPath) =>
          Effect.sync(() => {
            calls.push({ providerId, projectPath })
            return result
          }),
        listIssues: () => Effect.succeed({ issues: [], hasNextPage: false, totalCount: 0 }),
        importIssues: () => Effect.succeed({ imported: [], skipped: [] }),
        getAvailableStatuses: () => Effect.succeed([]),
        updateRemoteStatus: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'ticket-import-detect-repo-1',
        method: 'ticketImport.detectRepo',
        params: { providerId: 'github', projectPath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'ticket-import-detect-repo-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual([{ providerId: 'github', projectPath: '/tmp/hive' }])
  })

  it('validates ticketImport.detectRepo params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      ticketImport: {
        listProviders: () => Effect.succeed([]),
        getSettingsSchema: () => Effect.succeed([]),
        authenticate: () => Effect.succeed({ success: true, error: null }),
        detectRepo: (providerId) =>
          Effect.sync(() => {
            calls.push(providerId)
            return { repo: null }
          }),
        listIssues: () => Effect.succeed({ issues: [], hasNextPage: false, totalCount: 0 }),
        importIssues: () => Effect.succeed({ imported: [], skipped: [] }),
        getAvailableStatuses: () => Effect.succeed([]),
        updateRemoteStatus: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'ticket-import-detect-repo-2',
        method: 'ticketImport.detectRepo',
        params: { providerId: 'github', projectPath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'ticket-import-detect-repo-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles ticketImport.listIssues through the ticket import RPC domain', async () => {
    const calls: Array<{
      providerId: string
      repo: string
      options: { page: number; perPage: number; state: string; search?: string }
      settings: Record<string, string>
    }> = []
    const result = {
      issues: [
        {
          externalId: '42',
          title: 'Fix issue',
          body: 'Details',
          state: 'open' as const,
          url: 'https://github.com/acme/hive/issues/42',
          createdAt: '2026-05-26T00:00:00.000Z',
          updatedAt: '2026-05-27T00:00:00.000Z'
        }
      ],
      hasNextPage: true,
      totalCount: 5,
      nextPageToken: 'next'
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      ticketImport: {
        listProviders: () => Effect.succeed([]),
        getSettingsSchema: () => Effect.succeed([]),
        authenticate: () => Effect.succeed({ success: true, error: null }),
        detectRepo: () => Effect.succeed({ repo: null }),
        listIssues: (providerId, repo, options, settings) =>
          Effect.sync(() => {
            calls.push({ providerId, repo, options, settings })
            return result
          }),
        importIssues: () => Effect.succeed({ imported: [], skipped: [] }),
        getAvailableStatuses: () => Effect.succeed([]),
        updateRemoteStatus: () => Effect.succeed({ success: true })
      }
    })

    const options = { page: 2, perPage: 25, state: 'open' as const, search: 'fix' }
    const settings = { token: 'ghp_test' }
    const response = await Effect.runPromise(
      router.handle({
        id: 'ticket-import-list-issues-1',
        method: 'ticketImport.listIssues',
        params: { providerId: 'github', repo: 'acme/hive', options, settings }
      })
    )

    expect(response).toEqual({
      id: 'ticket-import-list-issues-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual([{ providerId: 'github', repo: 'acme/hive', options, settings }])
  })

  it('validates ticketImport.listIssues params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      ticketImport: {
        listProviders: () => Effect.succeed([]),
        getSettingsSchema: () => Effect.succeed([]),
        authenticate: () => Effect.succeed({ success: true, error: null }),
        detectRepo: () => Effect.succeed({ repo: null }),
        listIssues: (providerId) =>
          Effect.sync(() => {
            calls.push(providerId)
            return { issues: [], hasNextPage: false, totalCount: 0 }
          }),
        importIssues: () => Effect.succeed({ imported: [], skipped: [] }),
        getAvailableStatuses: () => Effect.succeed([]),
        updateRemoteStatus: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'ticket-import-list-issues-2',
        method: 'ticketImport.listIssues',
        params: {
          providerId: 'github',
          repo: 'acme/hive',
          options: { page: 1, perPage: 25, state: 'triaged' },
          settings: { token: 'ghp_test' }
        }
      })
    )

    expect(response).toMatchObject({
      id: 'ticket-import-list-issues-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles ticketImport.importIssues through the ticket import RPC domain', async () => {
    const calls: Array<{
      providerId: string
      projectId: string
      repo: string
      issues: Array<{
        externalId: string
        title: string
        body: string | null
        state: string
        url: string
      }>
    }> = []
    const result = { imported: ['42'], skipped: ['43'] }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      ticketImport: {
        listProviders: () => Effect.succeed([]),
        getSettingsSchema: () => Effect.succeed([]),
        authenticate: () => Effect.succeed({ success: true, error: null }),
        detectRepo: () => Effect.succeed({ repo: null }),
        listIssues: () => Effect.succeed({ issues: [], hasNextPage: false, totalCount: 0 }),
        importIssues: (providerId, projectId, repo, issues) =>
          Effect.sync(() => {
            calls.push({ providerId, projectId, repo, issues })
            return result
          }),
        getAvailableStatuses: () => Effect.succeed([]),
        updateRemoteStatus: () => Effect.succeed({ success: true })
      }
    })

    const issues = [
      {
        externalId: '42',
        title: 'Fix issue',
        body: null,
        state: 'open',
        url: 'https://github.com/acme/hive/issues/42'
      }
    ]
    const response = await Effect.runPromise(
      router.handle({
        id: 'ticket-import-import-issues-1',
        method: 'ticketImport.importIssues',
        params: { providerId: 'github', projectId: 'project-1', repo: 'acme/hive', issues }
      })
    )

    expect(response).toEqual({
      id: 'ticket-import-import-issues-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual([
      { providerId: 'github', projectId: 'project-1', repo: 'acme/hive', issues }
    ])
  })

  it('validates ticketImport.importIssues params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      ticketImport: {
        listProviders: () => Effect.succeed([]),
        getSettingsSchema: () => Effect.succeed([]),
        authenticate: () => Effect.succeed({ success: true, error: null }),
        detectRepo: () => Effect.succeed({ repo: null }),
        listIssues: () => Effect.succeed({ issues: [], hasNextPage: false, totalCount: 0 }),
        importIssues: (providerId) =>
          Effect.sync(() => {
            calls.push(providerId)
            return { imported: [], skipped: [] }
          }),
        getAvailableStatuses: () => Effect.succeed([]),
        updateRemoteStatus: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'ticket-import-import-issues-2',
        method: 'ticketImport.importIssues',
        params: {
          providerId: 'github',
          projectId: '',
          repo: 'acme/hive',
          issues: []
        }
      })
    )

    expect(response).toMatchObject({
      id: 'ticket-import-import-issues-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles ticketImport.getAvailableStatuses through the ticket import RPC domain', async () => {
    const calls: Array<{
      providerId: string
      repo: string
      externalId: string
      settings: Record<string, string>
    }> = []
    const statuses = [
      { id: 'todo', label: 'To Do' },
      { id: 'done', label: 'Done' }
    ]
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      ticketImport: {
        listProviders: () => Effect.succeed([]),
        getSettingsSchema: () => Effect.succeed([]),
        authenticate: () => Effect.succeed({ success: true, error: null }),
        detectRepo: () => Effect.succeed({ repo: null }),
        listIssues: () => Effect.succeed({ issues: [], hasNextPage: false, totalCount: 0 }),
        importIssues: () => Effect.succeed({ imported: [], skipped: [] }),
        getAvailableStatuses: (providerId, repo, externalId, settings) =>
          Effect.sync(() => {
            calls.push({ providerId, repo, externalId, settings })
            return statuses
          }),
        updateRemoteStatus: () => Effect.succeed({ success: true })
      }
    })

    const settings = { token: 'ghp_test' }
    const response = await Effect.runPromise(
      router.handle({
        id: 'ticket-import-get-available-statuses-1',
        method: 'ticketImport.getAvailableStatuses',
        params: { providerId: 'github', repo: 'acme/hive', externalId: '42', settings }
      })
    )

    expect(response).toEqual({
      id: 'ticket-import-get-available-statuses-1',
      ok: true,
      value: statuses
    })
    expect(calls).toEqual([{ providerId: 'github', repo: 'acme/hive', externalId: '42', settings }])
  })

  it('validates ticketImport.getAvailableStatuses params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      ticketImport: {
        listProviders: () => Effect.succeed([]),
        getSettingsSchema: () => Effect.succeed([]),
        authenticate: () => Effect.succeed({ success: true, error: null }),
        detectRepo: () => Effect.succeed({ repo: null }),
        listIssues: () => Effect.succeed({ issues: [], hasNextPage: false, totalCount: 0 }),
        importIssues: () => Effect.succeed({ imported: [], skipped: [] }),
        getAvailableStatuses: (providerId) =>
          Effect.sync(() => {
            calls.push(providerId)
            return []
          }),
        updateRemoteStatus: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'ticket-import-get-available-statuses-2',
        method: 'ticketImport.getAvailableStatuses',
        params: {
          providerId: 'github',
          repo: 'acme/hive',
          externalId: '',
          settings: { token: 'ghp_test' }
        }
      })
    )

    expect(response).toMatchObject({
      id: 'ticket-import-get-available-statuses-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles ticketImport.updateRemoteStatus through the ticket import RPC domain', async () => {
    const calls: Array<{
      providerId: string
      repo: string
      externalId: string
      statusId: string
      settings: Record<string, string>
    }> = []
    const result = { success: false, error: 'Cannot transition' }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      ticketImport: {
        listProviders: () => Effect.succeed([]),
        getSettingsSchema: () => Effect.succeed([]),
        authenticate: () => Effect.succeed({ success: true, error: null }),
        detectRepo: () => Effect.succeed({ repo: null }),
        listIssues: () => Effect.succeed({ issues: [], hasNextPage: false, totalCount: 0 }),
        importIssues: () => Effect.succeed({ imported: [], skipped: [] }),
        getAvailableStatuses: () => Effect.succeed([]),
        updateRemoteStatus: (providerId, repo, externalId, statusId, settings) =>
          Effect.sync(() => {
            calls.push({ providerId, repo, externalId, statusId, settings })
            return result
          })
      }
    })

    const settings = { token: 'ghp_test' }
    const response = await Effect.runPromise(
      router.handle({
        id: 'ticket-import-update-remote-status-1',
        method: 'ticketImport.updateRemoteStatus',
        params: {
          providerId: 'github',
          repo: 'acme/hive',
          externalId: '42',
          statusId: 'done',
          settings
        }
      })
    )

    expect(response).toEqual({
      id: 'ticket-import-update-remote-status-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual([
      { providerId: 'github', repo: 'acme/hive', externalId: '42', statusId: 'done', settings }
    ])
  })

  it('validates ticketImport.updateRemoteStatus params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      ticketImport: {
        listProviders: () => Effect.succeed([]),
        getSettingsSchema: () => Effect.succeed([]),
        authenticate: () => Effect.succeed({ success: true, error: null }),
        detectRepo: () => Effect.succeed({ repo: null }),
        listIssues: () => Effect.succeed({ issues: [], hasNextPage: false, totalCount: 0 }),
        importIssues: () => Effect.succeed({ imported: [], skipped: [] }),
        getAvailableStatuses: () => Effect.succeed([]),
        updateRemoteStatus: (providerId) =>
          Effect.sync(() => {
            calls.push(providerId)
            return { success: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'ticket-import-update-remote-status-2',
        method: 'ticketImport.updateRemoteStatus',
        params: {
          providerId: 'github',
          repo: 'acme/hive',
          externalId: '42',
          statusId: '',
          settings: { token: 'ghp_test' }
        }
      })
    )

    expect(response).toMatchObject({
      id: 'ticket-import-update-remote-status-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles bash.run through the bash RPC domain', async () => {
    const calls: Array<{ sessionId: string; command: string; cwd: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      bash: {
        abort: () => Effect.succeed(false),
        getRun: () => Effect.succeed(null),
        run: (sessionId, command, cwd) =>
          Effect.sync(() => {
            calls.push({ sessionId, command, cwd })
            return { runId: 'run-1' }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'bash-run-1',
        method: 'bash.run',
        params: {
          sessionId: 'session-1',
          command: 'pnpm test',
          cwd: '/tmp/hive'
        }
      })
    )

    expect(response).toEqual({
      id: 'bash-run-1',
      ok: true,
      value: { runId: 'run-1' }
    })
    expect(calls).toEqual([
      {
        sessionId: 'session-1',
        command: 'pnpm test',
        cwd: '/tmp/hive'
      }
    ])
  })

  it('validates bash.run params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      bash: {
        abort: () => Effect.succeed(false),
        getRun: () => Effect.succeed(null),
        run: (sessionId) =>
          Effect.sync(() => {
            calls.push(sessionId)
            return { runId: 'run-1' }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'bash-run-2',
        method: 'bash.run',
        params: {
          sessionId: 'session-1',
          command: '',
          cwd: '/tmp/hive'
        }
      })
    )

    expect(response).toMatchObject({
      id: 'bash-run-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles bash.abort through the bash RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      bash: {
        getRun: () => Effect.succeed(null),
        run: () => Effect.succeed({ runId: 'run-1' }),
        abort: (sessionId) =>
          Effect.sync(() => {
            calls.push(sessionId)
            return true
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'bash-abort-1',
        method: 'bash.abort',
        params: { sessionId: 'session-1' }
      })
    )

    expect(response).toEqual({
      id: 'bash-abort-1',
      ok: true,
      value: true
    })
    expect(calls).toEqual(['session-1'])
  })

  it('validates bash.abort params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      bash: {
        getRun: () => Effect.succeed(null),
        run: () => Effect.succeed({ runId: 'run-1' }),
        abort: (sessionId) =>
          Effect.sync(() => {
            calls.push(sessionId)
            return true
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'bash-abort-2',
        method: 'bash.abort',
        params: { sessionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'bash-abort-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles bash.getRun through the bash RPC domain', async () => {
    const calls: string[] = []
    const snapshot = {
      sessionId: 'session-1',
      id: 'run-1',
      command: 'pnpm test',
      cwd: '/tmp/hive',
      startedAt: 1770000000000,
      status: 'running' as const,
      outputBuffer: 'stdout',
      outputBytes: 6
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      bash: {
        run: () => Effect.succeed({ runId: 'run-1' }),
        abort: () => Effect.succeed(false),
        getRun: (sessionId) =>
          Effect.sync(() => {
            calls.push(sessionId)
            return snapshot
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'bash-get-run-1',
        method: 'bash.getRun',
        params: { sessionId: 'session-1' }
      })
    )

    expect(response).toEqual({
      id: 'bash-get-run-1',
      ok: true,
      value: snapshot
    })
    expect(calls).toEqual(['session-1'])
  })

  it('validates bash.getRun params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      bash: {
        run: () => Effect.succeed({ runId: 'run-1' }),
        abort: () => Effect.succeed(false),
        getRun: (sessionId) =>
          Effect.sync(() => {
            calls.push(sessionId)
            return null
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'bash-get-run-2',
        method: 'bash.getRun',
        params: { sessionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'bash-get-run-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.getConfig through the terminal ops RPC domain', async () => {
    const calls: string[] = []
    const config = {
      fontFamily: 'JetBrains Mono',
      fontSize: 14,
      background: '#101010',
      palette: { 1: '#ff0000' }
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () =>
          Effect.sync(() => {
            calls.push('getConfig')
            return config
          }),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-get-config-1',
        method: 'terminalOps.getConfig',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'terminal-get-config-1',
      ok: true,
      value: config
    })
    expect(calls).toEqual(['getConfig'])
  })

  it('validates terminalOps.getConfig params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () =>
          Effect.sync(() => {
            calls.push('getConfig')
            return {}
          }),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-get-config-2',
        method: 'terminalOps.getConfig',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-get-config-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.create through the terminal ops RPC domain', async () => {
    const calls: Array<{ terminalId: string; cwd: string; shell?: string }> = []
    const result = { success: true, cols: 120, rows: 32 }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: (terminalId, cwd, shell) =>
          Effect.sync(() => {
            calls.push({ terminalId, cwd, shell })
            return result
          }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-create-1',
        method: 'terminalOps.create',
        params: { terminalId: 'terminal-1', cwd: '/tmp/project', shell: '/bin/zsh' }
      })
    )

    expect(response).toEqual({
      id: 'terminal-create-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual([{ terminalId: 'terminal-1', cwd: '/tmp/project', shell: '/bin/zsh' }])
  })

  it('validates terminalOps.create params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () =>
          Effect.sync(() => {
            calls.push('create')
            return { success: true, cols: 80, rows: 24 }
          }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-create-2',
        method: 'terminalOps.create',
        params: { terminalId: '', cwd: '/tmp/project' }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-create-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.createClaudeCli through the terminal ops RPC domain', async () => {
    const calls: Array<{ sessionId: string; opts?: { pendingPrompt?: string | null } }> = []
    const result = { success: true, cols: 120, rows: 32 }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: (sessionId, opts) =>
          Effect.sync(() => {
            calls.push({ sessionId, opts })
            return result
          }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-create-claude-cli-1',
        method: 'terminalOps.createClaudeCli',
        params: { sessionId: 'session-1', opts: { pendingPrompt: 'continue' } }
      })
    )

    expect(response).toEqual({
      id: 'terminal-create-claude-cli-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual([{ sessionId: 'session-1', opts: { pendingPrompt: 'continue' } }])
  })

  it('handles terminalOps.setClaudeCliPlanAutoApprove through the terminal ops RPC domain', async () => {
    const calls: Array<{ sessionId: string; enabled: boolean }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: (sessionId, enabled) =>
          Effect.sync(() => {
            calls.push({ sessionId, enabled })
            return { success: true }
          }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-plan-auto-approve-1',
        method: 'terminalOps.setClaudeCliPlanAutoApprove',
        params: { sessionId: 'session-1', enabled: true }
      })
    )

    expect(response).toEqual({
      id: 'terminal-plan-auto-approve-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([{ sessionId: 'session-1', enabled: true }])

    const invalid = await Effect.runPromise(
      router.handle({
        id: 'terminal-plan-auto-approve-2',
        method: 'terminalOps.setClaudeCliPlanAutoApprove',
        params: { sessionId: 'session-1', enabled: true, extra: 'nope' }
      })
    )
    expect(invalid.ok).toBe(false)
    expect(calls).toHaveLength(1)
  })

  it('validates terminalOps.createClaudeCli params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () =>
          Effect.sync(() => {
            calls.push('createClaudeCli')
            return { success: true, cols: 80, rows: 24 }
          }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-create-claude-cli-2',
        method: 'terminalOps.createClaudeCli',
        params: { sessionId: '', opts: { pendingPrompt: null } }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-create-claude-cli-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.ghosttyInit through the terminal ops RPC domain', async () => {
    const calls: string[] = []
    const result = { success: true, version: '1.0.0' }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () =>
          Effect.sync(() => {
            calls.push('ghosttyInit')
            return result
          }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-init-1',
        method: 'terminalOps.ghosttyInit',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'terminal-ghostty-init-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual(['ghosttyInit'])
  })

  it('validates terminalOps.ghosttyInit params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () =>
          Effect.sync(() => {
            calls.push('ghosttyInit')
            return { success: true }
          }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-init-2',
        method: 'terminalOps.ghosttyInit',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-ghostty-init-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.ghosttyIsAvailable through the terminal ops RPC domain', async () => {
    const calls: string[] = []
    const result = { available: true, initialized: false, platform: 'darwin' }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.sync(() => {
            calls.push('ghosttyIsAvailable')
            return result
          }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-is-available-1',
        method: 'terminalOps.ghosttyIsAvailable',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'terminal-ghostty-is-available-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual(['ghosttyIsAvailable'])
  })

  it('validates terminalOps.ghosttyIsAvailable params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.sync(() => {
            calls.push('ghosttyIsAvailable')
            return { available: false, initialized: false, platform: 'linux' }
          }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-is-available-2',
        method: 'terminalOps.ghosttyIsAvailable',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-ghostty-is-available-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.ghosttyCreateSurface through the terminal ops RPC domain', async () => {
    const calls: Array<{
      terminalId: string
      rect: { x: number; y: number; w: number; h: number }
      opts?: { cwd?: string; shell?: string; scaleFactor?: number; fontSize?: number }
    }> = []
    const rect = { x: 10, y: 20, w: 800, h: 600 }
    const opts = { cwd: '/tmp/project', shell: '/bin/zsh', scaleFactor: 2, fontSize: 14 }
    const result = { success: true, surfaceId: 42 }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: (terminalId, rectArg, optsArg) =>
          Effect.sync(() => {
            calls.push({ terminalId, rect: rectArg, opts: optsArg })
            return result
          }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-create-surface-1',
        method: 'terminalOps.ghosttyCreateSurface',
        params: { terminalId: 'terminal-1', rect, opts }
      })
    )

    expect(response).toEqual({
      id: 'terminal-ghostty-create-surface-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual([{ terminalId: 'terminal-1', rect, opts }])
  })

  it('validates terminalOps.ghosttyCreateSurface params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () =>
          Effect.sync(() => {
            calls.push('ghosttyCreateSurface')
            return { success: true, surfaceId: 42 }
          }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-create-surface-2',
        method: 'terminalOps.ghosttyCreateSurface',
        params: { terminalId: '', rect: { x: 10, y: 20, w: 800, h: 600 } }
      })
    )
    const invalidRectResponse = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-create-surface-3',
        method: 'terminalOps.ghosttyCreateSurface',
        params: { terminalId: 'terminal-1', rect: { x: 10, y: 20, w: '800', h: 600 } }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-ghostty-create-surface-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(invalidRectResponse).toMatchObject({
      id: 'terminal-ghostty-create-surface-3',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.ghosttySetFrame through the terminal ops RPC domain', async () => {
    const calls: Array<{
      terminalId: string
      rect: { x: number; y: number; w: number; h: number }
    }> = []
    const rect = { x: 10, y: 20, w: 800, h: 600 }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: (terminalId, rectArg) =>
          Effect.sync(() => {
            calls.push({ terminalId, rect: rectArg })
          }),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-set-frame-1',
        method: 'terminalOps.ghosttySetFrame',
        params: { terminalId: 'terminal-1', rect }
      })
    )

    expect(response).toEqual({
      id: 'terminal-ghostty-set-frame-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ terminalId: 'terminal-1', rect }])
  })

  it('validates terminalOps.ghosttySetFrame params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () =>
          Effect.sync(() => {
            calls.push('ghosttySetFrame')
          }),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-set-frame-2',
        method: 'terminalOps.ghosttySetFrame',
        params: { terminalId: '', rect: { x: 10, y: 20, w: 800, h: 600 } }
      })
    )
    const invalidRectResponse = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-set-frame-3',
        method: 'terminalOps.ghosttySetFrame',
        params: { terminalId: 'terminal-1', rect: { x: 10, y: 20, w: 800 } }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-ghostty-set-frame-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(invalidRectResponse).toMatchObject({
      id: 'terminal-ghostty-set-frame-3',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.ghosttySetSize through the terminal ops RPC domain', async () => {
    const calls: Array<{ terminalId: string; width: number; height: number }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: (terminalId, width, height) =>
          Effect.sync(() => {
            calls.push({ terminalId, width, height })
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-set-size-1',
        method: 'terminalOps.ghosttySetSize',
        params: { terminalId: 'terminal-1', width: 800, height: 600 }
      })
    )

    expect(response).toEqual({
      id: 'terminal-ghostty-set-size-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ terminalId: 'terminal-1', width: 800, height: 600 }])
  })

  it('validates terminalOps.ghosttySetSize params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () =>
          Effect.sync(() => {
            calls.push('ghosttySetSize')
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-set-size-2',
        method: 'terminalOps.ghosttySetSize',
        params: { terminalId: '', width: 800, height: 600 }
      })
    )
    const invalidSizeResponse = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-set-size-3',
        method: 'terminalOps.ghosttySetSize',
        params: { terminalId: 'terminal-1', width: '800', height: 600 }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-ghostty-set-size-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(invalidSizeResponse).toMatchObject({
      id: 'terminal-ghostty-set-size-3',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.ghosttyKeyEvent through the terminal ops RPC domain', async () => {
    const calls: Array<{
      terminalId: string
      event: {
        action: number
        keycode: number
        mods: number
        consumedMods?: number
        text?: string
        unshiftedCodepoint?: number
        composing?: boolean
      }
    }> = []
    const event = {
      action: 1,
      keycode: 36,
      mods: 2,
      consumedMods: 0,
      text: '\r',
      unshiftedCodepoint: 13,
      composing: false
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: (terminalId, eventArg) =>
          Effect.sync(() => {
            calls.push({ terminalId, event: eventArg })
            return true
          }),
        ghosttyMouseButton: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-key-event-1',
        method: 'terminalOps.ghosttyKeyEvent',
        params: { terminalId: 'terminal-1', event }
      })
    )

    expect(response).toEqual({
      id: 'terminal-ghostty-key-event-1',
      ok: true,
      value: true
    })
    expect(calls).toEqual([{ terminalId: 'terminal-1', event }])
  })

  it('validates terminalOps.ghosttyKeyEvent params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () =>
          Effect.sync(() => {
            calls.push('ghosttyKeyEvent')
            return true
          }),
        ghosttyMouseButton: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-key-event-2',
        method: 'terminalOps.ghosttyKeyEvent',
        params: { terminalId: '', event: { action: 1, keycode: 36, mods: 2 } }
      })
    )
    const invalidEventResponse = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-key-event-3',
        method: 'terminalOps.ghosttyKeyEvent',
        params: { terminalId: 'terminal-1', event: { action: 1, keycode: '36', mods: 2 } }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-ghostty-key-event-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(invalidEventResponse).toMatchObject({
      id: 'terminal-ghostty-key-event-3',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.ghosttyMouseButton through the terminal ops RPC domain', async () => {
    const calls: Array<{ terminalId: string; state: number; button: number; mods: number }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () => Effect.succeed(false),
        ghosttyMouseButton: (terminalId, state, button, mods) =>
          Effect.sync(() => {
            calls.push({ terminalId, state, button, mods })
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-mouse-button-1',
        method: 'terminalOps.ghosttyMouseButton',
        params: { terminalId: 'terminal-1', state: 1, button: 0, mods: 2 }
      })
    )

    expect(response).toEqual({
      id: 'terminal-ghostty-mouse-button-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ terminalId: 'terminal-1', state: 1, button: 0, mods: 2 }])
  })

  it('validates terminalOps.ghosttyMouseButton params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () => Effect.succeed(false),
        ghosttyMouseButton: () =>
          Effect.sync(() => {
            calls.push('ghosttyMouseButton')
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-mouse-button-2',
        method: 'terminalOps.ghosttyMouseButton',
        params: { terminalId: '', state: 1, button: 0, mods: 2 }
      })
    )
    const invalidButtonResponse = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-mouse-button-3',
        method: 'terminalOps.ghosttyMouseButton',
        params: { terminalId: 'terminal-1', state: 1, button: '0', mods: 2 }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-ghostty-mouse-button-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(invalidButtonResponse).toMatchObject({
      id: 'terminal-ghostty-mouse-button-3',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.ghosttyMousePos through the terminal ops RPC domain', async () => {
    const calls: Array<{ terminalId: string; x: number; y: number; mods: number }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () => Effect.succeed(false),
        ghosttyMouseButton: () => Effect.succeed(undefined),
        ghosttyMousePos: (terminalId, x, y, mods) =>
          Effect.sync(() => {
            calls.push({ terminalId, x, y, mods })
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-mouse-pos-1',
        method: 'terminalOps.ghosttyMousePos',
        params: { terminalId: 'terminal-1', x: 320, y: 180, mods: 2 }
      })
    )

    expect(response).toEqual({
      id: 'terminal-ghostty-mouse-pos-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ terminalId: 'terminal-1', x: 320, y: 180, mods: 2 }])
  })

  it('validates terminalOps.ghosttyMousePos params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () => Effect.succeed(false),
        ghosttyMouseButton: () => Effect.succeed(undefined),
        ghosttyMousePos: () =>
          Effect.sync(() => {
            calls.push('ghosttyMousePos')
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-mouse-pos-2',
        method: 'terminalOps.ghosttyMousePos',
        params: { terminalId: '', x: 320, y: 180, mods: 2 }
      })
    )
    const invalidPositionResponse = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-mouse-pos-3',
        method: 'terminalOps.ghosttyMousePos',
        params: { terminalId: 'terminal-1', x: '320', y: 180, mods: 2 }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-ghostty-mouse-pos-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(invalidPositionResponse).toMatchObject({
      id: 'terminal-ghostty-mouse-pos-3',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.ghosttyMouseScroll through the terminal ops RPC domain', async () => {
    const calls: Array<{ terminalId: string; dx: number; dy: number; mods: number }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () => Effect.succeed(false),
        ghosttyMouseButton: () => Effect.succeed(undefined),
        ghosttyMousePos: () => Effect.succeed(undefined),
        ghosttyMouseScroll: (terminalId, dx, dy, mods) =>
          Effect.sync(() => {
            calls.push({ terminalId, dx, dy, mods })
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-mouse-scroll-1',
        method: 'terminalOps.ghosttyMouseScroll',
        params: { terminalId: 'terminal-1', dx: 0, dy: -120, mods: 2 }
      })
    )

    expect(response).toEqual({
      id: 'terminal-ghostty-mouse-scroll-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ terminalId: 'terminal-1', dx: 0, dy: -120, mods: 2 }])
  })

  it('validates terminalOps.ghosttyMouseScroll params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () => Effect.succeed(false),
        ghosttyMouseButton: () => Effect.succeed(undefined),
        ghosttyMousePos: () => Effect.succeed(undefined),
        ghosttyMouseScroll: () =>
          Effect.sync(() => {
            calls.push('ghosttyMouseScroll')
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-mouse-scroll-2',
        method: 'terminalOps.ghosttyMouseScroll',
        params: { terminalId: '', dx: 0, dy: -120, mods: 2 }
      })
    )
    const invalidDeltaResponse = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-mouse-scroll-3',
        method: 'terminalOps.ghosttyMouseScroll',
        params: { terminalId: 'terminal-1', dx: 0, dy: '-120', mods: 2 }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-ghostty-mouse-scroll-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(invalidDeltaResponse).toMatchObject({
      id: 'terminal-ghostty-mouse-scroll-3',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.ghosttySetFocus through the terminal ops RPC domain', async () => {
    const calls: Array<{ terminalId: string; focused: boolean }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () => Effect.succeed(false),
        ghosttyMouseButton: () => Effect.succeed(undefined),
        ghosttyMousePos: () => Effect.succeed(undefined),
        ghosttyMouseScroll: () => Effect.succeed(undefined),
        ghosttySetFocus: (terminalId, focused) =>
          Effect.sync(() => {
            calls.push({ terminalId, focused })
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-set-focus-1',
        method: 'terminalOps.ghosttySetFocus',
        params: { terminalId: 'terminal-1', focused: true }
      })
    )

    expect(response).toEqual({
      id: 'terminal-ghostty-set-focus-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ terminalId: 'terminal-1', focused: true }])
  })

  it('validates terminalOps.ghosttySetFocus params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () => Effect.succeed(false),
        ghosttyMouseButton: () => Effect.succeed(undefined),
        ghosttyMousePos: () => Effect.succeed(undefined),
        ghosttyMouseScroll: () => Effect.succeed(undefined),
        ghosttySetFocus: () =>
          Effect.sync(() => {
            calls.push('ghosttySetFocus')
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-set-focus-2',
        method: 'terminalOps.ghosttySetFocus',
        params: { terminalId: '', focused: true }
      })
    )
    const invalidFocusedResponse = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-set-focus-3',
        method: 'terminalOps.ghosttySetFocus',
        params: { terminalId: 'terminal-1', focused: 'true' }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-ghostty-set-focus-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(invalidFocusedResponse).toMatchObject({
      id: 'terminal-ghostty-set-focus-3',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.ghosttyPasteText through the terminal ops RPC domain', async () => {
    const calls: Array<{ terminalId: string; text: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () => Effect.succeed(false),
        ghosttyMouseButton: () => Effect.succeed(undefined),
        ghosttyMousePos: () => Effect.succeed(undefined),
        ghosttyMouseScroll: () => Effect.succeed(undefined),
        ghosttySetFocus: () => Effect.succeed(undefined),
        ghosttyPasteText: (terminalId, text) =>
          Effect.sync(() => {
            calls.push({ terminalId, text })
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-paste-text-1',
        method: 'terminalOps.ghosttyPasteText',
        params: { terminalId: 'terminal-1', text: 'hello\n' }
      })
    )

    expect(response).toEqual({
      id: 'terminal-ghostty-paste-text-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ terminalId: 'terminal-1', text: 'hello\n' }])
  })

  it('validates terminalOps.ghosttyPasteText params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () => Effect.succeed(false),
        ghosttyMouseButton: () => Effect.succeed(undefined),
        ghosttyMousePos: () => Effect.succeed(undefined),
        ghosttyMouseScroll: () => Effect.succeed(undefined),
        ghosttySetFocus: () => Effect.succeed(undefined),
        ghosttyPasteText: () =>
          Effect.sync(() => {
            calls.push('ghosttyPasteText')
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-paste-text-2',
        method: 'terminalOps.ghosttyPasteText',
        params: { terminalId: '', text: 'hello\n' }
      })
    )
    const invalidTextResponse = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-paste-text-3',
        method: 'terminalOps.ghosttyPasteText',
        params: { terminalId: 'terminal-1', text: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-ghostty-paste-text-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(invalidTextResponse).toMatchObject({
      id: 'terminal-ghostty-paste-text-3',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.ghosttyFocusDiagnostics through the terminal ops RPC domain', async () => {
    const diagnostics = [
      {
        surfaceId: 42,
        subviewCount: 2,
        firstResponderClass: 'GhosttyView',
        isHostView: true,
        isDescendant: true,
        hasWindow: true
      }
    ]
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () => Effect.succeed(false),
        ghosttyMouseButton: () => Effect.succeed(undefined),
        ghosttyMousePos: () => Effect.succeed(undefined),
        ghosttyMouseScroll: () => Effect.succeed(undefined),
        ghosttySetFocus: () => Effect.succeed(undefined),
        ghosttyPasteText: () => Effect.succeed(undefined),
        ghosttyFocusDiagnostics: () => Effect.succeed(diagnostics),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-focus-diagnostics-1',
        method: 'terminalOps.ghosttyFocusDiagnostics',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'terminal-ghostty-focus-diagnostics-1',
      ok: true,
      value: diagnostics
    })
  })

  it('validates terminalOps.ghosttyFocusDiagnostics params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () => Effect.succeed(false),
        ghosttyMouseButton: () => Effect.succeed(undefined),
        ghosttyMousePos: () => Effect.succeed(undefined),
        ghosttyMouseScroll: () => Effect.succeed(undefined),
        ghosttySetFocus: () => Effect.succeed(undefined),
        ghosttyPasteText: () => Effect.succeed(undefined),
        ghosttyFocusDiagnostics: () =>
          Effect.sync(() => {
            calls.push('ghosttyFocusDiagnostics')
            return []
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-focus-diagnostics-2',
        method: 'terminalOps.ghosttyFocusDiagnostics',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-ghostty-focus-diagnostics-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.ghosttyDestroySurface through the terminal ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () => Effect.succeed(false),
        ghosttyMouseButton: () => Effect.succeed(undefined),
        ghosttyMousePos: () => Effect.succeed(undefined),
        ghosttyMouseScroll: () => Effect.succeed(undefined),
        ghosttySetFocus: () => Effect.succeed(undefined),
        ghosttyPasteText: () => Effect.succeed(undefined),
        ghosttyFocusDiagnostics: () => Effect.succeed([]),
        ghosttyDestroySurface: (terminalId) =>
          Effect.sync(() => {
            calls.push(terminalId)
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-destroy-surface-1',
        method: 'terminalOps.ghosttyDestroySurface',
        params: { terminalId: 'terminal-1' }
      })
    )

    expect(response).toEqual({
      id: 'terminal-ghostty-destroy-surface-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual(['terminal-1'])
  })

  it('validates terminalOps.ghosttyDestroySurface params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () => Effect.succeed(false),
        ghosttyMouseButton: () => Effect.succeed(undefined),
        ghosttyMousePos: () => Effect.succeed(undefined),
        ghosttyMouseScroll: () => Effect.succeed(undefined),
        ghosttySetFocus: () => Effect.succeed(undefined),
        ghosttyPasteText: () => Effect.succeed(undefined),
        ghosttyFocusDiagnostics: () => Effect.succeed([]),
        ghosttyDestroySurface: (terminalId) =>
          Effect.sync(() => {
            calls.push(terminalId)
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-destroy-surface-2',
        method: 'terminalOps.ghosttyDestroySurface',
        params: { terminalId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-ghostty-destroy-surface-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.ghosttyShutdown through the terminal ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () => Effect.succeed(false),
        ghosttyMouseButton: () => Effect.succeed(undefined),
        ghosttyMousePos: () => Effect.succeed(undefined),
        ghosttyMouseScroll: () => Effect.succeed(undefined),
        ghosttySetFocus: () => Effect.succeed(undefined),
        ghosttyPasteText: () => Effect.succeed(undefined),
        ghosttyFocusDiagnostics: () => Effect.succeed([]),
        ghosttyDestroySurface: () => Effect.succeed(undefined),
        ghosttyShutdown: () =>
          Effect.sync(() => {
            calls.push('ghosttyShutdown')
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-shutdown-1',
        method: 'terminalOps.ghosttyShutdown',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'terminal-ghostty-shutdown-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual(['ghosttyShutdown'])
  })

  it('validates terminalOps.ghosttyShutdown params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        ghosttyKeyEvent: () => Effect.succeed(false),
        ghosttyMouseButton: () => Effect.succeed(undefined),
        ghosttyMousePos: () => Effect.succeed(undefined),
        ghosttyMouseScroll: () => Effect.succeed(undefined),
        ghosttySetFocus: () => Effect.succeed(undefined),
        ghosttyPasteText: () => Effect.succeed(undefined),
        ghosttyFocusDiagnostics: () => Effect.succeed([]),
        ghosttyDestroySurface: () => Effect.succeed(undefined),
        ghosttyShutdown: () =>
          Effect.sync(() => {
            calls.push('ghosttyShutdown')
          }),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-ghostty-shutdown-2',
        method: 'terminalOps.ghosttyShutdown',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-ghostty-shutdown-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.resize through the terminal ops RPC domain', async () => {
    const calls: Array<{ terminalId: string; cols: number; rows: number }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: (terminalId, cols, rows) =>
          Effect.sync(() => {
            calls.push({ terminalId, cols, rows })
          }),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-resize-1',
        method: 'terminalOps.resize',
        params: { terminalId: 'terminal-1', cols: 120, rows: 32 }
      })
    )

    expect(response).toEqual({
      id: 'terminal-resize-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ terminalId: 'terminal-1', cols: 120, rows: 32 }])
  })

  it('validates terminalOps.resize params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () =>
          Effect.sync(() => {
            calls.push('resize')
          }),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-resize-2',
        method: 'terminalOps.resize',
        params: { terminalId: '', cols: 120, rows: 32 }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-resize-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.destroy through the terminal ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: (terminalId) =>
          Effect.sync(() => {
            calls.push(terminalId)
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-destroy-1',
        method: 'terminalOps.destroy',
        params: { terminalId: 'terminal-1' }
      })
    )

    expect(response).toEqual({
      id: 'terminal-destroy-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual(['terminal-1'])
  })

  it('validates terminalOps.destroy params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () => Effect.succeed(undefined),
        resize: () => Effect.succeed(undefined),
        destroy: () =>
          Effect.sync(() => {
            calls.push('destroy')
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-destroy-2',
        method: 'terminalOps.destroy',
        params: { terminalId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-destroy-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles terminalOps.write through the terminal ops RPC domain', async () => {
    const calls: Array<{ terminalId: string; data: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: (terminalId, data) =>
          Effect.sync(() => {
            calls.push({ terminalId, data })
          }),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-write-1',
        method: 'terminalOps.write',
        params: { terminalId: 'terminal-1', data: 'ls\n' }
      })
    )

    expect(response).toEqual({
      id: 'terminal-write-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ terminalId: 'terminal-1', data: 'ls\n' }])
  })

  it('validates terminalOps.write params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      terminalOps: {
        getConfig: () => Effect.succeed({}),
        create: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        createClaudeCli: () => Effect.succeed({ success: true, cols: 80, rows: 24 }),
        setClaudeCliPlanAutoApprove: () => Effect.succeed({ success: true }),
        ghosttyInit: () => Effect.succeed({ success: true, version: 'test' }),
        ghosttyIsAvailable: () =>
          Effect.succeed({ available: false, initialized: false, platform: 'linux' }),
        ghosttyCreateSurface: () => Effect.succeed({ success: true, surfaceId: 1 }),
        ghosttySetFrame: () => Effect.succeed(undefined),
        ghosttySetSize: () => Effect.succeed(undefined),
        write: () =>
          Effect.sync(() => {
            calls.push('write')
          }),
        resize: () => Effect.succeed(undefined),
        destroy: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'terminal-write-2',
        method: 'terminalOps.write',
        params: { terminalId: '', data: 'ls\n' }
      })
    )

    expect(response).toMatchObject({
      id: 'terminal-write-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles scriptOps.getPort through the script ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      scriptOps: {
        killPid: () => Effect.succeed({ killed: false, reason: 'EINVAL' }),
        kill: () => Effect.succeed({ success: true }),
        runArchive: () => Effect.succeed({ success: true, output: '' }),
        runSetup: () => Effect.succeed({ success: true }),
        runProject: () => Effect.succeed({ success: true, pid: 1234 }),
        getPort: (cwd) =>
          Effect.sync(() => {
            calls.push(cwd)
            return { port: 3011 }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'script-get-port-1',
        method: 'scriptOps.getPort',
        params: { cwd: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'script-get-port-1',
      ok: true,
      value: { port: 3011 }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates scriptOps.getPort params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      scriptOps: {
        killPid: () => Effect.succeed({ killed: false, reason: 'EINVAL' }),
        kill: () => Effect.succeed({ success: true }),
        runArchive: () => Effect.succeed({ success: true, output: '' }),
        runSetup: () => Effect.succeed({ success: true }),
        runProject: () => Effect.succeed({ success: true, pid: 1234 }),
        getPort: (cwd) =>
          Effect.sync(() => {
            calls.push(cwd)
            return { port: null }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'script-get-port-2',
        method: 'scriptOps.getPort',
        params: { cwd: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'script-get-port-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles scriptOps.killPid through the script ops RPC domain', async () => {
    const calls: number[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      scriptOps: {
        getPort: () => Effect.succeed({ port: null }),
        kill: () => Effect.succeed({ success: true }),
        runArchive: () => Effect.succeed({ success: true, output: '' }),
        runSetup: () => Effect.succeed({ success: true }),
        runProject: () => Effect.succeed({ success: true, pid: 1234 }),
        killPid: (pid) =>
          Effect.sync(() => {
            calls.push(pid)
            return { killed: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'script-kill-pid-1',
        method: 'scriptOps.killPid',
        params: { pid: 12345 }
      })
    )

    expect(response).toEqual({
      id: 'script-kill-pid-1',
      ok: true,
      value: { killed: true }
    })
    expect(calls).toEqual([12345])
  })

  it('validates scriptOps.killPid params', async () => {
    const calls: number[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      scriptOps: {
        getPort: () => Effect.succeed({ port: null }),
        kill: () => Effect.succeed({ success: true }),
        runArchive: () => Effect.succeed({ success: true, output: '' }),
        runSetup: () => Effect.succeed({ success: true }),
        runProject: () => Effect.succeed({ success: true, pid: 1234 }),
        killPid: (pid) =>
          Effect.sync(() => {
            calls.push(pid)
            return { killed: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'script-kill-pid-2',
        method: 'scriptOps.killPid',
        params: { pid: 1.5 }
      })
    )

    expect(response).toMatchObject({
      id: 'script-kill-pid-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles scriptOps.kill through the script ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      scriptOps: {
        getPort: () => Effect.succeed({ port: null }),
        killPid: () => Effect.succeed({ killed: false, reason: 'EINVAL' }),
        runArchive: () => Effect.succeed({ success: true, output: '' }),
        runSetup: () => Effect.succeed({ success: true }),
        runProject: () => Effect.succeed({ success: true, pid: 1234 }),
        kill: (worktreeId) =>
          Effect.sync(() => {
            calls.push(worktreeId)
            return { success: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'script-kill-1',
        method: 'scriptOps.kill',
        params: { worktreeId: 'worktree-1' }
      })
    )

    expect(response).toEqual({
      id: 'script-kill-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['worktree-1'])
  })

  it('validates scriptOps.kill params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      scriptOps: {
        getPort: () => Effect.succeed({ port: null }),
        killPid: () => Effect.succeed({ killed: false, reason: 'EINVAL' }),
        runArchive: () => Effect.succeed({ success: true, output: '' }),
        runSetup: () => Effect.succeed({ success: true }),
        runProject: () => Effect.succeed({ success: true, pid: 1234 }),
        kill: (worktreeId) =>
          Effect.sync(() => {
            calls.push(worktreeId)
            return { success: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'script-kill-2',
        method: 'scriptOps.kill',
        params: { worktreeId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'script-kill-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles scriptOps.runArchive through the script ops RPC domain', async () => {
    const calls: Array<{ commands: string[]; cwd: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      scriptOps: {
        getPort: () => Effect.succeed({ port: null }),
        killPid: () => Effect.succeed({ killed: false, reason: 'EINVAL' }),
        kill: () => Effect.succeed({ success: true }),
        runSetup: () => Effect.succeed({ success: true }),
        runProject: () => Effect.succeed({ success: true, pid: 1234 }),
        runArchive: (commands, cwd) =>
          Effect.sync(() => {
            calls.push({ commands, cwd })
            return { success: true, output: 'archived\n' }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'script-run-archive-1',
        method: 'scriptOps.runArchive',
        params: { commands: ['npm test'], cwd: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'script-run-archive-1',
      ok: true,
      value: { success: true, output: 'archived\n' }
    })
    expect(calls).toEqual([{ commands: ['npm test'], cwd: '/tmp/hive' }])
  })

  it('validates scriptOps.runArchive params', async () => {
    const calls: Array<{ commands: string[]; cwd: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      scriptOps: {
        getPort: () => Effect.succeed({ port: null }),
        killPid: () => Effect.succeed({ killed: false, reason: 'EINVAL' }),
        kill: () => Effect.succeed({ success: true }),
        runSetup: () => Effect.succeed({ success: true }),
        runProject: () => Effect.succeed({ success: true, pid: 1234 }),
        runArchive: (commands, cwd) =>
          Effect.sync(() => {
            calls.push({ commands, cwd })
            return { success: true, output: '' }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'script-run-archive-2',
        method: 'scriptOps.runArchive',
        params: { commands: ['npm test'], cwd: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'script-run-archive-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles scriptOps.runSetup through the script ops RPC domain', async () => {
    const calls: Array<{ commands: string[]; cwd: string; worktreeId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      scriptOps: {
        getPort: () => Effect.succeed({ port: null }),
        killPid: () => Effect.succeed({ killed: false, reason: 'EINVAL' }),
        kill: () => Effect.succeed({ success: true }),
        runArchive: () => Effect.succeed({ success: true, output: '' }),
        runProject: () => Effect.succeed({ success: true, pid: 1234 }),
        runSetup: (commands, cwd, worktreeId) =>
          Effect.sync(() => {
            calls.push({ commands, cwd, worktreeId })
            return { success: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'script-run-setup-1',
        method: 'scriptOps.runSetup',
        params: {
          commands: ['npm install', 'npm run setup'],
          cwd: '/tmp/hive',
          worktreeId: 'worktree-1'
        }
      })
    )

    expect(response).toEqual({
      id: 'script-run-setup-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([
      {
        commands: ['npm install', 'npm run setup'],
        cwd: '/tmp/hive',
        worktreeId: 'worktree-1'
      }
    ])
  })

  it('validates scriptOps.runSetup params', async () => {
    const calls: Array<{ commands: string[]; cwd: string; worktreeId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      scriptOps: {
        getPort: () => Effect.succeed({ port: null }),
        killPid: () => Effect.succeed({ killed: false, reason: 'EINVAL' }),
        kill: () => Effect.succeed({ success: true }),
        runArchive: () => Effect.succeed({ success: true, output: '' }),
        runProject: () => Effect.succeed({ success: true, pid: 1234 }),
        runSetup: (commands, cwd, worktreeId) =>
          Effect.sync(() => {
            calls.push({ commands, cwd, worktreeId })
            return { success: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'script-run-setup-2',
        method: 'scriptOps.runSetup',
        params: { commands: ['npm install'], cwd: '/tmp/hive', worktreeId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'script-run-setup-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles scriptOps.runProject through the script ops RPC domain', async () => {
    const calls: Array<{ commands: string[]; cwd: string; worktreeId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      scriptOps: {
        getPort: () => Effect.succeed({ port: null }),
        killPid: () => Effect.succeed({ killed: false, reason: 'EINVAL' }),
        kill: () => Effect.succeed({ success: true }),
        runArchive: () => Effect.succeed({ success: true, output: '' }),
        runSetup: () => Effect.succeed({ success: true }),
        runProject: (commands, cwd, worktreeId) =>
          Effect.sync(() => {
            calls.push({ commands, cwd, worktreeId })
            return { success: true, pid: 4321 }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'script-run-project-1',
        method: 'scriptOps.runProject',
        params: {
          commands: ['npm run dev'],
          cwd: '/tmp/hive',
          worktreeId: 'worktree-1'
        }
      })
    )

    expect(response).toEqual({
      id: 'script-run-project-1',
      ok: true,
      value: { success: true, pid: 4321 }
    })
    expect(calls).toEqual([
      {
        commands: ['npm run dev'],
        cwd: '/tmp/hive',
        worktreeId: 'worktree-1'
      }
    ])
  })

  it('validates scriptOps.runProject params', async () => {
    const calls: Array<{ commands: string[]; cwd: string; worktreeId: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      scriptOps: {
        getPort: () => Effect.succeed({ port: null }),
        killPid: () => Effect.succeed({ killed: false, reason: 'EINVAL' }),
        kill: () => Effect.succeed({ success: true }),
        runArchive: () => Effect.succeed({ success: true, output: '' }),
        runSetup: () => Effect.succeed({ success: true }),
        runProject: (commands, cwd, worktreeId) =>
          Effect.sync(() => {
            calls.push({ commands, cwd, worktreeId })
            return { success: true, pid: 4321 }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'script-run-project-2',
        method: 'scriptOps.runProject',
        params: { commands: ['npm run dev'], cwd: '', worktreeId: 'worktree-1' }
      })
    )

    expect(response).toMatchObject({
      id: 'script-run-project-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  const inactiveTelegramStatus = {
    active: false,
    sessionId: null,
    worktreeId: null,
    connectionId: null,
    mode: null,
    health: 'ok' as const,
    lastError: null
  }

  it('handles telegramOps.getConfig through the telegram ops RPC domain', async () => {
    const calls: string[] = []
    const config = {
      botToken: '123456:test-token',
      chatId: 42,
      chatName: 'Hive',
      contextSize: 12
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        setConfig: () => Effect.succeed({ ok: true }),
        verifyToken: () => Effect.succeed({ ok: true }),
        discoverChats: () => Effect.succeed([]),
        sendTestMessage: () => Effect.succeed({ ok: true }),
        startForwarding: () => Effect.succeed({ ok: true, status: inactiveTelegramStatus }),
        stopForwarding: () => Effect.succeed({ status: inactiveTelegramStatus }),
        getStatus: () => Effect.succeed(inactiveTelegramStatus),
        getConfig: () =>
          Effect.sync(() => {
            calls.push('getConfig')
            return config
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-get-config-1',
        method: 'telegramOps.getConfig',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'telegram-ops-get-config-1',
      ok: true,
      value: config
    })
    expect(calls).toEqual(['getConfig'])
  })

  it('validates telegramOps.getConfig params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        setConfig: () => Effect.succeed({ ok: true }),
        verifyToken: () => Effect.succeed({ ok: true }),
        discoverChats: () => Effect.succeed([]),
        sendTestMessage: () => Effect.succeed({ ok: true }),
        startForwarding: () => Effect.succeed({ ok: true, status: inactiveTelegramStatus }),
        stopForwarding: () => Effect.succeed({ status: inactiveTelegramStatus }),
        getStatus: () => Effect.succeed(inactiveTelegramStatus),
        getConfig: () =>
          Effect.sync(() => {
            calls.push('getConfig')
            return null
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-get-config-2',
        method: 'telegramOps.getConfig',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'telegram-ops-get-config-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles telegramOps.setConfig through the telegram ops RPC domain', async () => {
    const calls: Array<{
      botToken: string
      chatId: number
      chatName: string
      contextSize: number
    } | null> = []
    const config = {
      botToken: '123456:test-token',
      chatId: 42,
      chatName: 'Hive',
      contextSize: 12
    }
    const result = { ok: true }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        getConfig: () => Effect.succeed(null),
        verifyToken: () => Effect.succeed({ ok: true }),
        discoverChats: () => Effect.succeed([]),
        sendTestMessage: () => Effect.succeed({ ok: true }),
        startForwarding: () => Effect.succeed({ ok: true, status: inactiveTelegramStatus }),
        stopForwarding: () => Effect.succeed({ status: inactiveTelegramStatus }),
        getStatus: () => Effect.succeed(inactiveTelegramStatus),
        setConfig: (nextConfig) =>
          Effect.sync(() => {
            calls.push(nextConfig)
            return result
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-set-config-1',
        method: 'telegramOps.setConfig',
        params: { config }
      })
    )

    expect(response).toEqual({
      id: 'telegram-ops-set-config-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual([config])
  })

  it('validates telegramOps.setConfig params', async () => {
    const calls: unknown[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        getConfig: () => Effect.succeed(null),
        verifyToken: () => Effect.succeed({ ok: true }),
        discoverChats: () => Effect.succeed([]),
        sendTestMessage: () => Effect.succeed({ ok: true }),
        startForwarding: () => Effect.succeed({ ok: true, status: inactiveTelegramStatus }),
        stopForwarding: () => Effect.succeed({ status: inactiveTelegramStatus }),
        getStatus: () => Effect.succeed(inactiveTelegramStatus),
        setConfig: (config) =>
          Effect.sync(() => {
            calls.push(config)
            return { ok: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-set-config-2',
        method: 'telegramOps.setConfig',
        params: {
          config: {
            botToken: '123456:test-token',
            chatId: '42',
            chatName: 'Hive',
            contextSize: 12
          }
        }
      })
    )

    expect(response).toMatchObject({
      id: 'telegram-ops-set-config-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles telegramOps.verifyToken through the telegram ops RPC domain', async () => {
    const calls: string[] = []
    const result = { ok: true, botUsername: 'hive_bot' }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        getConfig: () => Effect.succeed(null),
        setConfig: () => Effect.succeed({ ok: true }),
        discoverChats: () => Effect.succeed([]),
        sendTestMessage: () => Effect.succeed({ ok: true }),
        startForwarding: () => Effect.succeed({ ok: true, status: inactiveTelegramStatus }),
        stopForwarding: () => Effect.succeed({ status: inactiveTelegramStatus }),
        getStatus: () => Effect.succeed(inactiveTelegramStatus),
        verifyToken: (botToken) =>
          Effect.sync(() => {
            calls.push(botToken)
            return result
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-verify-token-1',
        method: 'telegramOps.verifyToken',
        params: { botToken: '123456:test-token' }
      })
    )

    expect(response).toEqual({
      id: 'telegram-ops-verify-token-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual(['123456:test-token'])
  })

  it('validates telegramOps.verifyToken params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        getConfig: () => Effect.succeed(null),
        setConfig: () => Effect.succeed({ ok: true }),
        discoverChats: () => Effect.succeed([]),
        sendTestMessage: () => Effect.succeed({ ok: true }),
        startForwarding: () => Effect.succeed({ ok: true, status: inactiveTelegramStatus }),
        stopForwarding: () => Effect.succeed({ status: inactiveTelegramStatus }),
        getStatus: () => Effect.succeed(inactiveTelegramStatus),
        verifyToken: (botToken) =>
          Effect.sync(() => {
            calls.push(botToken)
            return { ok: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-verify-token-2',
        method: 'telegramOps.verifyToken',
        params: { botToken: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'telegram-ops-verify-token-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles telegramOps.discoverChats through the telegram ops RPC domain', async () => {
    const calls: Array<
      | {
          botToken: string
          chatId: number
          chatName: string
          contextSize: number
        }
      | null
      | undefined
    > = []
    const config = {
      botToken: '123456:test-token',
      chatId: 42,
      chatName: 'Hive',
      contextSize: 12
    }
    const chats = [{ chatId: 42, firstName: 'Hive', type: 'group' as const }]
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        getConfig: () => Effect.succeed(null),
        setConfig: () => Effect.succeed({ ok: true }),
        verifyToken: () => Effect.succeed({ ok: true }),
        sendTestMessage: () => Effect.succeed({ ok: true }),
        startForwarding: () => Effect.succeed({ ok: true, status: inactiveTelegramStatus }),
        stopForwarding: () => Effect.succeed({ status: inactiveTelegramStatus }),
        getStatus: () => Effect.succeed(inactiveTelegramStatus),
        discoverChats: (nextConfig) =>
          Effect.sync(() => {
            calls.push(nextConfig)
            return chats
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-discover-chats-1',
        method: 'telegramOps.discoverChats',
        params: { config }
      })
    )

    expect(response).toEqual({
      id: 'telegram-ops-discover-chats-1',
      ok: true,
      value: chats
    })
    expect(calls).toEqual([config])
  })

  it('allows telegramOps.discoverChats to use the stored config', async () => {
    const calls: Array<
      | {
          botToken: string
          chatId: number
          chatName: string
          contextSize: number
        }
      | null
      | undefined
    > = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        getConfig: () => Effect.succeed(null),
        setConfig: () => Effect.succeed({ ok: true }),
        verifyToken: () => Effect.succeed({ ok: true }),
        sendTestMessage: () => Effect.succeed({ ok: true }),
        startForwarding: () => Effect.succeed({ ok: true, status: inactiveTelegramStatus }),
        stopForwarding: () => Effect.succeed({ status: inactiveTelegramStatus }),
        getStatus: () => Effect.succeed(inactiveTelegramStatus),
        discoverChats: (config) =>
          Effect.sync(() => {
            calls.push(config)
            return []
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-discover-chats-2',
        method: 'telegramOps.discoverChats',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'telegram-ops-discover-chats-2',
      ok: true,
      value: []
    })
    expect(calls).toEqual([undefined])
  })

  it('validates telegramOps.discoverChats params', async () => {
    const calls: unknown[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        getConfig: () => Effect.succeed(null),
        setConfig: () => Effect.succeed({ ok: true }),
        verifyToken: () => Effect.succeed({ ok: true }),
        sendTestMessage: () => Effect.succeed({ ok: true }),
        startForwarding: () => Effect.succeed({ ok: true, status: inactiveTelegramStatus }),
        stopForwarding: () => Effect.succeed({ status: inactiveTelegramStatus }),
        getStatus: () => Effect.succeed(inactiveTelegramStatus),
        discoverChats: (config) =>
          Effect.sync(() => {
            calls.push(config)
            return []
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-discover-chats-3',
        method: 'telegramOps.discoverChats',
        params: {
          config: {
            botToken: '123456:test-token',
            chatId: '42',
            chatName: 'Hive',
            contextSize: 12
          }
        }
      })
    )

    expect(response).toMatchObject({
      id: 'telegram-ops-discover-chats-3',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles telegramOps.sendTestMessage through the telegram ops RPC domain', async () => {
    const calls: string[] = []
    const result = { ok: false, error: 'Telegram is not configured' }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        getConfig: () => Effect.succeed(null),
        setConfig: () => Effect.succeed({ ok: true }),
        verifyToken: () => Effect.succeed({ ok: true }),
        discoverChats: () => Effect.succeed([]),
        startForwarding: () => Effect.succeed({ ok: true, status: inactiveTelegramStatus }),
        stopForwarding: () => Effect.succeed({ status: inactiveTelegramStatus }),
        getStatus: () => Effect.succeed(inactiveTelegramStatus),
        sendTestMessage: () =>
          Effect.sync(() => {
            calls.push('sendTestMessage')
            return result
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-send-test-message-1',
        method: 'telegramOps.sendTestMessage',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'telegram-ops-send-test-message-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual(['sendTestMessage'])
  })

  it('validates telegramOps.sendTestMessage params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        getConfig: () => Effect.succeed(null),
        setConfig: () => Effect.succeed({ ok: true }),
        verifyToken: () => Effect.succeed({ ok: true }),
        discoverChats: () => Effect.succeed([]),
        startForwarding: () => Effect.succeed({ ok: true, status: inactiveTelegramStatus }),
        stopForwarding: () => Effect.succeed({ status: inactiveTelegramStatus }),
        getStatus: () => Effect.succeed(inactiveTelegramStatus),
        sendTestMessage: () =>
          Effect.sync(() => {
            calls.push('sendTestMessage')
            return { ok: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-send-test-message-2',
        method: 'telegramOps.sendTestMessage',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'telegram-ops-send-test-message-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles telegramOps.startForwarding through the telegram ops RPC domain', async () => {
    const calls: Array<{
      sessionId: string
      worktreeId: string | null
      connectionId: string | null
      mode: 'questions' | 'all'
    }> = []
    const status = {
      active: true,
      sessionId: 'session-1',
      worktreeId: 'worktree-1',
      connectionId: null,
      mode: 'questions' as const,
      health: 'ok' as const,
      lastError: null
    }
    const result = { ok: true, status }
    const params = {
      sessionId: 'session-1',
      worktreeId: 'worktree-1',
      connectionId: null,
      mode: 'questions' as const
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        getConfig: () => Effect.succeed(null),
        setConfig: () => Effect.succeed({ ok: true }),
        verifyToken: () => Effect.succeed({ ok: true }),
        discoverChats: () => Effect.succeed([]),
        sendTestMessage: () => Effect.succeed({ ok: true }),
        startForwarding: (nextParams) =>
          Effect.sync(() => {
            calls.push(nextParams)
            return result
          }),
        stopForwarding: () => Effect.succeed({ status: inactiveTelegramStatus }),
        getStatus: () => Effect.succeed(inactiveTelegramStatus)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-start-forwarding-1',
        method: 'telegramOps.startForwarding',
        params: { params }
      })
    )

    expect(response).toEqual({
      id: 'telegram-ops-start-forwarding-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual([params])
  })

  it('validates telegramOps.startForwarding params', async () => {
    const calls: unknown[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        getConfig: () => Effect.succeed(null),
        setConfig: () => Effect.succeed({ ok: true }),
        verifyToken: () => Effect.succeed({ ok: true }),
        discoverChats: () => Effect.succeed([]),
        sendTestMessage: () => Effect.succeed({ ok: true }),
        startForwarding: (params) =>
          Effect.sync(() => {
            calls.push(params)
            return { ok: true, status: inactiveTelegramStatus }
          }),
        stopForwarding: () => Effect.succeed({ status: inactiveTelegramStatus }),
        getStatus: () => Effect.succeed(inactiveTelegramStatus)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-start-forwarding-2',
        method: 'telegramOps.startForwarding',
        params: {
          params: {
            sessionId: '',
            worktreeId: 'worktree-1',
            connectionId: null,
            mode: 'questions'
          }
        }
      })
    )

    expect(response).toMatchObject({
      id: 'telegram-ops-start-forwarding-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles telegramOps.stopForwarding through the telegram ops RPC domain', async () => {
    const calls: string[] = []
    const status = {
      active: false,
      sessionId: null,
      worktreeId: null,
      connectionId: null,
      mode: null,
      health: 'ok' as const,
      lastError: null
    }
    const result = { status }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        getConfig: () => Effect.succeed(null),
        setConfig: () => Effect.succeed({ ok: true }),
        verifyToken: () => Effect.succeed({ ok: true }),
        discoverChats: () => Effect.succeed([]),
        sendTestMessage: () => Effect.succeed({ ok: true }),
        startForwarding: () => Effect.succeed({ ok: true, status: inactiveTelegramStatus }),
        stopForwarding: () =>
          Effect.sync(() => {
            calls.push('stopForwarding')
            return result
          }),
        getStatus: () => Effect.succeed(inactiveTelegramStatus)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-stop-forwarding-1',
        method: 'telegramOps.stopForwarding',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'telegram-ops-stop-forwarding-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual(['stopForwarding'])
  })

  it('validates telegramOps.stopForwarding params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        getConfig: () => Effect.succeed(null),
        setConfig: () => Effect.succeed({ ok: true }),
        verifyToken: () => Effect.succeed({ ok: true }),
        discoverChats: () => Effect.succeed([]),
        sendTestMessage: () => Effect.succeed({ ok: true }),
        startForwarding: () => Effect.succeed({ ok: true, status: inactiveTelegramStatus }),
        stopForwarding: () =>
          Effect.sync(() => {
            calls.push('stopForwarding')
            return { status: inactiveTelegramStatus }
          }),
        getStatus: () => Effect.succeed(inactiveTelegramStatus)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-stop-forwarding-2',
        method: 'telegramOps.stopForwarding',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'telegram-ops-stop-forwarding-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles telegramOps.getStatus through the telegram ops RPC domain', async () => {
    const calls: string[] = []
    const status = {
      active: true,
      sessionId: 'session-1',
      worktreeId: 'worktree-1',
      connectionId: null,
      mode: 'all' as const,
      health: 'ok' as const,
      lastError: null
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        getConfig: () => Effect.succeed(null),
        setConfig: () => Effect.succeed({ ok: true }),
        verifyToken: () => Effect.succeed({ ok: true }),
        discoverChats: () => Effect.succeed([]),
        sendTestMessage: () => Effect.succeed({ ok: true }),
        startForwarding: () => Effect.succeed({ ok: true, status: inactiveTelegramStatus }),
        stopForwarding: () => Effect.succeed({ status: inactiveTelegramStatus }),
        getStatus: () =>
          Effect.sync(() => {
            calls.push('getStatus')
            return status
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-get-status-1',
        method: 'telegramOps.getStatus',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'telegram-ops-get-status-1',
      ok: true,
      value: status
    })
    expect(calls).toEqual(['getStatus'])
  })

  it('validates telegramOps.getStatus params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      telegramOps: {
        getConfig: () => Effect.succeed(null),
        setConfig: () => Effect.succeed({ ok: true }),
        verifyToken: () => Effect.succeed({ ok: true }),
        discoverChats: () => Effect.succeed([]),
        sendTestMessage: () => Effect.succeed({ ok: true }),
        startForwarding: () => Effect.succeed({ ok: true, status: inactiveTelegramStatus }),
        stopForwarding: () => Effect.succeed({ status: inactiveTelegramStatus }),
        getStatus: () =>
          Effect.sync(() => {
            calls.push('getStatus')
            return inactiveTelegramStatus
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'telegram-ops-get-status-2',
        method: 'telegramOps.getStatus',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'telegram-ops-get-status-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles settingsOps.detectEditors through the settings ops RPC domain', async () => {
    const editors = [
      {
        id: 'vscode',
        name: 'Visual Studio Code',
        command: '/usr/bin/code',
        available: true
      }
    ]
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      settingsOps: {
        detectEditors: () =>
          Effect.sync(() => {
            calls.push('detectEditors')
            return editors
          }),
        detectTerminals: () => Effect.succeed([]),
        getAll: () => Effect.succeed({}),
        openWithEditor: () => Effect.succeed({ success: true }),
        openWithTerminal: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'settings-ops-detect-editors-1',
        method: 'settingsOps.detectEditors',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'settings-ops-detect-editors-1',
      ok: true,
      value: editors
    })
    expect(calls).toEqual(['detectEditors'])
  })

  it('validates settingsOps.detectEditors params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      settingsOps: {
        detectEditors: () => Effect.succeed([]),
        detectTerminals: () => Effect.succeed([]),
        getAll: () => Effect.succeed({}),
        openWithEditor: () => Effect.succeed({ success: true }),
        openWithTerminal: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'settings-ops-detect-editors-2',
        method: 'settingsOps.detectEditors',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'settings-ops-detect-editors-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles settingsOps.detectTerminals through the settings ops RPC domain', async () => {
    const terminals = [
      {
        id: 'terminal',
        name: 'Default Terminal',
        command: 'x-terminal-emulator',
        available: true
      }
    ]
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      settingsOps: {
        detectEditors: () => Effect.succeed([]),
        detectTerminals: () =>
          Effect.sync(() => {
            calls.push('detectTerminals')
            return terminals
          }),
        getAll: () => Effect.succeed({}),
        openWithEditor: () => Effect.succeed({ success: true }),
        openWithTerminal: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'settings-ops-detect-terminals-1',
        method: 'settingsOps.detectTerminals',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'settings-ops-detect-terminals-1',
      ok: true,
      value: terminals
    })
    expect(calls).toEqual(['detectTerminals'])
  })

  it('validates settingsOps.detectTerminals params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      settingsOps: {
        detectEditors: () => Effect.succeed([]),
        detectTerminals: () => Effect.succeed([]),
        getAll: () => Effect.succeed({}),
        openWithEditor: () => Effect.succeed({ success: true }),
        openWithTerminal: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'settings-ops-detect-terminals-2',
        method: 'settingsOps.detectTerminals',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'settings-ops-detect-terminals-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles settingsOps.openWithEditor through the settings ops RPC domain', async () => {
    const calls: Array<{ worktreePath: string; editorId: string; customCommand?: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      settingsOps: {
        detectEditors: () => Effect.succeed([]),
        detectTerminals: () => Effect.succeed([]),
        getAll: () => Effect.succeed({}),
        openWithEditor: (worktreePath, editorId, customCommand) =>
          Effect.sync(() => {
            calls.push({ worktreePath, editorId, customCommand })
            return { success: true }
          }),
        openWithTerminal: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'settings-ops-open-with-editor-1',
        method: 'settingsOps.openWithEditor',
        params: {
          worktreePath: '/tmp/hive',
          editorId: 'custom',
          customCommand: 'code'
        }
      })
    )

    expect(response).toEqual({
      id: 'settings-ops-open-with-editor-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([
      { worktreePath: '/tmp/hive', editorId: 'custom', customCommand: 'code' }
    ])
  })

  it('validates settingsOps.openWithEditor params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      settingsOps: {
        detectEditors: () => Effect.succeed([]),
        detectTerminals: () => Effect.succeed([]),
        getAll: () => Effect.succeed({}),
        openWithEditor: () => Effect.succeed({ success: false, error: 'should not run' }),
        openWithTerminal: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'settings-ops-open-with-editor-2',
        method: 'settingsOps.openWithEditor',
        params: { worktreePath: '', editorId: 'vscode' }
      })
    )

    expect(response).toMatchObject({
      id: 'settings-ops-open-with-editor-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles settingsOps.openWithTerminal through the settings ops RPC domain', async () => {
    const calls: Array<{ worktreePath: string; terminalId: string; customCommand?: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      settingsOps: {
        detectEditors: () => Effect.succeed([]),
        detectTerminals: () => Effect.succeed([]),
        getAll: () => Effect.succeed({}),
        openWithEditor: () => Effect.succeed({ success: true }),
        openWithTerminal: (worktreePath, terminalId, customCommand) =>
          Effect.sync(() => {
            calls.push({ worktreePath, terminalId, customCommand })
            return { success: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'settings-ops-open-with-terminal-1',
        method: 'settingsOps.openWithTerminal',
        params: {
          worktreePath: '/tmp/hive',
          terminalId: 'custom',
          customCommand: 'ghostty'
        }
      })
    )

    expect(response).toEqual({
      id: 'settings-ops-open-with-terminal-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([
      { worktreePath: '/tmp/hive', terminalId: 'custom', customCommand: 'ghostty' }
    ])
  })

  it('validates settingsOps.openWithTerminal params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      settingsOps: {
        detectEditors: () => Effect.succeed([]),
        detectTerminals: () => Effect.succeed([]),
        getAll: () => Effect.succeed({}),
        openWithEditor: () => Effect.succeed({ success: true }),
        openWithTerminal: () => Effect.succeed({ success: false, error: 'should not run' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'settings-ops-open-with-terminal-2',
        method: 'settingsOps.openWithTerminal',
        params: { worktreePath: '/tmp/hive', terminalId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'settings-ops-open-with-terminal-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles settingsOps.getAll through the settings ops RPC domain', async () => {
    const settings = {
      theme: 'dark',
      editor: 'vscode'
    }
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      settingsOps: {
        detectEditors: () => Effect.succeed([]),
        detectTerminals: () => Effect.succeed([]),
        getAll: () =>
          Effect.sync(() => {
            calls.push('getAll')
            return settings
          }),
        openWithEditor: () => Effect.succeed({ success: true }),
        openWithTerminal: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'settings-ops-get-all-1',
        method: 'settingsOps.getAll',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'settings-ops-get-all-1',
      ok: true,
      value: settings
    })
    expect(calls).toEqual(['getAll'])
  })

  it('validates settingsOps.getAll params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      settingsOps: {
        detectEditors: () => Effect.succeed([]),
        detectTerminals: () => Effect.succeed([]),
        getAll: () => Effect.succeed({}),
        openWithEditor: () => Effect.succeed({ success: true }),
        openWithTerminal: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'settings-ops-get-all-2',
        method: 'settingsOps.getAll',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'settings-ops-get-all-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles fileTreeOps.scan through the file tree ops RPC domain', async () => {
    const tree: FileTreeNode[] = [
      {
        name: 'src',
        path: '/tmp/hive/src',
        relativePath: 'src',
        isDirectory: true,
        extension: null,
        children: []
      }
    ]
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileTreeOps: {
        scan: (dirPath) =>
          Effect.sync(() => {
            calls.push(dirPath)
            return { success: true, tree }
          }),
        scanFlat: () => Effect.succeed({ success: true, files: [] }),
        loadChildren: () => Effect.succeed({ success: true, children: [] }),
        watch: () => Effect.succeed({ success: true }),
        unwatch: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-tree-ops-scan-1',
        method: 'fileTreeOps.scan',
        params: { dirPath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'file-tree-ops-scan-1',
      ok: true,
      value: { success: true, tree }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates fileTreeOps.scan params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileTreeOps: {
        scan: () => Effect.succeed({ success: true, tree: [] }),
        scanFlat: () => Effect.succeed({ success: true, files: [] }),
        loadChildren: () => Effect.succeed({ success: true, children: [] }),
        watch: () => Effect.succeed({ success: true }),
        unwatch: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-tree-ops-scan-2',
        method: 'fileTreeOps.scan',
        params: { dirPath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'file-tree-ops-scan-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles fileTreeOps.scanFlat through the file tree ops RPC domain', async () => {
    const files: FlatFile[] = [
      {
        name: 'App.tsx',
        path: '/tmp/hive/src/App.tsx',
        relativePath: 'src/App.tsx',
        extension: '.tsx'
      }
    ]
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileTreeOps: {
        scan: () => Effect.succeed({ success: true, tree: [] }),
        scanFlat: (dirPath) =>
          Effect.sync(() => {
            calls.push(dirPath)
            return { success: true, files }
          }),
        loadChildren: () => Effect.succeed({ success: true, children: [] }),
        watch: () => Effect.succeed({ success: true }),
        unwatch: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-tree-ops-scan-flat-1',
        method: 'fileTreeOps.scanFlat',
        params: { dirPath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'file-tree-ops-scan-flat-1',
      ok: true,
      value: { success: true, files }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates fileTreeOps.scanFlat params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileTreeOps: {
        scan: () => Effect.succeed({ success: true, tree: [] }),
        scanFlat: () => Effect.succeed({ success: true, files: [] }),
        loadChildren: () => Effect.succeed({ success: true, children: [] }),
        watch: () => Effect.succeed({ success: true }),
        unwatch: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-tree-ops-scan-flat-2',
        method: 'fileTreeOps.scanFlat',
        params: { dirPath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'file-tree-ops-scan-flat-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles fileTreeOps.loadChildren through the file tree ops RPC domain', async () => {
    const children: FileTreeNode[] = [
      {
        name: 'App.tsx',
        path: '/tmp/hive/src/App.tsx',
        relativePath: 'src/App.tsx',
        isDirectory: false,
        extension: '.tsx'
      }
    ]
    const calls: Array<{ dirPath: string; rootPath: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileTreeOps: {
        scan: () => Effect.succeed({ success: true, tree: [] }),
        scanFlat: () => Effect.succeed({ success: true, files: [] }),
        loadChildren: (dirPath, rootPath) =>
          Effect.sync(() => {
            calls.push({ dirPath, rootPath })
            return { success: true, children }
          }),
        watch: () => Effect.succeed({ success: true }),
        unwatch: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-tree-ops-load-children-1',
        method: 'fileTreeOps.loadChildren',
        params: { dirPath: '/tmp/hive/src', rootPath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'file-tree-ops-load-children-1',
      ok: true,
      value: { success: true, children }
    })
    expect(calls).toEqual([{ dirPath: '/tmp/hive/src', rootPath: '/tmp/hive' }])
  })

  it('validates fileTreeOps.loadChildren params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileTreeOps: {
        scan: () => Effect.succeed({ success: true, tree: [] }),
        scanFlat: () => Effect.succeed({ success: true, files: [] }),
        loadChildren: () => Effect.succeed({ success: true, children: [] }),
        watch: () => Effect.succeed({ success: true }),
        unwatch: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-tree-ops-load-children-2',
        method: 'fileTreeOps.loadChildren',
        params: { dirPath: '/tmp/hive/src', rootPath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'file-tree-ops-load-children-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles fileTreeOps.watch through the file tree ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileTreeOps: {
        scan: () => Effect.succeed({ success: true, tree: [] }),
        scanFlat: () => Effect.succeed({ success: true, files: [] }),
        loadChildren: () => Effect.succeed({ success: true, children: [] }),
        watch: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return { success: true }
          }),
        unwatch: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-tree-ops-watch-1',
        method: 'fileTreeOps.watch',
        params: { worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'file-tree-ops-watch-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates fileTreeOps.watch params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileTreeOps: {
        scan: () => Effect.succeed({ success: true, tree: [] }),
        scanFlat: () => Effect.succeed({ success: true, files: [] }),
        loadChildren: () => Effect.succeed({ success: true, children: [] }),
        watch: () => Effect.succeed({ success: true }),
        unwatch: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-tree-ops-watch-2',
        method: 'fileTreeOps.watch',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'file-tree-ops-watch-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles fileTreeOps.unwatch through the file tree ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileTreeOps: {
        scan: () => Effect.succeed({ success: true, tree: [] }),
        scanFlat: () => Effect.succeed({ success: true, files: [] }),
        loadChildren: () => Effect.succeed({ success: true, children: [] }),
        watch: () => Effect.succeed({ success: true }),
        unwatch: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return { success: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-tree-ops-unwatch-1',
        method: 'fileTreeOps.unwatch',
        params: { worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'file-tree-ops-unwatch-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates fileTreeOps.unwatch params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      fileTreeOps: {
        scan: () => Effect.succeed({ success: true, tree: [] }),
        scanFlat: () => Effect.succeed({ success: true, files: [] }),
        loadChildren: () => Effect.succeed({ success: true, children: [] }),
        watch: () => Effect.succeed({ success: true }),
        unwatch: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'file-tree-ops-unwatch-2',
        method: 'fileTreeOps.unwatch',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'file-tree-ops-unwatch-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.getFileStatuses through the git ops RPC domain', async () => {
    const files: GitFileStatus[] = [
      {
        path: '/tmp/hive/src/App.tsx',
        relativePath: 'src/App.tsx',
        status: 'M',
        staged: false
      }
    ]
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return { success: true, files }
          }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: '## Summary\n- Added RPC\n## Testing\n- Vitest'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-file-statuses-1',
        method: 'gitOps.getFileStatuses',
        params: { worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-file-statuses-1',
      ok: true,
      value: { success: true, files }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates gitOps.getFileStatuses params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-file-statuses-2',
        method: 'gitOps.getFileStatuses',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-file-statuses-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.stageFile through the git ops RPC domain', async () => {
    const calls: Array<{ worktreePath: string; filePath: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: (worktreePath, filePath) =>
          Effect.sync(() => {
            calls.push({ worktreePath, filePath })
            return { success: true }
          }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-stage-file-1',
        method: 'gitOps.stageFile',
        params: { worktreePath: '/tmp/hive', filePath: 'src/App.tsx' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-stage-file-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([{ worktreePath: '/tmp/hive', filePath: 'src/App.tsx' }])
  })

  it('validates gitOps.stageFile params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-stage-file-2',
        method: 'gitOps.stageFile',
        params: { worktreePath: '/tmp/hive', filePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-stage-file-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.unstageFile through the git ops RPC domain', async () => {
    const calls: Array<{ worktreePath: string; filePath: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: (worktreePath, filePath) =>
          Effect.sync(() => {
            calls.push({ worktreePath, filePath })
            return { success: true }
          }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-unstage-file-1',
        method: 'gitOps.unstageFile',
        params: { worktreePath: '/tmp/hive', filePath: 'src/App.tsx' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-unstage-file-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([{ worktreePath: '/tmp/hive', filePath: 'src/App.tsx' }])
  })

  it('validates gitOps.unstageFile params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-unstage-file-2',
        method: 'gitOps.unstageFile',
        params: { worktreePath: '/tmp/hive', filePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-unstage-file-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.discardChanges through the git ops RPC domain', async () => {
    const calls: Array<{ worktreePath: string; filePath: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: (worktreePath, filePath) =>
          Effect.sync(() => {
            calls.push({ worktreePath, filePath })
            return null
          }),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-discard-changes-1',
        method: 'gitOps.discardChanges',
        params: { worktreePath: '/tmp/hive', filePath: 'src/App.tsx' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-discard-changes-1',
      ok: true,
      value: null
    })
    expect(calls).toEqual([{ worktreePath: '/tmp/hive', filePath: 'src/App.tsx' }])
  })

  it('validates gitOps.discardChanges params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-discard-changes-2',
        method: 'gitOps.discardChanges',
        params: { worktreePath: '/tmp/hive', filePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-discard-changes-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.addToGitignore through the git ops RPC domain', async () => {
    const calls: Array<{ worktreePath: string; pattern: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: (worktreePath, pattern) =>
          Effect.sync(() => {
            calls.push({ worktreePath, pattern })
            return { success: true }
          }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-add-to-gitignore-1',
        method: 'gitOps.addToGitignore',
        params: { worktreePath: '/tmp/hive', pattern: 'dist/' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-add-to-gitignore-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([{ worktreePath: '/tmp/hive', pattern: 'dist/' }])
  })

  it('validates gitOps.addToGitignore params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-add-to-gitignore-2',
        method: 'gitOps.addToGitignore',
        params: { worktreePath: '/tmp/hive', pattern: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-add-to-gitignore-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.openInEditor through the git ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: (filePath) =>
          Effect.sync(() => {
            calls.push(filePath)
            return { success: true }
          }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-open-in-editor-1',
        method: 'gitOps.openInEditor',
        params: { filePath: '/tmp/hive/src/App.tsx' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-open-in-editor-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['/tmp/hive/src/App.tsx'])
  })

  it('validates gitOps.openInEditor params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-open-in-editor-2',
        method: 'gitOps.openInEditor',
        params: { filePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-open-in-editor-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.showInFinder through the git ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: (filePath) =>
          Effect.sync(() => {
            calls.push(filePath)
            return { success: true }
          }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-show-in-finder-1',
        method: 'gitOps.showInFinder',
        params: { filePath: '/tmp/hive/src/App.tsx' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-show-in-finder-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['/tmp/hive/src/App.tsx'])
  })

  it('validates gitOps.showInFinder params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-show-in-finder-2',
        method: 'gitOps.showInFinder',
        params: { filePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-show-in-finder-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.watchWorktree through the git ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return { success: true }
          }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-watch-worktree-1',
        method: 'gitOps.watchWorktree',
        params: { worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-watch-worktree-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates gitOps.watchWorktree params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-watch-worktree-2',
        method: 'gitOps.watchWorktree',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-watch-worktree-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.unwatchWorktree through the git ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return { success: true }
          }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-unwatch-worktree-1',
        method: 'gitOps.unwatchWorktree',
        params: { worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-unwatch-worktree-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates gitOps.unwatchWorktree params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-unwatch-worktree-2',
        method: 'gitOps.unwatchWorktree',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-unwatch-worktree-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.watchBranch through the git ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return { success: true }
          }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-watch-branch-1',
        method: 'gitOps.watchBranch',
        params: { worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-watch-branch-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates gitOps.watchBranch params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-watch-branch-2',
        method: 'gitOps.watchBranch',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-watch-branch-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.unwatchBranch through the git ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return { success: true }
          }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-unwatch-branch-1',
        method: 'gitOps.unwatchBranch',
        params: { worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-unwatch-branch-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates gitOps.unwatchBranch params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-unwatch-branch-2',
        method: 'gitOps.unwatchBranch',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-unwatch-branch-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.getBranchInfo through the git ops RPC domain', async () => {
    const calls: string[] = []
    const branch: GitBranchInfo = {
      name: 'main',
      tracking: 'origin/main',
      ahead: 1,
      behind: 2
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return { success: true, branch }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-branch-info-1',
        method: 'gitOps.getBranchInfo',
        params: { worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-branch-info-1',
      ok: true,
      value: { success: true, branch }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates gitOps.getBranchInfo params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-branch-info-2',
        method: 'gitOps.getBranchInfo',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-branch-info-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.stageAll through the git ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return { success: true }
          }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-stage-all-1',
        method: 'gitOps.stageAll',
        params: { worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-stage-all-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates gitOps.stageAll params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-stage-all-2',
        method: 'gitOps.stageAll',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-stage-all-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.unstageAll through the git ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return { success: true }
          }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-unstage-all-1',
        method: 'gitOps.unstageAll',
        params: { worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-unstage-all-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates gitOps.unstageAll params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-unstage-all-2',
        method: 'gitOps.unstageAll',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-unstage-all-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.stageHunk through the git ops RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        stageHunk: (worktreePath, patch) =>
          Effect.sync(() => {
            calls.push([worktreePath, patch])
            return { success: true }
          })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-stage-hunk-1',
        method: 'gitOps.stageHunk',
        params: { worktreePath: '/tmp/hive', patch: 'diff --git a/file b/file' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-stage-hunk-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([['/tmp/hive', 'diff --git a/file b/file']])
  })

  it('validates gitOps.stageHunk params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        stageHunk: () => Effect.succeed({ success: true })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-stage-hunk-2',
        method: 'gitOps.stageHunk',
        params: { worktreePath: '/tmp/hive', patch: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-stage-hunk-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.unstageHunk through the git ops RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        unstageHunk: (worktreePath, patch) =>
          Effect.sync(() => {
            calls.push([worktreePath, patch])
            return { success: true }
          })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-unstage-hunk-1',
        method: 'gitOps.unstageHunk',
        params: { worktreePath: '/tmp/hive', patch: 'diff --git a/file b/file' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-unstage-hunk-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([['/tmp/hive', 'diff --git a/file b/file']])
  })

  it('validates gitOps.unstageHunk params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        unstageHunk: () => Effect.succeed({ success: true })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-unstage-hunk-2',
        method: 'gitOps.unstageHunk',
        params: { worktreePath: '/tmp/hive', patch: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-unstage-hunk-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.revertHunk through the git ops RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        revertHunk: (worktreePath, patch) =>
          Effect.sync(() => {
            calls.push([worktreePath, patch])
            return { success: true }
          })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-revert-hunk-1',
        method: 'gitOps.revertHunk',
        params: { worktreePath: '/tmp/hive', patch: 'diff --git a/file b/file' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-revert-hunk-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([['/tmp/hive', 'diff --git a/file b/file']])
  })

  it('validates gitOps.revertHunk params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        revertHunk: () => Effect.succeed({ success: true })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-revert-hunk-2',
        method: 'gitOps.revertHunk',
        params: { worktreePath: '/tmp/hive', patch: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-revert-hunk-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.commit through the git ops RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: (worktreePath, message) =>
          Effect.sync(() => {
            calls.push([worktreePath, message])
            return { success: true, commitHash: 'abc123' }
          }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-commit-1',
        method: 'gitOps.commit',
        params: { worktreePath: '/tmp/hive', message: 'commit message' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-commit-1',
      ok: true,
      value: { success: true, commitHash: 'abc123' }
    })
    expect(calls).toEqual([['/tmp/hive', 'commit message']])
  })

  it('validates gitOps.commit params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-commit-2',
        method: 'gitOps.commit',
        params: { worktreePath: '', message: 'commit message' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-commit-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.push through the git ops RPC domain', async () => {
    const calls: Array<[string, string | undefined, string | undefined, boolean | undefined]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: (worktreePath, remote, branch, force) =>
          Effect.sync(() => {
            calls.push([worktreePath, remote, branch, force])
            return { success: true, pushed: true }
          }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-push-1',
        method: 'gitOps.push',
        params: { worktreePath: '/tmp/hive', remote: 'upstream', branch: 'feature', force: true }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-push-1',
      ok: true,
      value: { success: true, pushed: true }
    })
    expect(calls).toEqual([['/tmp/hive', 'upstream', 'feature', true]])
  })

  it('validates gitOps.push params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-push-2',
        method: 'gitOps.push',
        params: { worktreePath: '', remote: 'origin' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-push-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.pull through the git ops RPC domain', async () => {
    const calls: Array<[string, string | undefined, string | undefined, boolean | undefined]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: (worktreePath, remote, branch, rebase) =>
          Effect.sync(() => {
            calls.push([worktreePath, remote, branch, rebase])
            return { success: true, updated: true }
          }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-pull-1',
        method: 'gitOps.pull',
        params: { worktreePath: '/tmp/hive', remote: 'upstream', branch: 'feature', rebase: true }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-pull-1',
      ok: true,
      value: { success: true, updated: true }
    })
    expect(calls).toEqual([['/tmp/hive', 'upstream', 'feature', true]])
  })

  it('validates gitOps.pull params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-pull-2',
        method: 'gitOps.pull',
        params: { worktreePath: '', remote: 'origin' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-pull-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.merge through the git ops RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: (worktreePath, sourceBranch) =>
          Effect.sync(() => {
            calls.push([worktreePath, sourceBranch])
            return { success: true }
          }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-merge-1',
        method: 'gitOps.merge',
        params: { worktreePath: '/tmp/hive', sourceBranch: 'feature' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-merge-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([['/tmp/hive', 'feature']])
  })

  it('validates gitOps.merge params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-merge-2',
        method: 'gitOps.merge',
        params: { worktreePath: '/tmp/hive', sourceBranch: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-merge-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.mergeAbort through the git ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return { success: true }
          }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-merge-abort-1',
        method: 'gitOps.mergeAbort',
        params: { worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-merge-abort-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates gitOps.mergeAbort params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-merge-abort-2',
        method: 'gitOps.mergeAbort',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-merge-abort-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.hasUncommittedChanges through the git ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return true
          }),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-has-uncommitted-changes-1',
        method: 'gitOps.hasUncommittedChanges',
        params: { worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-has-uncommitted-changes-1',
      ok: true,
      value: true
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates gitOps.hasUncommittedChanges params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-has-uncommitted-changes-2',
        method: 'gitOps.hasUncommittedChanges',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-has-uncommitted-changes-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.branchDiffShortStat through the git ops RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: (worktreePath, baseBranch) =>
          Effect.sync(() => {
            calls.push([worktreePath, baseBranch])
            return {
              success: true,
              filesChanged: 2,
              insertions: 3,
              deletions: 1,
              commitsAhead: 1
            }
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-branch-diff-short-stat-1',
        method: 'gitOps.branchDiffShortStat',
        params: { worktreePath: '/tmp/hive', baseBranch: 'main' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-branch-diff-short-stat-1',
      ok: true,
      value: {
        success: true,
        filesChanged: 2,
        insertions: 3,
        deletions: 1,
        commitsAhead: 1
      }
    })
    expect(calls).toEqual([['/tmp/hive', 'main']])
  })

  it('validates gitOps.branchDiffShortStat params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-branch-diff-short-stat-2',
        method: 'gitOps.branchDiffShortStat',
        params: { worktreePath: '/tmp/hive', baseBranch: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-branch-diff-short-stat-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.getDiff through the git ops RPC domain', async () => {
    const calls: Array<[string, string, boolean, boolean, number | undefined]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getDiff: (worktreePath, filePath, staged, isUntracked, contextLines) =>
          Effect.sync(() => {
            calls.push([worktreePath, filePath, staged, isUntracked, contextLines])
            return {
              success: true,
              diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
              fileName: 'App.tsx'
            }
          })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-diff-1',
        method: 'gitOps.getDiff',
        params: {
          worktreePath: '/tmp/hive',
          filePath: 'src/App.tsx',
          staged: true,
          isUntracked: false,
          contextLines: 5
        }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-get-diff-1',
      ok: true,
      value: {
        success: true,
        diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
        fileName: 'App.tsx'
      }
    })
    expect(calls).toEqual([['/tmp/hive', 'src/App.tsx', true, false, 5]])
  })

  it('validates gitOps.getDiff params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getDiff: () => Effect.succeed({ success: true, diff: '', fileName: 'App.tsx' })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-diff-2',
        method: 'gitOps.getDiff',
        params: {
          worktreePath: '/tmp/hive',
          filePath: '',
          staged: true,
          isUntracked: false
        }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-get-diff-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.listBranchesWithStatus through the git ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        listBranchesWithStatus: (projectPath) =>
          Effect.sync(() => {
            calls.push(projectPath)
            return {
              success: true,
              branches: [
                { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
                { name: 'origin/main', isRemote: true, isCheckedOut: false }
              ]
            }
          })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-list-branches-with-status-1',
        method: 'gitOps.listBranchesWithStatus',
        params: { projectPath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-list-branches-with-status-1',
      ok: true,
      value: {
        success: true,
        branches: [
          { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
          { name: 'origin/main', isRemote: true, isCheckedOut: false }
        ]
      }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates gitOps.listBranchesWithStatus params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        listBranchesWithStatus: () => Effect.succeed({ success: true, branches: [] })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-list-branches-with-status-2',
        method: 'gitOps.listBranchesWithStatus',
        params: { projectPath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-list-branches-with-status-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.getFileContent through the git ops RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: (worktreePath, filePath) =>
          Effect.sync(() => {
            calls.push([worktreePath, filePath])
            return { success: true, content: 'export const value = 1\n' }
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-file-content-1',
        method: 'gitOps.getFileContent',
        params: { worktreePath: '/tmp/hive', filePath: 'src/App.tsx' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-get-file-content-1',
      ok: true,
      value: { success: true, content: 'export const value = 1\n' }
    })
    expect(calls).toEqual([['/tmp/hive', 'src/App.tsx']])
  })

  it('validates gitOps.getFileContent params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-file-content-2',
        method: 'gitOps.getFileContent',
        params: { worktreePath: '/tmp/hive', filePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-get-file-content-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.getFileContentBase64 through the git ops RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: (worktreePath, filePath) =>
          Effect.sync(() => {
            calls.push([worktreePath, filePath])
            return { success: true, data: 'aGVsbG8=', mimeType: 'image/png' }
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-file-content-base64-1',
        method: 'gitOps.getFileContentBase64',
        params: { worktreePath: '/tmp/hive', filePath: 'assets/icon.png' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-get-file-content-base64-1',
      ok: true,
      value: { success: true, data: 'aGVsbG8=', mimeType: 'image/png' }
    })
    expect(calls).toEqual([['/tmp/hive', 'assets/icon.png']])
  })

  it('validates gitOps.getFileContentBase64 params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-file-content-base64-2',
        method: 'gitOps.getFileContentBase64',
        params: { worktreePath: '/tmp/hive', filePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-get-file-content-base64-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.getRefContent through the git ops RPC domain', async () => {
    const calls: Array<[string, string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getRefContent: (worktreePath, ref, filePath) =>
          Effect.sync(() => {
            calls.push([worktreePath, ref, filePath])
            return { success: true, content: 'export const value = 1\n' }
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-ref-content-1',
        method: 'gitOps.getRefContent',
        params: { worktreePath: '/tmp/hive', ref: 'HEAD', filePath: 'src/App.tsx' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-get-ref-content-1',
      ok: true,
      value: { success: true, content: 'export const value = 1\n' }
    })
    expect(calls).toEqual([['/tmp/hive', 'HEAD', 'src/App.tsx']])
  })

  it('validates gitOps.getRefContent params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-ref-content-2',
        method: 'gitOps.getRefContent',
        params: { worktreePath: '/tmp/hive', ref: 'HEAD', filePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-get-ref-content-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.getBranchBaseContent through the git ops RPC domain', async () => {
    const calls: Array<[string, string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getBranchBaseContent: (worktreePath, branch, filePath) =>
          Effect.sync(() => {
            calls.push([worktreePath, branch, filePath])
            return { success: true, content: 'export const base = 1\n' }
          })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-branch-base-content-1',
        method: 'gitOps.getBranchBaseContent',
        params: { worktreePath: '/tmp/hive', branch: 'main', filePath: 'src/App.tsx' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-get-branch-base-content-1',
      ok: true,
      value: { success: true, content: 'export const base = 1\n' }
    })
    expect(calls).toEqual([['/tmp/hive', 'main', 'src/App.tsx']])
  })

  it('validates gitOps.getBranchBaseContent params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getBranchBaseContent: () =>
          Effect.succeed({ success: true, content: 'export const base = 1\n' })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-branch-base-content-2',
        method: 'gitOps.getBranchBaseContent',
        params: { worktreePath: '/tmp/hive', branch: '', filePath: 'src/App.tsx' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-get-branch-base-content-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.getBranchBaseContentBase64 through the git ops RPC domain', async () => {
    const calls: Array<[string, string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getBranchBaseContentBase64: (worktreePath, branch, filePath) =>
          Effect.sync(() => {
            calls.push([worktreePath, branch, filePath])
            return { success: true, data: 'YmFzZS1iaW5hcnk=', mimeType: 'image/png' }
          })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-branch-base-content-base64-1',
        method: 'gitOps.getBranchBaseContentBase64',
        params: { worktreePath: '/tmp/hive', branch: 'main', filePath: 'assets/icon.png' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-get-branch-base-content-base64-1',
      ok: true,
      value: { success: true, data: 'YmFzZS1iaW5hcnk=', mimeType: 'image/png' }
    })
    expect(calls).toEqual([['/tmp/hive', 'main', 'assets/icon.png']])
  })

  it('validates gitOps.getBranchBaseContentBase64 params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getBranchBaseContentBase64: () =>
          Effect.succeed({ success: true, data: 'YmFzZS1iaW5hcnk=', mimeType: 'image/png' })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-branch-base-content-base64-2',
        method: 'gitOps.getBranchBaseContentBase64',
        params: { worktreePath: '/tmp/hive', branch: 'main', filePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-get-branch-base-content-base64-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.getRefContentBase64 through the git ops RPC domain', async () => {
    const calls: Array<[string, string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: (worktreePath, ref, filePath) =>
          Effect.sync(() => {
            calls.push([worktreePath, ref, filePath])
            return { success: true, data: 'aGVsbG8=', mimeType: 'image/png' }
          })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-ref-content-base64-1',
        method: 'gitOps.getRefContentBase64',
        params: { worktreePath: '/tmp/hive', ref: 'HEAD', filePath: 'assets/icon.png' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-get-ref-content-base64-1',
      ok: true,
      value: { success: true, data: 'aGVsbG8=', mimeType: 'image/png' }
    })
    expect(calls).toEqual([['/tmp/hive', 'HEAD', 'assets/icon.png']])
  })

  it('validates gitOps.getRefContentBase64 params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () => Effect.succeed({ success: true, data: 'aGVsbG8=' })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-ref-content-base64-2',
        method: 'gitOps.getRefContentBase64',
        params: { worktreePath: '/tmp/hive', ref: 'HEAD', filePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-get-ref-content-base64-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.getRemoteUrl through the git ops RPC domain', async () => {
    const calls: Array<[string, string | undefined]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: (worktreePath, remote) =>
          Effect.sync(() => {
            calls.push([worktreePath, remote])
            return {
              success: true,
              url: 'https://github.com/example/hive.git',
              remote: remote || 'origin'
            }
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-remote-url-1',
        method: 'gitOps.getRemoteUrl',
        params: { worktreePath: '/tmp/hive', remote: 'upstream' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-get-remote-url-1',
      ok: true,
      value: {
        success: true,
        url: 'https://github.com/example/hive.git',
        remote: 'upstream'
      }
    })
    expect(calls).toEqual([['/tmp/hive', 'upstream']])
  })

  it('validates gitOps.getRemoteUrl params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-remote-url-2',
        method: 'gitOps.getRemoteUrl',
        params: { worktreePath: '', remote: 'origin' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-get-remote-url-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.getDiffStat through the git ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return {
              success: true,
              files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
            }
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-diff-stat-1',
        method: 'gitOps.getDiffStat',
        params: { worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-get-diff-stat-1',
      ok: true,
      value: {
        success: true,
        files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
      }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates gitOps.getDiffStat params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-diff-stat-2',
        method: 'gitOps.getDiffStat',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-get-diff-stat-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.getBranchDiffFiles through the git ops RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: (worktreePath, branch) =>
          Effect.sync(() => {
            calls.push([worktreePath, branch])
            return {
              success: true,
              files: [
                {
                  relativePath: 'src/App.tsx',
                  status: 'M',
                  additions: 3,
                  deletions: 1,
                  binary: false
                }
              ]
            }
          })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-branch-diff-files-1',
        method: 'gitOps.getBranchDiffFiles',
        params: { worktreePath: '/tmp/hive', branch: 'main' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-get-branch-diff-files-1',
      ok: true,
      value: {
        success: true,
        files: [
          { relativePath: 'src/App.tsx', status: 'M', additions: 3, deletions: 1, binary: false }
        ]
      }
    })
    expect(calls).toEqual([['/tmp/hive', 'main']])
  })

  it('validates gitOps.getBranchDiffFiles params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () => Effect.succeed({ success: true, files: [] })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-branch-diff-files-2',
        method: 'gitOps.getBranchDiffFiles',
        params: { worktreePath: '/tmp/hive', branch: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-get-branch-diff-files-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.getBranchFileDiff through the git ops RPC domain', async () => {
    const calls: Array<[string, string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getBranchFileDiff: (worktreePath, branch, filePath) =>
          Effect.sync(() => {
            calls.push([worktreePath, branch, filePath])
            return {
              success: true,
              diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
            }
          })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-branch-file-diff-1',
        method: 'gitOps.getBranchFileDiff',
        params: { worktreePath: '/tmp/hive', branch: 'main', filePath: 'src/App.tsx' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-get-branch-file-diff-1',
      ok: true,
      value: {
        success: true,
        diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
      }
    })
    expect(calls).toEqual([['/tmp/hive', 'main', 'src/App.tsx']])
  })

  it('validates gitOps.getBranchFileDiff params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getBranchFileDiff: () => Effect.succeed({ success: true, diff: '' })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-branch-file-diff-2',
        method: 'gitOps.getBranchFileDiff',
        params: { worktreePath: '/tmp/hive', branch: 'main', filePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-get-branch-file-diff-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.getRangeDiff through the git ops RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getRangeDiff: (worktreePath, baseBranch) =>
          Effect.sync(() => {
            calls.push([worktreePath, baseBranch])
            return {
              commitSummary: 'abc123 feature changes\n',
              diffSummary: ' tracked.txt | 2 +-\n',
              diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
              commitCount: 1
            }
          })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-range-diff-1',
        method: 'gitOps.getRangeDiff',
        params: { worktreePath: '/tmp/hive', baseBranch: 'main' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-get-range-diff-1',
      ok: true,
      value: {
        commitSummary: 'abc123 feature changes\n',
        diffSummary: ' tracked.txt | 2 +-\n',
        diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
        commitCount: 1
      }
    })
    expect(calls).toEqual([['/tmp/hive', 'main']])
  })

  it('validates gitOps.getRangeDiff params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getRangeDiff: () =>
          Effect.succeed({ commitSummary: '', diffSummary: '', diffPatch: '', commitCount: 0 })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-range-diff-2',
        method: 'gitOps.getRangeDiff',
        params: { worktreePath: '/tmp/hive', baseBranch: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-get-range-diff-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.needsPush through the git ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        needsPush: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return true
          })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-needs-push-1',
        method: 'gitOps.needsPush',
        params: { worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-needs-push-1',
      ok: true,
      value: true
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates gitOps.needsPush params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        needsPush: () => Effect.succeed(false)
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-needs-push-2',
        method: 'gitOps.needsPush',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-needs-push-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.createPR through the git ops RPC domain', async () => {
    const calls: Array<[string, string, string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        createPR: (worktreePath, baseBranch, title, body) =>
          Effect.sync(() => {
            calls.push([worktreePath, baseBranch, title, body])
            return {
              success: true,
              url: 'https://github.com/acme/hive/pull/42',
              number: 42
            }
          })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-create-pr-1',
        method: 'gitOps.createPR',
        params: {
          worktreePath: '/tmp/hive',
          baseBranch: 'origin/main',
          title: 'Add RPC',
          body: 'Body text'
        }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-create-pr-1',
      ok: true,
      value: {
        success: true,
        url: 'https://github.com/acme/hive/pull/42',
        number: 42
      }
    })
    expect(calls).toEqual([['/tmp/hive', 'origin/main', 'Add RPC', 'Body text']])
  })

  it('validates gitOps.createPR params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        createPR: () => Effect.succeed({ success: true })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-create-pr-2',
        method: 'gitOps.createPR',
        params: { worktreePath: '/tmp/hive', baseBranch: '', title: 'Add RPC', body: 'Body text' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-create-pr-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.generatePRContent through the git ops RPC domain', async () => {
    const calls: Array<[string, string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        generatePRContent: (worktreePath, baseBranch, provider) =>
          Effect.sync(() => {
            calls.push([worktreePath, baseBranch, provider])
            return {
              success: true,
              title: 'Add RPC',
              body: '## Summary\n- Added RPC\n## Testing\n- Vitest'
            }
          })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-generate-pr-content-1',
        method: 'gitOps.generatePRContent',
        params: { worktreePath: '/tmp/hive', baseBranch: 'origin/main', provider: 'codex' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-generate-pr-content-1',
      ok: true,
      value: {
        success: true,
        title: 'Add RPC',
        body: '## Summary\n- Added RPC\n## Testing\n- Vitest'
      }
    })
    expect(calls).toEqual([['/tmp/hive', 'origin/main', 'codex']])
  })

  it('validates gitOps.generatePRContent params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        generatePRContent: () => Effect.succeed({ success: true })
      } as GitOpsRpcService
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-generate-pr-content-2',
        method: 'gitOps.generatePRContent',
        params: { worktreePath: '/tmp/hive', baseBranch: 'main', provider: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-generate-pr-content-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.prMerge through the git ops RPC domain and publishes a status event', async () => {
    const calls: Array<[string, number]> = []
    const eventBus = makeEventBus()
    const events: Array<{ channel: string; payload: unknown }> = []
    await Effect.runPromise(
      eventBus.subscribeAll((event) => {
        events.push(event)
      })
    )
    const router = makeRpcRouter({
      eventBus,
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: (worktreePath, prNumber) =>
          Effect.sync(() => {
            calls.push([worktreePath, prNumber])
            return { success: true }
          }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-pr-merge-1',
        method: 'gitOps.prMerge',
        params: { worktreePath: '/tmp/hive', prNumber: 123 }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-pr-merge-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([['/tmp/hive', 123]])
    expect(events).toEqual([
      {
        channel: GIT_STATUS_CHANGED_CHANNEL,
        payload: { worktreePath: '/tmp/hive' }
      }
    ])
  })

  it('validates gitOps.prMerge params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-pr-merge-2',
        method: 'gitOps.prMerge',
        params: { worktreePath: '/tmp/hive', prNumber: '123' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-pr-merge-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.isBranchMerged through the git ops RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: (worktreePath, branch) =>
          Effect.sync(() => {
            calls.push([worktreePath, branch])
            return { success: true, isMerged: true }
          }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-is-branch-merged-1',
        method: 'gitOps.isBranchMerged',
        params: { worktreePath: '/tmp/hive', branch: 'feature' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-is-branch-merged-1',
      ok: true,
      value: { success: true, isMerged: true }
    })
    expect(calls).toEqual([['/tmp/hive', 'feature']])
  })

  it('validates gitOps.isBranchMerged params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-is-branch-merged-2',
        method: 'gitOps.isBranchMerged',
        params: { worktreePath: '/tmp/hive', branch: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-is-branch-merged-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.deleteBranch through the git ops RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: (worktreePath, branchName) =>
          Effect.sync(() => {
            calls.push([worktreePath, branchName])
            return { success: true }
          }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-delete-branch-1',
        method: 'gitOps.deleteBranch',
        params: { worktreePath: '/tmp/hive', branchName: 'feature' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-delete-branch-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([['/tmp/hive', 'feature']])
  })

  it('validates gitOps.deleteBranch params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-delete-branch-2',
        method: 'gitOps.deleteBranch',
        params: { worktreePath: '/tmp/hive', branchName: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-delete-branch-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.listPRs through the git ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: (projectPath) =>
          Effect.sync(() => {
            calls.push(projectPath)
            return {
              success: true,
              prs: [{ number: 123, title: 'Add RPC', author: 'mor', headRefName: 'feature/rpc' }]
            }
          }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-list-prs-1',
        method: 'gitOps.listPRs',
        params: { projectPath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-list-prs-1',
      ok: true,
      value: {
        success: true,
        prs: [{ number: 123, title: 'Add RPC', author: 'mor', headRefName: 'feature/rpc' }]
      }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates gitOps.listPRs params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-list-prs-2',
        method: 'gitOps.listPRs',
        params: { projectPath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-list-prs-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.getPRState through the git ops RPC domain', async () => {
    const calls: Array<[string, number]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: (projectPath, prNumber) =>
          Effect.sync(() => {
            calls.push([projectPath, prNumber])
            return { success: true, state: 'OPEN', title: 'Add RPC' }
          }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-pr-state-1',
        method: 'gitOps.getPRState',
        params: { projectPath: '/tmp/hive', prNumber: 123 }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-get-pr-state-1',
      ok: true,
      value: { success: true, state: 'OPEN', title: 'Add RPC' }
    })
    expect(calls).toEqual([['/tmp/hive', 123]])
  })

  it('validates gitOps.getPRState params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-pr-state-2',
        method: 'gitOps.getPRState',
        params: { projectPath: '/tmp/hive', prNumber: '123' }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-get-pr-state-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles gitOps.getPRReviewComments through the git ops RPC domain', async () => {
    const calls: Array<[string, number]> = []
    const comment = {
      id: 101,
      body: 'Looks good',
      bodyHTML: '<p>Looks good</p>',
      path: 'src/App.tsx',
      line: 10,
      originalLine: 9,
      side: 'RIGHT' as const,
      diffHunk: '@@ -1 +1 @@',
      user: { login: 'mor', avatarUrl: 'https://example.com/avatar.png' },
      createdAt: '2026-05-27T00:00:00Z',
      updatedAt: '2026-05-27T00:00:00Z',
      inReplyToId: null,
      pullRequestReviewId: 500,
      subjectType: 'line' as const
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: (projectPath, prNumber) =>
          Effect.sync(() => {
            calls.push([projectPath, prNumber])
            return { success: true, comments: [comment], baseBranch: 'main' }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-pr-review-comments-1',
        method: 'gitOps.getPRReviewComments',
        params: { projectPath: '/tmp/hive', prNumber: 123 }
      })
    )

    expect(response).toEqual({
      id: 'git-ops-get-pr-review-comments-1',
      ok: true,
      value: { success: true, comments: [comment], baseBranch: 'main' }
    })
    expect(calls).toEqual([['/tmp/hive', 123]])
  })

  it('validates gitOps.getPRReviewComments params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      gitOps: {
        getFileStatuses: () => Effect.succeed({ success: true, files: [] }),
        stageFile: () => Effect.succeed({ success: true }),
        unstageFile: () => Effect.succeed({ success: true }),
        discardChanges: () => Effect.succeed(null),
        addToGitignore: () => Effect.succeed({ success: true }),
        openInEditor: () => Effect.succeed({ success: true }),
        showInFinder: () => Effect.succeed({ success: true }),
        watchWorktree: () => Effect.succeed({ success: true }),
        unwatchWorktree: () => Effect.succeed({ success: true }),
        watchBranch: () => Effect.succeed({ success: true }),
        unwatchBranch: () => Effect.succeed({ success: true }),
        getBranchInfo: () =>
          Effect.succeed({
            success: true,
            branch: { name: 'main', tracking: null, ahead: 0, behind: 0 }
          }),
        stageAll: () => Effect.succeed({ success: true }),
        unstageAll: () => Effect.succeed({ success: true }),
        stageHunk: () => Effect.succeed({ success: true }),
        unstageHunk: () => Effect.succeed({ success: true }),
        revertHunk: () => Effect.succeed({ success: true }),
        commit: () => Effect.succeed({ success: true, commitHash: 'abc123' }),
        push: () => Effect.succeed({ success: true, pushed: true }),
        pull: () => Effect.succeed({ success: true, updated: true }),
        merge: () => Effect.succeed({ success: true }),
        mergeAbort: () => Effect.succeed({ success: true }),
        hasUncommittedChanges: () => Effect.succeed(false),
        branchDiffShortStat: () =>
          Effect.succeed({
            success: true,
            filesChanged: 2,
            insertions: 3,
            deletions: 1,
            commitsAhead: 1
          }),
        getFileContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getFileContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),

        getRefContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const value = 1\n'
          }),
        getBranchBaseContent: () =>
          Effect.succeed({
            success: true,
            content: 'export const base = 1\n'
          }),
        getRefContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'aGVsbG8=',
            mimeType: 'image/png'
          }),
        getBranchBaseContentBase64: () =>
          Effect.succeed({
            success: true,
            data: 'YmFzZS1iaW5hcnk=',
            mimeType: 'image/png'
          }),
        getRemoteUrl: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/example/hive.git',
            remote: 'origin'
          }),
        getDiffStat: () =>
          Effect.succeed({
            success: true,
            files: [{ path: 'src/App.tsx', additions: 3, deletions: 1, binary: false }]
          }),
        getDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n',
            fileName: 'App.tsx'
          }),
        listBranchesWithStatus: () =>
          Effect.succeed({
            success: true,
            branches: [
              { name: 'main', isRemote: false, isCheckedOut: true, worktreePath: '/tmp/hive' },
              { name: 'origin/main', isRemote: true, isCheckedOut: false }
            ]
          }),
        getBranchDiffFiles: () =>
          Effect.succeed({
            success: true,
            files: [
              {
                relativePath: 'src/App.tsx',
                status: 'M',
                additions: 3,
                deletions: 1,
                binary: false
              }
            ]
          }),
        getBranchFileDiff: () =>
          Effect.succeed({
            success: true,
            diff: 'diff --git a/src/App.tsx b/src/App.tsx\n'
          }),
        getRangeDiff: () =>
          Effect.succeed({
            commitSummary: 'abc123 feature changes\n',
            diffSummary: ' tracked.txt | 2 +-\n',
            diffPatch: 'diff --git a/tracked.txt b/tracked.txt\n',
            commitCount: 1
          }),
        needsPush: () => Effect.succeed(true),
        createPR: () =>
          Effect.succeed({
            success: true,
            url: 'https://github.com/acme/hive/pull/42',
            number: 42
          }),
        generatePRContent: () =>
          Effect.succeed({
            success: true,
            title: 'Add RPC',
            body: 'Generated body'
          }),
        prMerge: () => Effect.succeed({ success: true }),
        isBranchMerged: () => Effect.succeed({ success: true, isMerged: true }),
        deleteBranch: () => Effect.succeed({ success: true }),
        listPRs: () => Effect.succeed({ success: true, prs: [] }),
        getPRState: () => Effect.succeed({ success: true, state: 'OPEN', title: 'Add RPC' }),
        getPRReviewComments: () =>
          Effect.succeed({ success: true, comments: [], baseBranch: 'main' })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'git-ops-get-pr-review-comments-2',
        method: 'gitOps.getPRReviewComments',
        params: { projectPath: '', prNumber: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'git-ops-get-pr-review-comments-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles systemOps.getLogDir through the system ops RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({ id: 'system-ops-log-dir-1', method: 'systemOps.getLogDir', params: {} })
    )

    expect(response).toEqual({
      id: 'system-ops-log-dir-1',
      ok: true,
      value: '/tmp/hive-logs'
    })
  })

  it('validates systemOps.getLogDir params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-log-dir-2',
        method: 'systemOps.getLogDir',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'system-ops-log-dir-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles systemOps.getAppVersion through the system ops RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-app-version-1',
        method: 'systemOps.getAppVersion',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'system-ops-app-version-1',
      ok: true,
      value: '1.1.10'
    })
  })

  it('validates systemOps.getAppVersion params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-app-version-2',
        method: 'systemOps.getAppVersion',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'system-ops-app-version-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles systemOps.getAppPaths through the system ops RPC domain', async () => {
    const appPaths = {
      userData: '/tmp/hive-user-data',
      home: '/tmp',
      logs: '/tmp/hive-logs'
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () => Effect.succeed(appPaths),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-app-paths-1',
        method: 'systemOps.getAppPaths',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'system-ops-app-paths-1',
      ok: true,
      value: appPaths
    })
  })

  it('validates systemOps.getAppPaths params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-app-paths-2',
        method: 'systemOps.getAppPaths',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'system-ops-app-paths-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles systemOps.isLogMode through the system ops RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-is-log-mode-1',
        method: 'systemOps.isLogMode',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'system-ops-is-log-mode-1',
      ok: true,
      value: true
    })
  })

  it('validates systemOps.isLogMode params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-is-log-mode-2',
        method: 'systemOps.isLogMode',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'system-ops-is-log-mode-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles systemOps.detectAgentSdks through the system ops RPC domain', async () => {
    const detected = { opencode: true, claude: false, codex: true }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed(detected),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-detect-agent-sdks-1',
        method: 'systemOps.detectAgentSdks',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'system-ops-detect-agent-sdks-1',
      ok: true,
      value: detected
    })
  })

  it('validates systemOps.detectAgentSdks params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-detect-agent-sdks-2',
        method: 'systemOps.detectAgentSdks',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'system-ops-detect-agent-sdks-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles systemOps.quitApp through the system ops RPC domain', async () => {
    let quitCalled = false
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () =>
          Effect.sync(() => {
            quitCalled = true
          }),
        openInApp: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-quit-app-1',
        method: 'systemOps.quitApp',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'system-ops-quit-app-1',
      ok: true,
      value: undefined
    })
    expect(quitCalled).toBe(true)
  })

  it('validates systemOps.quitApp params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-quit-app-2',
        method: 'systemOps.quitApp',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'system-ops-quit-app-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles systemOps.confirm through the system ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        confirm: (message) =>
          Effect.sync(() => {
            calls.push(message)
            return true
          }),
        openInApp: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-confirm-1',
        method: 'systemOps.confirm',
        params: { message: 'Discard changes?' }
      })
    )

    expect(response).toEqual({
      id: 'system-ops-confirm-1',
      ok: true,
      value: true
    })
    expect(calls).toEqual(['Discard changes?'])
  })

  it('validates systemOps.confirm params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        confirm: () => Effect.succeed(true),
        openInApp: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-confirm-2',
        method: 'systemOps.confirm',
        params: {}
      })
    )

    expect(response).toMatchObject({
      id: 'system-ops-confirm-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles systemOps.openInApp through the system ops RPC domain', async () => {
    const calls: Array<{ appName: string; path: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: (appName, path) =>
          Effect.sync(() => {
            calls.push({ appName, path })
            return { success: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-open-in-app-1',
        method: 'systemOps.openInApp',
        params: { appName: 'cursor', path: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'system-ops-open-in-app-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([{ appName: 'cursor', path: '/tmp/hive' }])
  })

  it('validates systemOps.openInApp params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-open-in-app-2',
        method: 'systemOps.openInApp',
        params: { appName: 'cursor' }
      })
    )

    expect(response).toMatchObject({
      id: 'system-ops-open-in-app-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('returns a validation error for malformed requests', async () => {
    const response = await Effect.runPromise(makeRouter().handle({ id: '1', params: {} }))

    expect(response).toMatchObject({
      id: '1',
      ok: false,
      error: { code: 'INVALID_REQUEST' }
    })
  })

  it('returns method-not-found for unknown methods', async () => {
    const response = await Effect.runPromise(
      makeRouter().handle({ id: '2', method: 'missing.method', params: {} })
    )

    expect(response).toEqual({
      id: '2',
      ok: false,
      error: {
        code: 'METHOD_NOT_FOUND',
        message: 'Unknown RPC method: missing.method'
      }
    })
  })

  it('handles opencodeOps.connect through the opencode RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: (worktreePath, hiveSessionId) =>
          Effect.sync(() => {
            calls.push([worktreePath, hiveSessionId])
            return { success: true, sessionId: 'oc-session-1' }
          }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-connect-1',
        method: 'opencodeOps.connect',
        params: { worktreePath: '/tmp/hive', hiveSessionId: 'hive-session-1' }
      })
    )

    expect(response).toEqual({
      id: 'opencode-connect-1',
      ok: true,
      value: { success: true, sessionId: 'oc-session-1' }
    })
    expect(calls).toEqual([['/tmp/hive', 'hive-session-1']])
  })

  it('validates opencodeOps.connect params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: true, sessionId: 'unused' }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-connect-2',
        method: 'opencodeOps.connect',
        params: { worktreePath: '/tmp/hive', hiveSessionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-connect-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.reconnect through the opencode RPC domain', async () => {
    const calls: Array<[string, string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: (worktreePath, opencodeSessionId, hiveSessionId) =>
          Effect.sync(() => {
            calls.push([worktreePath, opencodeSessionId, hiveSessionId])
            return { success: true, sessionStatus: 'busy' as const, revertMessageID: null }
          }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-reconnect-1',
        method: 'opencodeOps.reconnect',
        params: {
          worktreePath: '/tmp/hive',
          opencodeSessionId: 'oc-session-1',
          hiveSessionId: 'hive-session-1'
        }
      })
    )

    expect(response).toEqual({
      id: 'opencode-reconnect-1',
      ok: true,
      value: { success: true, sessionStatus: 'busy', revertMessageID: null }
    })
    expect(calls).toEqual([['/tmp/hive', 'oc-session-1', 'hive-session-1']])
  })

  it('validates opencodeOps.reconnect params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: true, sessionStatus: 'idle' as const }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-reconnect-2',
        method: 'opencodeOps.reconnect',
        params: {
          worktreePath: '/tmp/hive',
          opencodeSessionId: '',
          hiveSessionId: 'hive-session-1'
        }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-reconnect-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.prompt through the opencode RPC domain', async () => {
    const calls: Array<[string, string, unknown, unknown, unknown]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: (worktreePath, opencodeSessionId, messageOrParts, model, options) =>
          Effect.sync(() => {
            calls.push([worktreePath, opencodeSessionId, messageOrParts, model, options])
            return { success: true }
          }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })
    const model = { providerID: 'anthropic', modelID: 'claude-sonnet', variant: 'latest' }
    const options = { codexFastMode: true }

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-prompt-1',
        method: 'opencodeOps.prompt',
        params: {
          worktreePath: '/tmp/hive',
          opencodeSessionId: 'oc-session-1',
          messageOrParts: [{ type: 'text', text: 'hello' }],
          model,
          options
        }
      })
    )

    expect(response).toEqual({
      id: 'opencode-prompt-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([
      ['/tmp/hive', 'oc-session-1', [{ type: 'text', text: 'hello' }], model, options]
    ])
  })

  it('validates opencodeOps.prompt params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: true }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-prompt-2',
        method: 'opencodeOps.prompt',
        params: {
          worktreePath: '/tmp/hive',
          opencodeSessionId: '',
          messageOrParts: [{ type: 'text', text: 'hello' }]
        }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-prompt-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.abort through the opencode RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: (worktreePath, opencodeSessionId) =>
          Effect.sync(() => {
            calls.push([worktreePath, opencodeSessionId])
            return { success: true }
          }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-abort-1',
        method: 'opencodeOps.abort',
        params: { worktreePath: '/tmp/hive', opencodeSessionId: 'oc-session-1' }
      })
    )

    expect(response).toEqual({
      id: 'opencode-abort-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([['/tmp/hive', 'oc-session-1']])
  })

  it('validates opencodeOps.abort params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: true }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-abort-2',
        method: 'opencodeOps.abort',
        params: { worktreePath: '/tmp/hive', opencodeSessionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-abort-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.steer through the opencode RPC domain', async () => {
    const calls: Array<[string, string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: (worktreePath, opencodeSessionId, message) =>
          Effect.sync(() => {
            calls.push([worktreePath, opencodeSessionId, message])
            return {
              success: true,
              insertedMessageId: 'msg-inserted',
              nextAssistantMessageId: 'msg-next',
              turnId: 'turn-1'
            }
          }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-steer-1',
        method: 'opencodeOps.steer',
        params: {
          worktreePath: '/tmp/hive',
          opencodeSessionId: 'oc-session-1',
          message: 'continue'
        }
      })
    )

    expect(response).toEqual({
      id: 'opencode-steer-1',
      ok: true,
      value: {
        success: true,
        insertedMessageId: 'msg-inserted',
        nextAssistantMessageId: 'msg-next',
        turnId: 'turn-1'
      }
    })
    expect(calls).toEqual([['/tmp/hive', 'oc-session-1', 'continue']])
  })

  it('validates opencodeOps.steer params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-steer-2',
        method: 'opencodeOps.steer',
        params: {
          worktreePath: '/tmp/hive',
          opencodeSessionId: '',
          message: 'continue'
        }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-steer-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.disconnect through the opencode RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: (worktreePath, opencodeSessionId) =>
          Effect.sync(() => {
            calls.push([worktreePath, opencodeSessionId])
            return { success: true }
          }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-disconnect-1',
        method: 'opencodeOps.disconnect',
        params: { worktreePath: '/tmp/hive', opencodeSessionId: 'oc-session-1' }
      })
    )

    expect(response).toEqual({
      id: 'opencode-disconnect-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([['/tmp/hive', 'oc-session-1']])
  })

  it('validates opencodeOps.disconnect params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: true }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-disconnect-2',
        method: 'opencodeOps.disconnect',
        params: { worktreePath: '/tmp/hive', opencodeSessionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-disconnect-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.getMessages through the opencode RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: (worktreePath, opencodeSessionId) =>
          Effect.sync(() => {
            calls.push([worktreePath, opencodeSessionId])
            return { success: true, messages: [{ id: 'msg-1' }] }
          }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-get-messages-1',
        method: 'opencodeOps.getMessages',
        params: { worktreePath: '/tmp/hive', opencodeSessionId: 'oc-session-1' }
      })
    )

    expect(response).toEqual({
      id: 'opencode-get-messages-1',
      ok: true,
      value: { success: true, messages: [{ id: 'msg-1' }] }
    })
    expect(calls).toEqual([['/tmp/hive', 'oc-session-1']])
  })

  it('validates opencodeOps.getMessages params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: true, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-get-messages-2',
        method: 'opencodeOps.getMessages',
        params: { worktreePath: '/tmp/hive', opencodeSessionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-get-messages-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.refreshFromThread through the opencode RPC domain', async () => {
    const calls: Array<[string, string]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: (worktreePath, opencodeSessionId) =>
          Effect.sync(() => {
            calls.push([worktreePath, opencodeSessionId])
            return { success: true, count: 2 }
          }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-refresh-from-thread-1',
        method: 'opencodeOps.refreshFromThread',
        params: { worktreePath: '/tmp/hive', opencodeSessionId: 'oc-session-1' }
      })
    )

    expect(response).toEqual({
      id: 'opencode-refresh-from-thread-1',
      ok: true,
      value: { success: true, count: 2 }
    })
    expect(calls).toEqual([['/tmp/hive', 'oc-session-1']])
  })

  it('validates opencodeOps.refreshFromThread params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: true }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-refresh-from-thread-2',
        method: 'opencodeOps.refreshFromThread',
        params: { worktreePath: '/tmp/hive', opencodeSessionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-refresh-from-thread-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.listModels through the opencode RPC domain', async () => {
    const calls: Array<string | undefined> = []
    const providers = [{ id: 'codex', models: { 'gpt-5': { id: 'gpt-5' } } }]
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: (opts) =>
          Effect.sync(() => {
            calls.push(opts?.agentSdk)
            return { success: true, providers }
          }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-list-models-1',
        method: 'opencodeOps.listModels',
        params: { agentSdk: 'codex' }
      })
    )

    expect(response).toEqual({
      id: 'opencode-list-models-1',
      ok: true,
      value: { success: true, providers }
    })
    expect(calls).toEqual(['codex'])
  })

  it('validates opencodeOps.listModels params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: true, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-list-models-2',
        method: 'opencodeOps.listModels',
        params: { agentSdk: 'missing-sdk' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-list-models-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.setModel through the opencode RPC domain', async () => {
    const calls: unknown[] = []
    const model = {
      providerID: 'openai',
      modelID: 'gpt-5',
      variant: 'high',
      agentSdk: 'codex' as const
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: (nextModel) =>
          Effect.sync(() => {
            calls.push(nextModel)
            return { success: true }
          }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-set-model-1',
        method: 'opencodeOps.setModel',
        params: model
      })
    )

    expect(response).toEqual({
      id: 'opencode-set-model-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([model])
  })

  it('validates opencodeOps.setModel params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: true }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-set-model-2',
        method: 'opencodeOps.setModel',
        params: { providerID: 'openai', modelID: 'gpt-5', agentSdk: 'missing-sdk' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-set-model-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.modelInfo through the opencode RPC domain', async () => {
    const calls: Array<[string, string, string | undefined]> = []
    const model = {
      id: 'gpt-5',
      name: 'GPT-5',
      limit: { context: 400000, output: 128000 }
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: (worktreePath, modelId, agentSdk) =>
          Effect.sync(() => {
            calls.push([worktreePath, modelId, agentSdk])
            return { success: true, model }
          }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-model-info-1',
        method: 'opencodeOps.modelInfo',
        params: { worktreePath: '/tmp/hive', modelId: 'gpt-5', agentSdk: 'codex' }
      })
    )

    expect(response).toEqual({
      id: 'opencode-model-info-1',
      ok: true,
      value: { success: true, model }
    })
    expect(calls).toEqual([['/tmp/hive', 'gpt-5', 'codex']])
  })

  it('validates opencodeOps.modelInfo params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: true }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-model-info-2',
        method: 'opencodeOps.modelInfo',
        params: { worktreePath: '/tmp/hive', modelId: '', agentSdk: 'codex' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-model-info-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.questionReply through the opencode RPC domain', async () => {
    const calls: Array<[string, string[][], string | undefined]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: (requestId, answers, worktreePath) =>
          Effect.sync(() => {
            calls.push([requestId, answers, worktreePath])
            return { success: true }
          }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-question-reply-1',
        method: 'opencodeOps.questionReply',
        params: {
          requestId: 'question-1',
          answers: [['yes']],
          worktreePath: '/tmp/hive'
        }
      })
    )

    expect(response).toEqual({
      id: 'opencode-question-reply-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([['question-1', [['yes']], '/tmp/hive']])
  })

  it('validates opencodeOps.questionReply params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: true }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-question-reply-2',
        method: 'opencodeOps.questionReply',
        params: { requestId: '', answers: [['yes']], worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-question-reply-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.questionReject through the opencode RPC domain', async () => {
    const calls: Array<[string, string | undefined]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: (requestId, worktreePath) =>
          Effect.sync(() => {
            calls.push([requestId, worktreePath])
            return { success: true }
          }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-question-reject-1',
        method: 'opencodeOps.questionReject',
        params: {
          requestId: 'question-1',
          worktreePath: '/tmp/hive'
        }
      })
    )

    expect(response).toEqual({
      id: 'opencode-question-reject-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([['question-1', '/tmp/hive']])
  })

  it('validates opencodeOps.questionReject params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: true }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-question-reject-2',
        method: 'opencodeOps.questionReject',
        params: { requestId: '', worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-question-reject-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.planApprove through the opencode RPC domain', async () => {
    const calls: Array<[string, string, string | undefined]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: (worktreePath, hiveSessionId, requestId) =>
          Effect.sync(() => {
            calls.push([worktreePath, hiveSessionId, requestId])
            return { success: true }
          }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-plan-approve-1',
        method: 'opencodeOps.planApprove',
        params: {
          worktreePath: '/tmp/hive',
          hiveSessionId: 'hive-session-1',
          requestId: 'plan-1'
        }
      })
    )

    expect(response).toEqual({
      id: 'opencode-plan-approve-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([['/tmp/hive', 'hive-session-1', 'plan-1']])
  })

  it('validates opencodeOps.planApprove params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: true }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-plan-approve-2',
        method: 'opencodeOps.planApprove',
        params: { worktreePath: '', hiveSessionId: 'hive-session-1', requestId: 'plan-1' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-plan-approve-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.planReject through the opencode RPC domain', async () => {
    const calls: Array<[string, string, string, string | undefined]> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: (worktreePath, hiveSessionId, feedback, requestId) =>
          Effect.sync(() => {
            calls.push([worktreePath, hiveSessionId, feedback, requestId])
            return { success: true }
          }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-plan-reject-1',
        method: 'opencodeOps.planReject',
        params: {
          worktreePath: '/tmp/hive',
          hiveSessionId: 'hive-session-1',
          feedback: 'Needs more detail',
          requestId: 'plan-1'
        }
      })
    )

    expect(response).toEqual({
      id: 'opencode-plan-reject-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([['/tmp/hive', 'hive-session-1', 'Needs more detail', 'plan-1']])
  })

  it('validates opencodeOps.planReject params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: true }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-plan-reject-2',
        method: 'opencodeOps.planReject',
        params: {
          worktreePath: '',
          hiveSessionId: 'hive-session-1',
          feedback: 'Needs more detail',
          requestId: 'plan-1'
        }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-plan-reject-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.permissionReply through the opencode RPC domain', async () => {
    const calls: Array<
      [string, 'once' | 'always' | 'reject', string | undefined, string | undefined]
    > = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: (requestId, reply, worktreePath, message) =>
          Effect.sync(() => {
            calls.push([requestId, reply, worktreePath, message])
            return { success: true }
          }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-permission-reply-1',
        method: 'opencodeOps.permissionReply',
        params: {
          requestId: 'permission-1',
          reply: 'once',
          worktreePath: '/tmp/hive',
          message: 'Approved for this run'
        }
      })
    )

    expect(response).toEqual({
      id: 'opencode-permission-reply-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([['permission-1', 'once', '/tmp/hive', 'Approved for this run']])
  })

  it('validates opencodeOps.permissionReply params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: true }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-permission-reply-2',
        method: 'opencodeOps.permissionReply',
        params: { requestId: 'permission-1', reply: 'later', worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-permission-reply-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.permissionList through the opencode RPC domain', async () => {
    const calls: Array<string | undefined> = []
    const permissions = [{ id: 'permission-1' }]
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return { success: true, permissions }
          }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-permission-list-1',
        method: 'opencodeOps.permissionList',
        params: { worktreePath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'opencode-permission-list-1',
      ok: true,
      value: { success: true, permissions }
    })
    expect(calls).toEqual(['/tmp/hive'])
  })

  it('validates opencodeOps.permissionList params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: true, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-permission-list-2',
        method: 'opencodeOps.permissionList',
        params: { worktreePath: 42 }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-permission-list-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.commandApprovalReply through the opencode RPC domain', async () => {
    const calls: Array<{
      requestId: string
      approved: boolean
      remember?: 'allow' | 'block'
      pattern?: string
      worktreePath?: string
      patterns?: string[]
    }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: (requestId, approved, remember, pattern, worktreePath, patterns) =>
          Effect.sync(() => {
            calls.push({ requestId, approved, remember, pattern, worktreePath, patterns })
            return { success: true }
          }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-command-approval-reply-1',
        method: 'opencodeOps.commandApprovalReply',
        params: {
          requestId: 'approval-1',
          approved: true,
          remember: 'allow',
          pattern: 'npm test',
          worktreePath: '/tmp/hive',
          patterns: ['npm test', 'pnpm test']
        }
      })
    )

    expect(response).toEqual({
      id: 'opencode-command-approval-reply-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([
      {
        requestId: 'approval-1',
        approved: true,
        remember: 'allow',
        pattern: 'npm test',
        worktreePath: '/tmp/hive',
        patterns: ['npm test', 'pnpm test']
      }
    ])
  })

  it('validates opencodeOps.commandApprovalReply params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: true }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-command-approval-reply-2',
        method: 'opencodeOps.commandApprovalReply',
        params: { requestId: 'approval-1', approved: true, remember: 'later' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-command-approval-reply-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.sessionInfo through the opencode RPC domain', async () => {
    const calls: Array<{ worktreePath: string; opencodeSessionId: string }> = []
    const result = { success: true, revertMessageID: 'msg-2', revertDiff: 'diff --git' }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: (worktreePath, opencodeSessionId) =>
          Effect.sync(() => {
            calls.push({ worktreePath, opencodeSessionId })
            return result
          }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-session-info-1',
        method: 'opencodeOps.sessionInfo',
        params: { worktreePath: '/tmp/hive', opencodeSessionId: 'session-1' }
      })
    )

    expect(response).toEqual({
      id: 'opencode-session-info-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual([{ worktreePath: '/tmp/hive', opencodeSessionId: 'session-1' }])
  })

  it('validates opencodeOps.sessionInfo params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: true }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-session-info-2',
        method: 'opencodeOps.sessionInfo',
        params: { worktreePath: '/tmp/hive', opencodeSessionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-session-info-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.undo through the opencode RPC domain', async () => {
    const calls: Array<{ worktreePath: string; opencodeSessionId: string }> = []
    const result = {
      success: true,
      revertMessageID: 'msg-2',
      restoredPrompt: 'please change it',
      revertDiff: 'diff --git'
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: (worktreePath, opencodeSessionId) =>
          Effect.sync(() => {
            calls.push({ worktreePath, opencodeSessionId })
            return result
          }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-undo-1',
        method: 'opencodeOps.undo',
        params: { worktreePath: '/tmp/hive', opencodeSessionId: 'session-1' }
      })
    )

    expect(response).toEqual({
      id: 'opencode-undo-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual([{ worktreePath: '/tmp/hive', opencodeSessionId: 'session-1' }])
  })

  it('validates opencodeOps.undo params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: true }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-undo-2',
        method: 'opencodeOps.undo',
        params: { worktreePath: '', opencodeSessionId: 'session-1' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-undo-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.redo through the opencode RPC domain', async () => {
    const calls: Array<{ worktreePath: string; opencodeSessionId: string }> = []
    const result = { success: true, revertMessageID: null }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: (worktreePath, opencodeSessionId) =>
          Effect.sync(() => {
            calls.push({ worktreePath, opencodeSessionId })
            return result
          }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-redo-1',
        method: 'opencodeOps.redo',
        params: { worktreePath: '/tmp/hive', opencodeSessionId: 'session-1' }
      })
    )

    expect(response).toEqual({
      id: 'opencode-redo-1',
      ok: true,
      value: result
    })
    expect(calls).toEqual([{ worktreePath: '/tmp/hive', opencodeSessionId: 'session-1' }])
  })

  it('validates opencodeOps.redo params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: true }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-redo-2',
        method: 'opencodeOps.redo',
        params: { worktreePath: '/tmp/hive', opencodeSessionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-redo-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.command through the opencode RPC domain', async () => {
    const calls: Array<{
      worktreePath: string
      opencodeSessionId: string
      command: string
      args: string
      model?: { providerID: string; modelID: string; variant?: string }
      options?: { codexFastMode?: boolean }
    }> = []
    const model = { providerID: 'anthropic', modelID: 'claude-sonnet', variant: 'opus' }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: (worktreePath, opencodeSessionId, command, args, model, options) =>
          Effect.sync(() => {
            calls.push({ worktreePath, opencodeSessionId, command, args, model, options })
            return { success: true }
          }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-command-1',
        method: 'opencodeOps.command',
        params: {
          worktreePath: '/tmp/hive',
          opencodeSessionId: 'session-1',
          command: 'review',
          args: '--fast',
          model
        }
      })
    )

    expect(response).toEqual({
      id: 'opencode-command-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([
      {
        worktreePath: '/tmp/hive',
        opencodeSessionId: 'session-1',
        command: 'review',
        args: '--fast',
        model,
        options: undefined
      }
    ])
  })

  it('validates opencodeOps.command params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: true }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-command-2',
        method: 'opencodeOps.command',
        params: {
          worktreePath: '/tmp/hive',
          opencodeSessionId: 'session-1',
          command: '',
          args: ''
        }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-command-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.commands through the opencode RPC domain', async () => {
    const calls: Array<{ worktreePath: string; sessionId?: string }> = []
    const commandList = [
      {
        name: 'review',
        description: 'Review changes',
        template: '/review $ARGUMENTS',
        source: 'project',
        hints: ['diff']
      }
    ]
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: (worktreePath, sessionId) =>
          Effect.sync(() => {
            calls.push({ worktreePath, sessionId })
            return { success: true, commands: commandList }
          }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-commands-1',
        method: 'opencodeOps.commands',
        params: { worktreePath: '/tmp/hive', sessionId: 'session-1' }
      })
    )

    expect(response).toEqual({
      id: 'opencode-commands-1',
      ok: true,
      value: { success: true, commands: commandList }
    })
    expect(calls).toEqual([{ worktreePath: '/tmp/hive', sessionId: 'session-1' }])
  })

  it('validates opencodeOps.commands params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: true, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-commands-2',
        method: 'opencodeOps.commands',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-commands-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.renameSession through the opencode RPC domain', async () => {
    const calls: Array<{ opencodeSessionId: string; title: string; worktreePath?: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: (opencodeSessionId, title, worktreePath) =>
          Effect.sync(() => {
            calls.push({ opencodeSessionId, title, worktreePath })
            return { success: true }
          }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-rename-session-1',
        method: 'opencodeOps.renameSession',
        params: {
          opencodeSessionId: 'session-1',
          title: 'New title',
          worktreePath: '/tmp/hive'
        }
      })
    )

    expect(response).toEqual({
      id: 'opencode-rename-session-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([
      { opencodeSessionId: 'session-1', title: 'New title', worktreePath: '/tmp/hive' }
    ])
  })

  it('validates opencodeOps.renameSession params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: true }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-rename-session-2',
        method: 'opencodeOps.renameSession',
        params: { opencodeSessionId: '', title: 'New title' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-rename-session-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.capabilities through the opencode RPC domain', async () => {
    const calls: Array<{ sessionId?: string }> = []
    const capabilities = {
      supportsUndo: true,
      supportsRedo: false,
      supportsCommands: true,
      supportsPermissionRequests: true,
      supportsQuestionPrompts: false,
      supportsModelSelection: true,
      supportsReconnect: true,
      supportsPartialStreaming: false,
      supportsSteer: true
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: (sessionId) =>
          Effect.sync(() => {
            calls.push({ sessionId })
            return { success: true, capabilities }
          }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-capabilities-1',
        method: 'opencodeOps.capabilities',
        params: { sessionId: 'session-1' }
      })
    )

    expect(response).toEqual({
      id: 'opencode-capabilities-1',
      ok: true,
      value: { success: true, capabilities }
    })
    expect(calls).toEqual([{ sessionId: 'session-1' }])
  })

  it('validates opencodeOps.capabilities params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-capabilities-2',
        method: 'opencodeOps.capabilities',
        params: { sessionId: 42 }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-capabilities-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles opencodeOps.fork through the opencode RPC domain', async () => {
    const calls: Array<{
      worktreePath: string
      opencodeSessionId: string
      messageId?: string
    }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: (worktreePath, opencodeSessionId, messageId) =>
          Effect.sync(() => {
            calls.push({ worktreePath, opencodeSessionId, messageId })
            return { success: true, sessionId: 'forked-session-1' }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-fork-1',
        method: 'opencodeOps.fork',
        params: {
          worktreePath: '/tmp/hive',
          opencodeSessionId: 'session-1',
          messageId: 'message-1'
        }
      })
    )

    expect(response).toEqual({
      id: 'opencode-fork-1',
      ok: true,
      value: { success: true, sessionId: 'forked-session-1' }
    })
    expect(calls).toEqual([
      {
        worktreePath: '/tmp/hive',
        opencodeSessionId: 'session-1',
        messageId: 'message-1'
      }
    ])
  })

  it('validates opencodeOps.fork params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      opencodeOps: {
        connect: () => Effect.succeed({ success: false }),
        reconnect: () => Effect.succeed({ success: false }),
        prompt: () => Effect.succeed({ success: false }),
        abort: () => Effect.succeed({ success: false }),
        steer: () => Effect.succeed({ success: false }),
        disconnect: () => Effect.succeed({ success: false }),
        getMessages: () => Effect.succeed({ success: false, messages: [] }),
        refreshFromThread: () => Effect.succeed({ success: false }),
        listModels: () => Effect.succeed({ success: false, providers: {} }),
        setModel: () => Effect.succeed({ success: false }),
        modelInfo: () => Effect.succeed({ success: false }),
        questionReply: () => Effect.succeed({ success: false }),
        questionReject: () => Effect.succeed({ success: false }),
        planApprove: () => Effect.succeed({ success: false }),
        planReject: () => Effect.succeed({ success: false }),
        permissionReply: () => Effect.succeed({ success: false }),
        permissionList: () => Effect.succeed({ success: false, permissions: [] }),
        commandApprovalReply: () => Effect.succeed({ success: false }),
        sessionInfo: () => Effect.succeed({ success: false }),
        undo: () => Effect.succeed({ success: false }),
        redo: () => Effect.succeed({ success: false }),
        command: () => Effect.succeed({ success: false }),
        commands: () => Effect.succeed({ success: false, commands: [] }),
        renameSession: () => Effect.succeed({ success: false }),
        capabilities: () => Effect.succeed({ success: false }),
        fork: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'opencode-fork-2',
        method: 'opencodeOps.fork',
        params: { worktreePath: '/tmp/hive', opencodeSessionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'opencode-fork-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.setting.get through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: (key) => Effect.succeed(key === 'theme' ? 'dark' : null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({ id: 'settings-1', method: 'db.setting.get', params: { key: 'theme' } })
    )

    expect(response).toEqual({ id: 'settings-1', ok: true, value: 'dark' })
  })

  it('validates db.setting.get params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({ id: 'settings-2', method: 'db.setting.get', params: {} })
    )

    expect(response).toMatchObject({
      id: 'settings-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.setting.set through the database RPC domain', async () => {
    const writes: { key: string; value: string }[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: (key, value) =>
          Effect.sync(() => {
            writes.push({ key, value })
            return true
          }),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'settings-3',
        method: 'db.setting.set',
        params: { key: 'theme', value: 'dark' }
      })
    )

    expect(response).toEqual({ id: 'settings-3', ok: true, value: true })
    expect(writes).toEqual([{ key: 'theme', value: 'dark' }])
  })

  it('validates db.setting.set params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'settings-4',
        method: 'db.setting.set',
        params: { key: 'theme', value: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'settings-4',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.setting.delete through the database RPC domain', async () => {
    const deletes: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: (key) =>
          Effect.sync(() => {
            deletes.push(key)
            return true
          }),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'settings-5',
        method: 'db.setting.delete',
        params: { key: 'theme' }
      })
    )

    expect(response).toEqual({ id: 'settings-5', ok: true, value: true })
    expect(deletes).toEqual(['theme'])
  })

  it('validates db.setting.delete params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'settings-6',
        method: 'db.setting.delete',
        params: { key: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'settings-6',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.setting.getAll through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () =>
          Effect.succeed([
            { key: 'theme', value: 'dark' },
            { key: 'telemetry_enabled', value: 'false' }
          ]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'settings-7',
        method: 'db.setting.getAll',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'settings-7',
      ok: true,
      value: [
        { key: 'theme', value: 'dark' },
        { key: 'telemetry_enabled', value: 'false' }
      ]
    })
  })

  it('validates db.setting.getAll params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'settings-8',
        method: 'db.setting.getAll',
        params: { key: 'theme' }
      })
    )

    expect(response).toMatchObject({
      id: 'settings-8',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.project.create through the database RPC domain', async () => {
    const creates: unknown[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: (data) =>
          Effect.sync(() => {
            creates.push(data)
            return project
          }),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const params = {
      name: project.name,
      path: project.path,
      description: 'Desktop app',
      tags: ['typescript'],
      setup_script: 'pnpm install',
      run_script: 'pnpm dev',
      archive_script: 'pnpm archive'
    }
    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-create-1',
        method: 'db.project.create',
        params
      })
    )

    expect(response).toEqual({ id: 'projects-create-1', ok: true, value: project })
    expect(creates).toEqual([params])
  })

  it('validates db.project.create params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-create-2',
        method: 'db.project.create',
        params: { name: project.name }
      })
    )

    expect(response).toMatchObject({
      id: 'projects-create-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.project.update through the database RPC domain', async () => {
    const updates: Array<{ id: string; data: unknown }> = []
    const updatedProject = { ...project, name: 'Hive Updated', language: 'typescript' }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: (id, data) =>
          Effect.sync(() => {
            updates.push({ id, data })
            return updatedProject
          }),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const data = {
      name: 'Hive Updated',
      description: 'Desktop app',
      tags: ['typescript'],
      language: 'typescript',
      custom_icon: null,
      detected_icon: 'none',
      setup_script: 'pnpm install',
      run_script: 'pnpm dev',
      archive_script: 'pnpm archive',
      auto_assign_port: true,
      last_accessed_at: '2026-05-26T01:00:00.000Z'
    }
    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-update-1',
        method: 'db.project.update',
        params: { id: project.id, data }
      })
    )

    expect(response).toEqual({ id: 'projects-update-1', ok: true, value: updatedProject })
    expect(updates).toEqual([{ id: project.id, data }])
  })

  it('validates db.project.update params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-update-2',
        method: 'db.project.update',
        params: { id: project.id, data: { auto_assign_port: 'yes' } }
      })
    )

    expect(response).toMatchObject({
      id: 'projects-update-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.project.delete through the database RPC domain', async () => {
    const deletes: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: (id) =>
          Effect.sync(() => {
            deletes.push(id)
            return true
          }),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-delete-1',
        method: 'db.project.delete',
        params: { id: project.id }
      })
    )

    expect(response).toEqual({ id: 'projects-delete-1', ok: true, value: true })
    expect(deletes).toEqual([project.id])
  })

  it('validates db.project.delete params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-delete-2',
        method: 'db.project.delete',
        params: { id: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'projects-delete-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.project.reorder through the database RPC domain', async () => {
    const reorders: string[][] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: (orderedIds) =>
          Effect.sync(() => {
            reorders.push(orderedIds)
            return true
          }),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const orderedIds = ['project-2', project.id]
    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-reorder-1',
        method: 'db.project.reorder',
        params: { orderedIds }
      })
    )

    expect(response).toEqual({ id: 'projects-reorder-1', ok: true, value: true })
    expect(reorders).toEqual([orderedIds])
  })

  it('validates db.project.reorder params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-reorder-2',
        method: 'db.project.reorder',
        params: { orderedIds: [project.id, 123] }
      })
    )

    expect(response).toMatchObject({
      id: 'projects-reorder-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.project.getAll through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([project]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-1',
        method: 'db.project.getAll',
        params: {}
      })
    )

    expect(response).toEqual({ id: 'projects-1', ok: true, value: [project] })
  })

  it('validates db.project.getAll params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-2',
        method: 'db.project.getAll',
        params: { projectId: 'project-1' }
      })
    )

    expect(response).toMatchObject({
      id: 'projects-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.project.get through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: (id) => Effect.succeed(id === project.id ? project : null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-3',
        method: 'db.project.get',
        params: { id: project.id }
      })
    )

    expect(response).toEqual({ id: 'projects-3', ok: true, value: project })
  })

  it('validates db.project.get params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-4',
        method: 'db.project.get',
        params: { id: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'projects-4',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.project.getByPath through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: (path) => Effect.succeed(path === project.path ? project : null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-5',
        method: 'db.project.getByPath',
        params: { path: project.path }
      })
    )

    expect(response).toEqual({ id: 'projects-5', ok: true, value: project })
  })

  it('validates db.project.getByPath params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-6',
        method: 'db.project.getByPath',
        params: { path: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'projects-6',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.project.touch through the database RPC domain', async () => {
    const touched: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: (id) =>
          Effect.sync(() => {
            touched.push(id)
            return true
          }),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-7',
        method: 'db.project.touch',
        params: { id: project.id }
      })
    )

    expect(response).toEqual({ id: 'projects-7', ok: true, value: true })
    expect(touched).toEqual([project.id])
  })

  it('validates db.project.touch params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-8',
        method: 'db.project.touch',
        params: { id: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'projects-8',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.project.sortByLastMessage through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed(['project-2', project.id]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-9',
        method: 'db.project.sortByLastMessage',
        params: {}
      })
    )

    expect(response).toEqual({ id: 'projects-9', ok: true, value: ['project-2', project.id] })
  })

  it('validates db.project.sortByLastMessage params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'projects-10',
        method: 'db.project.sortByLastMessage',
        params: { projectId: project.id }
      })
    )

    expect(response).toMatchObject({
      id: 'projects-10',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.create through the database RPC domain', async () => {
    const creates: unknown[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: (data) =>
          Effect.sync(() => {
            creates.push(data)
            return worktree
          }),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const params = {
      project_id: project.id,
      name: worktree.name,
      branch_name: worktree.branch_name,
      path: worktree.path,
      is_default: false,
      base_branch: 'main'
    }
    const response = await Effect.runPromise(
      router.handle({
        id: 'worktrees-create-1',
        method: 'db.worktree.create',
        params
      })
    )

    expect(response).toEqual({ id: 'worktrees-create-1', ok: true, value: worktree })
    expect(creates).toEqual([params])
  })

  it('validates db.worktree.create params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktrees-create-2',
        method: 'db.worktree.create',
        params: {
          project_id: project.id,
          name: worktree.name,
          branch_name: 123,
          path: worktree.path
        }
      })
    )

    expect(response).toMatchObject({
      id: 'worktrees-create-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.get through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: (id) => Effect.succeed(id === worktree.id ? worktree : null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktrees-get-1',
        method: 'db.worktree.get',
        params: { id: worktree.id }
      })
    )

    expect(response).toEqual({ id: 'worktrees-get-1', ok: true, value: worktree })
  })

  it('validates db.worktree.get params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktrees-get-2',
        method: 'db.worktree.get',
        params: { id: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'worktrees-get-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.getByProject through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: (projectId) =>
          Effect.succeed(projectId === project.id ? [worktree] : []),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktrees-by-project-1',
        method: 'db.worktree.getByProject',
        params: { projectId: project.id }
      })
    )

    expect(response).toEqual({ id: 'worktrees-by-project-1', ok: true, value: [worktree] })
  })

  it('validates db.worktree.getByProject params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktrees-by-project-2',
        method: 'db.worktree.getByProject',
        params: { projectId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'worktrees-by-project-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.getActiveByProject through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: (projectId) =>
          Effect.succeed(projectId === project.id ? [worktree] : []),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'active-worktrees-by-project-1',
        method: 'db.worktree.getActiveByProject',
        params: { projectId: project.id }
      })
    )

    expect(response).toEqual({ id: 'active-worktrees-by-project-1', ok: true, value: [worktree] })
  })

  it('validates db.worktree.getActiveByProject params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'active-worktrees-by-project-2',
        method: 'db.worktree.getActiveByProject',
        params: { projectId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'active-worktrees-by-project-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.getRecentlyActive through the database RPC domain', async () => {
    const cutoffMs = 1_714_000_000_000
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: (actualCutoffMs) =>
          Effect.succeed(actualCutoffMs === cutoffMs ? [worktree] : []),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'recent-worktrees-1',
        method: 'db.worktree.getRecentlyActive',
        params: { cutoffMs }
      })
    )

    expect(response).toEqual({ id: 'recent-worktrees-1', ok: true, value: [worktree] })
  })

  it('validates db.worktree.getRecentlyActive params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'recent-worktrees-2',
        method: 'db.worktree.getRecentlyActive',
        params: { cutoffMs: 'yesterday' }
      })
    )

    expect(response).toMatchObject({
      id: 'recent-worktrees-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.update through the database RPC domain', async () => {
    const update = {
      name: 'Renamed worktree',
      status: 'archived' as const,
      last_message_at: 1_714_000_000_000
    }
    const updatedWorktree = { ...worktree, ...update }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: (id, data) =>
          Effect.succeed(id === worktree.id && data.name === update.name ? updatedWorktree : null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-update-1',
        method: 'db.worktree.update',
        params: { id: worktree.id, data: update }
      })
    )

    expect(response).toEqual({ id: 'worktree-update-1', ok: true, value: updatedWorktree })
  })

  it('validates db.worktree.update params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-update-2',
        method: 'db.worktree.update',
        params: { id: '', data: { status: 'deleted' } }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-update-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.delete through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: (id) => Effect.succeed(id === worktree.id),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-delete-1',
        method: 'db.worktree.delete',
        params: { id: worktree.id }
      })
    )

    expect(response).toEqual({ id: 'worktree-delete-1', ok: true, value: true })
  })

  it('validates db.worktree.delete params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-delete-2',
        method: 'db.worktree.delete',
        params: { id: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-delete-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.archive through the database RPC domain', async () => {
    const archivedWorktree = { ...worktree, status: 'archived' as const }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: (id) => Effect.succeed(id === worktree.id ? archivedWorktree : null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-archive-1',
        method: 'db.worktree.archive',
        params: { id: worktree.id }
      })
    )

    expect(response).toEqual({ id: 'worktree-archive-1', ok: true, value: archivedWorktree })
  })

  it('validates db.worktree.archive params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-archive-2',
        method: 'db.worktree.archive',
        params: { id: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-archive-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.touch through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: (id) => Effect.succeed(id === worktree.id),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-touch-1',
        method: 'db.worktree.touch',
        params: { id: worktree.id }
      })
    )

    expect(response).toEqual({ id: 'worktree-touch-1', ok: true, value: true })
  })

  it('validates db.worktree.touch params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-touch-2',
        method: 'db.worktree.touch',
        params: { id: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-touch-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.appendSessionTitle through the database RPC domain', async () => {
    const title = 'Implement RPC migration'
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: (worktreeId, actualTitle) =>
          Effect.succeed({ success: worktreeId === worktree.id && actualTitle === title }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-append-title-1',
        method: 'db.worktree.appendSessionTitle',
        params: { worktreeId: worktree.id, title }
      })
    )

    expect(response).toEqual({
      id: 'worktree-append-title-1',
      ok: true,
      value: { success: true }
    })
  })

  it('validates db.worktree.appendSessionTitle params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-append-title-2',
        method: 'db.worktree.appendSessionTitle',
        params: { worktreeId: worktree.id }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-append-title-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.updateModel through the database RPC domain', async () => {
    const params = {
      worktreeId: worktree.id,
      modelProviderId: 'anthropic',
      modelId: 'claude-sonnet-4',
      modelVariant: null
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: (actualParams) =>
          Effect.succeed({
            success:
              actualParams.worktreeId === params.worktreeId &&
              actualParams.modelProviderId === params.modelProviderId &&
              actualParams.modelId === params.modelId &&
              actualParams.modelVariant === params.modelVariant
          }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-update-model-1',
        method: 'db.worktree.updateModel',
        params
      })
    )

    expect(response).toEqual({
      id: 'worktree-update-model-1',
      ok: true,
      value: { success: true }
    })
  })

  it('validates db.worktree.updateModel params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-update-model-2',
        method: 'db.worktree.updateModel',
        params: {
          worktreeId: worktree.id,
          modelProviderId: 'anthropic',
          modelId: 'claude-sonnet-4'
        }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-update-model-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.addAttachment through the database RPC domain', async () => {
    const attachment = {
      type: 'jira' as const,
      url: 'https://example.atlassian.net/browse/HIVE-123',
      label: 'HIVE-123'
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: (worktreeId, actualAttachment) =>
          Effect.succeed({
            success:
              worktreeId === worktree.id &&
              actualAttachment.type === attachment.type &&
              actualAttachment.url === attachment.url &&
              actualAttachment.label === attachment.label
          }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-add-attachment-1',
        method: 'db.worktree.addAttachment',
        params: { worktreeId: worktree.id, attachment }
      })
    )

    expect(response).toEqual({
      id: 'worktree-add-attachment-1',
      ok: true,
      value: { success: true }
    })
  })

  it('validates db.worktree.addAttachment params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-add-attachment-2',
        method: 'db.worktree.addAttachment',
        params: {
          worktreeId: worktree.id,
          attachment: {
            type: 'github',
            url: 'https://github.com/org/repo/issues/1',
            label: 'Issue 1'
          }
        }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-add-attachment-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.removeAttachment through the database RPC domain', async () => {
    const attachmentId = 'attachment-1'
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: (worktreeId, actualAttachmentId) =>
          Effect.succeed({
            success: worktreeId === worktree.id && actualAttachmentId === attachmentId
          }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-remove-attachment-1',
        method: 'db.worktree.removeAttachment',
        params: { worktreeId: worktree.id, attachmentId }
      })
    )

    expect(response).toEqual({
      id: 'worktree-remove-attachment-1',
      ok: true,
      value: { success: true }
    })
  })

  it('validates db.worktree.removeAttachment params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-remove-attachment-2',
        method: 'db.worktree.removeAttachment',
        params: { worktreeId: worktree.id, attachmentId: 42 }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-remove-attachment-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.attachPR through the database RPC domain', async () => {
    const prNumber = 123
    const prUrl = 'https://github.com/example/hive/pull/123'
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: (worktreeId, actualPrNumber, actualPrUrl) =>
          Effect.succeed({
            success:
              worktreeId === worktree.id && actualPrNumber === prNumber && actualPrUrl === prUrl
          }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-attach-pr-1',
        method: 'db.worktree.attachPR',
        params: { worktreeId: worktree.id, prNumber, prUrl }
      })
    )

    expect(response).toEqual({
      id: 'worktree-attach-pr-1',
      ok: true,
      value: { success: true }
    })
  })

  it('validates db.worktree.attachPR params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-attach-pr-2',
        method: 'db.worktree.attachPR',
        params: {
          worktreeId: worktree.id,
          prNumber: '123',
          prUrl: 'https://github.com/example/hive/pull/123'
        }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-attach-pr-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.detachPR through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: (worktreeId) =>
          Effect.succeed({
            success: worktreeId === worktree.id
          }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-detach-pr-1',
        method: 'db.worktree.detachPR',
        params: { worktreeId: worktree.id }
      })
    )

    expect(response).toEqual({
      id: 'worktree-detach-pr-1',
      ok: true,
      value: { success: true }
    })
  })

  it('validates db.worktree.detachPR params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-detach-pr-2',
        method: 'db.worktree.detachPR',
        params: { worktreeId: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-detach-pr-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.setPinned through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: (worktreeId, pinned) =>
          Effect.succeed({
            success: worktreeId === worktree.id && pinned === true
          }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-set-pinned-1',
        method: 'db.worktree.setPinned',
        params: { worktreeId: worktree.id, pinned: true }
      })
    )

    expect(response).toEqual({
      id: 'worktree-set-pinned-1',
      ok: true,
      value: { success: true }
    })
  })

  it('validates db.worktree.setPinned params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-set-pinned-2',
        method: 'db.worktree.setPinned',
        params: { worktreeId: worktree.id, pinned: 1 }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-set-pinned-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.worktree.getPinned through the database RPC domain', async () => {
    const pinnedWorktree = { ...worktree, pinned: 1 }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([pinnedWorktree]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-get-pinned-1',
        method: 'db.worktree.getPinned',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'worktree-get-pinned-1',
      ok: true,
      value: [pinnedWorktree]
    })
  })

  it('validates db.worktree.getPinned params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-get-pinned-2',
        method: 'db.worktree.getPinned',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-get-pinned-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.session.create through the database RPC domain', async () => {
    const data = {
      worktree_id: worktree.id,
      project_id: project.id,
      name: 'Build session',
      agent_sdk: 'opencode' as const,
      mode: 'build' as const,
      session_type: 'default' as const,
      model_provider_id: 'anthropic',
      model_id: 'claude-sonnet-4',
      model_variant: null
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: (actualData) =>
          Effect.succeed({
            ...session,
            name: actualData.name ?? null,
            model_provider_id: actualData.model_provider_id ?? null,
            model_id: actualData.model_id ?? null,
            model_variant: actualData.model_variant ?? null
          }),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-create-1',
        method: 'db.session.create',
        params: data
      })
    )

    expect(response).toEqual({
      id: 'session-create-1',
      ok: true,
      value: {
        ...session,
        name: data.name,
        model_provider_id: data.model_provider_id,
        model_id: data.model_id,
        model_variant: data.model_variant
      }
    })
  })

  it('validates db.session.create params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-create-2',
        method: 'db.session.create',
        params: {
          worktree_id: worktree.id,
          project_id: project.id,
          agent_sdk: 'unknown'
        }
      })
    )

    expect(response).toMatchObject({
      id: 'session-create-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.session.get through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: (id) => Effect.succeed(id === session.id ? session : null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-1',
        method: 'db.session.get',
        params: { id: session.id }
      })
    )

    expect(response).toEqual({
      id: 'session-get-1',
      ok: true,
      value: session
    })
  })

  it('validates db.session.get params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-2',
        method: 'db.session.get',
        params: { id: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'session-get-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.session.getByWorktree through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: (worktreeId) =>
          Effect.succeed(worktreeId === worktree.id ? [session] : []),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-by-worktree-1',
        method: 'db.session.getByWorktree',
        params: { worktreeId: worktree.id }
      })
    )

    expect(response).toEqual({
      id: 'session-get-by-worktree-1',
      ok: true,
      value: [session]
    })
  })

  it('validates db.session.getByWorktree params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-by-worktree-2',
        method: 'db.session.getByWorktree',
        params: { worktreeId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'session-get-by-worktree-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.session.getByProject through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: (projectId) =>
          Effect.succeed(projectId === project.id ? [session] : []),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-by-project-1',
        method: 'db.session.getByProject',
        params: { projectId: project.id }
      })
    )

    expect(response).toEqual({
      id: 'session-get-by-project-1',
      ok: true,
      value: [session]
    })
  })

  it('validates db.session.getByProject params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-by-project-2',
        method: 'db.session.getByProject',
        params: { projectId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'session-get-by-project-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.session.getActiveByWorktree through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: (worktreeId) =>
          Effect.succeed(worktreeId === worktree.id ? [session] : []),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-active-by-worktree-1',
        method: 'db.session.getActiveByWorktree',
        params: { worktreeId: worktree.id }
      })
    )

    expect(response).toEqual({
      id: 'session-get-active-by-worktree-1',
      ok: true,
      value: [session]
    })
  })

  it('validates db.session.getActiveByWorktree params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-active-by-worktree-2',
        method: 'db.session.getActiveByWorktree',
        params: { worktreeId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'session-get-active-by-worktree-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.session.update through the database RPC domain', async () => {
    const data = {
      name: 'Renamed session',
      status: 'completed' as const,
      completed_at: '2026-05-26T19:30:00.000Z'
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: (id, actualData) =>
          Effect.succeed(id === session.id ? { ...session, ...actualData } : null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-update-1',
        method: 'db.session.update',
        params: { id: session.id, data }
      })
    )

    expect(response).toEqual({
      id: 'session-update-1',
      ok: true,
      value: { ...session, ...data }
    })
  })

  it('validates db.session.update params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-update-2',
        method: 'db.session.update',
        params: { id: session.id, data: { status: 'archived' } }
      })
    )

    expect(response).toMatchObject({
      id: 'session-update-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.session.delete through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: (id) => Effect.succeed(id === session.id),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-delete-1',
        method: 'db.session.delete',
        params: { id: session.id }
      })
    )

    expect(response).toEqual({
      id: 'session-delete-1',
      ok: true,
      value: true
    })
  })

  it('validates db.session.delete params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-delete-2',
        method: 'db.session.delete',
        params: { id: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'session-delete-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.session.search through the database RPC domain', async () => {
    const options = {
      keyword: 'Build',
      project_id: project.id,
      worktree_id: worktree.id,
      includeArchived: true
    }
    const searchResult = {
      ...session,
      worktree_name: worktree.name,
      worktree_branch_name: worktree.branch_name,
      project_name: project.name,
      connection_name: null
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: (actualOptions) =>
          Effect.succeed(
            actualOptions.project_id === project.id && actualOptions.worktree_id === worktree.id
              ? [searchResult]
              : []
          ),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-search-1',
        method: 'db.session.search',
        params: options
      })
    )

    expect(response).toEqual({
      id: 'session-search-1',
      ok: true,
      value: [searchResult]
    })
  })

  it('validates db.session.search params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-search-2',
        method: 'db.session.search',
        params: { includeArchived: 'yes' }
      })
    )

    expect(response).toMatchObject({
      id: 'session-search-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.session.getDraft through the database RPC domain', async () => {
    const draft = 'Continue migrating IPC to RPC'
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: (sessionId) => Effect.succeed(sessionId === session.id ? draft : null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-draft-1',
        method: 'db.session.getDraft',
        params: { sessionId: session.id }
      })
    )

    expect(response).toEqual({
      id: 'session-get-draft-1',
      ok: true,
      value: draft
    })
  })

  it('validates db.session.getDraft params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-draft-2',
        method: 'db.session.getDraft',
        params: { sessionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'session-get-draft-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.session.updateDraft through the database RPC domain', async () => {
    const draft = 'Updated draft body'
    const updates: Array<{ sessionId: string; draft: string | null }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: (sessionId, nextDraft) =>
          Effect.sync(() => {
            updates.push({ sessionId, draft: nextDraft })
          }),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-update-draft-1',
        method: 'db.session.updateDraft',
        params: { sessionId: session.id, draft }
      })
    )

    expect(response).toEqual({
      id: 'session-update-draft-1',
      ok: true,
      value: undefined
    })
    expect(updates).toEqual([{ sessionId: session.id, draft }])
  })

  it('validates db.session.updateDraft params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-update-draft-2',
        method: 'db.session.updateDraft',
        params: { sessionId: session.id, draft: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'session-update-draft-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.session.getByConnection through the database RPC domain', async () => {
    const connectionId = 'connection-1'
    const connectionSession = { ...session, connection_id: connectionId }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: (actualConnectionId) =>
          Effect.succeed(actualConnectionId === connectionId ? [connectionSession] : []),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-by-connection-1',
        method: 'db.session.getByConnection',
        params: { connectionId }
      })
    )

    expect(response).toEqual({
      id: 'session-get-by-connection-1',
      ok: true,
      value: [connectionSession]
    })
  })

  it('validates db.session.getByConnection params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-by-connection-2',
        method: 'db.session.getByConnection',
        params: { connectionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'session-get-by-connection-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.session.getActiveByConnection through the database RPC domain', async () => {
    const connectionId = 'connection-1'
    const activeSession = { ...session, connection_id: connectionId, status: 'active' as const }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: (actualConnectionId) =>
          Effect.succeed(actualConnectionId === connectionId ? [activeSession] : []),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-active-by-connection-1',
        method: 'db.session.getActiveByConnection',
        params: { connectionId }
      })
    )

    expect(response).toEqual({
      id: 'session-get-active-by-connection-1',
      ok: true,
      value: [activeSession]
    })
  })

  it('validates db.session.getActiveByConnection params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-active-by-connection-2',
        method: 'db.session.getActiveByConnection',
        params: { connectionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'session-get-active-by-connection-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.session.setPinnedToBoard through the database RPC domain', async () => {
    const pinnedSession = { ...session, pinned_to_board: true }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: (sessionId, pinned) =>
          Effect.succeed(sessionId === session.id && pinned ? pinnedSession : null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-set-pinned-to-board-1',
        method: 'db.session.setPinnedToBoard',
        params: { sessionId: session.id, pinned: true }
      })
    )

    expect(response).toEqual({
      id: 'session-set-pinned-to-board-1',
      ok: true,
      value: pinnedSession
    })
  })

  it('validates db.session.setPinnedToBoard params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-set-pinned-to-board-2',
        method: 'db.session.setPinnedToBoard',
        params: { sessionId: session.id, pinned: 'yes' }
      })
    )

    expect(response).toMatchObject({
      id: 'session-set-pinned-to-board-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.session.getPinnedSessions through the database RPC domain', async () => {
    const pinnedSession = { ...session, pinned_to_board: true }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: (worktreeId) =>
          Effect.succeed(worktreeId === worktree.id ? [pinnedSession] : []),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-pinned-sessions-1',
        method: 'db.session.getPinnedSessions',
        params: { worktreeId: worktree.id }
      })
    )

    expect(response).toEqual({
      id: 'session-get-pinned-sessions-1',
      ok: true,
      value: [pinnedSession]
    })
  })

  it('validates db.session.getPinnedSessions params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-pinned-sessions-2',
        method: 'db.session.getPinnedSessions',
        params: { worktreeId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'session-get-pinned-sessions-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.session.getActiveBoardAssistant through the database RPC domain', async () => {
    const boardAssistantSession = { ...session, session_type: 'board-assistant' as const }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: (projectId) =>
          Effect.succeed(projectId === project.id ? boardAssistantSession : null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-active-board-assistant-1',
        method: 'db.session.getActiveBoardAssistant',
        params: { projectId: project.id }
      })
    )

    expect(response).toEqual({
      id: 'session-get-active-board-assistant-1',
      ok: true,
      value: boardAssistantSession
    })
  })

  it('validates db.session.getActiveBoardAssistant params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-get-active-board-assistant-2',
        method: 'db.session.getActiveBoardAssistant',
        params: { projectId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'session-get-active-board-assistant-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.sessionMessage.list through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: (sessionId) =>
          Effect.succeed(sessionId === session.id ? [sessionMessage] : []),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-message-list-1',
        method: 'db.sessionMessage.list',
        params: { sessionId: session.id }
      })
    )

    expect(response).toEqual({
      id: 'session-message-list-1',
      ok: true,
      value: [sessionMessage]
    })
  })

  it('validates db.sessionMessage.list params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-message-list-2',
        method: 'db.sessionMessage.list',
        params: { sessionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'session-message-list-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.sessionActivity.list through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: (sessionId) =>
          Effect.succeed(sessionId === session.id ? [sessionActivity] : []),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-activity-list-1',
        method: 'db.sessionActivity.list',
        params: { sessionId: session.id }
      })
    )

    expect(response).toEqual({
      id: 'session-activity-list-1',
      ok: true,
      value: [sessionActivity]
    })
  })

  it('validates db.sessionActivity.list params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'session-activity-list-2',
        method: 'db.sessionActivity.list',
        params: { sessionId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'session-activity-list-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.space.list through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([space]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-list-1',
        method: 'db.space.list',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'space-list-1',
      ok: true,
      value: [space]
    })
  })

  it('validates db.space.list params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-list-2',
        method: 'db.space.list',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'space-list-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.space.create through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: (data) =>
          Effect.succeed({ ...space, name: data.name, icon_type: data.icon_type ?? 'default' }),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-create-1',
        method: 'db.space.create',
        params: { name: 'Backlog', icon_type: 'emoji' }
      })
    )

    expect(response).toEqual({
      id: 'space-create-1',
      ok: true,
      value: { ...space, name: 'Backlog', icon_type: 'emoji' }
    })
  })

  it('validates db.space.create params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-create-2',
        method: 'db.space.create',
        params: { icon_type: 'emoji' }
      })
    )

    expect(response).toMatchObject({
      id: 'space-create-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.space.update through the database RPC domain', async () => {
    const updatedSpace = { ...space, name: 'Planning', sort_order: 2 }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: (id, data) =>
          Effect.succeed(id === space.id && data.name === 'Planning' ? updatedSpace : null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-update-1',
        method: 'db.space.update',
        params: { id: space.id, data: { name: 'Planning', sort_order: 2 } }
      })
    )

    expect(response).toEqual({
      id: 'space-update-1',
      ok: true,
      value: updatedSpace
    })
  })

  it('validates db.space.update params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-update-2',
        method: 'db.space.update',
        params: { id: '', data: { sort_order: 'first' } }
      })
    )

    expect(response).toMatchObject({
      id: 'space-update-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.space.delete through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: (id) => Effect.succeed(id === space.id),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-delete-1',
        method: 'db.space.delete',
        params: { id: space.id }
      })
    )

    expect(response).toEqual({
      id: 'space-delete-1',
      ok: true,
      value: true
    })
  })

  it('validates db.space.delete params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-delete-2',
        method: 'db.space.delete',
        params: { id: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'space-delete-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.space.assignProject through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: (projectId, spaceId) =>
          Effect.succeed(projectId === project.id && spaceId === space.id),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-assign-project-1',
        method: 'db.space.assignProject',
        params: { projectId: project.id, spaceId: space.id }
      })
    )

    expect(response).toEqual({
      id: 'space-assign-project-1',
      ok: true,
      value: true
    })
  })

  it('validates db.space.assignProject params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-assign-project-2',
        method: 'db.space.assignProject',
        params: { projectId: project.id, spaceId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'space-assign-project-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.space.removeProject through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: (projectId, spaceId) =>
          Effect.succeed(projectId === project.id && spaceId === space.id),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-remove-project-1',
        method: 'db.space.removeProject',
        params: { projectId: project.id, spaceId: space.id }
      })
    )

    expect(response).toEqual({
      id: 'space-remove-project-1',
      ok: true,
      value: true
    })
  })

  it('validates db.space.removeProject params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-remove-project-2',
        method: 'db.space.removeProject',
        params: { projectId: '', spaceId: space.id }
      })
    )

    expect(response).toMatchObject({
      id: 'space-remove-project-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.space.getProjectIds through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: (spaceId) =>
          Effect.succeed(spaceId === space.id ? [project.id, 'project-2'] : []),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-get-project-ids-1',
        method: 'db.space.getProjectIds',
        params: { id: space.id }
      })
    )

    expect(response).toEqual({
      id: 'space-get-project-ids-1',
      ok: true,
      value: [project.id, 'project-2']
    })
  })

  it('validates db.space.getProjectIds params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-get-project-ids-2',
        method: 'db.space.getProjectIds',
        params: { id: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'space-get-project-ids-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.space.getAllAssignments through the database RPC domain', async () => {
    const assignments = [{ project_id: project.id, space_id: space.id }]
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed(assignments),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-get-all-assignments-1',
        method: 'db.space.getAllAssignments'
      })
    )

    expect(response).toEqual({
      id: 'space-get-all-assignments-1',
      ok: true,
      value: assignments
    })
  })

  it('validates db.space.getAllAssignments params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-get-all-assignments-2',
        method: 'db.space.getAllAssignments',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'space-get-all-assignments-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.space.reorder through the database RPC domain', async () => {
    const orderedIds = ['space-2', space.id]
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: (ids) => Effect.succeed(ids.join(',') === orderedIds.join(',')),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-reorder-1',
        method: 'db.space.reorder',
        params: { orderedIds }
      })
    )

    expect(response).toEqual({
      id: 'space-reorder-1',
      ok: true,
      value: true
    })
  })

  it('validates db.space.reorder params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'space-reorder-2',
        method: 'db.space.reorder',
        params: { orderedIds: [space.id, 123] }
      })
    )

    expect(response).toMatchObject({
      id: 'space-reorder-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.diffComment.create through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: (data) =>
          Effect.succeed(
            data.worktree_id === worktree.id && data.file_path === diffComment.file_path
              ? diffComment
              : { ...diffComment, id: 'unexpected' }
          ),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'diff-comment-create-1',
        method: 'db.diffComment.create',
        params: {
          worktree_id: worktree.id,
          file_path: diffComment.file_path,
          line_start: diffComment.line_start,
          body: diffComment.body
        }
      })
    )

    expect(response).toEqual({
      id: 'diff-comment-create-1',
      ok: true,
      value: diffComment
    })
  })

  it('validates db.diffComment.create params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'diff-comment-create-2',
        method: 'db.diffComment.create',
        params: {
          worktree_id: worktree.id,
          file_path: diffComment.file_path,
          line_start: '42',
          body: diffComment.body
        }
      })
    )

    expect(response).toMatchObject({
      id: 'diff-comment-create-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.diffComment.list through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: (worktreeId) =>
          Effect.succeed(worktreeId === worktree.id ? [diffComment] : []),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'diff-comment-list-1',
        method: 'db.diffComment.list',
        params: { worktreeId: worktree.id }
      })
    )

    expect(response).toEqual({
      id: 'diff-comment-list-1',
      ok: true,
      value: [diffComment]
    })
  })

  it('validates db.diffComment.list params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'diff-comment-list-2',
        method: 'db.diffComment.list',
        params: { worktreeId: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'diff-comment-list-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.diffComment.update through the database RPC domain', async () => {
    const updatedComment = { ...diffComment, body: 'Updated body' }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: (id, data) =>
          Effect.succeed(
            id === diffComment.id && data.body === updatedComment.body ? updatedComment : null
          ),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'diff-comment-update-1',
        method: 'db.diffComment.update',
        params: { id: diffComment.id, data: { body: updatedComment.body } }
      })
    )

    expect(response).toEqual({
      id: 'diff-comment-update-1',
      ok: true,
      value: updatedComment
    })
  })

  it('validates db.diffComment.update params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'diff-comment-update-2',
        method: 'db.diffComment.update',
        params: { id: diffComment.id, data: { is_outdated: 'yes' } }
      })
    )

    expect(response).toMatchObject({
      id: 'diff-comment-update-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.diffComment.setOutdated through the database RPC domain', async () => {
    const outdatedComment = { ...diffComment, is_outdated: true }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: (id, isOutdated) =>
          Effect.succeed(id === diffComment.id && isOutdated ? outdatedComment : null),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'diff-comment-set-outdated-1',
        method: 'db.diffComment.setOutdated',
        params: { id: diffComment.id, isOutdated: true }
      })
    )

    expect(response).toEqual({
      id: 'diff-comment-set-outdated-1',
      ok: true,
      value: outdatedComment
    })
  })

  it('validates db.diffComment.setOutdated params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'diff-comment-set-outdated-2',
        method: 'db.diffComment.setOutdated',
        params: { id: diffComment.id, isOutdated: 'true' }
      })
    )

    expect(response).toMatchObject({
      id: 'diff-comment-set-outdated-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.diffComment.delete through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: (id) => Effect.succeed(id === diffComment.id),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'diff-comment-delete-1',
        method: 'db.diffComment.delete',
        params: { id: diffComment.id }
      })
    )

    expect(response).toEqual({
      id: 'diff-comment-delete-1',
      ok: true,
      value: true
    })
  })

  it('validates db.diffComment.delete params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'diff-comment-delete-2',
        method: 'db.diffComment.delete',
        params: { id: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'diff-comment-delete-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.diffComment.clearAll through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: (worktreeId) => Effect.succeed(worktreeId === worktree.id ? 2 : 0),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'diff-comment-clear-all-1',
        method: 'db.diffComment.clearAll',
        params: { worktreeId: worktree.id }
      })
    )

    expect(response).toEqual({
      id: 'diff-comment-clear-all-1',
      ok: true,
      value: 2
    })
  })

  it('validates db.diffComment.clearAll params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'diff-comment-clear-all-2',
        method: 'db.diffComment.clearAll',
        params: { worktreeId: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'diff-comment-clear-all-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.schemaVersion through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(42),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'db-schema-version-1',
        method: 'db.schemaVersion',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'db-schema-version-1',
      ok: true,
      value: 42
    })
  })

  it('validates db.schemaVersion params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'db-schema-version-2',
        method: 'db.schemaVersion',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'db-schema-version-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.tableExists through the database RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: (tableName) => Effect.succeed(tableName === 'projects'),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'db-table-exists-1',
        method: 'db.tableExists',
        params: { tableName: 'projects' }
      })
    )

    expect(response).toEqual({
      id: 'db-table-exists-1',
      ok: true,
      value: true
    })
  })

  it('validates db.tableExists params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'db-table-exists-2',
        method: 'db.tableExists',
        params: { tableName: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'db-table-exists-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles db.getIndexes through the database RPC domain', async () => {
    const indexes = [{ name: 'idx_projects_path', tbl_name: 'projects' }]
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed(indexes)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'db-get-indexes-1',
        method: 'db.getIndexes',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'db-get-indexes-1',
      ok: true,
      value: indexes
    })
  })

  it('validates db.getIndexes params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      db: {
        getSetting: () => Effect.succeed(null),
        setSetting: () => Effect.succeed(true),
        deleteSetting: () => Effect.succeed(true),
        getAllSettings: () => Effect.succeed([]),
        createProject: () => Effect.succeed(project),
        updateProject: () => Effect.succeed(project),
        deleteProject: () => Effect.succeed(true),
        getProject: () => Effect.succeed(null),
        getProjectByPath: () => Effect.succeed(null),
        getAllProjects: () => Effect.succeed([]),
        reorderProjects: () => Effect.succeed(true),
        touchProject: () => Effect.succeed(true),
        sortProjectIdsByLastMessage: () => Effect.succeed([]),
        createWorktree: () => Effect.succeed(worktree),
        getWorktree: () => Effect.succeed(null),
        getWorktreesByProject: () => Effect.succeed([]),
        getActiveWorktreesByProject: () => Effect.succeed([]),
        getRecentlyActiveWorktrees: () => Effect.succeed([]),
        updateWorktree: () => Effect.succeed(null),
        deleteWorktree: () => Effect.succeed(false),
        archiveWorktree: () => Effect.succeed(null),
        touchWorktree: () => Effect.succeed(true),
        appendWorktreeSessionTitle: () => Effect.succeed({ success: true }),
        updateWorktreeModel: () => Effect.succeed({ success: true }),
        addWorktreeAttachment: () => Effect.succeed({ success: true }),
        removeWorktreeAttachment: () => Effect.succeed({ success: true }),
        attachWorktreePr: () => Effect.succeed({ success: true }),
        detachWorktreePr: () => Effect.succeed({ success: true }),
        setWorktreePinned: () => Effect.succeed({ success: true }),
        getPinnedWorktrees: () => Effect.succeed([]),
        createSession: () => Effect.succeed(session),
        getSession: () => Effect.succeed(null),
        getSessionsByWorktree: () => Effect.succeed([]),
        getSessionsByProject: () => Effect.succeed([]),
        getActiveSessionsByWorktree: () => Effect.succeed([]),
        updateSession: () => Effect.succeed(null),
        deleteSession: () => Effect.succeed(false),
        searchSessions: () => Effect.succeed([]),
        getSessionDraft: () => Effect.succeed(null),
        updateSessionDraft: () => Effect.succeed(undefined),
        getSessionsByConnection: () => Effect.succeed([]),
        getActiveSessionsByConnection: () => Effect.succeed([]),
        setSessionPinnedToBoard: () => Effect.succeed(null),
        getPinnedSessions: () => Effect.succeed([]),
        getActiveBoardAssistant: () => Effect.succeed(null),
        listSessionMessages: () => Effect.succeed([]),
        listSessionActivities: () => Effect.succeed([]),
        listSpaces: () => Effect.succeed([]),
        createSpace: () => Effect.succeed(space),
        updateSpace: () => Effect.succeed(null),
        deleteSpace: () => Effect.succeed(false),
        assignProjectToSpace: () => Effect.succeed(false),
        removeProjectFromSpace: () => Effect.succeed(false),
        getProjectIdsForSpace: () => Effect.succeed([]),
        getAllProjectSpaceAssignments: () => Effect.succeed([]),
        reorderSpaces: () => Effect.succeed(false),
        createDiffComment: () => Effect.succeed(diffComment),
        listDiffComments: () => Effect.succeed([diffComment]),
        updateDiffComment: () => Effect.succeed(diffComment),
        setDiffCommentOutdated: () => Effect.succeed(diffComment),
        deleteDiffComment: () => Effect.succeed(true),
        clearAllDiffComments: () => Effect.succeed(1),
        getSchemaVersion: () => Effect.succeed(0),
        tableExists: () => Effect.succeed(false),
        getIndexes: () => Effect.succeed([])
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'db-get-indexes-2',
        method: 'db.getIndexes',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'db-get-indexes-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.openDirectoryDialog through the project ops RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed('/tmp/hive'),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-open-directory-dialog-1',
        method: 'projectOps.openDirectoryDialog',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'project-ops-open-directory-dialog-1',
      ok: true,
      value: '/tmp/hive'
    })
  })

  it('validates projectOps.openDirectoryDialog params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-open-directory-dialog-2',
        method: 'projectOps.openDirectoryDialog',
        params: { path: '/tmp/hive' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-open-directory-dialog-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.showInFolder through the project ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: (path) =>
          Effect.sync(() => {
            calls.push(path)
          }),
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-show-in-folder-1',
        method: 'projectOps.showInFolder',
        params: { path: '/tmp/hive/package.json' }
      })
    )

    expect(response).toEqual({
      id: 'project-ops-show-in-folder-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual(['/tmp/hive/package.json'])
  })

  it('validates projectOps.showInFolder params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-show-in-folder-2',
        method: 'projectOps.showInFolder',
        params: { path: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-show-in-folder-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.openPath through the project ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: (path) =>
          Effect.sync(() => {
            calls.push(path)
            return ''
          }),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-open-path-1',
        method: 'projectOps.openPath',
        params: { path: '/tmp/hive/package.json' }
      })
    )

    expect(response).toEqual({
      id: 'project-ops-open-path-1',
      ok: true,
      value: ''
    })
    expect(calls).toEqual(['/tmp/hive/package.json'])
  })

  it('validates projectOps.openPath params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-open-path-2',
        method: 'projectOps.openPath',
        params: { path: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-open-path-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.copyToClipboard through the project ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: (text) =>
          Effect.sync(() => {
            calls.push(text)
          }),
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-copy-to-clipboard-1',
        method: 'projectOps.copyToClipboard',
        params: { text: 'hello from hive' }
      })
    )

    expect(response).toEqual({
      id: 'project-ops-copy-to-clipboard-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual(['hello from hive'])
  })

  it('validates projectOps.copyToClipboard params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-copy-to-clipboard-2',
        method: 'projectOps.copyToClipboard',
        params: { text: 42 }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-copy-to-clipboard-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.readFromClipboard through the project ops RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed('hello from clipboard'),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-read-from-clipboard-1',
        method: 'projectOps.readFromClipboard',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'project-ops-read-from-clipboard-1',
      ok: true,
      value: 'hello from clipboard'
    })
  })

  it('validates projectOps.readFromClipboard params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-read-from-clipboard-2',
        method: 'projectOps.readFromClipboard',
        params: { text: 'unexpected' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-read-from-clipboard-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.isGitRepository through the project ops RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: (path) => Effect.succeed(path === '/tmp/hive'),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-is-git-repository-1',
        method: 'projectOps.isGitRepository',
        params: { path: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'project-ops-is-git-repository-1',
      ok: true,
      value: true
    })
  })

  it('validates projectOps.isGitRepository params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-is-git-repository-2',
        method: 'projectOps.isGitRepository',
        params: { path: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-is-git-repository-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.validateProject through the project ops RPC domain', async () => {
    const result = { success: true, path: '/tmp/hive', name: 'hive' }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: (path) =>
          Effect.succeed(path === '/tmp/hive' ? result : { success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-validate-project-1',
        method: 'projectOps.validateProject',
        params: { path: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'project-ops-validate-project-1',
      ok: true,
      value: result
    })
  })

  it('validates projectOps.validateProject params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-validate-project-2',
        method: 'projectOps.validateProject',
        params: { path: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-validate-project-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.detectLanguage through the project ops RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: (projectPath) =>
          Effect.succeed(projectPath === '/tmp/hive' ? 'typescript' : null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-detect-language-1',
        method: 'projectOps.detectLanguage',
        params: { path: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'project-ops-detect-language-1',
      ok: true,
      value: 'typescript'
    })
  })

  it('validates projectOps.detectLanguage params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-detect-language-2',
        method: 'projectOps.detectLanguage',
        params: { path: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-detect-language-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.detectSetupSuggestions through the project ops RPC domain', async () => {
    const suggestions = [
      {
        id: 'install:pnpm',
        command: 'pnpm i',
        label: 'Install with pnpm',
        category: 'install' as const,
        defaultChecked: true
      }
    ]
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: (projectPath) =>
          Effect.succeed(projectPath === '/tmp/hive' ? suggestions : []),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-detect-setup-suggestions-1',
        method: 'projectOps.detectSetupSuggestions',
        params: { path: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'project-ops-detect-setup-suggestions-1',
      ok: true,
      value: suggestions
    })
  })

  it('validates projectOps.detectSetupSuggestions params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-detect-setup-suggestions-2',
        method: 'projectOps.detectSetupSuggestions',
        params: { path: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-detect-setup-suggestions-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.findXcworkspace through the project ops RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: (projectPath) =>
          Effect.succeed(projectPath === '/tmp/hive' ? '/tmp/hive/Hive.xcworkspace' : null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-find-xcworkspace-1',
        method: 'projectOps.findXcworkspace',
        params: { path: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'project-ops-find-xcworkspace-1',
      ok: true,
      value: '/tmp/hive/Hive.xcworkspace'
    })
  })

  it('validates projectOps.findXcworkspace params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-find-xcworkspace-2',
        method: 'projectOps.findXcworkspace',
        params: { path: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-find-xcworkspace-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.isAndroidProject through the project ops RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: (projectPath) => Effect.succeed(projectPath === '/tmp/hive'),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-is-android-project-1',
        method: 'projectOps.isAndroidProject',
        params: { path: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'project-ops-is-android-project-1',
      ok: true,
      value: true
    })
  })

  it('validates projectOps.isAndroidProject params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-is-android-project-2',
        method: 'projectOps.isAndroidProject',
        params: { path: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-is-android-project-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.loadLanguageIcons through the project ops RPC domain', async () => {
    const icons = {
      typescript: 'data:image/svg+xml;base64,PHN2Zy8+'
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed(icons),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-load-language-icons-1',
        method: 'projectOps.loadLanguageIcons',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'project-ops-load-language-icons-1',
      ok: true,
      value: icons
    })
  })

  it('validates projectOps.loadLanguageIcons params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-load-language-icons-2',
        method: 'projectOps.loadLanguageIcons',
        params: { path: '/tmp/hive' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-load-language-icons-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.initRepository through the project ops RPC domain', async () => {
    const result = { success: true }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: (path) =>
          Effect.succeed(path === '/tmp/hive' ? result : { success: false, error: 'bad path' }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-init-repository-1',
        method: 'projectOps.initRepository',
        params: { path: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'project-ops-init-repository-1',
      ok: true,
      value: result
    })
  })

  it('validates projectOps.initRepository params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-init-repository-2',
        method: 'projectOps.initRepository',
        params: { path: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-init-repository-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.pickProjectIcon through the project ops RPC domain', async () => {
    const result = { success: true, filename: 'project-1.svg' }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: (projectId) =>
          Effect.succeed(
            projectId === 'project-1' ? result : { success: false, error: 'cancelled' }
          ),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-pick-project-icon-1',
        method: 'projectOps.pickProjectIcon',
        params: { projectId: 'project-1' }
      })
    )

    expect(response).toEqual({
      id: 'project-ops-pick-project-icon-1',
      ok: true,
      value: result
    })
  })

  it('validates projectOps.pickProjectIcon params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-pick-project-icon-2',
        method: 'projectOps.pickProjectIcon',
        params: { projectId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-pick-project-icon-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.removeProjectIcon through the project ops RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: (projectId) =>
          Effect.succeed(projectId === 'project-1' ? { success: true } : { success: false }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-remove-project-icon-1',
        method: 'projectOps.removeProjectIcon',
        params: { projectId: 'project-1' }
      })
    )

    expect(response).toEqual({
      id: 'project-ops-remove-project-icon-1',
      ok: true,
      value: { success: true }
    })
  })

  it('validates projectOps.removeProjectIcon params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-remove-project-icon-2',
        method: 'projectOps.removeProjectIcon',
        params: { projectId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-remove-project-icon-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.getProjectIconPath through the project ops RPC domain', async () => {
    const dataUrl = 'data:image/svg+xml;base64,PHN2Zy8+'
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: (filename) =>
          Effect.succeed(filename === 'project-1.svg' ? dataUrl : null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-get-project-icon-path-1',
        method: 'projectOps.getProjectIconPath',
        params: { filename: 'project-1.svg' }
      })
    )

    expect(response).toEqual({
      id: 'project-ops-get-project-icon-path-1',
      ok: true,
      value: dataUrl
    })
  })

  it('validates projectOps.getProjectIconPath params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-get-project-icon-path-2',
        method: 'projectOps.getProjectIconPath',
        params: { filename: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-get-project-icon-path-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.detectFavicon through the project ops RPC domain', async () => {
    const faviconPath = '/tmp/hive/public/favicon.svg'
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: (projectPath) =>
          Effect.succeed(projectPath === '/tmp/hive' ? faviconPath : null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-detect-favicon-1',
        method: 'projectOps.detectFavicon',
        params: { path: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'project-ops-detect-favicon-1',
      ok: true,
      value: faviconPath
    })
  })

  it('validates projectOps.detectFavicon params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-detect-favicon-2',
        method: 'projectOps.detectFavicon',
        params: { path: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-detect-favicon-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles projectOps.getAbsoluteIconDataUrl through the project ops RPC domain', async () => {
    const dataUrl = 'data:image/svg+xml;base64,PHN2Zy8+'
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: (absolutePath) =>
          Effect.succeed(absolutePath === '/tmp/hive/public/favicon.svg' ? dataUrl : null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-get-absolute-icon-data-url-1',
        method: 'projectOps.getAbsoluteIconDataUrl',
        params: { path: '/tmp/hive/public/favicon.svg' }
      })
    )

    expect(response).toEqual({
      id: 'project-ops-get-absolute-icon-data-url-1',
      ok: true,
      value: dataUrl
    })
  })

  it('validates projectOps.getAbsoluteIconDataUrl params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      projectOps: {
        openDirectoryDialog: () => Effect.succeed(null),
        showInFolder: () => Effect.void,
        openPath: () => Effect.succeed(''),
        copyToClipboard: () => Effect.void,
        readFromClipboard: () => Effect.succeed(''),
        isGitRepository: () => Effect.succeed(false),
        validateProject: () => Effect.succeed({ success: false }),
        detectLanguage: () => Effect.succeed(null),
        detectSetupSuggestions: () => Effect.succeed([]),
        findXcworkspace: () => Effect.succeed(null),
        isAndroidProject: () => Effect.succeed(false),
        loadLanguageIcons: () => Effect.succeed({}),
        initRepository: () => Effect.succeed({ success: true }),
        createProjectFolder: () => Effect.succeed({ success: false }),
        pickProjectIcon: () => Effect.succeed({ success: false, error: 'cancelled' }),
        removeProjectIcon: () => Effect.succeed({ success: true }),
        getProjectIconPath: () => Effect.succeed(null),
        detectFavicon: () => Effect.succeed(null),
        getAbsoluteIconDataUrl: () => Effect.succeed(null)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'project-ops-get-absolute-icon-data-url-2',
        method: 'projectOps.getAbsoluteIconDataUrl',
        params: { path: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'project-ops-get-absolute-icon-data-url-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles worktreeOps.hasCommits through the worktree ops RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: (projectPath) => Effect.succeed(projectPath === '/tmp/hive'),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-has-commits-1',
        method: 'worktreeOps.hasCommits',
        params: { projectPath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'worktree-ops-has-commits-1',
      ok: true,
      value: true
    })
  })

  it('validates worktreeOps.hasCommits params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-has-commits-2',
        method: 'worktreeOps.hasCommits',
        params: { projectPath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-ops-has-commits-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles worktreeOps.create through the worktree ops RPC domain', async () => {
    const createdWorktree = {
      id: 'worktree-1',
      project_id: 'project-1',
      name: 'hive-feature',
      branch_name: 'hive-feature',
      path: '/tmp/hive-feature',
      status: 'active' as const,
      is_default: false,
      branch_renamed: 0,
      last_message_at: null,
      session_titles: '[]',
      last_model_provider_id: null,
      last_model_id: null,
      last_model_variant: null,
      attachments: '[]',
      pinned: 0,
      context: null,
      github_pr_number: null,
      github_pr_url: null,
      base_branch: 'main',
      created_at: '2026-05-26T00:00:00.000Z',
      last_accessed_at: '2026-05-26T00:00:00.000Z'
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: (params) =>
          Effect.succeed({
            success: true,
            worktree: {
              ...createdWorktree,
              project_id: params.projectId
            },
            pullInfo: { pulled: true, updated: false }
          }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-create-1',
        method: 'worktreeOps.create',
        params: {
          projectId: 'project-1',
          projectPath: '/tmp/hive',
          projectName: 'Hive'
        }
      })
    )

    expect(response).toEqual({
      id: 'worktree-ops-create-1',
      ok: true,
      value: {
        success: true,
        worktree: createdWorktree,
        pullInfo: { pulled: true, updated: false }
      }
    })
  })

  it('validates worktreeOps.create params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-create-2',
        method: 'worktreeOps.create',
        params: {
          projectId: 'project-1',
          projectPath: '',
          projectName: 'Hive'
        }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-ops-create-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles worktreeOps.delete through the worktree ops RPC domain', async () => {
    const calls: unknown[] = []
    const params = {
      worktreeId: 'worktree-1',
      worktreePath: '/tmp/hive-feature',
      branchName: 'hive-feature',
      projectPath: '/tmp/hive',
      archive: true
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: (deleteParams) =>
          Effect.sync(() => {
            calls.push(deleteParams)
            return { success: true }
          }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-delete-1',
        method: 'worktreeOps.delete',
        params
      })
    )

    expect(response).toEqual({
      id: 'worktree-ops-delete-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([params])
  })

  it('validates worktreeOps.delete params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-delete-2',
        method: 'worktreeOps.delete',
        params: {
          worktreeId: 'worktree-1',
          worktreePath: '/tmp/hive-feature',
          branchName: 'hive-feature',
          projectPath: '/tmp/hive',
          archive: 'yes'
        }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-ops-delete-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles worktreeOps.sync through the worktree ops RPC domain', async () => {
    const calls: unknown[] = []
    const params = {
      projectId: 'project-1',
      projectPath: '/tmp/hive'
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: (syncParams) =>
          Effect.sync(() => {
            calls.push(syncParams)
            return { success: true }
          }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-sync-1',
        method: 'worktreeOps.sync',
        params
      })
    )

    expect(response).toEqual({
      id: 'worktree-ops-sync-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([params])
  })

  it('validates worktreeOps.sync params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-sync-2',
        method: 'worktreeOps.sync',
        params: {
          projectId: 'project-1',
          projectPath: ''
        }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-ops-sync-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles worktreeOps.exists through the worktree ops RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: (worktreePath) => Effect.succeed(worktreePath === '/tmp/hive-feature'),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-exists-1',
        method: 'worktreeOps.exists',
        params: { worktreePath: '/tmp/hive-feature' }
      })
    )

    expect(response).toEqual({
      id: 'worktree-ops-exists-1',
      ok: true,
      value: true
    })
  })

  it('validates worktreeOps.exists params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-exists-2',
        method: 'worktreeOps.exists',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-ops-exists-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles worktreeOps.openInTerminal through the worktree ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return { success: true }
          }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-open-in-terminal-1',
        method: 'worktreeOps.openInTerminal',
        params: { worktreePath: '/tmp/hive-feature' }
      })
    )

    expect(response).toEqual({
      id: 'worktree-ops-open-in-terminal-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['/tmp/hive-feature'])
  })

  it('validates worktreeOps.openInTerminal params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-open-in-terminal-2',
        method: 'worktreeOps.openInTerminal',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-ops-open-in-terminal-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles worktreeOps.openInEditor through the worktree ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: (worktreePath) =>
          Effect.sync(() => {
            calls.push(worktreePath)
            return { success: true }
          }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-open-in-editor-1',
        method: 'worktreeOps.openInEditor',
        params: { worktreePath: '/tmp/hive-feature' }
      })
    )

    expect(response).toEqual({
      id: 'worktree-ops-open-in-editor-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual(['/tmp/hive-feature'])
  })

  it('validates worktreeOps.openInEditor params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-open-in-editor-2',
        method: 'worktreeOps.openInEditor',
        params: { worktreePath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-ops-open-in-editor-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles worktreeOps.getBranches through the worktree ops RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: (projectPath) =>
          Effect.succeed(
            projectPath === '/tmp/hive'
              ? { success: true, branches: ['main', 'feature'], currentBranch: 'main' }
              : { success: false, error: 'not found' }
          ),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-get-branches-1',
        method: 'worktreeOps.getBranches',
        params: { projectPath: '/tmp/hive' }
      })
    )

    expect(response).toEqual({
      id: 'worktree-ops-get-branches-1',
      ok: true,
      value: { success: true, branches: ['main', 'feature'], currentBranch: 'main' }
    })
  })

  it('validates worktreeOps.getBranches params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-get-branches-2',
        method: 'worktreeOps.getBranches',
        params: { projectPath: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-ops-get-branches-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles worktreeOps.branchExists through the worktree ops RPC domain', async () => {
    const calls: Array<{ projectPath: string; branchName: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: (projectPath, branchName) =>
          Effect.sync(() => {
            calls.push({ projectPath, branchName })
            return projectPath === '/tmp/hive' && branchName === 'feature'
          }),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-branch-exists-1',
        method: 'worktreeOps.branchExists',
        params: { projectPath: '/tmp/hive', branchName: 'feature' }
      })
    )

    expect(response).toEqual({
      id: 'worktree-ops-branch-exists-1',
      ok: true,
      value: true
    })
    expect(calls).toEqual([{ projectPath: '/tmp/hive', branchName: 'feature' }])
  })

  it('validates worktreeOps.branchExists params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-branch-exists-2',
        method: 'worktreeOps.branchExists',
        params: { projectPath: '/tmp/hive', branchName: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-ops-branch-exists-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles worktreeOps.duplicate through the worktree ops RPC domain', async () => {
    const duplicatedWorktree = {
      id: 'worktree-duplicate',
      project_id: 'project-1',
      name: 'hive-feature-copy',
      branch_name: 'hive-feature-copy',
      path: '/tmp/hive-feature-copy',
      status: 'active' as const,
      is_default: false,
      branch_renamed: 0,
      last_message_at: null,
      session_titles: '[]',
      last_model_provider_id: null,
      last_model_id: null,
      last_model_variant: null,
      attachments: '[]',
      pinned: 0,
      context: null,
      github_pr_number: null,
      github_pr_url: null,
      base_branch: 'main',
      created_at: '2026-05-26T00:00:00.000Z',
      last_accessed_at: '2026-05-26T00:00:00.000Z'
    }
    const calls: unknown[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: (params) =>
          Effect.sync(() => {
            calls.push(params)
            return { success: true, worktree: duplicatedWorktree }
          }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const params = {
      projectId: 'project-1',
      projectPath: '/tmp/hive',
      projectName: 'Hive',
      sourceBranch: 'feature',
      sourceWorktreePath: '/tmp/hive-feature',
      nameHint: 'copy'
    }
    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-duplicate-1',
        method: 'worktreeOps.duplicate',
        params
      })
    )

    expect(response).toEqual({
      id: 'worktree-ops-duplicate-1',
      ok: true,
      value: { success: true, worktree: duplicatedWorktree }
    })
    expect(calls).toEqual([params])
  })

  it('validates worktreeOps.duplicate params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-duplicate-2',
        method: 'worktreeOps.duplicate',
        params: {
          projectId: 'project-1',
          projectPath: '/tmp/hive',
          projectName: 'Hive',
          sourceBranch: '',
          sourceWorktreePath: '/tmp/hive-feature'
        }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-ops-duplicate-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles worktreeOps.renameBranch through the worktree ops RPC domain', async () => {
    const calls: unknown[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: (params) =>
          Effect.sync(() => {
            calls.push(params)
            return { success: true }
          }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const params = {
      worktreeId: 'worktree-1',
      worktreePath: '/tmp/hive-feature',
      oldBranch: 'feature',
      newBranch: 'feature-renamed'
    }
    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-rename-branch-1',
        method: 'worktreeOps.renameBranch',
        params
      })
    )

    expect(response).toEqual({
      id: 'worktree-ops-rename-branch-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([params])
  })

  it('validates worktreeOps.renameBranch params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-rename-branch-2',
        method: 'worktreeOps.renameBranch',
        params: {
          worktreeId: 'worktree-1',
          worktreePath: '/tmp/hive-feature',
          oldBranch: 'feature',
          newBranch: ''
        }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-ops-rename-branch-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles worktreeOps.createFromBranch through the worktree ops RPC domain', async () => {
    const createdWorktree = {
      id: 'worktree-from-branch',
      project_id: 'project-1',
      name: 'hive-release',
      branch_name: 'release',
      path: '/tmp/hive-release',
      status: 'active' as const,
      is_default: false,
      branch_renamed: 0,
      last_message_at: null,
      session_titles: '[]',
      last_model_provider_id: null,
      last_model_id: null,
      last_model_variant: null,
      attachments: '[]',
      pinned: 0,
      context: null,
      github_pr_number: 42,
      github_pr_url: null,
      base_branch: 'main',
      created_at: '2026-05-26T00:00:00.000Z',
      last_accessed_at: '2026-05-26T00:00:00.000Z'
    }
    const calls: unknown[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: (params) =>
          Effect.sync(() => {
            calls.push(params)
            return {
              success: true,
              worktree: createdWorktree,
              pullInfo: { pulled: true, updated: false }
            }
          })
      }
    })

    const params = {
      projectId: 'project-1',
      projectPath: '/tmp/hive',
      projectName: 'Hive',
      branchName: 'release',
      prNumber: 42,
      nameHint: 'release'
    }
    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-create-from-branch-1',
        method: 'worktreeOps.createFromBranch',
        params
      })
    )

    expect(response).toEqual({
      id: 'worktree-ops-create-from-branch-1',
      ok: true,
      value: {
        success: true,
        worktree: createdWorktree,
        pullInfo: { pulled: true, updated: false }
      }
    })
    expect(calls).toEqual([params])
  })

  it('validates worktreeOps.createFromBranch params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-create-from-branch-2',
        method: 'worktreeOps.createFromBranch',
        params: {
          projectId: 'project-1',
          projectPath: '/tmp/hive',
          projectName: 'Hive',
          branchName: ''
        }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-ops-create-from-branch-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles worktreeOps.getContext through the worktree ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false }),
        getContext: (worktreeId) =>
          Effect.sync(() => {
            calls.push(worktreeId)
            return { success: true, context: 'Use pnpm for local tasks.' }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-get-context-1',
        method: 'worktreeOps.getContext',
        params: { worktreeId: 'worktree-1' }
      })
    )

    expect(response).toEqual({
      id: 'worktree-ops-get-context-1',
      ok: true,
      value: { success: true, context: 'Use pnpm for local tasks.' }
    })
    expect(calls).toEqual(['worktree-1'])
  })

  it('validates worktreeOps.getContext params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false }),
        getContext: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-get-context-2',
        method: 'worktreeOps.getContext',
        params: { worktreeId: '' }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-ops-get-context-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles worktreeOps.updateContext through the worktree ops RPC domain', async () => {
    const calls: Array<{ worktreeId: string; context: string | null }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false }),
        updateContext: (worktreeId, context) =>
          Effect.sync(() => {
            calls.push({ worktreeId, context })
            return { success: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-update-context-1',
        method: 'worktreeOps.updateContext',
        params: { worktreeId: 'worktree-1', context: 'Use pnpm for local tasks.' }
      })
    )

    expect(response).toEqual({
      id: 'worktree-ops-update-context-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([{ worktreeId: 'worktree-1', context: 'Use pnpm for local tasks.' }])
  })

  it('validates worktreeOps.updateContext params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      worktreeOps: {
        hasCommits: () => Effect.succeed(false),
        create: () => Effect.succeed({ success: false }),
        delete: () => Effect.succeed({ success: false }),
        sync: () => Effect.succeed({ success: false }),
        exists: () => Effect.succeed(false),
        openInTerminal: () => Effect.succeed({ success: false }),
        openInEditor: () => Effect.succeed({ success: false }),
        getBranches: () => Effect.succeed({ success: false }),
        branchExists: () => Effect.succeed(false),
        duplicate: () => Effect.succeed({ success: false }),
        renameBranch: () => Effect.succeed({ success: false }),
        createFromBranch: () => Effect.succeed({ success: false }),
        updateContext: () => Effect.succeed({ success: false })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'worktree-ops-update-context-2',
        method: 'worktreeOps.updateContext',
        params: { worktreeId: 'worktree-1', context: undefined }
      })
    )

    expect(response).toMatchObject({
      id: 'worktree-ops-update-context-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles systemOps.openInChrome through the system ops RPC domain', async () => {
    const calls: Array<{ url: string; customCommand?: string }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true }),
        openInChrome: (url, customCommand) =>
          Effect.sync(() => {
            calls.push({ url, customCommand })
            return { success: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-open-in-chrome-1',
        method: 'systemOps.openInChrome',
        params: { url: 'https://example.com', customCommand: 'google-chrome {url}' }
      })
    )

    expect(response).toEqual({
      id: 'system-ops-open-in-chrome-1',
      ok: true,
      value: { success: true }
    })
    expect(calls).toEqual([{ url: 'https://example.com', customCommand: 'google-chrome {url}' }])
  })

  it('validates systemOps.openInChrome params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true }),
        openInChrome: () => Effect.succeed({ success: true })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-open-in-chrome-2',
        method: 'systemOps.openInChrome',
        params: { customCommand: 'google-chrome {url}' }
      })
    )

    expect(response).toMatchObject({
      id: 'system-ops-open-in-chrome-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles systemOps.updateMenuState through the system ops RPC domain', async () => {
    const calls: Array<{
      hasActiveSession: boolean
      hasActiveWorktree: boolean
      canUndo?: boolean
      canRedo?: boolean
    }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true }),
        updateMenuState: (state) =>
          Effect.sync(() => {
            calls.push(state)
          })
      }
    })

    const state = {
      hasActiveSession: true,
      hasActiveWorktree: false,
      canUndo: false,
      canRedo: true
    }
    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-update-menu-state-1',
        method: 'systemOps.updateMenuState',
        params: state
      })
    )

    expect(response).toEqual({
      id: 'system-ops-update-menu-state-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([state])
  })

  it('validates systemOps.updateMenuState params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true }),
        updateMenuState: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-update-menu-state-2',
        method: 'systemOps.updateMenuState',
        params: { hasActiveSession: true }
      })
    )

    expect(response).toMatchObject({
      id: 'system-ops-update-menu-state-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles systemOps.isPackaged through the system ops RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true }),
        isPackaged: () => Effect.succeed(true)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-is-packaged-1',
        method: 'systemOps.isPackaged',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'system-ops-is-packaged-1',
      ok: true,
      value: true
    })
  })

  it('validates systemOps.isPackaged params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true }),
        isPackaged: () => Effect.succeed(false)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-is-packaged-2',
        method: 'systemOps.isPackaged',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'system-ops-is-packaged-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles systemOps.getPlatform through the system ops RPC domain', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true }),
        getPlatform: () => Effect.succeed('darwin')
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-get-platform-1',
        method: 'systemOps.getPlatform',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'system-ops-get-platform-1',
      ok: true,
      value: 'darwin'
    })
  })

  it('validates systemOps.getPlatform params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true }),
        getPlatform: () => Effect.succeed('linux')
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-get-platform-2',
        method: 'systemOps.getPlatform',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'system-ops-get-platform-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles systemOps.setKeepAwake through the system ops RPC domain', async () => {
    const calls: boolean[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true }),
        setKeepAwake: (active) =>
          Effect.sync(() => {
            calls.push(active)
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-set-keep-awake-1',
        method: 'systemOps.setKeepAwake',
        params: { active: true }
      })
    )

    expect(response).toEqual({
      id: 'system-ops-set-keep-awake-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([true])
  })

  it('validates systemOps.setKeepAwake params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true }),
        setKeepAwake: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-set-keep-awake-2',
        method: 'systemOps.setKeepAwake',
        params: { active: 'yes' }
      })
    )

    expect(response).toMatchObject({
      id: 'system-ops-set-keep-awake-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles systemOps.setSessionQueuedState through the system ops RPC domain', async () => {
    const calls: Array<{ sessionId: string; hasQueued: boolean }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true }),
        setSessionQueuedState: (sessionId, hasQueued) =>
          Effect.sync(() => {
            calls.push({ sessionId, hasQueued })
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-set-session-queued-state-1',
        method: 'systemOps.setSessionQueuedState',
        params: { sessionId: 'session-1', hasQueued: true }
      })
    )

    expect(response).toEqual({
      id: 'system-ops-set-session-queued-state-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ sessionId: 'session-1', hasQueued: true }])
  })

  it('validates systemOps.setSessionQueuedState params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      systemOps: {
        getLogDir: () => Effect.succeed('/tmp/hive-logs'),
        getAppVersion: () => Effect.succeed('1.1.10'),
        getAppPaths: () =>
          Effect.succeed({ userData: '/tmp/hive-user-data', home: '/tmp', logs: '/tmp/hive-logs' }),
        isLogMode: () => Effect.succeed(true),
        detectAgentSdks: () => Effect.succeed({ opencode: true, claude: false, codex: true }),
        quitApp: () => Effect.succeed(undefined),
        openInApp: () => Effect.succeed({ success: true }),
        setSessionQueuedState: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'system-ops-set-session-queued-state-2',
        method: 'systemOps.setSessionQueuedState',
        params: { sessionId: 'session-1', hasQueued: 'yes' }
      })
    )

    expect(response).toMatchObject({
      id: 'system-ops-set-session-queued-state-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles updaterOps.checkForUpdate through the updater ops RPC domain', async () => {
    const calls: Array<{ manual?: boolean }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      updaterOps: {
        checkForUpdate: (options) =>
          Effect.sync(() => {
            calls.push(options ?? {})
          }),
        downloadUpdate: () => Effect.succeed(undefined),
        installUpdate: () => Effect.succeed(undefined),
        setChannel: () => Effect.succeed(undefined),
        getVersion: () => Effect.succeed('1.1.10')
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'updater-check-for-update-1',
        method: 'updaterOps.checkForUpdate',
        params: { manual: true }
      })
    )

    expect(response).toEqual({
      id: 'updater-check-for-update-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([{ manual: true }])
  })

  it('validates updaterOps.checkForUpdate params', async () => {
    const calls: Array<{ manual?: boolean }> = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      updaterOps: {
        checkForUpdate: (options) =>
          Effect.sync(() => {
            calls.push(options ?? {})
          }),
        downloadUpdate: () => Effect.succeed(undefined),
        installUpdate: () => Effect.succeed(undefined),
        setChannel: () => Effect.succeed(undefined),
        getVersion: () => Effect.succeed('1.1.10')
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'updater-check-for-update-2',
        method: 'updaterOps.checkForUpdate',
        params: { manual: 'yes' }
      })
    )

    expect(response).toMatchObject({
      id: 'updater-check-for-update-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles updaterOps.downloadUpdate through the updater ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      updaterOps: {
        checkForUpdate: () => Effect.succeed(undefined),
        downloadUpdate: () =>
          Effect.sync(() => {
            calls.push('downloadUpdate')
          }),
        installUpdate: () => Effect.succeed(undefined),
        setChannel: () => Effect.succeed(undefined),
        getVersion: () => Effect.succeed('1.1.10')
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'updater-download-update-1',
        method: 'updaterOps.downloadUpdate',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'updater-download-update-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual(['downloadUpdate'])
  })

  it('validates updaterOps.downloadUpdate params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      updaterOps: {
        checkForUpdate: () => Effect.succeed(undefined),
        downloadUpdate: () =>
          Effect.sync(() => {
            calls.push('downloadUpdate')
          }),
        installUpdate: () => Effect.succeed(undefined),
        setChannel: () => Effect.succeed(undefined),
        getVersion: () => Effect.succeed('1.1.10')
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'updater-download-update-2',
        method: 'updaterOps.downloadUpdate',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'updater-download-update-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles updaterOps.installUpdate through the updater ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      updaterOps: {
        checkForUpdate: () => Effect.succeed(undefined),
        downloadUpdate: () => Effect.succeed(undefined),
        installUpdate: () =>
          Effect.sync(() => {
            calls.push('installUpdate')
          }),
        setChannel: () => Effect.succeed(undefined),
        getVersion: () => Effect.succeed('1.1.10')
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'updater-install-update-1',
        method: 'updaterOps.installUpdate',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'updater-install-update-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual(['installUpdate'])
  })

  it('validates updaterOps.installUpdate params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      updaterOps: {
        checkForUpdate: () => Effect.succeed(undefined),
        downloadUpdate: () => Effect.succeed(undefined),
        installUpdate: () =>
          Effect.sync(() => {
            calls.push('installUpdate')
          }),
        setChannel: () => Effect.succeed(undefined),
        getVersion: () => Effect.succeed('1.1.10')
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'updater-install-update-2',
        method: 'updaterOps.installUpdate',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'updater-install-update-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles updaterOps.setChannel through the updater ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      updaterOps: {
        checkForUpdate: () => Effect.succeed(undefined),
        downloadUpdate: () => Effect.succeed(undefined),
        installUpdate: () => Effect.succeed(undefined),
        setChannel: (channel) =>
          Effect.sync(() => {
            calls.push(channel)
          }),
        getVersion: () => Effect.succeed('1.1.10')
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'updater-set-channel-1',
        method: 'updaterOps.setChannel',
        params: { channel: 'canary' }
      })
    )

    expect(response).toEqual({
      id: 'updater-set-channel-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual(['canary'])
  })

  it('validates updaterOps.setChannel params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      updaterOps: {
        checkForUpdate: () => Effect.succeed(undefined),
        downloadUpdate: () => Effect.succeed(undefined),
        installUpdate: () => Effect.succeed(undefined),
        setChannel: (channel) =>
          Effect.sync(() => {
            calls.push(channel)
          }),
        getVersion: () => Effect.succeed('1.1.10')
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'updater-set-channel-2',
        method: 'updaterOps.setChannel',
        params: { channel: 'beta' }
      })
    )

    expect(response).toMatchObject({
      id: 'updater-set-channel-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles updaterOps.getVersion through the updater ops RPC domain', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      updaterOps: {
        checkForUpdate: () => Effect.succeed(undefined),
        downloadUpdate: () => Effect.succeed(undefined),
        installUpdate: () => Effect.succeed(undefined),
        setChannel: () => Effect.succeed(undefined),
        getVersion: () =>
          Effect.sync(() => {
            calls.push('getVersion')
            return '1.2.3'
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'updater-get-version-1',
        method: 'updaterOps.getVersion',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'updater-get-version-1',
      ok: true,
      value: '1.2.3'
    })
    expect(calls).toEqual(['getVersion'])
  })

  it('validates updaterOps.getVersion params', async () => {
    const calls: string[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      updaterOps: {
        checkForUpdate: () => Effect.succeed(undefined),
        downloadUpdate: () => Effect.succeed(undefined),
        installUpdate: () => Effect.succeed(undefined),
        setChannel: () => Effect.succeed(undefined),
        getVersion: () =>
          Effect.sync(() => {
            calls.push('getVersion')
            return '1.2.3'
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'updater-get-version-2',
        method: 'updaterOps.getVersion',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'updater-get-version-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
    expect(calls).toEqual([])
  })

  it('handles petOps.show through the pet ops RPC domain', async () => {
    let shown = false
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () =>
          Effect.sync(() => {
            shown = true
          }),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-show-1',
        method: 'petOps.show',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'pet-ops-show-1',
      ok: true,
      value: undefined
    })
    expect(shown).toBe(true)
  })

  it('validates petOps.show params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-show-2',
        method: 'petOps.show',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'pet-ops-show-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles petOps.hide through the pet ops RPC domain', async () => {
    let hidden = false
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () =>
          Effect.sync(() => {
            hidden = true
          }),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-hide-1',
        method: 'petOps.hide',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'pet-ops-hide-1',
      ok: true,
      value: undefined
    })
    expect(hidden).toBe(true)
  })

  it('validates petOps.hide params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-hide-2',
        method: 'petOps.hide',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'pet-ops-hide-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles petOps.publishStatus through the pet ops RPC domain', async () => {
    const calls: Array<{ state: string; sourceWorktreeId: string | null }> = []
    const events: Array<{ channel: string; payload: unknown }> = []
    const eventBus = makeEventBus()
    const unsubscribe = Effect.runSync(
      eventBus.subscribeAll((event) => {
        events.push(event)
      })
    )
    const router = makeRpcRouter({
      eventBus,
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: (payload) =>
          Effect.sync(() => {
            calls.push(payload)
          }),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined)
      }
    })

    const payload = {
      state: 'working' as const,
      sourceWorktreeId: 'worktree-1',
      workingSessionCount: 1
    }
    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-publish-status-1',
        method: 'petOps.publishStatus',
        params: payload
      })
    )

    expect(response).toEqual({
      id: 'pet-ops-publish-status-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([payload])
    expect(events).toEqual([{ channel: PET_STATUS_CHANNEL, payload }])
    unsubscribe()
  })

  it('validates petOps.publishStatus params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-publish-status-2',
        method: 'petOps.publishStatus',
        params: { state: 'sleeping', sourceWorktreeId: 'worktree-1' }
      })
    )

    expect(response).toMatchObject({
      id: 'pet-ops-publish-status-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles petOps.setIgnoreMouse through the pet ops RPC domain', async () => {
    const calls: boolean[] = []
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: (ignore) =>
          Effect.sync(() => {
            calls.push(ignore)
          }),
        beginPointerInteraction: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-set-ignore-mouse-1',
        method: 'petOps.setIgnoreMouse',
        params: { ignore: true }
      })
    )

    expect(response).toEqual({
      id: 'pet-ops-set-ignore-mouse-1',
      ok: true,
      value: undefined
    })
    expect(calls).toEqual([true])
  })

  it('validates petOps.setIgnoreMouse params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-set-ignore-mouse-2',
        method: 'petOps.setIgnoreMouse',
        params: { ignore: 'yes' }
      })
    )

    expect(response).toMatchObject({
      id: 'pet-ops-set-ignore-mouse-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles petOps.beginPointerInteraction through the pet ops RPC domain', async () => {
    let began = false
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () =>
          Effect.sync(() => {
            began = true
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-begin-pointer-interaction-1',
        method: 'petOps.beginPointerInteraction',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'pet-ops-begin-pointer-interaction-1',
      ok: true,
      value: undefined
    })
    expect(began).toBe(true)
  })

  it('validates petOps.beginPointerInteraction params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-begin-pointer-interaction-2',
        method: 'petOps.beginPointerInteraction',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'pet-ops-begin-pointer-interaction-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles petOps.endPointerInteraction through the pet ops RPC domain', async () => {
    let ended = false
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined),
        endPointerInteraction: () =>
          Effect.sync(() => {
            ended = true
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-end-pointer-interaction-1',
        method: 'petOps.endPointerInteraction',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'pet-ops-end-pointer-interaction-1',
      ok: true,
      value: undefined
    })
    expect(ended).toBe(true)
  })

  it('validates petOps.endPointerInteraction params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined),
        endPointerInteraction: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-end-pointer-interaction-2',
        method: 'petOps.endPointerInteraction',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'pet-ops-end-pointer-interaction-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles petOps.move through the pet ops RPC domain', async () => {
    let movedTo: { x: number; y: number } | null = null
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined),
        endPointerInteraction: () => Effect.succeed(undefined),
        move: (position) =>
          Effect.sync(() => {
            movedTo = position
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-move-1',
        method: 'petOps.move',
        params: { x: 42, y: 84 }
      })
    )

    expect(response).toEqual({
      id: 'pet-ops-move-1',
      ok: true,
      value: undefined
    })
    expect(movedTo).toEqual({ x: 42, y: 84 })
  })

  it('validates petOps.move params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined),
        endPointerInteraction: () => Effect.succeed(undefined),
        move: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-move-2',
        method: 'petOps.move',
        params: { x: 42, y: '84' }
      })
    )

    expect(response).toMatchObject({
      id: 'pet-ops-move-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles petOps.focusMain through the pet ops RPC domain', async () => {
    let focusedWorktreeId: string | null | undefined
    const events: Array<{ channel: string; payload: unknown }> = []
    const eventBus = makeEventBus()
    const unsubscribe = Effect.runSync(
      eventBus.subscribeAll((event) => {
        events.push(event)
      })
    )
    const router = makeRpcRouter({
      eventBus,
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined),
        endPointerInteraction: () => Effect.succeed(undefined),
        move: () => Effect.succeed(undefined),
        focusMain: (payload) =>
          Effect.sync(() => {
            focusedWorktreeId = payload.worktreeId
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-focus-main-1',
        method: 'petOps.focusMain',
        params: { worktreeId: 'worktree-1' }
      })
    )

    expect(response).toEqual({
      id: 'pet-ops-focus-main-1',
      ok: true,
      value: undefined
    })
    expect(focusedWorktreeId).toBe('worktree-1')
    expect(events).toEqual([
      { channel: PET_JUMP_TO_WORKTREE_CHANNEL, payload: { worktreeId: 'worktree-1' } }
    ])
    unsubscribe()
  })

  it('validates petOps.focusMain params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined),
        endPointerInteraction: () => Effect.succeed(undefined),
        move: () => Effect.succeed(undefined),
        focusMain: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-focus-main-2',
        method: 'petOps.focusMain',
        params: { worktreeId: 123 }
      })
    )

    expect(response).toMatchObject({
      id: 'pet-ops-focus-main-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles petOps.getConfig through the pet ops RPC domain', async () => {
    const config = {
      settings: {
        enabled: true,
        petId: 'bee',
        size: 'M' as const,
        opacity: 1,
        hasHatched: false
      },
      position: { x: 42, y: 84 },
      manifest: {
        id: 'bee',
        name: 'Bee',
        version: '1.0.0',
        assets: {
          idle: 'assets/bee.png',
          working: 'assets/bee.png',
          question: 'assets/bee.png',
          permission: 'assets/bee.png',
          plan_ready: 'assets/bee.png'
        }
      }
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined),
        endPointerInteraction: () => Effect.succeed(undefined),
        move: () => Effect.succeed(undefined),
        focusMain: () => Effect.succeed(undefined),
        getConfig: () => Effect.succeed(config)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-get-config-1',
        method: 'petOps.getConfig',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'pet-ops-get-config-1',
      ok: true,
      value: config
    })
  })

  it('validates petOps.getConfig params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined),
        endPointerInteraction: () => Effect.succeed(undefined),
        move: () => Effect.succeed(undefined),
        focusMain: () => Effect.succeed(undefined),
        getConfig: () =>
          Effect.succeed({
            settings: {
              enabled: true,
              petId: 'bee',
              size: 'M' as const,
              opacity: 1,
              hasHatched: false
            },
            position: { x: 42, y: 84 },
            manifest: {
              id: 'bee',
              name: 'Bee',
              version: '1.0.0',
              assets: {
                idle: 'assets/bee.png',
                working: 'assets/bee.png',
                question: 'assets/bee.png',
                permission: 'assets/bee.png',
                plan_ready: 'assets/bee.png'
              }
            }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-get-config-2',
        method: 'petOps.getConfig',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'pet-ops-get-config-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles petOps.getCurrentStatus through the pet ops RPC domain', async () => {
    const status = {
      state: 'working' as const,
      sourceWorktreeId: 'worktree-1',
      workingSessionCount: 1
    }
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined),
        endPointerInteraction: () => Effect.succeed(undefined),
        move: () => Effect.succeed(undefined),
        focusMain: () => Effect.succeed(undefined),
        getConfig: () =>
          Effect.succeed({
            settings: {
              enabled: true,
              petId: 'bee',
              size: 'M' as const,
              opacity: 1,
              hasHatched: false
            },
            position: { x: 42, y: 84 },
            manifest: {
              id: 'bee',
              name: 'Bee',
              version: '1.0.0',
              assets: {
                idle: 'assets/bee.png',
                working: 'assets/bee.png',
                question: 'assets/bee.png',
                permission: 'assets/bee.png',
                plan_ready: 'assets/bee.png'
              }
            }
          }),
        getCurrentStatus: () => Effect.succeed(status)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-get-current-status-1',
        method: 'petOps.getCurrentStatus',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'pet-ops-get-current-status-1',
      ok: true,
      value: status
    })
  })

  it('validates petOps.getCurrentStatus params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined),
        endPointerInteraction: () => Effect.succeed(undefined),
        move: () => Effect.succeed(undefined),
        focusMain: () => Effect.succeed(undefined),
        getConfig: () =>
          Effect.succeed({
            settings: {
              enabled: true,
              petId: 'bee',
              size: 'M' as const,
              opacity: 1,
              hasHatched: false
            },
            position: { x: 42, y: 84 },
            manifest: {
              id: 'bee',
              name: 'Bee',
              version: '1.0.0',
              assets: {
                idle: 'assets/bee.png',
                working: 'assets/bee.png',
                question: 'assets/bee.png',
                permission: 'assets/bee.png',
                plan_ready: 'assets/bee.png'
              }
            }
          }),
        getCurrentStatus: () =>
          Effect.succeed({
            state: 'working' as const,
            sourceWorktreeId: 'worktree-1',
            workingSessionCount: 1
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-get-current-status-2',
        method: 'petOps.getCurrentStatus',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'pet-ops-get-current-status-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles petOps.updateSettings through the pet ops RPC domain', async () => {
    let updatedSettings: unknown
    let currentSettings: PetSettings = {
      enabled: false,
      petId: 'bee',
      size: 'M',
      opacity: 1,
      hasHatched: false
    }
    const events: Array<{ channel: string; payload: unknown }> = []
    const eventBus = makeEventBus()
    const unsubscribe = Effect.runSync(
      eventBus.subscribeAll((event) => {
        events.push(event)
      })
    )
    const router = makeRpcRouter({
      eventBus,
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined),
        endPointerInteraction: () => Effect.succeed(undefined),
        move: () => Effect.succeed(undefined),
        focusMain: () => Effect.succeed(undefined),
        getConfig: () =>
          Effect.succeed({
            settings: currentSettings,
            position: { x: 42, y: 84 },
            manifest: {
              id: 'bee',
              name: 'Bee',
              version: '1.0.0',
              assets: {
                idle: 'assets/bee.png',
                working: 'assets/bee.png',
                question: 'assets/bee.png',
                permission: 'assets/bee.png',
                plan_ready: 'assets/bee.png'
              }
            }
          }),
        getCurrentStatus: () =>
          Effect.succeed({
            state: 'working' as const,
            sourceWorktreeId: 'worktree-1',
            workingSessionCount: 1
          }),
        updateSettings: (partial) =>
          Effect.sync(() => {
            updatedSettings = partial
            currentSettings = { ...currentSettings, ...partial }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-update-settings-1',
        method: 'petOps.updateSettings',
        params: { enabled: true, size: 'L', opacity: 0.75 }
      })
    )

    expect(response).toEqual({
      id: 'pet-ops-update-settings-1',
      ok: true,
      value: undefined
    })
    expect(updatedSettings).toEqual({ enabled: true, size: 'L', opacity: 0.75 })
    expect(events).toEqual([{ channel: PET_SETTINGS_UPDATED_CHANNEL, payload: currentSettings }])
    unsubscribe()
  })

  it('validates petOps.updateSettings params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined),
        endPointerInteraction: () => Effect.succeed(undefined),
        move: () => Effect.succeed(undefined),
        focusMain: () => Effect.succeed(undefined),
        getConfig: () =>
          Effect.succeed({
            settings: {
              enabled: true,
              petId: 'bee',
              size: 'M' as const,
              opacity: 1,
              hasHatched: false
            },
            position: { x: 42, y: 84 },
            manifest: {
              id: 'bee',
              name: 'Bee',
              version: '1.0.0',
              assets: {
                idle: 'assets/bee.png',
                working: 'assets/bee.png',
                question: 'assets/bee.png',
                permission: 'assets/bee.png',
                plan_ready: 'assets/bee.png'
              }
            }
          }),
        getCurrentStatus: () =>
          Effect.succeed({
            state: 'working' as const,
            sourceWorktreeId: 'worktree-1',
            workingSessionCount: 1
          }),
        updateSettings: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-update-settings-2',
        method: 'petOps.updateSettings',
        params: { enabled: 'yes' }
      })
    )

    expect(response).toMatchObject({
      id: 'pet-ops-update-settings-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })

  it('handles petOps.markHatched through the pet ops RPC domain', async () => {
    let marked = false
    let currentSettings: PetSettings = {
      enabled: true,
      petId: 'bee',
      size: 'M',
      opacity: 1,
      hasHatched: false
    }
    const events: Array<{ channel: string; payload: unknown }> = []
    const eventBus = makeEventBus()
    const unsubscribe = Effect.runSync(
      eventBus.subscribeAll((event) => {
        events.push(event)
      })
    )
    const router = makeRpcRouter({
      eventBus,
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined),
        endPointerInteraction: () => Effect.succeed(undefined),
        move: () => Effect.succeed(undefined),
        focusMain: () => Effect.succeed(undefined),
        getConfig: () =>
          Effect.succeed({
            settings: currentSettings,
            position: { x: 42, y: 84 },
            manifest: {
              id: 'bee',
              name: 'Bee',
              version: '1.0.0',
              assets: {
                idle: 'assets/bee.png',
                working: 'assets/bee.png',
                question: 'assets/bee.png',
                permission: 'assets/bee.png',
                plan_ready: 'assets/bee.png'
              }
            }
          }),
        getCurrentStatus: () =>
          Effect.succeed({
            state: 'working' as const,
            sourceWorktreeId: 'worktree-1',
            workingSessionCount: 1
          }),
        updateSettings: () => Effect.succeed(undefined),
        markHatched: () =>
          Effect.sync(() => {
            marked = true
            currentSettings = { ...currentSettings, hasHatched: true }
          })
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-mark-hatched-1',
        method: 'petOps.markHatched',
        params: {}
      })
    )

    expect(response).toEqual({
      id: 'pet-ops-mark-hatched-1',
      ok: true,
      value: undefined
    })
    expect(marked).toBe(true)
    expect(events).toEqual([{ channel: PET_SETTINGS_UPDATED_CHANNEL, payload: currentSettings }])
    unsubscribe()
  })

  it('validates petOps.markHatched params', async () => {
    const router = makeRpcRouter({
      eventBus: makeEventBus(),
      petOps: {
        show: () => Effect.succeed(undefined),
        hide: () => Effect.succeed(undefined),
        publishStatus: () => Effect.succeed(undefined),
        setIgnoreMouse: () => Effect.succeed(undefined),
        beginPointerInteraction: () => Effect.succeed(undefined),
        endPointerInteraction: () => Effect.succeed(undefined),
        move: () => Effect.succeed(undefined),
        focusMain: () => Effect.succeed(undefined),
        getConfig: () =>
          Effect.succeed({
            settings: {
              enabled: true,
              petId: 'bee',
              size: 'M' as const,
              opacity: 1,
              hasHatched: false
            },
            position: { x: 42, y: 84 },
            manifest: {
              id: 'bee',
              name: 'Bee',
              version: '1.0.0',
              assets: {
                idle: 'assets/bee.png',
                working: 'assets/bee.png',
                question: 'assets/bee.png',
                permission: 'assets/bee.png',
                plan_ready: 'assets/bee.png'
              }
            }
          }),
        getCurrentStatus: () =>
          Effect.succeed({
            state: 'working' as const,
            sourceWorktreeId: 'worktree-1',
            workingSessionCount: 1
          }),
        updateSettings: () => Effect.succeed(undefined),
        markHatched: () => Effect.succeed(undefined)
      }
    })

    const response = await Effect.runPromise(
      router.handle({
        id: 'pet-ops-mark-hatched-2',
        method: 'petOps.markHatched',
        params: { unexpected: true }
      })
    )

    expect(response).toMatchObject({
      id: 'pet-ops-mark-hatched-2',
      ok: false,
      error: { code: 'VALIDATION_FAILED' }
    })
  })
})
