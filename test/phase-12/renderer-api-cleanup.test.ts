import { describe, expect, it } from 'vitest'
import fs from 'fs'
import path from 'path'

describe('renderer API cleanup', () => {
  const readOptionalSource = (relativePath: string): string => {
    const absolutePath = path.resolve(__dirname, relativePath)
    return fs.existsSync(absolutePath) ? fs.readFileSync(absolutePath, 'utf-8') : ''
  }

  const getPreloadUpdaterOpsSource = (preloadSource: string): string => {
    const updaterOpsStart = preloadSource.indexOf('const updaterOps = {')
    if (updaterOpsStart === -1) return ''

    const updaterOpsEnd = preloadSource.indexOf('// Connection operations API', updaterOpsStart)

    expect(updaterOpsEnd).toBeGreaterThan(updaterOpsStart)

    return preloadSource.slice(updaterOpsStart, updaterOpsEnd)
  }

  const getPreloadTerminalOpsSource = (preloadSource: string): string => {
    const terminalOpsStart = preloadSource.indexOf('const terminalOps =')
    if (terminalOpsStart === -1) return ''

    const terminalOpsEnd = preloadSource.indexOf('// Connection operations API', terminalOpsStart)

    expect(terminalOpsEnd).toBeGreaterThan(terminalOpsStart)

    return preloadSource.slice(terminalOpsStart, terminalOpsEnd)
  }

  const getPreloadScriptOpsSource = (preloadSource: string): string => {
    const scriptOpsStart = preloadSource.indexOf('const scriptOps =')
    if (scriptOpsStart === -1) return ''

    const scriptOpsEnd = preloadSource.indexOf('// File operations API', scriptOpsStart)

    expect(scriptOpsEnd).toBeGreaterThan(scriptOpsStart)

    return preloadSource.slice(scriptOpsStart, scriptOpsEnd)
  }

  it('keeps renderer startup off the legacy window compatibility shim', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/main.tsx'),
      'utf-8'
    )
    const bootstrapStart = source.indexOf('const bootstrap = async (): Promise<void> => {')
    const bootstrapEnd = source.indexOf('\nvoid bootstrap()', bootstrapStart)
    const bootstrapSource = source.slice(bootstrapStart, bootstrapEnd)
    const createClientIndex = bootstrapSource.indexOf('const client = await createHiveClient()')
    const setClientIndex = bootstrapSource.indexOf('setRendererRpcClient(client)')
    const renderIndex = bootstrapSource.indexOf('await renderApp()')

    expect(bootstrapStart).toBeGreaterThan(-1)
    expect(bootstrapEnd).toBeGreaterThan(bootstrapStart)
    expect(source).not.toContain("import App from './App'")
    expect(source).toContain("const { default: App } = await import('./App')")
    expect(source).toContain("import { createHiveClient } from './api/hive-client'")
    expect(source).toContain("import { setRendererRpcClient } from './api/rpc-client'")
    expect(source).not.toContain("import { installLegacyWindowApi } from './api/legacy-window-api'")
    expect(source).not.toContain('installLegacyWindowApi(client)')
    expect(source).not.toContain('legacy-window-api')
    expect(createClientIndex).toBeGreaterThan(-1)
    expect(setClientIndex).toBeGreaterThan(createClientIndex)
    expect(renderIndex).toBeGreaterThan(setClientIndex)
    expect(bootstrapSource).not.toContain('ipcRenderer')
    expect(bootstrapSource).not.toContain('window.db =')
    expect(bootstrapSource).not.toContain("contextBridge.exposeInMainWorld('db'")
  })

  it('removes the obsolete legacy window compatibility unit test suite', () => {
    expect(
      fs.existsSync(
        path.resolve(__dirname, '../../src/renderer/src/api/__tests__/legacy-window-api.test.ts')
      )
    ).toBe(false)
  })

  it('removes the obsolete legacy window compatibility shim source', () => {
    expect(
      fs.existsSync(path.resolve(__dirname, '../../src/renderer/src/api/legacy-window-api.ts'))
    ).toBe(false)
  })

  it('keeps preload namespace exposure limited to the desktop bridge', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const businessGlobals = [
      'accountOps',
      'analyticsOps',
      'attachmentOps',
      'bash',
      'codexDebugLoggerOps',
      'connectionOps',
      'db',
      'fileOps',
      'fileTreeOps',
      'gitOps',
      'kanban',
      'loggingOps',
      'opencodeOps',
      'perfDiagnosticsOps',
      'petOps',
      'projectOps',
      'scriptOps',
      'settingsOps',
      'systemOps',
      'telegramOps',
      'terminalOps',
      'ticketImport',
      'updaterOps',
      'usageOps',
      'worktreeOps'
    ]

    expect(preloadSource.match(/contextBridge\.exposeInMainWorld/g)).toHaveLength(1)
    expect(preloadSource).toContain("contextBridge.exposeInMainWorld('desktopBridge'")
    expect(preloadSource).toContain('window.desktopBridge = desktopBridge')
    expect(preloadTypesSource).toContain('interface Window')
    expect(preloadTypesSource).toContain('desktopBridge: {')

    for (const globalName of businessGlobals) {
      expect(preloadSource).not.toContain(`contextBridge.exposeInMainWorld('${globalName}'`)
      expect(preloadSource).not.toContain(`contextBridge.exposeInMainWorld("${globalName}"`)
      expect(preloadSource).not.toContain(`window.${globalName} =`)
      expect(preloadTypesSource).not.toContain(`${globalName}:`)
    }
  })

  it('keeps old IPC-specific tests converted to RPC and service contracts', () => {
    const convertedTests = [
      '../../test/phase-6/session-5/prompt-parts-api.test.ts',
      '../../test/phase-18/session-10/pr-github-backend.test.ts',
      '../../test/phase-22/session-5/codex-child-session-routing.test.ts'
    ]
    const stalePhrases = [
      'IPC handler contract',
      'Preload bridge contract',
      'IPC handler -',
      'Preload bridge -',
      'IPC handler style',
      'IPC handler prepends'
    ]

    for (const relativePath of convertedTests) {
      const source = fs.readFileSync(path.resolve(__dirname, relativePath), 'utf-8')

      for (const stalePhrase of stalePhrases) {
        expect(source).not.toContain(stalePhrase)
      }
      expect(source).not.toContain('ipcMain')
      expect(source).not.toContain('ipcRenderer')
    }
  })

  it('keeps the README architecture diagram on HTTP/RPC and WebSocket transport', () => {
    const source = fs.readFileSync(path.resolve(__dirname, '../../README.md'), 'utf-8')
    const architectureStart = source.indexOf('### Architecture')
    const architectureEnd = source.indexOf('### Tech Stack', architectureStart)
    const architectureSource = source.slice(architectureStart, architectureEnd)

    expect(architectureStart).toBeGreaterThan(-1)
    expect(architectureEnd).toBeGreaterThan(architectureStart)
    expect(architectureSource).toContain('local Effect backend')
    expect(architectureSource).toContain('localhost:{freePort}')
    expect(architectureSource).toContain('HTTP/RPC + WebSocket')
    expect(architectureSource).toContain('RPC Domains')
    expect(architectureSource).toContain('desktopBridge only')
    expect(architectureSource).toContain('src/server')
    expect(architectureSource).not.toContain('IPC Handlers')
    expect(architectureSource).not.toContain('Typed IPC')
    expect(architectureSource).not.toContain('window.* APIs')
    expect(architectureSource).not.toContain('├── ipc/')
  })

  it('keeps explicit renderer API modules for every migrated RPC domain', () => {
    const apiRoot = path.resolve(__dirname, '../../src/renderer/src/api')
    const requiredDomainApis = new Map<string, string>([
      ['accountOps', 'account-api.ts'],
      ['analyticsOps', 'analytics-api.ts'],
      ['attachmentOps', 'attachment-api.ts'],
      ['bash', 'bash-api.ts'],
      ['codexDebugLoggerOps', 'codex-debug-logger-api.ts'],
      ['connectionOps', 'connection-api.ts'],
      ['db', 'db-api.ts'],
      ['fileOps', 'file-api.ts'],
      ['fileTreeOps', 'file-tree-api.ts'],
      ['gitOps', 'git-api.ts'],
      ['kanban', 'kanban-api.ts'],
      ['loggingOps', 'logging-api.ts'],
      ['opencodeOps', 'opencode-api.ts'],
      ['perfDiagnosticsOps', 'perf-diagnostics-api.ts'],
      ['petOps', 'pet-api.ts'],
      ['projectOps', 'project-api.ts'],
      ['scriptOps', 'script-api.ts'],
      ['settingsOps', 'settings-api.ts'],
      ['systemOps', 'system-api.ts'],
      ['telegramOps', 'telegram-api.ts'],
      ['terminalOps', 'terminal-api.ts'],
      ['ticketImport', 'ticket-import-api.ts'],
      ['updaterOps', 'updater-api.ts'],
      ['usageOps', 'usage-api.ts'],
      ['worktreeOps', 'worktree-api.ts']
    ])

    for (const [domain, filename] of requiredDomainApis) {
      const source = fs.readFileSync(path.join(apiRoot, filename), 'utf-8')
      const exportedApiName = `${domain.replace(/Ops$/, '')}Api`

      expect(source).toContain(`export const ${exportedApiName}`)
      expect(source).toContain("getRendererRpcClient().request")
      expect(source).not.toContain(`window.${domain}`)
    }
  })

  it('keeps global test setup from reinstalling the legacy gitOps window fallback', () => {
    const source = fs.readFileSync(path.resolve(__dirname, '../../test/setup.ts'), 'utf-8')

    expect(source).not.toContain("Object.defineProperty(window, 'gitOps'")
    expect(source).not.toContain('window.gitOps')
    expect(source).not.toContain('mockGitOps')
  })

  it('keeps global test setup from reinstalling the legacy fileTreeOps window fallback', () => {
    const source = fs.readFileSync(path.resolve(__dirname, '../../test/setup.ts'), 'utf-8')

    expect(source).not.toContain("Object.defineProperty(window, 'fileTreeOps'")
    expect(source).not.toContain('window.fileTreeOps')
    expect(source).not.toContain('mockFileTreeOps')
  })

  it('keeps global test setup from reinstalling the legacy systemOps window fallback', () => {
    const source = fs.readFileSync(path.resolve(__dirname, '../../test/setup.ts'), 'utf-8')

    expect(source).not.toContain("Object.defineProperty(window, 'systemOps'")
    expect(source).not.toContain('window.systemOps')
  })

  it('keeps global test setup from reinstalling the legacy analyticsOps window fallback', () => {
    const source = fs.readFileSync(path.resolve(__dirname, '../../test/setup.ts'), 'utf-8')

    expect(source).not.toContain("Object.defineProperty(window, 'analyticsOps'")
    expect(source).not.toContain('window.analyticsOps')
  })

  it('keeps global test setup from reinstalling the legacy petOps window fallback', () => {
    const source = fs.readFileSync(path.resolve(__dirname, '../../test/setup.ts'), 'utf-8')

    expect(source).not.toContain("Object.defineProperty(window, 'petOps'")
    expect(source).not.toContain('window.petOps')
  })

  it('keeps open-in-chrome backend settings tests on explicit API mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-15/session-10/open-in-chrome-backend.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/pet-api'")
    expect(source).toContain("vi.mock('@/api/settings-api'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain('window.db')
  })

  it('keeps agent setup codex tests on explicit API mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-22/session-2/agent-setup-guard-codex.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/system-api'")
    expect(source).toContain("vi.mock('@/api/analytics-api'")
    expect(source).not.toContain("Object.defineProperty(window, 'systemOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'analyticsOps'")
    expect(source).not.toContain('window.systemOps')
    expect(source).not.toContain('window.analyticsOps')
  })

  it('keeps worktree watcher hook tests on explicit gitApi mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-20/session-11/worktree-watcher-hook.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/git-api'")
    expect(source).toContain("vi.mock('@/api/settings-api'")
    expect(source).not.toContain("Object.defineProperty(window, 'gitOps'")
    expect(source).not.toContain('window.gitOps')
  })

  it('keeps Git merge backend tests aligned to RPC contracts instead of IPC globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-14/session-9/git-merge-backend.test.ts'),
      'utf-8'
    )

    expect(source).toContain('RPC route delegates to git service')
    expect(source).toContain("const expectedChannel = 'gitOps.merge'")
    expect(source).toContain('Renderer API contract')
    expect(source).not.toContain('IPC handler')
    expect(source).not.toContain('Preload exposes')
    expect(source).not.toContain('window.gitOps')
  })

  it('keeps per-session model schema type tests on dbApi instead of Window db globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-17/session-8/per-session-model-schema.test.ts'),
      'utf-8'
    )

    expect(source).toContain("import type { dbApi } from '../../../src/renderer/src/api/db-api'")
    expect(source).toContain('Parameters<typeof dbApi.session.create>[0]')
    expect(source).toContain('Parameters<typeof dbApi.session.update>[1]')
    expect(source).not.toContain("Window['db']")
    expect(source).not.toContain('window.db')
  })

  it('keeps ticket last-message bump tests on explicit dbApi mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/kanban/ticket-last-message-bump.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/settings-api'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain('window.db')
  })

  it('keeps server title event tests on explicit dbApi mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-11/session-2/server-title-events.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/settings-api'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain('window.db')
  })

  it('keeps handoff goal-mode relink tests on explicit kanbanApi mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/kanban/relink-handoff-goal-mode.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/kanban-api'")
    expect(source).toContain("vi.mock('@/api/settings-api'")
    expect(source).not.toContain("Object.defineProperty(window, 'kanban'")
    expect(source).not.toContain('window.kanban')
  })

  it('keeps session-kanban coordination tests on explicit kanbanApi mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/kanban/session-10/session-kanban-coordination.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/kanban-api'")
    expect(source).toContain('mockKanbanApi.ticket.move')
    expect(source).toContain('mockKanbanApi.ticket.update')
    expect(source).not.toContain("Object.defineProperty(window, 'kanban'")
    expect(source).not.toContain('window.kanban')
    expect(source).not.toContain('via IPC')
  })

  it('keeps plan-review followup dispatch tests on explicit API mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/kanban/plan-review-followup-dispatch.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/kanban-api'")
    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/opencode-api'")
    expect(source).toContain("vi.mock('@/api/worktree-api'")
    expect(source).toContain("vi.mock('@/api/git-api'")
    expect(source).toContain("vi.mock('@/api/connection-api'")
    expect(source).not.toContain("Object.defineProperty(window, 'connectionOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'kanban'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'worktreeOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'gitOps'")
    expect(source).not.toContain('window.connectionOps')
    expect(source).not.toContain('window.kanban')
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.opencodeOps')
    expect(source).not.toContain('window.worktreeOps')
    expect(source).not.toContain('window.gitOps')
  })

  it('keeps worktree picker modal tests on explicit API mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/kanban/session-9/worktree-picker-modal.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/kanban-api'")
    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/worktree-api'")
    expect(source).toContain("vi.mock('@/api/git-api'")
    expect(source).toContain("vi.mock('@/api/usage-api'")
    expect(source).toContain("vi.mock('@/api/connection-api'")
    expect(source).toContain("vi.mock('@/api/opencode-api'")
    expect(source).not.toContain("Object.defineProperty(window, 'kanban'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'worktreeOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'gitOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'usageOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'connectionOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain('window.kanban')
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.worktreeOps')
    expect(source).not.toContain('window.gitOps')
    expect(source).not.toContain('window.usageOps')
    expect(source).not.toContain('window.connectionOps')
    expect(source).not.toContain('window.opencodeOps')
  })

  it('keeps Telegram forwarding target tests on explicit API mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/telegram/telegram-forwarding-target.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/telegram-api'")
    expect(source).toContain("vi.mock('@/api/kanban-api'")
    expect(source).toContain("vi.mock('@/api/opencode-api'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'telegramOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'kanban'")
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.telegramOps')
    expect(source).not.toContain('window.kanban')
    expect(source).not.toContain('window.opencodeOps')
  })

  it('keeps Codex steer boundary tests on explicit API mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-22/session-10/codex-steer-boundary.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/opencode-api'")
    expect(source).toContain("vi.mock('@/api/system-api'")
    expect(source).toContain("vi.mock('@/api/logging-api'")
    expect(source).toContain("vi.mock('@/api/bash-api'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'systemOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'loggingOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'bash'")
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.opencodeOps')
    expect(source).not.toContain('window.systemOps')
    expect(source).not.toContain('window.bash')
  })

  it('keeps Codex lifecycle tests on agent event bus assertions', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-22/session-4/codex-lifecycle.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('../../../src/main/services/agent-event-bus'")
    expect(source).toContain('eventBusMocks.publish')
    expect(source).not.toContain('webContents.send')
    expect(source).not.toContain('setMainWindow')
    expect(source).not.toContain('getMainWindow')
  })

  it('keeps Codex title integration tests on agent event bus assertions', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/codex-session-title-integration.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('../src/main/services/agent-event-bus'")
    expect(source).toContain('mockAgentPublish')
    expect(source).not.toContain('webContents.send')
    expect(source).not.toContain('setMainWindow')
    expect(source).not.toContain('createMockWindow')
  })

  it('keeps Codex undo/redo tests on agent event bus assertions', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-22/session-7/codex-undo-redo.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('../../../src/main/services/agent-event-bus'")
    expect(source).toContain('eventBusMocks.publish')
    expect(source).not.toContain('webContents.send')
    expect(source).not.toContain('setMainWindow')
    expect(source).not.toContain('mockWindow')
  })

  it('keeps Codex abort/getMessages tests on agent event bus assertions', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-22/session-6/codex-abort-getmessages.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('../../../src/main/services/agent-event-bus'")
    expect(source).toContain('eventBusMocks.publish')
    expect(source).not.toContain('webContents.send')
    expect(source).not.toContain('setMainWindow')
    expect(source).not.toContain('mockWindow')
  })

  it('keeps Codex question prompt tests on agent event bus assertions', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-22/session-6/codex-question-prompts.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('../../../src/main/services/agent-event-bus'")
    expect(source).toContain('eventBusMocks.publish')
    expect(source).not.toContain('webContents.send')
    expect(source).not.toContain('setMainWindow')
    expect(source).not.toContain('mockWindow')
  })

  it('keeps Codex child-session routing tests on agent event bus assertions', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-22/session-5/codex-child-session-routing.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('../../../src/main/services/agent-event-bus'")
    expect(source).toContain('eventBusMocks.publish')
    expect(source).not.toContain('webContents.send')
    expect(source).not.toContain('setMainWindow')
    expect(source).not.toContain('mockWindow')
  })

  it('keeps Codex prompt streaming tests on agent event bus assertions', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-22/session-5/codex-prompt-streaming.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('../../../src/main/services/agent-event-bus'")
    expect(source).toContain('eventBusMocks.publish')
    expect(source).not.toContain('webContents.send')
    expect(source).not.toContain('setMainWindow')
    expect(source).not.toContain('mockWindow')
  })

  it('keeps Codex implementer skeleton tests on agent event bus assertions', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-22/session-3/codex-implementer-skeleton.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('../../../src/main/services/agent-event-bus'")
    expect(source).toContain('eventBusMocks.publish')
    expect(source).not.toContain('webContents.send')
    expect(source).not.toContain('setMainWindow')
    expect(source).not.toContain('mockWindow')
  })

  it('keeps Codex getSessionInfo tests free of desktop window fixtures', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-22/session-7/codex-getsessioninfo.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('../../../src/main/services/agent-event-bus'")
    expect(source).not.toContain('webContents.send')
    expect(source).not.toContain('setMainWindow')
    expect(source).not.toContain('mockWindow')
  })

  it('keeps auto-rename branch tests on worktreeApi subscription assertions', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-11/session-4/auto-rename-branch.test.ts'),
      'utf-8'
    )

    expect(source).toContain('getRendererRpcClient().subscribe(WORKTREE_BRANCH_RENAMED_CHANNEL')
    expect(source).not.toContain('ipcRenderer')
    expect(source).not.toContain('worktreeOps')
    expect(source).not.toContain('contextBridge')
  })

  it('keeps SessionView handoff relink tests on explicit API mocks', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../test/phase-22/session-11/session-view-handoff-ticket-relink.test.tsx'
      ),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/kanban-api'")
    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/opencode-api'")
    expect(source).toContain("vi.mock('@/api/system-api'")
    expect(source).toContain("vi.mock('@/api/logging-api'")
    expect(source).toContain("vi.mock('@/api/bash-api'")
    expect(source).toContain("vi.mock('@/api/worktree-api'")
    expect(source).not.toContain("Object.defineProperty(window, 'kanban'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'systemOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'loggingOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'bash'")
    expect(source).not.toContain("Object.defineProperty(window, 'worktreeOps'")
    expect(source).not.toContain('window.kanban')
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.opencodeOps')
    expect(source).not.toContain('window.systemOps')
    expect(source).not.toContain('window.bash')
    expect(source).not.toContain('window.worktreeOps')
  })

  it('keeps archive cascade integration tests on explicit API mocks', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../test/worktree-connection/session-10/archive-cascade-integration.test.ts'
      ),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/connection-api'")
    expect(source).toContain("vi.mock('@/api/worktree-api'")
    expect(source).toContain("vi.mock('@/api/kanban-api'")
    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/script-api'")
    expect(source).toContain("vi.mock('@/api/opencode-api'")
    expect(source).toContain("vi.mock('@/api/settings-api'")
    expect(source).toContain("vi.mock('@/api/system-api'")
    expect(source).toContain("vi.mock('@/api/updater-api'")
    expect(source).toContain("vi.mock('@/api/pet-api'")
    expect(source).toContain("vi.mock('@/api/telegram-api'")
    expect(source).toContain("vi.mock('@/api/terminal-api'")
    expect(source).not.toContain("Object.defineProperty(window, 'connectionOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'worktreeOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'scriptOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'kanban'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain('window.connectionOps')
    expect(source).not.toContain('window.worktreeOps')
    expect(source).not.toContain('window.opencodeOps')
    expect(source).not.toContain('window.kanban')
    expect(source).not.toContain('window.db')
  })

  it('keeps auto-launch goal-mode tests on explicit API mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/kanban/auto-launch-goal-mode.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/kanban-api'")
    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/opencode-api'")
    expect(source).toContain("vi.mock('@/api/usage-api'")
    expect(source).not.toContain("Object.defineProperty(window, 'kanban'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'usageOps'")
    expect(source).not.toContain('window.kanban')
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.opencodeOps')
  })

  it('keeps aggregate board startup race tests on the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/kanban/aggregate-board-startup-race.test.ts'),
      'utf-8'
    )

    expect(source).toContain('setRendererRpcClient')
    expect(source).not.toContain("Object.defineProperty(window, 'kanban'")
    expect(source).not.toContain("Object.defineProperty(window, 'connectionOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain('window.kanban')
    expect(source).not.toContain('window.db')
  })

  it('keeps connect dialog tests on explicit connection and db API mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/worktree-connection/session-7/connect-dialog.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/connection-api'")
    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain('mockConnectionApi.create')
    expect(source).not.toContain("Object.defineProperty(window, 'connectionOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain('window.connectionOps')
    expect(source).not.toContain('window.db')
  })

  it('keeps Cmd/Ctrl+R stop-toggle tests on the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-5/session-10/cmd-r-stop-toggle.test.tsx'),
      'utf-8'
    )

    expect(source).toContain('setRendererRpcClient')
    expect(source).toContain("'scriptOps.kill'")
    expect(source).not.toContain("Object.defineProperty(window, 'scriptOps'")
    expect(source).not.toContain('window.scriptOps')
  })

  it('keeps useScriptStore suggestion tests on the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/stores/__tests__/useScriptStore.suggestions.test.ts'
      ),
      'utf-8'
    )

    expect(source).toContain('setRendererRpcClient')
    expect(source).toContain("'scriptOps.runProject'")
    expect(source).not.toContain("Object.defineProperty(window, 'scriptOps'")
    expect(source).not.toContain('window.scriptOps')
  })

  it('keeps ticket attachment tests on the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/kanban/session-13/ticket-attachment.test.tsx'),
      'utf-8'
    )

    expect(source).toContain('setRendererRpcClient')
    expect(source).toContain("'kanban.ticket.update'")
    expect(source).not.toContain("Object.defineProperty(window, 'kanban'")
    expect(source).not.toContain("Object.defineProperty(window, 'fileOps'")
    expect(source).not.toContain('window.kanban')
    expect(source).not.toContain('window.fileOps')
  })

  it('keeps board assistant create navigation tests on the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/kanban/board-assistant-create-navigation.test.tsx'),
      'utf-8'
    )

    expect(source).toContain('setRendererRpcClient')
    expect(source).toContain("'kanban.ticket.createBatch'")
    expect(source).toContain("'opencodeOps.listModels'")
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain('window.opencodeOps')
  })

  it('keeps pinned-board ticket navigation tests on the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/kanban/pinned-board-ticket-navigation.test.tsx'),
      'utf-8'
    )

    expect(source).toContain('setRendererRpcClient')
    expect(source).toContain("'db.project.touch'")
    expect(source).toContain("'db.worktree.touch'")
    expect(source).toContain("'opencodeOps.command'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.opencodeOps')
  })

  it('keeps merge-conflict ticket button tests on the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/kanban/merge-conflicts-ticket-button.test.tsx'),
      'utf-8'
    )

    expect(source).toContain('setRendererRpcClient')
    expect(source).toContain("'db.project.touch'")
    expect(source).toContain("'db.worktree.touch'")
    expect(source).toContain("'db.worktree.get'")
    expect(source).toContain("'db.session.get'")
    expect(source).toContain("'systemOps.openInChrome'")
    expect(source).toContain("'gitOps.listBranchesWithStatus'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'systemOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'gitOps'")
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.systemOps')
    expect(source).not.toContain('window.gitOps')
  })

  it('keeps production renderer app code off legacy business globals', () => {
    const rendererRoot = path.resolve(__dirname, '../../src/renderer/src')
    const sources: Array<{ file: string; source: string }> = []

    const visit = (directory: string): void => {
      for (const entry of fs.readdirSync(directory, { withFileTypes: true })) {
        const absolutePath = path.join(directory, entry.name)
        if (entry.isDirectory()) {
          if (entry.name === '__tests__') continue
          visit(absolutePath)
          continue
        }
        if (!/\.(ts|tsx)$/.test(entry.name) || /\.test\.(ts|tsx)$/.test(entry.name)) continue
        sources.push({
          file: path.relative(rendererRoot, absolutePath),
          source: fs.readFileSync(absolutePath, 'utf-8')
        })
      }
    }

    visit(rendererRoot)

    const legacyReferences = sources.flatMap(({ file, source }) => {
      const matches = [
        ...source.matchAll(/\bwindow\.db\b/g),
        ...source.matchAll(/\bwindow\.[A-Za-z0-9_]*Ops\b/g)
      ]
      return matches.map((match) => `${file}:${match.index ?? 0}:${match[0]}`)
    })

    expect(legacyReferences).toEqual([])
    expect(sources.some(({ source }) => source.includes('window.desktopBridge'))).toBe(true)
  })

  it('keeps system RPC result types out of the desktop bridge module', () => {
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const systemTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/system-types.ts'),
      'utf-8'
    )

    expect(desktopBridgeSource).toContain('export interface LocalEnvironmentBootstrap')
    expect(desktopBridgeSource).not.toContain('DesktopAppPaths')
    expect(desktopBridgeSource).not.toContain('DesktopAgentSdkDetectionResult')
    expect(desktopBridgeSource).not.toContain('DesktopMenuState')
    expect(systemTypesSource).toContain('export interface SystemAppPaths')
  })

  it('keeps native shell operations behind RPC and backend desktop commands', () => {
    const rendererDesktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const systemApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const updaterApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/updater-api.ts'),
      'utf-8'
    )
    const projectOpsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/project-ops.ts'),
      'utf-8'
    )
    const systemOpsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/system-ops.ts'),
      'utf-8'
    )
    const updaterOpsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/updater-ops.ts'),
      'utf-8'
    )
    const backendManagerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/backend-manager.ts'),
      'utf-8'
    )

    const removedBridgeMethods = [
      'pickFolder',
      'confirm',
      'updateMenuState',
      'openInChrome',
      'openExternal',
      'checkForUpdate',
      'downloadUpdate',
      'installUpdate',
      'getUpdaterVersion',
      'setUpdateChannel'
    ]

    for (const method of removedBridgeMethods) {
      expect(rendererDesktopBridgeSource).not.toContain(method)
      expect(preloadTypesSource).not.toContain(`${method}:`)
    }

    expect(rendererDesktopBridgeSource).toContain(
      'readonly getLocalEnvironmentBootstrap: () => Promise<LocalEnvironmentBootstrap | null>'
    )
    expect(rendererDesktopBridgeSource).toContain('readonly getPathForFile?: (file: File) => string')
    expect(preloadTypesSource).toContain(
      'getLocalEnvironmentBootstrap: () => Promise<LocalEnvironmentBootstrap | null>'
    )
    expect(preloadTypesSource).toContain('getPathForFile: (file: File) => string')

    expect(systemApiSource).toContain(
      "return getRendererRpcClient().request<boolean>('systemOps.confirm', { message })"
    )
    expect(systemApiSource).toContain(
      "return getRendererRpcClient().request<void>('systemOps.updateMenuState', state)"
    )
    expect(systemApiSource).toContain(
      "return getRendererRpcClient().request<OpenInChromeResult>('systemOps.openInChrome', {"
    )
    expect(updaterApiSource).toContain(
      "return getRendererRpcClient().request<void>('updaterOps.checkForUpdate', options ?? {})"
    )
    expect(updaterApiSource).toContain(
      "return getRendererRpcClient().request<void>('updaterOps.downloadUpdate', {})"
    )
    expect(updaterApiSource).toContain(
      "return getRendererRpcClient().request<void>('updaterOps.installUpdate', {})"
    )

    expect(projectOpsSource).toContain("const command = 'projectOpenDirectoryDialog'")
    expect(systemOpsSource).toContain("requestDesktopCommand<boolean>('confirm', { message })")
    expect(systemOpsSource).toContain("requestDesktopCommand<void>('updateMenuState', state)")
    expect(updaterOpsSource).toContain("const command = 'updaterCheckForUpdate'")
    expect(updaterOpsSource).toContain("const command = 'updaterDownloadUpdate'")
    expect(updaterOpsSource).toContain("const command = 'updaterInstallUpdate'")

    expect(backendManagerSource).toContain("if (message.command === 'confirm')")
    expect(backendManagerSource).toContain("if (message.command === 'projectOpenDirectoryDialog')")
    expect(backendManagerSource).toContain("if (message.command === 'updateMenuState')")
    expect(backendManagerSource).toContain("if (message.command === 'updaterCheckForUpdate')")
    expect(backendManagerSource).toContain('dialog.showMessageBox')
    expect(backendManagerSource).toContain('dialog.showOpenDialog')
    expect(backendManagerSource).toContain('updateMenuState(message.payload)')
  })

  it('keeps broad migrated IPC handler guards out of the IPC source tree', () => {
    expect(
      fs.existsSync(
        path.resolve(__dirname, '../../src/main/ipc/_shared/__tests__/migrated-handlers.test.ts')
      )
    ).toBe(false)
    expect(fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/index.ts'))).toBe(false)
    expect(fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/.gitkeep'))).toBe(false)
    expect(
      fs.existsSync(path.resolve(__dirname, './migrated-ipc-handlers.test.ts'))
    ).toBe(true)
  })

  it('keeps server-imported git service worktree path helpers off Electron app APIs', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/git-service.ts'),
      'utf-8'
    )
    const getBaseDirStart = source.indexOf('static getWorktreesBaseDir(): string {')
    const getBaseDirEnd = source.indexOf('\n  }', getBaseDirStart)
    const getBaseDirSource = source.slice(getBaseDirStart, getBaseDirEnd)

    expect(getBaseDirStart).toBeGreaterThan(-1)
    expect(getBaseDirEnd).toBeGreaterThan(getBaseDirStart)
    expect(source).not.toContain("from 'electron'")
    expect(source).not.toContain("import('electron')")
    expect(getBaseDirSource).toContain("join(homedir(), '.hive-worktrees')")
    expect(getBaseDirSource).not.toContain('app.getPath')
  })

  it('keeps server-imported logger path helpers off Electron app APIs', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/logger.ts'),
      'utf-8'
    )
    const constructorStart = source.indexOf('private constructor() {')
    const constructorEnd = source.indexOf('\n  }', constructorStart)
    const constructorSource = source.slice(constructorStart, constructorEnd)

    expect(constructorStart).toBeGreaterThan(-1)
    expect(constructorEnd).toBeGreaterThan(constructorStart)
    expect(source).not.toContain("from 'electron'")
    expect(source).not.toContain("import('electron')")
    expect(source).toContain("import { homedir } from 'os'")
    expect(constructorSource).toContain('const homeDir = homedir()')
    expect(constructorSource).toContain("join(homeDir, '.hive', LOG_DIR_NAME)")
    expect(constructorSource).not.toContain('app.getPath')
  })

  it('keeps server-imported response logger path helpers off Electron app APIs', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/response-logger.ts'),
      'utf-8'
    )
    const getDirStart = source.indexOf('function getResponseLogDir(): string {')
    const getDirEnd = source.indexOf('\n}', getDirStart)
    const getDirSource = source.slice(getDirStart, getDirEnd)

    expect(getDirStart).toBeGreaterThan(-1)
    expect(getDirEnd).toBeGreaterThan(getDirStart)
    expect(source).not.toContain("from 'electron'")
    expect(source).not.toContain("import('electron')")
    expect(source).toContain("import { homedir } from 'os'")
    expect(getDirSource).toContain('const homeDir = homedir()')
    expect(getDirSource).toContain("join(homeDir, '.hive', 'logs', RESPONSE_LOG_DIR)")
    expect(getDirSource).not.toContain('app.getPath')
  })

  it('keeps server-imported attachment storage path helpers off Electron app APIs', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/attachment-storage.ts'),
      'utf-8'
    )
    const getDirStart = source.indexOf('async function getAttachmentsDir(): Promise<string> {')
    const getDirEnd = source.indexOf('\n}', getDirStart)
    const getDirSource = source.slice(getDirStart, getDirEnd)

    expect(getDirStart).toBeGreaterThan(-1)
    expect(getDirEnd).toBeGreaterThan(getDirStart)
    expect(source).not.toContain("from 'electron'")
    expect(source).not.toContain("import('electron')")
    expect(source).toContain("import { homedir } from 'os'")
    expect(getDirSource).toContain("join(homedir(), '.hive', 'attachments')")
    expect(getDirSource).not.toContain('app.getPath')
  })

  it('keeps server-imported system info helpers off Electron app APIs', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/system-info.ts'),
      'utf-8'
    )
    const getAppPathsStart = source.indexOf('export function getAppPaths(): AppPaths {')
    const getAppPathsEnd = source.indexOf('\n}', getAppPathsStart)
    const getAppPathsSource = source.slice(getAppPathsStart, getAppPathsEnd)
    const getAppVersionStart = source.indexOf('export function getAppVersion(): string {')
    const getAppVersionEnd = source.indexOf('\n}', getAppVersionStart)
    const getAppVersionSource = source.slice(getAppVersionStart, getAppVersionEnd)

    expect(getAppPathsStart).toBeGreaterThan(-1)
    expect(getAppPathsEnd).toBeGreaterThan(getAppPathsStart)
    expect(getAppVersionStart).toBeGreaterThan(-1)
    expect(getAppVersionEnd).toBeGreaterThan(getAppVersionStart)
    expect(source).not.toContain("from 'electron'")
    expect(source).not.toContain("import('electron')")
    expect(source).toContain("import { homedir } from 'os'")
    expect(getAppPathsSource).toContain('const home = homedir()')
    expect(getAppPathsSource).toContain('process.env.HIVE_SERVER_BASE_DIR ?? home')
    expect(getAppPathsSource).toContain('home,')
    expect(getAppPathsSource).not.toContain('app.getPath')
    expect(getAppVersionSource).toContain("process.env.npm_package_version ?? ''")
    expect(getAppVersionSource).not.toContain('app.getVersion')
  })

  it('keeps server-imported connection service path helpers off Electron app APIs', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/connection-service.ts'),
      'utf-8'
    )
    const getBaseDirStart = source.indexOf('export function getConnectionsBaseDir(): string {')
    const getBaseDirEnd = source.indexOf('\n}', getBaseDirStart)
    const getBaseDirSource = source.slice(getBaseDirStart, getBaseDirEnd)

    expect(getBaseDirStart).toBeGreaterThan(-1)
    expect(getBaseDirEnd).toBeGreaterThan(getBaseDirStart)
    expect(source).not.toContain("from 'electron'")
    expect(source).not.toContain("import('electron')")
    expect(source).toContain("import { homedir } from 'os'")
    expect(getBaseDirSource).toContain('const homeDir = homedir()')
    expect(getBaseDirSource).toContain("join(homeDir, '.hive', CONNECTIONS_DIR_NAME)")
    expect(getBaseDirSource).not.toContain('app.getPath')
  })

  it('keeps server-imported telemetry service event metadata off Electron app APIs', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/telemetry-service.ts'),
      'utf-8'
    )
    const versionStart = source.indexOf('export function resolveTelemetryAppVersion(): string {')
    const versionEnd = source.indexOf('\nclass TelemetryService', versionStart)
    const versionSource = source.slice(versionStart, versionEnd)

    expect(versionStart).toBeGreaterThan(-1)
    expect(versionEnd).toBeGreaterThan(versionStart)
    expect(source).not.toContain("from 'electron'")
    expect(source).not.toContain("import('electron')")
    expect(source).toContain("import { readFileSync } from 'fs'")
    expect(versionSource).toContain('process.env.npm_package_version')
    expect(versionSource).toContain("readFileSync('package.json', 'utf-8')")
    expect(source).toContain('app_version: resolveTelemetryAppVersion()')
    expect(source).not.toContain('app.getVersion')
  })

  it('keeps server-imported database path resolution off Electron app APIs', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/db/database.ts'),
      'utf-8'
    )
    const resolverStart = source.indexOf('export function resolveDatabasePath')
    const resolverEnd = source.indexOf('\nexport class DatabaseService', resolverStart)
    const resolverSource = source.slice(resolverStart, resolverEnd)

    expect(resolverStart).toBeGreaterThan(-1)
    expect(resolverEnd).toBeGreaterThan(resolverStart)
    expect(source).not.toContain("from 'electron'")
    expect(source).not.toContain("import('electron')")
    expect(source).toContain("import { homedir } from 'os'")
    expect(resolverSource).toContain('process.env.HIVE_SERVER_BASE_DIR')
    expect(resolverSource).toContain("join(serverBaseDir, 'userdata', 'state.sqlite')")
    expect(resolverSource).toContain("join(homeDir, '.hive', 'hive.db')")
    expect(source).not.toContain('app.getPath')
  })

  it('keeps server-imported Effect git worktree path resolution off Electron app APIs', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/effect/git/layers.ts'),
      'utf-8'
    )
    const resolverStart = source.indexOf('export const resolveGitWorktreesDir =')
    const resolverEnd = source.indexOf('\nconst ensureWorktreesDir', resolverStart)
    const resolverSource = source.slice(resolverStart, resolverEnd)

    expect(resolverStart).toBeGreaterThan(-1)
    expect(resolverEnd).toBeGreaterThan(resolverStart)
    expect(source).not.toContain("from 'electron'")
    expect(source).not.toContain("import('electron')")
    expect(source).toContain("import { homedir, tmpdir } from 'os'")
    expect(resolverSource).toContain('homeDir: string = homedir()')
    expect(resolverSource).toContain("join(homeDir, '.hive-worktrees', projectName)")
    expect(source).toContain('const projectWorktreesDir = resolveGitWorktreesDir(projectName)')
    expect(source).not.toContain('app.getPath')
  })

  it('keeps server-imported perf diagnostics off direct Electron imports', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/perf-diagnostics.ts'),
      'utf-8'
    )

    expect(source).not.toContain("from 'electron'")
    expect(source).not.toContain("import('electron')")
    expect(source).toContain("import { homedir } from 'os'")
    expect(source).toContain('getElectronProcessCounts?:')
    expect(source).toContain('collectors?.getElectronProcessCounts?.()')
    expect(source).toContain("join(homeDir, '.hive', 'logs')")
    expect(source).not.toContain('BrowserWindow')
    expect(source).not.toContain('webContents.getAllWebContents')
    expect(source).not.toContain('app.getPath')
  })

  it('keeps system setKeepAwake RPC fallback from importing the Electron power-save service', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/system-ops.ts'),
      'utf-8'
    )
    const fallbackStart = source.indexOf("if (command === 'setKeepAwake')")
    const fallbackEnd = source.indexOf("if (command === 'setSessionQueuedState')", fallbackStart)
    const fallbackSource = source.slice(fallbackStart, fallbackEnd)

    expect(fallbackStart).toBeGreaterThan(-1)
    expect(fallbackEnd).toBeGreaterThan(fallbackStart)
    expect(fallbackSource).toContain("if (!payload) throw new Error('Missing setKeepAwake payload')")
    expect(fallbackSource).toContain('return Promise.resolve(undefined as A)')
    expect(fallbackSource).not.toContain('power-save-blocker')
    expect(fallbackSource).not.toContain('setKeepAwake(')
    expect(source).toContain("requestDesktopCommand<void>('setKeepAwake', { active })")
  })

  it('keeps system updateMenuState RPC fallback from importing the Electron menu module', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/system-ops.ts'),
      'utf-8'
    )
    const fallbackStart = source.indexOf("if (command === 'updateMenuState')")
    const fallbackEnd = source.indexOf("if (command === 'setKeepAwake')", fallbackStart)
    const fallbackSource = source.slice(fallbackStart, fallbackEnd)

    expect(fallbackStart).toBeGreaterThan(-1)
    expect(fallbackEnd).toBeGreaterThan(fallbackStart)
    expect(fallbackSource).toContain("if (!payload) throw new Error('Missing updateMenuState payload')")
    expect(fallbackSource).toContain('return Promise.resolve(undefined as A)')
    expect(fallbackSource).not.toContain("import('../../../main/menu')")
    expect(fallbackSource).not.toContain('updateMenuState(payload')
    expect(source).toContain("requestDesktopCommand<void>('updateMenuState', state)")
  })

  it('keeps system setSessionQueuedState RPC fallback from importing Electron notifications', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/system-ops.ts'),
      'utf-8'
    )
    const fallbackStart = source.indexOf("if (command === 'setSessionQueuedState')")
    const fallbackEnd = source.indexOf(
      "if (!payload) throw new Error('Missing openInChrome payload')",
      fallbackStart
    )
    const fallbackSource = source.slice(fallbackStart, fallbackEnd)

    expect(fallbackStart).toBeGreaterThan(-1)
    expect(fallbackEnd).toBeGreaterThan(fallbackStart)
    expect(fallbackSource).toContain(
      "if (!payload) throw new Error('Missing setSessionQueuedState payload')"
    )
    expect(fallbackSource).toContain('return Promise.resolve(undefined as A)')
    expect(fallbackSource).not.toContain('notification-service')
    expect(fallbackSource).not.toContain('notificationService.setSessionQueuedState')
    expect(source).toContain(
      "requestDesktopCommand<void>('setSessionQueuedState', { sessionId, hasQueued })"
    )
  })

  it('keeps server-imported open-in-chrome off direct Electron shell imports', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/open-in-chrome.ts'),
      'utf-8'
    )
    const desktopSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/backend-manager.ts'),
      'utf-8'
    )

    expect(source).not.toContain("from 'electron'")
    expect(source).not.toContain("import('electron')")
    expect(source).toContain('readonly openExternal?: (url: string) => Promise<void>')
    expect(source).toContain("return { success: false, error: 'No external URL opener is available' }")
    expect(source).toContain('await deps.openExternal(url)')
    expect(source).not.toContain('electronShell')
    expect(desktopSource).toContain('openExternal: shell.openExternal')
  })

  it('keeps server-imported open-in-app off direct Electron clipboard imports', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/open-in-app.ts'),
      'utf-8'
    )
    const desktopSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/backend-manager.ts'),
      'utf-8'
    )

    expect(source).not.toContain("from 'electron'")
    expect(source).not.toContain("import('electron')")
    expect(source).not.toContain('electronClipboard')
    expect(source).toContain('readonly clipboard?: ClipboardWriter')
    expect(source).toContain("return { success: false, error: 'No clipboard writer is available' }")
    expect(desktopSource).toContain("import { app, BrowserWindow, clipboard, dialog, shell } from 'electron'")
    expect(desktopSource).toContain('openInApp(message.payload.appName, message.payload.path, { clipboard })')
  })

  it('provides a browser-mode dev server script that binds to a free loopback port', () => {
    const packageJson = JSON.parse(
      fs.readFileSync(path.resolve(__dirname, '../../package.json'), 'utf-8')
    ) as { scripts?: Record<string, string> }

    expect(packageJson.scripts?.['dev:server']).toBe(
      'pnpm run build && HIVE_SERVER_MODE=browser HIVE_SERVER_PORT=0 HIVE_SERVER_DEV_URL=http://localhost:5173 ELECTRON_RUN_AS_NODE=1 electron out/main/server.js'
    )
    expect(packageJson.scripts?.['dev:server']).toContain('HIVE_SERVER_MODE=browser')
    expect(packageJson.scripts?.['dev:server']).toContain('HIVE_SERVER_PORT=0')
    expect(packageJson.scripts?.['dev:server']).toContain('ELECTRON_RUN_AS_NODE=1')
    expect(packageJson.scripts?.['dev:server']).toContain('electron out/main/server.js')
    expect(packageJson.scripts?.['dev:server']).not.toContain('electron-vite dev')
  })

  it('provides a web dev script that starts Vite against the local HTTP backend', () => {
    const packageJson = JSON.parse(
      fs.readFileSync(path.resolve(__dirname, '../../package.json'), 'utf-8')
    ) as { scripts?: Record<string, string> }
    const devWebSource = fs.readFileSync(
      path.resolve(__dirname, '../../scripts/dev-web.mjs'),
      'utf-8'
    )
    const viteConfigSource = fs.readFileSync(
      path.resolve(__dirname, '../../vite.web.config.ts'),
      'utf-8'
    )

    expect(packageJson.scripts?.['dev:web']).toBe('node scripts/dev-web.mjs')
    expect(devWebSource).toContain("HIVE_SERVER_PORT: serverPort")
    expect(devWebSource).toContain("ELECTRON_RUN_AS_NODE: '1'")
    expect(devWebSource).toContain("HIVE_SERVER_MODE: 'browser'")
    expect(devWebSource).toContain("HIVE_DESKTOP_BOOTSTRAP_TOKEN: bootstrapToken")
    expect(devWebSource).toContain("VITE_HIVE_BOOTSTRAP_TOKEN: bootstrapToken")
    expect(devWebSource).toContain("VITE_HIVE_HTTP_BASE_URL: ready.httpBaseUrl")
    expect(devWebSource).toContain("VITE_HIVE_WS_BASE_URL: ready.wsBaseUrl")
    expect(devWebSource).toContain("'vite', '--config', 'vite.web.config.ts'")
    expect(viteConfigSource).toContain("root: resolve(__dirname, 'src/renderer')")
    expect(viteConfigSource).toContain("'@shared': resolve(__dirname, 'src/shared')")
    expect(viteConfigSource).not.toContain('electron-vite')
  })

  it('keeps the desktop dev script on the Electron workflow', () => {
    const packageJson = JSON.parse(
      fs.readFileSync(path.resolve(__dirname, '../../package.json'), 'utf-8')
    ) as { scripts?: Record<string, string> }

    expect(packageJson.scripts?.['dev']).toBe('pnpm run dev:desktop')
    expect(packageJson.scripts?.['dev:desktop']).toBe('electron-vite dev')
    expect(packageJson.scripts?.['dev:desktop']).not.toContain('vite.web.config.ts')
    expect(packageJson.scripts?.['dev:desktop']).not.toContain('HIVE_SERVER_MODE=browser')
  })

  it('covers browser mode with a Playwright smoke test that reads RPC-backed data', () => {
    const smokeSource = fs.readFileSync(
      path.resolve(__dirname, '../../test/e2e/browser-mode-smoke.spec.ts'),
      'utf-8'
    )
    const playwrightConfigSource = fs.readFileSync(
      path.resolve(__dirname, '../../playwright.config.ts'),
      'utf-8'
    )

    expect(playwrightConfigSource).toContain("testDir: './test/e2e'")
    expect(smokeSource).toContain('browser mode loads, authenticates, reads settings, and lists projects')
    expect(smokeSource).toContain('HIVE_DESKTOP_BOOTSTRAP_TOKEN: bootstrapToken')
    expect(smokeSource).toContain("ELECTRON_RUN_AS_NODE: '1'")
    expect(smokeSource).toContain('VITE_HIVE_BOOTSTRAP_TOKEN: bootstrapToken')
    expect(smokeSource).toContain("method: 'db.setting.getAll'")
    expect(smokeSource).toContain('Timed out reading settings through direct RPC')
    expect(smokeSource).toContain('settings.rpcResponse.ok')
    expect(smokeSource).not.toContain('window.db.setting.getAll()')
    expect(smokeSource).not.toContain('window.db?.setting?.getAll')
    expect(smokeSource).toContain("method: 'db.project.getAll'")
    expect(smokeSource).toContain('Timed out listing projects through direct RPC')
    expect(smokeSource).toContain('projects.rpcResponse.ok')
    expect(smokeSource).not.toContain('window.db.project.getAll()')
    expect(smokeSource).not.toContain('window.db?.project?.getAll')
    expect(smokeSource).not.toContain('window.db')
  })

  it('covers Electron mode with a Playwright smoke test that starts the desktop backend', () => {
    const smokeSource = fs.readFileSync(
      path.resolve(__dirname, '../../test/e2e/electron-mode-smoke.spec.ts'),
      'utf-8'
    )

    expect(smokeSource).toContain('electron mode starts the desktop backend, opens renderer, and reads settings')
    expect(smokeSource).toContain('_electron as electron')
    expect(smokeSource).toContain("args: ['out/main/index.js', '--no-sandbox', '--disable-gpu']")
    expect(smokeSource).toContain('window.desktopBridge?.getLocalEnvironmentBootstrap')
    expect(smokeSource).toContain('window.desktopBridge.getLocalEnvironmentBootstrap()')
    expect(smokeSource).toContain("method: 'db.setting.getAll'")
    expect(smokeSource).toContain('Timed out reading settings through direct desktop RPC')
    expect(smokeSource).toContain('settings.rpcResponse.ok')
    expect(smokeSource).not.toContain('window.db.setting.getAll()')
    expect(smokeSource).not.toContain('window.db?.setting?.getAll')
    expect(smokeSource).not.toContain('window.db')
    expect(smokeSource).toContain('Xvfb')
  })

  it('allows browser-mode renderer pages to connect to loopback HTTP and WebSocket backends', () => {
    for (const htmlPath of ['src/renderer/index.html', 'src/renderer/pet.html']) {
      const html = fs.readFileSync(path.resolve(__dirname, '../..', htmlPath), 'utf-8')

      expect(html).toContain('connect-src')
      expect(html).toContain('http://localhost:*')
      expect(html).toContain('http://127.0.0.1:*')
      expect(html).toContain('ws://localhost:*')
      expect(html).toContain('ws://127.0.0.1:*')
    }
  })

  it('allows the main renderer to test user-configured teleport remotes', () => {
    const html = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/index.html'),
      'utf-8'
    )
    const csp = html.match(/content="([^"]*connect-src[^"]*)"/)?.[1] ?? ''
    const tokens = csp.split(/\s+/)

    expect(tokens).toContain('http:')
    expect(tokens).toContain('https:')
    expect(tokens).toContain('ws:')
    expect(tokens).toContain('wss:')
  })

  it('routes ErrorBoundary clipboard fallback through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/error/ErrorBoundary.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('private handleCopyError = async (): Promise<void> => {')
    const handlerEnd = source.indexOf('  public render(): ReactNode {', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('await navigator.clipboard.writeText(errorText)')
    expect(handlerSource).toContain('await projectApi.copyToClipboard(errorText)')
    expect(handlerSource).toContain('this.setState({ copied: true })')
    expect(handlerSource).toContain("console.error('Failed to copy error to clipboard')")
    expect(handlerSource).not.toContain('window.projectOps.copyToClipboard')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes FileContextMenu absolute path copy through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/file-tree/FileContextMenu.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleCopyPath = useCallback(async () => {')
    const handlerEnd = source.indexOf(
      'const handleCopyRelativePath = useCallback(async () => {',
      handlerStart
    )
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('await projectApi.copyToClipboard(node.path)')
    expect(handlerSource).toContain('onClose?.()')
    expect(handlerSource).not.toContain('window.projectOps.copyToClipboard')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes FileContextMenu relative path copy through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/file-tree/FileContextMenu.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleCopyRelativePath = useCallback(async () => {')
    const handlerEnd = source.indexOf('const handleViewChanges = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('await projectApi.copyToClipboard(node.relativePath)')
    expect(handlerSource).toContain('onClose?.()')
    expect(handlerSource).not.toContain('window.projectOps.copyToClipboard')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes InlineDiffViewer diff copy through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/InlineDiffViewer.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleCopyDiff = useCallback(async () => {')
    const handlerEnd = source.indexOf('const toggleViewMode = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('const content = isNewFile ? fileContent : diff')
    expect(handlerSource).toContain('await projectApi.copyToClipboard(content)')
    expect(handlerSource).toContain(
      "toast.success(isNewFile ? 'File content copied to clipboard' : 'Diff copied to clipboard')"
    )
    expect(handlerSource).not.toContain('window.projectOps.copyToClipboard')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes InlineDiffViewer new-file content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/InlineDiffViewer.tsx'),
      'utf-8'
    )
    const fetchContentStart = source.indexOf('  // Fetch file content for new files')
    const fetchDiffStart = source.indexOf('  // Fetch diff', fetchContentStart)
    const fetchContentSource = source.slice(fetchContentStart, fetchDiffStart)

    expect(fetchContentStart).toBeGreaterThan(-1)
    expect(fetchDiffStart).toBeGreaterThan(fetchContentStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(fetchContentSource).toContain(
      'const result = await gitApi.getFileContent(worktreePath, filePath)'
    )
    expect(fetchContentSource).toContain('setFileContent(result.content)')
    expect(fetchContentSource).toContain("setError(result.error || 'Failed to load file content')")
    expect(fetchContentSource).not.toContain('window.gitOps.getFileContent')
    expect(fetchContentSource).not.toContain('unwrapEnvelope')
  })

  it('routes InlineDiffViewer diff loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/InlineDiffViewer.tsx'),
      'utf-8'
    )
    const fetchDiffStart = source.indexOf('  // Fetch diff')
    const loadOnMountStart = source.indexOf('  // Load on mount and when contextLines changes')
    const fetchDiffSource = source.slice(fetchDiffStart, loadOnMountStart)

    expect(fetchDiffStart).toBeGreaterThan(-1)
    expect(loadOnMountStart).toBeGreaterThan(fetchDiffStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
    expect(fetchDiffSource).toContain(
      'const result = await gitApi.getDiff(worktreePath, filePath, staged, isUntracked, ctx)'
    )
    expect(fetchDiffSource).toContain('setDiff(result.diff)')
    expect(fetchDiffSource).toContain("setError(result.error || 'Failed to load diff')")
    expect(fetchDiffSource).not.toContain('window.gitOps.getDiff')
    expect(fetchDiffSource).not.toContain('unwrapEnvelope')
  })

  it('routes ImageDiffView new SVG content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/ImageDiffView.tsx'),
      'utf-8'
    )
    const newFileStart = source.indexOf('      if (isNewFile || isUntracked) {')
    const binaryStart = source.indexOf('        } else {', newFileStart)
    const newSvgSource = source.slice(newFileStart, binaryStart)

    expect(newFileStart).toBeGreaterThan(-1)
    expect(binaryStart).toBeGreaterThan(newFileStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(newSvgSource).toContain(
      'const modResult = await gitApi.getFileContent(worktreePath, filePath)'
    )
    expect(newSvgSource).toContain('setModifiedSvg(modResult.content)')
    expect(newSvgSource).not.toContain('window.gitOps.getFileContent')
    expect(newSvgSource).not.toContain('unwrapEnvelope')
  })

  it('routes ImageDiffView new binary content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/ImageDiffView.tsx'),
      'utf-8'
    )
    const binaryStart = source.indexOf('        } else {', source.indexOf('if (isSvg) {'))
    const newFileEnd = source.indexOf('        setOriginalUri(null)', binaryStart)
    const newBinarySource = source.slice(binaryStart, newFileEnd)

    expect(binaryStart).toBeGreaterThan(-1)
    expect(newFileEnd).toBeGreaterThan(binaryStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(newBinarySource).toContain(
      'const modResult = await gitApi.getFileContentBase64(worktreePath, filePath)'
    )
    expect(newBinarySource).toContain(
      'setModifiedUri(buildDataUri(modResult.data, modResult.mimeType))'
    )
    expect(newBinarySource).not.toContain('window.gitOps.getFileContentBase64')
    expect(newBinarySource).not.toContain('unwrapEnvelope')
  })

  it('routes ImageDiffView compare-branch SVG modified content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/ImageDiffView.tsx'),
      'utf-8'
    )
    const compareStart = source.indexOf('      if (compareBranch) {')
    const svgStart = source.indexOf('        if (isSvg) {', compareStart)
    const binaryStart = source.indexOf('        } else {', svgStart)
    const compareSvgSource = source.slice(svgStart, binaryStart)

    expect(compareStart).toBeGreaterThan(-1)
    expect(svgStart).toBeGreaterThan(compareStart)
    expect(binaryStart).toBeGreaterThan(svgStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(compareSvgSource).toContain(
      '.getBranchBaseContent(worktreePath, compareBranch, filePath)'
    )
    expect(compareSvgSource).toContain('gitApi.getFileContent(worktreePath, filePath)')
    expect(compareSvgSource).toContain(
      'setModifiedSvg(modResult.success && modResult.content ? modResult.content : null)'
    )
    expect(compareSvgSource).not.toContain(
      'window.gitOps.getFileContent(worktreePath, filePath).then(unwrapEnvelope)'
    )
  })

  it('routes ImageDiffView compare-branch SVG base content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/ImageDiffView.tsx'),
      'utf-8'
    )
    const compareStart = source.indexOf('      if (compareBranch) {')
    const svgStart = source.indexOf('        if (isSvg) {', compareStart)
    const binaryStart = source.indexOf('        } else {', svgStart)
    const compareSvgSource = source.slice(svgStart, binaryStart)

    expect(compareStart).toBeGreaterThan(-1)
    expect(svgStart).toBeGreaterThan(compareStart)
    expect(binaryStart).toBeGreaterThan(svgStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(compareSvgSource).toContain(
      'gitApi.getBranchBaseContent(worktreePath, compareBranch, filePath)'
    )
    expect(compareSvgSource).toContain('gitApi.getFileContent(worktreePath, filePath)')
    expect(compareSvgSource).toContain(
      'setOriginalSvg(origResult.success && origResult.content ? origResult.content : null)'
    )
    expect(compareSvgSource).not.toContain('window.gitOps')
    expect(compareSvgSource).not.toContain('unwrapEnvelope')
  })

  it('routes ImageDiffView compare-branch binary modified content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/ImageDiffView.tsx'),
      'utf-8'
    )
    const compareStart = source.indexOf('      if (compareBranch) {')
    const binaryStart = source.indexOf('        } else {', compareStart)
    const stagedStart = source.indexOf('      } else if (staged) {', binaryStart)
    const compareBinarySource = source.slice(binaryStart, stagedStart)

    expect(compareStart).toBeGreaterThan(-1)
    expect(binaryStart).toBeGreaterThan(compareStart)
    expect(stagedStart).toBeGreaterThan(binaryStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(compareBinarySource).toContain(
      '.getBranchBaseContentBase64(worktreePath, compareBranch, filePath)'
    )
    expect(compareBinarySource).toContain('gitApi.getFileContentBase64(worktreePath, filePath)')
    expect(compareBinarySource).toContain('setModifiedUri(')
    expect(compareBinarySource).toContain('buildDataUri(modResult.data, modResult.mimeType)')
    expect(compareBinarySource).not.toContain(
      'window.gitOps.getFileContentBase64(worktreePath, filePath).then(unwrapEnvelope)'
    )
  })

  it('routes ImageDiffView compare-branch binary base content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/ImageDiffView.tsx'),
      'utf-8'
    )
    const compareStart = source.indexOf('      if (compareBranch) {')
    const binaryStart = source.indexOf('        } else {', compareStart)
    const stagedStart = source.indexOf('      } else if (staged) {', binaryStart)
    const compareBinarySource = source.slice(binaryStart, stagedStart)

    expect(compareStart).toBeGreaterThan(-1)
    expect(binaryStart).toBeGreaterThan(compareStart)
    expect(stagedStart).toBeGreaterThan(binaryStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(compareBinarySource).toContain(
      'gitApi.getBranchBaseContentBase64(worktreePath, compareBranch, filePath)'
    )
    expect(compareBinarySource).toContain('gitApi.getFileContentBase64(worktreePath, filePath)')
    expect(compareBinarySource).toContain('setOriginalUri(')
    expect(compareBinarySource).toContain('buildDataUri(origResult.data, origResult.mimeType)')
    expect(compareBinarySource).not.toContain('window.gitOps')
    expect(compareBinarySource).not.toContain('unwrapEnvelope')
  })

  it('routes ImageDiffView staged SVG original content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/ImageDiffView.tsx'),
      'utf-8'
    )
    const stagedStart = source.indexOf('      } else if (staged) {')
    const svgStart = source.indexOf('        if (isSvg) {', stagedStart)
    const binaryStart = source.indexOf('        } else {', svgStart)
    const stagedSvgSource = source.slice(svgStart, binaryStart)

    expect(stagedStart).toBeGreaterThan(-1)
    expect(svgStart).toBeGreaterThan(stagedStart)
    expect(binaryStart).toBeGreaterThan(svgStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(stagedSvgSource).toContain("gitApi.getRefContent(worktreePath, 'HEAD', filePath)")
    expect(stagedSvgSource).toContain(
      'setOriginalSvg(origResult.success && origResult.content ? origResult.content : null)'
    )
    expect(stagedSvgSource).not.toContain(
      "window.gitOps.getRefContent(worktreePath, 'HEAD', filePath).then(unwrapEnvelope)"
    )
  })

  it('routes ImageDiffView staged SVG modified content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/ImageDiffView.tsx'),
      'utf-8'
    )
    const stagedStart = source.indexOf('      } else if (staged) {')
    const svgStart = source.indexOf('        if (isSvg) {', stagedStart)
    const binaryStart = source.indexOf('        } else {', svgStart)
    const stagedSvgSource = source.slice(svgStart, binaryStart)

    expect(stagedStart).toBeGreaterThan(-1)
    expect(svgStart).toBeGreaterThan(stagedStart)
    expect(binaryStart).toBeGreaterThan(svgStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(stagedSvgSource).toContain("gitApi.getRefContent(worktreePath, '', filePath)")
    expect(stagedSvgSource).toContain(
      'setModifiedSvg(modResult.success && modResult.content ? modResult.content : null)'
    )
    expect(stagedSvgSource).not.toContain('window.gitOps')
    expect(stagedSvgSource).not.toContain('unwrapEnvelope')
  })

  it('routes ImageDiffView staged binary original content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/ImageDiffView.tsx'),
      'utf-8'
    )
    const stagedStart = source.indexOf('      } else if (staged) {')
    const svgStart = source.indexOf('        if (isSvg) {', stagedStart)
    const binaryStart = source.indexOf('        } else {', svgStart)
    const unstagedStart = source.indexOf('      } else {', binaryStart + '        } else {'.length)
    const stagedBinarySource = source.slice(binaryStart, unstagedStart)

    expect(stagedStart).toBeGreaterThan(-1)
    expect(svgStart).toBeGreaterThan(stagedStart)
    expect(binaryStart).toBeGreaterThan(svgStart)
    expect(unstagedStart).toBeGreaterThan(binaryStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(stagedBinarySource).toContain(
      "gitApi.getRefContentBase64(worktreePath, 'HEAD', filePath)"
    )
    expect(stagedBinarySource).toContain('setOriginalUri(')
    expect(stagedBinarySource).toContain('buildDataUri(origResult.data, origResult.mimeType)')
    expect(stagedBinarySource).not.toContain(
      "window.gitOps.getRefContentBase64(worktreePath, 'HEAD', filePath).then(unwrapEnvelope)"
    )
  })

  it('routes ImageDiffView staged binary modified content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/ImageDiffView.tsx'),
      'utf-8'
    )
    const stagedStart = source.indexOf('      } else if (staged) {')
    const svgStart = source.indexOf('        if (isSvg) {', stagedStart)
    const binaryStart = source.indexOf('        } else {', svgStart)
    const unstagedStart = source.indexOf('      } else {', binaryStart + '        } else {'.length)
    const stagedBinarySource = source.slice(binaryStart, unstagedStart)

    expect(stagedStart).toBeGreaterThan(-1)
    expect(svgStart).toBeGreaterThan(stagedStart)
    expect(binaryStart).toBeGreaterThan(svgStart)
    expect(unstagedStart).toBeGreaterThan(binaryStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(stagedBinarySource).toContain("gitApi.getRefContentBase64(worktreePath, '', filePath)")
    expect(stagedBinarySource).toContain('setModifiedUri(')
    expect(stagedBinarySource).toContain('buildDataUri(modResult.data, modResult.mimeType)')
    expect(stagedBinarySource).not.toContain('window.gitOps')
    expect(stagedBinarySource).not.toContain('unwrapEnvelope')
  })

  it('routes ImageDiffView unstaged SVG original content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/ImageDiffView.tsx'),
      'utf-8'
    )
    const stagedStart = source.indexOf('      } else if (staged) {')
    const stagedBinaryStart = source.indexOf('        } else {', stagedStart)
    const unstagedStart = source.indexOf(
      '      } else {',
      stagedBinaryStart + '        } else {'.length
    )
    const svgStart = source.indexOf('        if (isSvg) {', unstagedStart)
    const binaryStart = source.indexOf('        } else {', svgStart)
    const unstagedSvgSource = source.slice(svgStart, binaryStart)

    expect(stagedStart).toBeGreaterThan(-1)
    expect(stagedBinaryStart).toBeGreaterThan(stagedStart)
    expect(unstagedStart).toBeGreaterThan(stagedBinaryStart)
    expect(svgStart).toBeGreaterThan(unstagedStart)
    expect(binaryStart).toBeGreaterThan(svgStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(unstagedSvgSource).toContain("gitApi.getRefContent(worktreePath, '', filePath)")
    expect(unstagedSvgSource).toContain(
      'setOriginalSvg(origResult.success && origResult.content ? origResult.content : null)'
    )
    expect(unstagedSvgSource).not.toContain(
      "window.gitOps.getRefContent(worktreePath, '', filePath).then(unwrapEnvelope)"
    )
  })

  it('routes ImageDiffView unstaged SVG modified content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/ImageDiffView.tsx'),
      'utf-8'
    )
    const stagedStart = source.indexOf('      } else if (staged) {')
    const stagedBinaryStart = source.indexOf('        } else {', stagedStart)
    const unstagedStart = source.indexOf(
      '      } else {',
      stagedBinaryStart + '        } else {'.length
    )
    const svgStart = source.indexOf('        if (isSvg) {', unstagedStart)
    const binaryStart = source.indexOf('        } else {', svgStart)
    const unstagedSvgSource = source.slice(svgStart, binaryStart)

    expect(stagedStart).toBeGreaterThan(-1)
    expect(stagedBinaryStart).toBeGreaterThan(stagedStart)
    expect(unstagedStart).toBeGreaterThan(stagedBinaryStart)
    expect(svgStart).toBeGreaterThan(unstagedStart)
    expect(binaryStart).toBeGreaterThan(svgStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(unstagedSvgSource).toContain('gitApi.getFileContent(worktreePath, filePath)')
    expect(unstagedSvgSource).toContain(
      'setModifiedSvg(modResult.success && modResult.content ? modResult.content : null)'
    )
    expect(unstagedSvgSource).not.toContain('window.gitOps')
    expect(unstagedSvgSource).not.toContain('unwrapEnvelope')
  })

  it('routes ImageDiffView unstaged binary original content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/ImageDiffView.tsx'),
      'utf-8'
    )
    const stagedStart = source.indexOf('      } else if (staged) {')
    const stagedBinaryStart = source.indexOf('        } else {', stagedStart)
    const unstagedStart = source.indexOf(
      '      } else {',
      stagedBinaryStart + '        } else {'.length
    )
    const svgStart = source.indexOf('        if (isSvg) {', unstagedStart)
    const binaryStart = source.indexOf('        } else {', svgStart)
    const nextBranchEnd = source.indexOf('      }', binaryStart + '        } else {'.length)
    const unstagedBinarySource = source.slice(binaryStart, nextBranchEnd)

    expect(stagedStart).toBeGreaterThan(-1)
    expect(stagedBinaryStart).toBeGreaterThan(stagedStart)
    expect(unstagedStart).toBeGreaterThan(stagedBinaryStart)
    expect(svgStart).toBeGreaterThan(unstagedStart)
    expect(binaryStart).toBeGreaterThan(svgStart)
    expect(nextBranchEnd).toBeGreaterThan(binaryStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(unstagedBinarySource).toContain("gitApi.getRefContentBase64(worktreePath, '', filePath)")
    expect(unstagedBinarySource).toContain('setOriginalUri(')
    expect(unstagedBinarySource).toContain('buildDataUri(origResult.data, origResult.mimeType)')
    expect(unstagedBinarySource).not.toContain(
      "window.gitOps.getRefContentBase64(worktreePath, '', filePath).then(unwrapEnvelope)"
    )
  })

  it('routes ImageDiffView unstaged binary modified content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/ImageDiffView.tsx'),
      'utf-8'
    )
    const stagedStart = source.indexOf('      } else if (staged) {')
    const stagedBinaryStart = source.indexOf('        } else {', stagedStart)
    const unstagedStart = source.indexOf(
      '      } else {',
      stagedBinaryStart + '        } else {'.length
    )
    const svgStart = source.indexOf('        if (isSvg) {', unstagedStart)
    const binaryStart = source.indexOf('        } else {', svgStart)
    const nextBranchEnd = source.indexOf('      }', binaryStart + '        } else {'.length)
    const unstagedBinarySource = source.slice(binaryStart, nextBranchEnd)

    expect(stagedStart).toBeGreaterThan(-1)
    expect(stagedBinaryStart).toBeGreaterThan(stagedStart)
    expect(unstagedStart).toBeGreaterThan(stagedBinaryStart)
    expect(svgStart).toBeGreaterThan(unstagedStart)
    expect(binaryStart).toBeGreaterThan(svgStart)
    expect(nextBranchEnd).toBeGreaterThan(binaryStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(unstagedBinarySource).toContain('gitApi.getFileContentBase64(worktreePath, filePath)')
    expect(unstagedBinarySource).toContain('setModifiedUri(')
    expect(unstagedBinarySource).toContain('buildDataUri(modResult.data, modResult.mimeType)')
    expect(unstagedBinarySource).not.toContain('window.gitOps')
    expect(unstagedBinarySource).not.toContain('unwrapEnvelope')
  })

  it('routes HunkActionGutter stage-hunk action through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/HunkActionGutter.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleStageHunk = useCallback(')
    const handlerEnd = source.indexOf('const handleUnstageHunk = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(handlerSource).toContain(
      'const patch = createHunkPatch(filePath, originalLines, modifiedLines, hunk)'
    )
    expect(handlerSource).toContain('const result = await gitApi.stageHunk(worktreePath, patch)')
    expect(handlerSource).toContain("toast.success('Hunk staged')")
    expect(handlerSource).toContain('useGitStore.getState().refreshStatuses(worktreePath)')
    expect(handlerSource).toContain('onContentChanged()')
    expect(handlerSource).not.toContain('window.gitOps.stageHunk')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes HunkActionGutter unstage-hunk action through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/HunkActionGutter.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleUnstageHunk = useCallback(')
    const handlerEnd = source.indexOf('const handleRevertHunk = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(handlerSource).toContain(
      'const patch = createHunkPatch(filePath, originalLines, modifiedLines, hunk)'
    )
    expect(handlerSource).toContain('const result = await gitApi.unstageHunk(worktreePath, patch)')
    expect(handlerSource).toContain("toast.success('Hunk unstaged')")
    expect(handlerSource).toContain('useGitStore.getState().refreshStatuses(worktreePath)')
    expect(handlerSource).toContain('onContentChanged()')
    expect(handlerSource).not.toContain('window.gitOps.unstageHunk')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes HunkActionGutter revert-hunk action through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/HunkActionGutter.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleRevertHunk = useCallback(')
    const handlerEnd = source.indexOf(
      'if (!modifiedEditor || hunks.length === 0) return null',
      handlerStart
    )
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
    expect(handlerSource).toContain(
      'const patch = createHunkPatch(filePath, originalLines, modifiedLines, hunk)'
    )
    expect(handlerSource).toContain('const result = await gitApi.revertHunk(worktreePath, patch)')
    expect(handlerSource).toContain("toast.success('Hunk reverted')")
    expect(handlerSource).toContain('useGitStore.getState().refreshStatuses(worktreePath)')
    expect(handlerSource).toContain('onContentChanged()')
    expect(handlerSource).not.toContain('window.gitOps.revertHunk')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes MonacoDiffView new/untracked modified content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/MonacoDiffView.tsx'),
      'utf-8'
    )
    const fetchStart = source.indexOf('const fetchContent = useCallback(async () => {')
    const newStart = source.indexOf('if (isNewFile || isUntracked) {', fetchStart)
    const compareStart = source.indexOf('if (compareBranch) {', newStart)
    const newSource = source.slice(newStart, compareStart)

    expect(fetchStart).toBeGreaterThan(-1)
    expect(newStart).toBeGreaterThan(fetchStart)
    expect(compareStart).toBeGreaterThan(newStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(newSource).toContain(
      'const modResult = await gitApi.getFileContent(worktreePath, filePath)'
    )
    expect(newSource).toContain("setOriginalContent('')")
    expect(newSource).toContain(
      "setModifiedContent(modResult.success ? (modResult.content ?? '') : '')"
    )
    expect(newSource).not.toContain('window.gitOps.getFileContent')
    expect(newSource).not.toContain('unwrapEnvelope')
  })

  it('routes MonacoDiffView compare-branch original content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/MonacoDiffView.tsx'),
      'utf-8'
    )
    const fetchStart = source.indexOf('const fetchContent = useCallback(async () => {')
    const compareStart = source.indexOf('if (compareBranch) {', fetchStart)
    const stagedStart = source.indexOf('} else if (staged) {', compareStart)
    const compareSource = source.slice(compareStart, stagedStart)

    expect(fetchStart).toBeGreaterThan(-1)
    expect(compareStart).toBeGreaterThan(fetchStart)
    expect(stagedStart).toBeGreaterThan(compareStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(compareSource).toContain(
      'gitApi.getBranchBaseContent(worktreePath, compareBranch, filePath)'
    )
    expect(compareSource).toContain(
      "setOriginalContent(origResult.success ? (origResult.content ?? '') : '')"
    )
    expect(compareSource).not.toContain(
      'window.gitOps.getBranchBaseContent(worktreePath, compareBranch, filePath)'
    )
  })

  it('routes MonacoDiffView compare-branch modified content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/MonacoDiffView.tsx'),
      'utf-8'
    )
    const fetchStart = source.indexOf('const fetchContent = useCallback(async () => {')
    const compareStart = source.indexOf('if (compareBranch) {', fetchStart)
    const stagedStart = source.indexOf('} else if (staged) {', compareStart)
    const compareSource = source.slice(compareStart, stagedStart)

    expect(fetchStart).toBeGreaterThan(-1)
    expect(compareStart).toBeGreaterThan(fetchStart)
    expect(stagedStart).toBeGreaterThan(compareStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(compareSource).toContain('const [origResult, modResult] = await Promise.all([')
    expect(compareSource).toContain(
      'gitApi.getBranchBaseContent(worktreePath, compareBranch, filePath)'
    )
    expect(compareSource).toContain('gitApi.getFileContent(worktreePath, filePath)')
    expect(compareSource).toContain(
      "setModifiedContent(modResult.success ? (modResult.content ?? '') : '')"
    )
    expect(compareSource).not.toContain('window.gitOps.getFileContent(worktreePath, filePath)')
    expect(compareSource).not.toContain('unwrapEnvelope')
  })

  it('routes MonacoDiffView staged original HEAD content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/MonacoDiffView.tsx'),
      'utf-8'
    )
    const fetchStart = source.indexOf('const fetchContent = useCallback(async () => {')
    const stagedStart = source.indexOf('} else if (staged) {', fetchStart)
    const unstagedStart = source.indexOf('} else {', stagedStart)
    const stagedSource = source.slice(stagedStart, unstagedStart)

    expect(fetchStart).toBeGreaterThan(-1)
    expect(stagedStart).toBeGreaterThan(fetchStart)
    expect(unstagedStart).toBeGreaterThan(stagedStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(stagedSource).toContain('const [origResult, modResult] = await Promise.all([')
    expect(stagedSource).toContain("gitApi.getRefContent(worktreePath, 'HEAD', filePath)")
    expect(stagedSource).toContain("gitApi.getRefContent(worktreePath, '', filePath)")
    expect(stagedSource).toContain("setError(origResult.error || 'Failed to load HEAD version')")
    expect(stagedSource).toContain("setOriginalContent(origResult.content ?? '')")
    expect(stagedSource).not.toContain(
      "window.gitOps.getRefContent(worktreePath, 'HEAD', filePath)"
    )
  })

  it('routes MonacoDiffView staged modified index content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/MonacoDiffView.tsx'),
      'utf-8'
    )
    const fetchStart = source.indexOf('const fetchContent = useCallback(async () => {')
    const stagedStart = source.indexOf('} else if (staged) {', fetchStart)
    const unstagedStart = source.indexOf('} else {', stagedStart)
    const stagedSource = source.slice(stagedStart, unstagedStart)

    expect(fetchStart).toBeGreaterThan(-1)
    expect(stagedStart).toBeGreaterThan(fetchStart)
    expect(unstagedStart).toBeGreaterThan(stagedStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(stagedSource).toContain('const [origResult, modResult] = await Promise.all([')
    expect(stagedSource).toContain("gitApi.getRefContent(worktreePath, 'HEAD', filePath)")
    expect(stagedSource).toContain("gitApi.getRefContent(worktreePath, '', filePath)")
    expect(stagedSource).toContain("setError(modResult.error || 'Failed to load staged version')")
    expect(stagedSource).toContain("setModifiedContent(modResult.content ?? '')")
    expect(stagedSource).not.toContain("window.gitOps.getRefContent(worktreePath, '', filePath)")
    expect(stagedSource).not.toContain('unwrapEnvelope')
  })

  it('routes MonacoDiffView unstaged original index content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/MonacoDiffView.tsx'),
      'utf-8'
    )
    const fetchStart = source.indexOf('const fetchContent = useCallback(async () => {')
    const stagedStart = source.indexOf('} else if (staged) {', fetchStart)
    const unstagedStart = source.indexOf('} else {', stagedStart)
    const validationStart = source.indexOf('if (!origResult.success', unstagedStart)
    const unstagedOriginalSource = source.slice(unstagedStart, validationStart)

    expect(fetchStart).toBeGreaterThan(-1)
    expect(stagedStart).toBeGreaterThan(fetchStart)
    expect(unstagedStart).toBeGreaterThan(stagedStart)
    expect(validationStart).toBeGreaterThan(unstagedStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(unstagedOriginalSource).toContain('gitApi')
    expect(unstagedOriginalSource).toContain(".getRefContent(worktreePath, '', filePath)")
    expect(unstagedOriginalSource).toContain(".getRefContent(worktreePath, 'HEAD', filePath)")
    expect(unstagedOriginalSource).toContain('.catch(() =>')
    expect(unstagedOriginalSource).not.toContain(
      "window.gitOps.getRefContent(worktreePath, '', filePath)"
    )
  })

  it('routes MonacoDiffView unstaged original HEAD fallback loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/MonacoDiffView.tsx'),
      'utf-8'
    )
    const fetchStart = source.indexOf('const fetchContent = useCallback(async () => {')
    const stagedStart = source.indexOf('} else if (staged) {', fetchStart)
    const unstagedStart = source.indexOf('} else {', stagedStart)
    const validationStart = source.indexOf('if (!origResult.success', unstagedStart)
    const unstagedOriginalSource = source.slice(unstagedStart, validationStart)

    expect(fetchStart).toBeGreaterThan(-1)
    expect(stagedStart).toBeGreaterThan(fetchStart)
    expect(unstagedStart).toBeGreaterThan(stagedStart)
    expect(validationStart).toBeGreaterThan(unstagedStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(unstagedOriginalSource).toContain('.catch(() =>')
    expect(unstagedOriginalSource).toContain("gitApi.getRefContent(worktreePath, 'HEAD', filePath)")
    expect(source).toContain("setOriginalContent(origResult.content ?? '')")
    expect(unstagedOriginalSource).not.toContain(
      "window.gitOps.getRefContent(worktreePath, 'HEAD', filePath)"
    )
    expect(unstagedOriginalSource).not.toContain('unwrapEnvelope')
  })

  it('routes MonacoDiffView unstaged modified working-tree content loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/MonacoDiffView.tsx'),
      'utf-8'
    )
    const fetchStart = source.indexOf('const fetchContent = useCallback(async () => {')
    const stagedStart = source.indexOf('} else if (staged) {', fetchStart)
    const unstagedStart = source.indexOf('} else {', stagedStart)
    const validationStart = source.indexOf('if (!origResult.success', unstagedStart)
    const unstagedLoadSource = source.slice(unstagedStart, validationStart)

    expect(fetchStart).toBeGreaterThan(-1)
    expect(stagedStart).toBeGreaterThan(fetchStart)
    expect(unstagedStart).toBeGreaterThan(stagedStart)
    expect(validationStart).toBeGreaterThan(unstagedStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(unstagedLoadSource).toContain('const [origResult, modResult] = await Promise.all([')
    expect(unstagedLoadSource).toContain('gitApi.getFileContent(worktreePath, filePath)')
    expect(source).toContain("setError(modResult.error || 'Failed to load file content')")
    expect(source).toContain("setModifiedContent(modResult.content ?? '')")
    expect(unstagedLoadSource).not.toContain('window.gitOps.getFileContent(worktreePath, filePath)')
    expect(unstagedLoadSource).not.toContain('unwrapEnvelope')
  })

  it('routes MonacoDiffView stage-hunk action through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/MonacoDiffView.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleStageHunk = useCallback(')
    const handlerEnd = source.indexOf('const handleUnstageHunk = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(handlerSource).toContain(
      'const patch = createHunkPatch(filePath, originalLines, modifiedLines, hunk)'
    )
    expect(handlerSource).toContain('const result = await gitApi.stageHunk(worktreePath, patch)')
    expect(handlerSource).toContain("toast.success('Hunk staged')")
    expect(handlerSource).toContain('useGitStore.getState().refreshStatuses(worktreePath)')
    expect(handlerSource).toContain('handleContentChanged()')
    expect(handlerSource).not.toContain('window.gitOps.stageHunk')
    expect(handlerSource).not.toContain('unwrapEnvelope')
  })

  it('routes MonacoDiffView unstage-hunk action through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/MonacoDiffView.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleUnstageHunk = useCallback(')
    const handlerEnd = source.indexOf('const handleDiscardHunk = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(handlerSource).toContain(
      'const patch = createHunkPatch(filePath, originalLines, modifiedLines, hunk)'
    )
    expect(handlerSource).toContain('const result = await gitApi.unstageHunk(worktreePath, patch)')
    expect(handlerSource).toContain("toast.success('Hunk unstaged')")
    expect(handlerSource).toContain('useGitStore.getState().refreshStatuses(worktreePath)')
    expect(handlerSource).toContain('handleContentChanged()')
    expect(handlerSource).not.toContain('window.gitOps.unstageHunk')
    expect(handlerSource).not.toContain('unwrapEnvelope')
  })

  it('routes MonacoDiffView discard-hunk action through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/MonacoDiffView.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleDiscardHunk = useCallback(')
    const handlerEnd = source.indexOf('const hunkHeaderActions = useMemo', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(handlerSource).toContain(
      'const patch = createHunkPatch(filePath, originalLines, modifiedLines, hunk)'
    )
    expect(handlerSource).toContain('const result = await gitApi.revertHunk(worktreePath, patch)')
    expect(handlerSource).toContain("toast.success('Hunk discarded')")
    expect(handlerSource).toContain('useGitStore.getState().refreshStatuses(worktreePath)')
    expect(handlerSource).toContain('handleContentChanged()')
    expect(handlerSource).not.toContain('window.gitOps.revertHunk')
    expect(handlerSource).not.toContain('unwrapEnvelope')
  })

  it('routes MonacoDiffView compare-branch diff copy through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/MonacoDiffView.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleCopy = useCallback(async () => {')
    const branchStart = source.indexOf('if (compareBranch) {', handlerStart)
    const branchEnd = source.indexOf('} else {', branchStart)
    const branchSource = source.slice(branchStart, branchEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(branchStart).toBeGreaterThan(-1)
    expect(branchEnd).toBeGreaterThan(branchStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(branchSource).toContain(
      'const result = await gitApi.getBranchFileDiff(worktreePath, compareBranch, filePath)'
    )
    expect(branchSource).toContain('await projectApi.copyToClipboard(result.diff)')
    expect(branchSource).not.toContain('window.gitOps.getBranchFileDiff')
    expect(branchSource).not.toContain('unwrapEnvelope')
    expect(branchSource).not.toContain('window.projectOps.copyToClipboard')
  })

  it('routes MonacoDiffView status-change subscription through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/MonacoDiffView.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('// Listen for external file changes')
    const effectEnd = source.indexOf('// Handle Monaco mount', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(effectSource).toContain('const cleanup = gitApi.onStatusChanged((event) => {')
    expect(effectSource).toContain('event.worktreePath === worktreePath')
    expect(effectSource).toContain('!recentActionRef.current')
    expect(effectSource).toContain('setRefreshKey((k) => k + 1)')
    expect(effectSource).toContain('return cleanup')
    expect(effectSource).not.toContain('window.gitOps.onStatusChanged')
  })

  it('routes MonacoDiffView normal diff copy through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/MonacoDiffView.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleCopy = useCallback(async () => {')
    const branchStart = source.indexOf('if (compareBranch) {', handlerStart)
    const elseStart = source.indexOf('} else {', branchStart)
    const handlerEnd = source.indexOf(
      '}, [worktreePath, filePath, staged, isUntracked, compareBranch])',
      elseStart
    )
    const elseSource = source.slice(elseStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(branchStart).toBeGreaterThan(-1)
    expect(elseStart).toBeGreaterThan(branchStart)
    expect(handlerEnd).toBeGreaterThan(elseStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(elseSource).toContain(
      'const result = await gitApi.getDiff(worktreePath, filePath, staged, isUntracked)'
    )
    expect(elseSource).toContain('await projectApi.copyToClipboard(result.diff)')
    expect(elseSource).not.toContain('window.gitOps.getDiff')
    expect(elseSource).not.toContain('unwrapEnvelope')
    expect(elseSource).not.toContain('window.projectOps.copyToClipboard')
  })

  it('routes DiffModal diff copy through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/DiffModal.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleCopyDiff = useCallback(async () => {')
    const handlerEnd = source.indexOf('const toggleViewMode = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('if (diff) {')
    expect(handlerSource).toContain('await projectApi.copyToClipboard(diff)')
    expect(handlerSource).not.toContain('window.projectOps.copyToClipboard')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes DiffModal diff loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/diff/DiffModal.tsx'),
      'utf-8'
    )
    const loadStart = source.indexOf('  // Load diff when modal opens')
    const copyStart = source.indexOf('  // Copy diff content to clipboard', loadStart)
    const loadSource = source.slice(loadStart, copyStart)

    expect(loadStart).toBeGreaterThan(-1)
    expect(copyStart).toBeGreaterThan(loadStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(loadSource).toContain(
      'const result = await gitApi.getDiff(worktreePath, filePath, staged, isUntracked)'
    )
    expect(loadSource).toContain('setDiff(result.diff)')
    expect(loadSource).toContain("setError(result.error || 'Failed to load diff')")
    expect(loadSource).not.toContain('window.gitOps.getDiff')
    expect(loadSource).not.toContain('unwrapEnvelope')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('routes XtermBackend web link opening through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/XtermBackend.ts'
      ),
      'utf-8'
    )
    const handlerStart = source.indexOf(
      'const webLinksAddon = new WebLinksAddon((_event, uri) => {'
    )
    const handlerEnd = source.indexOf('terminal.loadAddon(webLinksAddon)', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('projectApi.openPath(uri).catch(console.error)')
    expect(handlerSource).not.toContain('window.projectOps.openPath')
    expect(handlerSource).not.toContain('then(unwrapEnvelope)')
  })

  it('routes XtermBackend clipboard paste fallback through projectApi and terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/XtermBackend.ts'
      ),
      'utf-8'
    )
    const branchStart = source.indexOf(
      "if (e.metaKey && e.shiftKey && e.key === 'V' && e.type === 'keydown') {"
    )
    const branchEnd = source.indexOf('return false', branchStart)
    const branchSource = source.slice(branchStart, branchEnd)

    expect(branchStart).toBeGreaterThan(-1)
    expect(branchEnd).toBeGreaterThan(branchStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(branchSource).toContain('navigator.clipboard')
    expect(branchSource).toContain('.readText()')
    expect(branchSource).toContain('.catch(() => projectApi.readFromClipboard())')
    expect(branchSource).toContain('terminalApi.write(this.terminalId, text)')
    expect(branchSource).not.toContain('window.terminalOps.write(this.terminalId, text)')
    expect(branchSource).not.toContain('window.projectOps.readFromClipboard')
    expect(branchSource).not.toContain('then(unwrapEnvelope)')
  })

  it('routes XtermBackend user input writes through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/XtermBackend.ts'
      ),
      'utf-8'
    )
    const inputStart = source.indexOf('// Wire user input -> PTY')
    const outputStart = source.indexOf('// Wire PTY output -> terminal display', inputStart)
    const inputSource = source.slice(inputStart, outputStart)

    expect(inputStart).toBeGreaterThan(-1)
    expect(outputStart).toBeGreaterThan(inputStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(inputSource).toContain('this.inputDisposable = terminal.onData((data) => {')
    expect(inputSource).toContain('terminalApi.write(this.terminalId, data)')
    expect(inputSource).not.toContain('window.terminalOps.write')
  })

  it('routes XtermBackend PTY output subscription through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/XtermBackend.ts'
      ),
      'utf-8'
    )
    const outputStart = source.indexOf('// Wire PTY output -> terminal display')
    const exitStart = source.indexOf('// Wire PTY exit -> status change', outputStart)
    const outputSource = source.slice(outputStart, exitStart)

    expect(outputStart).toBeGreaterThan(-1)
    expect(exitStart).toBeGreaterThan(outputStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(outputSource).toContain(
      'this.removeDataListener = terminalApi.onData(this.terminalId, (data) => {'
    )
    expect(outputSource).toContain('terminal.write(data)')
    expect(outputSource).not.toContain('window.terminalOps.onData')
  })

  it('routes XtermBackend PTY exit subscription through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/XtermBackend.ts'
      ),
      'utf-8'
    )
    const exitStart = source.indexOf('// Wire PTY exit -> status change')
    const createStart = source.indexOf('// Create the PTY', exitStart)
    const exitSource = source.slice(exitStart, createStart)

    expect(exitStart).toBeGreaterThan(-1)
    expect(createStart).toBeGreaterThan(exitStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(exitSource).toContain(
      'this.removeExitListener = terminalApi.onExit(this.terminalId, (code) => {'
    )
    expect(exitSource).toContain(
      'terminal.write(`\\r\\n\\x1b[90m[Process exited with code ${code}]\\x1b[0m\\r\\n`)'
    )
    expect(exitSource).toContain("callbacks.onStatusChange('exited', code)")
    expect(exitSource).not.toContain('window.terminalOps.onExit')
  })

  it('routes XtermBackend PTY creation through terminalApi by default', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/XtermBackend.ts'
      ),
      'utf-8'
    )
    const createStart = source.indexOf('// Create the PTY')
    const resizeSyncStart = source.indexOf('// Immediately sync PTY size', createStart)
    const createSource = source.slice(createStart, resizeSyncStart)

    expect(createStart).toBeGreaterThan(-1)
    expect(resizeSyncStart).toBeGreaterThan(createStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(createSource).toContain("callbacks.onStatusChange('creating')")
    expect(createSource).toContain(
      'const createTerminal = opts.createTerminal ?? terminalApi.create'
    )
    expect(createSource).toContain('createTerminal(this.terminalId, opts.cwd, opts.shell)')
    expect(createSource).toContain('.then(unwrapEnvelope)')
    expect(createSource).not.toContain('window.terminalOps.create')
  })

  it('keeps terminal.create on renderer RPC and server-owned PTY events', () => {
    const terminalApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/terminal-api.ts'),
      'utf-8'
    )
    const terminalOpsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/terminal-ops.ts'),
      'utf-8'
    )
    const terminalApiTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/__tests__/terminal-api.test.ts'),
      'utf-8'
    )
    const terminalOpsTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/terminal-ops.test.ts'),
      'utf-8'
    )
    const routerTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/rpc-router.test.ts'),
      'utf-8'
    )

    const createApiStart = terminalApiSource.indexOf(
      'create: async (\n    terminalId: string,\n    cwd: string,\n    shell?: string'
    )
    const createApiEnd = terminalApiSource.indexOf('  createClaudeCli:', createApiStart)
    const createApiSource = terminalApiSource.slice(createApiStart, createApiEnd)

    const createServiceStart = terminalOpsSource.indexOf('  create: (terminalId, cwd, shell) =>')
    const createServiceEnd = terminalOpsSource.indexOf('  createClaudeCli:', createServiceStart)
    const createServiceSource = terminalOpsSource.slice(createServiceStart, createServiceEnd)

    expect(createApiStart).toBeGreaterThan(-1)
    expect(createApiEnd).toBeGreaterThan(createApiStart)
    expect(createApiSource).toContain(
      "}>('terminalOps.create', { terminalId, cwd, shell })"
    )
    expect(createApiSource).toContain('return { success: true, value: result }')
    expect(createApiSource).not.toContain('window.terminalOps')

    expect(terminalOpsSource).toContain("'terminalOps.create'")
    expect(terminalOpsSource).toContain('createParamsSchema.parse(params)')
    expect(terminalOpsSource).toContain(
      'return yield* service.create(parsed.terminalId, parsed.cwd, parsed.shell)'
    )
    expect(createServiceStart).toBeGreaterThan(-1)
    expect(createServiceEnd).toBeGreaterThan(createServiceStart)
    expect(createServiceSource).toContain('ptyService.create(terminalId, { cwd, shell: shell || undefined })')
    expect(createServiceSource).toContain('attachBackendPtyListeners(eventBus, terminalId)')
    expect(createServiceSource).not.toContain('requestDesktop')
    expect(createServiceSource).not.toContain('BrowserWindow')

    expect(terminalOpsSource).toContain(
      "publishTerminalEvent(eventBus, `terminal:data:${terminalId}`, buffered)"
    )
    expect(terminalOpsSource).toContain(
      "publishTerminalEvent(eventBus, `terminal:exit:${terminalId}`, code)"
    )
    expect(terminalApiTestSource).toContain("expect(request).toHaveBeenCalledWith('terminalOps.create'")
    expect(routerTestSource).toContain(
      "it('handles terminalOps.create through the terminal ops RPC domain'"
    )
    expect(routerTestSource).toContain("it('validates terminalOps.create params'")
    expect(terminalOpsTestSource).toContain(
      "it('creates backend-owned PTYs and publishes buffered data and exit events'"
    )
  })

  it('keeps terminal.write on renderer RPC and backend-owned PTY writes', () => {
    const terminalApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/terminal-api.ts'),
      'utf-8'
    )
    const terminalOpsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/terminal-ops.ts'),
      'utf-8'
    )
    const terminalApiTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/__tests__/terminal-api.test.ts'),
      'utf-8'
    )
    const terminalOpsTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/terminal-ops.test.ts'),
      'utf-8'
    )
    const routerTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/rpc-router.test.ts'),
      'utf-8'
    )

    const writeApiStart = terminalApiSource.indexOf(
      'write: (terminalId: string, data: string): void =>'
    )
    const writeApiEnd = terminalApiSource.indexOf('  resize:', writeApiStart)
    const writeApiSource = terminalApiSource.slice(writeApiStart, writeApiEnd)

    const writeServiceStart = terminalOpsSource.indexOf('  write: (terminalId, data) =>')
    const writeServiceEnd = terminalOpsSource.indexOf('  resize:', writeServiceStart)
    const writeServiceSource = terminalOpsSource.slice(writeServiceStart, writeServiceEnd)

    expect(writeApiStart).toBeGreaterThan(-1)
    expect(writeApiEnd).toBeGreaterThan(writeApiStart)
    expect(writeApiSource).toContain(
      ".request<void>('terminalOps.write', { terminalId, data })"
    )
    expect(writeApiSource).toContain('.catch(() => undefined)')
    expect(writeApiSource).not.toContain('window.terminalOps')

    expect(terminalOpsSource).toContain("'terminalOps.write'")
    expect(terminalOpsSource).toContain('writeParamsSchema.parse(params)')
    expect(terminalOpsSource).toContain('return yield* service.write(parsed.terminalId, parsed.data)')
    expect(writeServiceStart).toBeGreaterThan(-1)
    expect(writeServiceEnd).toBeGreaterThan(writeServiceStart)
    expect(writeServiceSource).toContain('if (ptyService.has(terminalId))')
    expect(writeServiceSource).toContain('ptyService.write(terminalId, data)')
    expect(writeServiceSource).toContain('const result = await requestDesktopTerminalWrite')
    expect(writeServiceSource).not.toContain('BrowserWindow')

    expect(terminalApiTestSource).toContain(
      "it('routes write through the renderer RPC client without returning a promise'"
    )
    expect(terminalApiTestSource).toContain("expect(request).toHaveBeenCalledWith('terminalOps.write'")
    expect(terminalApiTestSource).toContain("it('swallows write request rejections'")
    expect(routerTestSource).toContain(
      "it('handles terminalOps.write through the terminal ops RPC domain'"
    )
    expect(routerTestSource).toContain("it('validates terminalOps.write params'")
    expect(terminalOpsTestSource).toContain(
      "it('writes to backend-owned PTYs without using the desktop command fallback'"
    )
  })

  it('keeps terminal.resize on renderer RPC and backend-owned PTY resizes', () => {
    const terminalApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/terminal-api.ts'),
      'utf-8'
    )
    const terminalOpsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/terminal-ops.ts'),
      'utf-8'
    )
    const terminalApiTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/__tests__/terminal-api.test.ts'),
      'utf-8'
    )
    const terminalOpsTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/terminal-ops.test.ts'),
      'utf-8'
    )
    const routerTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/rpc-router.test.ts'),
      'utf-8'
    )

    const resizeApiStart = terminalApiSource.indexOf(
      'resize: async (terminalId: string, cols: number, rows: number): Promise<Envelope<void>> =>'
    )
    const resizeApiEnd = terminalApiSource.indexOf('  onData:', resizeApiStart)
    const resizeApiSource = terminalApiSource.slice(resizeApiStart, resizeApiEnd)

    const resizeServiceStart = terminalOpsSource.indexOf('  resize: (terminalId, cols, rows) =>')
    const resizeServiceEnd = terminalOpsSource.indexOf('  destroy:', resizeServiceStart)
    const resizeServiceSource = terminalOpsSource.slice(resizeServiceStart, resizeServiceEnd)

    expect(resizeApiStart).toBeGreaterThan(-1)
    expect(resizeApiEnd).toBeGreaterThan(resizeApiStart)
    expect(resizeApiSource).toContain(
      "await getRendererRpcClient().request<void>('terminalOps.resize', { terminalId, cols, rows })"
    )
    expect(resizeApiSource).toContain('return { success: true, value: undefined }')
    expect(resizeApiSource).not.toContain('window.terminalOps')

    expect(terminalOpsSource).toContain("'terminalOps.resize'")
    expect(terminalOpsSource).toContain('resizeParamsSchema.parse(params)')
    expect(terminalOpsSource).toContain(
      'return yield* service.resize(parsed.terminalId, parsed.cols, parsed.rows)'
    )
    expect(resizeServiceStart).toBeGreaterThan(-1)
    expect(resizeServiceEnd).toBeGreaterThan(resizeServiceStart)
    expect(resizeServiceSource).toContain('if (ptyService.has(terminalId))')
    expect(resizeServiceSource).toContain('ptyService.resize(terminalId, cols, rows)')
    expect(resizeServiceSource).toContain('const result = await requestDesktopTerminalResize')
    expect(resizeServiceSource).not.toContain('BrowserWindow')

    expect(terminalApiTestSource).toContain("it('routes resize through the renderer RPC client'")
    expect(terminalApiTestSource).toContain(
      "expect(request).toHaveBeenCalledWith('terminalOps.resize'"
    )
    expect(routerTestSource).toContain(
      "it('handles terminalOps.resize through the terminal ops RPC domain'"
    )
    expect(routerTestSource).toContain("it('validates terminalOps.resize params'")
    expect(terminalOpsTestSource).toContain(
      "it('resizes backend-owned PTYs without using the desktop command fallback'"
    )
  })

  it('keeps terminal.destroy on renderer RPC and backend-owned PTY cleanup', () => {
    const terminalApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/terminal-api.ts'),
      'utf-8'
    )
    const terminalOpsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/terminal-ops.ts'),
      'utf-8'
    )
    const terminalApiTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/__tests__/terminal-api.test.ts'),
      'utf-8'
    )
    const terminalOpsTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/terminal-ops.test.ts'),
      'utf-8'
    )
    const routerTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/rpc-router.test.ts'),
      'utf-8'
    )

    const destroyApiStart = terminalApiSource.indexOf(
      'destroy: async (terminalId: string): Promise<Envelope<void>> =>'
    )
    const destroyApiEnd = terminalApiSource.indexOf('  write:', destroyApiStart)
    const destroyApiSource = terminalApiSource.slice(destroyApiStart, destroyApiEnd)

    const destroyServiceStart = terminalOpsSource.indexOf('  destroy: (terminalId) =>')
    const destroyServiceEnd = terminalOpsSource.indexOf('export const makeTerminalOpsRpcHandlers')
    const destroyServiceSource = terminalOpsSource.slice(destroyServiceStart, destroyServiceEnd)

    expect(destroyApiStart).toBeGreaterThan(-1)
    expect(destroyApiEnd).toBeGreaterThan(destroyApiStart)
    expect(destroyApiSource).toContain(
      "await getRendererRpcClient().request<void>('terminalOps.destroy', { terminalId })"
    )
    expect(destroyApiSource).toContain('return { success: true, value: undefined }')
    expect(destroyApiSource).not.toContain('window.terminalOps')

    expect(terminalOpsSource).toContain("'terminalOps.destroy'")
    expect(terminalOpsSource).toContain('terminalIdParamsSchema.parse(params)')
    expect(terminalOpsSource).toContain('return yield* service.destroy(parsed.terminalId)')
    expect(destroyServiceStart).toBeGreaterThan(-1)
    expect(destroyServiceEnd).toBeGreaterThan(destroyServiceStart)
    expect(destroyServiceSource).toContain('if (ptyService.has(terminalId))')
    expect(destroyServiceSource).toContain('detachBackendPtyListeners(terminalId)')
    expect(destroyServiceSource).toContain('ptyService.destroy(terminalId)')
    expect(destroyServiceSource).toContain('const result = await requestDesktopTerminalDestroy')
    expect(destroyServiceSource).not.toContain('BrowserWindow')

    expect(terminalApiTestSource).toContain("it('routes destroy through the renderer RPC client'")
    expect(terminalApiTestSource).toContain(
      "expect(request).toHaveBeenCalledWith('terminalOps.destroy'"
    )
    expect(routerTestSource).toContain(
      "it('handles terminalOps.destroy through the terminal ops RPC domain'"
    )
    expect(routerTestSource).toContain("it('validates terminalOps.destroy params'")
    expect(terminalOpsTestSource).toContain(
      "it('destroys backend-owned PTYs after detaching event listeners'"
    )
  })

  it('keeps terminal data on renderer subscriptions and backend-published WS events', () => {
    const terminalApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/terminal-api.ts'),
      'utf-8'
    )
    const terminalOpsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/terminal-ops.ts'),
      'utf-8'
    )
    const terminalApiTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/__tests__/terminal-api.test.ts'),
      'utf-8'
    )
    const terminalOpsTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/terminal-ops.test.ts'),
      'utf-8'
    )
    const subscriptionsTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/subscriptions.test.ts'),
      'utf-8'
    )
    const hiveClientTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/__tests__/hive-client.test.ts'),
      'utf-8'
    )

    const onDataApiStart = terminalApiSource.indexOf(
      'onData: (terminalId: string, callback: (data: string) => void): (() => void) =>'
    )
    const onDataApiEnd = terminalApiSource.indexOf('  onExit:', onDataApiStart)
    const onDataApiSource = terminalApiSource.slice(onDataApiStart, onDataApiEnd)

    const attachStart = terminalOpsSource.indexOf(
      'const attachBackendPtyListeners = (eventBus: EventBus | undefined, terminalId: string): void =>'
    )
    const attachEnd = terminalOpsSource.indexOf('const isTerminalCommandResult', attachStart)
    const attachSource = terminalOpsSource.slice(attachStart, attachEnd)

    expect(onDataApiStart).toBeGreaterThan(-1)
    expect(onDataApiEnd).toBeGreaterThan(onDataApiStart)
    expect(onDataApiSource).toContain(
      'return getRendererRpcClient().subscribe(`terminal:data:${terminalId}`, (event: ServerEvent) => {'
    )
    expect(onDataApiSource).toContain("if (typeof event.payload === 'string') callback(event.payload)")
    expect(onDataApiSource).not.toContain('window.terminalOps')

    expect(attachStart).toBeGreaterThan(-1)
    expect(attachEnd).toBeGreaterThan(attachStart)
    expect(attachSource).toContain('ptyService.onData(terminalId, (data) => {')
    expect(attachSource).toContain('dataBuffers.set(terminalId, existing ? existing + data : data)')
    expect(attachSource).toContain(
      "publishTerminalEvent(eventBus, `terminal:data:${terminalId}`, buffered)"
    )
    expect(attachSource).not.toContain('webContents.send')
    expect(attachSource).not.toContain('BrowserWindow')

    expect(terminalApiTestSource).toContain(
      "it('subscribes to terminal data events through the renderer RPC client'"
    )
    expect(terminalApiTestSource).toContain(
      "expect(subscribe).toHaveBeenCalledWith('terminal:data:terminal-1', expect.any(Function))"
    )
    expect(terminalOpsTestSource).toContain(
      "it('creates backend-owned PTYs and publishes buffered data and exit events'"
    )
    expect(terminalOpsTestSource).toContain(
      "{ channel: 'terminal:data:terminal-1', payload: 'hello' }"
    )
    expect(subscriptionsTestSource).toContain(
      "it('delivers terminal data events over dynamic subscribed WebSocket channels'"
    )
    expect(subscriptionsTestSource).toContain("const channel = 'terminal:data:terminal-1'")
    expect(hiveClientTestSource).toContain(
      "client.subscribe(\n      'terminal:data:terminal-1',"
    )
  })

  it('keeps terminal exit on renderer subscriptions and backend-published WS events', () => {
    const terminalApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/terminal-api.ts'),
      'utf-8'
    )
    const terminalOpsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/terminal-ops.ts'),
      'utf-8'
    )
    const terminalApiTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/__tests__/terminal-api.test.ts'),
      'utf-8'
    )
    const terminalOpsTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/terminal-ops.test.ts'),
      'utf-8'
    )
    const subscriptionsTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/subscriptions.test.ts'),
      'utf-8'
    )

    const onExitApiStart = terminalApiSource.indexOf(
      'onExit: (terminalId: string, callback: (code: number) => void): (() => void) =>'
    )
    const onExitApiEnd = terminalApiSource.indexOf('  onClaudeSessionId:', onExitApiStart)
    const onExitApiSource = terminalApiSource.slice(onExitApiStart, onExitApiEnd)

    const attachStart = terminalOpsSource.indexOf(
      'const attachBackendPtyListeners = (eventBus: EventBus | undefined, terminalId: string): void =>'
    )
    const attachEnd = terminalOpsSource.indexOf('const isTerminalCommandResult', attachStart)
    const attachSource = terminalOpsSource.slice(attachStart, attachEnd)

    expect(onExitApiStart).toBeGreaterThan(-1)
    expect(onExitApiEnd).toBeGreaterThan(onExitApiStart)
    expect(onExitApiSource).toContain(
      'return getRendererRpcClient().subscribe(`terminal:exit:${terminalId}`, (event: ServerEvent) => {'
    )
    expect(onExitApiSource).toContain("if (typeof event.payload === 'number') callback(event.payload)")
    expect(onExitApiSource).not.toContain('window.terminalOps')

    expect(attachStart).toBeGreaterThan(-1)
    expect(attachEnd).toBeGreaterThan(attachStart)
    expect(attachSource).toContain('ptyService.onExit(terminalId, (code) => {')
    expect(attachSource).toContain(
      'publishTerminalEvent(eventBus, `terminal:exit:${terminalId}`, code)'
    )
    expect(attachSource).toContain('detachBackendPtyListeners(terminalId)')
    expect(attachSource).not.toContain('webContents.send')
    expect(attachSource).not.toContain('BrowserWindow')

    expect(terminalApiTestSource).toContain(
      "it('subscribes to terminal exit events through the renderer RPC client'"
    )
    expect(terminalApiTestSource).toContain(
      "expect(subscribe).toHaveBeenCalledWith('terminal:exit:terminal-1', expect.any(Function))"
    )
    expect(terminalOpsTestSource).toContain(
      "{ channel: 'terminal:exit:terminal-1', payload: 7 }"
    )
    expect(subscriptionsTestSource).toContain(
      "it('delivers terminal exit events over dynamic subscribed WebSocket channels'"
    )
    expect(subscriptionsTestSource).toContain("const channel = 'terminal:exit:terminal-1'")
  })

  it('removes the old terminal:create IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('create: (')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:create'")
    expect(terminalHandlersSource).not.toContain("defineHandler(\n    'terminal:create'")
    expect(terminalHandlersSource).not.toContain("defineHandler('terminal:create'")
  })

  it('removes the old terminal:write IPC send adapter and listener', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('write: (terminalId: string, data: string)')
    expect(terminalOpsSource).not.toContain("ipcRenderer.send('terminal:write'")
    expect(terminalHandlersSource).not.toContain("ipcMain.on('terminal:write'")
  })

  it('removes the old terminal:resize IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('resize: (terminalId: string')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:resize'")
    expect(terminalHandlersSource).not.toContain("defineHandler(\n    'terminal:resize'")
    expect(terminalHandlersSource).not.toContain("defineHandler('terminal:resize'")
  })

  it('removes the old terminal:destroy IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('destroy: (terminalId: string)')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:destroy'")
    expect(terminalHandlersSource).not.toContain("defineHandler(\n    'terminal:destroy'")
    expect(terminalHandlersSource).not.toContain("defineHandler('terminal:destroy'")
  })

  it('removes the old terminal:createClaudeCli IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('createClaudeCli: (')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:createClaudeCli'")
    expect(terminalHandlersSource).not.toContain("defineHandler(\n    'terminal:createClaudeCli'")
    expect(terminalHandlersSource).not.toContain("defineHandler('terminal:createClaudeCli'")
  })

  it('removes the old terminal:data IPC event adapter and renderer send path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('onData: (terminalId: string')
    expect(terminalOpsSource).not.toContain('`terminal:data:${terminalId}`')
    expect(terminalHandlersSource).not.toContain(
      'mainWindow.webContents.send(`terminal:data:${terminalId}`'
    )
    expect(terminalHandlersSource).not.toContain('if (mainWindow.isDestroyed()) return')
  })

  it('removes the old terminal:exit IPC event adapter and renderer send path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('onExit: (terminalId: string')
    expect(terminalOpsSource).not.toContain('`terminal:exit:${terminalId}`')
    expect(terminalHandlersSource).not.toContain(
      'mainWindow.webContents.send(`terminal:exit:${terminalId}`'
    )
  })

  it('removes the old terminal:claude-session-id IPC event adapter and renderer send path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('onClaudeSessionId: (')
    expect(terminalOpsSource).not.toContain('`terminal:claude-session-id:${sessionId}`')
    expect(terminalHandlersSource).not.toContain(
      'mainWindow.webContents.send(`terminal:claude-session-id:${sessionId}`'
    )
  })

  it('removes the old claude-cli:status IPC event adapter and renderer send path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const hookServerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/claude-hook-server.ts'),
      'utf-8'
    )
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('onClaudeCliStatus: (')
    expect(terminalOpsSource).not.toContain("ipcRenderer.on('claude-cli:status'")
    expect(terminalOpsSource).not.toContain("ipcRenderer.off('claude-cli:status'")
    expect(hookServerSource).not.toContain("mainWindow.webContents.send('claude-cli:status'")
    expect(hookServerSource).not.toContain('BrowserWindow')
    expect(hookServerSource).not.toContain('rendererWindow')
    expect(hookServerSource).toContain("publishDesktopBackendEvent('claude-cli:status'")
    expect(terminalHandlersSource).not.toContain('publishClaudeCliStatus(mainWindow')
  })

  it('keeps Claude hook server tests on backend event publishing assertions', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/__tests__/claude-hook-server.test.ts'),
      'utf-8'
    )

    expect(source).toContain('publishDesktopBackendEvent')
    expect(source).not.toContain('webContents.send')
    expect(source).not.toContain('makeWindow')
  })

  it('removes the old terminal:getConfig IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('getConfig: (): Promise<')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:getConfig'")
    expect(terminalHandlersSource).not.toContain("defineHandler('terminal:getConfig'")
    expect(terminalHandlersSource).not.toContain("'IPC: terminal:getConfig")
  })

  it('removes the old terminal:ghostty:init IPC invoke adapter and handler', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')
    const backendManagerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/backend-manager.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')
    const terminalBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/terminal-pty-bridge.ts'),
      'utf-8'
    )

    expect(terminalOpsSource).not.toContain('ghosttyInit: (): Promise<')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:ghostty:init'")
    expect(terminalHandlersSource).not.toContain("defineHandler('terminal:ghostty:init'")
    expect(terminalHandlersSource).not.toContain("'IPC: terminal:ghostty:init")
    expect(terminalHandlersSource).not.toContain('registerTerminalHandlers')
    expect(fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/terminal-handlers.ts'))).toBe(
      false
    )
    expect(
      fs.existsSync(
        path.resolve(
          __dirname,
          '../../src/main/ipc/__tests__/terminal-handlers.claude-cli.test.ts'
        )
      )
    ).toBe(false)
    expect(
      fs.existsSync(
        path.resolve(__dirname, '../../src/main/services/terminal-pty-bridge.claude-cli.test.ts')
      )
    ).toBe(true)
    expect(terminalBridgeSource).toContain('export async function createClaudeCliTerminal')
    expect(terminalBridgeSource).toContain('export function destroyNodePtyTerminal')
    expect(terminalBridgeSource).toContain('export function cleanupTerminals')
    expect(mainSource).toContain("from './services/terminal-pty-bridge'")
    expect(backendManagerSource).toContain("from '../services/terminal-pty-bridge'")
    expect(backendManagerSource).not.toContain("from '../ipc/terminal-handlers'")
    expect(mainSource).not.toContain('registerTerminalHandlers')
    expect(ipcIndexSource).not.toContain('registerTerminalHandlers')
    expect(ipcIndexSource).not.toContain('terminal-handlers')
    expect(mainSource).toContain('ghosttyService.setMainWindow(mainWindow)')
  })

  it('keeps terminal PTY bridge tests free of IPC and renderer window send mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/terminal-pty-bridge.claude-cli.test.ts'),
      'utf-8'
    )

    expect(source).toContain('publishDesktopBackendEvent')
    expect(source).not.toContain('ipcMain')
    expect(source).not.toContain('ipcMainOn')
    expect(source).not.toContain('webContentsSend')
    expect(source).not.toContain('webContents.send')
  })

  it('removes the old terminal:ghostty:isAvailable IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('ghosttyIsAvailable: (): Promise<')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:ghostty:isAvailable'")
    expect(terminalHandlersSource).not.toContain("defineHandler('terminal:ghostty:isAvailable'")
    expect(terminalHandlersSource).not.toContain("'IPC: terminal:ghostty:isAvailable")
  })

  it('removes the old terminal:ghostty:createSurface IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('ghosttyCreateSurface: (')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:ghostty:createSurface'")
    expect(terminalHandlersSource).not.toContain("'terminal:ghostty:createSurface'")
    expect(terminalHandlersSource).not.toContain("'IPC: terminal:ghostty:createSurface")
  })

  it('removes the old terminal:ghostty:setFrame IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('ghosttySetFrame: (')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:ghostty:setFrame'")
    expect(terminalHandlersSource).not.toContain("'terminal:ghostty:setFrame'")
    expect(terminalHandlersSource).not.toContain("'IPC: terminal:ghostty:setFrame")
  })

  it('removes the old terminal:ghostty:setSize IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('ghosttySetSize: (')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:ghostty:setSize'")
    expect(terminalHandlersSource).not.toContain("'terminal:ghostty:setSize'")
    expect(terminalHandlersSource).not.toContain("'IPC: terminal:ghostty:setSize")
  })

  it('removes the old terminal:ghostty:keyEvent IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('ghosttyKeyEvent: (')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:ghostty:keyEvent'")
    expect(terminalHandlersSource).not.toContain("'terminal:ghostty:keyEvent'")
    expect(terminalHandlersSource).not.toContain("'IPC: terminal:ghostty:keyEvent")
  })

  it('removes the old terminal:ghostty:mouseButton IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('ghosttyMouseButton: (')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:ghostty:mouseButton'")
    expect(terminalHandlersSource).not.toContain("'terminal:ghostty:mouseButton'")
    expect(terminalHandlersSource).not.toContain("'IPC: terminal:ghostty:mouseButton")
  })

  it('removes the old terminal:ghostty:mousePos IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('ghosttyMousePos: (')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:ghostty:mousePos'")
    expect(terminalHandlersSource).not.toContain("'terminal:ghostty:mousePos'")
    expect(terminalHandlersSource).not.toContain("'IPC: terminal:ghostty:mousePos")
  })

  it('removes the old terminal:ghostty:mouseScroll IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('ghosttyMouseScroll: (')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:ghostty:mouseScroll'")
    expect(terminalHandlersSource).not.toContain("'terminal:ghostty:mouseScroll'")
    expect(terminalHandlersSource).not.toContain("'IPC: terminal:ghostty:mouseScroll")
  })

  it('removes the old terminal:ghostty:setFocus IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('ghosttySetFocus: (')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:ghostty:setFocus'")
    expect(terminalHandlersSource).not.toContain("'terminal:ghostty:setFocus'")
    expect(terminalHandlersSource).not.toContain("'IPC: terminal:ghostty:setFocus")
  })

  it('removes the old terminal:ghostty:pasteText IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('ghosttyPasteText: (')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:ghostty:pasteText'")
    expect(terminalHandlersSource).not.toContain("'terminal:ghostty:pasteText'")
    expect(terminalHandlersSource).not.toContain("'IPC: terminal:ghostty:pasteText")
  })

  it('removes the old terminal:ghostty:focusDiagnostics IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('ghosttyFocusDiagnostics: (')
    expect(terminalOpsSource).not.toContain(
      "ipcRenderer.invoke('terminal:ghostty:focusDiagnostics'"
    )
    expect(terminalHandlersSource).not.toContain("'terminal:ghostty:focusDiagnostics'")
    expect(terminalHandlersSource).not.toContain("'IPC: terminal:ghostty:focusDiagnostics")
  })

  it('removes the old terminal:ghostty:destroySurface IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('ghosttyDestroySurface: (')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:ghostty:destroySurface'")
    expect(terminalHandlersSource).not.toContain("'terminal:ghostty:destroySurface'")
    expect(terminalHandlersSource).not.toContain("'IPC: terminal:ghostty:destroySurface")
  })

  it('removes the old terminal:ghostty:shutdown IPC invoke adapter and handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const terminalOpsSource = getPreloadTerminalOpsSource(preloadSource)
    const terminalHandlersSource = readOptionalSource('../../src/main/ipc/terminal-handlers.ts')

    expect(terminalOpsSource).not.toContain('ghosttyShutdown: (')
    expect(terminalOpsSource).not.toContain("ipcRenderer.invoke('terminal:ghostty:shutdown'")
    expect(terminalHandlersSource).not.toContain("'terminal:ghostty:shutdown'")
    expect(terminalHandlersSource).not.toContain("'IPC: terminal:ghostty:shutdown")
  })

  it('keeps Ghostty native integration desktop-only behind Electron desktop commands', () => {
    const terminalOpsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/terminal-ops.ts'),
      'utf-8'
    )
    const desktopManagerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/backend-manager.ts'),
      'utf-8'
    )
    const terminalOpsTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/terminal-ops.test.ts'),
      'utf-8'
    )

    expect(terminalOpsSource).not.toContain('ghostty-service')
    expect(terminalOpsSource).not.toContain("from 'electron'")
    expect(terminalOpsSource).toContain("const command = 'terminalGhosttyIsAvailable'")
    expect(terminalOpsSource).toContain('makeDesktopCommandRequest(id, command')
    expect(terminalOpsSource).toContain('Desktop command transport is not available')
    expect(desktopManagerSource).toContain("import { ghosttyService } from '../services/ghostty-service'")
    expect(desktopManagerSource).toContain("message.command === 'terminalGhosttyInit'")
    expect(desktopManagerSource).toContain("message.command === 'terminalGhosttyCreateSurface'")
    expect(desktopManagerSource).toContain("message.command === 'terminalGhosttyShutdown'")
    expect(terminalOpsTestSource).toContain(
      'reports Ghostty unavailable without a desktop command transport'
    )
    expect(terminalOpsTestSource).toContain('available: false')
    expect(terminalOpsTestSource).toContain('initialized: false')
  })

  it('removes the old empty terminalOps preload global exposure', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const terminalOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('terminalOps'")
    expect(preloadSource).not.toContain('window.terminalOps = terminalOps')
    expect(preloadTypesSource).not.toContain('terminalOps:')
    expect(preloadTypesSource).not.toContain('createClaudeCli: (')
    expect(preloadTypesSource).not.toContain(
      'onClaudeCliStatus: (callback: (payload: ClaudeCliStatusPayload)'
    )
    expect(preloadTypesSource).not.toContain('ghosttyFocusDiagnostics: () => Promise<')
    expect(preloadTypesSource).not.toContain('interface GhosttyTerminalConfig')
    expect(preloadTypesSource).not.toContain('interface ClaudeCliStatusPayload')
  })

  it('routes XtermBackend initial PTY resize sync through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/XtermBackend.ts'
      ),
      'utf-8'
    )
    const resizeSyncStart = source.indexOf('// Immediately sync PTY size')
    const resizeSyncEnd = source.indexOf('          } catch {', resizeSyncStart)
    const resizeSyncSource = source.slice(resizeSyncStart, resizeSyncEnd)

    expect(resizeSyncStart).toBeGreaterThan(-1)
    expect(resizeSyncEnd).toBeGreaterThan(resizeSyncStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(resizeSyncSource).toContain('this.fitAddon.fit()')
    expect(resizeSyncSource).toContain('const dims = this.fitAddon.proposeDimensions()')
    expect(resizeSyncSource).toContain(
      'terminalApi.resize(this.terminalId, dims.cols, dims.rows).then(unwrapEnvelope)'
    )
    expect(resizeSyncSource).not.toContain('window.terminalOps')
  })

  it('routes XtermBackend ResizeObserver auto-fit resize through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/XtermBackend.ts'
      ),
      'utf-8'
    )
    const observerStart = source.indexOf('// ResizeObserver for auto-fit')
    const writeStart = source.indexOf('  write(data: string): void {', observerStart)
    const observerSource = source.slice(observerStart, writeStart)

    expect(observerStart).toBeGreaterThan(-1)
    expect(writeStart).toBeGreaterThan(observerStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(observerSource).toContain('this.resizeObserver = new ResizeObserver(() => {')
    expect(observerSource).toContain('this.fitAddon.fit()')
    expect(observerSource).toContain('const dims = this.fitAddon.proposeDimensions()')
    expect(observerSource).toContain(
      'terminalApi.resize(this.terminalId, dims.cols, dims.rows).then(unwrapEnvelope)'
    )
    expect(observerSource).toContain('this.resizeObserver.observe(container)')
    expect(observerSource).not.toContain('window.terminalOps')
  })

  it('routes XtermBackend public resize through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/XtermBackend.ts'
      ),
      'utf-8'
    )
    const resizeStart = source.indexOf('  resize(cols: number, rows: number): void {')
    const focusStart = source.indexOf('  focus(): void {', resizeStart)
    const resizeSource = source.slice(resizeStart, focusStart)

    expect(resizeStart).toBeGreaterThan(-1)
    expect(focusStart).toBeGreaterThan(resizeStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(resizeSource).toContain(
      'terminalApi.resize(this.terminalId, cols, rows).then(unwrapEnvelope)'
    )
    expect(resizeSource).not.toContain('window.terminalOps')
  })

  it('routes XtermBackend fit resize through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/XtermBackend.ts'
      ),
      'utf-8'
    )
    const fitStart = source.indexOf('  /** Re-fit after visibility change */')
    const searchStart = source.indexOf('  searchOpen(): void {', fitStart)
    const fitSource = source.slice(fitStart, searchStart)

    expect(fitStart).toBeGreaterThan(-1)
    expect(searchStart).toBeGreaterThan(fitStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(fitSource).toContain('this.fitAddon?.fit()')
    expect(fitSource).toContain('const dims = this.fitAddon?.proposeDimensions()')
    expect(fitSource).toContain(
      'terminalApi.resize(this.terminalId, dims.cols, dims.rows).then(unwrapEnvelope)'
    )
    expect(fitSource).toContain('// Ignore fit errors')
    expect(fitSource).not.toContain('window.terminalOps')
  })

  it('routes Claude CLI status listener through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useClaudeCliStatusListener.ts'),
      'utf-8'
    )
    const effectStart = source.indexOf('  useEffect(() => {')
    const effectEnd = source.indexOf('\n  }, [])', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(effectSource).toContain(
      'const unsubscribe = terminalApi.onClaudeCliStatus(({ sessionId, status, metadata }) => {'
    )
    expect(effectSource).toContain("status === 'plan_ready'")
    expect(effectSource).toContain('lastSendMode.set(sessionId,')
    expect(effectSource).toContain('return unsubscribe')
    expect(effectSource).not.toContain('window.terminalOps')
  })

  it('routes Claude CLI session-id listener through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/sessions/ClaudeCliSessionView.tsx'
      ),
      'utf-8'
    )
    const effectStart = source.indexOf(
      '  useEffect(() => {',
      source.indexOf('createClaudeTerminal')
    )
    const effectEnd = source.indexOf('\n  }, [sessionId])', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(effectSource).toContain(
      'return terminalApi.onClaudeSessionId(sessionId, (claudeSessionId) => {'
    )
    expect(effectSource).toContain(
      'useSessionStore.getState().setClaudeSessionId(sessionId, claudeSessionId)'
    )
    expect(effectSource).not.toContain('window.terminalOps.onClaudeSessionId')
  })

  it('routes Claude CLI terminal creation through terminalApi in ClaudeCliSessionView', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/sessions/ClaudeCliSessionView.tsx'
      ),
      'utf-8'
    )
    const createStart = source.indexOf('  const createClaudeTerminal = useCallback(async () => {')
    const createEnd = source.indexOf('\n  }, [sessionId])', createStart)
    const createSource = source.slice(createStart, createEnd)

    expect(createStart).toBeGreaterThan(-1)
    expect(createEnd).toBeGreaterThan(createStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(createSource).toContain('const pendingPrompt =')
    expect(createSource).toContain(
      'const envelope = await terminalApi.createClaudeCli(sessionId, {'
    )
    expect(createSource).toContain('const result = unwrapEnvelope(envelope)')
    expect(createSource).toContain(
      'useSessionStore.getState().requeuePendingMessage(sessionId, pendingPrompt)'
    )
    expect(createSource).not.toContain('window.terminalOps.createClaudeCli')
  })

  it('routes BottomPanel Chrome URL opening through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/BottomPanel.tsx'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain(
      'void systemApi.openInChrome(detectedUrl, customChromeCommand || undefined)'
    )
    expect(source).not.toContain('window.systemOps.openInChrome(detectedUrl')
  })

  it('routes AttachmentButton local file path extraction through fileApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/AttachmentButton.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf(
      'const handleFileChange = (e: React.ChangeEvent<HTMLInputElement>) => {'
    )
    const handlerEnd = source.indexOf('  return (', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { fileApi } from '@/api/file-api'")
    expect(handlerSource).toContain('filePath: fileApi.getPathForFile(file)')
    expect(handlerSource).toContain("mime: file.type || 'application/octet-stream'")
    expect(handlerSource).not.toContain('window.fileOps.getPathForFile')
  })

  it('routes AppLayout dropped file path extraction through fileApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/AppLayout.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleFileDrop = useCallback((files: FileList) => {')
    const handlerEnd = source.indexOf('  const { isDragging }', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { fileApi } from '@/api/file-api'")
    expect(handlerSource).toContain('filePath: fileApi.getPathForFile(file)')
    expect(handlerSource).toContain("mime: file.type || 'application/octet-stream'")
    expect(handlerSource).not.toContain('window.fileOps.getPathForFile')
  })

  it('routes TicketAttachmentEditor file attachment path extraction through fileApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/kanban/TicketAttachmentEditor.tsx'
      ),
      'utf-8'
    )
    const handlerStart = source.indexOf(
      'const handleFileSelect = useCallback(\n    (e: React.ChangeEvent<HTMLInputElement>) => {'
    )
    const handlerEnd = source.indexOf('  return (', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { fileApi } from '@/api/file-api'")
    expect(handlerSource).toContain('const filePath = fileApi.getPathForFile(file)')
    expect(handlerSource).toContain(
      "newAttachments.push({ type: 'file' as const, url: filePath, label: file.name })"
    )
    expect(handlerSource).not.toContain('window.fileOps.getPathForFile')
  })

  it('routes fileApi getPathForFile through desktopBridge instead of fileOps globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/file-api.ts'),
      'utf-8'
    )
    const adapterStart = source.indexOf('const getFileOpsAdapter = (): FileOpsAdapter => {')
    const adapterEnd = source.indexOf('export const setFileOpsAdapterForTests', adapterStart)
    const adapterSource = source.slice(adapterStart, adapterEnd)

    expect(adapterStart).toBeGreaterThan(-1)
    expect(adapterEnd).toBeGreaterThan(adapterStart)
    expect(source).toContain("import { getDesktopBridge } from './desktop-bridge'")
    expect(adapterSource).toContain('const desktopBridge = getDesktopBridge()')
    expect(adapterSource).toContain('desktopBridge.getPathForFile.bind(desktopBridge)')
    expect(adapterSource).toContain('desktopBridge.getPathForFile is unavailable')
    expect(adapterSource).not.toContain('window.fileOps')
    expect(adapterSource).not.toContain('fileOps.getPathForFile')
  })

  it('removes the old preload fileOps global exposure after compatibility shim migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const fileOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('fileOps'")
    expect(preloadSource).not.toContain('window.fileOps = fileOps')
    expect(preloadTypesSource).not.toContain('fileOps:')
    expect(preloadTypesSource).not.toContain('readFile: (filePath: string) => Promise<')
    expect(preloadTypesSource).not.toContain(
      'writeFile: (filePath: string, content: string) => Promise<Envelope<null>>'
    )
    expect(preloadTypesSource).not.toContain('readImageAsBase64: (')
  })

  it('removes the old preload kanban global exposure after compatibility shim migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const kanban =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('kanban'")
    expect(preloadSource).not.toContain('window.kanban = kanban')
    expect(preloadTypesSource).not.toContain('kanban: Enveloped<{')
    expect(preloadTypesSource).not.toContain('simpleMode: {')
    expect(preloadTypesSource).not.toContain('openImportFile: () => Promise<{')
    expect(preloadTypesSource).not.toContain('importTickets: (')
  })

  it('removes the old empty kanban IPC handler registrar after kanban RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')

    expect(fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/kanban-handlers.ts'))).toBe(
      false
    )
    expect(mainSource).not.toContain('registerKanbanHandlers')
    expect(ipcIndexSource).not.toContain('registerKanbanHandlers')
    expect(ipcIndexSource).not.toContain('./kanban-handlers')
  })

  it('removes the old preload db global exposure after compatibility shim migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const db =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('db'")
    expect(preloadSource).not.toContain('window.db = db')
    expect(preloadTypesSource).not.toContain('db: Enveloped<{')
    expect(preloadTypesSource).not.toContain('schemaVersion: () => Promise<number>')
    expect(preloadTypesSource).not.toContain('tableExists: (tableName: string)')
    expect(preloadTypesSource).not.toContain('sessionActivity: {')
    expect(preloadTypesSource).not.toContain('diffComment: {')
  })

  it('removes the old empty database IPC handler registrar after database RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')

    expect(fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/database-handlers.ts'))).toBe(
      false
    )
    expect(mainSource).not.toContain('registerDatabaseHandlers')
    expect(ipcIndexSource).not.toContain('registerDatabaseHandlers')
    expect(ipcIndexSource).not.toContain('./database-handlers')
  })

  it('removes the old preload projectOps global exposure after compatibility shim migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const projectOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('projectOps'")
    expect(preloadSource).not.toContain('window.projectOps = projectOps')
    expect(preloadTypesSource).not.toContain('projectOps:')
    expect(preloadTypesSource).not.toContain('openDirectoryDialog: () => Promise<Envelope<string | null>>')
    expect(preloadTypesSource).not.toContain('detectSetupSuggestions: (')
    expect(preloadTypesSource).not.toContain('getAbsoluteIconDataUrl: (')
    expect(preloadTypesSource).not.toContain('../shared/types/setup-suggestions')
  })

  it('removes the old empty project IPC handler registrar after project RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')

    expect(fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/project-handlers.ts'))).toBe(
      false
    )
    expect(mainSource).not.toContain('registerProjectHandlers')
    expect(ipcIndexSource).not.toContain('registerProjectHandlers')
    expect(ipcIndexSource).not.toContain('./project-handlers')
  })

  it('removes the old preload systemOps global exposure after compatibility shim migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const systemOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('systemOps'")
    expect(preloadSource).not.toContain('window.systemOps = systemOps')
    expect(preloadTypesSource).not.toContain('systemOps:')
    expect(preloadTypesSource).not.toContain(
      'detectAgentSdks: () => Promise<{ opencode: boolean; claude: boolean; codex: boolean }>'
    )
    expect(preloadTypesSource).not.toContain('onNotificationNavigate: (')
    expect(preloadTypesSource).not.toContain('setSessionQueuedState: (sessionId: string')
  })

  it('removes the old empty system IPC handler registrar after system RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(mainSource).not.toContain('function registerSystemHandlers')
    expect(mainSource).not.toContain('registerSystemHandlers()')
    expect(mainSource).not.toContain('Notification queued-state IPC handlers migrated to RPC')
  })

  it('removes the desktop bootstrap IPC channel after preload argument migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(preloadSource).toContain('decodeLocalEnvironmentBootstrapArg(process.argv)')
    expect(mainSource).toContain(
      'additionalArguments: getDesktopPreloadBootstrapArguments(backendBootstrap)'
    )
    expect(preloadSource).not.toContain(
      'ipcRenderer.invoke(DESKTOP_GET_LOCAL_ENVIRONMENT_BOOTSTRAP_CHANNEL'
    )
    expect(preloadSource).not.toContain("ipcRenderer.invoke('desktop:getLocalEnvironmentBootstrap'")
    expect(sharedBridgeSource).not.toContain('DESKTOP_GET_LOCAL_ENVIRONMENT_BOOTSTRAP_CHANNEL')
    expect(sharedBridgeSource).not.toContain('desktop:getLocalEnvironmentBootstrap')
    expect(handlerSource).not.toContain('ipcMain.handle')
    expect(mainSource).not.toContain('registerDesktopBridgeHandlers')
  })

  it('removes the old desktop getLogDir IPC path after system RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const systemApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('DESKTOP_GET_LOG_DIR_CHANNEL')
    expect(preloadSource).not.toContain('getLogDir: (): Promise<string> =>')
    expect(sharedBridgeSource).not.toContain('desktop:getLogDir')
    expect(handlerSource).not.toContain('DESKTOP_GET_LOG_DIR_CHANNEL')
    expect(handlerSource).not.toContain('readonly getLogDir: () => Promise<string>')
    expect(handlerSource).not.toContain('handlers.getLogDir()')
    expect(desktopBridgeTypeSource).not.toContain('readonly getLogDir?:')
    expect(systemApiSource).not.toContain('bridge?.getLogDir')
    expect(systemApiSource).toContain(
      "return getRendererRpcClient().request<string>('systemOps.getLogDir', {})"
    )
  })

  it('removes the old desktop getAppVersion IPC path after system RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const systemApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/system-ops.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )
    const serverFunctionStart = serverSource.indexOf('getAppVersion: () =>')
    const serverFunctionEnd = serverSource.indexOf('  getAppPaths:', serverFunctionStart)
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(preloadSource).not.toContain('DESKTOP_GET_APP_VERSION_CHANNEL')
    expect(preloadSource).not.toContain('getAppVersion: (): Promise<string> =>')
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('getAppVersion: () => Promise<string>')
    expect(sharedBridgeSource).not.toContain('desktop:getAppVersion')
    expect(handlerSource).not.toContain('DESKTOP_GET_APP_VERSION_CHANNEL')
    expect(handlerSource).not.toContain('readonly getAppVersion: () => Promise<string>')
    expect(handlerSource).not.toContain('handlers.getAppVersion()')
    expect(desktopBridgeTypeSource).not.toContain('readonly getAppVersion?:')
    expect(systemApiSource).not.toContain('bridge?.getAppVersion')
    expect(systemApiSource).toContain(
      "return getRendererRpcClient().request<string>('systemOps.getAppVersion', {})"
    )
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(serverFunctionSource).toContain("requestDesktopCommand<string>('systemGetAppVersion')")
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('app.getVersion')
  })

  it('removes the old desktop isLogMode IPC path after system RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const systemApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/system-ops.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadSource).not.toContain('DESKTOP_IS_LOG_MODE_CHANNEL')
    expect(preloadSource).not.toContain('isLogMode: (): Promise<boolean> =>')
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('isLogMode: () => Promise<boolean>')
    expect(sharedBridgeSource).not.toContain('desktop:isLogMode')
    expect(handlerSource).not.toContain('DESKTOP_IS_LOG_MODE_CHANNEL')
    expect(handlerSource).not.toContain('readonly isLogMode: () => Promise<boolean>')
    expect(handlerSource).not.toContain('handlers.isLogMode()')
    expect(desktopBridgeTypeSource).not.toContain('readonly isLogMode?:')
    expect(systemApiSource).not.toContain('bridge?.isLogMode')
    expect(systemApiSource).toContain(
      "return getRendererRpcClient().request<boolean>('systemOps.isLogMode', {})"
    )
  })

  it('removes the old desktop getPlatform IPC path after system RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const systemApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/system-ops.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadSource).not.toContain('DESKTOP_GET_PLATFORM_CHANNEL')
    expect(preloadSource).not.toContain('getPlatform: (): Promise<string> =>')
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('getPlatform: () => Promise<string>')
    expect(sharedBridgeSource).not.toContain('desktop:getPlatform')
    expect(handlerSource).not.toContain('DESKTOP_GET_PLATFORM_CHANNEL')
    expect(handlerSource).not.toContain('readonly getPlatform: () => Promise<NodeJS.Platform>')
    expect(handlerSource).not.toContain('handlers.getPlatform()')
    expect(desktopBridgeTypeSource).not.toContain('readonly getPlatform?:')
    expect(systemApiSource).not.toContain('bridge?.getPlatform')
    expect(systemApiSource).toContain(
      "return getRendererRpcClient().request<string>('systemOps.getPlatform', {})"
    )
  })

  it('removes the old desktop getAppPaths IPC path after system RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const systemApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/system-ops.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )
    const serverFunctionStart = serverSource.indexOf('getAppPaths: () =>')
    const serverFunctionEnd = serverSource.indexOf('  isLogMode:', serverFunctionStart)
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(preloadSource).not.toContain('DESKTOP_GET_APP_PATHS_CHANNEL')
    expect(preloadSource).not.toContain(
      'getAppPaths: (): Promise<{ userData: string; home: string; logs: string }> =>'
    )
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('getAppPaths: () => Promise<{')
    expect(sharedBridgeSource).not.toContain('desktop:getAppPaths')
    expect(handlerSource).not.toContain('DESKTOP_GET_APP_PATHS_CHANNEL')
    expect(handlerSource).not.toContain('readonly getAppPaths: () => Promise<DesktopAppPaths>')
    expect(handlerSource).not.toContain('handlers.getAppPaths()')
    expect(desktopBridgeTypeSource).not.toContain('readonly getAppPaths?:')
    expect(systemApiSource).not.toContain('bridge?.getAppPaths')
    expect(systemApiSource).not.toContain("import type { DesktopAppPaths } from '@shared/desktop-bridge'")
    expect(systemApiSource).toContain("import type { SystemAppPaths } from '@shared/system-types'")
    expect(systemApiSource).toContain(
      "return getRendererRpcClient().request<SystemAppPaths>('systemOps.getAppPaths', {})"
    )
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(serverFunctionSource).toContain(
      "requestDesktopCommand<Pick<SystemAppPaths, 'userData' | 'home'>>('systemGetAppPaths')"
    )
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('app.getPath')
  })

  it('removes the old desktop quitApp IPC path after system RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const systemApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/system-ops.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )
    const serverFunctionStart = serverSource.indexOf('quitApp: () =>')
    const serverFunctionEnd = serverSource.indexOf('  confirm:', serverFunctionStart)
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(preloadSource).not.toContain('DESKTOP_QUIT_APP_CHANNEL')
    expect(preloadSource).not.toContain('quitApp: (): Promise<void> =>')
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('quitApp: () => Promise<void>')
    expect(sharedBridgeSource).not.toContain('desktop:quitApp')
    expect(handlerSource).not.toContain('DESKTOP_QUIT_APP_CHANNEL')
    expect(handlerSource).not.toContain('readonly quitApp: () => Promise<void>')
    expect(handlerSource).not.toContain('handlers.quitApp()')
    expect(desktopBridgeTypeSource).not.toContain('readonly quitApp?:')
    expect(systemApiSource).not.toContain('bridge?.quitApp')
    expect(systemApiSource).toContain(
      "return getRendererRpcClient().request<void>('systemOps.quitApp', {})"
    )
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(serverFunctionSource).toContain("requestDesktopCommand('quitApp')")
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('app.quit')
  })

  it('removes the old desktop setKeepAwake IPC path after system RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const systemApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadSource).not.toContain('DESKTOP_SET_KEEP_AWAKE_CHANNEL')
    expect(preloadSource).not.toContain('setKeepAwake: (active: boolean): Promise<void> =>')
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain(
      'setKeepAwake: (active: boolean) => Promise<void>'
    )
    expect(sharedBridgeSource).not.toContain('desktop:setKeepAwake')
    expect(handlerSource).not.toContain('DESKTOP_SET_KEEP_AWAKE_CHANNEL')
    expect(handlerSource).not.toContain('readonly setKeepAwake: (active: boolean) => Promise<void>')
    expect(handlerSource).not.toContain('handlers.setKeepAwake(')
    expect(desktopBridgeTypeSource).not.toContain('readonly setKeepAwake?:')
    expect(systemApiSource).not.toContain('bridge?.setKeepAwake')
    expect(systemApiSource).toContain(
      "return getRendererRpcClient().request<void>('systemOps.setKeepAwake', { active })"
    )
  })

  it('removes the old desktop detectAgentSdks IPC path after system RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const systemApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadSource).not.toContain('DESKTOP_DETECT_AGENT_SDKS_CHANNEL')
    expect(preloadSource).not.toContain(
      'detectAgentSdks: (): Promise<{ opencode: boolean; claude: boolean; codex: boolean }> =>'
    )
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('detectAgentSdks: () => Promise<{')
    expect(sharedBridgeSource).not.toContain('desktop:detectAgentSdks')
    expect(handlerSource).not.toContain('DESKTOP_DETECT_AGENT_SDKS_CHANNEL')
    expect(handlerSource).not.toContain(
      'readonly detectAgentSdks: () => Promise<DesktopAgentSdkDetectionResult>'
    )
    expect(handlerSource).not.toContain('handlers.detectAgentSdks()')
    expect(desktopBridgeTypeSource).not.toContain('readonly detectAgentSdks?:')
    expect(systemApiSource).not.toContain('bridge?.detectAgentSdks')
    expect(systemApiSource).toContain(
      "return getRendererRpcClient().request<AgentSdkDetectionResult>('systemOps.detectAgentSdks', {})"
    )
  })

  it('removes the old desktop updateMenuState IPC path after system RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const systemApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf('    }', preloadDesktopBridgeStart)
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadSource).not.toContain('DESKTOP_UPDATE_MENU_STATE_CHANNEL')
    expect(preloadSource).not.toContain('updateMenuState: (state: {')
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('updateMenuState: (state: {')
    expect(sharedBridgeSource).not.toContain('desktop:updateMenuState')
    expect(handlerSource).not.toContain('DESKTOP_UPDATE_MENU_STATE_CHANNEL')
    expect(handlerSource).not.toContain(
      'readonly updateMenuState: (state: DesktopMenuState) => Promise<void>'
    )
    expect(handlerSource).not.toContain('handlers.updateMenuState(')
    expect(desktopBridgeTypeSource).not.toContain('readonly updateMenuState?:')
    expect(systemApiSource).not.toContain('bridge?.updateMenuState')
    expect(systemApiSource).toContain(
      "return getRendererRpcClient().request<void>('systemOps.updateMenuState', state)"
    )
  })

  it('removes the old desktop isPackaged IPC path after system RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const systemApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/system-ops.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )
    const serverFunctionStart = serverSource.indexOf('isPackaged: () =>')
    const serverFunctionEnd = serverSource.indexOf('  getPlatform:', serverFunctionStart)
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(preloadSource).not.toContain('DESKTOP_IS_PACKAGED_CHANNEL')
    expect(preloadSource).not.toContain('isPackaged: (): Promise<boolean> =>')
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('isPackaged: () => Promise<boolean>')
    expect(sharedBridgeSource).not.toContain('desktop:isPackaged')
    expect(handlerSource).not.toContain('DESKTOP_IS_PACKAGED_CHANNEL')
    expect(handlerSource).not.toContain('readonly isPackaged: () => Promise<boolean>')
    expect(handlerSource).not.toContain('handlers.isPackaged()')
    expect(desktopBridgeTypeSource).not.toContain('readonly isPackaged?:')
    expect(systemApiSource).not.toContain('bridge?.isPackaged')
    expect(systemApiSource).not.toContain('return bridge.isPackaged()')
    expect(systemApiSource).toContain(
      "return getRendererRpcClient().request<boolean>('systemOps.isPackaged', {})"
    )
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(serverFunctionSource).toContain("requestDesktopCommand<boolean>('systemIsPackaged')")
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('app.isPackaged')
  })

  it('removes the old desktop openInApp IPC path after system RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const systemApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf('\n  }', preloadDesktopBridgeStart)
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadSource).not.toContain('DESKTOP_OPEN_IN_APP_CHANNEL')
    expect(preloadSource).not.toContain('ipcRenderer.invoke(DESKTOP_OPEN_IN_APP_CHANNEL')
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('openInApp: (appName: string, path: string)')
    expect(sharedBridgeSource).not.toContain('desktop:openInApp')
    expect(handlerSource).not.toContain('DESKTOP_OPEN_IN_APP_CHANNEL')
    expect(handlerSource).not.toContain(
      'readonly openInApp: (appName: string, path: string) => Promise<DesktopOpenInAppResult>'
    )
    expect(handlerSource).not.toContain('handlers.openInApp(')
    expect(desktopBridgeTypeSource).not.toContain('readonly openInApp?:')
    expect(systemApiSource).not.toContain('bridge?.openInApp')
    expect(systemApiSource).not.toContain('return bridge.openInApp(appName, path)')
    expect(systemApiSource).toContain(
      "return getRendererRpcClient().request<OpenInAppResult>('systemOps.openInApp', { appName, path })"
    )
  })

  it('removes the old desktop openInChrome IPC path after system RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const systemApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf('\n  }', preloadDesktopBridgeStart)
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadSource).not.toContain('DESKTOP_OPEN_IN_CHROME_CHANNEL')
    expect(preloadSource).not.toContain('ipcRenderer.invoke(DESKTOP_OPEN_IN_CHROME_CHANNEL')
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('openInChrome: (')
    expect(sharedBridgeSource).not.toContain('desktop:openInChrome')
    expect(handlerSource).not.toContain('DESKTOP_OPEN_IN_CHROME_CHANNEL')
    expect(handlerSource).not.toContain(
      'readonly openInChrome: (url: string, customCommand?: string) => Promise<DesktopOpenInChromeResult>'
    )
    expect(handlerSource).not.toContain('handlers.openInChrome(')
    expect(desktopBridgeTypeSource).not.toContain('readonly openInChrome?:')
    expect(systemApiSource).not.toContain('bridge?.openInChrome')
    expect(systemApiSource).not.toContain('return bridge.openInChrome(url, customCommand)')
    expect(systemApiSource).toContain(
      "return getRendererRpcClient().request<OpenInChromeResult>('systemOps.openInChrome', {"
    )
  })

  it('removes the old desktop openExternal IPC path after system RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const systemApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf('\n  }', preloadDesktopBridgeStart)
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadSource).not.toContain('DESKTOP_OPEN_EXTERNAL_CHANNEL')
    expect(preloadSource).not.toContain('ipcRenderer.invoke(DESKTOP_OPEN_EXTERNAL_CHANNEL')
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('openExternal: (url: string)')
    expect(sharedBridgeSource).not.toContain('desktop:openExternal')
    expect(handlerSource).not.toContain('DESKTOP_OPEN_EXTERNAL_CHANNEL')
    expect(handlerSource).not.toContain('readonly openExternal: (url: string) => Promise<boolean>')
    expect(handlerSource).not.toContain('handlers.openExternal(')
    expect(desktopBridgeTypeSource).not.toContain('readonly openExternal?:')
    expect(systemApiSource).not.toContain('bridge?.openExternal')
    expect(systemApiSource).not.toContain('bridge.openExternal(url)')
    expect(systemApiSource).toContain(
      "return getRendererRpcClient().request<OpenInChromeResult>('systemOps.openInChrome', {"
    )
  })

  it('removes the old desktop openPath IPC path after project RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const projectApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/project-api.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf('\n  }', preloadDesktopBridgeStart)
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadSource).not.toContain('DESKTOP_OPEN_PATH_CHANNEL')
    expect(preloadSource).not.toContain('ipcRenderer.invoke(DESKTOP_OPEN_PATH_CHANNEL')
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('openPath: (path: string) => Promise<string>')
    expect(sharedBridgeSource).not.toContain('desktop:openPath')
    expect(handlerSource).not.toContain('DESKTOP_OPEN_PATH_CHANNEL')
    expect(handlerSource).not.toContain('readonly openPath: (path: string) => Promise<string>')
    expect(handlerSource).not.toContain('handlers.openPath(')
    expect(desktopBridgeTypeSource).not.toContain('readonly openPath?:')
    expect(projectApiSource).not.toContain('bridge?.openPath')
    expect(projectApiSource).not.toContain('bridge.openPath(path)')
    expect(projectApiSource).toContain(
      "return getRendererRpcClient().request<void>('projectOps.openPath', { path })"
    )
  })

  it('removes the old desktop showItemInFolder IPC path after project RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const projectApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/project-api.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf('\n  }', preloadDesktopBridgeStart)
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadSource).not.toContain('DESKTOP_SHOW_ITEM_IN_FOLDER_CHANNEL')
    expect(preloadSource).not.toContain('ipcRenderer.invoke(DESKTOP_SHOW_ITEM_IN_FOLDER_CHANNEL')
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('showItemInFolder: (path: string)')
    expect(sharedBridgeSource).not.toContain('desktop:showItemInFolder')
    expect(handlerSource).not.toContain('DESKTOP_SHOW_ITEM_IN_FOLDER_CHANNEL')
    expect(handlerSource).not.toContain(
      'readonly showItemInFolder: (path: string) => Promise<void>'
    )
    expect(handlerSource).not.toContain('handlers.showItemInFolder(')
    expect(desktopBridgeTypeSource).not.toContain('readonly showItemInFolder?:')
    expect(projectApiSource).not.toContain('bridge?.showItemInFolder')
    expect(projectApiSource).not.toContain('bridge.showItemInFolder(path)')
    expect(projectApiSource).toContain(
      "return getRendererRpcClient().request<void>('projectOps.showInFolder', { path })"
    )
  })

  it('removes the old desktop writeClipboardText IPC path after project RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const projectApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/project-api.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf('\n  }', preloadDesktopBridgeStart)
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadSource).not.toContain('DESKTOP_WRITE_CLIPBOARD_TEXT_CHANNEL')
    expect(preloadSource).not.toContain('ipcRenderer.invoke(DESKTOP_WRITE_CLIPBOARD_TEXT_CHANNEL')
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('writeClipboardText: (text: string)')
    expect(sharedBridgeSource).not.toContain('desktop:writeClipboardText')
    expect(handlerSource).not.toContain('DESKTOP_WRITE_CLIPBOARD_TEXT_CHANNEL')
    expect(handlerSource).not.toContain(
      'readonly writeClipboardText: (text: string) => Promise<void>'
    )
    expect(handlerSource).not.toContain('handlers.writeClipboardText(')
    expect(desktopBridgeTypeSource).not.toContain('readonly writeClipboardText?:')
    expect(projectApiSource).not.toContain('bridge?.writeClipboardText')
    expect(projectApiSource).not.toContain('bridge.writeClipboardText(text)')
    expect(projectApiSource).toContain(
      "return getRendererRpcClient().request<void>('projectOps.copyToClipboard', { text })"
    )
  })

  it('removes the old desktop readClipboardText IPC path after project RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const projectApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/project-api.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf('\n  }', preloadDesktopBridgeStart)
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadSource).not.toContain('DESKTOP_READ_CLIPBOARD_TEXT_CHANNEL')
    expect(preloadSource).not.toContain('ipcRenderer.invoke(DESKTOP_READ_CLIPBOARD_TEXT_CHANNEL')
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('readClipboardText: () => Promise<string>')
    expect(sharedBridgeSource).not.toContain('desktop:readClipboardText')
    expect(handlerSource).not.toContain('DESKTOP_READ_CLIPBOARD_TEXT_CHANNEL')
    expect(handlerSource).not.toContain('readonly readClipboardText: () => Promise<string>')
    expect(handlerSource).not.toContain('handlers.readClipboardText(')
    expect(desktopBridgeTypeSource).not.toContain('readonly readClipboardText?:')
    expect(projectApiSource).not.toContain('bridge?.readClipboardText')
    expect(projectApiSource).not.toContain('bridge.readClipboardText()')
    expect(projectApiSource).toContain(
      "return getRendererRpcClient().request<string>('projectOps.readFromClipboard', {})"
    )
  })

  it('removes the old desktop pickProjectIcon IPC path after project RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const projectApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/project-api.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf('\n  }', preloadDesktopBridgeStart)
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadSource).not.toContain('DESKTOP_PICK_PROJECT_ICON_CHANNEL')
    expect(preloadSource).not.toContain('ipcRenderer.invoke(DESKTOP_PICK_PROJECT_ICON_CHANNEL')
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('pickProjectIcon:')
    expect(sharedBridgeSource).not.toContain('desktop:pickProjectIcon')
    expect(sharedBridgeSource).not.toContain('DesktopPickProjectIconResult')
    expect(handlerSource).not.toContain('DESKTOP_PICK_PROJECT_ICON_CHANNEL')
    expect(handlerSource).not.toContain('readonly pickProjectIcon:')
    expect(handlerSource).not.toContain('handlers.pickProjectIcon(')
    expect(desktopBridgeTypeSource).not.toContain('readonly pickProjectIcon?:')
    expect(desktopBridgeTypeSource).not.toContain('DesktopPickProjectIconResult')
    expect(projectApiSource).not.toContain('bridge?.pickProjectIcon')
    expect(projectApiSource).not.toContain('bridge.pickProjectIcon(projectId)')
    expect(projectApiSource).toContain(
      "return getRendererRpcClient().request<PickProjectIconResult>('projectOps.pickProjectIcon', {"
    )
  })

  it('removes the old desktop pickFolder IPC path after project RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const sharedBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const handlerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const projectApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/project-api.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf('\n  }', preloadDesktopBridgeStart)
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadSource).not.toContain('DESKTOP_PICK_FOLDER_CHANNEL')
    expect(preloadSource).not.toContain('ipcRenderer.invoke(DESKTOP_PICK_FOLDER_CHANNEL')
    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadDesktopBridgeSource).not.toContain('pickFolder:')
    expect(sharedBridgeSource).not.toContain('desktop:pickFolder')
    expect(sharedBridgeSource).not.toContain('DesktopPickFolderOptions')
    expect(handlerSource).not.toContain('DESKTOP_PICK_FOLDER_CHANNEL')
    expect(handlerSource).not.toContain('readonly pickFolder:')
    expect(handlerSource).not.toContain('handlers.pickFolder(')
    expect(desktopBridgeTypeSource).not.toContain('readonly pickFolder?:')
    expect(desktopBridgeTypeSource).not.toContain('DesktopPickFolderOptions')
    expect(projectApiSource).not.toContain('bridge?.pickFolder')
    expect(projectApiSource).not.toContain('bridge.pickFolder()')
    expect(projectApiSource).toContain(
      "return getRendererRpcClient().request<string | null>('projectOps.openDirectoryDialog', {})"
    )
  })

  it('routes TicketAttachmentEditor thumbnail image reads through fileApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/kanban/TicketAttachmentEditor.tsx'
      ),
      'utf-8'
    )
    const effectStart = source.indexOf("useEffect(() => {\n    if (attachment.type !== 'image')")
    const effectEnd = source.indexOf('  return (', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(source).toContain("import { fileApi } from '@/api/file-api'")
    expect(effectSource).toContain('fileApi.readImageAsBase64(attachment.url).then((envelope) => {')
    expect(effectSource).toContain('if (cancelled || !envelope.success) return')
    expect(effectSource).toContain(
      "setThumbnailSrc(`data:${mimeType ?? 'image/png'};base64,${data}`)"
    )
    expect(effectSource).not.toContain('window.fileOps.readImageAsBase64')
  })

  it('routes TicketAttachmentEditor image deletion through attachmentApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/kanban/TicketAttachmentEditor.tsx'
      ),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleRemoveAttachment = useCallback(')
    const handlerEnd = source.indexOf('  const handleFileSelect = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { attachmentApi } from '@/api/attachment-api'")
    expect(handlerSource).toContain('attachmentApi.deleteImage(removed.url).catch(() => {})')
    expect(handlerSource).toContain("removed?.type === 'image' && removed.url")
    expect(handlerSource).toContain('onChange(attachments.filter((_, i) => i !== index))')
    expect(handlerSource).not.toContain('window.attachmentOps')
  })

  it('routes TicketCreateModal orphaned image cleanup through attachmentApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/TicketCreateModal.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf(
      '  // Clean up orphaned image files when modal closes without successful creation'
    )
    const effectEnd = source.indexOf('  // Reset form when modal opens', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(source).toContain("import { attachmentApi } from '@/api/attachment-api'")
    expect(effectSource).toContain('if (!createdSuccessfully.current) {')
    expect(effectSource).toContain("if (att.type === 'image' && att.url) {")
    expect(effectSource).toContain('attachmentApi.deleteImage(att.url).catch(() => {})')
    expect(effectSource).not.toContain('window.attachmentOps')
    expect(effectSource).not.toContain('unwrapEnvelope')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('routes FileViewer primary saves through fileApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/file-viewer/FileViewer.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleSave = useCallback(')
    const handlerEnd = source.indexOf('  const handleContentChange = useCallback', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { fileApi } from '@/api/file-api'")
    expect(handlerSource).toContain(
      'const envelope = await fileApi.writeFile(filePath, newContent)'
    )
    expect(handlerSource).toContain("toast.success('File saved')")
    expect(handlerSource).toContain("toast.error('Failed to save: ' + envelope.error)")
    expect(handlerSource).not.toContain('window.fileOps.writeFile')
  })

  it('routes FileViewer unsaved-dialog saves through fileApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/file-viewer/FileViewer.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleDialogSave = useCallback(async () => {')
    const handlerEnd = source.indexOf('  const handleDialogDontSave = useCallback', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { fileApi } from '@/api/file-api'")
    expect(handlerSource).toContain(
      'const envelope = await fileApi.writeFile(pendingClose, latestContentRef.current)'
    )
    expect(handlerSource).toContain("toast.success('File saved')")
    expect(handlerSource).toContain("toast.error('Failed to save: ' + envelope.error)")
    expect(handlerSource).toContain('useFileViewerStore.getState().confirmCloseFile(pendingClose)')
    expect(handlerSource).not.toContain('window.fileOps.writeFile')
  })

  it('removes the old file:write IPC adapter after file write RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const fileHandlersSource = readOptionalSource('../../src/main/ipc/file-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('file:write'")
    expect(fileHandlersSource).not.toContain("defineHandler('file:write'")
  })

  it('routes FileViewer binary image loading through fileApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/file-viewer/FileViewer.tsx'),
      'utf-8'
    )
    const branchStart = source.indexOf('if (isBinaryImage) {')
    const finallyStart = source.indexOf('        .finally(() => {', branchStart)
    const branchEnd = source.indexOf('    } else {', finallyStart)
    const branchSource = source.slice(branchStart, branchEnd)

    expect(branchStart).toBeGreaterThan(-1)
    expect(finallyStart).toBeGreaterThan(branchStart)
    expect(branchEnd).toBeGreaterThan(branchStart)
    expect(source).toContain("import { fileApi } from '@/api/file-api'")
    expect(branchSource).toContain('fileApi')
    expect(branchSource).toContain('.readImageAsBase64(filePath)')
    expect(branchSource).toContain("const mimeType = getImageMimeType(filePath) || 'image/png'")
    expect(branchSource).toContain(
      '`data:${envelope.value.mimeType ?? mimeType};base64,${envelope.value.data}`'
    )
    expect(branchSource).toContain("setError(envelope.error || 'Failed to read image')")
    expect(branchSource).not.toContain('window.fileOps')
  })

  it('removes the old file:readImageAsBase64 IPC adapter after file image RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const fileHandlersSource = readOptionalSource('../../src/main/ipc/file-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('file:readImageAsBase64'")
    expect(fileHandlersSource).not.toContain("defineHandler('file:readImageAsBase64'")
  })

  it('routes FileViewer initial text loading through fileApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/file-viewer/FileViewer.tsx'),
      'utf-8'
    )
    const branchStart = source.indexOf('    } else {', source.indexOf('if (isBinaryImage) {'))
    const branchEnd = source.indexOf('    }\n\n    return () => {', branchStart)
    const branchSource = source.slice(branchStart, branchEnd)

    expect(branchStart).toBeGreaterThan(-1)
    expect(branchEnd).toBeGreaterThan(branchStart)
    expect(source).toContain("import { fileApi } from '@/api/file-api'")
    expect(branchSource).toContain('fileApi')
    expect(branchSource).toContain('.readFile(filePath)')
    expect(branchSource).toContain('.then(unwrapEnvelope)')
    expect(branchSource).toContain('useFileViewerStore.getState().setOriginalContent')
    expect(branchSource).toContain("setError(result.error || 'Failed to read file')")
    expect(branchSource).not.toContain('window.fileOps')
  })

  it('routes FileViewer external-change re-reads through fileApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/file-viewer/FileViewer.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('// Subscribe to file watcher for external change detection')
    const effectEnd = source.indexOf('  const handleReload = useCallback', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(source).toContain("import { fileApi } from '@/api/file-api'")
    expect(source).toContain("import { fileTreeApi } from '@/api/file-tree-api'")
    expect(effectSource).toContain('const unsubscribe = fileTreeApi.onChange((event) => {')
    expect(effectSource).toContain("e.changedPath === filePath && e.eventType === 'unlink'")
    expect(effectSource).toContain("setError('File was deleted from disk')")
    expect(effectSource).toContain('store.clearExternallyChanged(filePath)')
    expect(effectSource).toContain("e.changedPath === filePath && e.eventType === 'change'")
    expect(effectSource).toContain('// Re-read from disk and compare with original')
    expect(effectSource).toContain('fileApi')
    expect(effectSource).toContain('.readFile(filePath)')
    expect(effectSource).toContain('.then(unwrapEnvelope)')
    expect(effectSource).toContain('markExternallyChanged(filePath)')
    expect(effectSource).not.toContain('window.fileOps')
    expect(effectSource).not.toContain('window.fileTreeOps')
  })

  it('routes FileViewer reload reads through fileApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/file-viewer/FileViewer.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleReload = useCallback(async () => {')
    const handlerEnd = source.indexOf('  const handleKeepMine = useCallback', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { fileApi } from '@/api/file-api'")
    expect(handlerSource).toContain(
      'const result = unwrapEnvelope(await fileApi.readFile(filePath))'
    )
    expect(handlerSource).toContain('store.setOriginalContent(filePath, result.content)')
    expect(handlerSource).toContain('store.markClean(filePath)')
    expect(handlerSource).toContain('clearExternallyChanged(filePath)')
    expect(handlerSource).toContain('setReloadKey((k) => k + 1)')
    expect(handlerSource).not.toContain('window.fileOps.readFile')
  })

  it('routes FileViewer keep-mine reads through fileApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/file-viewer/FileViewer.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleKeepMine = useCallback(async () => {')
    const handlerEnd = source.indexOf('  // Reset view mode when file changes', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { fileApi } from '@/api/file-api'")
    expect(handlerSource).toContain('clearExternallyChanged(filePath)')
    expect(handlerSource).toContain(
      'const result = unwrapEnvelope(await fileApi.readFile(filePath))'
    )
    expect(handlerSource).toContain(
      'useFileViewerStore.getState().setOriginalContent(filePath, result.content)'
    )
    expect(handlerSource).not.toContain('window.fileOps.readFile')
  })

  it('removes the old file:read IPC adapter after file read RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const fileHandlersSource = readOptionalSource('../../src/main/ipc/file-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('file:read'")
    expect(fileHandlersSource).not.toContain("defineHandler('file:read'")
  })

  it('removes the old empty file IPC handler registrar after file RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')

    expect(fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/file-handlers.ts'))).toBe(
      false
    )
    expect(mainSource).not.toContain('registerFileHandlers')
    expect(ipcIndexSource).not.toContain('registerFileHandlers')
    expect(ipcIndexSource).not.toContain('./file-handlers')
  })

  it('routes PinnedList attachment URL opening through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/PinnedList.tsx'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('await systemApi.openInChrome(url)')
    expect(source).not.toContain('await window.systemOps.openInChrome(url)')
  })

  it('routes PinnedList attachment removal through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/PinnedList.tsx'),
      'utf-8'
    )
    const detachStart = source.indexOf('const handleDetachAttachment = useCallback(')
    const addedStart = source.indexOf('const handleAttachmentAdded = useCallback(', detachStart)
    const detachSource = source.slice(detachStart, addedStart)

    expect(detachStart).toBeGreaterThan(-1)
    expect(addedStart).toBeGreaterThan(detachStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(detachSource).toContain(
      'const result = await dbApi.worktree.removeAttachment(worktreeId, attachmentId)'
    )
    expect(detachSource).toContain(
      'setAttachments((prev) => prev.filter((a) => a.id !== attachmentId))'
    )
    expect(detachSource).toContain("toast.success('Attachment removed')")
    expect(detachSource).toContain("toast.error(result.error || 'Failed to remove attachment')")
    expect(detachSource).not.toContain('db.worktree.removeAttachment')
  })

  it('routes PinnedList attachment refresh through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/PinnedList.tsx'),
      'utf-8'
    )
    const refreshStart = source.indexOf('const handleAttachmentAdded = useCallback(')
    const renameStart = source.indexOf('const startBranchRename = useCallback(', refreshStart)
    const refreshSource = source.slice(refreshStart, renameStart)

    expect(refreshStart).toBeGreaterThan(-1)
    expect(renameStart).toBeGreaterThan(refreshStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
    expect(refreshSource).toContain(
      'dbApi.worktree.get<WorktreeAttachmentRow>(worktreeId).then((w) => {'
    )
    expect(refreshSource).toContain("setAttachments(JSON.parse(w.attachments || '[]'))")
    expect(refreshSource).not.toContain('db.worktree.get(worktreeId)')
  })

  it('routes PinnedList archive dirty check through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/PinnedList.tsx'),
      'utf-8'
    )
    const archiveStart = source.indexOf(
      '  const handleArchive = useCallback(async (): Promise<void> => {'
    )
    const confirmStart = source.indexOf('  const handleArchiveConfirm = useCallback', archiveStart)
    const archiveSource = source.slice(archiveStart, confirmStart)

    expect(archiveStart).toBeGreaterThan(-1)
    expect(confirmStart).toBeGreaterThan(archiveStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(archiveSource).toContain('const result = await gitApi.getDiffStat(worktree.path)')
    expect(archiveSource).toContain('setArchiveConfirmFiles(result.files)')
    expect(archiveSource).toContain('setArchiveConfirmOpen(true)')
    expect(archiveSource).not.toContain('window.gitOps.getDiffStat')
    expect(archiveSource).not.toContain('unwrapEnvelope')
  })

  it('routes PinnedList worktree open-in-editor through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/PinnedList.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleOpenInEditor = async (): Promise<void> => {')
    const handlerEnd = source.indexOf(
      'const handleOpenInFinder = async (): Promise<void> => {',
      handlerStart
    )
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(handlerSource).toContain('const result = await worktreeApi.openInEditor(worktree.path)')
    expect(handlerSource).toContain("toast.success('Opened in Editor')")
    expect(handlerSource).toContain("toast.error(result.error || 'Failed to open in editor'")
    expect(handlerSource).not.toContain('window.worktreeOps.openInEditor')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes PinnedList worktree open-in-terminal through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/PinnedList.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleOpenInTerminal = async (): Promise<void> => {')
    const handlerEnd = source.indexOf(
      'const handleOpenInEditor = async (): Promise<void> => {',
      handlerStart
    )
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(handlerSource).toContain(
      'const result = await worktreeApi.openInTerminal(worktree.path)'
    )
    expect(handlerSource).toContain("toast.success('Opened in Terminal')")
    expect(handlerSource).toContain("toast.error(result.error || 'Failed to open in terminal'")
    expect(handlerSource).toContain("description: 'Make sure the worktree directory exists'")
    expect(handlerSource).not.toContain('window.worktreeOps.openInTerminal')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes PinnedList worktree branch rename through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/PinnedList.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleBranchRename = useCallback(async ()')
    const handlerEnd = source.indexOf(
      'const handleDuplicate = useCallback(async (): Promise<void> => {',
      handlerStart
    )
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(handlerSource).toContain('const result = await worktreeApi.renameBranch({')
    expect(handlerSource).toContain('worktreeId: worktree.id,')
    expect(handlerSource).toContain('worktreePath: worktree.path,')
    expect(handlerSource).toContain('oldBranch: worktree.branch_name,')
    expect(handlerSource).toContain('newBranch')
    expect(handlerSource).toContain('useWorktreeStore.getState().updateWorktreeBranch')
    expect(handlerSource).toContain('toast.success(`Branch renamed to ${newBranch}`)')
    expect(handlerSource).toContain("toast.error(result.error || 'Failed to rename branch')")
    expect(handlerSource).not.toContain('window.worktreeOps.renameBranch')
    expect(handlerSource).not.toContain('unwrapEnvelope(')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('routes worktree creation through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useWorktreeStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      'createWorktree: async (projectId: string, projectPath: string, projectName: string) => {'
    )
    const actionEnd = source.indexOf('  createWorktreeFromBranch:', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(actionSource).toContain('const result = await worktreeApi.create({')
    expect(actionSource).toContain('projectId,')
    expect(actionSource).toContain('projectPath,')
    expect(actionSource).toContain('projectName')
    expect(actionSource).toContain('return { success: false, error: result.error')
    expect(actionSource).not.toContain('window.worktreeOps.create({')
    expect(actionSource).not.toContain('unwrapEnvelope(await window.worktreeOps.create')
  })

  it('routes worktree create-from-branch through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useWorktreeStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf('createWorktreeFromBranch: async (')
    const actionEnd = source.indexOf('  duplicateWorktree:', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(actionSource).toContain('const result = await worktreeApi.createFromBranch({')
    expect(actionSource).toContain('projectId,')
    expect(actionSource).toContain('projectPath,')
    expect(actionSource).toContain('projectName,')
    expect(actionSource).toContain('branchName,')
    expect(actionSource).toContain('nameHint')
    expect(actionSource).toContain('return { success: false, error: result.error')
    expect(actionSource).not.toContain('window.worktreeOps.createFromBranch')
    expect(actionSource).not.toContain('unwrapEnvelope(await window.worktreeOps.createFromBranch')
  })

  it('routes worktree duplication through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useWorktreeStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf('duplicateWorktree: async (')
    const actionEnd = source.indexOf('  updateWorktreeBranch:', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(actionSource).toContain('const result = await worktreeApi.duplicate({')
    expect(actionSource).toContain('projectId,')
    expect(actionSource).toContain('projectPath,')
    expect(actionSource).toContain('projectName,')
    expect(actionSource).toContain('sourceBranch,')
    expect(actionSource).toContain('sourceWorktreePath,')
    expect(actionSource).toContain('nameHint')
    expect(actionSource).toContain('get().loadWorktrees(projectId, { force: true })')
    expect(actionSource).toContain(
      'fireSetupScript(projectId, result.worktree!.id, result.worktree!.path)'
    )
    expect(actionSource).not.toContain('window.worktreeOps.duplicate')
    expect(actionSource).not.toContain('unwrapEnvelope(await window.worktreeOps.duplicate')
  })

  it('keeps branch duplication tests on worktreeApi and dbApi mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-7/session-3/branch-duplication.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/worktree-api'")
    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("test('duplicateWorktree calls worktreeApi.duplicate'")
    expect(source).toContain('vi.mocked(worktreeApi.duplicate).mockResolvedValue')
    expect(source).toContain('vi.mocked(dbApi.worktree.getActiveByProject).mockResolvedValue([])')
    expect(source).not.toContain('window.worktreeOps')
    expect(source).not.toContain('window.projectOps')
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('Object.defineProperty(window')
  })

  it('routes WorktreeContextEditor context loading through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/worktrees/WorktreeContextEditor.tsx'
      ),
      'utf-8'
    )
    const loadStart = source.indexOf('  // Load context on mount')
    const saveStart = source.indexOf('  const handleSave = useCallback(async () => {', loadStart)
    const loadSource = source.slice(loadStart, saveStart)

    expect(loadStart).toBeGreaterThan(-1)
    expect(saveStart).toBeGreaterThan(loadStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(loadSource).toContain('const result = await worktreeApi.getContext(worktreeId)')
    expect(loadSource).toContain("toast.error('Failed to load worktree context')")
    expect(loadSource).not.toContain('window.worktreeOps.getContext')
    expect(loadSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes WorktreeContextEditor context saving through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/worktrees/WorktreeContextEditor.tsx'
      ),
      'utf-8'
    )
    const saveStart = source.indexOf('  const handleSave = useCallback(async () => {')
    const keydownStart = source.indexOf('  // Save on Cmd+S / Ctrl+S', saveStart)
    const saveSource = source.slice(saveStart, keydownStart)

    expect(saveStart).toBeGreaterThan(-1)
    expect(keydownStart).toBeGreaterThan(saveStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(saveSource).toContain(
      'const result = await worktreeApi.updateContext(worktreeId, content || null)'
    )
    expect(saveSource).toContain("toast.success('Context saved')")
    expect(saveSource).toContain("toast.error(result.error || 'Failed to save context')")
    expect(saveSource).not.toContain('window.worktreeOps.updateContext')
    expect(saveSource).not.toContain('unwrapEnvelope(')
  })

  it('routes WorktreeContextEditor close confirmation through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/worktrees/WorktreeContextEditor.tsx'
      ),
      'utf-8'
    )
    const closeStart = source.indexOf('  const handleClose = useCallback(async () => {')
    const renderStart = source.indexOf('  if (isLoading) {', closeStart)
    const closeSource = source.slice(closeStart, renderStart)

    expect(closeStart).toBeGreaterThan(-1)
    expect(renderStart).toBeGreaterThan(closeStart)
    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(closeSource).toContain(
      "const confirmed = await systemApi.confirm('You have unsaved changes. Discard them?')"
    )
    expect(closeSource).toContain('useFileViewerStore.getState().closeContextEditor()')
    expect(closeSource).not.toContain('window.confirm')
  })

  it('removes the old desktop confirm preload IPC compatibility handler', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const desktopBridgeHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('DESKTOP_CONFIRM_CHANNEL')
    expect(preloadSource).not.toContain('ipcRenderer.invoke(DESKTOP_CONFIRM_CHANNEL')
    expect(preloadSource).not.toContain('desktop:confirm')
    expect(preloadTypesSource).not.toContain('confirm: (message: string) => Promise<boolean>')
    expect(desktopBridgeSource).not.toContain('DESKTOP_CONFIRM_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:confirm')
    expect(desktopBridgeHandlersSource).not.toContain('DESKTOP_CONFIRM_CHANNEL')
    expect(desktopBridgeHandlersSource).not.toContain('ipcMain.handle(DESKTOP_CONFIRM_CHANNEL')
  })

  it('routes systemApi confirm through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/system-ops.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('confirm: async (message: string): Promise<boolean> =>')
    const functionEnd = source.indexOf('  onNotificationNavigate:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)
    const serverFunctionStart = serverSource.indexOf('confirm: (message) =>')
    const serverFunctionEnd = serverSource.indexOf('  openInApp:', serverFunctionStart)
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).not.toContain("import { getDesktopBridge } from './desktop-bridge'")
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.confirm')
    expect(functionSource).not.toContain('bridge.confirm(message)')
    expect(functionSource).not.toContain('window.confirm(message)')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<boolean>('systemOps.confirm', { message })"
    )
    expect(functionSource).not.toContain('window.systemOps')
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(serverFunctionSource).toContain("requestDesktopCommand<boolean>('confirm', { message })")
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('BrowserWindow')
    expect(serverFunctionSource).not.toContain('dialog.showMessageBox')
  })

  it('routes setup script output subscription through scriptApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useWorktreeStore.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'export function fireSetupScript(projectId: string, worktreeId: string, cwd: string): void {'
    )
    const functionEnd = source.indexOf(
      '// Worktree type matching the database schema',
      functionStart
    )
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { scriptApi } from '@/api/script-api'")
    expect(functionSource).toContain('const channel = `script:setup:${worktreeId}`')
    expect(functionSource).toContain('const unsub = scriptApi.onOutput(channel, (event) => {')
    expect(functionSource).toContain('s.appendSetupOutput(worktreeId, `\\x00CMD:${event.command}`)')
    expect(functionSource).toContain('s.setSetupRunning(worktreeId, false)')
    expect(functionSource).toContain('unsub()')
    expect(functionSource).not.toContain('window.scriptOps.onOutput')
  })

  it('routes setup script execution through scriptApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useWorktreeStore.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'export function fireSetupScript(projectId: string, worktreeId: string, cwd: string): void {'
    )
    const functionEnd = source.indexOf(
      '// Worktree type matching the database schema',
      functionStart
    )
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { scriptApi } from '@/api/script-api'")
    expect(functionSource).toContain('scriptApi.runSetup(commands, cwd, worktreeId).catch(() => {')
    expect(functionSource).toContain('useScriptStore.getState().setSetupRunning(worktreeId, false)')
    expect(functionSource).toContain('unsub()')
    expect(functionSource).not.toContain('window.scriptOps.runSetup')
    expect(functionSource).not.toContain('.then(unwrapEnvelope)')
  })

  it('removes the old script:runSetup IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const scriptHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/script-cleanup.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('script:runSetup'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("script:runSetup"')
    expect(scriptHandlersSource).not.toContain("defineHandler('script:runSetup'")
    expect(scriptHandlersSource).not.toContain('defineHandler("script:runSetup"')
  })

  it('keeps script setup on renderer RPC and backend-published output events', () => {
    const rendererApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/script-api.ts'),
      'utf-8'
    )
    const rpcSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/script-ops.ts'),
      'utf-8'
    )
    const liveServiceTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/__tests__/script-ops.test.ts'),
      'utf-8'
    )

    expect(rendererApiSource).toContain("'scriptOps.runSetup'")
    expect(rendererApiSource).toContain('getRendererRpcClient().request<ScriptRunSetupResult>')
    expect(rpcSource).toContain("'scriptOps.runSetup'")
    expect(rpcSource).toContain('runScriptParamsSchema.parse(params)')
    expect(rpcSource).toContain('scriptRunner.runSequential(')
    expect(rpcSource).toContain('`script:setup:${worktreeId}`')
    expect(rpcSource).toContain('eventBus.publish({ channel, payload })')
    expect(liveServiceTestSource).toContain('makeLiveScriptOpsRpcService(eventBus)')
    expect(liveServiceTestSource).toContain("service.runSetup(['printf setup-output']")
    expect(liveServiceTestSource).toContain("channel = 'script:setup:worktree-setup'")
    expect(rendererApiSource).not.toContain('window.scriptOps.runSetup')
  })

  it('routes run script output subscription through scriptApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useScriptStore.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'export function fireRunScript(worktreeId: string, commands: string[], cwd: string): void {'
    )
    const functionEnd = source.indexOf(
      '/** Kill a running project script and clean up its IPC subscription. */',
      functionStart
    )
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { scriptApi } from '@/api/script-api'")
    expect(functionSource).toContain('const channel = `script:run:${worktreeId}`')
    expect(functionSource).toContain('const unsub = scriptApi.onOutput(channel, (event) => {')
    expect(functionSource).toContain('s.appendRunOutput(worktreeId, `\\x00CMD:${event.command}`)')
    expect(functionSource).toContain('runSubscriptions.set(worktreeId, unsub)')
    expect(functionSource).toContain('runSubscriptions.delete(worktreeId)')
    expect(functionSource).not.toContain('window.scriptOps.onOutput')
  })

  it('removes the old scriptOps.onOutput IPC event adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const scriptOpsSource = getPreloadScriptOpsSource(preloadSource)
    expect(scriptOpsSource).not.toContain('onOutput:')
    expect(scriptOpsSource).not.toContain('ipcRenderer.on(channel, handler)')
    expect(scriptOpsSource).not.toContain('ipcRenderer.removeListener(channel, handler)')
  })

  it('keeps script output on renderer subscriptions and backend-published WS events', () => {
    const rendererApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/script-api.ts'),
      'utf-8'
    )
    const subscriptionTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/subscriptions.test.ts'),
      'utf-8'
    )
    const scriptRunnerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/script-runner.ts'),
      'utf-8'
    )

    expect(rendererApiSource).toContain('onOutput: (channel: string')
    expect(rendererApiSource).toContain('getRendererRpcClient().subscribe(channel')
    expect(rendererApiSource).toContain('isScriptOutputEvent(event.payload)')
    expect(rendererApiSource).not.toContain('window.scriptOps.onOutput')
    expect(subscriptionTestSource).toContain(
      'delivers script output events over dynamic subscribed WebSocket channels'
    )
    expect(subscriptionTestSource).toContain("const channel = 'script:run:worktree-1'")
    expect(subscriptionTestSource).toContain("payload = { type: 'output', data: 'hello from script' }")
    expect(scriptRunnerSource).toContain('setEventPublisher(publisher: ScriptEventPublisher | null)')
    expect(scriptRunnerSource).toContain('this.eventPublisher?.(eventKey, event)')
    expect(scriptRunnerSource).not.toContain('webContents.send(eventKey')
  })

  it('keeps high-volume terminal and script output batched before publishing events', () => {
    const terminalOpsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/terminal-ops.ts'),
      'utf-8'
    )
    const terminalPtyBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/terminal-pty-bridge.ts'),
      'utf-8'
    )
    const scriptRunnerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/script-runner.ts'),
      'utf-8'
    )
    const terminalOpsTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/terminal-ops.test.ts'),
      'utf-8'
    )
    const scriptRunnerTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-5/session-10/script-runner.test.ts'),
      'utf-8'
    )

    expect(terminalOpsSource).toContain('const dataBuffers = new Map<string, string>()')
    expect(terminalOpsSource).toContain('const flushScheduled = new Set<string>()')
    expect(terminalOpsSource).toContain('dataBuffers.set(terminalId, existing ? existing + data : data)')
    expect(terminalOpsSource).toContain('setImmediate(() => {')
    expect(terminalOpsSource).toContain(
      "publishTerminalEvent(eventBus, `terminal:data:${terminalId}`, buffered)"
    )
    expect(terminalOpsSource).not.toContain(
      'publishTerminalEvent(eventBus, `terminal:data:${terminalId}`, data)'
    )

    expect(terminalPtyBridgeSource).toContain('const dataBuffers = new Map<string, string>()')
    expect(terminalPtyBridgeSource).toContain('const flushScheduled = new Set<string>()')
    expect(terminalPtyBridgeSource).toContain(
      'dataBuffers.set(terminalId, existing ? existing + data : data)'
    )
    expect(terminalPtyBridgeSource).toContain('setImmediate(() => {')
    expect(terminalPtyBridgeSource).toContain(
      'publishDesktopBackendEvent(`terminal:data:${terminalId}`, buffered)'
    )
    expect(terminalPtyBridgeSource).not.toContain(
      'publishDesktopBackendEvent(`terminal:data:${terminalId}`, data)'
    )

    expect(scriptRunnerSource).toContain('private outputBuffers: Map<string, string> = new Map()')
    expect(scriptRunnerSource).toContain(
      'private outputFlushTimers: Map<string, NodeJS.Timeout> = new Map()'
    )
    expect(scriptRunnerSource).toContain('private static readonly OUTPUT_FLUSH_MS = 16')
    expect(scriptRunnerSource).toContain('this.outputBuffers.set(eventKey, existing ? existing + data : data)')
    expect(scriptRunnerSource).toContain('this.scheduleOutputFlush(eventKey)')
    expect(scriptRunnerSource).toContain("this.sendEvent(eventKey, { type: 'output', data: buffered })")
    expect(scriptRunnerSource).toContain('this.queueOutput(eventKey, chunk.toString())')

    expect(terminalOpsTestSource).toContain(
      "it('creates backend-owned PTYs and publishes buffered data and exit events'"
    )
    expect(terminalOpsTestSource).toContain(
      "{ channel: 'terminal:data:terminal-1', payload: 'hello' }"
    )
    expect(scriptRunnerTestSource).toContain(
      "test('runPersistent batches output chunks into fewer backend events'"
    )
    expect(scriptRunnerTestSource).toContain("data: 'ABC'")
  })

  it('removes the script-runner renderer IPC output sender after backend event migration', () => {
    const scriptRunnerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/script-runner.ts'),
      'utf-8'
    )
    const scriptCleanupSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/script-cleanup.ts'),
      'utf-8'
    )
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')

    expect(scriptRunnerSource).not.toContain('webContents.send(eventKey')
    expect(scriptRunnerSource).not.toContain('mainWindow.webContents.send(eventKey')
    expect(scriptRunnerSource).not.toContain('BrowserWindow')
    expect(scriptRunnerSource).not.toContain('setMainWindow')
    expect(
      fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/script-handlers.ts'))
    ).toBe(false)
    expect(scriptCleanupSource).toContain('export function cleanupScripts')
    expect(mainSource).toContain("from './services/script-cleanup'")
    expect(mainSource).not.toContain('registerScriptHandlers')
    expect(mainSource).not.toContain('Registering Script handlers')
    expect(ipcIndexSource).not.toContain('registerScriptHandlers')
    expect(ipcIndexSource).not.toContain('script-handlers')
  })

  it('removes the old scriptOps.offOutput IPC event adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const scriptOpsSource = getPreloadScriptOpsSource(preloadSource)
    expect(scriptOpsSource).not.toContain('offOutput:')
    expect(scriptOpsSource).not.toContain('ipcRenderer.removeAllListeners(channel)')
  })

  it('removes the old empty scriptOps preload global exposure', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const scriptOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('scriptOps'")
    expect(preloadSource).not.toContain('window.scriptOps = scriptOps')
    expect(preloadTypesSource).not.toContain('scriptOps:')
    expect(preloadTypesSource).not.toContain('runSetup: (')
    expect(preloadTypesSource).not.toContain('killPid: (pid: number)')
    expect(preloadTypesSource).not.toContain('onOutput: (channel: string')
    expect(preloadTypesSource).not.toContain('interface ScriptOutputEvent')
  })

  it('routes run script execution through scriptApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useScriptStore.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'export function fireRunScript(worktreeId: string, commands: string[], cwd: string): void {'
    )
    const functionEnd = source.indexOf(
      '/** Kill a running project script and clean up its IPC subscription. */',
      functionStart
    )
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { scriptApi } from '@/api/script-api'")
    expect(functionSource).toContain('scriptApi')
    expect(functionSource).toContain('.runProject(commands, cwd, worktreeId)')
    expect(functionSource).toContain('useScriptStore.getState().setRunPid(worktreeId, result.pid)')
    expect(functionSource).toContain('useScriptStore.getState().setRunRunning(worktreeId, false)')
    expect(functionSource).toContain('runSubscriptions.delete(worktreeId)')
    expect(functionSource).not.toContain('window.scriptOps.runProject')
    expect(functionSource).not.toContain('unwrapEnvelope(envelope)')
  })

  it('keeps script run on renderer RPC and backend-published output events', () => {
    const rendererApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/script-api.ts'),
      'utf-8'
    )
    const rpcSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/script-ops.ts'),
      'utf-8'
    )
    const liveServiceTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/__tests__/script-ops.test.ts'),
      'utf-8'
    )

    expect(rendererApiSource).toContain("'scriptOps.runProject'")
    expect(rendererApiSource).toContain('getRendererRpcClient().request<ScriptRunProjectResult>')
    expect(rpcSource).toContain("'scriptOps.runProject'")
    expect(rpcSource).toContain('runScriptParamsSchema.parse(params)')
    expect(rpcSource).toContain('scriptRunner.runPersistent(')
    expect(rpcSource).toContain('`script:run:${worktreeId}`')
    expect(rpcSource).toContain('return { success: true, pid: handle.pid }')
    expect(liveServiceTestSource).toContain('makeLiveScriptOpsRpcService(eventBus)')
    expect(liveServiceTestSource).toContain("service.runProject(['printf run-output; sleep 5']")
    expect(liveServiceTestSource).toContain("channel = 'script:run:worktree-run'")
    expect(rendererApiSource).not.toContain('window.scriptOps.runProject')
  })

  it('removes the old script:runProject IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const scriptHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/script-cleanup.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('script:runProject'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("script:runProject"')
    expect(scriptHandlersSource).not.toContain("defineHandler('script:runProject'")
    expect(scriptHandlersSource).not.toContain('defineHandler("script:runProject"')
  })

  it('routes run script kill through scriptApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useScriptStore.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'export async function killRunScript(worktreeId: string): Promise<void> {'
    )
    const functionSource = source.slice(functionStart)

    expect(functionStart).toBeGreaterThan(-1)
    expect(source).toContain("import { scriptApi } from '@/api/script-api'")
    expect(functionSource).toContain('await scriptApi.kill(worktreeId)')
    expect(functionSource).toContain('useScriptStore.getState().setRunRunning(worktreeId, false)')
    expect(functionSource).toContain('useScriptStore.getState().setRunPid(worktreeId, null)')
    expect(functionSource).toContain('runSubscriptions.delete(worktreeId)')
    expect(functionSource).not.toContain('window.scriptOps.kill')
    expect(functionSource).not.toContain('unwrapEnvelope(await window.scriptOps.kill')
  })

  it('removes the old script:kill IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const scriptHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/script-cleanup.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('script:kill'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("script:kill"')
    expect(scriptHandlersSource).not.toContain("defineHandler('script:kill'")
    expect(scriptHandlersSource).not.toContain('defineHandler("script:kill"')
  })

  it('keeps script kill on renderer RPC and backend-owned process cleanup', () => {
    const rendererApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/script-api.ts'),
      'utf-8'
    )
    const rpcSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/script-ops.ts'),
      'utf-8'
    )
    const liveServiceTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/__tests__/script-ops.test.ts'),
      'utf-8'
    )

    expect(rendererApiSource).toContain("'scriptOps.kill'")
    expect(rendererApiSource).toContain('getRendererRpcClient().request<ScriptKillResult>')
    expect(rpcSource).toContain("'scriptOps.kill'")
    expect(rpcSource).toContain('killParamsSchema.parse(params)')
    expect(rpcSource).toContain('scriptRunner.killProcess(`script:run:${worktreeId}`)')
    expect(rpcSource).toContain('return requestDesktopKillScript(worktreeId)')
    expect(liveServiceTestSource).toContain(
      'kills backend-owned run scripts before using the desktop fallback'
    )
    expect(liveServiceTestSource).toContain("service.runProject(['sleep 30']")
    expect(liveServiceTestSource).toContain("service.kill('worktree-kill')")
    expect(liveServiceTestSource).toContain('expect(desktopSend).not.toHaveBeenCalled()')
    expect(rendererApiSource).not.toContain('window.scriptOps.kill')
  })

  it('removes the old script:runArchive IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const scriptHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/script-cleanup.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('script:runArchive'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("script:runArchive"')
    expect(scriptHandlersSource).not.toContain("defineHandler('script:runArchive'")
    expect(scriptHandlersSource).not.toContain('defineHandler("script:runArchive"')
  })

  it('keeps script archive on the backend Effect RPC domain', () => {
    const rpcSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/script-ops.ts'),
      'utf-8'
    )
    const liveServiceTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/__tests__/script-ops.test.ts'),
      'utf-8'
    )

    expect(rpcSource).toContain("'scriptOps.runArchive'")
    expect(rpcSource).toContain('runArchiveParamsSchema.parse(params)')
    expect(rpcSource).toContain('scriptRunner.runAndWait(commands, cwd, 30000)')
    expect(rpcSource).toContain("telemetryService.track('script_run', { type: 'archive' })")
    expect(liveServiceTestSource).toContain('service.runArchive(')
    expect(liveServiceTestSource).toContain("'printf archive-output'")
    expect(liveServiceTestSource).toContain("output: 'archive-output\\narchive-error\\n'")
  })

  it('routes run suggestion killPid action through scriptApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/RunSuggestionBanner.tsx'),
      'utf-8'
    )
    const functionStart = source.indexOf('const handleAction = async (): Promise<void> => {')
    const functionEnd = source.indexOf('\n  return (', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { scriptApi } from '@/api/script-api'")
    expect(functionSource).toContain(
      'const result = await scriptApi.killPid(suggestion.action.pid)'
    )
    expect(functionSource).toContain('dismissSuggestion(worktreeId)')
    expect(functionSource).toContain('setError(humanReason(result.reason))')
    expect(functionSource).not.toContain('window.scriptOps.killPid')
    expect(functionSource).not.toContain('unwrapEnvelope(')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('removes the old script:killPid IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const scriptHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/script-cleanup.ts'),
      'utf-8'
    )

    expect(
      fs.existsSync(
        path.resolve(__dirname, '../../src/main/ipc/__tests__/script-handlers.killPid.test.ts')
      )
    ).toBe(false)
    expect(
      fs.existsSync(
        path.resolve(
          __dirname,
          '../../src/server/rpc/domains/__tests__/script-ops.kill-pid-ipc-cleanup.test.ts'
        )
      )
    ).toBe(true)
    expect(preloadSource).not.toContain("'script:killPid'")
    expect(scriptHandlersSource).not.toContain("defineHandler('script:killPid'")
    expect(scriptHandlersSource).not.toContain("scriptFailed('script:killPid'")
  })

  it('routes run tab assigned-port loading through scriptApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/RunTab.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('// Fetch assigned port when worktree changes')
    const effectEnd = source.indexOf('\n  const handleRun = useCallback', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(source).toContain("import { scriptApi } from '@/api/script-api'")
    expect(effectSource).toContain('scriptApi')
    expect(effectSource).toContain('.getPort(cwd)')
    expect(effectSource).toContain('.then(({ port }) => setAssignedPort(port))')
    expect(effectSource).toContain('.catch(() => setAssignedPort(null))')
    expect(effectSource).not.toContain('window.scriptOps.getPort')
    expect(effectSource).not.toContain('unwrapEnvelope')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('removes the old port:get IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const scriptHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/script-cleanup.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('port:get'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("port:get"')
    expect(scriptHandlersSource).not.toContain("defineHandler('port:get'")
    expect(scriptHandlersSource).not.toContain('defineHandler("port:get"')
  })

  it('routes file tree loading through fileTreeApi scan', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useFileTreeStore.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('loadFileTree: async (worktreePath: string) => {')
    const functionEnd = source.indexOf(
      '// Load full flat file index for a worktree (for search / @ mentions)',
      functionStart
    )
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { fileTreeApi } from '@/api/file-tree-api'")
    expect(functionSource).toContain('const result = await fileTreeApi.scan(worktreePath)')
    expect(functionSource).toContain("error: result.error || 'Failed to load file tree'")
    expect(functionSource).toContain('newMap.set(worktreePath, result.tree!)')
    expect(functionSource).not.toContain('window.fileTreeOps.scan(worktreePath)')
    expect(functionSource).not.toContain('unwrapEnvelope(await window.fileTreeOps.scan')
  })

  it('removes the old file-tree:scan IPC adapter after file tree scan RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const fileTreeHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/file-tree-watcher.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('file-tree:scan'")
    expect(fileTreeHandlersSource).not.toContain("defineHandler('file-tree:scan',")
    expect(fileTreeHandlersSource).not.toContain("fileTreeFailed('file-tree:scan',")
  })

  it('routes file index loading through fileTreeApi scanFlat', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useFileTreeStore.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('loadFileIndex: async (worktreePath: string) => {')
    const functionEnd = source.indexOf('// Lazy load children for a directory', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { fileTreeApi } from '@/api/file-tree-api'")
    expect(functionSource).toContain('const result = await fileTreeApi.scanFlat(worktreePath)')
    expect(functionSource).toContain('indexMap.set(worktreePath, result.files)')
    expect(functionSource).toContain('loadingMap.set(worktreePath, false)')
    expect(functionSource).toContain('get().startWatching(worktreePath)')
    expect(functionSource).not.toContain('window.fileTreeOps.scanFlat(worktreePath)')
    expect(functionSource).not.toContain('unwrapEnvelope(await window.fileTreeOps.scanFlat')
  })

  it('removes the old file-tree:scan-flat IPC adapter after file index RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const fileTreeHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/file-tree-watcher.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('file-tree:scan-flat'")
    expect(fileTreeHandlersSource).not.toContain("defineHandler('file-tree:scan-flat',")
    expect(fileTreeHandlersSource).not.toContain("fileTreeFailed('file-tree:scan-flat',")
  })

  it('routes file tree lazy children loading through fileTreeApi loadChildren', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useFileTreeStore.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'loadChildren: async (worktreePath: string, dirPath: string) => {'
    )
    const functionEnd = source.indexOf('// Set expanded paths', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { fileTreeApi } from '@/api/file-tree-api'")
    expect(functionSource).toContain(
      'const result = await fileTreeApi.loadChildren(dirPath, worktreePath)'
    )
    expect(functionSource).toContain('if (!result.success || !result.children)')
    expect(functionSource).toContain('return { ...node, children: result.children }')
    expect(functionSource).toContain('newMap.set(worktreePath, updateChildren(tree))')
    expect(functionSource).not.toContain('window.fileTreeOps.loadChildren')
    expect(functionSource).not.toContain('unwrapEnvelope(')
  })

  it('removes the old file-tree:loadChildren IPC adapter after lazy children RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const fileTreeHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/file-tree-watcher.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('file-tree:loadChildren'")
    expect(fileTreeHandlersSource).not.toContain("defineHandler('file-tree:loadChildren',")
    expect(fileTreeHandlersSource).not.toContain("fileTreeFailed('file-tree:loadChildren',")
  })

  it('routes file tree watcher start through fileTreeApi watch', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useFileTreeStore.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('startWatching: async (worktreePath: string) => {')
    const functionEnd = source.indexOf(
      '// Stop watching for file changes + unsubscribe',
      functionStart
    )
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { fileTreeApi } from '@/api/file-tree-api'")
    expect(functionSource).toContain('await fileTreeApi.watch(worktreePath)')
    expect(functionSource).toContain('existing.refCount++')
    expect(functionSource).toContain('const unsubscribe = fileTreeApi.onChange((event) => {')
    expect(functionSource).toContain(
      'watchSubscriptions.set(worktreePath, { unsubscribe, refCount: 1 })'
    )
    expect(functionSource).not.toContain('window.fileTreeOps.watch(worktreePath)')
    expect(functionSource).not.toContain('unwrapEnvelope(await window.fileTreeOps.watch')
  })

  it('removes the old file-tree:watch IPC adapter after watcher start RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const fileTreeHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/file-tree-watcher.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('file-tree:watch'")
    expect(fileTreeHandlersSource).not.toContain("defineHandler('file-tree:watch',")
    expect(fileTreeHandlersSource).not.toContain("fileTreeFailed('file-tree:watch',")
  })

  it('routes file tree change subscription through fileTreeApi onChange', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useFileTreeStore.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('startWatching: async (worktreePath: string) => {')
    const functionEnd = source.indexOf(
      '// Stop watching for file changes + unsubscribe',
      functionStart
    )
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { fileTreeApi } from '@/api/file-tree-api'")
    expect(functionSource).toContain('const unsubscribe = fileTreeApi.onChange((event) => {')
    expect(functionSource).toContain('if (event.worktreePath === worktreePath)')
    expect(functionSource).toContain('get().handleFileChange(worktreePath, event.events)')
    expect(functionSource).toContain(
      'watchSubscriptions.set(worktreePath, { unsubscribe, refCount: 1 })'
    )
    expect(functionSource).not.toContain('window.fileTreeOps.onChange')
  })

  it('removes the old file-tree:change IPC event adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const fileTreeHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/file-tree-watcher.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.on('file-tree:change'")
    expect(preloadSource).not.toContain('ipcRenderer.on("file-tree:change"')
    expect(preloadSource).not.toContain("ipcRenderer.removeListener('file-tree:change'")
    expect(preloadSource).not.toContain('ipcRenderer.removeListener("file-tree:change"')
    expect(fileTreeHandlersSource).not.toContain('webContents.send(FILE_TREE_CHANGE_CHANNEL')
    expect(fileTreeHandlersSource).not.toContain("webContents.send('file-tree:change'")
    expect(fileTreeHandlersSource).not.toContain('webContents.send("file-tree:change"')
  })

  it('routes file tree watcher stop through fileTreeApi unwatch', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useFileTreeStore.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('stopWatching: async (worktreePath: string) => {')
    const functionEnd = source.indexOf(
      '// Handle batched file change events from watcher',
      functionStart
    )
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { fileTreeApi } from '@/api/file-tree-api'")
    expect(functionSource).toContain('entry.unsubscribe()')
    expect(functionSource).toContain('watchSubscriptions.delete(worktreePath)')
    expect(functionSource).toContain('await fileTreeApi.unwatch(worktreePath)')
    expect(functionSource).not.toContain('window.fileTreeOps.unwatch')
    expect(functionSource).not.toContain('unwrapEnvelope(await window.fileTreeOps.unwatch')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('removes the old file-tree:unwatch IPC adapter after watcher stop RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const fileTreeHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/file-tree-watcher.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('file-tree:unwatch'")
    expect(fileTreeHandlersSource).not.toContain("defineHandler('file-tree:unwatch',")
    expect(fileTreeHandlersSource).not.toContain("fileTreeFailed('file-tree:unwatch',")
  })

  it('removes the old empty fileTreeOps preload global exposure', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const fileTreeOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('fileTreeOps'")
    expect(preloadSource).not.toContain('window.fileTreeOps = fileTreeOps')
    expect(preloadTypesSource).not.toContain('fileTreeOps:')
    expect(preloadTypesSource).not.toContain('scanFlat: (dirPath: string) => Promise<')
    expect(preloadTypesSource).not.toContain('loadChildren: (')
    expect(preloadTypesSource).not.toContain('onChange: (callback: (event: FileTreeChangeEvent)')
    expect(preloadTypesSource).not.toContain('interface FileTreeNode')
    expect(preloadTypesSource).not.toContain('interface FileTreeChangeEvent')
  })

  it('keeps file tree foundation tests on fileTreeApi mocks instead of window ops globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-2/session-1/file-tree-foundation.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('../../../src/renderer/src/api/file-tree-api'")
    expect(source).toContain('const mockFileTreeApi = vi.mocked(fileTreeApi)')
    expect(source).not.toContain('window.fileTreeOps')
    expect(source).not.toContain('window.worktreeOps')
    expect(source).not.toContain('global.window')
  })

  it('keeps hidden files scanner tests free of Electron IPC mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-9/session-2/hidden-files.test.ts'),
      'utf-8'
    )

    expect(source).toContain("from '../../../src/shared/file-tree-scan'")
    expect(source).not.toContain("vi.mock('electron'")
    expect(source).not.toContain('ipcMain')
    expect(source).not.toContain('ipcRenderer')
    expect(source).not.toContain('BrowserWindow')
  })

  it('removes the old empty file tree handler startup registration after file tree RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')
    const backendManagerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/backend-manager.ts'),
      'utf-8'
    )
    const fileTreeWatcherSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/file-tree-watcher.ts'),
      'utf-8'
    )
    const fileTreeRpcSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/file-tree-ops.ts'),
      'utf-8'
    )

    expect(
      fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/file-tree-handlers.ts'))
    ).toBe(false)
    expect(
      fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/file-tree-handlers.test.ts'))
    ).toBe(false)
    expect(
      fs.existsSync(path.resolve(__dirname, '../../src/main/services/file-tree-watcher.test.ts'))
    ).toBe(true)
    expect(fileTreeWatcherSource).toContain('export function startFileTreeWatcher')
    expect(fileTreeWatcherSource).toContain('export async function stopFileTreeWatcher')
    expect(fileTreeWatcherSource).toContain('export async function cleanupFileTreeWatchers')
    expect(fileTreeWatcherSource).toContain('export function getFileTreeWatcherCount')
    expect(mainSource).toContain("from './services/file-tree-watcher'")
    expect(backendManagerSource).toContain("from '../services/file-tree-watcher'")
    expect(backendManagerSource).not.toContain("from '../ipc/file-tree-handlers'")
    expect(fileTreeRpcSource).toContain('main/services/file-tree-watcher')
    expect(fileTreeRpcSource).not.toContain('main/ipc/file-tree-handlers')
    expect(mainSource).not.toContain('registerFileTreeHandlers')
    expect(mainSource).not.toContain('Registering FileTree handlers')
    expect(ipcIndexSource).not.toContain('registerFileTreeHandlers')
    expect(ipcIndexSource).not.toContain('file-tree-handlers')
  })

  it('routes PinnedList worktree show-in-folder through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/PinnedList.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleOpenInFinder = async (): Promise<void> => {')
    const handlerEnd = source.indexOf(
      'const handleCopyPath = async (): Promise<void> => {',
      handlerStart
    )
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('await projectApi.showInFolder(worktree.path)')
    expect(handlerSource).not.toContain('window.projectOps.showInFolder')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes PinnedList worktree path copy through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/PinnedList.tsx'),
      'utf-8'
    )
    const finderStart = source.indexOf('const handleOpenInFinder = async (): Promise<void> => {')
    const handlerStart = source.indexOf(
      'const handleCopyPath = async (): Promise<void> => {',
      finderStart
    )
    const handlerEnd = source.indexOf(
      'const handleUnpin = async (): Promise<void> => {',
      handlerStart
    )
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('await projectApi.copyToClipboard(worktree.path)')
    expect(handlerSource).toContain("clipboardToast.copied('Path')")
    expect(handlerSource).not.toContain('window.projectOps.copyToClipboard')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes PinnedList connection show-in-folder through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/PinnedList.tsx'),
      'utf-8'
    )
    const worktreeHandlerStart = source.indexOf(
      'const handleOpenInFinder = async (): Promise<void> => {'
    )
    const handlerStart = source.indexOf(
      'const handleOpenInFinder = useCallback(async (): Promise<void> => {',
      worktreeHandlerStart + 1
    )
    const handlerEnd = source.indexOf(
      'const handleCopyPath = useCallback(async (): Promise<void> => {',
      handlerStart
    )
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('await projectApi.showInFolder(connection.path)')
    expect(handlerSource).not.toContain('window.projectOps.showInFolder')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes PinnedList connection path copy through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/PinnedList.tsx'),
      'utf-8'
    )
    const worktreeHandlerStart = source.indexOf(
      'const handleCopyPath = async (): Promise<void> => {'
    )
    const handlerStart = source.indexOf(
      'const handleCopyPath = useCallback(async (): Promise<void> => {',
      worktreeHandlerStart + 1
    )
    const handlerEnd = source.indexOf('const handleUnpin = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('await projectApi.copyToClipboard(connection.path)')
    expect(handlerSource).toContain("clipboardToast.copied('Path')")
    expect(handlerSource).not.toContain('window.projectOps.copyToClipboard')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes PinnedList connection open-in-terminal through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/PinnedList.tsx'),
      'utf-8'
    )
    const worktreeHandlerStart = source.indexOf(
      'const handleOpenInTerminal = async (): Promise<void> => {'
    )
    const handlerStart = source.indexOf(
      'const handleOpenInTerminal = useCallback(async (): Promise<void> => {',
      worktreeHandlerStart + 1
    )
    const handlerEnd = source.indexOf(
      'const handleOpenInEditor = useCallback(async (): Promise<void> => {',
      handlerStart
    )
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(worktreeHandlerStart).toBeGreaterThan(-1)
    expect(handlerStart).toBeGreaterThan(worktreeHandlerStart)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(handlerSource).toContain(
      'const result = await connectionApi.openInTerminal(connection.path)'
    )
    expect(handlerSource).toContain("toast.success('Opened in Terminal')")
    expect(handlerSource).toContain("toast.error(result.error || 'Failed to open in terminal')")
    expect(handlerSource).not.toContain('window.connectionOps.openInTerminal')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('removes the old connection:openInTerminal IPC adapter after connection terminal RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const connectionHandlersSource = readOptionalSource('../../src/main/ipc/connection-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('connection:openInTerminal'")
    expect(connectionHandlersSource).not.toContain("defineHandler('connection:openInTerminal'")
    expect(connectionHandlersSource).not.toContain("connectionFailed('connection:openInTerminal'")
  })

  it('routes WorktreeItem attachment URL opening through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/worktrees/WorktreeItem.tsx'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('await systemApi.openInChrome(url)')
    expect(source).not.toContain('await window.systemOps.openInChrome(url)')
  })

  it('routes WorktreeItem worktree open-in-editor through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/worktrees/WorktreeItem.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleOpenInEditor = useCallback(')
    const handlerEnd = source.indexOf(
      'const handleOpenInFinder = async (): Promise<void> => {',
      handlerStart
    )
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(handlerSource).toContain('const result = await worktreeApi.openInEditor(worktree.path)')
    expect(handlerSource).toContain("toast.success('Opened in Editor')")
    expect(handlerSource).toContain("toast.error(result.error || 'Failed to open in editor'")
    expect(handlerSource).toContain('retry: handleOpenInEditor')
    expect(handlerSource).toContain("description: 'Make sure VS Code is installed'")
    expect(handlerSource).not.toContain('window.worktreeOps.openInEditor')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes WorktreeItem worktree open-in-terminal through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/worktrees/WorktreeItem.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleOpenInTerminal = useCallback(')
    const handlerEnd = source.indexOf('const handleOpenInEditor = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(handlerSource).toContain(
      'const result = await worktreeApi.openInTerminal(worktree.path)'
    )
    expect(handlerSource).toContain("toast.success('Opened in Terminal')")
    expect(handlerSource).toContain("toast.error(result.error || 'Failed to open in terminal'")
    expect(handlerSource).toContain('retry: handleOpenInTerminal')
    expect(handlerSource).toContain("description: 'Make sure the worktree directory exists'")
    expect(handlerSource).not.toContain('window.worktreeOps.openInTerminal')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes WorktreeItem worktree branch rename through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/worktrees/WorktreeItem.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleBranchRename = useCallback(async ()')
    const handlerEnd = source.indexOf('const handleClick = (): void => {', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(handlerSource).toContain('const result = await worktreeApi.renameBranch({')
    expect(handlerSource).toContain('worktreeId: worktree.id,')
    expect(handlerSource).toContain('worktreePath: worktree.path,')
    expect(handlerSource).toContain('oldBranch: worktree.branch_name,')
    expect(handlerSource).toContain('newBranch')
    expect(handlerSource).toContain('useWorktreeStore.getState().updateWorktreeBranch')
    expect(handlerSource).toContain('toast.success(`Branch renamed to ${newBranch}`)')
    expect(handlerSource).toContain("toast.error(result.error || 'Failed to rename branch')")
    expect(handlerSource).not.toContain('window.worktreeOps.renameBranch')
    expect(handlerSource).not.toContain('unwrapEnvelope(')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('routes WorktreeItem worktree show-in-folder through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/worktrees/WorktreeItem.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleOpenInFinder = async (): Promise<void> => {')
    const handlerEnd = source.indexOf(
      'const handleCopyPath = async (): Promise<void> => {',
      handlerStart
    )
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('await projectApi.showInFolder(worktree.path)')
    expect(handlerSource).not.toContain('window.projectOps.showInFolder')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes WorktreeItem path copy through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/worktrees/WorktreeItem.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleCopyPath = async (): Promise<void> => {')
    const handlerEnd = source.indexOf('const doArchive = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('await projectApi.copyToClipboard(worktree.path)')
    expect(handlerSource).toContain("clipboardToast.copied('Path')")
    expect(handlerSource).not.toContain('window.projectOps.copyToClipboard')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes WorktreeItem archive dirty check through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/worktrees/WorktreeItem.tsx'),
      'utf-8'
    )
    const archiveStart = source.indexOf(
      'const handleArchive = useCallback(async (): Promise<void> => {'
    )
    const confirmStart = source.indexOf('const handleArchiveConfirm = useCallback', archiveStart)
    const archiveSource = source.slice(archiveStart, confirmStart)

    expect(archiveStart).toBeGreaterThan(-1)
    expect(confirmStart).toBeGreaterThan(archiveStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(archiveSource).toContain('const result = await gitApi.getDiffStat(worktree.path)')
    expect(archiveSource).toContain('setArchiveConfirmFiles(result.files)')
    expect(archiveSource).toContain('setArchiveConfirmOpen(true)')
    expect(archiveSource).not.toContain('window.gitOps.getDiffStat')
    expect(archiveSource).not.toContain('unwrapEnvelope')
  })

  it('routes ProjectItem project show-in-folder through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/projects/ProjectItem.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleOpenInFinder = async (): Promise<void> => {')
    const handlerEnd = source.indexOf(
      'const handleCopyPath = async (): Promise<void> => {',
      handlerStart
    )
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('await projectApi.showInFolder(project.path)')
    expect(handlerSource).not.toContain('window.projectOps.showInFolder')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes ProjectItem path copy through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/projects/ProjectItem.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleCopyPath = async (): Promise<void> => {')
    const handlerEnd = source.indexOf(
      'const handleRefreshProject = async (): Promise<void> => {',
      handlerStart
    )
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('await projectApi.copyToClipboard(project.path)')
    expect(handlerSource).toContain("toast.success('Path copied to clipboard')")
    expect(handlerSource).not.toContain('window.projectOps.copyToClipboard')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes ProjectItem pre-create commit check through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/projects/ProjectItem.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const doCreateWorktree = useCallback(async ()')
    const handlerEnd = source.indexOf('const handleBranchSelect = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(handlerSource).toContain('const hasCommits = await worktreeApi.hasCommits(project.path)')
    expect(handlerSource).toContain('setNoCommitsDialogOpen(true)')
    expect(handlerSource).toContain("toast.loading('Creating worktree...')")
    expect(handlerSource).not.toContain('window.worktreeOps.hasCommits')
    expect(handlerSource).not.toContain('unwrapEnvelope(await window.worktreeOps.hasCommits')
  })

  it('removes the old worktree:hasCommits IPC path after RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/worktree-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'worktree:hasCommits'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('worktree:hasCommits'")
  })

  it('removes the old worktree:create IPC path after RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/worktree-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'worktree:create'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('worktree:create'")
  })

  it('removes the old worktree:delete IPC path after RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/worktree-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'worktree:delete'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('worktree:delete'")
  })

  it('removes the old worktree:sync IPC path after RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/worktree-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'worktree:sync'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('worktree:sync'")
  })

  it('removes the old worktree:duplicate IPC path after RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/worktree-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'worktree:duplicate'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('worktree:duplicate'")
  })

  it('removes the old worktree:exists IPC path after RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/worktree-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'worktree:exists'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('worktree:exists'")
  })

  it('removes the old worktree:renameBranch IPC path after RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/worktree-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'worktree:renameBranch'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('worktree:renameBranch'")
  })

  it('removes the old worktree:createFromBranch IPC path after RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/worktree-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'worktree:createFromBranch'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('worktree:createFromBranch'")
  })

  it('removes the old worktree:openInTerminal IPC path after RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/worktree-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'worktree:openInTerminal'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('worktree:openInTerminal'")
  })

  it('removes the old worktree:openInEditor IPC path after RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/worktree-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'worktree:openInEditor'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('worktree:openInEditor'")
  })

  it('removes the old worktree:getContext IPC path after RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/worktree-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'worktree:getContext'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('worktree:getContext'")
  })

  it('removes the old worktree:updateContext IPC path after RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/worktree-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'worktree:updateContext'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('worktree:updateContext'")
  })

  it('removes the old git:branches IPC path after worktree getBranches RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/worktree-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:branches'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:branches'")
  })

  it('removes the old git:branchExists IPC path after worktree branchExists RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/worktree-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:branchExists'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:branchExists'")
  })

  it('removes the old git:listBranchesWithStatus IPC path after git listBranchesWithStatus RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/worktree-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:listBranchesWithStatus'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:listBranchesWithStatus'")
  })

  it('removes the old git:merge IPC path after git merge RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:merge'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:merge'")
  })

  it('removes the old git:mergeAbort IPC path after git mergeAbort RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:mergeAbort'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:mergeAbort'")
  })

  it('removes the old git:hasUncommittedChanges IPC path after git hasUncommittedChanges RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:hasUncommittedChanges'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:hasUncommittedChanges'")
  })

  it('removes the old git:branchDiffShortStat IPC path after git branchDiffShortStat RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:branchDiffShortStat'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:branchDiffShortStat'")
  })

  it('removes the old git:getRemoteUrl IPC path after git getRemoteUrl RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:getRemoteUrl'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:getRemoteUrl'")
  })

  it('removes the old git:diffStat IPC path after git getDiffStat RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:diffStat'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:diffStat'")
  })

  it('removes the old git:prMerge IPC path after git prMerge RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:prMerge'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:prMerge'")
  })

  it('removes the old git:isBranchMerged IPC path after git isBranchMerged RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:isBranchMerged'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:isBranchMerged'")
  })

  it('removes the old git:deleteBranch IPC path after git deleteBranch RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:deleteBranch'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:deleteBranch'")
  })

  it('removes the old git:stageFile IPC path after git stageFile RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:stageFile'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:stageFile'")
  })

  it('removes the old git:unstageFile IPC path after git unstageFile RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:unstageFile'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:unstageFile'")
  })

  it('removes the old git:stageAll IPC path after git stageAll RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:stageAll'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:stageAll'")
  })

  it('removes the old git:unstageAll IPC path after git unstageAll RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:unstageAll'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:unstageAll'")
  })

  it('removes the old git:commit IPC path after git commit RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:commit'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:commit'")
  })

  it('removes the old git:push IPC path after git push RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:push'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:push'")
  })

  it('removes the old git:pull IPC path after git pull RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:pull'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:pull'")
  })

  it('removes the old git:diff IPC path after git diff RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:diff'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:diff'")
  })

  it('removes the old git:getFileContent IPC path after git file content RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:getFileContent'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:getFileContent'")
  })

  it('removes the old git:getFileContentBase64 IPC path after git base64 file content RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:getFileContentBase64'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:getFileContentBase64'")
  })

  it('removes the old git:getRefContentBase64 IPC path after git ref base64 content RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:getRefContentBase64'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:getRefContentBase64'")
  })

  it('removes the old git:getRefContent IPC path after git ref content RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:getRefContent'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:getRefContent'")
  })

  it('removes the old git:stageHunk IPC path after git stage hunk RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:stageHunk'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:stageHunk'")
  })

  it('removes the old git:unstageHunk IPC path after git unstage hunk RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:unstageHunk'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:unstageHunk'")
  })

  it('removes the old git:revertHunk IPC path after git revert hunk RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:revertHunk'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:revertHunk'")
  })

  it('removes the old git:branchDiffFiles IPC path after git branch diff files RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:branchDiffFiles'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:branchDiffFiles'")
  })

  it('removes the old git:branchBaseContent IPC path after git branch base content RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:branchBaseContent'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:branchBaseContent'")
  })

  it('removes the old git:branchBaseContentBase64 IPC path after git branch base content base64 RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:branchBaseContentBase64'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:branchBaseContentBase64'")
  })

  it('removes the old git:branchFileDiff IPC path after git branch file diff RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:branchFileDiff'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:branchFileDiff'")
  })

  it('removes the old git:getRangeDiff IPC path after git range diff RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:getRangeDiff'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:getRangeDiff'")
  })

  it('removes the old git:needsPush IPC path after git needs push RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:needsPush'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:needsPush'")
  })

  it('removes the old git:createPR IPC path after git create pull request RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:createPR'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:createPR'")
  })

  it('removes the old git:generatePRContent IPC path after git generate PR content RPC migration', () => {
    const mainSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'git:generatePRContent'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:generatePRContent'")
  })

  it('removes the old empty gitOps preload global exposure', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const gitOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('gitOps'")
    expect(preloadSource).not.toContain('window.gitOps = gitOps')
    expect(preloadTypesSource).not.toContain('gitOps:')
    expect(preloadTypesSource).not.toContain('getFileStatuses: (worktreePath: string)')
    expect(preloadTypesSource).not.toContain('onStatusChanged: (callback: (event: GitStatusChangedEvent)')
    expect(preloadTypesSource).not.toContain('getBranchFileDiff: (')
    expect(preloadTypesSource).not.toContain('interface GitDiffStatFile')
    expect(preloadTypesSource).not.toContain('interface GitFileStatus')
    expect(preloadTypesSource).not.toContain('interface GitStatusChangedEvent')
    expect(preloadTypesSource).not.toContain('interface GitBranchInfo')
  })

  it('moves OpenCode desktop command helpers behind a service import', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')
    const opencodeCommandsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )

    expect(
      fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/opencode-handlers.ts'))
    ).toBe(false)
    expect(opencodeCommandsSource).toContain('export async function connectOpenCodeSession')
    expect(opencodeCommandsSource).toContain('export async function cleanupOpenCode')
    expect(opencodeCommandsSource).not.toContain('IPC: opencode')
    expect(mainSource).toContain("from './services/opencode-session-commands'")
    expect(mainSource).not.toContain("from './ipc'")
    expect(ipcIndexSource).not.toContain('opencode-handlers')
    expect(ipcIndexSource).not.toContain('connectOpenCodeSession')
  })

  it('removes the old opencode:connect IPC path after OpenCode connect RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:connect'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:connect'")
  })

  it('removes the old opencode:reconnect IPC path after OpenCode reconnect RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:reconnect'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:reconnect'")
  })

  it('removes the old opencode:prompt IPC path after OpenCode prompt RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:prompt'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:prompt'")
  })

  it('removes the old opencode:abort IPC path after OpenCode abort RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:abort'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:abort'")
  })

  it('removes the old opencode:steer IPC path after OpenCode steer RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:steer'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:steer'")
  })

  it('removes the old opencode:disconnect IPC path after OpenCode disconnect RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:disconnect'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:disconnect'")
  })

  it('removes the old opencode:messages IPC path after OpenCode getMessages RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:messages'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:messages'")
  })

  it('removes the old opencode:refresh-from-thread IPC path after OpenCode refreshFromThread RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:refresh-from-thread'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:refresh-from-thread'")
  })

  it('removes the old opencode:models IPC path after OpenCode listModels RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:models'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:models'")
  })

  it('removes the old opencode:setModel IPC path after OpenCode setModel RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:setModel'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:setModel'")
  })

  it('removes the old opencode:modelInfo IPC path after OpenCode modelInfo RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:modelInfo'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:modelInfo'")
  })

  it('removes the old opencode:question:reply IPC path after OpenCode questionReply RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:question:reply'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:question:reply'")
  })

  it('removes the old opencode:question:reject IPC path after OpenCode questionReject RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:question:reject'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:question:reject'")
  })

  it('removes the old opencode:plan:approve IPC path after OpenCode planApprove RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:plan:approve'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:plan:approve'")
  })

  it('removes the old opencode:plan:reject IPC path after OpenCode planReject RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:plan:reject'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:plan:reject'")
  })

  it('removes the old opencode:permission:reply IPC path after OpenCode permissionReply RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:permission:reply'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:permission:reply'")
  })

  it('removes the old opencode:permission:list IPC path after OpenCode permissionList RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:permission:list'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:permission:list'")
  })

  it('removes the old opencode:commandApprovalReply IPC path after OpenCode commandApprovalReply RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:commandApprovalReply'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:commandApprovalReply'")
  })

  it('removes the old opencode:sessionInfo IPC path after OpenCode sessionInfo RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:sessionInfo'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:sessionInfo'")
  })

  it('removes the old opencode:commands IPC path after OpenCode commands RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:commands'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:commands'")
  })

  it('removes the old opencode:command IPC path after OpenCode command RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:command'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:command'")
  })

  it('removes the old opencode:undo IPC path after OpenCode undo RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:undo'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:undo'")
  })

  it('removes the old opencode:redo IPC path after OpenCode redo RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:redo'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:redo'")
  })

  it('removes the old opencode:capabilities IPC path after OpenCode capabilities RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:capabilities'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:capabilities'")
  })

  it('removes the old opencode:renameSession IPC path after OpenCode renameSession RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:renameSession'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:renameSession'")
  })

  it('removes the old opencode:fork IPC path after OpenCode fork RPC migration', () => {
    const mainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'opencode:fork'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('opencode:fork'")
  })

  it('removes the old opencode:stream IPC event path after OpenCode stream WebSocket migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')
    const agentSdkTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/agent-sdk-types.ts'),
      'utf-8'
    )
    const agentSdkManagerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/agent-sdk-manager.ts'),
      'utf-8'
    )
    const eventBusSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/agent-event-bus.ts'),
      'utf-8'
    )
    const openCodeServiceSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-service.ts'),
      'utf-8'
    )
    const openCodeHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-session-commands.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(agentSdkTypesSource).not.toContain('BrowserWindow')
    expect(agentSdkTypesSource).not.toContain('AgentMainWindowState')
    expect(agentSdkTypesSource).not.toContain('setMainWindow')
    expect(agentSdkManagerSource).not.toContain('BrowserWindow')
    expect(agentSdkManagerSource).not.toContain('setMainWindow')
    expect(eventBusSource).not.toContain('webContents.send(OPENCODE_STREAM_CHANNEL')
    expect(eventBusSource).not.toContain('setMainWindow')
    expect(openCodeServiceSource).not.toContain('emitWorktreeBranchRenamed(this.mainWindow')
    expect(openCodeServiceSource).not.toContain('BrowserWindow')
    expect(openCodeServiceSource).not.toContain('AgentMainWindowState')
    expect(openCodeServiceSource).not.toContain('setMainWindow(')
    expect(openCodeServiceSource).not.toContain('private mainWindow')
    expect(openCodeServiceSource).not.toContain('mainWindow.webContents.send(channel, data)')
    expect(openCodeServiceSource).not.toContain('webContents.send(channel, data)')
    expect(openCodeServiceSource).not.toContain('agentEventBus.setMainWindow')
    expect(openCodeServiceSource).toContain('notificationService.shouldNotifyWhenWindowUnfocused()')
    expect(openCodeHandlersSource).not.toContain('registerOpenCodeHandlers')
    expect(mainSource).not.toContain('registerOpenCodeHandlers')
    expect(ipcIndexSource).not.toContain('registerOpenCodeHandlers')
    expect(mainSource).not.toContain('openCodeService.setMainWindow(mainWindow)')
    expect(mainSource).not.toContain('sdkManager.setMainWindow(mainWindow)')
    expect(preloadSource).not.toContain("ipcRenderer.on('opencode:stream'")
    expect(preloadSource).not.toContain("ipcRenderer.removeListener('opencode:stream'")
  })

  it('keeps OpenCode routing tests on the agent event bus instead of fake window sends', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-5/session-9/opencode-routing.test.ts'),
      'utf-8'
    )

    expect(source).toContain("import { agentEventBus } from '../../../src/main/services/agent-event-bus'")
    expect(source).toContain('agentEventBus.subscribe((event) => events.push(event))')
    expect(source).not.toContain('openCodeService.setMainWindow')
    expect(source).not.toContain('webContents: { send')
    expect(source).not.toContain('webContents.send')
    expect(source).not.toContain('setMainWindow')
  })

  it('removes the old Claude Code generic renderer send fallback after event publisher migration', () => {
    const claudeCodeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/claude-code-implementer.ts'),
      'utf-8'
    )

    expect(claudeCodeSource).not.toContain('mainWindow.webContents.send(channel, data)')
    expect(claudeCodeSource).not.toContain('webContents.send(channel, data)')
    expect(claudeCodeSource).not.toContain('agentEventBus.setMainWindow')
    expect(claudeCodeSource).not.toContain('BrowserWindow')
    expect(claudeCodeSource).not.toContain('AgentMainWindowState')
    expect(claudeCodeSource).not.toContain('setMainWindow(')
    expect(claudeCodeSource).not.toContain('private mainWindow')
    expect(claudeCodeSource).toContain('notificationService.shouldNotifyWhenWindowUnfocused()')
  })

  it('removes the old Codex generic renderer send fallback after event publisher migration', () => {
    const codexSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/codex-implementer.ts'),
      'utf-8'
    )

    expect(codexSource).not.toContain('mainWindow.webContents.send(channel, data)')
    expect(codexSource).not.toContain('webContents.send(channel, data)')
    expect(codexSource).not.toContain('agentEventBus.setMainWindow')
    expect(codexSource).not.toContain('BrowserWindow')
    expect(codexSource).not.toContain('AgentMainWindowState')
    expect(codexSource).not.toContain('setMainWindow(')
    expect(codexSource).not.toContain('private mainWindow')
    expect(codexSource).not.toContain('getMainWindow()')
    expect(codexSource).toContain('notificationService.shouldNotifyWhenWindowUnfocused()')
  })

  it('keeps core agent services free of retained BrowserWindow dependencies', () => {
    const agentSdkManagerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/agent-sdk-manager.ts'),
      'utf-8'
    )
    const agentSdkTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/agent-sdk-types.ts'),
      'utf-8'
    )
    const eventBusSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/agent-event-bus.ts'),
      'utf-8'
    )
    const openCodeServiceSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/opencode-service.ts'),
      'utf-8'
    )
    const claudeCodeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/claude-code-implementer.ts'),
      'utf-8'
    )
    const codexSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/codex-implementer.ts'),
      'utf-8'
    )
    const sources = [
      agentSdkManagerSource,
      agentSdkTypesSource,
      eventBusSource,
      openCodeServiceSource,
      claudeCodeSource,
      codexSource
    ]

    for (const source of sources) {
      expect(source).not.toContain('BrowserWindow')
      expect(source).not.toContain('AgentMainWindowState')
      expect(source).not.toContain('setMainWindow(')
      expect(source).not.toContain('private mainWindow')
      expect(source).not.toContain('mainWindow.webContents')
      expect(source).not.toContain('getMainWindow()')
    }

    expect(openCodeServiceSource).toContain('notificationService.shouldNotifyWhenWindowUnfocused()')
    expect(claudeCodeSource).toContain('notificationService.shouldNotifyWhenWindowUnfocused()')
    expect(codexSource).toContain('notificationService.shouldNotifyWhenWindowUnfocused()')
  })

  it('keeps core OpenCode session actions registered as Effect RPC methods with mocked-provider coverage', () => {
    const rpcDomainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/opencode-ops.ts'),
      'utf-8'
    )
    const mockedProviderTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/agent-rpc.mock-provider.test.ts'),
      'utf-8'
    )
    const requiredMethods = [
      'connect',
      'reconnect',
      'prompt',
      'abort',
      'steer',
      'disconnect',
      'getMessages',
      'refreshFromThread',
      'listModels',
      'setModel',
      'modelInfo',
      'sessionInfo',
      'undo',
      'redo',
      'command',
      'commands',
      'renameSession',
      'capabilities',
      'fork'
    ]

    for (const method of requiredMethods) {
      expect(rpcDomainSource).toContain(`readonly ${method}:`)
      expect(rpcDomainSource).toContain(`'opencodeOps.${method}'`)
      expect(mockedProviderTestSource).toContain(`routes opencodeOps.${method} to the injected provider service`)
      expect(mockedProviderTestSource).toContain(
        `validates opencodeOps.${method} params before calling the provider service`
      )
    }

    expect(rpcDomainSource).not.toContain("ipcMain.handle('opencode:")
    expect(rpcDomainSource).not.toContain('ipcRenderer.invoke')
  })

  it('keeps OpenCode question reply and reject actions registered as Effect RPC methods with mocked-provider coverage', () => {
    const rpcDomainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/opencode-ops.ts'),
      'utf-8'
    )
    const mockedProviderTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/agent-rpc.mock-provider.test.ts'),
      'utf-8'
    )
    const requiredMethods = ['questionReply', 'questionReject']

    for (const method of requiredMethods) {
      expect(rpcDomainSource).toContain(`readonly ${method}:`)
      expect(rpcDomainSource).toContain(`const ${method}ParamsSchema = z`)
      expect(rpcDomainSource).toContain(`'opencodeOps.${method}'`)
      expect(mockedProviderTestSource).toContain(`routes opencodeOps.${method} to the injected provider service`)
      expect(mockedProviderTestSource).toContain(
        `validates opencodeOps.${method} params before calling the provider service`
      )
    }

    expect(rpcDomainSource).not.toContain("ipcMain.handle('opencode:question")
    expect(rpcDomainSource).not.toContain('ipcRenderer.invoke')
  })

  it('keeps OpenCode plan approve and reject actions registered as Effect RPC methods with mocked-provider coverage', () => {
    const rpcDomainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/opencode-ops.ts'),
      'utf-8'
    )
    const mockedProviderTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/agent-rpc.mock-provider.test.ts'),
      'utf-8'
    )
    const requiredMethods = ['planApprove', 'planReject']

    for (const method of requiredMethods) {
      expect(rpcDomainSource).toContain(`readonly ${method}:`)
      expect(rpcDomainSource).toContain(`const ${method}ParamsSchema = z`)
      expect(rpcDomainSource).toContain(`'opencodeOps.${method}'`)
      expect(mockedProviderTestSource).toContain(`routes opencodeOps.${method} to the injected provider service`)
      expect(mockedProviderTestSource).toContain(
        `validates opencodeOps.${method} params before calling the provider service`
      )
    }

    expect(rpcDomainSource).not.toContain("ipcMain.handle('opencode:plan")
    expect(rpcDomainSource).not.toContain('ipcRenderer.invoke')
  })

  it('keeps OpenCode permission and command approval replies registered as Effect RPC methods with mocked-provider coverage', () => {
    const rpcDomainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/opencode-ops.ts'),
      'utf-8'
    )
    const mockedProviderTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/agent-rpc.mock-provider.test.ts'),
      'utf-8'
    )
    const requiredMethods = ['permissionReply', 'commandApprovalReply']

    for (const method of requiredMethods) {
      expect(rpcDomainSource).toContain(`readonly ${method}:`)
      expect(rpcDomainSource).toContain(`const ${method}ParamsSchema = z`)
      expect(rpcDomainSource).toContain(`'opencodeOps.${method}'`)
      expect(mockedProviderTestSource).toContain(`routes opencodeOps.${method} to the injected provider service`)
      expect(mockedProviderTestSource).toContain(
        `validates opencodeOps.${method} params before calling the provider service`
      )
    }

    expect(rpcDomainSource).not.toContain("ipcMain.handle('opencode:permission")
    expect(rpcDomainSource).not.toContain("ipcMain.handle('opencode:commandApprovalReply")
    expect(rpcDomainSource).not.toContain('ipcRenderer.invoke')
  })

  it('keeps Phase 10 agent RPC coverage on mocked providers before live provider smoke tests', () => {
    const serverTestsDir = path.resolve(__dirname, '../../src/server/__tests__')
    const testNames = fs.readdirSync(serverTestsDir)
    const mockedProviderTestSource = fs.readFileSync(
      path.resolve(serverTestsDir, 'agent-rpc.mock-provider.test.ts'),
      'utf-8'
    )
    const requiredMethods = [
      'connect',
      'reconnect',
      'prompt',
      'abort',
      'steer',
      'disconnect',
      'getMessages',
      'refreshFromThread',
      'listModels',
      'setModel',
      'modelInfo',
      'questionReply',
      'questionReject',
      'planApprove',
      'planReject',
      'permissionReply',
      'permissionList',
      'commandApprovalReply',
      'sessionInfo',
      'undo',
      'redo',
      'command',
      'commands',
      'renameSession',
      'capabilities',
      'fork'
    ]

    expect(testNames).toContain('agent-rpc.mock-provider.test.ts')
    expect(mockedProviderTestSource).toContain("describe('agent RPC mocked provider'")
    expect(mockedProviderTestSource).toContain("import type { OpenCodeOpsRpcService } from '../rpc/domains/opencode-ops'")
    expect(mockedProviderTestSource).toContain('makeRpcRouter({')
    expect(mockedProviderTestSource).toContain('opencodeOps: service')

    for (const method of requiredMethods) {
      expect(mockedProviderTestSource).toContain(`routes opencodeOps.${method} to the injected provider service`)
      expect(mockedProviderTestSource).toContain(
        `validates opencodeOps.${method} params before calling the provider service`
      )
    }

    expect(mockedProviderTestSource).not.toContain('makeDesktopCommandRequest')
    expect(mockedProviderTestSource).not.toContain('requestOpenCode')
    expect(mockedProviderTestSource).not.toContain('../main/services')
    expect(mockedProviderTestSource).not.toContain('../../main/services')
  })

  it('keeps Git status, diff, staging, commit, push/pull, PR, and file-content operations registered as Effect RPC methods', () => {
    const rpcDomainSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/git-ops.ts'),
      'utf-8'
    )
    const mockedProviderTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/git-rpc.mock-provider.test.ts'),
      'utf-8'
    )
    const requiredMethods = [
      'getFileStatuses',
      'stageFile',
      'unstageFile',
      'discardChanges',
      'addToGitignore',
      'openInEditor',
      'showInFinder',
      'watchWorktree',
      'unwatchWorktree',
      'watchBranch',
      'unwatchBranch',
      'getBranchInfo',
      'stageAll',
      'unstageAll',
      'stageHunk',
      'unstageHunk',
      'revertHunk',
      'commit',
      'push',
      'pull',
      'merge',
      'mergeAbort',
      'hasUncommittedChanges',
      'branchDiffShortStat',
      'getDiff',
      'listBranchesWithStatus',
      'getFileContent',
      'getFileContentBase64',
      'getRefContent',
      'getBranchBaseContent',
      'getRefContentBase64',
      'getBranchBaseContentBase64',
      'getRemoteUrl',
      'getDiffStat',
      'getBranchDiffFiles',
      'getBranchFileDiff',
      'getRangeDiff',
      'needsPush',
      'createPR',
      'generatePRContent',
      'prMerge',
      'isBranchMerged',
      'deleteBranch',
      'listPRs',
      'getPRState',
      'getPRReviewComments'
    ]

    expect(rpcDomainSource).toContain('export interface GitOpsRpcService')
    expect(rpcDomainSource).toContain('export const makeGitOpsRpcHandlers')
    expect(mockedProviderTestSource).toContain("describe('git ops RPC mocked provider'")
    expect(mockedProviderTestSource).toContain("import type { GitOpsRpcService } from '../rpc/domains/git-ops'")
    expect(mockedProviderTestSource).toContain('gitOps: service')

    for (const method of requiredMethods) {
      expect(rpcDomainSource).toContain(`readonly ${method}:`)
      expect(rpcDomainSource).toContain(`'gitOps.${method}'`)
      expect(mockedProviderTestSource).toContain(`routes gitOps.${method} to the injected provider service`)
      expect(mockedProviderTestSource).toContain(
        `validates gitOps.${method} params before calling the provider service`
      )
    }

    expect(mockedProviderTestSource).not.toContain('makeLiveGitOpsRpcService')
    expect(mockedProviderTestSource).not.toContain('simpleGit(')
    expect(mockedProviderTestSource).not.toContain('../main/services')
    expect(mockedProviderTestSource).not.toContain('../../main/services')
  })

  it('keeps Git RPC integration coverage on temporary git repositories', () => {
    const integrationTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/git-ops-rpc.test.ts'),
      'utf-8'
    )
    const requiredCases = [
      'returns staged, unstaged, and untracked file statuses from a git worktree',
      'returns branch info from a git worktree',
      'stages a file in a git worktree',
      'stages all changes in a git worktree',
      'unstages all staged changes in a git worktree',
      'commits staged changes in a git worktree',
      'pushes committed changes to the default remote and current branch',
      'pulls committed changes from the default remote and current branch',
      'merges a branch into the current git worktree branch',
      'returns tracked and untracked file diffs from a git worktree',
      'reads file content from a worktree path',
      'reads file content from a git ref',
      'returns diff stats for staged, unstaged, and untracked files',
      'creates a pull request using the GitHub CLI and a temporary body file'
    ]

    expect(integrationTestSource).toContain("mkdtempSync(join(tmpdir(), 'hive-git-ops-rpc-'))")
    expect(integrationTestSource).toContain("execFileSync('git', args")
    expect(integrationTestSource).toContain("git(dir, ['init'])")
    expect(integrationTestSource).toContain('makeLiveGitOpsRpcService')

    for (const testName of requiredCases) {
      expect(integrationTestSource).toContain(`it('${testName}'`)
    }

    expect(integrationTestSource).not.toContain('ipcMain')
    expect(integrationTestSource).not.toContain('ipcRenderer')
    expect(integrationTestSource).not.toContain('window.gitOps')
  })

  it('keeps Git operations server-safe and free of Electron imports', () => {
    const gitServiceSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/git-service.ts'),
      'utf-8'
    )
    const gitRepositorySource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/git-repository.ts'),
      'utf-8'
    )
    const gitOpsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/git-ops.ts'),
      'utf-8'
    )
    const fileTreeOpsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/file-tree-ops.ts'),
      'utf-8'
    )
    const mockedProviderTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/git-rpc.mock-provider.test.ts'),
      'utf-8'
    )
    const integrationTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/__tests__/git-ops-rpc.test.ts'),
      'utf-8'
    )
    const productionSources = [
      gitServiceSource,
      gitRepositorySource,
      gitOpsSource,
      fileTreeOpsSource
    ]

    for (const source of productionSources) {
      expect(source).not.toContain("from 'electron'")
      expect(source).not.toContain('from "electron"')
      expect(source).not.toContain("import('electron')")
      expect(source).not.toContain('import("electron")')
      expect(source).not.toContain('ipcMain')
      expect(source).not.toContain('ipcRenderer')
      expect(source).not.toContain('webContents.send')
      expect(source).not.toContain('src/main/ipc')
      expect(source).not.toContain('main/ipc')
    }

    expect(gitServiceSource).toContain('export class GitService')
    expect(gitRepositorySource).toContain('export function initRepository')
    expect(gitOpsSource).toContain('export interface GitOpsRpcService')
    expect(gitOpsSource).toContain('export const makeLiveGitOpsRpcService')
    expect(gitOpsSource).toContain("const command = 'gitShowInFinder'")
    expect(gitOpsSource).toContain('makeDesktopCommandRequest(id, command, { filePath })')
    expect(gitOpsSource).not.toContain('shell.showItemInFolder')

    expect(mockedProviderTestSource).toContain("describe('git ops RPC mocked provider'")
    expect(mockedProviderTestSource).toContain('gitOps: service')
    expect(mockedProviderTestSource).not.toContain('../main/services')
    expect(mockedProviderTestSource).not.toContain('../../main/services')
    expect(integrationTestSource).toContain("mkdtempSync(join(tmpdir(), 'hive-git-ops-rpc-'))")
    expect(integrationTestSource).not.toContain("from 'electron'")
    expect(integrationTestSource).not.toContain('from "electron"')
  })

  it('removes the old empty opencodeOps preload global exposure', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const opencodeOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('opencodeOps'")
    expect(preloadSource).not.toContain('window.opencodeOps = opencodeOps')
    expect(preloadTypesSource).not.toContain('opencodeOps:')
    expect(preloadTypesSource).not.toContain('connect: (')
    expect(preloadTypesSource).not.toContain('prompt: (')
    expect(preloadTypesSource).not.toContain('permissionReply: (')
    expect(preloadTypesSource).not.toContain(
      'onStream: (callback: (event: OpenCodeStreamEvent) => void) => () => void'
    )
  })

  it('routes ProjectItem branch selection worktree creation through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/projects/ProjectItem.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleBranchSelect = useCallback(')
    const handlerEnd = source.indexOf('  return (', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(handlerSource).toContain('const result = await worktreeApi.createFromBranch({')
    expect(handlerSource).toContain('projectId: project.id,')
    expect(handlerSource).toContain('projectPath: project.path,')
    expect(handlerSource).toContain('projectName: project.name,')
    expect(handlerSource).toContain('branchName,')
    expect(handlerSource).toContain('...(prNumber === undefined ? {} : { prNumber })')
    expect(handlerSource).toContain('useWorktreeStore.getState().loadWorktrees')
    expect(handlerSource).toContain(
      'useWorktreeStore.getState().selectWorktree(result.worktree.id)'
    )
    expect(handlerSource).toContain('toast.dismiss(loadingToastId)')
    expect(handlerSource).not.toContain('window.worktreeOps.createFromBranch')
    expect(handlerSource).not.toContain('unwrapEnvelope(')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('routes ConnectionItem connection show-in-folder through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/connections/ConnectionItem.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleOpenInFinder = async (): Promise<void> => {')
    const handlerEnd = source.indexOf(
      'const handleCopyPath = async (): Promise<void> => {',
      handlerStart
    )
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('await projectApi.showInFolder(connection.path)')
    expect(handlerSource).not.toContain('window.projectOps.showInFolder')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes ConnectionItem path copy through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/connections/ConnectionItem.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleCopyPath = async (): Promise<void> => {')
    const handlerEnd = source.indexOf('const handleDelete = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('await projectApi.copyToClipboard(connection.path)')
    expect(handlerSource).toContain("clipboardToast.copied('Path')")
    expect(handlerSource).not.toContain('window.projectOps.copyToClipboard')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes ConnectionItem connection open-in-terminal through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/connections/ConnectionItem.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleOpenInTerminal = useCallback')
    const handlerEnd = source.indexOf('const handleOpenInEditor = useCallback', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(handlerSource).toContain(
      'const result = await connectionApi.openInTerminal(connection.path)'
    )
    expect(handlerSource).toContain("toast.success('Opened in Terminal')")
    expect(handlerSource).toContain("toast.error(result.error || 'Failed to open in terminal')")
    expect(handlerSource).not.toContain('window.connectionOps.openInTerminal')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes PinnedList connection open-in-editor through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/PinnedList.tsx'),
      'utf-8'
    )
    const worktreeHandlerStart = source.indexOf(
      'const handleOpenInEditor = async (): Promise<void> => {'
    )
    const handlerStart = source.indexOf(
      'const handleOpenInEditor = useCallback(async (): Promise<void> => {',
      worktreeHandlerStart + 1
    )
    const handlerEnd = source.indexOf(
      'const handleOpenInFinder = useCallback(async (): Promise<void> => {',
      handlerStart
    )
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(worktreeHandlerStart).toBeGreaterThan(-1)
    expect(handlerStart).toBeGreaterThan(worktreeHandlerStart)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(handlerSource).toContain(
      'const result = await connectionApi.openInEditor(connection.path)'
    )
    expect(handlerSource).toContain("toast.success('Opened in Editor')")
    expect(handlerSource).toContain("toast.error(result.error || 'Failed to open in editor')")
    expect(handlerSource).not.toContain('window.connectionOps.openInEditor')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes ConnectionItem connection open-in-editor through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/connections/ConnectionItem.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleOpenInEditor = useCallback')
    const handlerEnd = source.indexOf('const handleOpenInFinder = async', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(handlerSource).toContain(
      'const result = await connectionApi.openInEditor(connection.path)'
    )
    expect(handlerSource).toContain("toast.success('Opened in Editor')")
    expect(handlerSource).toContain("toast.error(result.error || 'Failed to open in editor')")
    expect(handlerSource).not.toContain('window.connectionOps.openInEditor')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('removes the old connection:openInEditor IPC adapter after connection editor RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const connectionHandlersSource = readOptionalSource('../../src/main/ipc/connection-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('connection:openInEditor'")
    expect(connectionHandlersSource).not.toContain("defineHandler('connection:openInEditor'")
    expect(connectionHandlersSource).not.toContain("connectionFailed('connection:openInEditor'")
  })

  it('routes WorktreeItem attachment removal through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/worktrees/WorktreeItem.tsx'),
      'utf-8'
    )
    const detachStart = source.indexOf('const handleDetachAttachment = useCallback(')
    const addedStart = source.indexOf('const handleAttachmentAdded = useCallback(', detachStart)
    const detachSource = source.slice(detachStart, addedStart)

    expect(detachStart).toBeGreaterThan(-1)
    expect(addedStart).toBeGreaterThan(detachStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(detachSource).toContain(
      'const result = await dbApi.worktree.removeAttachment(worktree.id, attachmentId)'
    )
    expect(detachSource).toContain(
      'setAttachments((prev) => prev.filter((a) => a.id !== attachmentId))'
    )
    expect(detachSource).toContain("toast.success('Attachment removed')")
    expect(detachSource).toContain("toast.error(result.error || 'Failed to remove attachment')")
    expect(detachSource).not.toContain('db.worktree.removeAttachment')
  })

  it('routes WorktreeItem attachment refresh through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/worktrees/WorktreeItem.tsx'),
      'utf-8'
    )
    const refreshStart = source.indexOf('const handleAttachmentAdded = useCallback(')
    const renameStart = source.indexOf('const startBranchRename = useCallback(', refreshStart)
    const refreshSource = source.slice(refreshStart, renameStart)

    expect(refreshStart).toBeGreaterThan(-1)
    expect(renameStart).toBeGreaterThan(refreshStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
    expect(refreshSource).toContain('dbApi.worktree.get<Worktree>(worktree.id).then((w) => {')
    expect(refreshSource).toContain("setAttachments(JSON.parse(w.attachments || '[]'))")
    expect(refreshSource).not.toContain('db.worktree.get(worktree.id)')
  })

  it('routes AddAttachmentDialog attachment creation through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/worktrees/AddAttachmentDialog.tsx'
      ),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const result = await dbApi.worktree.addAttachment(worktreeId, {')
    expect(source).toContain('type: detected.type')
    expect(source).toContain('url: url.trim()')
    expect(source).toContain('label: detected.label')
    expect(source).toContain("toast.error(result.error || 'Failed to add attachment')")
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
    expect(source).not.toContain('db.worktree.addAttachment')
  })

  it('routes KanbanTicketModal external ticket URL opening through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('void systemApi.openInChrome(ticket.external_url!)')
    expect(source).not.toContain('window.systemOps.openInChrome(ticket.external_url!')
  })

  it('routes KanbanTicketModal in-memory session worktree fallback through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const fallbackStart = source.indexOf('if (!worktreePath && found.worktree_id) {')
    const returnStart = source.indexOf('return { session: found', fallbackStart)
    const fallbackSource = source.slice(fallbackStart, returnStart)

    expect(fallbackStart).toBeGreaterThan(-1)
    expect(returnStart).toBeGreaterThan(fallbackStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(fallbackSource).toContain(
      'worktreePath = (await dbApi.worktree.get(found.worktree_id))?.path ?? null'
    )
    expect(fallbackSource).not.toContain('db.worktree.get(found.worktree_id)')
  })

  it('routes KanbanTicketModal missing session fallback through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const fallbackStart = source.indexOf('// DB fallback: session not in store')
    const hydrateStart = source.indexOf('// Hydrate into the in-memory store', fallbackStart)
    const fallbackSource = source.slice(fallbackStart, hydrateStart)

    expect(fallbackStart).toBeGreaterThan(-1)
    expect(hydrateStart).toBeGreaterThan(fallbackStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(fallbackSource).toContain(
      'const dbSession = await dbApi.session.get<Session>(sessionId)'
    )
    expect(fallbackSource).toContain('if (!dbSession) {')
    expect(fallbackSource).toContain('return null')
    expect(fallbackSource).not.toContain('db.session.get(sessionId)')
  })

  it('routes KanbanTicketModal missing session worktree path fallback through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const hydrateStart = source.indexOf('// Hydrate into the in-memory store')
    const returnStart = source.indexOf('return {', hydrateStart)
    const hydrateSource = source.slice(hydrateStart, returnStart)

    expect(hydrateStart).toBeGreaterThan(-1)
    expect(returnStart).toBeGreaterThan(hydrateStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(hydrateSource).toContain('useSessionStore.getState().hydrateSession(dbSession)')
    expect(hydrateSource).toContain(
      '((await dbApi.worktree.get<Worktree>(dbSession.worktree_id))?.path ?? null)'
    )
    expect(hydrateSource).not.toContain('db.worktree.get(dbSession.worktree_id)')
  })

  it('routes KanbanTicketModal active session worktree path fallback through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('useEffect(() => {')
    const lookupStart = source.indexOf('// Worktree not in store — load from DB', effectStart)
    const dependencyStart = source.indexOf('}, [sessionRecord?.worktree_id])', lookupStart)
    const effectSource = source.slice(effectStart, dependencyStart)

    expect(effectStart).toBeGreaterThan(-1)
    expect(lookupStart).toBeGreaterThan(effectStart)
    expect(dependencyStart).toBeGreaterThan(lookupStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(effectSource).toContain(
      'const inMemory = findWorktreePathById(sessionRecord.worktree_id)'
    )
    expect(effectSource).toContain('setDbWorktreePath(null)')
    expect(effectSource).toContain(
      'dbApi.worktree.get<Worktree>(sessionRecord.worktree_id).then((wt) => {'
    )
    expect(effectSource).toContain('setDbWorktreePath(wt?.path ?? null)')
    expect(effectSource).not.toContain('db.worktree.get(sessionRecord.worktree_id)')
  })

  it('routes KanbanTicketModal pending opencode session resolution through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const commentStart = source.indexOf('// If the Zustand store still has a placeholder')
    const cleanupStart = source.indexOf('return () => {', commentStart)
    const effectSource = source.slice(commentStart, cleanupStart)

    expect(commentStart).toBeGreaterThan(-1)
    expect(cleanupStart).toBeGreaterThan(commentStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(effectSource).toContain(
      '.get<{ opencode_session_id?: string | null }>(ticket.current_session_id)'
    )
    expect(effectSource).toContain('if (cancelled) return')
    expect(effectSource).toContain(
      'useSessionStore.getState().setOpenCodeSessionId(ticket.current_session_id!, dbId)'
    )
    expect(effectSource).toContain('setResolvedOpcSessionId(dbId)')
    expect(effectSource).not.toContain('db.session')
  })

  it('routes KanbanTicketModal eager supercharge session id persistence through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const functionStart = source.indexOf('const eagerSuperchargeStart = useCallback')
    const nextFunctionStart = source.indexOf('const eagerHandoffStart = useCallback', functionStart)
    const functionSource = source.slice(functionStart, nextFunctionStart)

    expect(functionStart).toBeGreaterThan(-1)
    expect(nextFunctionStart).toBeGreaterThan(functionStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(functionSource).toContain(
      'useSessionStore.getState().setOpenCodeSessionId(newSessionId, connectResult.sessionId)'
    )
    expect(functionSource).toContain('await dbApi.session.update<Session>(newSessionId, {')
    expect(functionSource).toContain('opencode_session_id: connectResult.sessionId')
    expect(functionSource).toContain('useWorktreeStatusStore.getState().setSessionStatus')
    expect(functionSource).not.toContain('db.session.update(newSessionId')
  })

  it('routes KanbanTicketModal eager handoff session id persistence through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const functionStart = source.indexOf('const eagerHandoffStart = useCallback')
    const nextFunctionStart = source.indexOf('const handleSupercharge = useCallback', functionStart)
    const functionSource = source.slice(functionStart, nextFunctionStart)

    expect(functionStart).toBeGreaterThan(-1)
    expect(nextFunctionStart).toBeGreaterThan(functionStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(functionSource).toContain(
      'useSessionStore.getState().setOpenCodeSessionId(newSessionId, connectResult.sessionId)'
    )
    expect(functionSource).toContain('await dbApi.session.update<Session>(newSessionId, {')
    expect(functionSource).toContain('opencode_session_id: connectResult.sessionId')
    expect(functionSource).toContain('useWorktreeStatusStore.getState().setSessionStatus')
    expect(functionSource).toContain(
      'await opencodeApi.prompt(\n          workingPath,\n          connectResult.sessionId'
    )
    expect(functionSource).not.toContain('db.session.update(newSessionId')
  })

  it('routes KanbanTicketModal diff-summary worktree lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const commentStart = source.indexOf(
      '// ── Resolve worktree for diff summary (base_branch lookup) ────────'
    )
    const effectStart = source.indexOf('useEffect(() => {', commentStart)
    const nextEffectStart = source.indexOf('useEffect(() => {', effectStart + 1)
    const effectSource = source.slice(commentStart, nextEffectStart)

    expect(commentStart).toBeGreaterThan(-1)
    expect(effectStart).toBeGreaterThan(-1)
    expect(nextEffectStart).toBeGreaterThan(effectStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(effectSource).toContain('.get<Worktree>(ticket.worktree_id)')
    expect(effectSource).toContain('if (!cancelled) {')
    expect(effectSource).toContain('setResolvedWorktree(dbWorktree ?? null)')
    expect(effectSource).toContain('setResolvedWorktree(null)')
    expect(effectSource).not.toContain('db.worktree')
  })

  it('routes KanbanTicketModal diff-summary base-branch default lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('if (!ticket.worktree_id || !resolvedWorktree) {')
    const dependencyStart = source.indexOf(
      '}, [ticket.project_id, ticket.worktree_id, resolvedWorktree])',
      effectStart
    )
    const effectSource = source.slice(effectStart, dependencyStart)

    expect(effectStart).toBeGreaterThan(-1)
    expect(dependencyStart).toBeGreaterThan(effectStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(effectSource).toContain(
      'const defaultWorktrees = await dbApi.worktree.getActiveByProject<Worktree>('
    )
    expect(effectSource).toContain('ticket.project_id')
    expect(effectSource).toContain('const defaultWt = defaultWorktrees.find((w) => w.is_default)')
    expect(effectSource).toContain(
      'setResolvedBaseBranch(resolvedWorktree.base_branch ?? defaultWt?.branch_name ?? null)'
    )
    expect(effectSource).toContain('setResolvedBaseBranch(resolvedWorktree.base_branch ?? null)')
    expect(effectSource).not.toContain('db.worktree.getActiveByProject')
  })

  it('routes KanbanTicketModal diff-summary changed-file loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('if (!dualPane || !resolvedWorktree?.path')
    const toggleModeStart = source.indexOf('  const toggleMode = useCallback', effectStart)
    const effectSource = source.slice(effectStart, toggleModeStart)

    expect(effectStart).toBeGreaterThan(-1)
    expect(toggleModeStart).toBeGreaterThan(effectStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(effectSource).toContain(
      'const result = await gitApi.getBranchDiffFiles(resolvedWorktree.path, resolvedBaseBranch)'
    )
    expect(effectSource).toContain('setDiffSummary(result.files ?? [])')
    expect(effectSource).toContain(
      "setDiffSummaryError(result.error ?? 'Failed to load changed files')"
    )
    expect(effectSource).toContain('const cleanup = gitApi.onStatusChanged((event) => {')
    expect(effectSource).toContain('event.worktreePath === resolvedWorktree.path')
    expect(effectSource).toContain('void loadDiffSummary()')
    expect(effectSource).toContain('cleanup()')
    expect(effectSource).not.toContain('window.gitOps.getBranchDiffFiles')
    expect(effectSource).not.toContain('window.gitOps.onStatusChanged')
    expect(effectSource).not.toContain('unwrapEnvelope')
  })

  it('routes KanbanTicketModal move-to-done worktree lookups through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const functionStart = source.indexOf('const handleMoveToDone = useCallback')
    const originalLogicStart = source.indexOf('// Original logic', functionStart)
    const functionSource = source.slice(functionStart, originalLogicStart)

    expect(functionStart).toBeGreaterThan(-1)
    expect(originalLogicStart).toBeGreaterThan(functionStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(functionSource).toContain(
      'const worktree = await dbApi.worktree.get<Worktree>(ticket.worktree_id)'
    )
    expect(functionSource).toContain(
      'const defaultWorktrees = await dbApi.worktree.getActiveByProject<Worktree>('
    )
    expect(functionSource).toContain('ticket.project_id')
    expect(functionSource).toContain('const resolvedBaseBranch = worktree.base_branch')
    expect(functionSource).toContain('kanbanStore.setPendingDoneMove({')
    expect(functionSource).toContain('// Fall through to normal move on error')
    expect(functionSource).not.toContain('db.worktree.get(ticket.worktree_id)')
    expect(functionSource).not.toContain('db.worktree.getActiveByProject(ticket.project_id)')
  })

  it('routes KanbanTicketModal main dropped file path extraction through fileApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleDropFiles = useCallback(')
    const handlerEnd = source.indexOf('  const { isDragging }', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { fileApi } from '@/api/file-api'")
    expect(handlerSource).toContain('filePath: fileApi.getPathForFile(file)')
    expect(handlerSource).toContain("mime: file.type || 'application/octet-stream'")
    expect(handlerSource).not.toContain('window.fileOps.getPathForFile')
  })

  it('routes KanbanTicketModal review dropped file path extraction through fileApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const firstHandlerStart = source.indexOf('const handleDropFiles = useCallback(')
    const handlerStart = source.indexOf(
      'const handleDropFiles = useCallback(',
      firstHandlerStart + 1
    )
    const handlerEnd = source.indexOf('  const { isDragging }', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(firstHandlerStart).toBeGreaterThan(-1)
    expect(handlerStart).toBeGreaterThan(firstHandlerStart)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { fileApi } from '@/api/file-api'")
    expect(handlerSource).toContain('filePath: fileApi.getPathForFile(file)')
    expect(handlerSource).toContain("mime: file.type || 'application/octet-stream'")
    expect(handlerSource).not.toContain('window.fileOps.getPathForFile')
  })

  it('routes KanbanTicketModal error dropped file path extraction through fileApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const firstHandlerStart = source.indexOf('const handleDropFiles = useCallback(')
    const secondHandlerStart = source.indexOf(
      'const handleDropFiles = useCallback(',
      firstHandlerStart + 1
    )
    const handlerStart = source.indexOf(
      'const handleDropFiles = useCallback(',
      secondHandlerStart + 1
    )
    const handlerEnd = source.indexOf('  const { isDragging }', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(firstHandlerStart).toBeGreaterThan(-1)
    expect(secondHandlerStart).toBeGreaterThan(firstHandlerStart)
    expect(handlerStart).toBeGreaterThan(secondHandlerStart)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { fileApi } from '@/api/file-api'")
    expect(handlerSource).toContain('filePath: fileApi.getPathForFile(file)')
    expect(handlerSource).toContain("mime: file.type || 'application/octet-stream'")
    expect(handlerSource).not.toContain('window.fileOps.getPathForFile')
  })

  it('routes KanbanTicketCard external ticket URL opening through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketCard.tsx'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('void systemApi.openInChrome(ticket.external_url)')
    expect(source).not.toContain('window.systemOps.openInChrome(ticket.external_url)')
  })

  it('routes KanbanTicketCard GitHub PR URL opening through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketCard.tsx'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('void systemApi.openInChrome(ticket.github_pr_url!)')
    expect(source).not.toContain('window.systemOps.openInChrome(ticket.github_pr_url!')
  })

  it('routes platform detection through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/lib/platform.ts'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('_platform = await systemApi.getPlatform()')
    expect(source).not.toContain('window.systemOps.getPlatform()')
  })

  it('routes AgentNotFoundDialog quit action through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/setup/AgentNotFoundDialog.tsx'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('onClick={() => void systemApi.quitApp()}')
    expect(source).not.toContain('window.systemOps.quitApp()')
  })

  it('routes settings store agent SDK detection through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSettingsStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('const result = await systemApi.detectAgentSdks()')
    expect(source).not.toContain('window.systemOps.detectAgentSdks()')
  })

  it('routes setup guard agent SDK detection through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/setup/AgentSetupGuard.tsx'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('systemApi\n      .detectAgentSdks()')
    expect(source).not.toContain('window.systemOps\n      .detectAgentSdks()')
  })

  it('routes keep-awake updates through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useKeepAwake.ts'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('systemApi.setKeepAwake(shouldBeAwake).catch((err) => {')
    expect(source).not.toContain('window.systemOps?.setKeepAwake')
  })

  it('routes session queued-state updates through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain(
      'systemApi.setSessionQueuedState(sessionId, hasQueued).catch(() => {})'
    )
    expect(source).not.toContain('window.systemOps?.setSessionQueuedState')
  })

  it('keeps notification queued-state tests on systemApi mocks instead of window system globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-23/session-1/notification-queued-suppression.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/system-api'")
    expect(source).toContain('const mockSetSessionQueuedState = vi.mocked(systemApi.setSessionQueuedState)')
    expect(source).toContain("describe('Phase 23 · Session 1: Zustand queue → systemApi push'")
    expect(source).not.toContain('window.systemOps')
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('Preload IPC contract')
  })

  it('removes the old notification:setSessionQueuedState IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(preloadSource).not.toContain("'notification:setSessionQueuedState'")
    expect(mainSource).not.toContain("ipcMain.handle('notification:setSessionQueuedState'")
  })

  it('routes code review background connect through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useLifecycleActions.ts'),
      'utf-8'
    )
    const reviewStart = source.indexOf('const createCodeReview = useCallback')
    const connectStart = source.indexOf(
      'const connectResult = unwrapEnvelope(await opencodeApi.connect(worktreePath, sessionId))',
      reviewStart
    )
    const promptStart = source.indexOf('await opencodeApi.prompt(', connectStart)
    const reviewConnectSource = source.slice(connectStart, promptStart)

    expect(reviewStart).toBeGreaterThan(-1)
    expect(connectStart).toBeGreaterThan(reviewStart)
    expect(promptStart).toBeGreaterThan(connectStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(reviewConnectSource).toContain(
      'const connectResult = unwrapEnvelope(await opencodeApi.connect(worktreePath, sessionId))'
    )
    expect(reviewConnectSource).toContain('connectResult.success && connectResult.sessionId')
    expect(reviewConnectSource).toContain(
      'sessionStore.setOpenCodeSessionId(sessionId, connectResult.sessionId)'
    )
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(reviewConnectSource).toContain(
      'dbApi.session\n              .update(sessionId, { opencode_session_id: connectResult.sessionId })'
    )
    expect(source).not.toContain('const db = unwrapEnvelopeApi(() => window.db)')
    expect(reviewConnectSource).not.toContain('db.session')
    expect(reviewConnectSource).not.toContain('window.opencodeOps.connect')
  })

  it('routes code review background prompt through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useLifecycleActions.ts'),
      'utf-8'
    )
    const reviewStart = source.indexOf('const createCodeReview = useCallback')
    const bumpStart = source.indexOf('bumpWorktreeLastMessage({ worktreeId })', reviewStart)
    const promptStart = source.indexOf('await opencodeApi.prompt(', bumpStart)
    const catchStart = source.indexOf('} catch {', promptStart)
    const reviewPromptSource = source.slice(bumpStart, catchStart)

    expect(reviewStart).toBeGreaterThan(-1)
    expect(bumpStart).toBeGreaterThan(reviewStart)
    expect(promptStart).toBeGreaterThan(bumpStart)
    expect(catchStart).toBeGreaterThan(promptStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(reviewPromptSource).toContain('worktreePath')
    expect(reviewPromptSource).toContain('connectResult.sessionId')
    expect(reviewPromptSource).toContain("[{ type: 'text', text: prompt }]")
    expect(reviewPromptSource).toContain('sessionModel')
    expect(reviewPromptSource).toContain('bumpWorktreeLastMessage({ worktreeId })')
    expect(reviewPromptSource).not.toContain('window.opencodeOps.prompt')
  })

  it('keeps code review tests on opencodeApi mocks instead of window opencode globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-7/session-4/code-review.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/opencode-api'")
    expect(source).toContain('vi.mocked(opencodeApi.connect).mockResolvedValue')
    expect(source).toContain('vi.mocked(opencodeApi.prompt).mockResolvedValue')
    expect(source).toContain('expect(opencodeApi.connect).toHaveBeenCalledWith')
    expect(source).toContain('expect(opencodeApi.prompt).toHaveBeenCalledWith')
    expect(source).not.toContain('window.opencodeOps')
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
  })

  it('keeps code review tests on injected RPC mocks instead of legacy window fixtures', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-7/session-4/code-review.test.ts'),
      'utf-8'
    )

    expect(source).toContain('setRendererRpcClient({')
    expect(source).toContain("method === 'db.setting.get'")
    expect(source).toContain("method === 'gitOps.listBranchesWithStatus'")
    expect(source).not.toContain("Object.defineProperty(window, 'gitOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'fileOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'worktreeOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'projectOps'")
    expect(source).not.toContain('window.gitOps')
    expect(source).not.toContain('window.fileOps')
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.worktreeOps')
    expect(source).not.toContain('window.projectOps')
  })

  it('routes lifecycle remote branch loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useLifecycleActions.ts'),
      'utf-8'
    )
    const branchLoadStart = source.indexOf(
      '  // --- Load remote branches when worktree path changes ---'
    )
    const clearLiveStateStart = source.indexOf(
      '  // --- Clear live state when attached PR changes ---',
      branchLoadStart
    )
    const branchLoadSource = source.slice(branchLoadStart, clearLiveStateStart)

    expect(branchLoadStart).toBeGreaterThan(-1)
    expect(clearLiveStateStart).toBeGreaterThan(branchLoadStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(branchLoadSource).toContain('gitApi')
    expect(branchLoadSource).toContain('.listBranchesWithStatus(worktree.path)')
    expect(branchLoadSource).toContain('setRemoteBranches([])')
    expect(branchLoadSource).toContain(
      'result.branches.filter((b: { isRemote: boolean }) => b.isRemote)'
    )
    expect(branchLoadSource).toContain('setRemoteBranches(filtered)')
    expect(branchLoadSource).not.toContain('window.gitOps.listBranchesWithStatus')
    expect(branchLoadSource).not.toContain('unwrapEnvelope')
  })

  it('routes QuickActions generic app opening through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/QuickActions.tsx'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('await systemApi.openInApp(actionId, activePath)')
    expect(source).not.toContain('await window.systemOps.openInApp(actionId, activePath)')
  })

  it('routes QuickActions show-in-folder through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/QuickActions.tsx'),
      'utf-8'
    )
    const branchStart = source.indexOf("} else if (actionId === 'finder') {")
    const branchEnd = source.indexOf("} else if (actionId === 'terminal') {", branchStart)
    const branchSource = source.slice(branchStart, branchEnd)

    expect(branchStart).toBeGreaterThan(-1)
    expect(branchEnd).toBeGreaterThan(branchStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(branchSource).toContain('await projectApi.showInFolder(activePath)')
    expect(branchSource).not.toContain('window.projectOps.showInFolder')
    expect(branchSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes QuickActions copy-path through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/QuickActions.tsx'),
      'utf-8'
    )
    const branchStart = source.indexOf("} else if (actionId === 'copy-path') {")
    const branchEnd = source.indexOf("} else if (actionId === 'copy-branch') {", branchStart)
    const branchSource = source.slice(branchStart, branchEnd)

    expect(branchStart).toBeGreaterThan(-1)
    expect(branchEnd).toBeGreaterThan(branchStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(branchSource).toContain('await projectApi.copyToClipboard(activePath)')
    expect(branchSource).toContain('setCopied(true)')
    expect(branchSource).toContain('setTimeout(() => setCopied(false), 1500)')
    expect(branchSource).not.toContain('window.projectOps.copyToClipboard')
    expect(branchSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes QuickActions copy-branch through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/QuickActions.tsx'),
      'utf-8'
    )
    const branchStart = source.indexOf("} else if (actionId === 'copy-branch') {")
    const branchEnd = source.indexOf("} else if (actionId === 'finder') {", branchStart)
    const branchSource = source.slice(branchStart, branchEnd)

    expect(branchStart).toBeGreaterThan(-1)
    expect(branchEnd).toBeGreaterThan(branchStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(branchSource).toContain('if (!branchName) return')
    expect(branchSource).toContain('await projectApi.copyToClipboard(branchName)')
    expect(branchSource).toContain('setBranchCopied(true)')
    expect(branchSource).toContain('setTimeout(() => setBranchCopied(false), 1500)')
    expect(branchSource).not.toContain('window.projectOps.copyToClipboard')
    expect(branchSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes QuickActions Xcode workspace opening through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/QuickActions.tsx'),
      'utf-8'
    )
    const buttonEnd = source.indexOf('data-testid="quick-action-xcode"')
    const buttonStart = source.lastIndexOf('<Button', buttonEnd)
    const buttonSource = source.slice(buttonStart, buttonEnd)

    expect(buttonStart).toBeGreaterThan(-1)
    expect(buttonEnd).toBeGreaterThan(buttonStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(buttonSource).toContain('projectApi.openPath(xcworkspacePath).catch(console.error)')
    expect(buttonSource).not.toContain('window.projectOps.openPath')
    expect(buttonSource).not.toContain('then(unwrapEnvelope)')
  })

  it('routes QuickActions Xcode workspace detection through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/QuickActions.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('if (!isSwiftProject || isConnectionMode)')
    const nextEffectStart = source.indexOf('if (!isKotlinOrJava || isConnectionMode)', effectStart)
    const effectSource = source.slice(effectStart, nextEffectStart)

    expect(effectStart).toBeGreaterThan(-1)
    expect(nextEffectStart).toBeGreaterThan(effectStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(effectSource).toContain('const searchPath = activePath || selectedProject?.path')
    expect(effectSource).toContain('.findXcworkspace(searchPath)')
    expect(effectSource).toContain('.then(setXcworkspacePath)')
    expect(effectSource).toContain('.catch(() => setXcworkspacePath(null))')
    expect(effectSource).not.toContain('window.projectOps.findXcworkspace')
    expect(effectSource).not.toContain('.then(unwrapEnvelope)')
  })

  it('routes QuickActions Android Studio app opening through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/QuickActions.tsx'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain("if (openPath) void systemApi.openInApp('android-studio', openPath)")
    expect(source).not.toContain("window.systemOps.openInApp('android-studio', openPath)")
  })

  it('routes QuickActions Android project detection through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/QuickActions.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('if (!isKotlinOrJava || isConnectionMode)')
    const nextBlockStart = source.indexOf(
      'const editorLabel = EDITOR_LABELS[defaultEditor]',
      effectStart
    )
    const effectSource = source.slice(effectStart, nextBlockStart)

    expect(effectStart).toBeGreaterThan(-1)
    expect(nextBlockStart).toBeGreaterThan(effectStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(effectSource).toContain('const searchPath = activePath || selectedProject?.path')
    expect(effectSource).toContain('.isAndroidProject(searchPath)')
    expect(effectSource).toContain('.then(setIsAndroidProject)')
    expect(effectSource).toContain('.catch(() => setIsAndroidProject(false))')
    expect(effectSource).not.toContain('window.projectOps.isAndroidProject')
    expect(effectSource).not.toContain('.then(unwrapEnvelope)')
  })

  it('routes QuickActions worktree open-in-editor through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/QuickActions.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleOpenInEditor = useCallback(async () => {')
    const handlerEnd = source.indexOf('const handleAction = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(handlerSource).toContain('await worktreeApi.openInEditor(activePath)')
    expect(handlerSource).not.toContain('window.worktreeOps.openInEditor')
  })

  it('routes QuickActions connection open-in-editor through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/QuickActions.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleOpenInEditor = useCallback(async () => {')
    const handlerEnd = source.indexOf('const handleAction = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(handlerSource).toContain('if (isConnectionMode) {')
    expect(handlerSource).toContain('await connectionApi.openInEditor(activePath)')
    expect(handlerSource).not.toContain('window.connectionOps.openInEditor')
  })

  it('routes QuickActions open-in-terminal through settingsApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/QuickActions.tsx'),
      'utf-8'
    )
    const terminalBranchStart = source.indexOf("} else if (actionId === 'terminal') {")
    const nextBranchStart = source.indexOf('} else {', terminalBranchStart)
    const terminalBranchSource = source.slice(terminalBranchStart, nextBranchStart)

    expect(terminalBranchStart).toBeGreaterThan(-1)
    expect(nextBranchStart).toBeGreaterThan(terminalBranchStart)
    expect(source).toContain("import { settingsApi } from '@/api/settings-api'")
    expect(terminalBranchSource).toContain('const result = await settingsApi.openWithTerminal(')
    expect(terminalBranchSource).toContain('activePath')
    expect(terminalBranchSource).toContain('defaultTerminal')
    expect(terminalBranchSource).toContain(
      "defaultTerminal === 'custom' ? customTerminalCommand : undefined"
    )
    expect(terminalBranchSource).toContain('if (!result.success) throw new Error(result.error ??')
    expect(terminalBranchSource).not.toContain('window.settingsOps.openWithTerminal')
    expect(terminalBranchSource).not.toContain('unwrapEnvelope')
  })

  it('removes the old settings:openWithTerminal IPC adapter after settings terminal open RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const settingsHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/settings-openers.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('settings:openWithTerminal'")
    expect(settingsHandlersSource).not.toContain("defineHandler('settings:openWithTerminal'")
  })

  it('removes the old settings:getAll IPC adapter after settings get-all RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const settingsHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/settings-openers.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('settings:getAll'")
    expect(settingsHandlersSource).not.toContain("defineHandler('settings:getAll'")
  })

  it('removes the old settings:updated IPC listener after settings update WebSocket migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const settingsEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/settings-events.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.on('settings:updated'")
    expect(preloadSource).not.toContain("ipcRenderer.removeListener('settings:updated'")
    expect(settingsEventsSource).not.toContain('webContents.send(SETTINGS_UPDATED_CHANNEL')
    expect(settingsEventsSource).not.toContain('BrowserWindow')
    expect(settingsEventsSource).not.toContain('webContents')
    expect(settingsEventsSource).not.toContain('window:')
    expect(settingsEventsSource).toContain('publishDesktopBackendEvent(SETTINGS_UPDATED_CHANNEL')
  })

  it('routes auto-update available subscription through updaterApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useAutoUpdate.ts'),
      'utf-8'
    )
    const subscriptionStart = source.indexOf('updaterApi.onUpdateAvailable((data) => {')
    const subscriptionEnd = source.indexOf('// No update available', subscriptionStart)
    const subscriptionSource = source.slice(subscriptionStart, subscriptionEnd)

    expect(subscriptionStart).toBeGreaterThan(-1)
    expect(subscriptionEnd).toBeGreaterThan(subscriptionStart)
    expect(source).toContain("import { updaterApi } from '@/api/updater-api'")
    expect(subscriptionSource).toContain('const { skippedUpdateVersion, updateSetting }')
    expect(subscriptionSource).toContain('versionRef.current = data.version')
    expect(subscriptionSource).toContain('createElement(UpdateAvailableToast, {')
    expect(subscriptionSource).toContain('onDownload: () => {')
    expect(subscriptionSource).toContain('updaterApi.downloadUpdate().catch(() => {})')
    expect(subscriptionSource).not.toContain('window.updaterOps.onUpdateAvailable')
  })

  it('routes auto-update download action through updaterApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useAutoUpdate.ts'),
      'utf-8'
    )
    const downloadStart = source.indexOf('onDownload: () => {')
    const laterStart = source.indexOf('onLater: () => {', downloadStart)
    const downloadSource = source.slice(downloadStart, laterStart)

    expect(downloadStart).toBeGreaterThan(-1)
    expect(laterStart).toBeGreaterThan(downloadStart)
    expect(source).toContain("import { updaterApi } from '@/api/updater-api'")
    expect(downloadSource).toContain('updaterApi.downloadUpdate().catch(() => {})')
    expect(downloadSource).toContain('progressToastId.current = sonnerToast.custom(')
    expect(downloadSource).toContain('createElement(UpdateProgressToast, {')
    expect(downloadSource).not.toContain('window.updaterOps.downloadUpdate')
    expect(downloadSource).not.toContain('.then(unwrapEnvelope)')
  })

  it('routes auto-update not-available subscription through updaterApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useAutoUpdate.ts'),
      'utf-8'
    )
    const subscriptionStart = source.indexOf('// No update available')
    const subscriptionEnd = source.indexOf('// Download progress', subscriptionStart)
    const subscriptionSource = source.slice(subscriptionStart, subscriptionEnd)

    expect(subscriptionStart).toBeGreaterThan(-1)
    expect(subscriptionEnd).toBeGreaterThan(subscriptionStart)
    expect(source).toContain("import { updaterApi } from '@/api/updater-api'")
    expect(subscriptionSource).toContain('updaterApi.onUpdateNotAvailable((data) => {')
    expect(subscriptionSource).toContain('if (data.isManualCheck) {')
    expect(subscriptionSource).toContain("toast.info('You\\u2019re up to date', {")
    expect(subscriptionSource).toContain(
      'description: `Hive v${data.version} is the latest version`'
    )
    expect(subscriptionSource).not.toContain('window.updaterOps.onUpdateNotAvailable')
  })

  it('routes auto-update progress subscription through updaterApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useAutoUpdate.ts'),
      'utf-8'
    )
    const subscriptionStart = source.indexOf('// Download progress')
    const subscriptionEnd = source.indexOf('// Update downloaded', subscriptionStart)
    const subscriptionSource = source.slice(subscriptionStart, subscriptionEnd)

    expect(subscriptionStart).toBeGreaterThan(-1)
    expect(subscriptionEnd).toBeGreaterThan(subscriptionStart)
    expect(source).toContain("import { updaterApi } from '@/api/updater-api'")
    expect(subscriptionSource).toContain('updaterApi.onProgress((data) => {')
    expect(subscriptionSource).toContain('if (progressToastId.current == null) return')
    expect(subscriptionSource).toContain('createElement(UpdateProgressToast, {')
    expect(subscriptionSource).toContain('version: versionRef.current')
    expect(subscriptionSource).toContain('percent: data.percent')
    expect(subscriptionSource).toContain('{ id: progressToastId.current, duration: Infinity }')
    expect(subscriptionSource).not.toContain('window.updaterOps.onProgress')
  })

  it('routes auto-update downloaded subscription through updaterApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useAutoUpdate.ts'),
      'utf-8'
    )
    const subscriptionStart = source.indexOf('// Update downloaded')
    const subscriptionEnd = source.indexOf('// Error', subscriptionStart)
    const subscriptionSource = source.slice(subscriptionStart, subscriptionEnd)

    expect(subscriptionStart).toBeGreaterThan(-1)
    expect(subscriptionEnd).toBeGreaterThan(subscriptionStart)
    expect(source).toContain("import { updaterApi } from '@/api/updater-api'")
    expect(subscriptionSource).toContain('updaterApi.onUpdateDownloaded((data) => {')
    expect(subscriptionSource).toContain('sonnerToast.dismiss(progressToastId.current)')
    expect(subscriptionSource).toContain('progressToastId.current = null')
    expect(subscriptionSource).toContain(
      'toast.success(`Update v${data.version} ready to install`, {'
    )
    expect(subscriptionSource).toContain("label: 'Restart to Update'")
    expect(subscriptionSource).not.toContain('window.updaterOps.onUpdateDownloaded')
  })

  it('routes auto-update restart action through updaterApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useAutoUpdate.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf("label: 'Restart to Update'")
    const actionEnd = source.indexOf('// Error', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(source).toContain("import { updaterApi } from '@/api/updater-api'")
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
    expect(actionSource).toContain('onClick: () => {')
    expect(actionSource).toContain('updaterApi.installUpdate().catch(() => {})')
    expect(actionSource).not.toContain('window.updaterOps.installUpdate')
    expect(actionSource).not.toContain('.then(unwrapEnvelope)')
  })

  it('routes auto-update error subscription through updaterApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useAutoUpdate.ts'),
      'utf-8'
    )
    const subscriptionStart = source.indexOf('// Error')
    const subscriptionEnd = source.indexOf('return () => {', subscriptionStart)
    const subscriptionSource = source.slice(subscriptionStart, subscriptionEnd)

    expect(subscriptionStart).toBeGreaterThan(-1)
    expect(subscriptionEnd).toBeGreaterThan(subscriptionStart)
    expect(source).toContain("import { updaterApi } from '@/api/updater-api'")
    expect(subscriptionSource).toContain('updaterApi.onError((data) => {')
    expect(subscriptionSource).toContain('sonnerToast.dismiss(progressToastId.current)')
    expect(subscriptionSource).toContain('progressToastId.current = null')
    expect(subscriptionSource).toContain("toast.error('Update check failed', {")
    expect(subscriptionSource).toContain('description: data.message')
    expect(subscriptionSource).not.toContain('window.updaterOps.onError')
  })

  it('does not guard auto-update setup on the updaterOps compatibility global', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useAutoUpdate.ts'),
      'utf-8'
    )
    const effectStart = source.indexOf('useEffect(() => {')
    const setupStart = source.indexOf('const cleanups: (() => void)[] = []', effectStart)
    const setupSource = source.slice(effectStart, setupStart)

    expect(effectStart).toBeGreaterThan(-1)
    expect(setupStart).toBeGreaterThan(effectStart)
    expect(source).toContain("import { updaterApi } from '@/api/updater-api'")
    expect(setupSource).not.toContain('window.updaterOps')
    expect(source).not.toContain('if (!window.updaterOps) return')
  })

  it('routes Anthropic account email loading through accountApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useAccountStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf('fetchEmail: async (provider: UsageProvider) => {')
    const branchStart = source.indexOf("if (provider === 'anthropic') {", actionStart)
    const branchEnd = source.indexOf('      } else {', branchStart)
    const branchSource = source.slice(branchStart, branchEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(branchStart).toBeGreaterThan(actionStart)
    expect(branchEnd).toBeGreaterThan(branchStart)
    expect(source).toContain("import { accountApi } from '@/api/account-api'")
    expect(branchSource).toContain('const email = await accountApi.getClaudeEmail()')
    expect(branchSource).toContain('set({ anthropicEmail: email })')
    expect(branchSource).not.toContain('window.accountOps.getClaudeEmail()')
    expect(branchSource).not.toContain('unwrapEnvelope(await window.accountOps.getClaudeEmail())')
  })

  it('removes the old account:getClaudeEmail IPC adapter after Anthropic account email RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const accountHandlersSource = readOptionalSource('../../src/main/ipc/account-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('account:getClaudeEmail')")
    expect(accountHandlersSource).not.toContain("defineHandler('account:getClaudeEmail',")
    expect(accountHandlersSource).not.toContain("accountFailed('account:getClaudeEmail',")
  })

  it('routes OpenAI account email loading through accountApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useAccountStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf('fetchEmail: async (provider: UsageProvider) => {')
    const branchStart = source.indexOf('      } else {', actionStart)
    const branchEnd = source.indexOf('    } catch {', branchStart)
    const branchSource = source.slice(branchStart, branchEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(branchStart).toBeGreaterThan(actionStart)
    expect(branchEnd).toBeGreaterThan(branchStart)
    expect(source).toContain("import { accountApi } from '@/api/account-api'")
    expect(branchSource).toContain('const email = await accountApi.getOpenAIEmail()')
    expect(branchSource).toContain('set({ openaiEmail: email })')
    expect(branchSource).not.toContain('window.accountOps.getOpenAIEmail()')
    expect(branchSource).not.toContain('unwrapEnvelope(await window.accountOps.getOpenAIEmail())')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('removes the old account:getOpenAIEmail IPC adapter after OpenAI account email RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const accountHandlersSource = readOptionalSource('../../src/main/ipc/account-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('account:getOpenAIEmail')")
    expect(accountHandlersSource).not.toContain("defineHandler('account:getOpenAIEmail',")
    expect(accountHandlersSource).not.toContain("accountFailed('account:getOpenAIEmail',")
  })

  it('routes usage saved-account loading through accountApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useUsageStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf('loadSavedAccounts: async (provider?: UsageProvider) => {')
    const actionEnd = source.indexOf('  refreshAllForProvider:', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(source).toContain("import { accountApi } from '@/api/account-api'")
    expect(actionSource).toContain('const accounts = await accountApi.listSaved(provider)')
    expect(actionSource).not.toContain('window.accountOps.listSaved')
    expect(actionSource).not.toContain('unwrapEnvelope(await window.accountOps.listSaved')
  })

  it('removes the old account:listSaved IPC adapter after saved-account loading RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const accountHandlersSource = readOptionalSource('../../src/main/ipc/account-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('account:listSaved'")
    expect(accountHandlersSource).not.toContain("defineHandler('account:listSaved',")
    expect(accountHandlersSource).not.toContain("accountFailed('account:listSaved',")
  })

  it('routes usage provider refresh through usageApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useUsageStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      'refreshAllForProvider: async (provider: UsageProvider, excludeAccountIds?: string[]) => {'
    )
    const actionEnd = source.indexOf('  refreshSavedAccount:', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(source).toContain("import { usageApi } from '@/api/usage-api'")
    expect(actionSource).toContain(
      'await usageApi.refreshAllForProvider(provider, excludeAccountIds)'
    )
    expect(actionSource).toContain('await get().loadSavedAccounts(provider)')
    expect(actionSource).not.toContain('window.usageOps.refreshAllForProvider')
    expect(actionSource).not.toContain('unwrapEnvelope(await window.usageOps.refreshAllForProvider')
  })

  it('removes the old usage:refreshAllForProvider IPC adapter after provider usage refresh RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const usageHandlersSource = readOptionalSource('../../src/main/ipc/usage-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('usage:refreshAllForProvider'")
    expect(usageHandlersSource).not.toContain("defineHandler('usage:refreshAllForProvider',")
    expect(usageHandlersSource).not.toContain("usageFailed('usage:refreshAllForProvider',")
  })

  it('routes usage saved-account refresh through usageApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useUsageStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      'refreshSavedAccount: async (id: string, opts?: { userInitiated?: boolean }) => {'
    )
    const actionEnd = source.indexOf('  removeSavedAccount:', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(source).toContain("import { usageApi } from '@/api/usage-api'")
    expect(actionSource).toContain('await usageApi.fetchForAccount(id, userInitiated)')
    expect(actionSource).toContain('.loadSavedAccounts(provider)')
    expect(actionSource).not.toContain('window.usageOps.fetchForAccount')
    expect(actionSource).not.toContain('unwrapEnvelope(await window.usageOps.fetchForAccount')
  })

  it('removes the old usage:fetchForAccount IPC adapter after saved account usage refresh RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const usageHandlersSource = readOptionalSource('../../src/main/ipc/usage-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('usage:fetchForAccount'")
    expect(usageHandlersSource).not.toContain("defineHandler('usage:fetchForAccount',")
    expect(usageHandlersSource).not.toContain("usageFailed('usage:fetchForAccount',")
  })

  it('routes usage saved-account removal through accountApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useUsageStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf('removeSavedAccount: async (id: string) => {')
    const actionEnd = source.indexOf('  switchAccount:', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(source).toContain("import { accountApi } from '@/api/account-api'")
    expect(actionSource).toContain('await accountApi.removeSaved(id)')
    expect(actionSource).toContain('.loadSavedAccounts(provider)')
    expect(actionSource).not.toContain('window.accountOps.removeSaved')
    expect(actionSource).not.toContain('unwrapEnvelope(await window.accountOps.removeSaved')
  })

  it('removes the old account:removeSaved IPC adapter after saved-account removal RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const accountHandlersSource = readOptionalSource('../../src/main/ipc/account-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('account:removeSaved'")
    expect(accountHandlersSource).not.toContain("defineHandler('account:removeSaved',")
    expect(accountHandlersSource).not.toContain("accountFailed('account:removeSaved',")
  })

  it('removes the old empty accountOps preload global exposure', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const accountOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('accountOps'")
    expect(preloadSource).not.toContain('window.accountOps = accountOps')
    expect(preloadTypesSource).not.toContain('accountOps:')
    expect(preloadTypesSource).not.toContain('getClaudeEmail: ()')
    expect(preloadTypesSource).not.toContain('getOpenAIEmail: ()')
    expect(preloadTypesSource).not.toContain('removeSaved: (accountId: string)')
  })

  it('removes the old empty account IPC handler registrar after account RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')

    expect(fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/account-handlers.ts'))).toBe(
      false
    )
    expect(mainSource).not.toContain('registerAccountHandlers')
    expect(ipcIndexSource).not.toContain('registerAccountHandlers')
    expect(ipcIndexSource).not.toContain('./account-handlers')
  })

  it('routes usage provider Anthropic fetch through usageApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useUsageStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      'fetchUsageForProvider: async (provider: UsageProvider) => {'
    )
    const branchStart = source.indexOf("if (provider === 'anthropic') {", actionStart)
    const branchEnd = source.indexOf('    } else {', branchStart)
    const branchSource = source.slice(branchStart, branchEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(branchStart).toBeGreaterThan(actionStart)
    expect(branchEnd).toBeGreaterThan(branchStart)
    expect(source).toContain("import { usageApi } from '@/api/usage-api'")
    expect(branchSource).toContain('const result = await usageApi.fetch()')
    expect(branchSource).toContain('anthropicUsage: result.data ?? null')
    expect(branchSource).toContain('anthropicLastRetryAfter: null')
    expect(branchSource).toContain('.loadSavedAccounts(provider)')
    expect(branchSource).not.toContain('window.usageOps.fetch()')
    expect(branchSource).not.toContain('unwrapEnvelope(await window.usageOps.fetch())')
  })

  it('removes the old usage:fetch IPC adapter after Anthropic usage fetch RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const usageHandlersSource = readOptionalSource('../../src/main/ipc/usage-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('usage:fetch')")
    expect(usageHandlersSource).not.toContain("defineHandler('usage:fetch',")
    expect(usageHandlersSource).not.toContain("usageFailed('usage:fetch',")
  })

  it('routes usage provider OpenAI fetch through usageApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useUsageStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      'fetchUsageForProvider: async (provider: UsageProvider) => {'
    )
    const branchStart = source.indexOf('    } else {', actionStart)
    const branchEnd = source.indexOf('  forceRefreshProvider:', branchStart)
    const branchSource = source.slice(branchStart, branchEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(branchStart).toBeGreaterThan(actionStart)
    expect(branchEnd).toBeGreaterThan(branchStart)
    expect(source).toContain("import { usageApi } from '@/api/usage-api'")
    expect(branchSource).toContain('const result = await usageApi.fetchOpenai()')
    expect(branchSource).toContain('openaiUsage: result.data ?? null')
    expect(branchSource).toContain('.loadSavedAccounts(provider)')
    expect(branchSource).not.toContain('window.usageOps.fetchOpenai()')
    expect(branchSource).not.toContain('unwrapEnvelope(await window.usageOps.fetchOpenai())')
  })

  it('removes the old usage:fetchOpenai IPC adapter after OpenAI usage fetch RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const usageHandlersSource = readOptionalSource('../../src/main/ipc/usage-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('usage:fetchOpenai')")
    expect(usageHandlersSource).not.toContain("defineHandler('usage:fetchOpenai',")
    expect(usageHandlersSource).not.toContain("usageFailed('usage:fetchOpenai',")
  })

  it('routes usage force-refresh Anthropic fetch through usageApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useUsageStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf('forceRefreshProvider: async (provider: UsageProvider) => {')
    const branchStart = source.indexOf("if (provider === 'anthropic') {", actionStart)
    const branchEnd = source.indexOf('    } else {', branchStart)
    const branchSource = source.slice(branchStart, branchEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(branchStart).toBeGreaterThan(actionStart)
    expect(branchEnd).toBeGreaterThan(branchStart)
    expect(source).toContain("import { usageApi } from '@/api/usage-api'")
    expect(branchSource).toContain('const result = await usageApi.fetch()')
    expect(branchSource).toContain('anthropicUsage: result.data ?? null')
    expect(branchSource).toContain('anthropicLastRetryAfter: null')
    expect(branchSource).toContain('.loadSavedAccounts(provider)')
    expect(branchSource).not.toContain('window.usageOps.fetch()')
    expect(branchSource).not.toContain('unwrapEnvelope(await window.usageOps.fetch())')
  })

  it('routes usage force-refresh OpenAI fetch through usageApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useUsageStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf('forceRefreshProvider: async (provider: UsageProvider) => {')
    const branchStart = source.indexOf('    } else {', actionStart)
    const branchEnd = source.indexOf('  setActiveProvider:', branchStart)
    const branchSource = source.slice(branchStart, branchEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(branchStart).toBeGreaterThan(actionStart)
    expect(branchEnd).toBeGreaterThan(branchStart)
    expect(source).toContain("import { usageApi } from '@/api/usage-api'")
    expect(branchSource).toContain('const result = await usageApi.fetchOpenai()')
    expect(branchSource).toContain('openaiUsage: result.data ?? null')
    expect(branchSource).toContain('.loadSavedAccounts(provider)')
    expect(branchSource).not.toContain('window.usageOps.fetchOpenai()')
    expect(branchSource).not.toContain('unwrapEnvelope(await window.usageOps.fetchOpenai())')
  })

  it('removes the old empty usageOps preload global exposure', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const usageOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('usageOps'")
    expect(preloadSource).not.toContain('window.usageOps = usageOps')
    expect(preloadTypesSource).not.toContain('usageOps:')
    expect(preloadTypesSource).not.toContain('fetchOpenai: ()')
    expect(preloadTypesSource).not.toContain('fetchForAccount: (')
    expect(preloadTypesSource).not.toContain('refreshAllForProvider: (')
  })

  it('removes the old empty usage IPC handler registrar after usage RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')

    expect(fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/usage-handlers.ts'))).toBe(
      false
    )
    expect(mainSource).not.toContain('registerUsageHandlers')
    expect(ipcIndexSource).not.toContain('registerUsageHandlers')
    expect(ipcIndexSource).not.toContain('./usage-handlers')
  })

  it('routes settings update version load through updaterApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/settings/SettingsUpdates.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('useEffect(() => {')
    const effectEnd = source.indexOf('const handleCheckForUpdates', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(source).toContain("import { updaterApi } from '@/api/updater-api'")
    expect(effectSource).toContain('updaterApi')
    expect(effectSource).toContain('.getVersion()')
    expect(effectSource).toContain('.then(setVersion)')
    expect(effectSource).toContain('.catch(() => {})')
    expect(effectSource).not.toContain('window.updaterOps')
    expect(effectSource).not.toContain('unwrapEnvelope')
  })

  it('routes settings manual update check through updaterApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/settings/SettingsUpdates.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf(
      'const handleCheckForUpdates = async (): Promise<void> => {'
    )
    const handlerEnd = source.indexOf('return (', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { updaterApi } from '@/api/updater-api'")
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
    expect(handlerSource).toContain('setChecking(true)')
    expect(handlerSource).toContain('await updaterApi.checkForUpdate({ manual: true })')
    expect(handlerSource).toContain('setTimeout(() => setChecking(false), 2000)')
    expect(handlerSource).not.toContain('window.updaterOps')
    expect(handlerSource).not.toContain('unwrapEnvelope')
  })

  it('routes updaterApi checkForUpdate directly through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/updater-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'checkForUpdate: async (options?: { manual?: boolean }): Promise<void> =>'
    )
    const functionEnd = source.indexOf('  setChannel: async', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<void>('updaterOps.checkForUpdate', options ?? {})"
    )
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.checkForUpdate')
    expect(functionSource).not.toContain('bridge.checkForUpdate')
    expect(functionSource).not.toContain('window.updaterOps')
  })

  it('removes the old updater:check main IPC handler', () => {
    const updaterHandlersSource = readOptionalSource('../../src/main/ipc/updater-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(updaterHandlersSource).not.toContain("defineHandler('updater:check'")
    expect(updaterHandlersSource).not.toContain("updaterFailed('updater:check'")
    expect(preloadSource).not.toContain("'updater:check'")
  })

  it('keeps updater startup and update events free of BrowserWindow event dependencies', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const updaterSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/updater.ts'),
      'utf-8'
    )
    const updaterEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/updater-events.ts'),
      'utf-8'
    )
    const updaterEventsTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/updater-events.test.ts'),
      'utf-8'
    )

    expect(mainSource).toContain('updaterService.init()')
    expect(mainSource).not.toContain('updaterService.init(mainWindow)')
    expect(updaterSource).toContain('import { app } from')
    expect(updaterSource).not.toContain('BrowserWindow')
    expect(updaterSource).toContain('init(): void {')
    expect(updaterSource).not.toContain('init(mainWindow')
    expect(updaterSource).not.toContain('webContents.send')
    expect(updaterSource).toContain('emitUpdaterChecking()')
    expect(updaterSource).toContain('emitUpdaterAvailable({')
    expect(updaterSource).toContain('emitUpdaterProgress({')
    expect(updaterSource).toContain('emitUpdaterDownloaded({')
    expect(updaterSource).toContain('emitUpdaterError({')

    expect(updaterEventsSource).toContain('publishDesktopBackendEvent')
    expect(updaterEventsSource).not.toContain('BrowserWindow')
    expect(updaterEventsSource).not.toContain('webContents.send')
    expect(updaterEventsTestSource).toContain(
      "it('publishes the updater checking event without a window dependency'"
    )
  })

  it('keeps notification navigation on backend events without retaining BrowserWindow types', () => {
    const notificationServiceSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/notification-service.ts'),
      'utf-8'
    )
    const notificationEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/notification-events.ts'),
      'utf-8'
    )
    const notificationEventsTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/notification-events.test.ts'),
      'utf-8'
    )

    expect(notificationServiceSource).toContain("import { Notification, app } from 'electron'")
    expect(notificationServiceSource).toContain('interface NotificationWindowTarget')
    expect(notificationServiceSource).toContain(
      'private mainWindow: NotificationWindowTarget | null = null'
    )
    expect(notificationServiceSource).toContain(
      'setMainWindow(window: NotificationWindowTarget): void'
    )
    expect(notificationServiceSource).not.toContain('BrowserWindow')
    expect(notificationServiceSource).not.toContain('webContents.send')
    expect(notificationServiceSource).toContain('emitNotificationNavigate({')

    expect(notificationEventsSource).toContain('publishDesktopBackendEvent')
    expect(notificationEventsSource).toContain('NOTIFICATION_NAVIGATE_CHANNEL')
    expect(notificationEventsSource).not.toContain('BrowserWindow')
    expect(notificationEventsSource).not.toContain('webContents.send')
    expect(notificationEventsTestSource).toContain(
      "it('publishes notification navigation payloads through the backend event stream'"
    )
  })

  it('removes the old updaterOps.checkForUpdate preload compatibility method and desktop check IPC path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const desktopHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterOpsSource = getPreloadUpdaterOpsSource(preloadSource)
    expect(updaterOpsSource).not.toContain('checkForUpdate:')
    expect(updaterOpsSource).not.toContain('desktopBridge.checkForUpdate')
    expect(preloadSource).not.toContain('DESKTOP_CHECK_FOR_UPDATE_CHANNEL')
    expect(preloadSource).not.toContain('desktop:checkForUpdate')
    expect(preloadSource).not.toContain(
      'checkForUpdate: (options?: { manual?: boolean }): Promise<void> =>'
    )
    expect(preloadSource).not.toContain('ipcRenderer.invoke(DESKTOP_CHECK_FOR_UPDATE_CHANNEL')
    expect(preloadTypesSource).not.toContain(
      'checkForUpdate: (options?: { manual?: boolean }) => Promise<void>'
    )
    expect(desktopBridgeSource).not.toContain('DESKTOP_CHECK_FOR_UPDATE_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:checkForUpdate')
    expect(desktopBridgeSource).not.toContain('DesktopCheckForUpdateOptions')
    expect(desktopHandlersSource).not.toContain('DESKTOP_CHECK_FOR_UPDATE_CHANNEL')
    expect(desktopHandlersSource).not.toContain('readonly checkForUpdate')
    expect(desktopHandlersSource).not.toContain('handlers.checkForUpdate')
    expect(desktopBridgeTypeSource).not.toContain('readonly checkForUpdate?:')
    expect(desktopBridgeTypeSource).not.toContain('DesktopCheckForUpdateOptions')
  })

  it('routes updaterApi getVersion directly through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/updater-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/updater-ops.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('getVersion: async (): Promise<string> =>')
    const functionEnd = source.indexOf('  checkForUpdate:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)
    const serverFunctionStart = serverSource.indexOf(
      'const requestUpdaterGetVersion = async (): Promise<string> =>'
    )
    const serverFunctionEnd = serverSource.indexOf(
      '\nconst requestUpdaterDownloadUpdate',
      serverFunctionStart
    )
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<string>('updaterOps.getVersion', {})"
    )
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.getUpdaterVersion')
    expect(functionSource).not.toContain('bridge.getUpdaterVersion')
    expect(functionSource).not.toContain('window.updaterOps')
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(serverFunctionSource).toContain("const command = 'updaterGetVersion'")
    expect(serverFunctionSource).toContain("makeDesktopCommandRequest(id, command)")
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('app.getVersion')
  })

  it('removes the old updaterOps.getVersion preload compatibility method and desktop version IPC path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const desktopHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterOpsSource = getPreloadUpdaterOpsSource(preloadSource)
    expect(updaterOpsSource).not.toContain('getVersion:')
    expect(updaterOpsSource).not.toContain('desktopBridge.getUpdaterVersion')
    expect(preloadSource).not.toContain('DESKTOP_GET_UPDATER_VERSION_CHANNEL')
    expect(preloadSource).not.toContain('desktop:getUpdaterVersion')
    expect(preloadSource).not.toContain('getUpdaterVersion: (): Promise<string> =>')
    expect(preloadSource).not.toContain('ipcRenderer.invoke(DESKTOP_GET_UPDATER_VERSION_CHANNEL)')
    expect(preloadTypesSource).not.toContain('getUpdaterVersion: () => Promise<string>')
    expect(desktopBridgeSource).not.toContain('DESKTOP_GET_UPDATER_VERSION_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:getUpdaterVersion')
    expect(desktopHandlersSource).not.toContain('DESKTOP_GET_UPDATER_VERSION_CHANNEL')
    expect(desktopHandlersSource).not.toContain('readonly getUpdaterVersion')
    expect(desktopHandlersSource).not.toContain('handlers.getUpdaterVersion()')
    expect(desktopBridgeTypeSource).not.toContain('readonly getUpdaterVersion?:')
  })

  it('removes the old updater:getVersion main IPC handler', () => {
    const updaterHandlersSource = readOptionalSource('../../src/main/ipc/updater-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(updaterHandlersSource).not.toContain("defineHandler('updater:getVersion'")
    expect(updaterHandlersSource).not.toContain("updaterFailed('updater:getVersion'")
    expect(preloadSource).not.toContain("'updater:getVersion'")
  })

  it('routes updaterApi setChannel directly through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/updater-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      "setChannel: async (channel: 'stable' | 'canary'): Promise<void> =>"
    )
    const functionEnd = source.indexOf('  downloadUpdate: async', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<void>('updaterOps.setChannel', { channel })"
    )
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.setUpdateChannel')
    expect(functionSource).not.toContain('bridge.setUpdateChannel')
    expect(functionSource).not.toContain('window.updaterOps')
  })

  it('removes the old updaterOps.setChannel preload compatibility method and desktop channel IPC path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const desktopHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterOpsSource = getPreloadUpdaterOpsSource(preloadSource)
    expect(updaterOpsSource).not.toContain('setChannel:')
    expect(updaterOpsSource).not.toContain('desktopBridge.setUpdateChannel')
    expect(preloadSource).not.toContain('DESKTOP_SET_UPDATE_CHANNEL_CHANNEL')
    expect(preloadSource).not.toContain('desktop:setUpdateChannel')
    expect(preloadSource).not.toContain(
      "setUpdateChannel: (channel: 'stable' | 'canary'): Promise<void> =>"
    )
    expect(preloadSource).not.toContain('ipcRenderer.invoke(DESKTOP_SET_UPDATE_CHANNEL_CHANNEL')
    expect(preloadTypesSource).not.toContain(
      "setUpdateChannel: (channel: 'stable' | 'canary') => Promise<void>"
    )
    expect(desktopBridgeSource).not.toContain('DESKTOP_SET_UPDATE_CHANNEL_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:setUpdateChannel')
    expect(desktopBridgeSource).not.toContain('DesktopUpdateChannel')
    expect(desktopHandlersSource).not.toContain('DESKTOP_SET_UPDATE_CHANNEL_CHANNEL')
    expect(desktopHandlersSource).not.toContain('readonly setUpdateChannel')
    expect(desktopHandlersSource).not.toContain('handlers.setUpdateChannel')
    expect(desktopBridgeTypeSource).not.toContain('readonly setUpdateChannel?:')
    expect(desktopBridgeTypeSource).not.toContain('DesktopUpdateChannel')
  })

  it('removes the old updater:setChannel main IPC handler', () => {
    const updaterHandlersSource = readOptionalSource('../../src/main/ipc/updater-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(updaterHandlersSource).not.toContain("defineHandler('updater:setChannel'")
    expect(updaterHandlersSource).not.toContain("updaterFailed('updater:setChannel'")
    expect(preloadSource).not.toContain("'updater:setChannel'")
  })

  it('routes updaterApi downloadUpdate directly through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/updater-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('downloadUpdate: async (): Promise<void> =>')
    const functionEnd = source.indexOf('  installUpdate: async', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<void>('updaterOps.downloadUpdate', {})"
    )
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.downloadUpdate')
    expect(functionSource).not.toContain('bridge.downloadUpdate')
    expect(functionSource).not.toContain('window.updaterOps')
  })

  it('removes the old updaterOps.downloadUpdate preload compatibility method and desktop download IPC path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const desktopHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterOpsSource = getPreloadUpdaterOpsSource(preloadSource)
    expect(updaterOpsSource).not.toContain('downloadUpdate:')
    expect(updaterOpsSource).not.toContain('desktopBridge.downloadUpdate')
    expect(preloadSource).not.toContain('DESKTOP_DOWNLOAD_UPDATE_CHANNEL')
    expect(preloadSource).not.toContain('desktop:downloadUpdate')
    expect(preloadSource).not.toContain('downloadUpdate: (): Promise<void> =>')
    expect(preloadSource).not.toContain('ipcRenderer.invoke(DESKTOP_DOWNLOAD_UPDATE_CHANNEL')
    expect(preloadTypesSource).not.toContain('downloadUpdate: () => Promise<void>')
    expect(desktopBridgeSource).not.toContain('DESKTOP_DOWNLOAD_UPDATE_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:downloadUpdate')
    expect(desktopHandlersSource).not.toContain('DESKTOP_DOWNLOAD_UPDATE_CHANNEL')
    expect(desktopHandlersSource).not.toContain('readonly downloadUpdate')
    expect(desktopHandlersSource).not.toContain('handlers.downloadUpdate')
    expect(desktopBridgeTypeSource).not.toContain('readonly downloadUpdate?:')
  })

  it('removes the old updater:download main IPC handler', () => {
    const updaterHandlersSource = readOptionalSource('../../src/main/ipc/updater-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(updaterHandlersSource).not.toContain("defineHandler('updater:download'")
    expect(updaterHandlersSource).not.toContain("updaterFailed('updater:download'")
    expect(preloadSource).not.toContain("'updater:download'")
  })

  it('routes updaterApi installUpdate directly through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/updater-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('installUpdate: async (): Promise<void> =>')
    const functionEnd = source.indexOf('  onChecking:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<void>('updaterOps.installUpdate', {})"
    )
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.installUpdate')
    expect(functionSource).not.toContain('bridge.installUpdate')
    expect(functionSource).not.toContain('window.updaterOps')
  })

  it('removes the old updaterOps.installUpdate preload compatibility method and desktop install IPC path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const desktopHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/desktop-bridge-handlers.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterOpsSource = getPreloadUpdaterOpsSource(preloadSource)
    expect(updaterOpsSource).not.toContain('installUpdate:')
    expect(updaterOpsSource).not.toContain('desktopBridge.installUpdate')
    expect(preloadSource).not.toContain('DESKTOP_INSTALL_UPDATE_CHANNEL')
    expect(preloadSource).not.toContain('desktop:installUpdate')
    expect(preloadSource).not.toContain('installUpdate: (): Promise<void> =>')
    expect(preloadSource).not.toContain('ipcRenderer.invoke(DESKTOP_INSTALL_UPDATE_CHANNEL')
    expect(preloadTypesSource).not.toContain('installUpdate: () => Promise<void>')
    expect(desktopBridgeSource).not.toContain('DESKTOP_INSTALL_UPDATE_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:installUpdate')
    expect(desktopHandlersSource).not.toContain('DESKTOP_INSTALL_UPDATE_CHANNEL')
    expect(desktopHandlersSource).not.toContain('readonly installUpdate')
    expect(desktopHandlersSource).not.toContain('handlers.installUpdate')
    expect(desktopBridgeTypeSource).not.toContain('readonly installUpdate?:')
  })

  it('removes the old updater:install main IPC handler', () => {
    const updaterHandlersSource = readOptionalSource('../../src/main/ipc/updater-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(updaterHandlersSource).not.toContain("defineHandler('updater:install'")
    expect(updaterHandlersSource).not.toContain("updaterFailed('updater:install'")
    expect(preloadSource).not.toContain("'updater:install'")
  })

  it('removes the old empty updater IPC handler registrar after updater RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')

    expect(fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/updater-handlers.ts'))).toBe(
      false
    )
    expect(
      fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/__tests__/updater-handlers.test.ts'))
    ).toBe(false)
    expect(mainSource).not.toContain('registerUpdaterHandlers')
    expect(ipcIndexSource).not.toContain('registerUpdaterHandlers')
    expect(ipcIndexSource).not.toContain('./updater-handlers')
  })

  it('routes updaterApi checking events directly through WebSocket after desktop bridge event migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/updater-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('onChecking: (callback: () => void): (() => void) =>')
    const functionEnd = source.indexOf('  onUpdateAvailable:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain('UPDATER_CHECKING_CHANNEL')
    expect(functionSource).toContain(
      'return getRendererRpcClient().subscribe(UPDATER_CHECKING_CHANNEL, () => {'
    )
    expect(functionSource).toContain('callback()')
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.onUpdaterChecking')
    expect(functionSource).not.toContain('bridge.onUpdaterChecking')
    expect(functionSource).not.toContain('window.updaterOps')
  })

  it('removes the old updaterOps.onChecking preload compatibility listener and desktop checking event path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/updater-events.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterOpsSource = getPreloadUpdaterOpsSource(preloadSource)
    expect(updaterOpsSource).not.toContain('onChecking:')
    expect(updaterOpsSource).not.toContain('desktopBridge.onUpdaterChecking')
    expect(preloadSource).not.toContain('DESKTOP_UPDATER_CHECKING_CHANNEL')
    expect(preloadSource).not.toContain('desktop:updaterChecking')
    expect(preloadSource).not.toContain(
      'onUpdaterChecking: (listener: () => void): (() => void) =>'
    )
    expect(preloadSource).not.toContain('ipcRenderer.on(DESKTOP_UPDATER_CHECKING_CHANNEL')
    expect(preloadSource).not.toContain('removeListener(DESKTOP_UPDATER_CHECKING_CHANNEL')
    expect(preloadTypesSource).not.toContain(
      'onUpdaterChecking: (listener: () => void) => () => void'
    )
    expect(desktopBridgeSource).not.toContain('DESKTOP_UPDATER_CHECKING_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:updaterChecking')
    expect(updaterEventsSource).not.toContain('DESKTOP_UPDATER_CHECKING_CHANNEL')
    expect(updaterEventsSource).not.toContain('webContents.send(UPDATER_CHECKING_CHANNEL')
    expect(updaterEventsSource).not.toContain('window.webContents.send(UPDATER_CHECKING_CHANNEL')
    expect(updaterEventsSource).toContain('emitUpdaterChecking = (): void')
    expect(updaterEventsSource).not.toContain('emitUpdaterChecking = (_window')
    expect(desktopBridgeTypeSource).not.toContain('readonly onUpdaterChecking?:')
  })

  it('routes updaterApi update-available events directly through WebSocket after desktop bridge event migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/updater-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'onUpdateAvailable: (callback: (data: UpdaterAvailablePayload) => void): (() => void) =>'
    )
    const functionEnd = source.indexOf('  onUpdateNotAvailable:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain('UPDATER_AVAILABLE_CHANNEL')
    expect(functionSource).toContain(
      'return getRendererRpcClient().subscribe(UPDATER_AVAILABLE_CHANNEL, (event: ServerEvent) => {'
    )
    expect(functionSource).toContain('if (isUpdaterAvailablePayload(event.payload)) {')
    expect(functionSource).toContain('callback(event.payload)')
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.onUpdaterAvailable')
    expect(functionSource).not.toContain('bridge.onUpdaterAvailable')
    expect(functionSource).not.toContain('window.updaterOps')
  })

  it('removes the old updaterOps.onUpdateAvailable preload compatibility listener and desktop available event path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/updater-events.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterOpsSource = getPreloadUpdaterOpsSource(preloadSource)
    expect(updaterOpsSource).not.toContain('onUpdateAvailable:')
    expect(updaterOpsSource).not.toContain('desktopBridge.onUpdaterAvailable')
    expect(preloadSource).not.toContain('DESKTOP_UPDATER_AVAILABLE_CHANNEL')
    expect(preloadSource).not.toContain('desktop:updaterAvailable')
    expect(preloadSource).not.toContain('onUpdaterAvailable: (')
    expect(preloadSource).not.toContain('ipcRenderer.on(DESKTOP_UPDATER_AVAILABLE_CHANNEL')
    expect(preloadSource).not.toContain('removeListener(DESKTOP_UPDATER_AVAILABLE_CHANNEL')
    expect(preloadTypesSource).not.toContain('onUpdaterAvailable: (')
    expect(desktopBridgeSource).not.toContain('DESKTOP_UPDATER_AVAILABLE_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:updaterAvailable')
    expect(updaterEventsSource).not.toContain('DESKTOP_UPDATER_AVAILABLE_CHANNEL')
    expect(updaterEventsSource).not.toContain('webContents.send(UPDATER_AVAILABLE_CHANNEL')
    expect(updaterEventsSource).not.toContain('window.webContents.send(UPDATER_AVAILABLE_CHANNEL')
    expect(updaterEventsSource).toContain(
      'emitUpdaterAvailable = (payload: UpdaterAvailablePayload): void'
    )
    expect(updaterEventsSource).not.toContain('emitUpdaterAvailable = (_window')
    expect(desktopBridgeTypeSource).not.toContain('readonly onUpdaterAvailable?:')
  })

  it('routes updaterApi update-not-available events directly through WebSocket after desktop bridge event migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/updater-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'onUpdateNotAvailable: (callback: (data: UpdaterNotAvailablePayload) => void): (() => void) =>'
    )
    const functionEnd = source.indexOf('  onProgress:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain('UPDATER_NOT_AVAILABLE_CHANNEL')
    expect(functionSource).toContain(
      'return getRendererRpcClient().subscribe(UPDATER_NOT_AVAILABLE_CHANNEL, (event: ServerEvent) => {'
    )
    expect(functionSource).toContain('if (isUpdaterNotAvailablePayload(event.payload)) {')
    expect(functionSource).toContain('callback(event.payload)')
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.onUpdaterNotAvailable')
    expect(functionSource).not.toContain('bridge.onUpdaterNotAvailable')
    expect(functionSource).not.toContain('window.updaterOps')
  })

  it('removes the old updaterOps.onUpdateNotAvailable preload compatibility listener and desktop not-available event path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/updater-events.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterOpsSource = getPreloadUpdaterOpsSource(preloadSource)
    expect(updaterOpsSource).not.toContain('onUpdateNotAvailable:')
    expect(updaterOpsSource).not.toContain('desktopBridge.onUpdaterNotAvailable')
    expect(preloadSource).not.toContain('DESKTOP_UPDATER_NOT_AVAILABLE_CHANNEL')
    expect(preloadSource).not.toContain('desktop:updaterNotAvailable')
    expect(preloadSource).not.toContain('onUpdaterNotAvailable: (')
    expect(preloadSource).not.toContain('ipcRenderer.on(DESKTOP_UPDATER_NOT_AVAILABLE_CHANNEL')
    expect(preloadSource).not.toContain('removeListener(DESKTOP_UPDATER_NOT_AVAILABLE_CHANNEL')
    expect(preloadTypesSource).not.toContain('onUpdaterNotAvailable: (')
    expect(desktopBridgeSource).not.toContain('DESKTOP_UPDATER_NOT_AVAILABLE_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:updaterNotAvailable')
    expect(updaterEventsSource).not.toContain('DESKTOP_UPDATER_NOT_AVAILABLE_CHANNEL')
    expect(updaterEventsSource).not.toContain('webContents.send(UPDATER_NOT_AVAILABLE_CHANNEL')
    expect(updaterEventsSource).not.toContain(
      'window.webContents.send(UPDATER_NOT_AVAILABLE_CHANNEL'
    )
    expect(updaterEventsSource).toContain(
      'emitUpdaterNotAvailable = (payload: UpdaterNotAvailablePayload): void'
    )
    expect(updaterEventsSource).not.toContain('emitUpdaterNotAvailable = (_window')
    expect(desktopBridgeTypeSource).not.toContain('readonly onUpdaterNotAvailable?:')
  })

  it('routes updaterApi progress events directly through WebSocket after desktop bridge event migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/updater-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'onProgress: (callback: (data: UpdaterProgressPayload) => void): (() => void) =>'
    )
    const functionEnd = source.indexOf('  onUpdateDownloaded:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain('UPDATER_PROGRESS_CHANNEL')
    expect(functionSource).toContain(
      'return getRendererRpcClient().subscribe(UPDATER_PROGRESS_CHANNEL, (event: ServerEvent) => {'
    )
    expect(functionSource).toContain('if (isUpdaterProgressPayload(event.payload)) {')
    expect(functionSource).toContain('callback(event.payload)')
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.onUpdaterProgress')
    expect(functionSource).not.toContain('bridge.onUpdaterProgress')
    expect(functionSource).not.toContain('window.updaterOps')
  })

  it('removes the old updaterOps.onProgress preload compatibility listener and desktop progress event path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/updater-events.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterOpsSource = getPreloadUpdaterOpsSource(preloadSource)
    expect(updaterOpsSource).not.toContain('onProgress:')
    expect(updaterOpsSource).not.toContain('desktopBridge.onUpdaterProgress')
    expect(preloadSource).not.toContain('DESKTOP_UPDATER_PROGRESS_CHANNEL')
    expect(preloadSource).not.toContain('desktop:updaterProgress')
    expect(preloadSource).not.toContain('onUpdaterProgress: (')
    expect(preloadSource).not.toContain('ipcRenderer.on(DESKTOP_UPDATER_PROGRESS_CHANNEL')
    expect(preloadSource).not.toContain('removeListener(DESKTOP_UPDATER_PROGRESS_CHANNEL')
    expect(preloadTypesSource).not.toContain('onUpdaterProgress: (')
    expect(desktopBridgeSource).not.toContain('DESKTOP_UPDATER_PROGRESS_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:updaterProgress')
    expect(updaterEventsSource).not.toContain('DESKTOP_UPDATER_PROGRESS_CHANNEL')
    expect(updaterEventsSource).not.toContain('webContents.send(UPDATER_PROGRESS_CHANNEL')
    expect(updaterEventsSource).not.toContain('window.webContents.send(UPDATER_PROGRESS_CHANNEL')
    expect(updaterEventsSource).toContain(
      'emitUpdaterProgress = (payload: UpdaterProgressPayload): void'
    )
    expect(updaterEventsSource).not.toContain('emitUpdaterProgress = (_window')
    expect(desktopBridgeTypeSource).not.toContain('readonly onUpdaterProgress?:')
  })

  it('removes the old updaterOps.onUpdateDownloaded preload compatibility listener and desktop downloaded event path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/updater-events.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterOpsSource = getPreloadUpdaterOpsSource(preloadSource)
    expect(updaterOpsSource).not.toContain('onUpdateDownloaded:')
    expect(updaterOpsSource).not.toContain('desktopBridge.onUpdaterDownloaded')
    expect(preloadSource).not.toContain('DESKTOP_UPDATER_DOWNLOADED_CHANNEL')
    expect(preloadSource).not.toContain('desktop:updaterDownloaded')
    expect(preloadSource).not.toContain('onUpdaterDownloaded: (')
    expect(preloadSource).not.toContain('ipcRenderer.on(DESKTOP_UPDATER_DOWNLOADED_CHANNEL')
    expect(preloadSource).not.toContain('removeListener(DESKTOP_UPDATER_DOWNLOADED_CHANNEL')
    expect(preloadTypesSource).not.toContain('onUpdaterDownloaded: (')
    expect(desktopBridgeSource).not.toContain('DESKTOP_UPDATER_DOWNLOADED_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:updaterDownloaded')
    expect(updaterEventsSource).not.toContain('DESKTOP_UPDATER_DOWNLOADED_CHANNEL')
    expect(updaterEventsSource).not.toContain('webContents.send(UPDATER_DOWNLOADED_CHANNEL')
    expect(updaterEventsSource).not.toContain('window.webContents.send(UPDATER_DOWNLOADED_CHANNEL')
    expect(updaterEventsSource).toContain(
      'emitUpdaterDownloaded = (payload: UpdaterDownloadedPayload): void'
    )
    expect(updaterEventsSource).not.toContain('emitUpdaterDownloaded = (_window')
    expect(desktopBridgeTypeSource).not.toContain('readonly onUpdaterDownloaded?:')
  })

  it('removes the old updaterOps.onError preload compatibility listener and desktop error event path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/updater-events.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const updaterOpsSource = getPreloadUpdaterOpsSource(preloadSource)
    expect(updaterOpsSource).not.toContain('onError:')
    expect(updaterOpsSource).not.toContain('desktopBridge.onUpdaterError')
    expect(preloadSource).not.toContain('DESKTOP_UPDATER_ERROR_CHANNEL')
    expect(preloadSource).not.toContain('desktop:updaterError')
    expect(preloadSource).not.toContain('onUpdaterError: (')
    expect(preloadSource).not.toContain('ipcRenderer.on(DESKTOP_UPDATER_ERROR_CHANNEL')
    expect(preloadSource).not.toContain('removeListener(DESKTOP_UPDATER_ERROR_CHANNEL')
    expect(preloadTypesSource).not.toContain('onUpdaterError: (')
    expect(desktopBridgeSource).not.toContain('DESKTOP_UPDATER_ERROR_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:updaterError')
    expect(updaterEventsSource).not.toContain('DESKTOP_UPDATER_ERROR_CHANNEL')
    expect(updaterEventsSource).not.toContain('webContents.send(UPDATER_ERROR_CHANNEL')
    expect(updaterEventsSource).not.toContain('window.webContents.send(UPDATER_ERROR_CHANNEL')
    expect(updaterEventsSource).toContain('emitUpdaterError = (payload: UpdaterErrorPayload): void')
    expect(updaterEventsSource).not.toContain('emitUpdaterError = (_window')
    expect(desktopBridgeTypeSource).not.toContain('readonly onUpdaterError?:')
  })

  it('removes the old empty updaterOps preload global exposure', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const updaterOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('updaterOps'")
    expect(preloadSource).not.toContain('window.updaterOps = updaterOps')
    expect(preloadTypesSource).not.toContain('updaterOps:')
    expect(preloadTypesSource).not.toContain('checkForUpdate: (options?: { manual?: boolean })')
    expect(preloadTypesSource).not.toContain('downloadUpdate: () => Promise<Envelope')
    expect(preloadTypesSource).not.toContain('onUpdateDownloaded: (')
  })

  it('routes updaterApi downloaded events directly through WebSocket after desktop bridge event migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/updater-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'onUpdateDownloaded: (callback: (data: UpdaterDownloadedPayload) => void): (() => void) =>'
    )
    const functionEnd = source.indexOf('  onError:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain('UPDATER_DOWNLOADED_CHANNEL')
    expect(functionSource).toContain(
      'return getRendererRpcClient().subscribe(UPDATER_DOWNLOADED_CHANNEL, (event: ServerEvent) => {'
    )
    expect(functionSource).toContain('if (isUpdaterDownloadedPayload(event.payload)) {')
    expect(functionSource).toContain('callback(event.payload)')
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.onUpdaterDownloaded')
    expect(functionSource).not.toContain('bridge.onUpdaterDownloaded')
    expect(functionSource).not.toContain('window.updaterOps')
  })

  it('routes updaterApi error events directly through WebSocket after desktop bridge event migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/updater-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'onError: (callback: (data: UpdaterErrorPayload) => void): (() => void) =>'
    )
    const functionEnd = source.indexOf('\n}', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain('UPDATER_ERROR_CHANNEL')
    expect(functionSource).toContain(
      'return getRendererRpcClient().subscribe(UPDATER_ERROR_CHANNEL, (event: ServerEvent) => {'
    )
    expect(functionSource).toContain('if (isUpdaterErrorPayload(event.payload)) {')
    expect(functionSource).toContain('callback(event.payload)')
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.onUpdaterError')
    expect(functionSource).not.toContain('bridge.onUpdaterError')
    expect(functionSource).not.toContain('window.updaterOps')
  })

  it('routes settings update channel side effect through updaterApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSettingsStore.ts'),
      'utf-8'
    )
    const sideEffectStart = source.indexOf("if (key === 'updateChannel') {")
    const sideEffectEnd = source.indexOf("if (key === 'pet'", sideEffectStart)
    const sideEffectSource = source.slice(sideEffectStart, sideEffectEnd)

    expect(sideEffectStart).toBeGreaterThan(-1)
    expect(sideEffectEnd).toBeGreaterThan(sideEffectStart)
    expect(source).toContain("import { updaterApi } from '@/api/updater-api'")
    expect(sideEffectSource).toContain(
      "updaterApi.setChannel(value as AppSettings['updateChannel']).catch(() => {})"
    )
    expect(sideEffectSource).not.toContain('window.updaterOps')
    expect(sideEffectSource).not.toContain('unwrapEnvelope')
  })

  it('routes settings pet update side effect through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSettingsStore.ts'),
      'utf-8'
    )
    const sideEffectStart = source.indexOf("if (key === 'pet') {")
    const sideEffectEnd = source.indexOf("if (key === 'boardMode')", sideEffectStart)
    const sideEffectSource = source.slice(sideEffectStart, sideEffectEnd)

    expect(sideEffectStart).toBeGreaterThan(-1)
    expect(sideEffectEnd).toBeGreaterThan(sideEffectStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(sideEffectSource).toContain('const pet = value as PetSettings')
    expect(sideEffectSource).toContain('petApi.updateSettings(pet)')
    expect(sideEffectSource).not.toContain('window.petOps.updateSettings')
    expect(sideEffectSource).not.toContain('window.petOps?.updateSettings')
  })

  it('removes the old pet:update-settings IPC sender after pet settings RPC migration', () => {
    const petHandlersSource = readOptionalSource('../../src/main/ipc/pet-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.send('pet:update-settings'")
    expect(preloadSource).not.toContain('ipcRenderer.send("pet:update-settings"')
    expect(petHandlersSource).not.toContain("ipcMain.on('pet:update-settings'")
    expect(petHandlersSource).not.toContain('ipcMain.on("pet:update-settings"')
  })

  it('routes settings pet show side effect through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSettingsStore.ts'),
      'utf-8'
    )
    const sideEffectStart = source.indexOf("if (key === 'pet') {")
    const sideEffectEnd = source.indexOf("if (key === 'boardMode')", sideEffectStart)
    const sideEffectSource = source.slice(sideEffectStart, sideEffectEnd)

    expect(sideEffectStart).toBeGreaterThan(-1)
    expect(sideEffectEnd).toBeGreaterThan(sideEffectStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(sideEffectSource).toContain('if (pet.enabled) {')
    expect(sideEffectSource).toContain('petApi.show().catch(() => {})')
    expect(sideEffectSource).not.toMatch(/window\.petOps[\s\S]*?\.show\(\)/)
    expect(sideEffectSource).not.toContain('window.petOps?.show')
  })

  it('removes the old pet:show IPC adapter after pet show RPC migration', () => {
    const petHandlersSource = readOptionalSource('../../src/main/ipc/pet-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('pet:show'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("pet:show"')
    expect(petHandlersSource).not.toContain("defineHandler('pet:show'")
    expect(petHandlersSource).not.toContain('defineHandler("pet:show"')
  })

  it('removes the old pet:hide IPC adapter after pet hide RPC migration', () => {
    const petHandlersSource = readOptionalSource('../../src/main/ipc/pet-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('pet:hide'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("pet:hide"')
    expect(petHandlersSource).not.toContain("defineHandler('pet:hide'")
    expect(petHandlersSource).not.toContain('defineHandler("pet:hide"')
  })

  it('routes settings pet hide side effect through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSettingsStore.ts'),
      'utf-8'
    )
    const sideEffectStart = source.indexOf("if (key === 'pet') {")
    const sideEffectEnd = source.indexOf("if (key === 'boardMode')", sideEffectStart)
    const sideEffectSource = source.slice(sideEffectStart, sideEffectEnd)

    expect(sideEffectStart).toBeGreaterThan(-1)
    expect(sideEffectEnd).toBeGreaterThan(sideEffectStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(sideEffectSource).toContain('} else {')
    expect(sideEffectSource).toContain('petApi.hide().catch(() => {})')
    expect(sideEffectSource).not.toMatch(/window\.petOps[\s\S]*?\.hide\(\)/)
    expect(sideEffectSource).not.toContain('window.petOps?.hide')
  })

  it('routes SettingsPet show button through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/settings/SettingsPet.tsx'),
      'utf-8'
    )
    const buttonStart = source.indexOf('data-testid="show-pet-button"')
    const buttonSearchStart = source.lastIndexOf('{pet.enabled && (', buttonStart)
    const buttonEnd = source.indexOf('</Button>', buttonStart)
    const buttonSource = source.slice(buttonSearchStart, buttonEnd)

    expect(buttonStart).toBeGreaterThan(-1)
    expect(buttonSearchStart).toBeGreaterThan(-1)
    expect(buttonEnd).toBeGreaterThan(buttonStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(buttonSource).toContain('petApi.show().catch(() => {})')
    expect(buttonSource).toContain('data-testid="show-pet-button"')
    expect(buttonSource).not.toContain('window.petOps')
    expect(buttonSource).not.toContain('unwrapEnvelope')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('keeps petOps.show no-transport fallback from importing the Electron pet window', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/pet-ops.ts'),
      'utf-8'
    )
    const helperStart = source.indexOf('const requestPetCommand = (')
    const helperEnd = source.indexOf('\nconst requestPublishPetStatusCommand', helperStart)
    const helperSource = source.slice(helperStart, helperEnd)
    const noTransportStart = helperSource.indexOf("if (typeof send !== 'function') {")
    const showNoopIndex = helperSource.indexOf(
      "if (command === 'showPet') return Promise.resolve()",
      noTransportStart
    )

    expect(helperStart).toBeGreaterThan(-1)
    expect(helperEnd).toBeGreaterThan(helperStart)
    expect(noTransportStart).toBeGreaterThan(-1)
    expect(showNoopIndex).toBeGreaterThan(noTransportStart)
    expect(helperSource).not.toContain("return import('../../../main/services/pet-window')")
    expect(helperSource).toContain("if (command === 'showPet') return makeDesktopCommandRequest(id, 'showPet')")
  })

  it('keeps petOps.hide no-transport fallback from importing the Electron pet window', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/pet-ops.ts'),
      'utf-8'
    )
    const helperStart = source.indexOf('const requestPetCommand = (')
    const helperEnd = source.indexOf('\nconst requestPublishPetStatusCommand', helperStart)
    const helperSource = source.slice(helperStart, helperEnd)
    const noTransportStart = helperSource.indexOf("if (typeof send !== 'function') {")
    const hideNoopIndex = helperSource.indexOf(
      "if (command === 'hidePet') return Promise.resolve()",
      noTransportStart
    )

    expect(helperStart).toBeGreaterThan(-1)
    expect(helperEnd).toBeGreaterThan(helperStart)
    expect(noTransportStart).toBeGreaterThan(-1)
    expect(hideNoopIndex).toBeGreaterThan(noTransportStart)
    expect(helperSource).not.toContain("return import('../../../main/services/pet-window')")
    expect(helperSource).toContain("return makeDesktopCommandRequest(id, 'hidePet')")
    expect(helperSource).not.toContain('destroyPetWindow')
  })

  it('keeps petOps.beginPointerInteraction no-transport fallback from importing the Electron pet window', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/pet-ops.ts'),
      'utf-8'
    )
    const helperStart = source.indexOf('const requestPetCommand = (')
    const helperEnd = source.indexOf('\nconst requestPublishPetStatusCommand', helperStart)
    const helperSource = source.slice(helperStart, helperEnd)
    const noTransportStart = helperSource.indexOf("if (typeof send !== 'function') {")
    const beginNoopIndex = helperSource.indexOf(
      "if (command === 'beginPetPointerInteraction') return Promise.resolve()",
      noTransportStart
    )

    expect(helperStart).toBeGreaterThan(-1)
    expect(helperEnd).toBeGreaterThan(helperStart)
    expect(noTransportStart).toBeGreaterThan(-1)
    expect(beginNoopIndex).toBeGreaterThan(noTransportStart)
    expect(helperSource).not.toContain("return import('../../../main/services/pet-window')")
    expect(helperSource).toContain(
      "return makeDesktopCommandRequest(id, 'beginPetPointerInteraction')"
    )
    expect(helperSource).not.toContain('beginPetPointerInteraction,')
  })

  it('keeps petOps.endPointerInteraction no-transport fallback from importing the Electron pet window', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/pet-ops.ts'),
      'utf-8'
    )
    const helperStart = source.indexOf('const requestPetCommand = (')
    const helperEnd = source.indexOf('\nconst requestPublishPetStatusCommand', helperStart)
    const helperSource = source.slice(helperStart, helperEnd)
    const noTransportStart = helperSource.indexOf("if (typeof send !== 'function') {")
    const endNoopIndex = helperSource.indexOf(
      "if (command === 'endPetPointerInteraction') return Promise.resolve()",
      noTransportStart
    )

    expect(helperStart).toBeGreaterThan(-1)
    expect(helperEnd).toBeGreaterThan(helperStart)
    expect(noTransportStart).toBeGreaterThan(-1)
    expect(endNoopIndex).toBeGreaterThan(noTransportStart)
    expect(helperSource).not.toContain("return import('../../../main/services/pet-window')")
    expect(helperSource).toContain(
      "return makeDesktopCommandRequest(id, 'endPetPointerInteraction')"
    )
    expect(helperSource).not.toContain('endPetPointerInteraction,')
  })

  it('keeps petOps.publishStatus no-transport fallback from importing the Electron pet window', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/pet-ops.ts'),
      'utf-8'
    )
    const helperStart = source.indexOf(
      'const requestPublishPetStatusCommand = (payload: PublishPetStatusPayload): Promise<void> => {'
    )
    const helperEnd = source.indexOf('\nconst requestSetPetIgnoreMouseCommand', helperStart)
    const helperSource = source.slice(helperStart, helperEnd)
    const noTransportStart = helperSource.indexOf("if (typeof send !== 'function') {")
    const noTransportEnd = helperSource.indexOf('\n  }\n', noTransportStart)
    const noTransportSource = helperSource.slice(noTransportStart, noTransportEnd)

    expect(helperStart).toBeGreaterThan(-1)
    expect(helperEnd).toBeGreaterThan(helperStart)
    expect(noTransportStart).toBeGreaterThan(-1)
    expect(noTransportSource).toContain('return Promise.resolve()')
    expect(noTransportSource).not.toContain("import('../../../main/services/pet-window')")
    expect(noTransportSource).not.toContain('forwardStatusToPet')
    expect(helperSource).toContain("const command = 'publishPetStatus'")
    expect(helperSource).toContain('makeDesktopCommandRequest(id, command, payload)')
  })

  it('keeps petOps.setIgnoreMouse no-transport fallback from importing the Electron pet window', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/pet-ops.ts'),
      'utf-8'
    )
    const helperStart = source.indexOf(
      'const requestSetPetIgnoreMouseCommand = (payload: SetPetIgnoreMousePayload): Promise<void> => {'
    )
    const helperEnd = source.indexOf('\nconst requestMovePetCommand', helperStart)
    const helperSource = source.slice(helperStart, helperEnd)
    const noTransportStart = helperSource.indexOf("if (typeof send !== 'function') {")
    const noTransportEnd = helperSource.indexOf('\n  }\n', noTransportStart)
    const noTransportSource = helperSource.slice(noTransportStart, noTransportEnd)

    expect(helperStart).toBeGreaterThan(-1)
    expect(helperEnd).toBeGreaterThan(helperStart)
    expect(noTransportStart).toBeGreaterThan(-1)
    expect(noTransportSource).toContain('return Promise.resolve()')
    expect(noTransportSource).not.toContain("import('../../../main/services/pet-window')")
    expect(noTransportSource).not.toContain('setPetIgnoreMouseEvents')
    expect(helperSource).toContain("const command = 'setPetIgnoreMouse'")
    expect(helperSource).toContain('makeDesktopCommandRequest(id, command, payload)')
  })

  it('keeps petOps.move no-transport fallback from importing the Electron pet window', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/pet-ops.ts'),
      'utf-8'
    )
    const helperStart = source.indexOf(
      'const requestMovePetCommand = (payload: MovePetPayload): Promise<void> => {'
    )
    const helperEnd = source.indexOf('\nconst requestFocusMainFromPetCommand', helperStart)
    const helperSource = source.slice(helperStart, helperEnd)
    const noTransportStart = helperSource.indexOf("if (typeof send !== 'function') {")
    const noTransportEnd = helperSource.indexOf('\n  }\n', noTransportStart)
    const noTransportSource = helperSource.slice(noTransportStart, noTransportEnd)

    expect(helperStart).toBeGreaterThan(-1)
    expect(helperEnd).toBeGreaterThan(helperStart)
    expect(noTransportStart).toBeGreaterThan(-1)
    expect(noTransportSource).toContain('return Promise.resolve()')
    expect(noTransportSource).not.toContain("import('../../../main/services/pet-window')")
    expect(noTransportSource).not.toContain('movePetWindow')
    expect(helperSource).toContain("const command = 'movePet'")
    expect(helperSource).toContain('makeDesktopCommandRequest(id, command, payload)')
  })

  it('keeps petOps.focusMain no-transport fallback from importing the Electron pet window', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/pet-ops.ts'),
      'utf-8'
    )
    const helperStart = source.indexOf(
      'const requestFocusMainFromPetCommand = (payload: FocusMainFromPetPayload): Promise<void> => {'
    )
    const helperEnd = source.indexOf('\nconst requestGetPetConfigCommand', helperStart)
    const helperSource = source.slice(helperStart, helperEnd)
    const noTransportStart = helperSource.indexOf("if (typeof send !== 'function') {")
    const noTransportEnd = helperSource.indexOf('\n  }\n', noTransportStart)
    const noTransportSource = helperSource.slice(noTransportStart, noTransportEnd)

    expect(helperStart).toBeGreaterThan(-1)
    expect(helperEnd).toBeGreaterThan(helperStart)
    expect(noTransportStart).toBeGreaterThan(-1)
    expect(noTransportSource).toContain('return Promise.resolve()')
    expect(noTransportSource).not.toContain("import('../../../main/services/pet-window')")
    expect(noTransportSource).not.toContain('focusMainWindowFromPet')
    expect(helperSource).toContain("const command = 'focusMainFromPet'")
    expect(helperSource).toContain('makeDesktopCommandRequest(id, command, payload)')
  })

  it('keeps petOps.getConfig no-transport fallback from importing the Electron pet window', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/pet-ops.ts'),
      'utf-8'
    )
    const defaultSettingsStart = source.indexOf('const DEFAULT_PET_SETTINGS: PetSettings = {')
    const defaultStart = source.indexOf('const makeDefaultPetConfigResult = (): GetPetConfigResult => ({')
    const helperStart = source.indexOf('const requestGetPetConfigCommand = (): Promise<GetPetConfigResult> => {')
    const helperEnd = source.indexOf('\nconst requestGetCurrentPetStatusCommand', helperStart)
    const helperSource = source.slice(helperStart, helperEnd)
    const noTransportStart = helperSource.indexOf("if (typeof send !== 'function') {")
    const noTransportEnd = helperSource.indexOf('\n  }\n', noTransportStart)
    const noTransportSource = helperSource.slice(noTransportStart, noTransportEnd)

    expect(defaultSettingsStart).toBeGreaterThan(-1)
    expect(source.slice(defaultSettingsStart, defaultStart)).toContain("petId: 'bee'")
    expect(defaultStart).toBeGreaterThan(defaultSettingsStart)
    expect(source.slice(defaultStart, helperStart)).toContain('settings: { ...fallbackPetSettings }')
    expect(source.slice(defaultStart, helperStart)).toContain("working: 'assets/honey-bee.lottie'")
    expect(helperStart).toBeGreaterThan(defaultStart)
    expect(helperEnd).toBeGreaterThan(helperStart)
    expect(noTransportStart).toBeGreaterThan(-1)
    expect(noTransportSource).toContain('return Promise.resolve(makeDefaultPetConfigResult())')
    expect(noTransportSource).not.toContain("import('../../../main/services/pet-window')")
    expect(noTransportSource).not.toContain('getPetConfig')
    expect(helperSource).toContain("const command = 'getPetConfig'")
    expect(helperSource).toContain('makeDesktopCommandRequest(id, command)')
  })

  it('keeps petOps.getCurrentStatus no-transport fallback from importing the Electron pet window', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/pet-ops.ts'),
      'utf-8'
    )
    const defaultStart = source.indexOf(
      'const makeDefaultPetStatusResult = (): GetCurrentPetStatusResult => ({'
    )
    const helperStart = source.indexOf(
      'const requestGetCurrentPetStatusCommand = (): Promise<GetCurrentPetStatusResult> => {'
    )
    const helperEnd = source.indexOf('\nconst requestUpdatePetSettingsCommand', helperStart)
    const helperSource = source.slice(helperStart, helperEnd)
    const noTransportStart = helperSource.indexOf("if (typeof send !== 'function') {")
    const noTransportEnd = helperSource.indexOf('\n  }\n', noTransportStart)
    const noTransportSource = helperSource.slice(noTransportStart, noTransportEnd)

    expect(defaultStart).toBeGreaterThan(-1)
    expect(source.slice(defaultStart, helperStart)).toContain("state: 'idle'")
    expect(source.slice(defaultStart, helperStart)).toContain('sourceWorktreeId: null')
    expect(helperStart).toBeGreaterThan(defaultStart)
    expect(helperEnd).toBeGreaterThan(helperStart)
    expect(noTransportStart).toBeGreaterThan(-1)
    expect(noTransportSource).toContain('return Promise.resolve(makeDefaultPetStatusResult())')
    expect(noTransportSource).not.toContain("import('../../../main/services/pet-window')")
    expect(noTransportSource).not.toContain('getCurrentPetStatus')
    expect(helperSource).toContain("const command = 'getCurrentPetStatus'")
    expect(helperSource).toContain('makeDesktopCommandRequest(id, command)')
  })

  it('keeps petOps.updateSettings no-transport fallback from importing the Electron pet window', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/pet-ops.ts'),
      'utf-8'
    )
    const fallbackStart = source.indexOf('const applyFallbackPetSettings = (partial: Partial<PetSettings>): void => {')
    const helperStart = source.indexOf(
      'const requestUpdatePetSettingsCommand = (payload: UpdatePetSettingsPayload): Promise<void> => {'
    )
    const helperEnd = source.indexOf('\nconst requestMarkPetHatchedCommand', helperStart)
    const helperSource = source.slice(helperStart, helperEnd)
    const noTransportStart = helperSource.indexOf("if (typeof send !== 'function') {")
    const noTransportEnd = helperSource.indexOf('\n  }\n', noTransportStart)
    const noTransportSource = helperSource.slice(noTransportStart, noTransportEnd)

    expect(fallbackStart).toBeGreaterThan(-1)
    expect(source.slice(fallbackStart, helperStart)).toContain(
      'fallbackPetSettings = { ...fallbackPetSettings, ...partial }'
    )
    expect(helperStart).toBeGreaterThan(fallbackStart)
    expect(helperEnd).toBeGreaterThan(helperStart)
    expect(noTransportStart).toBeGreaterThan(-1)
    expect(noTransportSource).toContain('applyFallbackPetSettings(payload)')
    expect(noTransportSource).toContain('return Promise.resolve()')
    expect(noTransportSource).not.toContain("import('../../../main/services/pet-window')")
    expect(noTransportSource).not.toContain('updatePetSettings')
    expect(noTransportSource).not.toContain('createPetWindow')
    expect(noTransportSource).not.toContain('destroyPetWindow')
    expect(helperSource).toContain("const command = 'updatePetSettings'")
    expect(helperSource).toContain('makeDesktopCommandRequest(id, command, payload)')
  })

  it('keeps petOps.markHatched no-transport fallback from importing the Electron pet window', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/pet-ops.ts'),
      'utf-8'
    )
    const fallbackStart = source.indexOf('const applyFallbackPetSettings = (partial: Partial<PetSettings>): void => {')
    const helperStart = source.indexOf('const requestMarkPetHatchedCommand = (): Promise<void> => {')
    const helperEnd = source.indexOf('\nexport const __testing', helperStart)
    const helperSource =
      helperEnd === -1 ? source.slice(helperStart) : source.slice(helperStart, helperEnd)
    const noTransportStart = helperSource.indexOf("if (typeof send !== 'function') {")
    const noTransportEnd = helperSource.indexOf('\n  }\n', noTransportStart)
    const noTransportSource = helperSource.slice(noTransportStart, noTransportEnd)

    expect(fallbackStart).toBeGreaterThan(-1)
    expect(helperStart).toBeGreaterThan(fallbackStart)
    expect(noTransportStart).toBeGreaterThan(-1)
    expect(noTransportSource).toContain('applyFallbackPetSettings({ hasHatched: true })')
    expect(noTransportSource).toContain('return Promise.resolve()')
    expect(noTransportSource).not.toContain("import('../../../main/services/pet-window')")
    expect(noTransportSource).not.toContain('persistPetSettings')
    expect(helperSource).toContain("const command = 'markPetHatched'")
    expect(helperSource).toContain('makeDesktopCommandRequest(id, command)')
  })

  it('routes reset-to-defaults pet settings update through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSettingsStore.ts'),
      'utf-8'
    )
    const resetStart = source.indexOf('resetToDefaults: () => {')
    const resetEnd = source.indexOf('loadFromDatabase: async () => {', resetStart)
    const resetSource = source.slice(resetStart, resetEnd)

    expect(resetStart).toBeGreaterThan(-1)
    expect(resetEnd).toBeGreaterThan(resetStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(resetSource).toContain('set({ ...DEFAULT_SETTINGS })')
    expect(resetSource).toContain('saveToDatabase(DEFAULT_SETTINGS)')
    expect(resetSource).toContain('petApi.updateSettings(DEFAULT_SETTINGS.pet)')
    expect(resetSource).not.toContain('window.petOps?.updateSettings')
    expect(resetSource).not.toContain('window.petOps.updateSettings')
  })

  it('routes reset-to-defaults pet hide through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSettingsStore.ts'),
      'utf-8'
    )
    const resetStart = source.indexOf('resetToDefaults: () => {')
    const resetEnd = source.indexOf('loadFromDatabase: async () => {', resetStart)
    const resetSource = source.slice(resetStart, resetEnd)

    expect(resetStart).toBeGreaterThan(-1)
    expect(resetEnd).toBeGreaterThan(resetStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(resetSource).toContain('petApi.hide().catch(() => {})')
    expect(resetSource).not.toMatch(/window\.petOps[\s\S]*?\.hide\(\)/)
    expect(resetSource).not.toContain('window.petOps?.hide')
  })

  it('routes loaded database pet settings update through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSettingsStore.ts'),
      'utf-8'
    )
    const loadStart = source.indexOf('loadFromDatabase: async () => {')
    const hasDbStart = source.indexOf('if (dbSettings) {', loadStart)
    const noDbStart = source.indexOf(
      'set({ isLoading: false, telegramConfig: telegramConfig ?? null })',
      hasDbStart
    )
    const hasDbSource = source.slice(hasDbStart, noDbStart)

    expect(loadStart).toBeGreaterThan(-1)
    expect(hasDbStart).toBeGreaterThan(loadStart)
    expect(noDbStart).toBeGreaterThan(hasDbStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(hasDbSource).toContain('petApi.updateSettings(dbSettings.pet)')
    expect(hasDbSource).not.toContain('window.petOps?.updateSettings(dbSettings.pet)')
    expect(hasDbSource).not.toContain('window.petOps.updateSettings(dbSettings.pet)')
  })

  it('routes loaded database enabled pet show through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSettingsStore.ts'),
      'utf-8'
    )
    const loadStart = source.indexOf('loadFromDatabase: async () => {')
    const hasDbStart = source.indexOf('if (dbSettings) {', loadStart)
    const noDbStart = source.indexOf(
      'set({ isLoading: false, telegramConfig: telegramConfig ?? null })',
      hasDbStart
    )
    const hasDbSource = source.slice(hasDbStart, noDbStart)

    expect(loadStart).toBeGreaterThan(-1)
    expect(hasDbStart).toBeGreaterThan(loadStart)
    expect(noDbStart).toBeGreaterThan(hasDbStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(hasDbSource).toContain('if (dbSettings.pet.enabled) {')
    expect(hasDbSource).toContain('petApi.show().catch(() => {})')
    expect(hasDbSource).not.toMatch(/window\.petOps[\s\S]*?\.show\(\)/)
    expect(hasDbSource).not.toContain('window.petOps?.show')
    expect(hasDbSource).not.toContain('unwrapEnvelope')
  })

  it('routes initial database pet settings update through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSettingsStore.ts'),
      'utf-8'
    )
    const loadStart = source.indexOf('loadFromDatabase: async () => {')
    const noDbStart = source.indexOf(
      'set({ isLoading: false, telegramConfig: telegramConfig ?? null })',
      loadStart
    )
    const loadEnd = source.indexOf('detectAvailableAgentSdks: async () => {', noDbStart)
    const noDbSource = source.slice(noDbStart, loadEnd)

    expect(loadStart).toBeGreaterThan(-1)
    expect(noDbStart).toBeGreaterThan(loadStart)
    expect(loadEnd).toBeGreaterThan(noDbStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(noDbSource).toContain('await saveToDatabase(extractSettings(get()))')
    expect(noDbSource).toContain('petApi.updateSettings(get().pet)')
    expect(noDbSource).not.toContain('window.petOps?.updateSettings(get().pet)')
    expect(noDbSource).not.toContain('window.petOps.updateSettings(get().pet)')
  })

  it('routes pet app initial config load through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/pet/PetApp.tsx'),
      'utf-8'
    )
    const loadStart = source.indexOf('    async function loadInitialState(): Promise<void> {')
    const loadEnd = source.indexOf('\n    loadInitialState().catch(console.error)', loadStart)
    const loadSource = source.slice(loadStart, loadEnd)

    expect(loadStart).toBeGreaterThan(-1)
    expect(loadEnd).toBeGreaterThan(loadStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(loadSource).toContain('const [config, currentStatus] = await Promise.all([')
    expect(loadSource).toContain('petApi.getConfig(),')
    expect(loadSource).toContain('setSettings(config.settings)')
    expect(loadSource).toContain('setPosition(config.position)')
    expect(loadSource).toContain('setHatching(!config.settings.hasHatched)')
    expect(loadSource).not.toContain('window.petOps.getConfig')
  })

  it('removes the old pet:get-config IPC invoke after pet config RPC migration', () => {
    const petHandlersSource = readOptionalSource('../../src/main/ipc/pet-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('pet:get-config'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("pet:get-config"')
    expect(petHandlersSource).not.toContain("defineHandler('pet:get-config'")
    expect(petHandlersSource).not.toContain('defineHandler("pet:get-config"')
  })

  it('routes pet app initial status load through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/pet/PetApp.tsx'),
      'utf-8'
    )
    const loadStart = source.indexOf('    async function loadInitialState(): Promise<void> {')
    const loadEnd = source.indexOf('\n    loadInitialState().catch(console.error)', loadStart)
    const loadSource = source.slice(loadStart, loadEnd)

    expect(loadStart).toBeGreaterThan(-1)
    expect(loadEnd).toBeGreaterThan(loadStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(loadSource).toContain('const [config, currentStatus] = await Promise.all([')
    expect(loadSource).toContain('petApi.getCurrentStatus()')
    expect(loadSource).toContain('setStatus(currentStatus)')
    expect(loadSource).toContain('latestStatusRef.current = currentStatus')
    expect(loadSource).not.toContain('window.petOps.getCurrentStatus')
  })

  it('removes the old pet:get-current-status IPC invoke after pet status RPC migration', () => {
    const petHandlersSource = readOptionalSource('../../src/main/ipc/pet-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('pet:get-current-status'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("pet:get-current-status"')
    expect(petHandlersSource).not.toContain("defineHandler('pet:get-current-status'")
    expect(petHandlersSource).not.toContain('defineHandler("pet:get-current-status"')
  })

  it('routes pet app status subscription through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/pet/PetApp.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('  useEffect(() => {')
    const effectEnd = source.indexOf('\n  const handleHatchComplete', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(effectSource).toContain('const cleanupStatus = petApi.onStatus((payload) => {')
    expect(effectSource).toContain('latestStatusRef.current = payload')
    expect(effectSource).toContain('setStatus(payload)')
    expect(effectSource).toContain('cleanupStatus()')
    expect(effectSource).not.toContain('window.petOps.onStatus')
  })

  it('removes the old pet:status preload IPC subscription after WebSocket migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const petWindowSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/pet-window.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.on('pet:status'")
    expect(preloadSource).not.toContain("ipcRenderer.removeListener('pet:status'")
    expect(petWindowSource).not.toContain('petWindow.webContents.send(PET_STATUS_CHANNEL')
    expect(petWindowSource).not.toContain('sendToPet(PET_STATUS_CHANNEL')
  })

  it('routes pet app settings subscription through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/pet/PetApp.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('  useEffect(() => {')
    const effectEnd = source.indexOf('\n  const handleHatchComplete', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(effectSource).toContain(
      'const cleanupSettings = petApi.onSettingsUpdated((nextSettings) => {'
    )
    expect(effectSource).toContain('setSettings(nextSettings)')
    expect(effectSource).toContain('cleanupSettings()')
    expect(effectSource).not.toContain('window.petOps.onSettingsUpdated')
  })

  it('removes the old pet:settings-updated preload IPC subscription after WebSocket migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const petWindowSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/pet-window.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.on('pet:settings-updated'")
    expect(preloadSource).not.toContain("ipcRenderer.removeListener('pet:settings-updated'")
    expect(petWindowSource).not.toContain('webContents.send(PET_SETTINGS_UPDATED_CHANNEL')
    expect(petWindowSource).not.toContain('broadcast(PET_SETTINGS_UPDATED_CHANNEL')
  })

  it('keeps pet activation tests on backend event assertions without renderer IPC mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/main/pet-window-activation.test.ts'),
      'utf-8'
    )

    expect(source).toContain('publishDesktopBackendEvent')
    expect(source).not.toContain('webContents.send')
    expect(source).not.toContain('webContents:')
  })

  it('keeps pet native window events on dedicated backend event publishers', () => {
    const petWindowSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/pet-window.ts'),
      'utf-8'
    )
    const petEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/pet-events.ts'),
      'utf-8'
    )
    const petEventsTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/pet-events.test.ts'),
      'utf-8'
    )

    expect(petWindowSource).toContain(
      "import { emitPetJumpToWorktree, emitPetSettingsUpdated, emitPetStatus } from './pet-events'"
    )
    expect(petWindowSource).toContain('emitPetStatus(payload)')
    expect(petWindowSource).toContain('emitPetSettingsUpdated(latestSettings)')
    expect(petWindowSource).toContain('emitPetJumpToWorktree(worktreeId)')
    expect(petWindowSource).not.toContain("import('../desktop/backend-manager')")
    expect(petWindowSource).not.toContain("import('../desktop/backend-event-publisher')")
    expect(petWindowSource).not.toContain('publishDesktopBackendEvent(PET_STATUS_CHANNEL')
    expect(petWindowSource).not.toContain(
      'publishDesktopBackendEvent(PET_SETTINGS_UPDATED_CHANNEL'
    )
    expect(petWindowSource).not.toContain('publishDesktopBackendEvent(PET_JUMP_TO_WORKTREE_CHANNEL')
    expect(petWindowSource).not.toContain('webContents.send')

    expect(petEventsSource).toContain('publishDesktopBackendEvent(PET_STATUS_CHANNEL, payload)')
    expect(petEventsSource).toContain(
      'publishDesktopBackendEvent(PET_SETTINGS_UPDATED_CHANNEL, settings)'
    )
    expect(petEventsSource).toContain(
      'publishDesktopBackendEvent(PET_JUMP_TO_WORKTREE_CHANNEL, { worktreeId })'
    )
    expect(petEventsSource).not.toContain('BrowserWindow')
    expect(petEventsSource).not.toContain('webContents.send')
    expect(petEventsTestSource).toContain(
      "it('publishes pet status through the backend event stream'"
    )
    expect(petEventsTestSource).toContain(
      "it('publishes pet settings updates through the backend event stream'"
    )
    expect(petEventsTestSource).toContain(
      "it('publishes pet worktree jumps through the backend event stream'"
    )
  })

  it('keeps service-level BrowserWindow usage limited to native window ownership', () => {
    const serviceDir = path.resolve(__dirname, '../../src/main/services')
    const serviceSources = fs
      .readdirSync(serviceDir)
      .filter((fileName) => fileName.endsWith('.ts') && !fileName.endsWith('.test.ts'))
      .map((fileName) => ({
        fileName,
        source: fs.readFileSync(path.join(serviceDir, fileName), 'utf-8')
      }))

    const sourcesWithBrowserWindow = serviceSources
      .filter(({ source }) => source.includes('BrowserWindow'))
      .map(({ fileName }) => fileName)
      .sort()

    expect(sourcesWithBrowserWindow).toEqual(['ghostty-service.ts', 'pet-window.ts'])

    for (const { source } of serviceSources) {
      expect(source).not.toContain('webContents.send')
      expect(source).not.toContain('mainWindow.webContents.send')
    }

    const eventPublisherFiles = [
      'app-events.ts',
      'edit-events.ts',
      'git-events.ts',
      'menu-events.ts',
      'notification-events.ts',
      'pet-events.ts',
      'settings-events.ts',
      'shortcut-events.ts',
      'updater-events.ts',
      'worktree-events.ts'
    ]

    for (const fileName of eventPublisherFiles) {
      const source = fs.readFileSync(path.join(serviceDir, fileName), 'utf-8')
      expect(source).toContain('publishDesktopBackendEvent')
      expect(source).not.toContain('BrowserWindow')
      expect(source).not.toContain('webContents.send')
    }

    const ghosttySource = fs.readFileSync(path.join(serviceDir, 'ghostty-service.ts'), 'utf-8')
    const petWindowSource = fs.readFileSync(path.join(serviceDir, 'pet-window.ts'), 'utf-8')

    expect(ghosttySource).toContain('getNativeWindowHandle()')
    expect(ghosttySource).not.toContain('publishDesktopBackendEvent')
    expect(ghosttySource).not.toContain('webContents.send')
    expect(petWindowSource).toContain('new BrowserWindow({')
    expect(petWindowSource).toContain('emitPetStatus(payload)')
    expect(petWindowSource).not.toContain('publishDesktopBackendEvent')
    expect(petWindowSource).not.toContain('webContents.send')
  })

  it('routes pet app hatch completion through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/pet/PetApp.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('  const handleHatchComplete = useCallback(() => {')
    const handlerEnd = source.indexOf('\n  const handleClick = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(handlerSource).toContain('setHatching(false)')
    expect(handlerSource).toContain(
      'setSettings((current) => (current ? { ...current, hasHatched: true } : current))'
    )
    expect(handlerSource).toContain('petApi.markHatched()')
    expect(handlerSource).not.toContain('window.petOps.markHatched')
  })

  it('removes the old pet:mark-hatched IPC sender after pet hatch RPC migration', () => {
    const petHandlersSource = readOptionalSource('../../src/main/ipc/pet-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.send('pet:mark-hatched'")
    expect(preloadSource).not.toContain('ipcRenderer.send("pet:mark-hatched"')
    expect(petHandlersSource).not.toContain("ipcMain.on('pet:mark-hatched'")
    expect(petHandlersSource).not.toContain('ipcMain.on("pet:mark-hatched"')
  })

  it('routes pet app main-window focus through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/pet/PetApp.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('  const handleClick = useCallback(')
    const handlerEnd = source.indexOf('\n  const handleContextMenu = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(handlerSource).toContain('if (wasDraggedRef.current) {')
    expect(handlerSource).toContain('wasDraggedRef.current = false')
    expect(handlerSource).toContain('petApi')
    expect(handlerSource).toContain(
      '.focusMain({ worktreeId: latestStatusRef.current.sourceWorktreeId })'
    )
    expect(handlerSource).toContain('.catch(console.error)')
    expect(handlerSource).not.toContain('window.petOps.focusMain')
    expect(handlerSource).not.toContain('unwrapEnvelope')
  })

  it('removes the old pet:focus-main IPC invoke after pet focus RPC migration', () => {
    const petHandlersSource = readOptionalSource('../../src/main/ipc/pet-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('pet:focus-main'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("pet:focus-main"')
    expect(petHandlersSource).not.toContain("defineHandler('pet:focus-main'")
    expect(petHandlersSource).not.toContain('defineHandler("pet:focus-main"')
  })

  it('routes pet app context-menu hide through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/pet/PetApp.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('  const handleContextMenu = useCallback(')
    const handlerEnd = source.indexOf('\n\n  if (!settings)', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(handlerSource).toContain('event.preventDefault()')
    expect(handlerSource).toContain('petApi.hide().catch(console.error)')
    expect(handlerSource).not.toContain('window.petOps.hide')
    expect(handlerSource).not.toContain('unwrapEnvelope')
  })

  it('routes pet drag post-click mouse-ignore restore through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/pet/usePetDrag.ts'),
      'utf-8'
    )
    const restoreStart = source.indexOf('  const restoreIgnoreMouseAfterClick = useCallback(')
    const restoreEnd = source.indexOf('\n\n  useEffect(', restoreStart)
    const restoreSource = source.slice(restoreStart, restoreEnd)

    expect(restoreStart).toBeGreaterThan(-1)
    expect(restoreEnd).toBeGreaterThan(restoreStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(restoreSource).toContain('clearRestoreIgnoreMouseTimer()')
    expect(restoreSource).toContain('window.setTimeout(() => {')
    expect(restoreSource).toContain('restoreIgnoreMouseTimerRef.current = null')
    expect(restoreSource).toContain('petApi.setIgnoreMouse(true)')
    expect(restoreSource).not.toContain('window.petOps.setIgnoreMouse(true)')
  })

  it('routes pet drag animation-frame move through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/pet/usePetDrag.ts'),
      'utf-8'
    )
    const flushStart = source.indexOf('  const flushMove = useCallback(() => {')
    const flushEnd = source.indexOf('\n\n  const onPointerDown = useCallback(', flushStart)
    const flushSource = source.slice(flushStart, flushEnd)

    expect(flushStart).toBeGreaterThan(-1)
    expect(flushEnd).toBeGreaterThan(flushStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(flushSource).toContain('if (!drag?.pending) return')
    expect(flushSource).toContain('positionRef.current = next')
    expect(flushSource).toContain('petApi.move(next)')
    expect(flushSource).not.toContain('window.petOps.move(next)')
  })

  it('routes pet drag pointer interaction start through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/pet/usePetDrag.ts'),
      'utf-8'
    )
    const pointerStart = source.indexOf('  const onPointerDown = useCallback(')
    const dragStateStart = source.indexOf('\n\n      dragRef.current = {', pointerStart)
    const pointerSetupSource = source.slice(pointerStart, dragStateStart)

    expect(pointerStart).toBeGreaterThan(-1)
    expect(dragStateStart).toBeGreaterThan(pointerStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(pointerSetupSource).toContain('event.currentTarget.setPointerCapture(event.pointerId)')
    expect(pointerSetupSource).toContain('isDraggingRef.current = true')
    expect(pointerSetupSource).toContain('wasDraggedRef.current = false')
    expect(pointerSetupSource).toContain('petApi.beginPointerInteraction()')
    expect(pointerSetupSource).not.toContain('window.petOps.beginPointerInteraction')
  })

  it('removes the old pet:begin-pointer-interaction IPC sender after pet pointer-start RPC migration', () => {
    const petHandlersSource = readOptionalSource('../../src/main/ipc/pet-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.send('pet:begin-pointer-interaction'")
    expect(preloadSource).not.toContain('ipcRenderer.send("pet:begin-pointer-interaction"')
    expect(petHandlersSource).not.toContain("ipcMain.on('pet:begin-pointer-interaction'")
    expect(petHandlersSource).not.toContain('ipcMain.on("pet:begin-pointer-interaction"')
  })

  it('routes pet drag pointer-down mouse-ignore release through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/pet/usePetDrag.ts'),
      'utf-8'
    )
    const pointerStart = source.indexOf('  const onPointerDown = useCallback(')
    const dragStateStart = source.indexOf('\n\n      dragRef.current = {', pointerStart)
    const pointerSetupSource = source.slice(pointerStart, dragStateStart)

    expect(pointerStart).toBeGreaterThan(-1)
    expect(dragStateStart).toBeGreaterThan(pointerStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(pointerSetupSource).toContain('event.currentTarget.setPointerCapture(event.pointerId)')
    expect(pointerSetupSource).toContain('petApi.beginPointerInteraction()')
    expect(pointerSetupSource).toContain('petApi.setIgnoreMouse(false)')
    expect(pointerSetupSource).not.toContain('window.petOps.setIgnoreMouse(false)')
  })

  it('routes pet drag final pending move through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/pet/usePetDrag.ts'),
      'utf-8'
    )
    const stopStart = source.indexOf('      const stopDrag = (upEvent: PointerEvent): void => {')
    const stopEnd = source.indexOf('\n\n      window.addEventListener', stopStart)
    const stopSource = source.slice(stopStart, stopEnd)

    expect(stopStart).toBeGreaterThan(-1)
    expect(stopEnd).toBeGreaterThan(stopStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(stopSource).toContain('if (drag.pending) {')
    expect(stopSource).toContain('positionRef.current = drag.pending')
    expect(stopSource).toContain('petApi.move(drag.pending)')
    expect(stopSource).not.toContain('window.petOps.move(drag.pending)')
  })

  it('removes the old pet:move IPC sender after pet move RPC migration', () => {
    const petHandlersSource = readOptionalSource('../../src/main/ipc/pet-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.send('pet:move'")
    expect(preloadSource).not.toContain('ipcRenderer.send("pet:move"')
    expect(petHandlersSource).not.toContain("ipcMain.on('pet:move'")
    expect(petHandlersSource).not.toContain('ipcMain.on("pet:move"')
  })

  it('routes pet drag pointer interaction end through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/pet/usePetDrag.ts'),
      'utf-8'
    )
    const stopStart = source.indexOf('      const stopDrag = (upEvent: PointerEvent): void => {')
    const stopEnd = source.indexOf('\n\n      window.addEventListener', stopStart)
    const stopSource = source.slice(stopStart, stopEnd)

    expect(stopStart).toBeGreaterThan(-1)
    expect(stopEnd).toBeGreaterThan(stopStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(stopSource).toContain('dragRef.current = null')
    expect(stopSource).toContain('isDraggingRef.current = false')
    expect(stopSource).toContain('petApi.endPointerInteraction()')
    expect(stopSource).toContain('restoreIgnoreMouseAfterClick()')
    expect(stopSource).not.toContain('window.petOps.endPointerInteraction')
  })

  it('removes the old pet:end-pointer-interaction IPC sender after pet pointer-end RPC migration', () => {
    const petHandlersSource = readOptionalSource('../../src/main/ipc/pet-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.send('pet:end-pointer-interaction'")
    expect(preloadSource).not.toContain('ipcRenderer.send("pet:end-pointer-interaction"')
    expect(petHandlersSource).not.toContain("ipcMain.on('pet:end-pointer-interaction'")
    expect(petHandlersSource).not.toContain('ipcMain.on("pet:end-pointer-interaction"')
  })

  it('routes pet hover mouse-enter mouse-ignore release through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/pet/usePetHover.ts'),
      'utf-8'
    )
    const enterStart = source.indexOf('  const onMouseEnter = useCallback(() => {')
    const leaveStart = source.indexOf('\n\n  const onMouseLeave = useCallback', enterStart)
    const enterSource = source.slice(enterStart, leaveStart)

    expect(enterStart).toBeGreaterThan(-1)
    expect(leaveStart).toBeGreaterThan(enterStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(enterSource).toContain('clearLeaveTimer()')
    expect(enterSource).toContain('petApi.setIgnoreMouse(false)')
    expect(enterSource).not.toContain('window.petOps.setIgnoreMouse(false)')
  })

  it('routes pet hover mouse-leave mouse-ignore restore through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/pet/usePetHover.ts'),
      'utf-8'
    )
    const leaveStart = source.indexOf('  const onMouseLeave = useCallback(() => {')
    const returnStart = source.indexOf('\n\n  return { onMouseEnter, onMouseLeave }', leaveStart)
    const leaveSource = source.slice(leaveStart, returnStart)

    expect(leaveStart).toBeGreaterThan(-1)
    expect(returnStart).toBeGreaterThan(leaveStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(leaveSource).toContain('clearLeaveTimer()')
    expect(leaveSource).toContain('leaveTimerRef.current = window.setTimeout(() => {')
    expect(leaveSource).toContain('if (!isDraggingRef.current) {')
    expect(leaveSource).toContain('petApi.setIgnoreMouse(true)')
    expect(leaveSource).toContain('}, 50)')
    expect(leaveSource).not.toContain('window.petOps.setIgnoreMouse(true)')
  })

  it('removes the old pet:set-ignore-mouse IPC sender after pet mouse-ignore RPC migration', () => {
    const petHandlersSource = readOptionalSource('../../src/main/ipc/pet-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.send('pet:set-ignore-mouse'")
    expect(preloadSource).not.toContain('ipcRenderer.send("pet:set-ignore-mouse"')
    expect(petHandlersSource).not.toContain("ipcMain.on('pet:set-ignore-mouse'")
    expect(petHandlersSource).not.toContain('ipcMain.on("pet:set-ignore-mouse"')
  })

  it('routes pet status bridge status publishing through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/pet/PetStatusBridge.tsx'),
      'utf-8'
    )
    const publishStart = source.indexOf('    const publishIfChanged = (): void => {')
    const publishEnd = source.indexOf('\n\n    publishIfChanged()', publishStart)
    const publishSource = source.slice(publishStart, publishEnd)

    expect(publishStart).toBeGreaterThan(-1)
    expect(publishEnd).toBeGreaterThan(publishStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(publishSource).toContain('const next = computeStatus()')
    expect(publishSource).toContain('if (sameStatus(lastPublishedRef.current, next)) return')
    expect(publishSource).toContain('lastPublishedRef.current = next')
    expect(publishSource).toContain('petApi.publishStatus(next)')
    expect(publishSource).not.toContain('window.petOps.publishStatus')
  })

  it('removes the old pet:publish-status IPC sender after pet status RPC migration', () => {
    const petHandlersSource = readOptionalSource('../../src/main/ipc/pet-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.send('pet:publish-status'")
    expect(preloadSource).not.toContain('ipcRenderer.send("pet:publish-status"')
    expect(petHandlersSource).not.toContain("ipcMain.on('pet:publish-status'")
    expect(petHandlersSource).not.toContain('ipcMain.on("pet:publish-status"')
  })

  it('routes pet status bridge jump subscription through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/pet/PetStatusBridge.tsx'),
      'utf-8'
    )
    const subscriptionStart = source.indexOf(
      '    const cleanupJump = petApi.onJumpToWorktree(({ worktreeId }) => {'
    )
    const subscriptionEnd = source.indexOf('\n    const cleanupSettings =', subscriptionStart)
    const subscriptionSource = source.slice(subscriptionStart, subscriptionEnd)

    expect(subscriptionStart).toBeGreaterThan(-1)
    expect(subscriptionEnd).toBeGreaterThan(subscriptionStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(subscriptionSource).toContain('petApi.onJumpToWorktree(({ worktreeId }) => {')
    expect(subscriptionSource).toContain('if (worktreeId) jumpToWorktree(worktreeId)')
    expect(subscriptionSource).not.toContain('window.petOps.onJumpToWorktree')
  })

  it('removes the old pet:jump-to-worktree IPC subscription after WebSocket migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const petWindowSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/pet-window.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.on('pet:jump-to-worktree'")
    expect(preloadSource).not.toContain("ipcRenderer.removeListener('pet:jump-to-worktree'")
    expect(petWindowSource).not.toContain(
      'mainWindow.webContents.send(PET_JUMP_TO_WORKTREE_CHANNEL'
    )
  })

  it('routes pet status bridge settings subscription through petApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/pet/PetStatusBridge.tsx'),
      'utf-8'
    )
    const subscriptionStart = source.indexOf(
      '    const cleanupSettings = petApi.onSettingsUpdated((settings) => {'
    )
    const cleanupStart = source.indexOf('\n\n    return () => {', subscriptionStart)
    const subscriptionSource = source.slice(subscriptionStart, cleanupStart)

    expect(subscriptionStart).toBeGreaterThan(-1)
    expect(cleanupStart).toBeGreaterThan(subscriptionStart)
    expect(source).toContain("import { petApi } from '@/api/pet-api'")
    expect(subscriptionSource).toContain('petApi.onSettingsUpdated((settings) => {')
    expect(subscriptionSource).toContain('const current = useSettingsStore.getState().pet')
    expect(subscriptionSource).toContain('if (current.hasHatched !== settings.hasHatched) {')
    expect(subscriptionSource).toContain(
      'useSettingsStore.setState({ pet: { ...current, hasHatched: settings.hasHatched } })'
    )
    expect(subscriptionSource).not.toContain('window.petOps.onSettingsUpdated')
  })

  it('removes the old empty petOps preload global exposure', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const petOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('petOps'")
    expect(preloadSource).not.toContain('window.petOps = petOps')
    expect(preloadTypesSource).not.toContain('petOps:')
    expect(preloadTypesSource).not.toContain('show: () => Promise<Envelope<void>>')
    expect(preloadTypesSource).not.toContain('getCurrentStatus: () => Promise<Envelope<PetStatusPayload>>')
    expect(preloadTypesSource).not.toContain('onJumpToWorktree: (')
  })

  it('removes the old empty pet IPC handler registrar after pet RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')

    expect(fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/pet-handlers.ts'))).toBe(false)
    expect(mainSource).not.toContain('registerPetHandlers')
    expect(ipcIndexSource).not.toContain('registerPetHandlers')
    expect(ipcIndexSource).not.toContain('./pet-handlers')
  })

  it('routes settings load telegram config through telegramApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSettingsStore.ts'),
      'utf-8'
    )
    const loadStart = source.indexOf('loadFromDatabase: async () => {')
    const hasDbStart = source.indexOf('if (dbSettings) {', loadStart)
    const loadPrelude = source.slice(loadStart, hasDbStart)

    expect(loadStart).toBeGreaterThan(-1)
    expect(hasDbStart).toBeGreaterThan(loadStart)
    expect(source).toContain("import { telegramApi } from '@/api/telegram-api'")
    expect(loadPrelude).toContain(
      'const telegramConfig = await telegramApi.getConfig().catch(() => null)'
    )
    expect(loadPrelude).not.toContain('window.telegramOps')
    expect(loadPrelude).not.toContain('unwrapEnvelope')
  })

  it('routes settings Telegram component config load through telegramApi getConfig', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/settings/SettingsTelegram.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('  useEffect(() => {')
    const effectEnd = source.indexOf('  const canTest = useMemo', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(source).toContain("import { telegramApi } from '@/api/telegram-api'")
    expect(effectSource).toContain('telegramApi.getConfig().then((config) => {')
    expect(effectSource).toContain('const next = config ?? DEFAULT_CONFIG')
    expect(effectSource).toContain('setDraft(next)')
    expect(effectSource).toContain('setTelegramConfig(config)')
    expect(effectSource).not.toContain('window.telegramOps.getConfig')
    expect(effectSource).not.toContain('unwrapEnvelope')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('removes the old telegram:getConfig IPC path after Telegram getConfig RPC migration', () => {
    const telegramHandlersSource = readOptionalSource('../../src/main/ipc/telegram-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(telegramHandlersSource).not.toContain("defineHandler('telegram:getConfig'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('telegram:getConfig'")
  })

  it('routes settings save telegram config through telegramApi setConfig', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/settings/SettingsTelegram.tsx'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'const saveConfig = async (next: TelegramConfig): Promise<void> => {'
    )
    const functionEnd = source.indexOf(
      '  const verifyToken = async (): Promise<void> => {',
      functionStart
    )
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { telegramApi } from '@/api/telegram-api'")
    expect(functionSource).toContain('const result = await telegramApi.setConfig(')
    expect(functionSource).toContain('normalized.botToken || normalized.chatId ? normalized : null')
    expect(functionSource).toContain(
      "toast.error(result.error ?? 'Failed to save Telegram settings')"
    )
    expect(functionSource).not.toContain('window.telegramOps.setConfig')
    expect(functionSource).not.toContain('unwrapEnvelope(')
  })

  it('removes the old telegram:setConfig IPC path after Telegram setConfig RPC migration', () => {
    const telegramHandlersSource = readOptionalSource('../../src/main/ipc/telegram-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(telegramHandlersSource).not.toContain("defineHandler('telegram:setConfig'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('telegram:setConfig'")
  })

  it('routes settings verify telegram token through telegramApi verifyToken', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/settings/SettingsTelegram.tsx'),
      'utf-8'
    )
    const functionStart = source.indexOf('const verifyToken = async (): Promise<void> => {')
    const functionEnd = source.indexOf(
      '  const refreshChats = async (): Promise<void> => {',
      functionStart
    )
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { telegramApi } from '@/api/telegram-api'")
    expect(functionSource).toContain(
      'const result = await telegramApi.verifyToken(draft.botToken.trim())'
    )
    expect(functionSource).toContain('setVerifyResult(result.ok)')
    expect(functionSource).toContain(
      "`Telegram bot configured${result.botUsername ? `: ${result.botUsername}` : ''}"
    )
    expect(functionSource).toContain("toast.error(result.error ?? 'Invalid Telegram bot token')")
    expect(functionSource).toContain('setVerifying(false)')
    expect(functionSource).not.toContain('window.telegramOps.verifyToken')
    expect(functionSource).not.toContain('unwrapEnvelope(')
  })

  it('removes the old telegram:verifyToken IPC path after Telegram verifyToken RPC migration', () => {
    const telegramHandlersSource = readOptionalSource('../../src/main/ipc/telegram-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(telegramHandlersSource).not.toContain("defineHandler('telegram:verifyToken'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('telegram:verifyToken'")
  })

  it('routes settings chat discovery through telegramApi discoverChats', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/settings/SettingsTelegram.tsx'),
      'utf-8'
    )
    const functionStart = source.indexOf('const refreshChats = async (): Promise<void> => {')
    const functionEnd = source.indexOf(
      '  const sendTest = async (): Promise<void> => {',
      functionStart
    )
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { telegramApi } from '@/api/telegram-api'")
    expect(functionSource).toContain('await saveConfig(draft)')
    expect(functionSource).toContain('const chats = await telegramApi.discoverChats(draft)')
    expect(functionSource).toContain('setDiscoveredChats(chats)')
    expect(functionSource).toContain(
      "toast.error('No chats found. Send a message to the bot, then refresh.')"
    )
    expect(functionSource).toContain('setRefreshing(false)')
    expect(functionSource).not.toContain('window.telegramOps.discoverChats')
    expect(functionSource).not.toContain('unwrapEnvelope(')
  })

  it('removes the old telegram:discoverChats IPC path after Telegram discoverChats RPC migration', () => {
    const telegramHandlersSource = readOptionalSource('../../src/main/ipc/telegram-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(telegramHandlersSource).not.toContain("defineHandler('telegram:discoverChats'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('telegram:discoverChats'")
  })

  it('routes settings Telegram test message through telegramApi sendTestMessage', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/settings/SettingsTelegram.tsx'),
      'utf-8'
    )
    const functionStart = source.indexOf('const sendTest = async (): Promise<void> => {')
    const functionEnd = source.indexOf('  return (', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { telegramApi } from '@/api/telegram-api'")
    expect(functionSource).toContain('await saveConfig(draft)')
    expect(functionSource).toContain('const result = await telegramApi.sendTestMessage()')
    expect(functionSource).toContain("toast.success('Telegram test message sent')")
    expect(functionSource).toContain("toast.error(result.error ?? 'Telegram test failed')")
    expect(functionSource).toContain('setTesting(false)')
    expect(functionSource).not.toContain('window.telegramOps.sendTestMessage')
    expect(functionSource).not.toContain('unwrapEnvelope(')
  })

  it('removes the old telegram:sendTestMessage IPC path after Telegram sendTestMessage RPC migration', () => {
    const telegramHandlersSource = readOptionalSource('../../src/main/ipc/telegram-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(telegramHandlersSource).not.toContain("defineHandler('telegram:sendTestMessage'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('telegram:sendTestMessage'")
  })

  it('routes Telegram store status refresh through telegramApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useTelegramStore.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('refreshStatus: async () => {')
    const functionEnd = source.indexOf('  setDiscoveredChats:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { telegramApi } from '@/api/telegram-api'")
    expect(functionSource).toContain('const status = await telegramApi.getStatus()')
    expect(functionSource).toContain('useTelegramStore.getState().setStatus(status)')
    expect(functionSource).toContain("connectionStatus: 'error'")
    expect(functionSource).not.toContain('window.telegramOps.getStatus')
    expect(functionSource).not.toContain('unwrapEnvelope(await window.telegramOps.getStatus')
  })

  it('removes the old telegram:getStatus IPC path after Telegram getStatus RPC migration', () => {
    const telegramHandlersSource = readOptionalSource('../../src/main/ipc/telegram-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(telegramHandlersSource).not.toContain("defineHandler('telegram:getStatus'")
    expect(telegramHandlersSource).not.toContain("'telegram:getStatus'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('telegram:getStatus'")
  })

  it('routes Telegram store status listener through telegramApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useTelegramStore.ts'),
      'utf-8'
    )
    const listenerStart = source.indexOf('telegramApi.onStatusChanged((status) => {')
    const listenerEnd = source.indexOf('  telegramApi.onPlanImplementRequested', listenerStart)
    const listenerSource = source.slice(listenerStart, listenerEnd)

    expect(listenerStart).toBeGreaterThan(-1)
    expect(listenerEnd).toBeGreaterThan(listenerStart)
    expect(source).toContain("import { telegramApi } from '@/api/telegram-api'")
    expect(listenerSource).toContain('const previous = useTelegramStore.getState()')
    expect(listenerSource).toContain('useTelegramStore.getState().setStatus(status)')
    expect(listenerSource).toContain('toast.error(`Telegram unreachable: ${status.lastError}`)')
    expect(listenerSource).not.toContain('window.telegramOps.onStatusChanged')
    expect(listenerSource).not.toContain('window.telegramOps?.onStatusChanged')
  })

  it('removes the old telegram:statusChanged IPC listener path after Telegram status WebSocket migration', () => {
    const telegramServiceSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/telegram-forwarding-service.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(telegramServiceSource).toContain(
      'this.backendEventPublisher?.(TELEGRAM_STATUS_CHANGED_CHANNEL, payload)'
    )
    expect(telegramServiceSource).not.toContain('webContents.send(TELEGRAM_STATUS_CHANGED_CHANNEL')
    expect(preloadSource).not.toContain("ipcRenderer.on('telegram:statusChanged'")
    expect(preloadSource).not.toContain("removeListener('telegram:statusChanged'")
  })

  it('routes Telegram store plan implement listener through telegramApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useTelegramStore.ts'),
      'utf-8'
    )
    const listenerStart = source.indexOf('telegramApi.onPlanImplementRequested((payload) => {')
    const listenerSource = source.slice(listenerStart)

    expect(listenerStart).toBeGreaterThan(-1)
    expect(source).toContain("import { telegramApi } from '@/api/telegram-api'")
    expect(listenerSource).toContain('const session = await dbApi.session.get')
    expect(listenerSource).toContain('if (payload.connectionId) {')
    expect(listenerSource).toContain('await startBackgroundSessionPrompt({')
    expect(listenerSource).toContain('toast.error(error instanceof Error ? error.message')
    expect(listenerSource).not.toContain('window.telegramOps.onPlanImplementRequested')
    expect(listenerSource).not.toContain('window.telegramOps?.onPlanImplementRequested')
  })

  it('removes the old telegram:planImplementRequested IPC listener path after Telegram plan WebSocket migration', () => {
    const telegramServiceSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/telegram-forwarding-service.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(telegramServiceSource).toContain(
      'this.backendEventPublisher?.(TELEGRAM_PLAN_IMPLEMENT_REQUESTED_CHANNEL, payload)'
    )
    expect(telegramServiceSource).not.toContain(
      'webContents.send(TELEGRAM_PLAN_IMPLEMENT_REQUESTED_CHANNEL'
    )
    expect(preloadSource).not.toContain("ipcRenderer.on('telegram:planImplementRequested'")
    expect(preloadSource).not.toContain("removeListener('telegram:planImplementRequested'")
  })

  it('keeps Telegram forwarding tests on backend event publishing for plan handoff callbacks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/telegram/telegram-forwarding-service.test.ts'),
      'utf-8'
    )

    expect(source).toContain('service.setBackendEventPublisher(backendPublish)')
    expect(source).toContain('TELEGRAM_PLAN_IMPLEMENT_REQUESTED_CHANNEL')
    expect(source).not.toContain('webContentsSend')
    expect(source).not.toContain('webContents: { send')
    expect(source).not.toContain('telegram:planImplementRequested')
  })

  it('routes Telegram connection handoff forwarding through telegramApi startForwarding', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useTelegramStore.ts'),
      'utf-8'
    )
    const branchStart = source.indexOf('if (payload.connectionId) {')
    const branchEnd = source.indexOf(
      '        if (!session.project_id || !payload.worktreeId) {',
      branchStart
    )
    const branchSource = source.slice(branchStart, branchEnd)

    expect(branchStart).toBeGreaterThan(-1)
    expect(branchEnd).toBeGreaterThan(branchStart)
    expect(source).toContain("import { telegramApi } from '@/api/telegram-api'")
    expect(branchSource).toContain('const forwarding = await telegramApi.startForwarding({')
    expect(branchSource).toContain('sessionId: result.session.id')
    expect(branchSource).toContain('worktreeId: null')
    expect(branchSource).toContain('connectionId: payload.connectionId')
    expect(branchSource).toContain('mode')
    expect(branchSource).toContain('useTelegramStore.getState().setStatus(forwarding.status)')
    expect(branchSource).not.toContain('window.telegramOps.startForwarding')
    expect(branchSource).not.toContain('unwrapEnvelope(')
  })

  it('routes Telegram worktree handoff forwarding through telegramApi startForwarding', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useTelegramStore.ts'),
      'utf-8'
    )
    const branchStart = source.indexOf('if (!session.project_id || !payload.worktreeId) {')
    const branchEnd = source.indexOf('      } catch (error) {', branchStart)
    const branchSource = source.slice(branchStart, branchEnd)

    expect(branchStart).toBeGreaterThan(-1)
    expect(branchEnd).toBeGreaterThan(branchStart)
    expect(source).toContain("import { telegramApi } from '@/api/telegram-api'")
    expect(branchSource).toContain('const forwarding = await telegramApi.startForwarding({')
    expect(branchSource).toContain('sessionId: result.session.id')
    expect(branchSource).toContain('worktreeId: payload.worktreeId')
    expect(branchSource).toContain('connectionId: null')
    expect(branchSource).toContain('mode')
    expect(branchSource).toContain('useTelegramStore.getState().setStatus(forwarding.status)')
    expect(branchSource).not.toContain('window.telegramOps.startForwarding')
    expect(branchSource).not.toContain('unwrapEnvelope(')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('routes header Telegram start forwarding through telegramApi startForwarding', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/HeaderTelegramToggle.tsx'),
      'utf-8'
    )
    const functionStart = source.indexOf('const startForwarding = async (mode: TelegramMode)')
    const functionEnd = source.indexOf(
      '  const stopForwarding = async (): Promise<void> => {',
      functionStart
    )
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { telegramApi } from '@/api/telegram-api'")
    expect(functionSource).toContain('const result = await telegramApi.startForwarding({')
    expect(functionSource).toContain('sessionId: activeSessionId')
    expect(functionSource).toContain('worktreeId: activeWorktreeId')
    expect(functionSource).toContain('connectionId: activeConnectionId')
    expect(functionSource).toContain('mode')
    expect(functionSource).toContain('useTelegramStore.getState().setStatus(result.status)')
    expect(functionSource).not.toContain('window.telegramOps.startForwarding')
    expect(functionSource).not.toContain('unwrapEnvelope(')
  })

  it('removes the old telegram:startForwarding IPC path after Telegram startForwarding RPC migration', () => {
    const telegramHandlersSource = readOptionalSource('../../src/main/ipc/telegram-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(telegramHandlersSource).not.toContain("defineHandler('telegram:startForwarding'")
    expect(telegramHandlersSource).not.toContain("'telegram:startForwarding'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('telegram:startForwarding'")
  })

  it('routes header Telegram stop forwarding through telegramApi stopForwarding', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/layout/HeaderTelegramToggle.tsx'),
      'utf-8'
    )
    const functionStart = source.indexOf('const stopForwarding = async (): Promise<void> => {')
    const functionEnd = source.indexOf('  const trigger = (', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain("import { telegramApi } from '@/api/telegram-api'")
    expect(functionSource).toContain('const result = await telegramApi.stopForwarding()')
    expect(functionSource).toContain('useTelegramStore.getState().setStatus(result.status)')
    expect(functionSource).toContain("toast.success('Telegram forwarding stopped')")
    expect(functionSource).not.toContain('window.telegramOps.stopForwarding')
    expect(functionSource).not.toContain('unwrapEnvelope(')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('removes the old telegram:stopForwarding IPC path after Telegram stopForwarding RPC migration', () => {
    const telegramHandlersSource = readOptionalSource('../../src/main/ipc/telegram-handlers.ts')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(telegramHandlersSource).not.toContain("defineHandler('telegram:stopForwarding'")
    expect(telegramHandlersSource).not.toContain("'telegram:stopForwarding'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('telegram:stopForwarding'")
  })

  it('removes the old empty telegramOps preload global exposure', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const telegramOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('telegramOps'")
    expect(preloadSource).not.toContain('window.telegramOps = telegramOps')
    expect(preloadTypesSource).not.toContain('telegramOps:')
    expect(preloadTypesSource).not.toContain('verifyToken: (')
    expect(preloadTypesSource).not.toContain('startForwarding: (params:')
    expect(preloadTypesSource).not.toContain('onPlanImplementRequested: (')
  })

  it('removes the old empty telegram IPC handler registrar after Telegram RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')

    expect(fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/telegram-handlers.ts'))).toBe(
      false
    )
    expect(mainSource).not.toContain('registerTelegramHandlers')
    expect(ipcIndexSource).not.toContain('registerTelegramHandlers')
    expect(ipcIndexSource).not.toContain('./telegram-handlers')
  })

  it('routes settings update listener through settingsApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSettingsStore.ts'),
      'utf-8'
    )
    const listenerStart = source.indexOf('// Listen for settings updates from main process')
    const listenerEnd = source.indexOf('  })\n\n}', listenerStart)
    const listenerSource = source.slice(listenerStart, listenerEnd)

    expect(listenerStart).toBeGreaterThan(-1)
    expect(listenerEnd).toBeGreaterThan(listenerStart)
    expect(source).toContain("import { settingsApi } from '@/api/settings-api'")
    expect(listenerSource).toContain('settingsApi.onSettingsUpdated((data) => {')
    expect(listenerSource).toContain(
      'useSettingsStore.setState({ commandFilter: typedData.commandFilter })'
    )
    expect(listenerSource).not.toContain('window.settingsOps')
  })

  it('removes the old empty settingsOps preload global exposure', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const settingsOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('settingsOps'")
    expect(preloadSource).not.toContain('window.settingsOps = settingsOps')
    expect(preloadTypesSource).not.toContain('settingsOps:')
    expect(preloadTypesSource).not.toContain('detectEditors: () => Promise<Envelope<DetectedApp[]>>')
    expect(preloadTypesSource).not.toContain('openWithTerminal: (')
    expect(preloadTypesSource).not.toContain('onSettingsUpdated: (callback: (data: unknown) => void)')
    expect(preloadTypesSource).not.toContain('interface DetectedApp')
  })

  it('removes the old empty settings IPC handler registrar after settings RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const backendManagerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/backend-manager.ts'),
      'utf-8'
    )
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')
    const settingsOpenersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/settings-openers.ts'),
      'utf-8'
    )
    const settingsRpcSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/settings-ops.ts'),
      'utf-8'
    )
    const worktreeRpcSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/worktree-ops.ts'),
      'utf-8'
    )
    const connectionRpcSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/connection-ops.ts'),
      'utf-8'
    )
    const gitRpcSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/git-ops.ts'),
      'utf-8'
    )

    expect(
      fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/settings-handlers.ts'))
    ).toBe(false)
    expect(settingsOpenersSource).toContain('export function openPathWithEditor')
    expect(settingsOpenersSource).toContain('export function openPathWithTerminal')
    expect(settingsOpenersSource).toContain('export function getAllSettingsMap')
    expect(backendManagerSource).toContain("from '../services/settings-openers'")
    expect(backendManagerSource).not.toContain("from '../ipc/settings-handlers'")
    for (const source of [
      settingsRpcSource,
      worktreeRpcSource,
      connectionRpcSource,
      gitRpcSource
    ]) {
      expect(source).toContain('main/services/settings-openers')
      expect(source).not.toContain('main/ipc/settings-handlers')
    }
    expect(mainSource).not.toContain('registerSettingsHandlers')
    expect(ipcIndexSource).not.toContain('registerSettingsHandlers')
    expect(ipcIndexSource).not.toContain('settings-handlers')
  })

  it('routes image paste attachment saves through attachmentApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useImagePaste.ts'),
      'utf-8'
    )
    const saveStart = source.indexOf('const saveAndAttach = useCallback(')
    const saveEnd = source.indexOf('const handlePaste = useCallback(', saveStart)
    const saveSource = source.slice(saveStart, saveEnd)

    expect(saveStart).toBeGreaterThan(-1)
    expect(saveEnd).toBeGreaterThan(saveStart)
    expect(source).toContain("import { attachmentApi } from '@/api/attachment-api'")
    expect(saveSource).toContain('const result = await attachmentApi.saveImage(arrayBuffer, name)')
    expect(saveSource).not.toContain('window.attachmentOps.saveImage')
    expect(saveSource).not.toContain('unwrapEnvelope')
  })

  it('removes the old attachment:save IPC adapter after attachment save RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const attachmentHandlersSource = readOptionalSource('../../src/main/ipc/attachment-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('attachment:save'")
    expect(attachmentHandlersSource).not.toContain("defineHandler('attachment:save'")
  })

  it('removes the old attachment:delete IPC adapter after attachment delete RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const attachmentHandlersSource = readOptionalSource('../../src/main/ipc/attachment-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('attachment:delete'")
    expect(attachmentHandlersSource).not.toContain("defineHandler('attachment:delete'")
  })

  it('removes the old empty attachment IPC handler registrar after attachment RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')

    expect(
      fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/attachment-handlers.ts'))
    ).toBe(false)
    expect(mainSource).not.toContain('registerAttachmentHandlers')
    expect(ipcIndexSource).not.toContain('registerAttachmentHandlers')
    expect(ipcIndexSource).not.toContain('./attachment-handlers')
  })

  it('removes the old empty attachmentOps preload global exposure', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const attachmentOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('attachmentOps'")
    expect(preloadSource).not.toContain('window.attachmentOps = attachmentOps')
    expect(preloadTypesSource).not.toContain('attachmentOps:')
    expect(preloadTypesSource).not.toContain('saveImage: (')
    expect(preloadTypesSource).not.toContain('deleteImage: (filePath: string)')
  })

  it('routes notification navigation through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useNotificationNavigation.ts'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('const cleanup = systemApi.onNotificationNavigate((data) => {')
    expect(source).not.toContain('window.systemOps.onNotificationNavigate')
  })

  it('routes window focus refresh through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useWindowFocusRefresh.ts'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('const unsubscribeFocus = systemApi.onWindowFocused(() => {')
    expect(source).not.toContain('window.systemOps.onWindowFocused')
  })

  it('routes window focus git status tracking through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useWindowFocusRefresh.ts'),
      'utf-8'
    )
    const statusStart = source.indexOf(
      '// Track incoming git status events so we can skip idle polls when the watcher is working.'
    )
    const focusStart = source.indexOf(
      'const unsubscribeFocus = systemApi.onWindowFocused',
      statusStart
    )
    const statusSource = source.slice(statusStart, focusStart)

    expect(statusStart).toBeGreaterThan(-1)
    expect(focusStart).toBeGreaterThan(statusStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(statusSource).toContain('const unsubscribeStatus = gitApi.onStatusChanged((event) => {')
    expect(statusSource).toContain('const paths = getActiveWorktreePaths()')
    expect(statusSource).toContain('paths.includes(event.worktreePath)')
    expect(statusSource).toContain('lastEventRef.current = Date.now()')
    expect(statusSource).not.toContain('window.gitOps.onStatusChanged')
  })

  it('routes useWorktreeWatcher status subscription through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useWorktreeWatcher.ts'),
      'utf-8'
    )
    const effectStart = source.indexOf(
      '// Centralized onStatusChanged subscription — replaces per-component subscriptions'
    )
    const cleanupStart = source.indexOf('  useEffect(() => {\n    return () => {', effectStart)
    const effectSource = source.slice(effectStart, cleanupStart)

    expect(effectStart).toBeGreaterThan(-1)
    expect(cleanupStart).toBeGreaterThan(effectStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(effectSource).toContain('const unsubscribe = gitApi.onStatusChanged((event) => {')
    expect(effectSource).toContain('const currentPath = previousPathRef.current')
    expect(effectSource).toContain('event.worktreePath === currentPath')
    expect(effectSource).toContain('useGitStore.getState().refreshStatuses(currentPath)')
    expect(effectSource).toContain('unsubscribe()')
    expect(effectSource).not.toContain('window.gitOps.onStatusChanged')
  })

  it('routes useWorktreeWatcher start watch through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useWorktreeWatcher.ts'),
      'utf-8'
    )
    const startWatchStart = source.indexOf('    // Start watching the new worktree')
    const initialLoadStart = source.indexOf(
      '      // Also do an initial load of statuses when switching worktrees',
      startWatchStart
    )
    const startWatchSource = source.slice(startWatchStart, initialLoadStart)

    expect(startWatchStart).toBeGreaterThan(-1)
    expect(initialLoadStart).toBeGreaterThan(startWatchStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(startWatchSource).toContain('if (worktreePath) {')
    expect(startWatchSource).toContain('gitApi.watchWorktree(worktreePath).catch(() => {')
    expect(startWatchSource).toContain('// Non-critical - watcher setup failed')
    expect(startWatchSource).not.toContain('window.gitOps.watchWorktree')
  })

  it('routes useWorktreeWatcher previous-path unwatch through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useWorktreeWatcher.ts'),
      'utf-8'
    )
    const stopWatchStart = source.indexOf('    // Stop watching the previous worktree')
    const startWatchStart = source.indexOf('    // Start watching the new worktree', stopWatchStart)
    const stopWatchSource = source.slice(stopWatchStart, startWatchStart)

    expect(stopWatchStart).toBeGreaterThan(-1)
    expect(startWatchStart).toBeGreaterThan(stopWatchStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(stopWatchSource).toContain('if (prevPath) {')
    expect(stopWatchSource).toContain('gitApi.unwatchWorktree(prevPath).catch(() => {')
    expect(stopWatchSource).toContain('// Non-critical - watcher may already be stopped')
    expect(stopWatchSource).not.toContain('window.gitOps.unwatchWorktree')
  })

  it('routes useWorktreeWatcher cleanup unwatch through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useWorktreeWatcher.ts'),
      'utf-8'
    )
    const cleanupStart = source.indexOf('  useEffect(() => {\n    return () => {')
    const cleanupSource = source.slice(cleanupStart)

    expect(cleanupStart).toBeGreaterThan(-1)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(cleanupSource).toContain('const currentPath = previousPathRef.current')
    expect(cleanupSource).toContain('if (currentPath) {')
    expect(cleanupSource).toContain('gitApi.unwatchWorktree(currentPath).catch(() => {')
    expect(cleanupSource).toContain('// Non-critical')
    expect(cleanupSource).not.toContain('window.gitOps.unwatchWorktree')
  })

  it('routes useConnectionWatcher status subscription through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useConnectionWatcher.ts'),
      'utf-8'
    )
    const effectStart = source.indexOf(
      '// Subscribe to git status change events for any member path'
    )
    const cleanupStart = source.indexOf('  // Cleanup on unmount', effectStart)
    const effectSource = source.slice(effectStart, cleanupStart)

    expect(effectStart).toBeGreaterThan(-1)
    expect(cleanupStart).toBeGreaterThan(effectStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(effectSource).toContain('if (!isConnectionMode) return')
    expect(effectSource).toContain('const unsubscribe = gitApi.onStatusChanged((event) => {')
    expect(effectSource).toContain('const currentPaths = previousPathsRef.current')
    expect(effectSource).toContain('currentPaths.includes(event.worktreePath)')
    expect(effectSource).toContain('useGitStore.getState().refreshStatuses(event.worktreePath)')
    expect(effectSource).toContain('unsubscribe()')
    expect(effectSource).not.toContain('window.gitOps.onStatusChanged')
  })

  it('routes useConnectionWatcher previous-path unwatch through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useConnectionWatcher.ts'),
      'utf-8'
    )
    const stopWatchStart = source.indexOf('    // Stop watching previous paths')
    const startWatchStart = source.indexOf(
      '    // Start watching new paths and load initial statuses',
      stopWatchStart
    )
    const stopWatchSource = source.slice(stopWatchStart, startWatchStart)

    expect(stopWatchStart).toBeGreaterThan(-1)
    expect(startWatchStart).toBeGreaterThan(stopWatchStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(stopWatchSource).toContain('for (const path of prevPaths) {')
    expect(stopWatchSource).toContain('gitApi.unwatchWorktree(path).catch(() => {')
    expect(stopWatchSource).toContain('// Non-critical - watcher may already be stopped')
    expect(stopWatchSource).not.toContain('window.gitOps.unwatchWorktree')
  })

  it('routes useConnectionWatcher start watch through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useConnectionWatcher.ts'),
      'utf-8'
    )
    const startWatchStart = source.indexOf(
      '    // Start watching new paths and load initial statuses'
    )
    const initialLoadStart = source.indexOf(
      '      // Load initial statuses for all members',
      startWatchStart
    )
    const startWatchSource = source.slice(startWatchStart, initialLoadStart)

    expect(startWatchStart).toBeGreaterThan(-1)
    expect(initialLoadStart).toBeGreaterThan(startWatchStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(startWatchSource).toContain('if (memberPaths.length > 0) {')
    expect(startWatchSource).toContain('for (const path of memberPaths) {')
    expect(startWatchSource).toContain('gitApi.watchWorktree(path).catch(() => {')
    expect(startWatchSource).toContain('// Non-critical - watcher setup failed')
    expect(startWatchSource).not.toContain('window.gitOps.watchWorktree')
  })

  it('routes useConnectionWatcher cleanup unwatch through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useConnectionWatcher.ts'),
      'utf-8'
    )
    const cleanupStart = source.indexOf('  // Cleanup on unmount')
    const cleanupSource = source.slice(cleanupStart)

    expect(cleanupStart).toBeGreaterThan(-1)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(cleanupSource).toContain('for (const path of previousPathsRef.current) {')
    expect(cleanupSource).toContain('gitApi.unwatchWorktree(path).catch(() => {')
    expect(cleanupSource).toContain('// Non-critical')
    expect(cleanupSource).not.toContain('window.gitOps.unwatchWorktree')
  })

  it('routes useSidebarBranchWatcher branch subscription through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useSidebarBranchWatcher.ts'),
      'utf-8'
    )
    const effectStart = source.indexOf('// Subscribe to branch change events')
    const cleanupStart = source.indexOf('  // Cleanup all watchers on unmount', effectStart)
    const effectSource = source.slice(effectStart, cleanupStart)

    expect(effectStart).toBeGreaterThan(-1)
    expect(cleanupStart).toBeGreaterThan(effectStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(effectSource).toContain('if (worktreePaths.length === 0) return')
    expect(effectSource).toContain('const unsubscribe = gitApi.onBranchChanged((event) => {')
    expect(effectSource).toContain('const currentPaths = previousPathsRef.current')
    expect(effectSource).toContain('currentPaths.includes(event.worktreePath)')
    expect(effectSource).toContain('useGitStore')
    expect(effectSource).toContain('.loadBranchInfo(event.worktreePath, { force: true })')
    expect(effectSource).toContain('unsubscribe()')
    expect(effectSource).not.toContain('window.gitOps.onBranchChanged')
  })

  it('routes useSidebarBranchWatcher removed-path unwatch through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useSidebarBranchWatcher.ts'),
      'utf-8'
    )
    const stopWatchStart = source.indexOf('    // Stop watching removed paths')
    const startWatchStart = source.indexOf('    // Start watching added paths', stopWatchStart)
    const stopWatchSource = source.slice(stopWatchStart, startWatchStart)

    expect(stopWatchStart).toBeGreaterThan(-1)
    expect(startWatchStart).toBeGreaterThan(stopWatchStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(stopWatchSource).toContain('for (const path of prevPaths) {')
    expect(stopWatchSource).toContain('if (!newSet.has(path)) {')
    expect(stopWatchSource).toContain('gitApi.unwatchBranch(path).catch(() => {')
    expect(stopWatchSource).toContain('// Non-critical')
    expect(stopWatchSource).not.toContain('window.gitOps.unwatchBranch')
  })

  it('routes useSidebarBranchWatcher added-path watch through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useSidebarBranchWatcher.ts'),
      'utf-8'
    )
    const startWatchStart = source.indexOf('    // Start watching added paths')
    const initialLoadStart = source.indexOf(
      "    // Load initial branch info only for paths we don't already have.",
      startWatchStart
    )
    const startWatchSource = source.slice(startWatchStart, initialLoadStart)

    expect(startWatchStart).toBeGreaterThan(-1)
    expect(initialLoadStart).toBeGreaterThan(startWatchStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(startWatchSource).toContain('for (const path of worktreePaths) {')
    expect(startWatchSource).toContain('if (!prevSet.has(path)) {')
    expect(startWatchSource).toContain('gitApi.watchBranch(path).catch(() => {')
    expect(startWatchSource).toContain('// Non-critical')
    expect(startWatchSource).not.toContain('window.gitOps.watchBranch')
  })

  it('routes useSidebarBranchWatcher cleanup unwatch through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useSidebarBranchWatcher.ts'),
      'utf-8'
    )
    const cleanupStart = source.indexOf('  // Cleanup all watchers on unmount')
    const cleanupSource = source.slice(cleanupStart)

    expect(cleanupStart).toBeGreaterThan(-1)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(cleanupSource).toContain('for (const path of previousPathsRef.current) {')
    expect(cleanupSource).toContain('gitApi.unwatchBranch(path).catch(() => {')
    expect(cleanupSource).toContain('// Non-critical')
    expect(cleanupSource).not.toContain('window.gitOps.unwatchBranch')
  })

  it('routes diff comment focus refresh through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useDiffCommentStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('const unsubscribe = systemApi.onWindowFocused(() => {')
    expect(source).not.toContain('window.systemOps.onWindowFocused')
  })

  it('routes new-session shortcut through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useKeyboardShortcuts.ts'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('const cleanup = systemApi.onNewSessionShortcut(() => {')
    expect(source).not.toContain('window.systemOps?.onNewSessionShortcut')
    expect(source).not.toContain('window.systemOps.onNewSessionShortcut')
  })

  it('routes file-search shortcut through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useKeyboardShortcuts.ts'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('const cleanup = systemApi.onFileSearchShortcut(() => {')
    expect(source).not.toContain('window.systemOps?.onFileSearchShortcut')
    expect(source).not.toContain('window.systemOps.onFileSearchShortcut')
  })

  it('routes close-session shortcut through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useKeyboardShortcuts.ts'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('const cleanup = systemApi.onCloseSessionShortcut(() => {')
    expect(source).not.toContain('window.systemOps?.onCloseSessionShortcut')
    expect(source).not.toContain('window.systemOps.onCloseSessionShortcut')
  })

  it('routes terminal edit paste through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/terminal/TerminalView.tsx'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('const cleanup = systemApi.onEditPaste((text) => {')
    expect(source).not.toContain('window.systemOps?.onEditPaste')
    expect(source).not.toContain('window.systemOps.onEditPaste')
  })

  it('routes menu actions through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useKeyboardShortcuts.ts'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('cleanups.push(systemApi.onMenuAction(channel, handler))')
    expect(source).not.toContain('window.systemOps?.onMenuAction')
    expect(source).not.toContain('window.systemOps.onMenuAction')
  })

  it('removes the old generic menu renderer IPC fallback after backend event migration', () => {
    const source = fs.readFileSync(path.resolve(__dirname, '../../src/main/menu.ts'), 'utf-8')

    expect(source).not.toContain('_mainWindow.webContents.send(channel')
    expect(source).not.toContain('webContents.send(channel, ...args)')
    expect(source).toContain('emitMenuActionIfKnown(channel, ...args)')
    expect(source).not.toContain('emitMenuActionIfKnown(_mainWindow')
  })

  it('removes the old desktop menu-action preload compatibility listener and duplicate event path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const menuEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/menu-events.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadSource).not.toContain('DESKTOP_MENU_ACTION_CHANNEL')
    expect(preloadSource).not.toContain('desktop:menuAction')
    expect(preloadSource).not.toContain(
      'onMenuAction: (listener: (action: string) => void): (() => void) =>'
    )
    expect(preloadSource).not.toContain('ipcRenderer.on(DESKTOP_MENU_ACTION_CHANNEL')
    expect(preloadSource).not.toContain('removeListener(DESKTOP_MENU_ACTION_CHANNEL')
    expect(preloadDesktopBridgeSource).not.toContain(
      'onMenuAction: (listener: (action: string) => void)'
    )
    expect(desktopBridgeSource).not.toContain('DESKTOP_MENU_ACTION_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:menuAction')
    expect(menuEventsSource).not.toContain('DESKTOP_MENU_ACTION_CHANNEL')
    expect(menuEventsSource).not.toContain(
      'window.webContents.send(DESKTOP_MENU_ACTION_CHANNEL, channel)'
    )
    expect(menuEventsSource).toContain('publishDesktopBackendEvent(channel')
    expect(menuEventsSource).toContain(
      'emitMenuAction = (channel: MenuActionChannel, ...args: unknown[]): void'
    )
    expect(menuEventsSource).not.toContain('emitMenuAction = (_window')
    expect(menuEventsSource).toContain(
      'emitMenuActionIfKnown = (channel: string, ...args: unknown[]): boolean'
    )
    expect(menuEventsSource).not.toContain('emitMenuActionIfKnown = (window')
    expect(desktopBridgeTypeSource).not.toContain('readonly onMenuAction?:')
  })

  it('routes systemApi menu actions directly through WebSocket after desktop bridge event migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'onMenuAction: (channel: MenuActionChannel, callback: () => void): (() => void) =>'
    )
    const functionEnd = source.indexOf('  openInApp:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).toContain('return getRendererRpcClient().subscribe(channel, () => {')
    expect(functionSource).toContain('callback()')
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.onMenuAction')
    expect(functionSource).not.toContain('bridge.onMenuAction')
    expect(functionSource).not.toContain('if (action === channel)')
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('keeps menu and shortcut events on WebSocket subscriptions instead of desktopBridge listeners', () => {
    const systemApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const rendererDesktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const sharedDesktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const menuSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/menu.ts'), 'utf-8')
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const menuEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/menu-events.ts'),
      'utf-8'
    )
    const shortcutEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/shortcut-events.ts'),
      'utf-8'
    )
    const menuEventsTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/menu-events.test.ts'),
      'utf-8'
    )
    const shortcutEventsTestSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/shortcut-events.test.ts'),
      'utf-8'
    )

    expect(systemApiSource).toContain(
      'return getRendererRpcClient().subscribe(NEW_SESSION_SHORTCUT_CHANNEL, () => {'
    )
    expect(systemApiSource).toContain(
      'return getRendererRpcClient().subscribe(FILE_SEARCH_SHORTCUT_CHANNEL, () => {'
    )
    expect(systemApiSource).toContain(
      'return getRendererRpcClient().subscribe(CLOSE_SESSION_SHORTCUT_CHANNEL, () => {'
    )
    expect(systemApiSource).toContain(
      'return getRendererRpcClient().subscribe(QUIT_CONFIRMATION_SHOW_CHANNEL, () => {'
    )
    expect(systemApiSource).toContain(
      'return getRendererRpcClient().subscribe(QUIT_CONFIRMATION_HIDE_CHANNEL, () => {'
    )
    expect(systemApiSource).toContain('return getRendererRpcClient().subscribe(channel, () => {')
    expect(systemApiSource).not.toContain('bridge.onMenuAction')
    expect(systemApiSource).not.toContain('bridge?.onMenuAction')
    expect(systemApiSource).not.toContain('bridge.onNewSessionShortcut')
    expect(systemApiSource).not.toContain('bridge?.onNewSessionShortcut')

    for (const source of [rendererDesktopBridgeSource, sharedDesktopBridgeSource, preloadSource]) {
      expect(source).not.toContain('onMenuAction')
      expect(source).not.toContain('onNewSessionShortcut')
      expect(source).not.toContain('onFileSearchShortcut')
      expect(source).not.toContain('onCloseSessionShortcut')
      expect(source).not.toContain('onQuitConfirmationShow')
      expect(source).not.toContain('onQuitConfirmationHide')
      expect(source).not.toContain('DESKTOP_MENU_ACTION_CHANNEL')
    }

    expect(menuSource).toContain('emitMenuActionIfKnown(channel, ...args)')
    expect(menuSource).toContain('emitNewSessionShortcut()')
    expect(menuSource).toContain('emitCloseSessionShortcut()')
    expect(menuSource).toContain('emitFileSearchShortcut()')
    expect(mainSource).toContain('emitNewSessionShortcut()')
    expect(mainSource).toContain('emitFileSearchShortcut()')
    expect(mainSource).toContain('emitCloseSessionShortcut()')
    expect(mainSource).toContain('emitQuitConfirmationShow()')
    expect(mainSource).toContain('emitQuitConfirmationHide()')

    expect(menuEventsSource).toContain('publishDesktopBackendEvent(channel')
    expect(menuEventsSource).not.toContain('BrowserWindow')
    expect(menuEventsSource).not.toContain('webContents.send')
    expect(shortcutEventsSource).toContain(
      'publishDesktopBackendEvent(NEW_SESSION_SHORTCUT_CHANNEL, {})'
    )
    expect(shortcutEventsSource).toContain(
      'publishDesktopBackendEvent(CLOSE_SESSION_SHORTCUT_CHANNEL, {})'
    )
    expect(shortcutEventsSource).toContain(
      'publishDesktopBackendEvent(FILE_SEARCH_SHORTCUT_CHANNEL, {})'
    )
    expect(shortcutEventsSource).not.toContain('BrowserWindow')
    expect(shortcutEventsSource).not.toContain('webContents.send')

    expect(menuEventsTestSource).toContain(
      "it('publishes menu actions without a window dependency'"
    )
    expect(shortcutEventsTestSource).toContain(
      "it('publishes the new-session shortcut event without a window dependency'"
    )
  })

  it('routes systemApi openInApp through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'openInApp: async (appName: string, path: string): Promise<OpenInAppResult> =>'
    )
    const functionEnd = source.indexOf('  openInChrome:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.openInApp')
    expect(functionSource).not.toContain('return bridge.openInApp(appName, path)')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<OpenInAppResult>('systemOps.openInApp', { appName, path })"
    )
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('removes the old desktop window focused preload compatibility listener and duplicate event path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const appEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/app-events.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadSource).not.toContain('DESKTOP_WINDOW_FOCUSED_CHANNEL')
    expect(preloadSource).not.toContain('desktop:windowFocused')
    expect(preloadSource).not.toContain('onWindowFocused: (')
    expect(preloadSource).not.toContain('ipcRenderer.on(DESKTOP_WINDOW_FOCUSED_CHANNEL')
    expect(preloadSource).not.toContain('removeListener(DESKTOP_WINDOW_FOCUSED_CHANNEL')
    expect(preloadDesktopBridgeSource).not.toContain('onWindowFocused: (')
    expect(desktopBridgeSource).not.toContain('DESKTOP_WINDOW_FOCUSED_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:windowFocused')
    expect(appEventsSource).not.toContain('DESKTOP_WINDOW_FOCUSED_CHANNEL')
    expect(appEventsSource).not.toContain('webContents.send(WINDOW_FOCUSED_CHANNEL')
    expect(appEventsSource).not.toContain('window.webContents.send(WINDOW_FOCUSED_CHANNEL')
    expect(appEventsSource).not.toContain('_window')
    expect(appEventsSource).toContain('emitWindowFocused = (): void')
    expect(desktopBridgeTypeSource).not.toContain('readonly onWindowFocused?:')
  })

  it('routes systemApi window focus directly through WebSocket after desktop bridge event migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('onWindowFocused: (callback: () => void): (() => void) =>')
    const functionEnd = source.indexOf('  onNewSessionShortcut:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain('WINDOW_FOCUSED_CHANNEL')
    expect(functionSource).toContain(
      'return getRendererRpcClient().subscribe(WINDOW_FOCUSED_CHANNEL, () => {'
    )
    expect(functionSource).toContain('callback()')
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.onWindowFocused')
    expect(functionSource).not.toContain('bridge.onWindowFocused')
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('keeps git focus refresh tests free of IPC terminology and mocks', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-17/session-4/git-focus-refresh.test.ts'),
      'utf-8'
    )

    expect(source).toContain('app:windowFocused')
    expect(source).toContain('same event subscription shape as systemApi')
    expect(source).not.toContain('ipcRenderer')
    expect(source).not.toContain('ipcMain')
    expect(source).not.toContain("vi.mock('electron'")
    expect(source).not.toContain('BrowserWindow')
  })

  it('removes the old desktop new-session shortcut preload compatibility listener and duplicate event path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const shortcutEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/shortcut-events.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadSource).not.toContain('DESKTOP_NEW_SESSION_SHORTCUT_CHANNEL')
    expect(preloadSource).not.toContain('desktop:newSessionShortcut')
    expect(preloadSource).not.toContain(
      'onNewSessionShortcut: (listener: () => void): (() => void) =>'
    )
    expect(preloadSource).not.toContain('ipcRenderer.on(DESKTOP_NEW_SESSION_SHORTCUT_CHANNEL')
    expect(preloadSource).not.toContain('removeListener(DESKTOP_NEW_SESSION_SHORTCUT_CHANNEL')
    expect(preloadDesktopBridgeSource).not.toContain('onNewSessionShortcut: (listener: () => void)')
    expect(desktopBridgeSource).not.toContain('DESKTOP_NEW_SESSION_SHORTCUT_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:newSessionShortcut')
    expect(shortcutEventsSource).not.toContain('DESKTOP_NEW_SESSION_SHORTCUT_CHANNEL')
    expect(shortcutEventsSource).not.toContain(
      'window.webContents.send(DESKTOP_NEW_SESSION_SHORTCUT_CHANNEL)'
    )
    expect(shortcutEventsSource).not.toContain('webContents.send(NEW_SESSION_SHORTCUT_CHANNEL')
    expect(shortcutEventsSource).not.toContain(
      'window.webContents.send(NEW_SESSION_SHORTCUT_CHANNEL'
    )
    expect(shortcutEventsSource).toContain('emitNewSessionShortcut = (): void')
    expect(shortcutEventsSource).not.toContain('emitNewSessionShortcut = (_window')
    expect(desktopBridgeTypeSource).not.toContain('readonly onNewSessionShortcut?:')
  })

  it('routes systemApi new-session shortcut directly through WebSocket after desktop bridge event migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'onNewSessionShortcut: (callback: () => void): (() => void) =>'
    )
    const functionEnd = source.indexOf('  onFileSearchShortcut:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain('NEW_SESSION_SHORTCUT_CHANNEL')
    expect(functionSource).toContain(
      'return getRendererRpcClient().subscribe(NEW_SESSION_SHORTCUT_CHANNEL, () => {'
    )
    expect(functionSource).toContain('callback()')
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.onNewSessionShortcut')
    expect(functionSource).not.toContain('bridge.onNewSessionShortcut')
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('removes the old desktop file-search shortcut preload compatibility listener and duplicate event path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const shortcutEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/shortcut-events.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadSource).not.toContain('DESKTOP_FILE_SEARCH_SHORTCUT_CHANNEL')
    expect(preloadSource).not.toContain('desktop:fileSearchShortcut')
    expect(preloadSource).not.toContain(
      'onFileSearchShortcut: (listener: () => void): (() => void) =>'
    )
    expect(preloadSource).not.toContain('ipcRenderer.on(DESKTOP_FILE_SEARCH_SHORTCUT_CHANNEL')
    expect(preloadSource).not.toContain('removeListener(DESKTOP_FILE_SEARCH_SHORTCUT_CHANNEL')
    expect(preloadDesktopBridgeSource).not.toContain('onFileSearchShortcut: (listener: () => void)')
    expect(desktopBridgeSource).not.toContain('DESKTOP_FILE_SEARCH_SHORTCUT_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:fileSearchShortcut')
    expect(shortcutEventsSource).not.toContain('DESKTOP_FILE_SEARCH_SHORTCUT_CHANNEL')
    expect(shortcutEventsSource).not.toContain(
      'window.webContents.send(DESKTOP_FILE_SEARCH_SHORTCUT_CHANNEL)'
    )
    expect(shortcutEventsSource).not.toContain('webContents.send(FILE_SEARCH_SHORTCUT_CHANNEL')
    expect(shortcutEventsSource).not.toContain(
      'window.webContents.send(FILE_SEARCH_SHORTCUT_CHANNEL'
    )
    expect(shortcutEventsSource).toContain('emitFileSearchShortcut = (): void')
    expect(shortcutEventsSource).not.toContain('emitFileSearchShortcut = (_window')
    expect(desktopBridgeTypeSource).not.toContain('readonly onFileSearchShortcut?:')
  })

  it('routes systemApi file-search shortcut directly through WebSocket after desktop bridge event migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'onFileSearchShortcut: (callback: () => void): (() => void) =>'
    )
    const functionEnd = source.indexOf('  onCloseSessionShortcut:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain('FILE_SEARCH_SHORTCUT_CHANNEL')
    expect(functionSource).toContain(
      'return getRendererRpcClient().subscribe(FILE_SEARCH_SHORTCUT_CHANNEL, () => {'
    )
    expect(functionSource).toContain('callback()')
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.onFileSearchShortcut')
    expect(functionSource).not.toContain('bridge.onFileSearchShortcut')
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('removes the old desktop close-session shortcut preload compatibility listener and duplicate event path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const shortcutEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/shortcut-events.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadSource).not.toContain('DESKTOP_CLOSE_SESSION_SHORTCUT_CHANNEL')
    expect(preloadSource).not.toContain('desktop:closeSessionShortcut')
    expect(preloadSource).not.toContain(
      'onCloseSessionShortcut: (listener: () => void): (() => void) =>'
    )
    expect(preloadSource).not.toContain('ipcRenderer.on(DESKTOP_CLOSE_SESSION_SHORTCUT_CHANNEL')
    expect(preloadSource).not.toContain('removeListener(DESKTOP_CLOSE_SESSION_SHORTCUT_CHANNEL')
    expect(preloadDesktopBridgeSource).not.toContain(
      'onCloseSessionShortcut: (listener: () => void)'
    )
    expect(desktopBridgeSource).not.toContain('DESKTOP_CLOSE_SESSION_SHORTCUT_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:closeSessionShortcut')
    expect(shortcutEventsSource).not.toContain('DESKTOP_CLOSE_SESSION_SHORTCUT_CHANNEL')
    expect(shortcutEventsSource).not.toContain(
      'window.webContents.send(DESKTOP_CLOSE_SESSION_SHORTCUT_CHANNEL)'
    )
    expect(shortcutEventsSource).not.toContain('webContents.send(CLOSE_SESSION_SHORTCUT_CHANNEL')
    expect(shortcutEventsSource).not.toContain(
      'window.webContents.send(CLOSE_SESSION_SHORTCUT_CHANNEL'
    )
    expect(shortcutEventsSource).toContain('emitCloseSessionShortcut = (): void')
    expect(shortcutEventsSource).not.toContain('emitCloseSessionShortcut = (_window')
    expect(desktopBridgeTypeSource).not.toContain('readonly onCloseSessionShortcut?:')
  })

  it('routes systemApi close-session shortcut directly through WebSocket after desktop bridge event migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'onCloseSessionShortcut: (callback: () => void): (() => void) =>'
    )
    const functionEnd = source.indexOf('  onQuitConfirmationShow:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain('CLOSE_SESSION_SHORTCUT_CHANNEL')
    expect(functionSource).toContain(
      'return getRendererRpcClient().subscribe(CLOSE_SESSION_SHORTCUT_CHANNEL, () => {'
    )
    expect(functionSource).toContain('callback()')
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.onCloseSessionShortcut')
    expect(functionSource).not.toContain('bridge.onCloseSessionShortcut')
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('removes the old desktop quit-confirmation-show preload compatibility listener and duplicate event path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const shortcutEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/shortcut-events.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadSource).not.toContain('DESKTOP_QUIT_CONFIRMATION_SHOW_CHANNEL')
    expect(preloadSource).not.toContain('desktop:quitConfirmationShow')
    expect(preloadSource).not.toContain('onQuitConfirmationShow: (')
    expect(preloadSource).not.toContain('ipcRenderer.on(DESKTOP_QUIT_CONFIRMATION_SHOW_CHANNEL')
    expect(preloadSource).not.toContain('removeListener(DESKTOP_QUIT_CONFIRMATION_SHOW_CHANNEL')
    expect(preloadDesktopBridgeSource).not.toContain('onQuitConfirmationShow: (')
    expect(desktopBridgeSource).not.toContain('DESKTOP_QUIT_CONFIRMATION_SHOW_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:quitConfirmationShow')
    expect(shortcutEventsSource).not.toContain('DESKTOP_QUIT_CONFIRMATION_SHOW_CHANNEL')
    expect(shortcutEventsSource).not.toContain('webContents.send(QUIT_CONFIRMATION_SHOW_CHANNEL')
    expect(shortcutEventsSource).not.toContain(
      'window.webContents.send(QUIT_CONFIRMATION_SHOW_CHANNEL'
    )
    expect(shortcutEventsSource).toContain('emitQuitConfirmationShow = (): void')
    expect(shortcutEventsSource).not.toContain('emitQuitConfirmationShow = (_window')
    expect(desktopBridgeTypeSource).not.toContain('readonly onQuitConfirmationShow?:')
  })

  it('routes systemApi quit-confirmation-show directly through WebSocket after desktop bridge event migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'onQuitConfirmationShow: (callback: () => void): (() => void) =>'
    )
    const functionEnd = source.indexOf('  onQuitConfirmationHide:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain('QUIT_CONFIRMATION_SHOW_CHANNEL')
    expect(functionSource).toContain(
      'return getRendererRpcClient().subscribe(QUIT_CONFIRMATION_SHOW_CHANNEL, () => {'
    )
    expect(functionSource).toContain('callback()')
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.onQuitConfirmationShow')
    expect(functionSource).not.toContain('bridge.onQuitConfirmationShow')
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('removes the old desktop quit-confirmation-hide preload compatibility listener and duplicate event path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const shortcutEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/shortcut-events.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadSource).not.toContain('DESKTOP_QUIT_CONFIRMATION_HIDE_CHANNEL')
    expect(preloadSource).not.toContain('desktop:quitConfirmationHide')
    expect(preloadSource).not.toContain('onQuitConfirmationHide: (')
    expect(preloadSource).not.toContain('ipcRenderer.on(DESKTOP_QUIT_CONFIRMATION_HIDE_CHANNEL')
    expect(preloadSource).not.toContain('removeListener(DESKTOP_QUIT_CONFIRMATION_HIDE_CHANNEL')
    expect(preloadDesktopBridgeSource).not.toContain('onQuitConfirmationHide: (')
    expect(desktopBridgeSource).not.toContain('DESKTOP_QUIT_CONFIRMATION_HIDE_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:quitConfirmationHide')
    expect(shortcutEventsSource).not.toContain('DESKTOP_QUIT_CONFIRMATION_HIDE_CHANNEL')
    expect(shortcutEventsSource).not.toContain('webContents.send(QUIT_CONFIRMATION_HIDE_CHANNEL')
    expect(shortcutEventsSource).not.toContain(
      'window.webContents.send(QUIT_CONFIRMATION_HIDE_CHANNEL'
    )
    expect(shortcutEventsSource).toContain('emitQuitConfirmationHide = (): void')
    expect(shortcutEventsSource).not.toContain('emitQuitConfirmationHide = (_window')
    expect(desktopBridgeTypeSource).not.toContain('readonly onQuitConfirmationHide?:')
  })

  it('routes systemApi quit-confirmation-hide directly through WebSocket after desktop bridge event migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'onQuitConfirmationHide: (callback: () => void): (() => void) =>'
    )
    const functionEnd = source.indexOf('  onEditPaste:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain('QUIT_CONFIRMATION_HIDE_CHANNEL')
    expect(functionSource).toContain(
      'return getRendererRpcClient().subscribe(QUIT_CONFIRMATION_HIDE_CHANNEL, () => {'
    )
    expect(functionSource).toContain('callback()')
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.onQuitConfirmationHide')
    expect(functionSource).not.toContain('bridge.onQuitConfirmationHide')
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('removes the old desktop edit paste preload compatibility listener and duplicate event path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const editEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/edit-events.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadSource).not.toContain('DESKTOP_EDIT_PASTE_CHANNEL')
    expect(preloadSource).not.toContain('desktop:editPaste')
    expect(preloadSource).not.toContain('onEditPaste: (')
    expect(preloadSource).not.toContain('ipcRenderer.on(DESKTOP_EDIT_PASTE_CHANNEL')
    expect(preloadSource).not.toContain('removeListener(DESKTOP_EDIT_PASTE_CHANNEL')
    expect(preloadDesktopBridgeSource).not.toContain('onEditPaste: (')
    expect(desktopBridgeSource).not.toContain('DESKTOP_EDIT_PASTE_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:editPaste')
    expect(editEventsSource).not.toContain('DESKTOP_EDIT_PASTE_CHANNEL')
    expect(editEventsSource).not.toContain('webContents.send(EDIT_PASTE_CHANNEL')
    expect(editEventsSource).not.toContain('window.webContents.send(EDIT_PASTE_CHANNEL')
    expect(editEventsSource).not.toContain('_window')
    expect(editEventsSource).toContain('emitEditPaste = (text: string): void')
    expect(desktopBridgeTypeSource).not.toContain('readonly onEditPaste?:')
  })

  it('routes systemApi edit-paste directly through WebSocket after desktop bridge event migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'onEditPaste: (callback: (text: string) => void): (() => void) =>'
    )
    const functionEnd = source.indexOf('  onMenuAction:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain('EDIT_PASTE_CHANNEL')
    expect(functionSource).toContain(
      'return getRendererRpcClient().subscribe(EDIT_PASTE_CHANNEL, (event: ServerEvent) => {'
    )
    expect(functionSource).toContain("if (typeof event.payload === 'string') {")
    expect(functionSource).toContain('callback(event.payload)')
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.onEditPaste')
    expect(functionSource).not.toContain('bridge.onEditPaste')
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('routes menu open-in-editor through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useKeyboardShortcuts.ts'),
      'utf-8'
    )
    const handlerStart = source.indexOf("on('menu:open-in-editor', () => {")
    const handlerEnd = source.indexOf("on('menu:open-in-terminal'", handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(handlerSource).toContain('worktreeApi.openInEditor(worktreePath).catch(console.error)')
    expect(handlerSource).not.toContain('window.worktreeOps.openInEditor')
    expect(handlerSource).not.toContain('unwrapEnvelope')
  })

  it('routes menu open-in-terminal through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useKeyboardShortcuts.ts'),
      'utf-8'
    )
    const handlerStart = source.indexOf("on('menu:open-in-terminal', () => {")
    const handlerEnd = source.indexOf("on('menu:command-palette'", handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(handlerSource).toContain('worktreeApi.openInTerminal(worktreePath).catch(console.error)')
    expect(handlerSource).not.toContain('window.worktreeOps.openInTerminal')
    expect(handlerSource).not.toContain('unwrapEnvelope')
  })

  it('routes command palette open-in-editor through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useCommands.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf("id: 'action:open-in-editor'")
    const nextActionStart = source.indexOf("id: 'action:open-in-terminal'", actionStart)
    const actionSource = source.slice(actionStart, nextActionStart)

    expect(actionStart).toBeGreaterThan(-1)
    expect(nextActionStart).toBeGreaterThan(actionStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(actionSource).toContain('await worktreeApi.openInEditor(worktreePath)')
    expect(actionSource).toContain("toast.success('Opened in editor')")
    expect(actionSource).toContain("toast.error('Failed to open in editor')")
    expect(actionSource).not.toContain('window.worktreeOps.openInEditor')
    expect(actionSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes command palette open-in-terminal through settingsApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useCommands.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf("id: 'action:open-in-terminal'")
    const nextActionStart = source.indexOf("id: 'action:reveal-in-finder'", actionStart)
    const actionSource = source.slice(actionStart, nextActionStart)

    expect(actionStart).toBeGreaterThan(-1)
    expect(nextActionStart).toBeGreaterThan(actionStart)
    expect(source).toContain("import { settingsApi } from '@/api/settings-api'")
    expect(actionSource).toContain('const result = await settingsApi.openWithTerminal(')
    expect(actionSource).toContain('worktreePath')
    expect(actionSource).toContain('defaultTerminal')
    expect(actionSource).toContain(
      "defaultTerminal === 'custom' ? customTerminalCommand : undefined"
    )
    expect(actionSource).toContain('if (!result.success) throw new Error(result.error ??')
    expect(actionSource).toContain("toast.success('Opened in terminal')")
    expect(actionSource).toContain("toast.error('Failed to open in terminal')")
    expect(actionSource).not.toContain('window.settingsOps.openWithTerminal')
    expect(actionSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes command palette add-project directory selection through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useCommands.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf("id: 'action:add-project'")
    const nextActionStart = source.indexOf("id: 'action:open-in-editor'", actionStart)
    const actionSource = source.slice(actionStart, nextActionStart)

    expect(actionStart).toBeGreaterThan(-1)
    expect(nextActionStart).toBeGreaterThan(actionStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(actionSource).toContain('const selectedPath = await projectApi.openDirectoryDialog()')
    expect(actionSource).toContain('await useProjectStore.getState().addProject(selectedPath)')
    expect(actionSource).toContain("toast.success('Project added successfully')")
    expect(actionSource).toContain("toast.error(addResult.error || 'Failed to add project')")
    expect(actionSource).not.toContain('window.projectOps.openDirectoryDialog')
    expect(actionSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes projectApi directory selection through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/project-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/project-ops.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('openDirectoryDialog: async (): Promise<string | null> =>')
    const functionEnd = source.indexOf('  openPath: async', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)
    const serverFunctionStart = serverSource.indexOf('openDirectoryDialog: () =>')
    const serverFunctionEnd = serverSource.indexOf('  showInFolder:', serverFunctionStart)
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(source).not.toContain("import { getDesktopBridge } from './desktop-bridge'")
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.pickFolder')
    expect(functionSource).not.toContain('bridge.pickFolder()')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<string | null>('projectOps.openDirectoryDialog', {})"
    )
    expect(functionSource).not.toContain('window.projectOps.openDirectoryDialog')
    expect(serverFunctionSource).toContain('requestProjectOpenDirectoryDialog()')
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('showOpenDialog')
  })

  it('routes projectApi reveal-in-folder through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/project-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/project-ops.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('showInFolder: async (path: string): Promise<void> =>')
    const functionEnd = source.indexOf('  copyToClipboard: async', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)
    const serverFunctionStart = serverSource.indexOf('showInFolder: (path) =>')
    const serverFunctionEnd = serverSource.indexOf('  openPath:', serverFunctionStart)
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.showItemInFolder')
    expect(functionSource).not.toContain('bridge.showItemInFolder(path)')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<void>('projectOps.showInFolder', { path })"
    )
    expect(functionSource).not.toContain('window.projectOps.showInFolder')
    expect(serverFunctionSource).toContain('requestProjectShowInFolder(path)')
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('showItemInFolder')
  })

  it('routes projectApi openPath through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/project-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/project-ops.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('openPath: async (path: string): Promise<void> =>')
    const functionEnd = source.indexOf('  showInFolder: async', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)
    const serverFunctionStart = serverSource.indexOf('openPath: (path) =>')
    const serverFunctionEnd = serverSource.indexOf('  copyToClipboard:', serverFunctionStart)
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.openPath')
    expect(functionSource).not.toContain('bridge.openPath(path)')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<void>('projectOps.openPath', { path })"
    )
    expect(functionSource).not.toContain('window.projectOps.openPath')
    expect(serverFunctionSource).toContain('requestProjectOpenPath(path)')
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('shell.openPath')
  })

  it('routes projectApi copyToClipboard through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/project-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/project-ops.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('copyToClipboard: async (text: string): Promise<void> =>')
    const functionEnd = source.indexOf('  readFromClipboard: async', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)
    const serverFunctionStart = serverSource.indexOf('copyToClipboard: (text) =>')
    const serverFunctionEnd = serverSource.indexOf('  readFromClipboard:', serverFunctionStart)
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.writeClipboardText')
    expect(functionSource).not.toContain('bridge.writeClipboardText(text)')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<void>('projectOps.copyToClipboard', { text })"
    )
    expect(functionSource).not.toContain('window.projectOps.copyToClipboard')
    expect(serverFunctionSource).toContain('requestProjectWriteClipboardText(text)')
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('clipboard.writeText')
  })

  it('routes projectApi readFromClipboard through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/project-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/project-ops.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('readFromClipboard: async (): Promise<string> =>')
    const functionEnd = source.indexOf('  validateProject: async', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)
    const serverFunctionStart = serverSource.indexOf('readFromClipboard: () =>')
    const serverFunctionEnd = serverSource.indexOf('  isGitRepository:', serverFunctionStart)
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.readClipboardText')
    expect(functionSource).not.toContain('bridge.readClipboardText()')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<string>('projectOps.readFromClipboard', {})"
    )
    expect(functionSource).not.toContain('window.projectOps.readFromClipboard')
    expect(serverFunctionSource).toContain('requestProjectReadClipboardText()')
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('clipboard.readText')
  })

  it('routes projectApi isGitRepository through RPC instead of the compatibility global', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/project-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'isGitRepository: async (path: string): Promise<boolean> =>'
    )
    const functionEnd = source.indexOf('  validateProject: async', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).toContain(
      "getRendererRpcClient().request<boolean>('projectOps.isGitRepository', { path })"
    )
    expect(functionSource).not.toContain('window.projectOps.isGitRepository')
  })

  it('removes the old git:isRepository preload IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:isRepository'")
    expect(preloadSource).not.toContain('isGitRepository: (path: string)')
    expect(projectHandlersSource).not.toContain("defineHandler('git:isRepository'")
    expect(projectHandlersSource).not.toContain("projectFailed('git:isRepository'")
  })

  it('removes the old project:validate preload IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('project:validate'")
    expect(preloadSource).not.toContain('validateProject: (')
    expect(projectHandlersSource).not.toContain("defineHandler('project:validate'")
    expect(projectHandlersSource).not.toContain("projectFailed('project:validate'")
  })

  it('removes the old project:detectLanguage preload IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('project:detectLanguage'")
    expect(preloadSource).not.toContain('detectLanguage: (projectPath: string)')
    expect(projectHandlersSource).not.toContain("defineHandler('project:detectLanguage'")
    expect(projectHandlersSource).not.toContain("projectFailed('project:detectLanguage'")
  })

  it('removes the old project:detectSetupSuggestions preload IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('project:detectSetupSuggestions'")
    expect(preloadSource).not.toContain('detectSetupSuggestions: (projectPath: string)')
    expect(projectHandlersSource).not.toContain("defineHandler('project:detectSetupSuggestions'")
    expect(projectHandlersSource).not.toContain("projectFailed('project:detectSetupSuggestions'")
  })

  it('removes the old project:findXcworkspace preload IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('project:findXcworkspace'")
    expect(preloadSource).not.toContain('findXcworkspace: (projectPath: string)')
    expect(projectHandlersSource).not.toContain("defineHandler('project:findXcworkspace'")
  })

  it('removes the old project:isAndroidProject preload IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('project:isAndroidProject'")
    expect(preloadSource).not.toContain('isAndroidProject: (projectPath: string)')
    expect(projectHandlersSource).not.toContain("defineHandler('project:isAndroidProject'")
  })

  it('removes the old project:loadLanguageIcons preload IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('project:loadLanguageIcons'")
    expect(preloadSource).not.toContain('loadLanguageIcons: ()')
    expect(projectHandlersSource).not.toContain("defineHandler('project:loadLanguageIcons'")
    expect(projectHandlersSource).not.toContain("projectFailed('project:loadLanguageIcons'")
  })

  it('removes the old project:getIconPath preload IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('project:getIconPath'")
    expect(preloadSource).not.toContain('getProjectIconPath: (filename: string)')
    expect(projectHandlersSource).not.toContain("defineHandler('project:getIconPath'")
    expect(projectHandlersSource).not.toContain("projectFailed('project:getIconPath'")
  })

  it('removes the old project:getAbsoluteIconDataUrl preload IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('project:getAbsoluteIconDataUrl'")
    expect(preloadSource).not.toContain('getAbsoluteIconDataUrl: (absolutePath: string)')
    expect(projectHandlersSource).not.toContain("defineHandler('project:getAbsoluteIconDataUrl'")
    expect(projectHandlersSource).not.toContain("projectFailed('project:getAbsoluteIconDataUrl'")
  })

  it('removes the old project:detectFavicon preload IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('project:detectFavicon'")
    expect(preloadSource).not.toContain('detectFavicon: (projectPath: string)')
    expect(projectHandlersSource).not.toContain("defineHandler('project:detectFavicon'")
    expect(projectHandlersSource).not.toContain("projectFailed('project:detectFavicon'")
  })

  it('removes the old project:removeIcon preload IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('project:removeIcon'")
    expect(preloadSource).not.toContain('removeProjectIcon: (projectId: string)')
    expect(projectHandlersSource).not.toContain("defineHandler('project:removeIcon'")
    expect(projectHandlersSource).not.toContain("projectFailed('project:removeIcon'")
  })

  it('removes the old project:pickIcon main IPC handler after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('project:pickIcon'")
    expect(projectHandlersSource).not.toContain("defineHandler('project:pickIcon'")
    expect(projectHandlersSource).not.toContain("projectFailed('project:pickIcon'")
    expect(projectHandlersSource).not.toContain('saveProjectIcon(projectId')
  })

  it('removes the old dialog:openDirectory main IPC handler after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('dialog:openDirectory'")
    expect(projectHandlersSource).not.toContain("defineHandler('dialog:openDirectory'")
    expect(projectHandlersSource).not.toContain("projectFailed('dialog:openDirectory'")
    expect(projectHandlersSource).not.toContain("title: 'Select Project Folder'")
  })

  it('removes the old git:init preload IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:init'")
    expect(preloadSource).not.toContain('initRepository: (path: string)')
    expect(projectHandlersSource).not.toContain("defineHandler('git:init'")
    expect(projectHandlersSource).not.toContain("projectFailed('git:init'")
  })

  it('removes the old shell:showItemInFolder main IPC handler after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('shell:showItemInFolder'")
    expect(projectHandlersSource).not.toContain("defineHandler('shell:showItemInFolder'")
    expect(projectHandlersSource).not.toContain("projectFailed('shell:showItemInFolder'")
    expect(projectHandlersSource).not.toContain('shell.showItemInFolder(path)')
  })

  it('removes the old shell:openPath main IPC handler after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('shell:openPath'")
    expect(projectHandlersSource).not.toContain("defineHandler('shell:openPath'")
    expect(projectHandlersSource).not.toContain("projectFailed('shell:openPath'")
    expect(projectHandlersSource).not.toContain('shell.openPath(path)')
  })

  it('removes the old clipboard:writeText main IPC handler after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('clipboard:writeText'")
    expect(projectHandlersSource).not.toContain("defineHandler('clipboard:writeText'")
    expect(projectHandlersSource).not.toContain("projectFailed('clipboard:writeText'")
    expect(projectHandlersSource).not.toContain('clipboard.writeText(text)')
  })

  it('removes the old clipboard:readText main IPC handler after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const projectHandlersSource = readOptionalSource('../../src/main/ipc/project-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('clipboard:readText'")
    expect(projectHandlersSource).not.toContain("defineHandler('clipboard:readText'")
    expect(projectHandlersSource).not.toContain("projectFailed('clipboard:readText'")
    expect(projectHandlersSource).not.toContain('clipboard.readText()')
  })

  it('routes command palette reveal-in-finder through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useCommands.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf("id: 'action:reveal-in-finder'")
    const nextSectionStart = source.indexOf('// =====================', actionStart)
    const actionSource = source.slice(actionStart, nextSectionStart)

    expect(actionStart).toBeGreaterThan(-1)
    expect(nextSectionStart).toBeGreaterThan(actionStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(actionSource).toContain('await projectApi.showInFolder(worktreePath)')
    expect(actionSource).toContain("toast.error('Please select a worktree first')")
    expect(actionSource).toContain('toast.error(`Failed to reveal in ${fileManagerName()}`)')
    expect(actionSource).toContain('closeCommandPalette()')
    expect(actionSource).not.toContain('window.projectOps.showInFolder')
    expect(actionSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes AddProjectButton directory selection through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/projects/AddProjectButton.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleAddProject = useCallback(')
    const initStart = source.indexOf('const handleInitRepository = useCallback(', handlerStart)
    const handlerSource = source.slice(handlerStart, initStart)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(initStart).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('const selectedPath = await projectApi.openDirectoryDialog()')
    expect(handlerSource).toContain('const result = await addProject(selectedPath)')
    expect(handlerSource).toContain('projectToast.added(selectedPath.split')
    expect(handlerSource).toContain('setGitInitPath(selectedPath)')
    expect(handlerSource).toContain("toast.error(result.error || 'Failed to add project'")
    expect(handlerSource).not.toContain('window.projectOps.openDirectoryDialog')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes AddProjectButton repository initialization through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/projects/AddProjectButton.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleInitRepository = useCallback(')
    const renderStart = source.indexOf('return (', handlerStart)
    const handlerSource = source.slice(handlerStart, renderStart)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(renderStart).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('if (!gitInitPath) return')
    expect(handlerSource).toContain(
      'const initResult = await projectApi.initRepository(gitInitPath)'
    )
    expect(handlerSource).toContain(
      "toast.error(initResult.error || 'Failed to initialize repository')"
    )
    expect(handlerSource).toContain("toast.success('Git repository initialized')")
    expect(handlerSource).toContain('const addResult = await addProject(gitInitPath)')
    expect(handlerSource).toContain('setGitInitPath(null)')
    expect(handlerSource).not.toContain('window.projectOps.initRepository')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('keeps GitInitDialog AddProjectButton tests on projectApi mocks instead of window project globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-13/session-8/git-init-dialog.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('../../../src/renderer/src/api/project-api'")
    expect(source).toContain('mockOpenDirectoryDialog = vi.mocked(projectApi.openDirectoryDialog)')
    expect(source).toContain('_mockInitRepository = vi.mocked(projectApi.initRepository)')
    expect(source).not.toContain('window.projectOps')
  })

  it('routes LanguageIcon custom language icon loading through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/projects/LanguageIcon.tsx'),
      'utf-8'
    )
    const functionStart = source.indexOf('function loadCustomIcons(): void {')
    const nextFunctionStart = source.indexOf('function useCustomIcons(): Record<string, string> {')
    const functionSource = source.slice(functionStart, nextFunctionStart)

    expect(functionStart).toBeGreaterThan(-1)
    expect(nextFunctionStart).toBeGreaterThan(functionStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(functionSource).toContain('if (customIconsCache !== null || customIconsLoading) return')
    expect(functionSource).toContain('.loadLanguageIcons()')
    expect(functionSource).toContain('customIconsCache = icons')
    expect(functionSource).toContain('for (const listener of customIconsListeners) listener()')
    expect(functionSource).toContain('customIconsCache = {}')
    expect(functionSource).not.toContain('window.projectOps.loadLanguageIcons')
    expect(functionSource).not.toContain('.then(unwrapEnvelope)')
  })

  it('routes LanguageIcon custom project icon resolution through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/projects/LanguageIcon.tsx'),
      'utf-8'
    )
    const hookStart = source.indexOf(
      'function useProjectIconUrl(customIcon: string | null | undefined): string | null {'
    )
    const nextCacheStart = source.indexOf(
      '// Module-level cache for resolved detected icon paths',
      hookStart
    )
    const hookSource = source.slice(hookStart, nextCacheStart)

    expect(hookStart).toBeGreaterThan(-1)
    expect(nextCacheStart).toBeGreaterThan(hookStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(hookSource).toContain('projectIconCache.get(customIcon)')
    expect(hookSource).toContain('.getProjectIconPath(customIcon)')
    expect(hookSource).toContain('if (cancelled) return')
    expect(hookSource).toContain('projectIconCache.set(customIcon, dataUrl)')
    expect(hookSource).toContain('setUrl(dataUrl)')
    expect(hookSource).toContain('cancelled = true')
    expect(hookSource).not.toContain('window.projectOps.getProjectIconPath')
    expect(hookSource).not.toContain('.then(unwrapEnvelope)')
  })

  it('routes projectApi getProjectIconPath through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/project-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/project-ops.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'getProjectIconPath: async (filename: string): Promise<string | null> =>'
    )
    const functionEnd = source.indexOf('  getAbsoluteIconDataUrl:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)
    const serverFunctionStart = serverSource.indexOf('getProjectIconPath: (filename) =>')
    const serverFunctionEnd = serverSource.indexOf('  detectFavicon:', serverFunctionStart)
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.getProjectIconPath')
    expect(functionSource).not.toContain('bridge.getProjectIconPath(filename)')
    expect(functionSource).toContain(
      "getRendererRpcClient().request<string | null>('projectOps.getProjectIconPath', { filename })"
    )
    expect(functionSource).not.toContain('window.projectOps')
    expect(serverFunctionSource).toContain('requestProjectGetProjectIconPath(filename)')
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('app.getPath')
    expect(serverFunctionSource).not.toContain('getProjectIconDataUrl(filename, iconDir)')
  })

  it('routes LanguageIcon detected icon resolution through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/projects/LanguageIcon.tsx'),
      'utf-8'
    )
    const hookStart = source.indexOf(
      'function useDetectedIconUrl(detectedIcon: string | null | undefined): string | null {'
    )
    const componentStart = source.indexOf('export function LanguageIcon(', hookStart)
    const hookSource = source.slice(hookStart, componentStart)

    expect(hookStart).toBeGreaterThan(-1)
    expect(componentStart).toBeGreaterThan(hookStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
    expect(hookSource).toContain("if (!detectedIcon || detectedIcon === 'none')")
    expect(hookSource).toContain('detectedIconCache.get(detectedIcon)')
    expect(hookSource).toContain('.getAbsoluteIconDataUrl(detectedIcon)')
    expect(hookSource).toContain('if (cancelled) return')
    expect(hookSource).toContain('detectedIconCache.set(detectedIcon, dataUrl)')
    expect(hookSource).toContain('setUrl(dataUrl)')
    expect(hookSource).toContain('cancelled = true')
    expect(hookSource).not.toContain('window.projectOps.getAbsoluteIconDataUrl')
    expect(hookSource).not.toContain('.then(unwrapEnvelope)')
  })

  it('routes ProjectSettingsDialog setup suggestion detection through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/projects/ProjectSettingsDialog.tsx'
      ),
      'utf-8'
    )
    const effectStart = source.indexOf('// Load current values when dialog opens')
    const pickIconStart = source.indexOf('const handlePickIcon = async (): Promise<void> => {')
    const effectSource = source.slice(effectStart, pickIconStart)

    expect(effectStart).toBeGreaterThan(-1)
    expect(pickIconStart).toBeGreaterThan(effectStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(effectSource).toContain('if (project.is_remote === true)')
    expect(effectSource).toContain('setSuggestions([])')
    expect(effectSource).toContain('.detectSetupSuggestions(project.path)')
    expect(effectSource).toContain('setSuggestions(nextSuggestions)')
    expect(effectSource).toContain('cancelled = true')
    expect(effectSource).not.toContain('window.projectOps.detectSetupSuggestions')
    expect(effectSource).not.toContain('unwrapEnvelope(envelope)')
  })

  it('routes ProjectSettingsDialog project icon picking through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/projects/ProjectSettingsDialog.tsx'
      ),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handlePickIcon = async (): Promise<void> => {')
    const clearStart = source.indexOf('const handleClearIcon = async (): Promise<void> => {')
    const handlerSource = source.slice(handlerStart, clearStart)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(clearStart).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(handlerSource).toContain('setPickingIcon(true)')
    expect(handlerSource).toContain('const result = await projectApi.pickProjectIcon(project.id)')
    expect(handlerSource).toContain('if (result.success && result.filename)')
    expect(handlerSource).toContain('setCustomIcon(result.filename)')
    expect(handlerSource).toContain("toast.error('Failed to pick icon')")
    expect(handlerSource).toContain('setPickingIcon(false)')
    expect(handlerSource).not.toContain('window.projectOps.pickProjectIcon')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes projectApi pickProjectIcon through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/project-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/project-ops.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'pickProjectIcon: async (projectId: string): Promise<PickProjectIconResult> =>'
    )
    const functionEnd = source.indexOf('  removeProjectIcon:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)
    const serverFunctionStart = serverSource.indexOf('pickProjectIcon: (projectId) =>')
    const serverFunctionEnd = serverSource.indexOf('  removeProjectIcon:', serverFunctionStart)
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.pickProjectIcon')
    expect(functionSource).not.toContain('bridge.pickProjectIcon(projectId)')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<PickProjectIconResult>('projectOps.pickProjectIcon', {"
    )
    expect(functionSource).toContain('projectId')
    expect(functionSource).not.toContain('window.projectOps')
    expect(serverFunctionSource).toContain('requestProjectPickProjectIcon(projectId)')
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('dialog.showOpenDialog')
    expect(serverFunctionSource).not.toContain('app.getPath')
  })

  it('routes ProjectSettingsDialog project icon removal through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/projects/ProjectSettingsDialog.tsx'
      ),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleClearIcon = async (): Promise<void> => {')
    const saveStart = source.indexOf('const handleSave = async (): Promise<void> => {')
    const handlerSource = source.slice(handlerStart, saveStart)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(saveStart).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
    expect(handlerSource).toContain('await projectApi.removeProjectIcon(project.id)')
    expect(handlerSource).toContain('setCustomIcon(null)')
    expect(handlerSource).toContain("toast.error('Failed to remove icon')")
    expect(handlerSource).not.toContain('window.projectOps.removeProjectIcon')
    expect(handlerSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes projectApi removeProjectIcon through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/project-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/project-ops.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'removeProjectIcon: async (projectId: string): Promise<RemoveProjectIconResult> =>'
    )
    const functionEnd = source.indexOf('  initRepository:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)
    const serverFunctionStart = serverSource.indexOf('removeProjectIcon: (projectId) =>')
    const serverFunctionEnd = serverSource.indexOf('  getProjectIconPath:', serverFunctionStart)
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.removeProjectIcon')
    expect(functionSource).not.toContain('bridge.removeProjectIcon(projectId)')
    expect(functionSource).toContain(
      "getRendererRpcClient().request<RemoveProjectIconResult>('projectOps.removeProjectIcon', {"
    )
    expect(functionSource).toContain('projectId')
    expect(functionSource).not.toContain('window.projectOps')
    expect(serverFunctionSource).toContain('requestProjectRemoveProjectIcon(projectId)')
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('app.getPath')
    expect(serverFunctionSource).not.toContain('removeProjectIcon(projectId, iconDir)')
  })

  it('routes quit-confirmation show through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/layout/QuitConfirmationOverlay.tsx'
      ),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('const unsubscribeShow = systemApi.onQuitConfirmationShow(show)')
    expect(source).not.toContain('window.systemOps.onQuitConfirmationShow')
  })

  it('routes quit-confirmation hide through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/layout/QuitConfirmationOverlay.tsx'
      ),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('const unsubscribeHide = systemApi.onQuitConfirmationHide(hide)')
    expect(source).not.toContain('window.systemOps.onQuitConfirmationHide')
  })

  it('routes menu-state updates through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useKeyboardShortcuts.ts'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('void systemApi.updateMenuState(baseState).catch(() => {})')
    expect(source).toContain('void systemApi\n          .updateMenuState({')
    expect(source).not.toContain('window.systemOps?.updateMenuState')
    expect(source).not.toContain('window.systemOps.updateMenuState')
  })

  it('routes systemApi updateMenuState through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'updateMenuState: async (state: MenuStatePayload): Promise<void> =>'
    )
    const functionEnd = source.indexOf('  confirm:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.updateMenuState')
    expect(functionSource).not.toContain('return bridge.updateMenuState(state)')
    expect(functionSource).toContain(
      "getRendererRpcClient().request<void>('systemOps.updateMenuState', state)"
    )
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('routes systemApi quitApp through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('quitApp: async (): Promise<void> =>')
    const functionEnd = source.indexOf('  setKeepAwake:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.quitApp')
    expect(functionSource).not.toContain('return bridge.quitApp()')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<void>('systemOps.quitApp', {})"
    )
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('routes systemApi isPackaged through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('isPackaged: async (): Promise<boolean> =>')
    const functionEnd = source.indexOf('  isLogMode:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.isPackaged')
    expect(functionSource).not.toContain('return bridge.isPackaged()')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<boolean>('systemOps.isPackaged', {})"
    )
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('routes systemApi isLogMode through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('isLogMode: async (): Promise<boolean> =>')
    const functionEnd = source.indexOf('  getPlatform:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.isLogMode')
    expect(functionSource).not.toContain('return bridge.isLogMode()')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<boolean>('systemOps.isLogMode', {})"
    )
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('routes systemApi getPlatform through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('getPlatform: async (): Promise<string> =>')
    const functionEnd = source.indexOf('  getLogDir:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.getPlatform')
    expect(functionSource).not.toContain('return bridge.getPlatform()')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<string>('systemOps.getPlatform', {})"
    )
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('routes systemApi getLogDir through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('getLogDir: async (): Promise<string> =>')
    const functionEnd = source.indexOf('  getAppVersion:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.getLogDir')
    expect(functionSource).not.toContain('return bridge.getLogDir()')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<string>('systemOps.getLogDir', {})"
    )
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('removes the old system:getLogDir IPC handler after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(mainSource).not.toContain("ipcMain.handle('system:getLogDir'")
  })

  it('removes the old system:getAppVersion IPC handler after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(mainSource).not.toContain("ipcMain.handle('system:getAppVersion'")
  })

  it('removes the old system:getAppPaths IPC handler after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(mainSource).not.toContain("ipcMain.handle('system:getAppPaths'")
  })

  it('removes the old system:isLogMode IPC handler after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(mainSource).not.toContain("ipcMain.handle('system:isLogMode'")
  })

  it('removes the old system:openInChrome IPC handler after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(mainSource).not.toContain("ipcMain.handle('system:openInChrome'")
  })

  it('removes the old system:openInApp IPC handler after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(mainSource).not.toContain("ipcMain.handle('system:openInApp'")
  })

  it('removes the old system:detectAgentSdks IPC handler after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(mainSource).not.toContain("ipcMain.handle('system:detectAgentSdks'")
  })

  it('removes the old system:quitApp IPC handler after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(mainSource).not.toContain("ipcMain.handle('system:quitApp'")
  })

  it('removes the old system:isPackaged IPC handler after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(mainSource).not.toContain("ipcMain.handle('system:isPackaged'")
  })

  it('removes the old system:getPlatform IPC handler after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(mainSource).not.toContain("ipcMain.handle('system:getPlatform'")
  })

  it('removes the old system:setKeepAwake IPC handler after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(mainSource).not.toContain("ipcMain.handle('system:setKeepAwake'")
  })

  it('removes the old telemetry:track IPC path after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'telemetry:track'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('telemetry:track'")
  })

  it('removes the old telemetry:setEnabled IPC path after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'telemetry:setEnabled'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('telemetry:setEnabled'")
  })

  it('removes the old telemetry:isEnabled IPC path after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'telemetry:isEnabled'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('telemetry:isEnabled'")
    expect(preloadSource).not.toContain("exposeInMainWorld('analyticsOps'")
  })

  it('removes the old perf-diagnostics:enable IPC path after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'perf-diagnostics:enable'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('perf-diagnostics:enable'")
  })

  it('removes the old perf-diagnostics:snapshot IPC path after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'perf-diagnostics:snapshot'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('perf-diagnostics:snapshot'")
    expect(preloadSource).not.toContain("exposeInMainWorld('perfDiagnosticsOps'")
  })

  it('removes the old codex-debug-logger:configure IPC path after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )

    expect(mainSource).not.toContain("'codex-debug-logger:configure'")
    expect(preloadSource).not.toContain("ipcRenderer.invoke('codex-debug-logger:configure'")
    expect(preloadSource).not.toContain("exposeInMainWorld('codexDebugLoggerOps'")
  })

  it('removes the old menu:updateState IPC handler after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(mainSource).not.toContain("'menu:updateState'")
  })

  it('routes systemApi getAppVersion through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('getAppVersion: async (): Promise<string> =>')
    const functionEnd = source.indexOf('  getAppPaths:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.getAppVersion')
    expect(functionSource).not.toContain('return bridge.getAppVersion()')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<string>('systemOps.getAppVersion', {})"
    )
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('routes systemApi getAppPaths through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('getAppPaths: async (): Promise<SystemAppPaths> =>')
    const functionEnd = source.indexOf('  quitApp:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).not.toContain("import type { DesktopAppPaths } from '@shared/desktop-bridge'")
    expect(source).toContain("import type { SystemAppPaths } from '@shared/system-types'")
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.getAppPaths')
    expect(functionSource).not.toContain('return bridge.getAppPaths()')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<SystemAppPaths>('systemOps.getAppPaths', {})"
    )
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('keeps polish logging tests on systemApi mocks instead of window system globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/session-10/polish.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/system-api'")
    expect(source).toContain('vi.mocked(systemApi.getLogDir).mockResolvedValue')
    expect(source).toContain('vi.mocked(systemApi.getAppPaths).mockResolvedValue')
    expect(source).toContain('const logDir = await systemApi.getLogDir()')
    expect(source).toContain('const paths = await systemApi.getAppPaths()')
    expect(source).not.toContain('window.systemOps')
    expect(source).not.toContain("Object.defineProperty(window, 'systemOps'")
  })

  it('routes systemApi setKeepAwake through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('setKeepAwake: async (active: boolean): Promise<void> =>')
    const functionEnd = source.indexOf('  setSessionQueuedState:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.setKeepAwake')
    expect(functionSource).not.toContain('return bridge.setKeepAwake(active)')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<void>('systemOps.setKeepAwake', { active })"
    )
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('routes systemApi detectAgentSdks through RPC after desktop bridge IPC migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'detectAgentSdks: async (): Promise<AgentSdkDetectionResult> =>'
    )
    const functionEnd = source.indexOf('  updateMenuState:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.detectAgentSdks')
    expect(functionSource).not.toContain('return bridge.detectAgentSdks()')
    expect(functionSource).toContain(
      "return getRendererRpcClient().request<AgentSdkDetectionResult>('systemOps.detectAgentSdks', {})"
    )
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('removes the old desktop notification navigation preload compatibility listener and duplicate event path', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const desktopBridgeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/shared/desktop-bridge.ts'),
      'utf-8'
    )
    const notificationEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/notification-events.ts'),
      'utf-8'
    )
    const desktopBridgeTypeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/desktop-bridge.ts'),
      'utf-8'
    )
    const preloadDesktopBridgeStart = preloadTypesSource.indexOf('desktopBridge: {')
    const preloadDesktopBridgeEnd = preloadTypesSource.indexOf(
      '\n  }',
      preloadDesktopBridgeStart
    )
    const preloadDesktopBridgeSource = preloadTypesSource.slice(
      preloadDesktopBridgeStart,
      preloadDesktopBridgeEnd
    )

    expect(preloadDesktopBridgeStart).toBeGreaterThan(-1)
    expect(preloadDesktopBridgeEnd).toBeGreaterThan(preloadDesktopBridgeStart)
    expect(preloadSource).not.toContain('DESKTOP_NOTIFICATION_NAVIGATE_CHANNEL')
    expect(preloadSource).not.toContain('desktop:notificationNavigate')
    expect(preloadSource).not.toContain('onNotificationNavigate: (')
    expect(preloadSource).not.toContain('ipcRenderer.on(DESKTOP_NOTIFICATION_NAVIGATE_CHANNEL')
    expect(preloadSource).not.toContain('removeListener(DESKTOP_NOTIFICATION_NAVIGATE_CHANNEL')
    expect(preloadDesktopBridgeSource).not.toContain('onNotificationNavigate: (')
    expect(desktopBridgeSource).not.toContain('DESKTOP_NOTIFICATION_NAVIGATE_CHANNEL')
    expect(desktopBridgeSource).not.toContain('desktop:notificationNavigate')
    expect(notificationEventsSource).not.toContain('DESKTOP_NOTIFICATION_NAVIGATE_CHANNEL')
    expect(notificationEventsSource).not.toContain('webContents.send(NOTIFICATION_NAVIGATE_CHANNEL')
    expect(notificationEventsSource).not.toContain(
      'window.webContents.send(NOTIFICATION_NAVIGATE_CHANNEL'
    )
    expect(notificationEventsSource).not.toContain('_window')
    expect(notificationEventsSource).toContain(
      'emitNotificationNavigate = (payload: NotificationNavigatePayload): void'
    )
    expect(desktopBridgeTypeSource).not.toContain('readonly onNotificationNavigate?:')
  })

  it('routes systemApi notification navigation directly through WebSocket after desktop bridge event migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'onNotificationNavigate: (callback: (data: NotificationNavigatePayload) => void): (() => void) =>'
    )
    const functionEnd = source.indexOf('  onWindowFocused:', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(source).toContain('NOTIFICATION_NAVIGATE_CHANNEL')
    expect(functionSource).toContain(
      'return getRendererRpcClient().subscribe(NOTIFICATION_NAVIGATE_CHANNEL, (event: ServerEvent) => {'
    )
    expect(functionSource).toContain("typeof payload.projectId === 'string'")
    expect(functionSource).toContain("typeof payload.worktreeId === 'string'")
    expect(functionSource).toContain("typeof payload.sessionId === 'string'")
    expect(functionSource).toContain('callback({')
    expect(functionSource).not.toContain('getDesktopBridge')
    expect(functionSource).not.toContain('bridge?.onNotificationNavigate')
    expect(functionSource).not.toContain('bridge.onNotificationNavigate')
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('keeps pending feedback notification tests on backend navigation events', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../test/phase-23/session-1/pending-user-feedback-notification.test.ts'
      ),
      'utf-8'
    )

    expect(source).toContain('backendManagerMock.publishDesktopBackendEvent')
    expect(source).toContain("'notification:navigate'")
    expect(source).not.toContain('webContents')
    expect(source).not.toContain('sendSpy')
    expect(source).not.toContain('webContents.send')
  })

  it('routes systemApi openInChrome through RPC after desktop openExternal migration', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/system-api.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf(
      'openInChrome: async (url: string, customCommand?: string): Promise<OpenInChromeResult> =>'
    )
    const functionEnd = source.indexOf('\n}', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).not.toContain('const bridge = getDesktopBridge()')
    expect(functionSource).not.toContain('bridge?.openExternal')
    expect(functionSource).not.toContain('bridge.openExternal(url)')
    expect(functionSource).not.toContain('bridge?.openInChrome')
    expect(functionSource).not.toContain('return bridge.openInChrome(url, customCommand)')
    expect(functionSource).toContain(
      "getRendererRpcClient().request<OpenInChromeResult>('systemOps.openInChrome', {"
    )
    expect(functionSource).toContain('customCommand')
    expect(functionSource).not.toContain('window.systemOps')
  })

  it('routes menu-state capabilities lookup through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useKeyboardShortcuts.ts'),
      'utf-8'
    )
    const stateStart = source.indexOf('const baseState = {')
    const effectEnd = source.indexOf(
      '}, [activeSessionId, selectedWorktreeId, opencodeSessionId])',
      stateStart
    )
    const stateSource = source.slice(stateStart, effectEnd)

    expect(stateStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(stateStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(stateSource).toContain('opencodeApi\n      .capabilities(opencodeSessionId)')
    expect(stateSource).toContain(
      'canUndo: result.success ? result.capabilities?.supportsUndo : true'
    )
    expect(stateSource).toContain(
      'canRedo: result.success ? result.capabilities?.supportsRedo : true'
    )
    expect(stateSource).not.toContain('window.opencodeOps')
  })

  it('routes session response logging detection through systemApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )

    expect(source).toContain("import { systemApi } from '@/api/system-api'")
    expect(source).toContain('systemApi\n      .isLogMode()')
    expect(source).not.toContain('window.systemOps\n      .isLogMode()')
    expect(source).not.toContain('window.systemOps.isLogMode')
  })

  it('routes session response log part updates through loggingApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const partLogStart = source.indexOf("          if (event.type === 'message.part.updated') {")
    const messageLogStart = source.indexOf(
      "          } else if (event.type === 'message.updated') {",
      partLogStart
    )
    const partLogSource = source.slice(partLogStart, messageLogStart)

    expect(partLogStart).toBeGreaterThan(-1)
    expect(messageLogStart).toBeGreaterThan(partLogStart)
    expect(source).toContain("import { loggingApi } from '@/api/logging-api'")
    expect(partLogSource).toContain('loggingApi.appendResponseLog(logFilePathRef.current, {')
    expect(partLogSource).toContain("type: 'part_updated'")
    expect(partLogSource).toContain('event: event.data')
    expect(partLogSource).not.toContain('window.loggingOps.appendResponseLog')
  })

  it('routes session response log message updates through loggingApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const messageLogStart = source.indexOf(
      "          } else if (event.type === 'message.updated') {"
    )
    const idleLogStart = source.indexOf(
      "          } else if (event.type === 'session.idle') {",
      messageLogStart
    )
    const messageLogSource = source.slice(messageLogStart, idleLogStart)

    expect(messageLogStart).toBeGreaterThan(-1)
    expect(idleLogStart).toBeGreaterThan(messageLogStart)
    expect(source).toContain("import { loggingApi } from '@/api/logging-api'")
    expect(messageLogSource).toContain('loggingApi.appendResponseLog(logFilePathRef.current, {')
    expect(messageLogSource).toContain("type: 'message_updated'")
    expect(messageLogSource).toContain('event: event.data')
    expect(messageLogSource).not.toContain('window.loggingOps.appendResponseLog')
  })

  it('routes session response log idle events through loggingApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const idleLogStart = source.indexOf("          } else if (event.type === 'session.idle') {")
    const loggingCatchStart = source.indexOf('        } catch {', idleLogStart)
    const idleLogSource = source.slice(idleLogStart, loggingCatchStart)

    expect(idleLogStart).toBeGreaterThan(-1)
    expect(loggingCatchStart).toBeGreaterThan(idleLogStart)
    expect(source).toContain("import { loggingApi } from '@/api/logging-api'")
    expect(idleLogSource).toContain('loggingApi.appendResponseLog(logFilePathRef.current, {')
    expect(idleLogSource).toContain("type: 'session_idle'")
    expect(idleLogSource).not.toContain('window.loggingOps.appendResponseLog')
  })

  it('routes reconnect response log creation through loggingApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const reconnectLogStart = source.indexOf(
      '            // Create response log file if logging is enabled'
    )
    const reconnectStatusStart = source.indexOf(
      "            setViewState({ status: 'connected' })",
      reconnectLogStart
    )
    const reconnectLogSource = source.slice(reconnectLogStart, reconnectStatusStart)

    expect(reconnectLogStart).toBeGreaterThan(-1)
    expect(reconnectStatusStart).toBeGreaterThan(reconnectLogStart)
    expect(source).toContain("import { loggingApi } from '@/api/logging-api'")
    expect(reconnectLogSource).toContain('await loggingApi.createResponseLog(sessionId)')
    expect(reconnectLogSource).toContain('logFilePathRef.current = logPath')
    expect(reconnectLogSource).not.toContain('window.loggingOps.createResponseLog')
  })

  it('routes first-time connect response log creation through loggingApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const reconnectLogStart = source.indexOf(
      '            // Create response log file if logging is enabled'
    )
    const connectLogStart = source.indexOf(
      '          // Create response log file if logging is enabled',
      reconnectLogStart + 1
    )
    const connectStatusStart = source.indexOf(
      "          setViewState({ status: 'connected' })",
      connectLogStart
    )
    const connectLogSource = source.slice(connectLogStart, connectStatusStart)

    expect(reconnectLogStart).toBeGreaterThan(-1)
    expect(connectLogStart).toBeGreaterThan(reconnectLogStart)
    expect(connectStatusStart).toBeGreaterThan(connectLogStart)
    expect(source).toContain("import { loggingApi } from '@/api/logging-api'")
    expect(connectLogSource).toContain('await loggingApi.createResponseLog(sessionId)')
    expect(connectLogSource).toContain('logFilePathRef.current = logPath')
    expect(connectLogSource).not.toContain('window.loggingOps.createResponseLog')
  })

  it('removes the old logging:createResponseLog IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(preloadSource).not.toContain("'logging:createResponseLog'")
    expect(mainSource).not.toContain("ipcMain.handle('logging:createResponseLog'")
  })

  it('removes the old logging:appendResponseLog IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(preloadSource).not.toContain("'logging:appendResponseLog'")
    expect(mainSource).not.toContain("ipcMain.handle('logging:appendResponseLog'")
  })

  it('removes the old empty response logging IPC handler registrar after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(mainSource).not.toContain('function registerLoggingHandlers')
    expect(mainSource).not.toContain('registerLoggingHandlers()')
    expect(mainSource).not.toContain('Response logging IPC handlers migrated to RPC')
    expect(mainSource).not.toContain('Registering response logging handlers')
  })

  it('removes the old empty loggingOps preload global exposure', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const loggingOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('loggingOps'")
    expect(preloadSource).not.toContain('window.loggingOps = loggingOps')
    expect(preloadTypesSource).not.toContain('loggingOps:')
    expect(preloadTypesSource).not.toContain('createResponseLog: (sessionId: string)')
    expect(preloadTypesSource).not.toContain('appendResponseLog: (filePath: string')
  })

  it('routes user prompt response logging through loggingApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const userPromptLogStart = source.indexOf(
      '        // Log user prompt if response logging is active'
    )
    const sendToOpenCodeStart = source.indexOf(
      '        // Send to OpenCode if connected',
      userPromptLogStart
    )
    const userPromptLogSource = source.slice(userPromptLogStart, sendToOpenCodeStart)

    expect(userPromptLogStart).toBeGreaterThan(-1)
    expect(sendToOpenCodeStart).toBeGreaterThan(userPromptLogStart)
    expect(source).toContain("import { loggingApi } from '@/api/logging-api'")
    expect(userPromptLogSource).toContain('loggingApi.appendResponseLog(logFilePathRef.current, {')
    expect(userPromptLogSource).toContain("type: 'user_prompt'")
    expect(userPromptLogSource).toContain('content: trimmedValue')
    expect(userPromptLogSource).toContain('mode: currentMode')
    expect(userPromptLogSource).not.toContain('window.loggingOps.appendResponseLog')
  })

  it('routes theme setting load through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useThemeStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const value = await dbApi.setting.get(THEME_SETTING_KEY)')
    expect(source).not.toContain('const value = await db.setting.get(THEME_SETTING_KEY)')
  })

  it('removes the old db:setting:get IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:setting:get'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:setting:get'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:setting:get'")
  })

  it('routes theme setting save through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useThemeStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('await dbApi.setting.set(THEME_SETTING_KEY, themeId)')
    expect(source).not.toContain('await db.setting.set(THEME_SETTING_KEY, themeId)')
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
  })

  it('routes seen-tip setting load through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useTipStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const value = await dbApi.setting.get(SEEN_TIPS_DB_KEY)')
    expect(source).not.toContain('const value = await db.setting.get(SEEN_TIPS_DB_KEY)')
  })

  it('routes seen-tip setting save through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useTipStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain(
      'await dbApi.setting.set(SEEN_TIPS_DB_KEY, JSON.stringify(seenTipIds))'
    )
    expect(source).not.toContain('await db.setting.set(SEEN_TIPS_DB_KEY')
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
  })

  it('routes app settings load through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSettingsStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const value = await dbApi.setting.get(APP_SETTINGS_DB_KEY)')
    expect(source).not.toContain('const value = await db.setting.get(APP_SETTINGS_DB_KEY)')
  })

  it('routes app settings save through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSettingsStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain(
      'await dbApi.setting.set(APP_SETTINGS_DB_KEY, JSON.stringify(settings))'
    )
    expect(source).not.toContain('await db.setting.set(APP_SETTINGS_DB_KEY')
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
  })

  it('routes keyboard shortcut setting load through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useShortcutStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const value = await dbApi.setting.get(SHORTCUTS_SETTING_KEY)')
    expect(source).not.toContain('const value = await db.setting.get(SHORTCUTS_SETTING_KEY)')
  })

  it('routes keyboard shortcut setting save through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useShortcutStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain(
      'await dbApi.setting.set(SHORTCUTS_SETTING_KEY, JSON.stringify(bindings))'
    )
    expect(source).not.toContain('await db.setting.set(SHORTCUTS_SETTING_KEY')
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
  })

  it('routes provider setting save through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/lib/provider-settings.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain(
      'await dbApi.setting.set(PROVIDER_SETTINGS_KEY, JSON.stringify(settings))'
    )
    expect(source).not.toContain('await db.setting.set(PROVIDER_SETTINGS_KEY')
  })

  it('removes the old db:setting:set IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:setting:set'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:setting:set'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:setting:set'")
  })

  it('removes the old db:setting:delete IPC adapter after RPC migration', () => {
    const dbApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/db-api.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(dbApiSource).toContain("request<boolean>('db.setting.delete', { key })")
    expect(preloadSource).not.toContain("'db:setting:delete'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:setting:delete'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:setting:delete'")
  })

  it('removes the old db:setting:getAll IPC adapter after RPC migration', () => {
    const dbApiSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/db-api.ts'),
      'utf-8'
    )
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(dbApiSource).toContain("request<Setting[]>('db.setting.getAll', {})")
    expect(preloadSource).not.toContain("'db:setting:getAll'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:setting:getAll'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:setting:getAll'")
  })

  it('routes provider setting load through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/lib/provider-settings.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const value = await dbApi.setting.get(PROVIDER_SETTINGS_KEY)')
    expect(source).not.toContain('const value = await db.setting.get(PROVIDER_SETTINGS_KEY)')
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
  })

  it('routes session history search through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionHistoryStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const results = await dbApi.session.search<SessionWithWorktree>({')
    expect(source).not.toContain('const results = await db.session.search({')
  })

  it('routes active session loading through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain(
      'const sessions = await dbApi.session.getActiveByWorktree<Session>(worktreeId)'
    )
    expect(source).not.toContain(
      'const sessions = await db.session.getActiveByWorktree(worktreeId)'
    )
  })

  it('routes loadSessions pinned-session loading through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const loadSessionsStart = source.indexOf('loadSessions: async (worktreeId: string')
    const loadPinnedSessionsStart = source.indexOf('loadPinnedSessions: async (worktreeId: string')
    const loadSessionsSource = source.slice(loadSessionsStart, loadPinnedSessionsStart)

    expect(loadSessionsStart).toBeGreaterThan(-1)
    expect(loadPinnedSessionsStart).toBeGreaterThan(loadSessionsStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(loadSessionsSource).toContain(
      'const pinnedSessions = await dbApi.session.getPinnedSessions<Session>(worktreeId)'
    )
    expect(loadSessionsSource).not.toContain(
      'const pinnedSessions = await db.session.getPinnedSessions(worktreeId)'
    )
  })

  it('routes standalone pinned-session loading through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const loadPinnedSessionsStart = source.indexOf('loadPinnedSessions: async (worktreeId: string')
    const loadPinnedSessionsEnd = source.indexOf(
      'closeOrphanedSessions: () =>',
      loadPinnedSessionsStart
    )
    const loadPinnedSessionsSource = source.slice(loadPinnedSessionsStart, loadPinnedSessionsEnd)

    expect(loadPinnedSessionsStart).toBeGreaterThan(-1)
    expect(loadPinnedSessionsEnd).toBeGreaterThan(loadPinnedSessionsStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(loadPinnedSessionsSource).toContain(
      'const pinnedSessions = await dbApi.session.getPinnedSessions<Session>(worktreeId)'
    )
    expect(loadPinnedSessionsSource).not.toContain(
      'const pinnedSessions = await db.session.getPinnedSessions(worktreeId)'
    )
    expect(source).not.toContain(
      'const pinnedSessions = await db.session.getPinnedSessions(worktreeId)'
    )
  })

  it('routes board pin persistence through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const pinStart = source.indexOf('pinSessionToBoard: async (sessionId: string)')
    const loadPinnedSessionsStart = source.indexOf(
      'loadPinnedSessions: async (worktreeId: string',
      pinStart
    )
    const pinSource = source.slice(pinStart, loadPinnedSessionsStart)

    expect(pinStart).toBeGreaterThan(-1)
    expect(loadPinnedSessionsStart).toBeGreaterThan(pinStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(pinSource).toContain('await dbApi.session.setPinnedToBoard<Session>(sessionId, true)')
    expect(pinSource).toContain(
      'dbApi.session.setPinnedToBoard<Session>(sessionId, false).catch(() => {})'
    )
    expect(pinSource).not.toContain('await db.session.setPinnedToBoard(sessionId, true)')
    expect(pinSource).not.toContain('db.session.setPinnedToBoard(sessionId, false).catch(() => {})')
    expect(source).not.toContain('db.session.setPinnedToBoard')
  })

  it('routes default session creation through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const createSessionStart = source.indexOf('createSession: async (')
    const closeSessionStart = source.indexOf(
      'closeSession: async (sessionId: string)',
      createSessionStart
    )
    const createSessionSource = source.slice(createSessionStart, closeSessionStart)

    expect(createSessionStart).toBeGreaterThan(-1)
    expect(closeSessionStart).toBeGreaterThan(createSessionStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(createSessionSource).toContain('const session = await dbApi.session.create<Session>({')
    expect(createSessionSource).not.toContain('const session = await db.session.create({')
  })

  it('routes board assistant session creation through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const createBoardAssistantStart = source.indexOf(
      'createBoardAssistantSession: async (projectId: string)'
    )
    const closeBoardAssistantStart = source.indexOf(
      'closeBoardAssistantSession: async (projectId: string)',
      createBoardAssistantStart
    )
    const createBoardAssistantSource = source.slice(
      createBoardAssistantStart,
      closeBoardAssistantStart
    )

    expect(createBoardAssistantStart).toBeGreaterThan(-1)
    expect(closeBoardAssistantStart).toBeGreaterThan(createBoardAssistantStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(createBoardAssistantSource).toContain(
      'const session = await dbApi.session.create<Session>({'
    )
    expect(createBoardAssistantSource).not.toContain('const session = await db.session.create({')
  })

  it('routes board assistant session loading through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const loadBoardAssistantStart = source.indexOf(
      'loadBoardAssistantSession: async (projectId: string)'
    )
    const createBoardAssistantStart = source.indexOf(
      'createBoardAssistantSession: async (projectId: string)',
      loadBoardAssistantStart
    )
    const loadBoardAssistantSource = source.slice(
      loadBoardAssistantStart,
      createBoardAssistantStart
    )

    expect(loadBoardAssistantStart).toBeGreaterThan(-1)
    expect(createBoardAssistantStart).toBeGreaterThan(loadBoardAssistantStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(loadBoardAssistantSource).toContain(
      'const session = await dbApi.session.getActiveBoardAssistant<Session>(projectId)'
    )
    expect(loadBoardAssistantSource).not.toContain(
      'const session = await db.session.getActiveBoardAssistant(projectId)'
    )
  })

  it('routes connection session creation through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const createConnectionStart = source.indexOf('createConnectionSession: async (')
    const closeOtherConnectionStart = source.indexOf(
      'closeOtherConnectionSessions: async (connectionId: string',
      createConnectionStart
    )
    const createConnectionSource = source.slice(createConnectionStart, closeOtherConnectionStart)

    expect(createConnectionStart).toBeGreaterThan(-1)
    expect(closeOtherConnectionStart).toBeGreaterThan(createConnectionStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(createConnectionSource).toContain('const result = await connectionApi.get(connectionId)')
    expect(createConnectionSource).toContain(
      'const session = await dbApi.session.create<Session>({'
    )
    expect(createConnectionSource).not.toContain(
      'const result = unwrapEnvelope(await window.connectionOps.get(connectionId))'
    )
    expect(createConnectionSource).not.toContain('const session = await db.session.create({')
    expect(source).not.toContain('const session = await db.session.create({')
  })

  it('removes the old connection:get IPC adapter after connection get RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const connectionHandlersSource = readOptionalSource('../../src/main/ipc/connection-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('connection:get'")
    expect(connectionHandlersSource).not.toContain("defineHandler('connection:get'")
    expect(connectionHandlersSource).not.toContain("connectionFailed('connection:get'")
  })

  it('routes connection store loading through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useConnectionStore.ts'),
      'utf-8'
    )
    const loadStart = source.indexOf('loadConnections: async () => {')
    const createStart = source.indexOf('createConnection: async (worktreeIds: string[])', loadStart)
    const loadSource = source.slice(loadStart, createStart)

    expect(loadStart).toBeGreaterThan(-1)
    expect(createStart).toBeGreaterThan(loadStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(loadSource).toContain('const result = await connectionApi.getAll()')
    expect(loadSource).toContain(
      'set({ connections: result.connections || [], isLoading: false, loaded: true })'
    )
    expect(loadSource).not.toContain('window.connectionOps.getAll')
  })

  it('removes the old connection:getAll IPC adapter after connection get-all RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const connectionHandlersSource = readOptionalSource('../../src/main/ipc/connection-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('connection:getAll'")
    expect(connectionHandlersSource).not.toContain("defineHandler('connection:getAll'")
    expect(connectionHandlersSource).not.toContain("connectionFailed('connection:getAll'")
  })

  it('routes connection store creation through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useConnectionStore.ts'),
      'utf-8'
    )
    const createStart = source.indexOf('createConnection: async (worktreeIds: string[])')
    const deleteStart = source.indexOf(
      'deleteConnection: async (connectionId: string)',
      createStart
    )
    const createSource = source.slice(createStart, deleteStart)

    expect(createStart).toBeGreaterThan(-1)
    expect(deleteStart).toBeGreaterThan(createStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(createSource).toContain('const result = await connectionApi.create(worktreeIds)')
    expect(createSource).toContain('connections: [...state.connections, connection]')
    expect(createSource).not.toContain('window.connectionOps.create')
  })

  it('removes the old connection:create IPC adapter after connection create RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const connectionHandlersSource = readOptionalSource('../../src/main/ipc/connection-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('connection:create'")
    expect(connectionHandlersSource).not.toContain("defineHandler('connection:create'")
    expect(connectionHandlersSource).not.toContain("connectionFailed('connection:create'")
  })

  it('routes connection store deletion through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useConnectionStore.ts'),
      'utf-8'
    )
    const deleteStart = source.indexOf('deleteConnection: async (connectionId: string)')
    const addMemberStart = source.indexOf(
      'addMember: async (connectionId: string, worktreeId: string)',
      deleteStart
    )
    const deleteSource = source.slice(deleteStart, addMemberStart)

    expect(deleteStart).toBeGreaterThan(-1)
    expect(addMemberStart).toBeGreaterThan(deleteStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(deleteSource).toContain('const result = await connectionApi.delete(connectionId)')
    expect(deleteSource).toContain(
      'const connections = state.connections.filter((c) => c.id !== connectionId)'
    )
    expect(deleteSource).not.toContain('window.connectionOps.delete')
  })

  it('removes the old connection:delete IPC adapter after connection delete RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const connectionHandlersSource = readOptionalSource('../../src/main/ipc/connection-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('connection:delete'")
    expect(connectionHandlersSource).not.toContain("defineHandler('connection:delete'")
    expect(connectionHandlersSource).not.toContain("connectionFailed('connection:delete'")
  })

  it('routes connection store add-member mutation and reload through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useConnectionStore.ts'),
      'utf-8'
    )
    const addMemberStart = source.indexOf(
      'addMember: async (connectionId: string, worktreeId: string)'
    )
    const removeMemberStart = source.indexOf(
      'removeMember: async (connectionId: string, worktreeId: string)',
      addMemberStart
    )
    const addMemberSource = source.slice(addMemberStart, removeMemberStart)

    expect(addMemberStart).toBeGreaterThan(-1)
    expect(removeMemberStart).toBeGreaterThan(addMemberStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(addMemberSource).toContain(
      'const addResult = await connectionApi.addMember(connectionId, worktreeId)'
    )
    expect(addMemberSource).toContain('const result = await connectionApi.get(connectionId)')
    expect(addMemberSource).not.toContain('window.connectionOps.addMember')
    expect(addMemberSource).not.toContain('window.connectionOps.get')
  })

  it('removes the old connection:addMember IPC adapter after connection add-member RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const connectionHandlersSource = readOptionalSource('../../src/main/ipc/connection-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('connection:addMember'")
    expect(connectionHandlersSource).not.toContain("defineHandler('connection:addMember'")
    expect(connectionHandlersSource).not.toContain("connectionFailed('connection:addMember'")
  })

  it('routes connection store remove-member mutation through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useConnectionStore.ts'),
      'utf-8'
    )
    const removeMemberStart = source.indexOf(
      'removeMember: async (connectionId: string, worktreeId: string)'
    )
    const updateMembersStart = source.indexOf(
      'updateConnectionMembers: async (connectionId: string, desiredWorktreeIds: string[])',
      removeMemberStart
    )
    const removeMemberSource = source.slice(removeMemberStart, updateMembersStart)

    expect(removeMemberStart).toBeGreaterThan(-1)
    expect(updateMembersStart).toBeGreaterThan(removeMemberStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(removeMemberSource).toContain(
      'const result = await connectionApi.removeMember(connectionId, worktreeId)'
    )
    expect(removeMemberSource).toContain('const getResult = await connectionApi.get(connectionId)')
    expect(removeMemberSource).not.toContain('window.connectionOps.removeMember')
    expect(removeMemberSource).not.toContain('window.connectionOps.get')
  })

  it('removes the old connection:removeMember IPC adapter after connection remove-member RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const connectionHandlersSource = readOptionalSource('../../src/main/ipc/connection-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('connection:removeMember'")
    expect(connectionHandlersSource).not.toContain("defineHandler('connection:removeMember'")
    expect(connectionHandlersSource).not.toContain("connectionFailed('connection:removeMember'")
  })

  it('routes connection store member-update through the batched connectionApi.updateMembers RPC', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useConnectionStore.ts'),
      'utf-8'
    )
    const updateMembersStart = source.indexOf(
      'updateConnectionMembers: async (connectionId: string, desiredWorktreeIds: string[])'
    )
    const renameStart = source.indexOf(
      'renameConnection: async (connectionId: string, customName: string | null)',
      updateMembersStart
    )
    const updateMembersSource = source.slice(updateMembersStart, renameStart)

    expect(updateMembersStart).toBeGreaterThan(-1)
    expect(renameStart).toBeGreaterThan(updateMembersStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(updateMembersSource).toContain(
      'connectionApi.updateMembers(connectionId, desiredWorktreeIds)'
    )
  })

  it('no longer routes member-update additions through the per-member addMember RPC', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useConnectionStore.ts'),
      'utf-8'
    )
    const updateMembersStart = source.indexOf(
      'updateConnectionMembers: async (connectionId: string, desiredWorktreeIds: string[])'
    )
    const renameStart = source.indexOf(
      'renameConnection: async (connectionId: string, customName: string | null)',
      updateMembersStart
    )
    const updateMembersSource = source.slice(updateMembersStart, renameStart)

    expect(updateMembersStart).toBeGreaterThan(-1)
    expect(renameStart).toBeGreaterThan(updateMembersStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(updateMembersSource).not.toContain('connectionApi.addMember(')
    expect(updateMembersSource).not.toContain('window.connectionOps.addMember')
  })

  it('no longer routes member-update removals through the per-member removeMember RPC', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useConnectionStore.ts'),
      'utf-8'
    )
    const updateMembersStart = source.indexOf(
      'updateConnectionMembers: async (connectionId: string, desiredWorktreeIds: string[])'
    )
    const renameStart = source.indexOf(
      'renameConnection: async (connectionId: string, customName: string | null)',
      updateMembersStart
    )
    const updateMembersSource = source.slice(updateMembersStart, renameStart)

    expect(updateMembersStart).toBeGreaterThan(-1)
    expect(renameStart).toBeGreaterThan(updateMembersStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(updateMembersSource).not.toContain('connectionApi.removeMember(')
    expect(updateMembersSource).not.toContain('window.connectionOps.removeMember')
  })

  it('routes connection store rename through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useConnectionStore.ts'),
      'utf-8'
    )
    const renameStart = source.indexOf(
      'renameConnection: async (connectionId: string, customName: string | null)'
    )
    const enterConnectionModeStart = source.indexOf(
      'enterConnectionMode: (sourceWorktreeId: string)',
      renameStart
    )
    const renameSource = source.slice(renameStart, enterConnectionModeStart)

    expect(renameStart).toBeGreaterThan(-1)
    expect(enterConnectionModeStart).toBeGreaterThan(renameStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(renameSource).toContain(
      'const result = await connectionApi.rename(connectionId, customName)'
    )
    expect(renameSource).not.toContain('window.connectionOps.rename')
  })

  it('removes the old connection:rename IPC adapter after connection rename RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const connectionHandlersSource = readOptionalSource('../../src/main/ipc/connection-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('connection:rename'")
    expect(connectionHandlersSource).not.toContain("defineHandler('connection:rename'")
    expect(connectionHandlersSource).not.toContain("connectionFailed('connection:rename'")
  })

  it('routes connection session loading through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const loadBackgroundStart = source.indexOf(
      'loadConnectionSessionsBackground: async (connectionId: string)'
    )
    const createConnectionStart = source.indexOf(
      'createConnectionSession: async (',
      loadBackgroundStart
    )
    const connectionLoadSource = source.slice(loadBackgroundStart, createConnectionStart)
    const explicitApiCalls =
      connectionLoadSource.match(
        /const sessions = await dbApi\.session\.getActiveByConnection<Session>\(connectionId\)/g
      ) ?? []

    expect(loadBackgroundStart).toBeGreaterThan(-1)
    expect(createConnectionStart).toBeGreaterThan(loadBackgroundStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(explicitApiCalls).toHaveLength(2)
    expect(connectionLoadSource).not.toContain(
      'const sessions = await db.session.getActiveByConnection(connectionId)'
    )
  })

  it('routes close-session completion updates through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const closeSessionStart = source.indexOf('closeSession: async (sessionId: string)')
    const reopenSessionStart = source.indexOf(
      'reopenSession: async (sessionId: string',
      closeSessionStart
    )
    const closeSessionSource = source.slice(closeSessionStart, reopenSessionStart)

    expect(closeSessionStart).toBeGreaterThan(-1)
    expect(reopenSessionStart).toBeGreaterThan(closeSessionStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(closeSessionSource).toContain('await dbApi.session.update<Session>(sessionId, {')
    expect(closeSessionSource).toContain('unwrapEnvelope(await terminalApi.destroy(sessionId))')
    expect(closeSessionSource).toContain(
      'unwrapEnvelope(await opencodeApi.disconnect(worktreePath, opencodeSessionId))'
    )
    expect(closeSessionSource).not.toContain('await db.session.update(sessionId, {')
    expect(closeSessionSource).not.toContain(
      'unwrapEnvelope(await window.terminalOps.destroy(sessionId))'
    )
    expect(closeSessionSource).not.toContain(
      'unwrapEnvelope(await window.opencodeOps.disconnect(worktreePath, opencodeSessionId))'
    )
    expect(source).not.toContain('await db.session.update(')
  })

  it('keeps tab-context store tests on dbApi mocks instead of db globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-19/session-7/tab-context-store.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain('dbApi: dbApiMocks')
    expect(source).toContain('dbApiMocks.session.getActiveByWorktree.mockResolvedValue')
    expect(source).toContain('dbApiMocks.session.create.mockResolvedValue')
    expect(source).toContain('dbApiMocks.session.update.mockResolvedValue')
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain('window.db')
  })

  it('keeps tab-context UI tests on dbApi mocks instead of db globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-19/session-8/tab-context-ui.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain('dbApi: dbApiMocks')
    expect(source).toContain('dbApiMocks.session.getActiveByWorktree.mockResolvedValue')
    expect(source).toContain('dbApiMocks.session.create.mockResolvedValue')
    expect(source).toContain('dbApiMocks.session.update.mockResolvedValue')
    expect(source).toContain('dbApiMocks.worktree.updateModel.mockResolvedValue')
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain('window.db')
  })

  it('keeps clear-command tests on dbApi and opencodeApi mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-18/session-2/clear-command.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain('dbApi: dbApiMocks')
    expect(source).toContain("vi.mock('@/api/opencode-api'")
    expect(source).toContain('opencodeApi: opencodeApiMocks')
    expect(source).toContain('dbApiMocks.session.create')
    expect(source).toContain('dbApiMocks.session.update')
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.opencodeOps')
  })

  it('keeps question-store type checks on opencodeApi instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-10/session-1/question-store-ipc.test.ts'),
      'utf-8'
    )

    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(source).toContain('typeof opencodeApi.questionReply')
    expect(source).toContain('typeof opencodeApi.questionReject')
    expect(source).not.toContain('typeof window.opencodeOps')
    expect(source).not.toContain('window.opencodeOps')
  })

  it('keeps closed-terminal tests on API mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/terminal/closed-terminal-signal.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/opencode-api'")
    expect(source).toContain("vi.mock('@/api/terminal-api'")
    expect(source).toContain('dbApi: dbApiMocks')
    expect(source).toContain('opencodeApi: opencodeApiMocks')
    expect(source).toContain('terminalApi: terminalApiMocks')
    expect(source).toContain('expect(opencodeApiMocks.setModel).not.toHaveBeenCalled()')
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'terminalOps'")
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.opencodeOps')
    expect(source).not.toContain('window.terminalOps')
  })

  it('keeps Ghostty visibility tests on terminalApi mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/terminal/ghostty-backend-visibility.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/terminal-api'")
    expect(source).toContain('terminalApi: terminalApiMocks')
    expect(source).toContain('terminalApiMocks.ghosttyCreateSurface')
    expect(source).toContain('terminalApiMocks.ghosttySetFrame')
    expect(source).toContain('terminalApiMocks.ghosttySetFocus')
    expect(source).not.toContain('mockTerminalOps')
    expect(source).not.toContain("Object.defineProperty(window, 'terminalOps'")
    expect(source).not.toContain('window.terminalOps')
  })

  it('keeps rate-limit listener tests on API mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/usage/rate-limit-event-listener.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/opencode-api'")
    expect(source).toContain("vi.mock('@/api/settings-api'")
    expect(source).toContain("vi.mock('@/api/worktree-api'")
    expect(source).toContain('apiMocks.opencodeApi.onStream.mockImplementation')
    expect(source).toContain('apiMocks.dbApi.setting.get.mockResolvedValue(null)')
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.opencodeOps')
  })

  it('keeps unavailable-provider session-store tests on API mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../test/phase-22/session-2/session-store-unavailable-provider.test.ts'
      ),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/settings-api'")
    expect(source).toContain("vi.mock('@/api/system-api'")
    expect(source).toContain('dbApi: apiMocks.dbApi')
    expect(source).toContain('systemApi: apiMocks.systemApi')
    expect(source).toContain('apiMocks.dbApi.session.create')
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'systemOps'")
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.systemOps')
  })

  it('keeps model-selector provider pill tests on opencodeApi mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../test/phase-22/session-11/model-selector-provider-pill.test.tsx'
      ),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/opencode-api'")
    expect(source).toContain('opencodeApi: apiMocks.opencodeApi')
    expect(source).toContain('apiMocks.opencodeApi.listModels.mockImplementation')
    expect(source).toContain('expect(apiMocks.opencodeApi.listModels).toHaveBeenCalled()')
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain('window.opencodeOps')
  })

  it('keeps remove-haiku naming tests on API mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-11/session-1/remove-haiku-naming.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/settings-api'")
    expect(source).toContain("vi.mock('@/api/system-api'")
    expect(source).toContain('dbApi: apiMocks.dbApi')
    expect(source).toContain('apiMocks.dbApi.session.create')
    expect(source).toContain('apiMocks.systemApi.detectAgentSdks')
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.opencodeOps')
  })

  it('keeps integration-verification finder tests on projectApi mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-10/session-8/integration-verification.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/project-api'")
    expect(source).toContain('projectApi: apiMocks.projectApi')
    expect(source).toContain('apiMocks.projectApi.showInFolder')
    expect(source).toContain('projectApi.showInFolder')
    expect(source).not.toContain("Object.defineProperty(window, 'projectOps'")
    expect(source).not.toContain('window.projectOps')
  })

  it('keeps show-in-finder tests on projectApi source assertions instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-10/session-6/show-in-finder.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/settings-api'")
    expect(source).toContain('projectApi.showInFolder')
    expect(source).not.toContain('window.projectOps')
    expect(source).not.toContain('window.worktreeOps')
  })

  it('keeps handoff model picker tests on opencodeApi mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-22/session-11/handoff-model-picker.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/opencode-api'")
    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/system-api'")
    expect(source).toContain('opencodeApi: apiMocks.opencodeApi')
    expect(source).toContain('apiMocks.opencodeApi.listModels')
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'systemOps'")
    expect(source).not.toContain('window.opencodeOps')
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.systemOps')
  })

  it('keeps codex steer boundary assertions on opencodeApi mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-22/session-10/codex-steer-boundary.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/opencode-api'")
    expect(source).toContain('opencodeApi: apiMocks.opencodeApi')
    expect(source).toContain('apiMocks.opencodeApi.steer.mockResolvedValue')
    expect(source).toContain('expect(apiMocks.opencodeApi.steer).toHaveBeenCalledWith')
    expect(source).not.toContain('window.opencodeOps.steer')
  })

  it('routes worktree session reopen updates through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const reopenSessionStart = source.indexOf(
      'reopenSession: async (sessionId: string, worktreeId: string)'
    )
    const reopenConnectionSessionStart = source.indexOf(
      'reopenConnectionSession: async (sessionId: string',
      reopenSessionStart
    )
    const reopenSessionSource = source.slice(reopenSessionStart, reopenConnectionSessionStart)

    expect(reopenSessionStart).toBeGreaterThan(-1)
    expect(reopenConnectionSessionStart).toBeGreaterThan(reopenSessionStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(reopenSessionSource).toContain(
      'const updatedSession = await dbApi.session.update<Session>(sessionId, {'
    )
    expect(reopenSessionSource).not.toContain(
      'const updatedSession = await db.session.update(sessionId, {'
    )
    expect(source).not.toContain('await db.session.update(')
  })

  it('routes connection session reopen updates through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const reopenConnectionSessionStart = source.indexOf(
      'reopenConnectionSession: async (sessionId: string, connectionId: string)'
    )
    const setActiveSessionStart = source.indexOf(
      'setActiveSession: (sessionId: string | null)',
      reopenConnectionSessionStart
    )
    const reopenConnectionSessionSource = source.slice(
      reopenConnectionSessionStart,
      setActiveSessionStart
    )

    expect(reopenConnectionSessionStart).toBeGreaterThan(-1)
    expect(setActiveSessionStart).toBeGreaterThan(reopenConnectionSessionStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(reopenConnectionSessionSource).toContain(
      'const updatedSession = await dbApi.session.update<Session>(sessionId, {'
    )
    expect(reopenConnectionSessionSource).not.toContain(
      'const updatedSession = await db.session.update(sessionId, {'
    )
    expect(source).not.toContain('await db.session.update(')
  })

  it('routes session rename updates through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const updateSessionNameStart = source.indexOf(
      'updateSessionName: async (sessionId: string, name: string)'
    )
    const reorderTabsStart = source.indexOf(
      'reorderTabs: (worktreeId: string',
      updateSessionNameStart
    )
    const updateSessionNameSource = source.slice(updateSessionNameStart, reorderTabsStart)

    expect(updateSessionNameStart).toBeGreaterThan(-1)
    expect(reorderTabsStart).toBeGreaterThan(updateSessionNameStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(updateSessionNameSource).toContain(
      'const updatedSession = await dbApi.session.update<Session>(sessionId, { name })'
    )
    expect(updateSessionNameSource).not.toContain(
      'const updatedSession = await db.session.update(sessionId, { name })'
    )
    expect(source).not.toContain('await db.session.update(')
  })

  it('routes session mode toggle updates through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const toggleSessionModeStart = source.indexOf('toggleSessionMode: async (sessionId: string)')
    const toggleSuperModeStart = source.indexOf(
      'toggleSuperMode: async (sessionId: string)',
      toggleSessionModeStart
    )
    const toggleSessionModeSource = source.slice(toggleSessionModeStart, toggleSuperModeStart)

    expect(toggleSessionModeStart).toBeGreaterThan(-1)
    expect(toggleSuperModeStart).toBeGreaterThan(toggleSessionModeStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(toggleSessionModeSource).toContain(
      'await dbApi.session.update<Session>(sessionId, { mode: newMode })'
    )
    expect(toggleSessionModeSource).not.toContain(
      'await db.session.update(sessionId, { mode: newMode })'
    )
    expect(source).not.toContain('await db.session.update(')
  })

  it('routes Claude CLI permission-mode sync through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const syncStart = source.indexOf('function syncClaudeCliPermissionModeIfNeeded(')
    const storeStart = source.indexOf('export const useSessionStore', syncStart)
    const syncSource = source.slice(syncStart, storeStart)

    expect(syncStart).toBeGreaterThan(-1)
    expect(storeStart).toBeGreaterThan(syncStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(syncSource).toContain("terminalApi.write(sessionId, '\\x1b[Z')")
    expect(syncSource).not.toContain("window.terminalOps.write(sessionId, '\\x1b[Z')")
  })

  it('routes Ghostty promotion availability checks through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useGhosttyPromotion.ts'),
      'utf-8'
    )
    const checkStart = source.indexOf('async function checkAndPromote(): Promise<void> {')
    const cleanupStart = source.indexOf('\n    checkAndPromote()', checkStart)
    const checkSource = source.slice(checkStart, cleanupStart)

    expect(checkStart).toBeGreaterThan(-1)
    expect(cleanupStart).toBeGreaterThan(checkStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
    expect(checkSource).toContain('const result = await terminalApi.ghosttyIsAvailable()')
    expect(checkSource).toContain('if (cancelled || !result.available) return')
    expect(checkSource).toContain('shownRef.current = true')
    expect(checkSource).toContain('toast.success')
    expect(checkSource).toContain('toast.info')
    expect(checkSource).not.toContain('window.terminalOps.ghosttyIsAvailable')
    expect(checkSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes SettingsTerminal Ghostty availability checks through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/settings/SettingsTerminal.tsx'),
      'utf-8'
    )
    const checkStart = source.indexOf('async function checkGhostty(): Promise<void> {')
    const cleanupStart = source.indexOf('\n    checkGhostty()', checkStart)
    const checkSource = source.slice(checkStart, cleanupStart)

    expect(checkStart).toBeGreaterThan(-1)
    expect(cleanupStart).toBeGreaterThan(checkStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(checkSource).toContain('const result = await terminalApi.ghosttyIsAvailable()')
    expect(checkSource).toContain('setGhosttyAvailable(result.available)')
    expect(checkSource).toContain("setIsMac(result.platform === 'darwin')")
    expect(checkSource).toContain('setGhosttyAvailable(false)')
    expect(checkSource).toContain('setIsMac(false)')
    expect(checkSource).not.toContain('window.terminalOps.ghosttyIsAvailable')
    expect(checkSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes SettingsTerminal terminal detection through settingsApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/settings/SettingsTerminal.tsx'),
      'utf-8'
    )
    const detectStart = source.indexOf('async function detect(): Promise<void> {')
    const cleanupStart = source.indexOf('\n    detect()', detectStart)
    const detectSource = source.slice(detectStart, cleanupStart)

    expect(detectStart).toBeGreaterThan(-1)
    expect(cleanupStart).toBeGreaterThan(detectStart)
    expect(source).toContain("import { settingsApi } from '@/api/settings-api'")
    expect(detectSource).toContain('const terminals = await settingsApi.detectTerminals()')
    expect(detectSource).toContain('if (!cancelled) {')
    expect(detectSource).toContain('setDetectedTerminals(terminals)')
    expect(detectSource).toContain('if (!cancelled) setIsDetecting(false)')
    expect(detectSource).not.toContain('window.settingsOps')
    expect(detectSource).not.toContain('unwrapEnvelope(')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('removes the old settings:detectTerminals IPC adapter after settings detect terminals RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const settingsHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/settings-openers.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('settings:detectTerminals'")
    expect(settingsHandlersSource).not.toContain("defineHandler('settings:detectTerminals'")
  })

  it('routes SettingsEditor editor detection through settingsApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/settings/SettingsEditor.tsx'),
      'utf-8'
    )
    const detectStart = source.indexOf('async function detect(): Promise<void> {')
    const cleanupStart = source.indexOf('\n    detect()', detectStart)
    const detectSource = source.slice(detectStart, cleanupStart)

    expect(detectStart).toBeGreaterThan(-1)
    expect(cleanupStart).toBeGreaterThan(detectStart)
    expect(source).toContain("import { settingsApi } from '@/api/settings-api'")
    expect(detectSource).toContain('const editors = await settingsApi.detectEditors()')
    expect(detectSource).toContain('if (!cancelled) {')
    expect(detectSource).toContain('setDetectedEditors(editors)')
    expect(detectSource).toContain('if (!cancelled) setIsDetecting(false)')
    expect(detectSource).not.toContain('window.settingsOps')
    expect(detectSource).not.toContain('unwrapEnvelope(')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('removes the old settings:detectEditors IPC adapter after settings detect editors RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const settingsHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/settings-openers.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('settings:detectEditors'")
    expect(settingsHandlersSource).not.toContain("defineHandler('settings:detectEditors'")
  })

  it('removes the old settings:openWithEditor IPC adapter after settings editor open RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const settingsHandlersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/settings-openers.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('settings:openWithEditor'")
    expect(settingsHandlersSource).not.toContain("defineHandler('settings:openWithEditor'")
  })

  it('routes GhosttyBackend availability checks through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/GhosttyBackend.ts'
      ),
      'utf-8'
    )
    const helperStart = source.indexOf('export async function isGhosttyAvailable()')
    const helperSource = source.slice(helperStart)

    expect(helperStart).toBeGreaterThan(-1)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(helperSource).toContain('const result = await terminalApi.ghosttyIsAvailable()')
    expect(helperSource).toContain('return result.available')
    expect(helperSource).toContain('return false')
    expect(helperSource).not.toContain('window.terminalOps.ghosttyIsAvailable')
    expect(helperSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes GhosttyBackend runtime initialization through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/GhosttyBackend.ts'
      ),
      'utf-8'
    )
    const initStart = source.indexOf('private async ensureRuntimeReady(): Promise<boolean> {')
    const surfaceStart = source.indexOf('private async ensureSurface(): Promise<void> {', initStart)
    const initSource = source.slice(initStart, surfaceStart)

    expect(initStart).toBeGreaterThan(-1)
    expect(surfaceStart).toBeGreaterThan(initStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(initSource).toContain('const initResult = await terminalApi.ghosttyInit()')
    expect(initSource).toContain('if (!initResult.success)')
    expect(initSource).toContain("console.error('Failed to initialize Ghostty:', initResult.error)")
    expect(initSource).toContain('this.runtimeReady = true')
    expect(initSource).toContain('this.runtimeInitPromise = null')
    expect(initSource).not.toContain('window.terminalOps.ghosttyInit')
    expect(initSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes GhosttyBackend surface creation through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/GhosttyBackend.ts'
      ),
      'utf-8'
    )
    const surfaceStart = source.indexOf('private async ensureSurface(): Promise<void> {')
    const createCallStart = source.indexOf(
      'const result = await terminalApi.ghosttyCreateSurface(',
      surfaceStart
    )
    const createFailureStart = source.indexOf('if (!result.success)', createCallStart)
    const createSuccessStart = source.indexOf('this.callbacks?.onStatusChange', createFailureStart)
    const createSource = source.slice(createCallStart, createFailureStart)
    const surfaceSource = source.slice(surfaceStart, createSuccessStart)

    expect(surfaceStart).toBeGreaterThan(-1)
    expect(createCallStart).toBeGreaterThan(surfaceStart)
    expect(createFailureStart).toBeGreaterThan(createCallStart)
    expect(createSuccessStart).toBeGreaterThan(createFailureStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(createSource).toContain(
      'const result = await terminalApi.ghosttyCreateSurface(this.terminalId, createRect, {'
    )
    expect(createSource).toContain('cwd: this.opts.cwd')
    expect(createSource).toContain('shell: this.opts.shell')
    expect(createSource).toContain('scaleFactor: window.devicePixelRatio || 2.0')
    expect(createSource).toContain(
      'useSettingsStore.getState().ghosttyFontSize || GhosttyBackend.FALLBACK_FONT_SIZE'
    )
    expect(surfaceSource).toContain('if (!result.success)')
    expect(surfaceSource).toContain(
      "console.error('Failed to create Ghostty surface:', result.error)"
    )
    expect(surfaceSource).toContain('this.surfaceCreated = true')
    expect(createSource).not.toContain('window.terminalOps.ghosttyCreateSurface')
    expect(createSource).not.toContain('unwrapEnvelope(')
  })

  it('routes GhosttyBackend disposed-after-create surface cleanup through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/GhosttyBackend.ts'
      ),
      'utf-8'
    )
    const cleanupStart = source.indexOf('if (!this.mounted || this.failed) {')
    const cleanupEnd = source.indexOf('this.surfaceCreated = true', cleanupStart)
    const cleanupSource = source.slice(cleanupStart, cleanupEnd)

    expect(cleanupStart).toBeGreaterThan(-1)
    expect(cleanupEnd).toBeGreaterThan(cleanupStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(cleanupSource).toContain('terminalApi.ghosttyDestroySurface(this.terminalId).catch')
    expect(cleanupSource).toContain('// Best-effort cleanup')
    expect(cleanupSource).toContain('return')
    expect(cleanupSource).not.toContain('window.terminalOps.ghosttyDestroySurface')
  })

  it('routes GhosttyBackend dispose surface cleanup through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/GhosttyBackend.ts'
      ),
      'utf-8'
    )
    const disposeStart = source.indexOf('dispose(): void {')
    const resetStart = source.indexOf('this.opts = null', disposeStart)
    const disposeSource = source.slice(disposeStart, resetStart)

    expect(disposeStart).toBeGreaterThan(-1)
    expect(resetStart).toBeGreaterThan(disposeStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(disposeSource).toContain('if (this.surfaceCreated)')
    expect(disposeSource).toContain('terminalApi.ghosttyDestroySurface(this.terminalId).catch')
    expect(disposeSource).toContain('// Best-effort cleanup')
    expect(disposeSource).not.toContain('window.terminalOps.ghosttyDestroySurface')
  })

  it('routes GhosttyBackend post-create focus restore through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/GhosttyBackend.ts'
      ),
      'utf-8'
    )
    const createSuccessStart = source.indexOf('this.surfaceCreated = true')
    const hiddenBranchStart = source.indexOf('} else {', createSuccessStart)
    const createSuccessSource = source.slice(createSuccessStart, hiddenBranchStart)

    expect(createSuccessStart).toBeGreaterThan(-1)
    expect(hiddenBranchStart).toBeGreaterThan(createSuccessStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(createSuccessSource).toContain('this.callbacks?.onStatusChange')
    expect(createSuccessSource).toContain('if (this.visible)')
    expect(createSuccessSource).toContain('this.syncFrame()')
    expect(createSuccessSource).toContain('if (!hasFocusedEditableElement())')
    expect(createSuccessSource).toContain(
      'await terminalApi.ghosttySetFocus(this.terminalId, true)'
    )
    expect(createSuccessSource).not.toContain('window.terminalOps.ghosttySetFocus')
    expect(createSuccessSource).not.toContain('unwrapEnvelope(')
  })

  it('routes GhosttyBackend hide-surface focus release through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/GhosttyBackend.ts'
      ),
      'utf-8'
    )
    const hideStart = source.indexOf('private hideSurface(): void {')
    const hiddenRectStart = source.indexOf('const hiddenRect =', hideStart)
    const hideFocusSource = source.slice(hideStart, hiddenRectStart)

    expect(hideStart).toBeGreaterThan(-1)
    expect(hiddenRectStart).toBeGreaterThan(hideStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(hideFocusSource).toContain('if (!this.surfaceCreated) return')
    expect(hideFocusSource).toContain('cancelAnimationFrame(this.syncFrameTimer)')
    expect(hideFocusSource).toContain('terminalApi.ghosttySetFocus(this.terminalId, false).catch')
    expect(hideFocusSource).toContain('// Ignore focus errors')
    expect(hideFocusSource).not.toContain('window.terminalOps.ghosttySetFocus')
  })

  it('routes GhosttyBackend show-surface focus restore through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/GhosttyBackend.ts'
      ),
      'utf-8'
    )
    const showStart = source.indexOf('private showSurface(): void {')
    const nextMethodStart = source.indexOf('/**', showStart)
    const showSource = source.slice(showStart, nextMethodStart)

    expect(showStart).toBeGreaterThan(-1)
    expect(nextMethodStart).toBeGreaterThan(showStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(showSource).toContain('if (!this.surfaceCreated)')
    expect(showSource).toContain('void this.ensureSurface()')
    expect(showSource).toContain('this.syncFrame()')
    expect(showSource).toContain('if (!hasFocusedEditableElement())')
    expect(showSource).toContain('terminalApi.ghosttySetFocus(this.terminalId, true).catch')
    expect(showSource).toContain('// Ignore focus errors')
    expect(showSource).not.toContain('window.terminalOps.ghosttySetFocus')
  })

  it('routes GhosttyBackend explicit focus through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/GhosttyBackend.ts'
      ),
      'utf-8'
    )
    const focusStart = source.indexOf('focus(): void {')
    const setVisibleStart = source.indexOf('setVisible(visible: boolean): void {', focusStart)
    const focusSource = source.slice(focusStart, setVisibleStart)

    expect(focusStart).toBeGreaterThan(-1)
    expect(setVisibleStart).toBeGreaterThan(focusStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(focusSource).toContain('if (!this.mounted) return')
    expect(focusSource).toContain('if (!this.surfaceCreated)')
    expect(focusSource).toContain('void this.ensureSurface()')
    expect(focusSource).toContain('terminalApi.ghosttySetFocus(this.terminalId, true).catch')
    expect(focusSource).toContain('// Ignore focus errors')
    expect(focusSource).not.toContain('window.terminalOps.ghosttySetFocus')
  })

  it('routes GhosttyBackend hide-surface frame sync through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/GhosttyBackend.ts'
      ),
      'utf-8'
    )
    const hiddenRectStart = source.indexOf('const hiddenRect =')
    const showStart = source.indexOf('private showSurface(): void {', hiddenRectStart)
    const hideFrameSource = source.slice(hiddenRectStart, showStart)

    expect(hiddenRectStart).toBeGreaterThan(-1)
    expect(showStart).toBeGreaterThan(hiddenRectStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(hideFrameSource).toContain('x: GhosttyBackend.HIDDEN_RECT.x')
    expect(hideFrameSource).toContain('w: this.lastVisibleRect.w')
    expect(hideFrameSource).toContain(
      'terminalApi.ghosttySetFrame(this.terminalId, hiddenRect).catch'
    )
    expect(hideFrameSource).toContain('// Ignore frame sync errors during teardown')
    expect(hideFrameSource).not.toContain('window.terminalOps.ghosttySetFrame')
  })

  it('routes GhosttyBackend visible frame sync through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/terminal/backends/GhosttyBackend.ts'
      ),
      'utf-8'
    )
    const syncStart = source.indexOf('private syncFrame(): void {')
    const writeStart = source.indexOf('write(): void {', syncStart)
    const syncSource = source.slice(syncStart, writeStart)

    expect(syncStart).toBeGreaterThan(-1)
    expect(writeStart).toBeGreaterThan(syncStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(syncSource).toContain('if (!this.mounted || this.failed) return')
    expect(syncSource).toContain('if (!this.surfaceCreated)')
    expect(syncSource).toContain('if (!this.visible) return')
    expect(syncSource).toContain('const rect = this.getContainerRect()')
    expect(syncSource).toContain('this.lastVisibleRect = rect')
    expect(syncSource).toContain('terminalApi.ghosttySetFrame(this.terminalId, rect).catch')
    expect(syncSource).toContain('// Ignore frame sync errors during teardown')
    expect(syncSource).not.toContain('window.terminalOps.ghosttySetFrame')
  })

  it('routes terminal store destroy through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useTerminalStore.ts'),
      'utf-8'
    )
    const destroyStart = source.indexOf('destroyTerminal: async (worktreeId: string) => {')
    const setStatusStart = source.indexOf('setTerminalStatus:', destroyStart)
    const destroySource = source.slice(destroyStart, setStatusStart)

    expect(destroyStart).toBeGreaterThan(-1)
    expect(setStatusStart).toBeGreaterThan(destroyStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(destroySource).toContain('unwrapEnvelope(await terminalApi.destroy(worktreeId))')
    expect(destroySource).toContain('// Best-effort destroy')
    expect(destroySource).toContain('terminals.delete(worktreeId)')
    expect(destroySource).not.toContain('window.terminalOps.destroy')
  })

  it('routes TerminalView paste through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/terminal/TerminalView.tsx'),
      'utf-8'
    )
    const pasteStart = source.indexOf('const cleanup = systemApi.onEditPaste((text) => {')
    const pasteEnd = source.indexOf('return cleanup', pasteStart)
    const pasteSource = source.slice(pasteStart, pasteEnd)

    expect(pasteStart).toBeGreaterThan(-1)
    expect(pasteEnd).toBeGreaterThan(pasteStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(pasteSource).toContain("activeBackendTypeRef.current === 'ghostty'")
    expect(pasteSource).toContain(
      'terminalApi.ghosttyPasteText(terminalId, text).then(unwrapEnvelope)'
    )
    expect(pasteSource).toContain("activeBackendTypeRef.current === 'xterm'")
    expect(pasteSource).toContain('terminalApi.write(terminalId, text)')
    expect(pasteSource).not.toContain('window.terminalOps.ghosttyPasteText')
    expect(pasteSource).not.toContain('window.terminalOps.write(terminalId, text)')
  })

  it('routes TerminalView setup config fetch through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/terminal/TerminalView.tsx'),
      'utf-8'
    )
    const configStart = source.indexOf('// Fetch Ghostty config for theming and shell preferences')
    const abortGuardStart = source.indexOf(
      '// CRITICAL: bail out if a newer setupTerminal',
      configStart
    )
    const configSource = source.slice(configStart, abortGuardStart)

    expect(configStart).toBeGreaterThan(-1)
    expect(abortGuardStart).toBeGreaterThan(configStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(configSource).toContain('let config: GhosttyTerminalConfig = {}')
    expect(configSource).toContain('config = unwrapEnvelope(await terminalApi.getConfig())')
    expect(configSource).toContain('// Failed to fetch config, use defaults')
    expect(configSource).not.toContain('window.terminalOps.getConfig')
  })

  it('routes TerminalView restart config fetch through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/terminal/TerminalView.tsx'),
      'utf-8'
    )
    const restartStart = source.indexOf('const handleRestart = useCallback(async () => {')
    const restartEnd = source.indexOf(
      "setupTerminal(effectiveBackendType || 'xterm')",
      restartStart
    )
    const restartSource = source.slice(restartStart, restartEnd)

    expect(restartStart).toBeGreaterThan(-1)
    expect(restartEnd).toBeGreaterThan(restartStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(restartSource).toContain('// Get the current config for shell preference')
    expect(restartSource).toContain('const config = unwrapEnvelope(await terminalApi.getConfig())')
    expect(restartSource).toContain('shell = config.shell')
    expect(restartSource).toContain('// Use default shell')
    expect(restartSource).not.toContain('window.terminalOps.getConfig')
  })

  it('routes super mode toggle updates through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const toggleSuperModeStart = source.indexOf('toggleSuperMode: async (sessionId: string)')
    const toggleSuperPlanShortcutStart = source.indexOf(
      'toggleSuperPlanShortcut: async (sessionId: string)',
      toggleSuperModeStart
    )
    const toggleSuperModeSource = source.slice(toggleSuperModeStart, toggleSuperPlanShortcutStart)

    expect(toggleSuperModeStart).toBeGreaterThan(-1)
    expect(toggleSuperPlanShortcutStart).toBeGreaterThan(toggleSuperModeStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(toggleSuperModeSource).toContain(
      'await dbApi.session.update<Session>(sessionId, { mode: newMode })'
    )
    expect(toggleSuperModeSource).not.toContain(
      'await db.session.update(sessionId, { mode: newMode })'
    )
    expect(source).not.toContain('await db.session.update(')
  })

  it('routes super-plan shortcut updates through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const toggleSuperPlanShortcutStart = source.indexOf(
      'toggleSuperPlanShortcut: async (sessionId: string)'
    )
    const setSessionModeStart = source.indexOf(
      'setSessionMode: async (sessionId: string, mode: SessionMode)',
      toggleSuperPlanShortcutStart
    )
    const toggleSuperPlanShortcutSource = source.slice(
      toggleSuperPlanShortcutStart,
      setSessionModeStart
    )

    expect(toggleSuperPlanShortcutStart).toBeGreaterThan(-1)
    expect(setSessionModeStart).toBeGreaterThan(toggleSuperPlanShortcutStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(toggleSuperPlanShortcutSource).toContain(
      'await dbApi.session.update<Session>(sessionId, { mode: newMode })'
    )
    expect(toggleSuperPlanShortcutSource).not.toContain(
      'await db.session.update(sessionId, { mode: newMode })'
    )
    expect(source).not.toContain('await db.session.update(')
  })

  it('routes explicit session mode updates through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const setSessionModeStart = source.indexOf(
      'setSessionMode: async (sessionId: string, mode: SessionMode)'
    )
    const setSessionModelStart = source.indexOf('setSessionModel: async (', setSessionModeStart)
    const setSessionModeSource = source.slice(setSessionModeStart, setSessionModelStart)

    expect(setSessionModeStart).toBeGreaterThan(-1)
    expect(setSessionModelStart).toBeGreaterThan(setSessionModeStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(setSessionModeSource).toContain(
      'await dbApi.session.update<Session>(sessionId, { mode })'
    )
    expect(setSessionModeSource).not.toContain('await db.session.update(sessionId, { mode })')
    expect(source).not.toContain('await db.session.update(')
  })

  it('routes session model updates through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const setSessionModelStart = source.indexOf('setSessionModel: async (')
    const setOpenCodeSessionIdStart = source.indexOf(
      'setOpenCodeSessionId: (sessionId: string',
      setSessionModelStart
    )
    const setSessionModelSource = source.slice(setSessionModelStart, setOpenCodeSessionIdStart)

    expect(setSessionModelStart).toBeGreaterThan(-1)
    expect(setOpenCodeSessionIdStart).toBeGreaterThan(setSessionModelStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(setSessionModelSource).toContain(
      'await dbApi.session.update<Session>(sessionId, {\n            model_provider_id: model.providerID'
    )
    expect(setSessionModelSource).toContain(
      'unwrapEnvelope(await opencodeApi.setModel({ ...model, agentSdk }))'
    )
    expect(setSessionModelSource).toContain('await dbApi.worktree.updateModel({')
    expect(setSessionModelSource).not.toContain(
      'await db.session.update(sessionId, {\n            model_provider_id: model.providerID'
    )
    expect(setSessionModelSource).not.toContain(
      'unwrapEnvelope(await window.opencodeOps.setModel({ ...model, agentSdk }))'
    )
    expect(setSessionModelSource).not.toContain('await db.worktree.updateModel({')
    expect(source).not.toContain('await db.session.update(')
    expect(source).not.toContain('await db.worktree.updateModel(')
  })

  it('routes board assistant close completion updates through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionStore.ts'),
      'utf-8'
    )
    const closeBoardAssistantStart = source.indexOf(
      'closeBoardAssistantSession: async (projectId: string)'
    )
    const focusBoardAssistantStart = source.indexOf(
      'focusBoardAssistantSession: (projectId: string)',
      closeBoardAssistantStart
    )
    const closeBoardAssistantSource = source.slice(
      closeBoardAssistantStart,
      focusBoardAssistantStart
    )

    expect(closeBoardAssistantStart).toBeGreaterThan(-1)
    expect(focusBoardAssistantStart).toBeGreaterThan(closeBoardAssistantStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(closeBoardAssistantSource).toContain('await dbApi.session.update<Session>(session.id, {')
    expect(closeBoardAssistantSource).toContain(
      'await opencodeApi.abort(\n                    chatSession.snapshot.runtimePath,'
    )
    expect(closeBoardAssistantSource).toContain(
      'await opencodeApi.disconnect(\n                    chatSession.snapshot.runtimePath,'
    )
    expect(closeBoardAssistantSource).not.toContain('await db.session.update(session.id, {')
    expect(closeBoardAssistantSource).not.toContain(
      'await window.opencodeOps.abort(\n                    chatSession.snapshot.runtimePath,'
    )
    expect(closeBoardAssistantSource).not.toContain(
      'await window.opencodeOps.disconnect(\n                    chatSession.snapshot.runtimePath,'
    )
    expect(source).not.toContain('await db.session.update(')
  })

  it('routes board chat store runtime disconnect cleanup through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useBoardChatStore.ts'),
      'utf-8'
    )
    const cleanupRuntimeStart = source.indexOf('async function cleanupRuntime(')
    const buildBoardContextStart = source.indexOf(
      'async function buildBoardContext(',
      cleanupRuntimeStart
    )
    const cleanupRuntimeSource = source.slice(cleanupRuntimeStart, buildBoardContextStart)

    expect(cleanupRuntimeStart).toBeGreaterThan(-1)
    expect(buildBoardContextStart).toBeGreaterThan(cleanupRuntimeStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(cleanupRuntimeSource).toContain(
      'unwrapEnvelope(await opencodeApi.disconnect(runtimePath, opencodeSessionId))'
    )
    expect(cleanupRuntimeSource).toContain('await dbApi.session.delete(sessionId)')
    expect(cleanupRuntimeSource).not.toContain(
      'unwrapEnvelope(await window.opencodeOps.disconnect(runtimePath, opencodeSessionId))'
    )
    expect(cleanupRuntimeSource).not.toContain('await db.session.delete(sessionId)')
  })

  it('keeps board assistant persistence tests on API mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/kanban/board-assistant-persistence.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('../../src/renderer/src/api/db-api'")
    expect(source).toContain("vi.mock('../../src/renderer/src/api/opencode-api'")
    expect(source).toContain('const mockSessionUpdate = vi.mocked(dbApi.session.update)')
    expect(source).toContain('const mockDisconnect = vi.mocked(opencodeApi.disconnect)')
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.opencodeOps')
    expect(source).not.toContain('Object.defineProperty(window')
    expect(source).not.toContain('setRendererRpcClient')
  })

  it('routes board chat project ticket context loading through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useBoardChatStore.ts'),
      'utf-8'
    )
    const projectBranchStart = source.indexOf("  if (scope.kind === 'project') {")
    const connectionBranchStart = source.indexOf(
      "  if (scope.kind === 'connection') {",
      projectBranchStart
    )
    const projectBranchSource = source.slice(projectBranchStart, connectionBranchStart)

    expect(projectBranchStart).toBeGreaterThan(-1)
    expect(connectionBranchStart).toBeGreaterThan(projectBranchStart)
    expect(source).toContain("import { kanbanApi } from '@/api/kanban-api'")
    expect(projectBranchSource).toContain(
      'const tickets = await kanbanApi.ticket.getByProject<KanbanTicket>(scope.projectId, false)'
    )
    expect(projectBranchSource).toContain('Single-project board: ${scope.projectName}')
    expect(projectBranchSource).toContain('tickets.slice(0, 50)')
    expect(projectBranchSource).not.toContain('kanban.ticket.getByProject')
  })

  it('routes board chat connection ticket context loading through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useBoardChatStore.ts'),
      'utf-8'
    )
    const connectionBranchStart = source.indexOf("  if (scope.kind === 'connection') {")
    const pinnedFallbackStart = source.indexOf(
      "  return 'Pinned multi-project boards are not supported.'",
      connectionBranchStart
    )
    const connectionBranchSource = source.slice(connectionBranchStart, pinnedFallbackStart)

    expect(connectionBranchStart).toBeGreaterThan(-1)
    expect(pinnedFallbackStart).toBeGreaterThan(connectionBranchStart)
    expect(source).toContain("import { kanbanApi } from '@/api/kanban-api'")
    expect(connectionBranchSource).toContain(
      'tickets: await kanbanApi.ticket.getByProject<KanbanTicket>(project.id, false)'
    )
    expect(connectionBranchSource).toContain('scope.availableProjects.map(async (project) => ({')
    expect(connectionBranchSource).toContain('Connection board: ${scope.connectionName}')
    expect(connectionBranchSource).toContain('.slice(0, 20)')
    expect(connectionBranchSource).not.toContain('kanban.ticket.getByProject')
  })

  it('routes board chat selected draft creation through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useBoardChatStore.ts'),
      'utf-8'
    )
    const createSelectedStart = source.indexOf('  createSelected: async () => {')
    const toggleDraftStart = source.indexOf(
      '  toggleDraftSelected: (draftId) =>',
      createSelectedStart
    )
    const createSelectedSource = source.slice(createSelectedStart, toggleDraftStart)

    expect(createSelectedStart).toBeGreaterThan(-1)
    expect(toggleDraftStart).toBeGreaterThan(createSelectedStart)
    expect(source).toContain("import { kanbanApi } from '@/api/kanban-api'")
    expect(source).toContain('KanbanTicketBatchCreate')
    expect(source).toContain('KanbanTicketBatchCreateResult')
    expect(source).not.toContain('const kanban = unwrapEnvelopeApi(() => window.kanban)')
    expect(createSelectedSource).toContain('const result = await kanbanApi.ticket.createBatch<')
    expect(createSelectedSource).toContain('KanbanTicketBatchCreateResult')
    expect(createSelectedSource).toContain('KanbanTicketBatchCreate')
    expect(createSelectedSource).toContain('depends_on: draft.dependsOn.filter(')
    expect(createSelectedSource).toContain('await useKanbanStore.getState().loadTickets(projectId)')
    expect(createSelectedSource).toContain(
      'await useKanbanStore.getState().loadDependencies(projectId)'
    )
    expect(createSelectedSource).toContain('get().markDraftsCreated(')
    expect(createSelectedSource).not.toContain('kanban.ticket.createBatch')
  })

  it('routes board chat store runtime connect through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useBoardChatStore.ts'),
      'utf-8'
    )
    const runtimeStart = source.indexOf('async function ensureRuntime()')
    const resetStart = source.indexOf('async function resetAndCleanup(', runtimeStart)
    const runtimeSource = source.slice(runtimeStart, resetStart)

    expect(runtimeStart).toBeGreaterThan(-1)
    expect(resetStart).toBeGreaterThan(runtimeStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(runtimeSource).toContain(
      'const connectResult = unwrapEnvelope(await opencodeApi.connect(runtimePath, session.id))'
    )
    expect(runtimeSource).toContain('!connectResult.success || !connectResult.sessionId')
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(runtimeSource).toContain('const session = await dbApi.session.create<{ id: string }>({')
    expect(runtimeSource).toContain('await dbApi.session.delete(session.id).catch(() => {})')
    expect(runtimeSource).toContain(
      'await dbApi.session.update(session.id, { opencode_session_id: connectResult.sessionId })'
    )
    expect(runtimeSource).toContain('patchActiveSnapshot(state, {')
    expect(source).not.toContain('const db = unwrapEnvelopeApi(() => window.db)')
    expect(runtimeSource).not.toContain('const session = await db.session.create({')
    expect(runtimeSource).not.toContain('await db.session.delete(session.id).catch(() => {})')
    expect(runtimeSource).not.toContain(
      'await db.session.update(session.id, { opencode_session_id: connectResult.sessionId })'
    )
    expect(runtimeSource).not.toContain('window.opencodeOps.connect')
  })

  it('routes board chat store existing runtime reconnect through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useBoardChatStore.ts'),
      'utf-8'
    )
    const runtimeStart = source.indexOf('async function ensureRuntime()')
    const settingsStart = source.indexOf(
      'const settings = useSettingsStore.getState()',
      runtimeStart
    )
    const existingRuntimeSource = source.slice(runtimeStart, settingsStart)

    expect(runtimeStart).toBeGreaterThan(-1)
    expect(settingsStart).toBeGreaterThan(runtimeStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(existingRuntimeSource).toContain(
      'await opencodeApi.reconnect(runtimePath, state.opencodeSessionId, state.sessionId)'
    )
    expect(existingRuntimeSource).toContain('sessionId: state.sessionId')
    expect(existingRuntimeSource).toContain('opencodeSessionId: state.opencodeSessionId')
    expect(existingRuntimeSource).toContain('runtimePath')
    expect(existingRuntimeSource).not.toContain('window.opencodeOps.reconnect')
  })

  it('routes board chat store prompt dispatch through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useBoardChatStore.ts'),
      'utf-8'
    )
    const sendStart = source.indexOf('sendMessage: async (message) => {')
    const buildPromptStart = source.indexOf('const prompt = buildAssistantPrompt(', sendStart)
    const promptStart = source.indexOf('await opencodeApi.prompt(', buildPromptStart)
    const failureStart = source.indexOf('if (!result.success) {', promptStart)
    const promptSource = source.slice(buildPromptStart, failureStart)

    expect(sendStart).toBeGreaterThan(-1)
    expect(buildPromptStart).toBeGreaterThan(sendStart)
    expect(promptStart).toBeGreaterThan(buildPromptStart)
    expect(failureStart).toBeGreaterThan(promptStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(promptSource).toContain('runtime.runtimePath')
    expect(promptSource).toContain('runtime.opencodeSessionId')
    expect(promptSource).toContain('prompt')
    expect(promptSource).toContain('undefined')
    expect(promptSource).toContain('{ codexFastMode: useSettingsStore.getState().codexFastMode }')
    expect(promptSource).not.toContain('window.opencodeOps.prompt')
  })

  it('routes board assistant view runtime disconnect cleanup through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/BoardAssistantView.tsx'),
      'utf-8'
    )
    const cleanupRuntimeStart = source.indexOf('async function cleanupBoardChatRuntime()')
    const boardChatHeaderStart = source.indexOf('function BoardChatHeader(', cleanupRuntimeStart)
    const cleanupRuntimeSource = source.slice(cleanupRuntimeStart, boardChatHeaderStart)

    expect(cleanupRuntimeStart).toBeGreaterThan(-1)
    expect(boardChatHeaderStart).toBeGreaterThan(cleanupRuntimeStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(cleanupRuntimeSource).toContain(
      'unwrapEnvelope(await opencodeApi.disconnect(runtimePath, opencodeSessionId))'
    )
    expect(cleanupRuntimeSource).not.toContain(
      'unwrapEnvelope(await window.opencodeOps.disconnect(runtimePath, opencodeSessionId))'
    )
  })

  it('routes board assistant runtime fallback worktree lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/BoardAssistantView.tsx'),
      'utf-8'
    )
    const runtimeResolverStart = source.indexOf('async function resolveProjectRuntime(')
    const buildPromptStart = source.indexOf('function buildBoardPrompt(', runtimeResolverStart)
    const resolverSource = source.slice(runtimeResolverStart, buildPromptStart)

    expect(runtimeResolverStart).toBeGreaterThan(-1)
    expect(buildPromptStart).toBeGreaterThan(runtimeResolverStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(resolverSource).toContain(
      'const fallbackWorktrees = await dbApi.worktree.getActiveByProject<Worktree>(projectId)'
    )
    expect(resolverSource).toContain(
      'fallbackWorktrees.find((worktree) => worktree.is_default) ?? fallbackWorktrees[0] ?? null'
    )
    expect(resolverSource).not.toContain('db.worktree.getActiveByProject(projectId)')
  })

  it('routes board assistant view runtime abort cleanup through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/BoardAssistantView.tsx'),
      'utf-8'
    )
    const cleanupRuntimeStart = source.indexOf('async function cleanupBoardChatRuntime()')
    const boardChatHeaderStart = source.indexOf('function BoardChatHeader(', cleanupRuntimeStart)
    const cleanupRuntimeSource = source.slice(cleanupRuntimeStart, boardChatHeaderStart)

    expect(cleanupRuntimeStart).toBeGreaterThan(-1)
    expect(boardChatHeaderStart).toBeGreaterThan(cleanupRuntimeStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(cleanupRuntimeSource).toContain(
      'unwrapEnvelope(await opencodeApi.abort(runtimePath, opencodeSessionId))'
    )
    expect(cleanupRuntimeSource).not.toContain(
      'unwrapEnvelope(await window.opencodeOps.abort(runtimePath, opencodeSessionId))'
    )
  })

  it('routes board assistant runtime connect through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/BoardAssistantView.tsx'),
      'utf-8'
    )
    const runtimeStart = source.indexOf('async function ensureRuntimeSession(')
    const cleanupStart = source.indexOf('async function cleanupBoardChatRuntime()', runtimeStart)
    const runtimeSource = source.slice(runtimeStart, cleanupStart)

    expect(runtimeStart).toBeGreaterThan(-1)
    expect(cleanupStart).toBeGreaterThan(runtimeStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(runtimeSource).toContain(
      'const connectResult = unwrapEnvelope(await opencodeApi.connect(runtimePath, session.id))'
    )
    expect(runtimeSource).toContain('!connectResult.success || !connectResult.sessionId')
    expect(runtimeSource).toContain('await dbApi.session.delete(session.id).catch(() => {})')
    expect(runtimeSource).not.toContain('window.opencodeOps.connect')
  })

  it('routes board assistant reused-session model update through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/BoardAssistantView.tsx'),
      'utf-8'
    )
    const runtimeStart = source.indexOf('async function ensureRuntimeSession(')
    const createSessionStart = source.indexOf(
      '} else {',
      source.indexOf('if (existingStoreSession)', runtimeStart)
    )
    const reusedSessionSource = source.slice(runtimeStart, createSessionStart)

    expect(runtimeStart).toBeGreaterThan(-1)
    expect(createSessionStart).toBeGreaterThan(runtimeStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(reusedSessionSource).toContain('await dbApi.session.update(session.id, {')
    expect(reusedSessionSource).toContain('agent_sdk: agentSdk')
    expect(reusedSessionSource).toContain('model_provider_id: selectedModel.providerID')
    expect(reusedSessionSource).toContain('model_id: selectedModel.modelID')
    expect(reusedSessionSource).toContain('model_variant: selectedModel.variant ?? null')
    expect(reusedSessionSource).not.toContain('db.session.update(session.id, {')
  })

  it('routes board assistant runtime session creation through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/BoardAssistantView.tsx'),
      'utf-8'
    )
    const runtimeStart = source.indexOf('async function ensureRuntimeSession(')
    const createSessionStart = source.indexOf(
      '} else {',
      source.indexOf('if (existingStoreSession)', runtimeStart)
    )
    const connectStart = source.indexOf('const connectResult = unwrapEnvelope(', createSessionStart)
    const createSessionSource = source.slice(createSessionStart, connectStart)

    expect(runtimeStart).toBeGreaterThan(-1)
    expect(createSessionStart).toBeGreaterThan(runtimeStart)
    expect(connectStart).toBeGreaterThan(createSessionStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(createSessionSource).toContain('session = await dbApi.session.create<{ id: string }>({')
    expect(createSessionSource).toContain('worktree_id: worktreeId')
    expect(createSessionSource).toContain('connection_id: connectionId')
    expect(createSessionSource).toContain("session_type: 'board-assistant'")
    expect(createSessionSource).toContain('agent_sdk: agentSdk')
    expect(createSessionSource).not.toContain('db.session.create({')
  })

  it('routes board assistant connect-failure session cleanup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/BoardAssistantView.tsx'),
      'utf-8'
    )
    const runtimeStart = source.indexOf('async function ensureRuntimeSession(')
    const connectFailureStart = source.indexOf(
      'if (!connectResult.success || !connectResult.sessionId)',
      runtimeStart
    )
    const loadSessionStart = source.indexOf(
      'await useSessionStore.getState().loadBoardAssistantSession(targetProjectId)',
      connectFailureStart
    )
    const cleanupSource = source.slice(connectFailureStart, loadSessionStart)

    expect(runtimeStart).toBeGreaterThan(-1)
    expect(connectFailureStart).toBeGreaterThan(runtimeStart)
    expect(loadSessionStart).toBeGreaterThan(connectFailureStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(cleanupSource).toContain('if (!isReused) {')
    expect(cleanupSource).toContain('await dbApi.session.delete(session.id).catch(() => {})')
    expect(cleanupSource).not.toContain('db.session.delete(session.id)')
  })

  it('routes board assistant connect-success session id persistence through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/BoardAssistantView.tsx'),
      'utf-8'
    )
    const runtimeStart = source.indexOf('async function ensureRuntimeSession(')
    const connectSuccessStart = source.indexOf(
      'await dbApi.session.update(session.id, {',
      source.indexOf('if (!connectResult.success || !connectResult.sessionId)', runtimeStart)
    )
    const setRuntimeStart = source.indexOf(
      'useBoardChatStore.getState().setRuntimeSession({',
      connectSuccessStart
    )
    const persistenceSource = source.slice(connectSuccessStart, setRuntimeStart)

    expect(runtimeStart).toBeGreaterThan(-1)
    expect(connectSuccessStart).toBeGreaterThan(runtimeStart)
    expect(setRuntimeStart).toBeGreaterThan(connectSuccessStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(persistenceSource).toContain('await dbApi.session.update(session.id, {')
    expect(persistenceSource).toContain('opencode_session_id: connectResult.sessionId')
    expect(persistenceSource).not.toContain('db.session.update(session.id, {')
  })

  it('routes board assistant materialized session id persistence through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/BoardAssistantView.tsx'),
      'utf-8'
    )
    const materializedStart = source.indexOf('const handleMaterializedSessionId = useCallback(')
    const syncScopeStart = source.indexOf(
      'const syncScope = async (): Promise<void> => {',
      materializedStart
    )
    const materializedSource = source.slice(materializedStart, syncScopeStart)

    expect(materializedStart).toBeGreaterThan(-1)
    expect(syncScopeStart).toBeGreaterThan(materializedStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).not.toContain('const db = unwrapEnvelopeApi(() => window.db)')
    expect(materializedSource).toContain('updateOpencodeSessionId(nextOpencodeSessionId)')
    expect(materializedSource).toContain('if (sessionId) {')
    expect(materializedSource).toContain('void dbApi.session')
    expect(materializedSource).toContain('.update(sessionId, {')
    expect(materializedSource).toContain('opencode_session_id: nextOpencodeSessionId')
    expect(materializedSource).toContain('.catch(() => {})')
    expect(materializedSource).not.toContain('db.session')
  })

  it('routes board assistant prompt dispatch through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/BoardAssistantView.tsx'),
      'utf-8'
    )
    const sendStart = source.indexOf('const handleSend = useCallback(async () => {')
    const promptStart = source.indexOf('await opencodeApi.prompt(', sendStart)
    const failureStart = source.indexOf('if (!result.success) {', promptStart)
    const promptSource = source.slice(sendStart, failureStart)

    expect(sendStart).toBeGreaterThan(-1)
    expect(promptStart).toBeGreaterThan(sendStart)
    expect(failureStart).toBeGreaterThan(promptStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(promptSource).toContain(
      'const runtime = await ensureRuntimeSession(scope, targetProjectId)'
    )
    expect(promptSource).toContain('const prompt = buildBoardPrompt(input, scope, targetProjectId)')
    expect(promptSource).toContain(
      'await opencodeApi.prompt(runtime.runtimePath, runtime.opencodeSessionId, prompt)'
    )
    expect(promptSource).not.toContain('window.opencodeOps.prompt')
  })

  it('routes board assistant draft creation through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/BoardAssistantView.tsx'),
      'utf-8'
    )
    const createStart = source.indexOf('  const handleCreateDrafts = useCallback(')
    const clearStart = source.indexOf(
      '  const handleClear = useCallback(async () => {',
      createStart
    )
    const createSource = source.slice(createStart, clearStart)

    expect(createStart).toBeGreaterThan(-1)
    expect(clearStart).toBeGreaterThan(createStart)
    expect(source).toContain("import { kanbanApi } from '@/api/kanban-api'")
    expect(source).toContain('KanbanTicketBatchCreate')
    expect(source).toContain('KanbanTicketBatchCreateResult')
    expect(source).not.toContain('const kanban = unwrapEnvelopeApi(() => window.kanban)')
    expect(createSource).toContain('const result = await kanbanApi.ticket.createBatch<')
    expect(createSource).toContain('KanbanTicketBatchCreateResult')
    expect(createSource).toContain('KanbanTicketBatchCreate')
    expect(createSource).toContain('depends_on: draft.dependsOn.filter(')
    expect(createSource).toContain('await useKanbanStore.getState().loadTickets(')
    expect(createSource).toContain('await useKanbanStore.getState().loadDependencies(')
    expect(createSource).toContain('markDraftsCreated(draftsToCreate.map((draft) => draft.id))')
    expect(createSource).not.toContain('kanban.ticket.createBatch')
  })

  it('removes the old kanban:ticket:createBatch IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("'kanban:ticket:createBatch'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:createBatch'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:createBatch'")
    expect(kanbanHandlersSource).not.toContain('kanbanTicketBatchCreateSchema')
  })

  it('removes the old kanban:ticket:get IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("'kanban:ticket:get'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:get'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:get'")
  })

  it('routes Hive JSON ticket import through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/HiveImportModal.tsx'),
      'utf-8'
    )
    const importStart = source.indexOf('  const handleImport = async () => {')
    const columnLabelStart = source.indexOf(
      '  const columnLabel = (col?: string) => {',
      importStart
    )
    const importSource = source.slice(importStart, columnLabelStart)

    expect(importStart).toBeGreaterThan(-1)
    expect(columnLabelStart).toBeGreaterThan(importStart)
    expect(source).toContain("import { kanbanApi } from '@/api/kanban-api'")
    expect(source).toContain('interface HiveImportResult')
    expect(source).not.toContain("import { unwrapEnvelopeApi } from '@/lib/ipc-envelope'")
    expect(source).not.toContain('const kanban = unwrapEnvelopeApi(() => window.kanban)')
    expect(importSource).toContain('const result = await kanbanApi.board.importTickets<')
    expect(importSource).toContain('HiveImportResult')
    expect(importSource).toContain('ExportedTicket')
    expect(importSource).toContain('ExportedDependency')
    expect(importSource).toContain('await loadTickets(projectId)')
    expect(importSource).toContain('await useKanbanStore.getState().loadDependencies(projectId)')
    expect(importSource).not.toContain('kanban.board.importTickets')
  })

  it('removes the old kanban:board:importTickets IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("'kanban:board:importTickets'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:board:importTickets'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:board:importTickets'")
    expect(kanbanHandlersSource).not.toContain('getDatabase()')
  })

  it('routes board assistant existing runtime reconnect through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/BoardAssistantView.tsx'),
      'utf-8'
    )
    const syncStart = source.indexOf('const syncScope = async (): Promise<void> => {')
    const systemMessageStart = source.indexOf(
      'if (!existingSnapshot && scope && scope.kind',
      syncStart
    )
    const syncSource = source.slice(syncStart, systemMessageStart)

    expect(syncStart).toBeGreaterThan(-1)
    expect(systemMessageStart).toBeGreaterThan(syncStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(syncSource).toContain(
      'await opencodeApi.reconnect(state.runtimePath, state.opencodeSessionId, state.sessionId)'
    )
    expect(syncSource).toContain('// useSessionStream will handle reconnection failures')
    expect(syncSource).not.toContain('window.opencodeOps.reconnect')
  })

  it('routes board assistant question replies through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/BoardAssistantView.tsx'),
      'utf-8'
    )
    const replyStart = source.indexOf('const handleQuestionReply = useCallback')
    const rejectStart = source.indexOf('const handleQuestionReject = useCallback', replyStart)
    const replySource = source.slice(replyStart, rejectStart)

    expect(replyStart).toBeGreaterThan(-1)
    expect(rejectStart).toBeGreaterThan(replyStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(replySource).toContain('await opencodeApi.questionReply(')
    expect(replySource).toContain('requestId')
    expect(replySource).toContain('answers')
    expect(replySource).toContain('runtimePath || undefined')
    expect(replySource).toContain(
      'useQuestionStore.getState().removeQuestion(sessionId, requestId)'
    )
    expect(replySource).toContain('unwrapEnvelope(')
    expect(replySource).not.toContain('window.opencodeOps.questionReply')
  })

  it('routes board assistant question rejections through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/BoardAssistantView.tsx'),
      'utf-8'
    )
    const rejectStart = source.indexOf('const handleQuestionReject = useCallback')
    const permissionStart = source.indexOf('const handlePermissionReply = useCallback', rejectStart)
    const rejectSource = source.slice(rejectStart, permissionStart)

    expect(rejectStart).toBeGreaterThan(-1)
    expect(permissionStart).toBeGreaterThan(rejectStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(rejectSource).toContain('await opencodeApi.questionReject(')
    expect(rejectSource).toContain('requestId')
    expect(rejectSource).toContain('runtimePath || undefined')
    expect(rejectSource).toContain(
      'useQuestionStore.getState().removeQuestion(sessionId, requestId)'
    )
    expect(rejectSource).toContain('unwrapEnvelope(')
    expect(rejectSource).not.toContain('window.opencodeOps.questionReject')
  })

  it('routes board assistant permission replies through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/BoardAssistantView.tsx'),
      'utf-8'
    )
    const permissionStart = source.indexOf('const handlePermissionReply = useCallback')
    const commandStart = source.indexOf(
      'const handleCommandApprovalReply = useCallback',
      permissionStart
    )
    const permissionSource = source.slice(permissionStart, commandStart)

    expect(permissionStart).toBeGreaterThan(-1)
    expect(commandStart).toBeGreaterThan(permissionStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(permissionSource).toContain('await opencodeApi.permissionReply(')
    expect(permissionSource).toContain('requestId')
    expect(permissionSource).toContain('reply')
    expect(permissionSource).toContain('runtimePath || undefined')
    expect(permissionSource).toContain('message')
    expect(permissionSource).toContain(
      'usePermissionStore.getState().removePermission(sessionId, requestId)'
    )
    expect(permissionSource).toContain('unwrapEnvelope(')
    expect(permissionSource).not.toContain('window.opencodeOps.permissionReply')
  })

  it('routes board assistant command approval replies through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/BoardAssistantView.tsx'),
      'utf-8'
    )
    const commandStart = source.indexOf('const handleCommandApprovalReply = useCallback')
    const renderStart = source.indexOf('if (!scope) return null', commandStart)
    const commandSource = source.slice(commandStart, renderStart)

    expect(commandStart).toBeGreaterThan(-1)
    expect(renderStart).toBeGreaterThan(commandStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(commandSource).toContain('await opencodeApi.commandApprovalReply(')
    expect(commandSource).toContain('requestId')
    expect(commandSource).toContain('approved')
    expect(commandSource).toContain('remember')
    expect(commandSource).toContain('pattern')
    expect(commandSource).toContain('runtimePath || undefined')
    expect(commandSource).toContain('patterns')
    expect(commandSource).toContain(
      'useCommandApprovalStore.getState().removeApproval(sessionId, requestId)'
    )
    expect(commandSource).toContain('unwrapEnvelope(')
    expect(commandSource).not.toContain('window.opencodeOps.commandApprovalReply')
  })

  it('routes session history preview worktree load through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionHistoryStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const worktree = await dbApi.worktree.get(session.worktree_id)')
    expect(source).not.toContain('const worktree = await db.worktree.get(session.worktree_id)')
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
  })

  it('routes session history preview message load through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSessionHistoryStore.ts'),
      'utf-8'
    )
    const previewStart = source.indexOf('getSessionPreviewMessages: async (')
    const exportStart = source.indexOf('// Re-export the SessionWithWorktree type', previewStart)
    const previewSource = source.slice(previewStart, exportStart)

    expect(previewStart).toBeGreaterThan(-1)
    expect(exportStart).toBeGreaterThan(previewStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(previewSource).toContain(
      'await opencodeApi.getMessages(worktree.path, session.opencode_session_id)'
    )
    expect(previewSource).not.toContain(
      'await window.opencodeOps.getMessages(worktree.path, session.opencode_session_id)'
    )
  })

  it('keeps session history tests on API mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/session-9/session-history.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('../../src/renderer/src/api/db-api'")
    expect(source).toContain("vi.mock('../../src/renderer/src/api/opencode-api'")
    expect(source).toContain('const mockDbSession = apiMocks.dbApi.session')
    expect(source).toContain('const mockGetMessages = vi.mocked(opencodeApi.getMessages)')
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.opencodeOps')
    expect(source).not.toContain('Object.defineProperty(window')
  })

  it('routes session stream transcript loads through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useSessionStream.ts'),
      'utf-8'
    )

    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(
      source.match(/opencodeApi\.getMessages\(worktreePath, opencodeSessionId\)/g)
    ).toHaveLength(3)
    expect(source).not.toContain('window.opencodeOps.getMessages(worktreePath, opencodeSessionId)')
  })

  it('routes session stream subscription through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useSessionStream.ts'),
      'utf-8'
    )
    const subscribeStart = source.indexOf('const unsubscribe = opencodeApi.onStream((event) => {')
    const initStart = source.indexOf('// ---- Async initialization: load initial messages ----')
    const subscribeSource = source.slice(subscribeStart, initStart)

    expect(subscribeStart).toBeGreaterThan(-1)
    expect(initStart).toBeGreaterThan(subscribeStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(subscribeSource).toContain('if (event.sessionId !== sessionId) return')
    expect(subscribeSource).toContain("event.type === 'message.part.updated'")
    expect(subscribeSource).toContain("event.type === 'session.materialized'")
    expect(subscribeSource).not.toContain('window.opencodeOps')
  })

  it('routes active session view stream subscription through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const commentStart = source.indexOf(
      '// Subscribe to OpenCode stream events SYNCHRONOUSLY before any async work.'
    )
    const subscribeStart = source.indexOf(
      'const unsubscribe = opencodeApi.onStream((event) => {',
      commentStart
    )
    const initStart = source.indexOf(
      'const initializeSession = async (): Promise<void> => {',
      subscribeStart
    )
    const subscribeSource = source.slice(subscribeStart, initStart)

    expect(commentStart).toBeGreaterThan(-1)
    expect(subscribeStart).toBeGreaterThan(commentStart)
    expect(initStart).toBeGreaterThan(subscribeStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(subscribeSource).toContain('if (event.sessionId !== sessionId)')
    expect(subscribeSource).toContain('streamGenerationRef.current !== currentGeneration')
    expect(subscribeSource).toContain("event.type === 'session.updated'")
    expect(subscribeSource).toContain("event.type === 'session.materialized'")
    expect(subscribeSource).toContain("event.type === 'session.commands_available'")
    expect(subscribeSource).toContain("event.type === 'message.part.updated'")
    expect(subscribeSource).toContain("event.type === 'message.updated'")
    expect(subscribeSource).toContain("event.type === 'session.idle'")
    expect(subscribeSource).toContain("event.type === 'session.status'")
    expect(subscribeSource).not.toContain('window.opencodeOps.onStream')
  })

  it('routes kanban ticket session pre-warm message load through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const readyStart = source.indexOf('[KanbanModal:sessionReady] starting')
    const readyEnd = source.indexOf('const handleHandoff = useCallback', readyStart)
    const readySource = source.slice(readyStart, readyEnd)

    expect(readyStart).toBeGreaterThan(-1)
    expect(readyEnd).toBeGreaterThan(readyStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(readySource).toContain(
      'const warmResult = unwrapEnvelope(await opencodeApi.getMessages(worktreePath, opcSessionId))'
    )
    expect(readySource).not.toContain(
      'await window.opencodeOps.getMessages(worktreePath, opcSessionId)'
    )
  })

  it('routes kanban ticket session pre-warm reconnect through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const readyStart = source.indexOf('[KanbanModal:sessionReady] starting')
    const readyEnd = source.indexOf('const handleHandoff = useCallback', readyStart)
    const readySource = source.slice(readyStart, readyEnd)

    expect(readyStart).toBeGreaterThan(-1)
    expect(readyEnd).toBeGreaterThan(readyStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(readySource).toContain(
      'await opencodeApi.reconnect(worktreePath, opcSessionId, ticket.current_session_id)'
    )
    expect(readySource).toContain(
      'const warmResult = unwrapEnvelope(await opencodeApi.getMessages(worktreePath, opcSessionId))'
    )
    expect(readySource).not.toContain('window.opencodeOps.reconnect')
  })

  it('routes session initial transcript source load through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const loadMessagesStart = source.indexOf('const loadMessages = async (')
    const finalizeStart = source.indexOf('const finalizeResponse = async', loadMessagesStart)
    const loadMessagesSource = source.slice(loadMessagesStart, finalizeStart)

    expect(loadMessagesStart).toBeGreaterThan(-1)
    expect(finalizeStart).toBeGreaterThan(loadMessagesStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(loadMessagesSource).toContain(
      'await opencodeApi.getMessages(sourceWorktreePath, sourceOpencodeSessionId)'
    )
    expect(loadMessagesSource).not.toContain(
      'await window.opencodeOps.getMessages(sourceWorktreePath, sourceOpencodeSessionId)'
    )
    expect(loadMessagesSource).not.toContain('Boolean(window.opencodeOps)')
  })

  it('routes session revert-boundary hydration through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const hydrateStart = source.indexOf('// 1b. Hydrate revert boundary BEFORE loading messages')
    const transcriptStart = source.indexOf('// 2. Hydrate transcript', hydrateStart)
    const hydrateSource = source.slice(hydrateStart, transcriptStart)

    expect(hydrateStart).toBeGreaterThan(-1)
    expect(transcriptStart).toBeGreaterThan(hydrateStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(hydrateSource).toContain('await opencodeApi.sessionInfo(wtPath, existingOpcSessionId)')
    expect(hydrateSource).not.toContain('window.opencodeOps')
  })

  it('routes session retry transcript load through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const retryStart = source.indexOf('const handleRetry = useCallback')
    const questionReplyStart = source.indexOf('// Handle question reply', retryStart)
    const retrySource = source.slice(retryStart, questionReplyStart)

    expect(retryStart).toBeGreaterThan(-1)
    expect(questionReplyStart).toBeGreaterThan(retryStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(retrySource).toContain(
      'await opencodeApi.getMessages(worktree.path, activeOpcSessionId)'
    )
    expect(retrySource).not.toContain(
      'await window.opencodeOps.getMessages(worktree.path, activeOpcSessionId)'
    )
  })

  it('routes session retry reconnect through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const retryStart = source.indexOf('const handleRetry = useCallback')
    const questionReplyStart = source.indexOf('// Handle question reply', retryStart)
    const retrySource = source.slice(retryStart, questionReplyStart)

    expect(retryStart).toBeGreaterThan(-1)
    expect(questionReplyStart).toBeGreaterThan(retryStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(retrySource).toContain(
      'await opencodeApi.reconnect(worktree.path, existingOpcSessionId, sessionId)'
    )
    expect(retrySource).toContain('reconnectResult.revertMessageID')
    expect(retrySource).toContain('activeOpcSessionId = existingOpcSessionId')
    expect(retrySource).toContain('activeOpcSessionId = null')
    expect(retrySource).not.toContain('window.opencodeOps.reconnect')
  })

  it('routes session question replies through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const replyStart = source.indexOf('// Handle question reply')
    const rejectStart = source.indexOf('// Handle question reject', replyStart)
    const replySource = source.slice(replyStart, rejectStart)

    expect(replyStart).toBeGreaterThan(-1)
    expect(rejectStart).toBeGreaterThan(replyStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(replySource).toContain('await opencodeApi.questionReply(')
    expect(replySource).toContain('requestId')
    expect(replySource).toContain('answers')
    expect(replySource).toContain('worktreePath || undefined')
    expect(replySource).toContain('unwrapEnvelope(')
    expect(replySource).not.toContain('window.opencodeOps.questionReply')
  })

  it('routes session question rejections through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const rejectStart = source.indexOf('// Handle question reject/dismiss')
    const permissionStart = source.indexOf('// Handle permission reply', rejectStart)
    const rejectSource = source.slice(rejectStart, permissionStart)

    expect(rejectStart).toBeGreaterThan(-1)
    expect(permissionStart).toBeGreaterThan(rejectStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(rejectSource).toContain('await opencodeApi.questionReject(')
    expect(rejectSource).toContain('requestId')
    expect(rejectSource).toContain('worktreePath || undefined')
    expect(rejectSource).toContain('unwrapEnvelope(')
    expect(rejectSource).not.toContain('window.opencodeOps.questionReject')
  })

  it('routes session permission replies through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const permissionStart = source.indexOf('// Handle permission reply')
    const commandStart = source.indexOf('// Handle command approval reply', permissionStart)
    const permissionSource = source.slice(permissionStart, commandStart)

    expect(permissionStart).toBeGreaterThan(-1)
    expect(commandStart).toBeGreaterThan(permissionStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(permissionSource).toContain('await opencodeApi.permissionReply(')
    expect(permissionSource).toContain('requestId')
    expect(permissionSource).toContain('reply')
    expect(permissionSource).toContain('worktreePath || undefined')
    expect(permissionSource).toContain('message')
    expect(permissionSource).toContain('unwrapEnvelope(')
    expect(permissionSource).not.toContain('window.opencodeOps.permissionReply')
  })

  it('routes session permission auto-approve replies through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const permissionAskedStart = source.indexOf("if (event.type === 'permission.asked')")
    const permissionRepliedStart = source.indexOf(
      "if (event.type === 'permission.replied')",
      permissionAskedStart
    )
    const permissionAskedSource = source.slice(permissionAskedStart, permissionRepliedStart)

    expect(permissionAskedStart).toBeGreaterThan(-1)
    expect(permissionRepliedStart).toBeGreaterThan(permissionAskedStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(permissionAskedSource).toContain('opencodeApi')
    expect(permissionAskedSource).toContain('.permissionReply(')
    expect(permissionAskedSource).toContain("request.id, 'once', worktreePath || undefined")
    expect(permissionAskedSource).toContain(
      "console.warn('Auto-approve permissionReply failed:', err)"
    )
    expect(permissionAskedSource).not.toContain('window.opencodeOps')
  })

  it('routes session command approval replies through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const commandStart = source.indexOf('// Handle command approval reply')
    const refreshStart = source.indexOf(
      'const refreshMessagesFromOpenCode = useCallback',
      commandStart
    )
    const commandSource = source.slice(commandStart, refreshStart)

    expect(commandStart).toBeGreaterThan(-1)
    expect(refreshStart).toBeGreaterThan(commandStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(commandSource).toContain('await opencodeApi.commandApprovalReply(')
    expect(commandSource).toContain('requestId')
    expect(commandSource).toContain('approved')
    expect(commandSource).toContain('remember')
    expect(commandSource).toContain('pattern')
    expect(commandSource).toContain('worktreePath || undefined')
    expect(commandSource).toContain('patterns')
    expect(commandSource).toContain(
      'useCommandApprovalStore.getState().removeApproval(sessionId, requestId)'
    )
    expect(commandSource).toContain('unwrapEnvelope(')
    expect(commandSource).not.toContain('window.opencodeOps.commandApprovalReply')
  })

  it('routes session unmount draft save through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const draftEffectStart = source.indexOf('// Save draft on unmount or session change')
    const retryStart = source.indexOf('// Handle retry connection', draftEffectStart)
    const draftEffectSource = source.slice(draftEffectStart, retryStart)

    expect(draftEffectStart).toBeGreaterThan(-1)
    expect(retryStart).toBeGreaterThan(draftEffectStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(draftEffectSource).toContain('const currentValue = inputValueRef.current')
    expect(draftEffectSource).toContain('dbApi.session.updateDraft(sessionId, currentValue)')
    expect(draftEffectSource).not.toContain('db.session.updateDraft(sessionId, currentValue)')
  })

  it('routes session initial draft load through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const loadDraftStart = source.indexOf('// Load saved draft for this session')
    const transcriptSourceStart = source.indexOf('transcriptSourceRef.current =', loadDraftStart)
    const loadDraftSource = source.slice(loadDraftStart, transcriptSourceStart)

    expect(loadDraftStart).toBeGreaterThan(-1)
    expect(transcriptSourceStart).toBeGreaterThan(loadDraftStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(loadDraftSource).toContain('dbApi.session.getDraft(sessionId).then((draft) => {')
    expect(loadDraftSource).toContain('setInputValue(draft)')
    expect(loadDraftSource).toContain('inputValueRef.current = draft')
    expect(loadDraftSource).not.toContain('db.session.getDraft(sessionId)')
  })

  it('routes durable Codex session message and activity loading through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const loaderStart = source.indexOf('async function loadCodexDurableState(')
    const cachePrefixStart = source.indexOf(
      "const TRANSCRIPT_CACHE_KEY_PREFIX = 'hive:session-transcript:'",
      loaderStart
    )
    const loaderSource = source.slice(loaderStart, cachePrefixStart)

    expect(loaderStart).toBeGreaterThan(-1)
    expect(cachePrefixStart).toBeGreaterThan(loaderStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(loaderSource).toContain('dbApi.sessionMessage.list<SessionMessage>(sessionId)')
    expect(loaderSource).toContain('dbApi.sessionActivity.list<SessionActivity>(sessionId)')
    expect(loaderSource).toContain('deriveCodexTimelineMessages(messageRows, activityRows, true)')
    expect(loaderSource).not.toContain('db.sessionMessage.list(sessionId)')
    expect(loaderSource).not.toContain('db.sessionActivity.list(sessionId)')
  })

  it('keeps SessionView free of the window.db compatibility alias', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )

    expect(source).toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).not.toContain('import { unwrapEnvelope, unwrapEnvelopeApi }')
    expect(source).not.toContain('const db = unwrapEnvelopeApi(() => window.db)')
    expect(source).not.toContain('db?.')
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('db.')
  })

  it('routes SessionView prompt-send worktree model persistence through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const modelTrackStart = source.indexOf('// Track which model is being used on this worktree')
    const slashCommandStart = source.indexOf(
      '// Detect slash commands and route through the SDK command endpoint',
      modelTrackStart
    )
    const modelTrackSource = source.slice(modelTrackStart, slashCommandStart)

    expect(modelTrackStart).toBeGreaterThan(-1)
    expect(slashCommandStart).toBeGreaterThan(modelTrackStart)
    expect(modelTrackSource).toContain(
      'dbApi.worktree\n              .updateModel({\n                worktreeId,'
    )
    expect(modelTrackSource).toContain('modelProviderId: requestModel.providerID')
    expect(modelTrackSource).toContain('modelId: requestModel.modelID')
    expect(modelTrackSource).toContain('modelVariant: requestModel.variant ?? null')
    expect(modelTrackSource).not.toContain('db?.worktree')
    expect(modelTrackSource).not.toContain('window.db')
  })

  it('routes session initialization record lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const lookupStart = source.indexOf(
      '// 1. Resolve session/worktree metadata so transcript loading can prefer OpenCode'
    )
    const modelHydrationStart = source.indexOf(
      'if (session.model_provider_id && session.model_id)',
      lookupStart
    )
    const lookupSource = source.slice(lookupStart, modelHydrationStart)

    expect(lookupStart).toBeGreaterThan(-1)
    expect(modelHydrationStart).toBeGreaterThan(lookupStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(lookupSource).toContain('const session = await dbApi.session.get<DbSession>(sessionId)')
    expect(lookupSource).toContain("throw new Error('Session not found')")
    expect(lookupSource).not.toContain('db.session.get(sessionId)')
  })

  it('routes session initialization worktree lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const worktreeBranchStart = source.indexOf('if (session.worktree_id) {')
    const connectionBranchStart = source.indexOf(
      '} else if (session.connection_id) {',
      worktreeBranchStart
    )
    const worktreeBranchSource = source.slice(worktreeBranchStart, connectionBranchStart)

    expect(worktreeBranchStart).toBeGreaterThan(-1)
    expect(connectionBranchStart).toBeGreaterThan(worktreeBranchStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(worktreeBranchSource).toContain('setWorktreeId(session.worktree_id)')
    expect(worktreeBranchSource).toContain(
      'const worktree = await dbApi.worktree.get(session.worktree_id)'
    )
    expect(worktreeBranchSource).toContain('setWorktreePath(wtPath)')
    expect(worktreeBranchSource).toContain('transcriptSourceRef.current.worktreePath = wtPath')
    expect(worktreeBranchSource).not.toContain('db.worktree.get(session.worktree_id)')
  })

  it('routes session initialization connection lookup through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const connectionBranchStart = source.indexOf('} else if (session.connection_id) {')
    const branchEnd = source.indexOf('const existingOpcSessionId = session.opencode_session_id')
    const connectionBranchSource = source.slice(connectionBranchStart, branchEnd)

    expect(connectionBranchStart).toBeGreaterThan(-1)
    expect(branchEnd).toBeGreaterThan(connectionBranchStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(connectionBranchSource).toContain('setConnectionId(session.connection_id)')
    expect(connectionBranchSource).toContain(
      'const connResult = await connectionApi.get(session.connection_id)'
    )
    expect(connectionBranchSource).toContain('if (shouldAbortInit()) return')
    expect(connectionBranchSource).toContain('wtPath = connResult.connection.path')
    expect(connectionBranchSource).toContain('setWorktreePath(wtPath)')
    expect(connectionBranchSource).toContain('transcriptSourceRef.current.worktreePath = wtPath')
    expect(connectionBranchSource).toContain(
      "console.warn('Failed to resolve connection path for session')"
    )
    expect(connectionBranchSource).not.toContain('window.connectionOps.get')
    expect(connectionBranchSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes session retry record lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const retryStart = source.indexOf('// Handle retry connection')
    const worktreeCheckStart = source.indexOf('if (!session.worktree_id) {', retryStart)
    const retryLookupSource = source.slice(retryStart, worktreeCheckStart)

    expect(retryStart).toBeGreaterThan(-1)
    expect(worktreeCheckStart).toBeGreaterThan(retryStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(retryLookupSource).toContain(
      'const session = await dbApi.session.get<DbSession>(sessionId)'
    )
    expect(retryLookupSource).toContain("throw new Error('Session not found')")
    expect(retryLookupSource).not.toContain('db.session.get(sessionId)')
  })

  it('routes session retry worktree lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const retryStart = source.indexOf('// Handle retry connection')
    const noWorktreeStart = source.indexOf('if (!session.worktree_id) {', retryStart)
    const setWorktreePathStart = source.indexOf('setWorktreePath(worktree.path)', noWorktreeStart)
    const retryWorktreeSource = source.slice(noWorktreeStart, setWorktreePathStart)

    expect(retryStart).toBeGreaterThan(-1)
    expect(noWorktreeStart).toBeGreaterThan(retryStart)
    expect(setWorktreePathStart).toBeGreaterThan(noWorktreeStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(retryWorktreeSource).toContain(
      'const worktree = await dbApi.worktree.get(session.worktree_id)'
    )
    expect(retryWorktreeSource).toContain('if (!worktree) {')
    expect(retryWorktreeSource).toContain("setViewState({ status: 'connected' })")
    expect(retryWorktreeSource).not.toContain('db.worktree.get(session.worktree_id)')
  })

  it('routes session bash-mode draft clear through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const bashStart = source.indexOf('// === BASH MODE ===')
    const bashEnd = source.indexOf('// === END BASH MODE ===', bashStart)
    const bashSource = source.slice(bashStart, bashEnd)

    expect(bashStart).toBeGreaterThan(-1)
    expect(bashEnd).toBeGreaterThan(bashStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(bashSource).toContain("inputValueRef.current.startsWith('!')")
    expect(bashSource).toContain('if (draftTimerRef.current) clearTimeout(draftTimerRef.current)')
    expect(bashSource).toContain('dbApi.session.updateDraft(sessionId, null)')
    expect(bashSource).toContain('await runBashCommand(command, worktreePath)')
    expect(bashSource).not.toContain('db.session.updateDraft(sessionId, null)')
  })

  it('routes useBashRuns getRun through bashApi instead of the compatibility global', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useBashRuns.ts'),
      'utf-8'
    )
    const effectStart = source.indexOf('// Seed state from any existing run on mount')
    const effectEnd = source.indexOf('// Subscribe to stream events', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(effectSource).toContain('runIpcEffect(() => bashApi.getRun(sessionId))')
    expect(effectSource).not.toContain('window.bash.getRun')
  })

  it('removes the old bash:getRun IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const bashHandlersPath = path.resolve(__dirname, '../../src/main/ipc/bash-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('bash:getRun'")
    expect(fs.existsSync(bashHandlersPath)).toBe(false)
  })

  it('routes useBashRuns stream subscription through bashApi instead of the compatibility global', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useBashRuns.ts'),
      'utf-8'
    )
    const effectStart = source.indexOf('// Subscribe to stream events')
    const effectEnd = source.indexOf('  const isRunning = runs.some', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(effectSource).toContain('const unsubscribe = bashApi.onStream((event) =>')
    expect(effectSource).not.toContain('window.bash.onStream')
  })

  it('removes the old preload bash:stream IPC listener after WebSocket migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.on('bash:stream'")
    expect(preloadSource).not.toContain("ipcRenderer.removeListener('bash:stream'")
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('bash'")
    expect(preloadSource).not.toContain('window.bash = bash')
    expect(preloadTypesSource).not.toContain('bash: {')
    expect(preloadTypesSource).not.toContain(
      'run: (sessionId: string, command: string, cwd: string)'
    )
    expect(preloadTypesSource).not.toContain('getRun: (sessionId: string)')
    expect(preloadTypesSource).not.toContain('onStream: (callback: (event: BashStreamEvent)')
    expect(preloadTypesSource).not.toContain('interface BashRunSnapshot')
    expect(preloadTypesSource).not.toContain('type BashStreamEvent')
  })

  it('removes the Effect bash:stream renderer IPC sender after backend event migration', () => {
    const bashEffectLayersSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/effect/bash/layers.ts'),
      'utf-8'
    )
    const bashRuntimeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/effect/bash/runtime.ts'),
      'utf-8'
    )
    const bashFacadeSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/effect/bash/facade.ts'),
      'utf-8'
    )
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(bashEffectLayersSource).not.toContain('webContents.send(BASH_STREAM_CHANNEL')
    expect(bashEffectLayersSource).not.toContain('win.webContents.send(BASH_STREAM_CHANNEL')
    expect(bashEffectLayersSource).not.toContain('BrowserWindow')
    expect(bashRuntimeSource).not.toContain('BrowserWindow')
    expect(bashRuntimeSource).not.toContain('setMainWindow')
    expect(bashFacadeSource).not.toContain('setMainWindow')
    expect(mainSource).not.toContain('registerBashHandlers')
    expect(mainSource).not.toContain('Registering Bash handlers')
    expect(fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/bash-handlers.ts'))).toBe(
      false
    )
  })

  it('removes the standalone BashService bash:stream renderer IPC sender after backend event migration', () => {
    const bashServiceSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/bash-service.ts'),
      'utf-8'
    )

    expect(bashServiceSource).not.toContain('webContents.send(BASH_STREAM_CHANNEL')
    expect(bashServiceSource).not.toContain('mainWindow.webContents.send(BASH_STREAM_CHANNEL')
    expect(bashServiceSource).not.toContain('BrowserWindow')
    expect(bashServiceSource).not.toContain('setMainWindow')
  })

  it('routes useBashRuns runCommand through bashApi instead of the compatibility global', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useBashRuns.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('  const runCommand = useCallback(')
    const functionEnd = source.indexOf('  const abort = useCallback', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).toContain('runIpcEffect(() => bashApi.run(sessionId, command, cwd))')
    expect(functionSource).not.toContain('window.bash.run')
  })

  it('removes the old bash:run IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const bashHandlersPath = path.resolve(__dirname, '../../src/main/ipc/bash-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('bash:run'")
    expect(fs.existsSync(bashHandlersPath)).toBe(false)
  })

  it('routes useBashRuns abort through bashApi instead of the compatibility global', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useBashRuns.ts'),
      'utf-8'
    )
    const functionStart = source.indexOf('  const abort = useCallback')
    const functionEnd = source.indexOf(
      '  return { runs, isRunning, runCommand, abort }',
      functionStart
    )
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).toContain('runIpcEffect(() => bashApi.abort(sessionId))')
    expect(functionSource).not.toContain('window.bash.abort')
  })

  it('removes the old bash:abort IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const bashHandlersPath = path.resolve(__dirname, '../../src/main/ipc/bash-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('bash:abort'")
    expect(fs.existsSync(bashHandlersPath)).toBe(false)
  })

  it('routes settings integration schema loading through ticketImportApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/settings/SettingsIntegrations.tsx'
      ),
      'utf-8'
    )
    const effectStart = source.indexOf('  useEffect(() => {')
    const effectEnd = source.indexOf('  const handleFieldChange =', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(effectSource).toContain(
      'schemaMap[p.id] = unwrapEnvelope(await ticketImportApi.getSettingsSchema(p.id))'
    )
    expect(effectSource).not.toContain('window.ticketImport.getSettingsSchema')
  })

  it('removes the old ticketImport:getSettingsSchema IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const ticketImportHandlersSource = readOptionalSource(
      '../../src/main/ipc/ticket-import-handlers.ts'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('ticketImport:getSettingsSchema'")
    expect(ticketImportHandlersSource).not.toContain(
      "defineHandler('ticketImport:getSettingsSchema'"
    )
    expect(ticketImportHandlersSource).not.toContain(
      "ticketImportFailed('ticketImport:getSettingsSchema'"
    )
  })

  it('routes settings integration provider loading through ticketImportApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/settings/SettingsIntegrations.tsx'
      ),
      'utf-8'
    )
    const effectStart = source.indexOf('  useEffect(() => {')
    const effectEnd = source.indexOf('  const handleFieldChange =', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(effectSource).toContain('ticketImportApi')
    expect(effectSource).toContain('.listProviders()')
    expect(effectSource).not.toContain('window.ticketImport.listProviders')
  })

  it('removes the old ticketImport:listProviders IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const ticketImportHandlersSource = readOptionalSource(
      '../../src/main/ipc/ticket-import-handlers.ts'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('ticketImport:listProviders'")
    expect(ticketImportHandlersSource).not.toContain("defineHandler('ticketImport:listProviders'")
    expect(ticketImportHandlersSource).not.toContain(
      "ticketImportFailed('ticketImport:listProviders'"
    )
  })

  it('routes settings integration authentication through ticketImportApi', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/settings/SettingsIntegrations.tsx'
      ),
      'utf-8'
    )
    const functionStart = source.indexOf('  const handleTest = async (providerId: string) => {')
    const functionEnd = source.indexOf('  return (', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).toContain('const result = unwrapEnvelope(')
    expect(functionSource).toContain(
      'await ticketImportApi.authenticate(providerId, providerSettings)'
    )
    expect(functionSource).not.toContain('window.ticketImport.authenticate')
  })

  it('removes the old ticketImport:authenticate IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const ticketImportHandlersSource = readOptionalSource(
      '../../src/main/ipc/ticket-import-handlers.ts'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('ticketImport:authenticate'")
    expect(ticketImportHandlersSource).not.toContain("defineHandler('ticketImport:authenticate'")
    expect(ticketImportHandlersSource).not.toContain(
      "ticketImportFailed('ticketImport:authenticate'"
    )
  })

  it('routes GitHub ticket import repo detection through ticketImportApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/ImportTicketsModal.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('  // Auto-detect repo when modal opens')
    const effectEnd = source.indexOf('  // Fetch issues when repo/page/search/showClosed changes')
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(effectSource).toContain('ticketImportApi')
    expect(effectSource).toContain(".detectRepo('github', effectiveProjectPath)")
    expect(effectSource).not.toContain('window.ticketImport.detectRepo')
  })

  it('removes the old ticketImport:detectRepo IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const ticketImportHandlersSource = readOptionalSource(
      '../../src/main/ipc/ticket-import-handlers.ts'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('ticketImport:detectRepo'")
    expect(ticketImportHandlersSource).not.toContain("defineHandler('ticketImport:detectRepo'")
    expect(ticketImportHandlersSource).not.toContain("ticketImportFailed('ticketImport:detectRepo'")
  })

  it('routes GitHub ticket issue listing through ticketImportApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/ImportTicketsModal.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('  // Fetch issues when repo/page/search/showClosed changes')
    const effectEnd = source.indexOf('  // Search debounce', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(effectSource).toContain('ticketImportApi')
    expect(effectSource).toContain('.listIssues(')
    expect(effectSource).toContain("'github'")
    expect(effectSource).toContain('effectiveRepo')
    expect(effectSource).toContain('getProviderSettings()')
    expect(effectSource).not.toContain('window.ticketImport.listIssues')
  })

  it('routes GitHub ticket importing through ticketImportApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/ImportTicketsModal.tsx'),
      'utf-8'
    )
    const functionStart = source.indexOf('  const handleImport = async () => {')
    const functionEnd = source.indexOf('  return (', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).toContain('await ticketImportApi.importIssues(')
    expect(functionSource).toContain("'github'")
    expect(functionSource).toContain('effectiveProjectId')
    expect(functionSource).toContain('effectiveRepo')
    expect(functionSource).toContain('toImport.map((i) => ({')
    expect(functionSource).not.toContain('window.ticketImport.importIssues')
  })

  it('routes Jira ticket issue listing through ticketImportApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/JiraImportModal.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('  // Fetch issues when committed JQL / page changes')
    const effectEnd = source.indexOf('  const handleSearch = () => {', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(effectSource).toContain('ticketImportApi')
    expect(effectSource).toContain('.listIssues(')
    expect(effectSource).toContain("'jira'")
    expect(effectSource).toContain('domain')
    expect(effectSource).toContain('committedJql')
    expect(effectSource).toContain('nextPageToken: token')
    expect(effectSource).toContain('getProviderSettings()')
    expect(effectSource).not.toContain('window.ticketImport.listIssues')
  })

  it('removes the old ticketImport:listIssues IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const ticketImportHandlersSource = readOptionalSource(
      '../../src/main/ipc/ticket-import-handlers.ts'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('ticketImport:listIssues'")
    expect(ticketImportHandlersSource).not.toContain("defineHandler('ticketImport:listIssues'")
    expect(ticketImportHandlersSource).not.toContain("ticketImportFailed('ticketImport:listIssues'")
  })

  it('routes Jira ticket importing through ticketImportApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/JiraImportModal.tsx'),
      'utf-8'
    )
    const functionStart = source.indexOf('  const handleImport = async () => {')
    const functionEnd = source.indexOf(
      "  const stateBadgeClass = (state: RemoteIssue['state']) => {"
    )
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).toContain('await ticketImportApi.importIssues(')
    expect(functionSource).toContain("'jira'")
    expect(functionSource).toContain('projectId')
    expect(functionSource).toContain('domain')
    expect(functionSource).toContain('toImport.map((i) => ({')
    expect(functionSource).not.toContain('window.ticketImport.importIssues')
  })

  it('removes the old ticketImport:importIssues IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const ticketImportHandlersSource = readOptionalSource(
      '../../src/main/ipc/ticket-import-handlers.ts'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('ticketImport:importIssues'")
    expect(ticketImportHandlersSource).not.toContain("defineHandler('ticketImport:importIssues'")
    expect(ticketImportHandlersSource).not.toContain(
      "ticketImportFailed('ticketImport:importIssues'"
    )
  })

  it('routes remote status loading through ticketImportApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/UpdateStatusModal.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('  useEffect(() => {')
    const effectEnd = source.indexOf('  const handleUpdate = async (statusId: string) => {')
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(effectSource).toContain('ticketImportApi')
    expect(effectSource).toContain('.getAvailableStatuses(')
    expect(effectSource).toContain('externalProvider')
    expect(effectSource).toContain('repo')
    expect(effectSource).toContain('externalId')
    expect(effectSource).toContain('getProviderSettings()')
    expect(effectSource).not.toContain('window.ticketImport.getAvailableStatuses')
  })

  it('removes the old ticketImport:getAvailableStatuses IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const ticketImportHandlersSource = readOptionalSource(
      '../../src/main/ipc/ticket-import-handlers.ts'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('ticketImport:getAvailableStatuses'")
    expect(ticketImportHandlersSource).not.toContain(
      "defineHandler('ticketImport:getAvailableStatuses'"
    )
    expect(ticketImportHandlersSource).not.toContain(
      "ticketImportFailed('ticketImport:getAvailableStatuses'"
    )
  })

  it('routes remote status updates through ticketImportApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/UpdateStatusModal.tsx'),
      'utf-8'
    )
    const functionStart = source.indexOf('  const handleUpdate = async (statusId: string) => {')
    const functionEnd = source.indexOf('  return (', functionStart)
    const functionSource = source.slice(functionStart, functionEnd)

    expect(functionStart).toBeGreaterThan(-1)
    expect(functionEnd).toBeGreaterThan(functionStart)
    expect(functionSource).toContain('await ticketImportApi.updateRemoteStatus(')
    expect(functionSource).toContain('externalProvider')
    expect(functionSource).toContain('repo')
    expect(functionSource).toContain('externalId')
    expect(functionSource).toContain('statusId')
    expect(functionSource).toContain('getProviderSettings()')
    expect(functionSource).not.toContain('window.ticketImport.updateRemoteStatus')
  })

  it('removes the old ticketImport:updateRemoteStatus IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )
    const ticketImportHandlersSource = readOptionalSource(
      '../../src/main/ipc/ticket-import-handlers.ts'
    )

    expect(preloadSource).not.toContain("ipcRenderer.invoke('ticketImport:updateRemoteStatus'")
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('ticketImport'")
    expect(preloadSource).not.toContain('window.ticketImport = ticketImport')
    expect(preloadTypesSource).not.toContain('ticketImport: {')
    expect(preloadTypesSource).not.toContain('listProviders: () => Promise<Envelope<')
    expect(preloadTypesSource).not.toContain('getSettingsSchema: (providerId: string)')
    expect(preloadTypesSource).not.toContain('updateRemoteStatus: (')
    expect(ticketImportHandlersSource).not.toContain(
      "defineHandler('ticketImport:updateRemoteStatus'"
    )
    expect(ticketImportHandlersSource).not.toContain(
      "ticketImportFailed('ticketImport:updateRemoteStatus'"
    )
  })

  it('removes the old empty ticket import IPC handler registrar after RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')

    expect(
      fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/ticket-import-handlers.ts'))
    ).toBe(false)
    expect(mainSource).not.toContain('registerTicketImportHandlers')
    expect(mainSource).not.toContain('./ipc/ticket-import-handlers')
  })

  it('routes kanban ticket loading through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf('      loadTickets: async (projectId: string) => {')
    const actionEnd = source.indexOf('      // ── createTicket', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain('kanbanApi.ticket.getByProject<KanbanTicket>(')
    expect(actionSource).toContain('projectId')
    expect(actionSource).toContain('includeArchived')
    expect(actionSource).not.toContain('kanban.ticket.getByProject')
  })

  it('routes connection kanban ticket loading through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      '      loadTicketsForConnection: async (connectionId: string) => {'
    )
    const actionEnd = source.indexOf('      // ── getTicketsByColumnForConnection', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain('kanbanApi.ticket.getByProject<KanbanTicket>(')
    expect(actionSource).toContain('pid')
    expect(actionSource).toContain('includeArchived(pid)')
    expect(actionSource).not.toContain('kanban.ticket.getByProject')
  })

  it('routes pinned kanban ticket loading through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf('      loadTicketsForPinnedProjects: async () => {')
    const actionEnd = source.indexOf('      // ── getTicketsByColumnForPinned', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain('kanbanApi.ticket.getByProject<KanbanTicket>(')
    expect(actionSource).toContain('pid')
    expect(actionSource).toContain('includeArchived(pid)')
    expect(actionSource).not.toContain('kanban.ticket.getByProject')
  })

  it('removes the old kanban:ticket:getByProject IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("invokeEnvelope('kanban:ticket:getByProject'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:getByProject'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:getByProject'")
  })

  it('routes kanban ticket creation through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      '      createTicket: async (projectId: string, data: KanbanTicketCreate) => {'
    )
    const actionEnd = source.indexOf('      // ── updateTicket', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain(
      'await kanbanApi.ticket.create<KanbanTicket, KanbanTicketCreate>(data)'
    )
    expect(actionSource).toContain('next.set(projectId, [...existing, ticket])')
    expect(actionSource).not.toContain('kanban.ticket.create')
  })

  it('removes the old kanban:ticket:create IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("invokeEnvelope('kanban:ticket:create',")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:create',")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:create',")
  })

  it('routes kanban ticket updates through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      '      updateTicket: async (ticketId: string, projectId: string, data: KanbanTicketUpdate) => {'
    )
    const actionEnd = source.indexOf('      // ── deleteTicket', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain(
      'await kanbanApi.ticket.update<KanbanTicket | null, KanbanTicketUpdate>(ticketId, data)'
    )
    expect(actionSource).toContain('const snapshot = prev.map((t) => ({ ...t }))')
    expect(actionSource).not.toContain('kanban.ticket.update')
  })

  it('removes the old kanban:ticket:update IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("invokeEnvelope('kanban:ticket:update'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:update'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:update'")
    expect(kanbanHandlersSource).not.toContain('kanbanTicketUpdateSchema')
  })

  it('routes kanban ticket deletion through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      '      deleteTicket: async (ticketId: string, projectId: string) => {'
    )
    const actionEnd = source.indexOf('      // ── archiveTicket', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain('await kanbanApi.ticket.delete(ticketId)')
    expect(actionSource).toContain('const snapshot = prev.map((t) => ({ ...t }))')
    expect(actionSource).toContain('kanbanApi.dependency.removeAll(ticketId).catch(() => {})')
    expect(actionSource).not.toContain('kanban.ticket.delete')
    expect(actionSource).not.toContain('kanban.dependency.removeAll')
  })

  it('removes the old kanban:ticket:delete IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("invokeEnvelope('kanban:ticket:delete'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:delete'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:delete'")
  })

  it('routes kanban ticket archiving through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      '      archiveTicket: async (ticketId: string, projectId: string) => {'
    )
    const actionEnd = source.indexOf('      // ── archiveAllDone', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain('await kanbanApi.ticket.archive<KanbanTicket | null>(ticketId)')
    expect(actionSource).toContain('await kanbanApi.dependency.removeAll(ticketId)')
    expect(actionSource).not.toContain('kanban.ticket.archive')
    expect(actionSource).not.toContain('kanban.dependency.removeAll')
  })

  it('removes the old kanban:ticket:archive IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("invokeEnvelope('kanban:ticket:archive',")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:archive',")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:archive',")
  })

  it('removes the old kanban:dependency:removeAll IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("invokeEnvelope<number>('kanban:dependency:removeAll'")
    expect(preloadSource).not.toContain("invokeEnvelope('kanban:dependency:removeAll'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:dependency:removeAll'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:dependency:removeAll'")
  })

  it('routes done-ticket bulk archiving through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      '      archiveAllDone: async (projectId: string): Promise<number> => {'
    )
    const actionEnd = source.indexOf('      // ── unarchiveTicket', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain('await kanbanApi.ticket.archiveAllDone(projectId)')
    expect(actionSource).toContain('return count')
    expect(actionSource).not.toContain('kanban.ticket.archiveAllDone')
  })

  it('removes the old kanban:ticket:archiveAllDone IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("invokeEnvelope('kanban:ticket:archiveAllDone'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:archiveAllDone'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:archiveAllDone'")
  })

  it('routes kanban ticket unarchiving through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      '      unarchiveTicket: async (ticketId: string, projectId: string) => {'
    )
    const actionEnd = source.indexOf('      // ── detachWorktreeTickets', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain(
      'await kanbanApi.ticket.unarchive<KanbanTicket | null>(ticketId)'
    )
    expect(actionSource).toContain('const snapshot = prev.map((t) => ({ ...t }))')
    expect(actionSource).not.toContain('kanban.ticket.unarchive')
  })

  it('removes the old kanban:ticket:unarchive IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("invokeEnvelope('kanban:ticket:unarchive'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:unarchive'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:unarchive'")
  })

  it('routes kanban worktree detachment through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      '      detachWorktreeTickets: async (worktreeId: string) => {'
    )
    const actionEnd = source.indexOf('      // ── setShowArchived', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain('await kanbanApi.ticket.detachWorktree(worktreeId)')
    expect(actionSource).toContain('const snapshot = new Map<string, KanbanTicket[]>()')
    expect(actionSource).not.toContain('kanban.ticket.detachWorktree')
  })

  it('removes the old kanban:ticket:detachWorktree IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("invokeEnvelope('kanban:ticket:detachWorktree'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:detachWorktree'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:detachWorktree'")
  })

  it('routes kanban ticket moves through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf('      moveTicket: async (')
    const actionEnd = source.indexOf('      // ── reorderTicket', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain(
      'await kanbanApi.ticket.move<KanbanTicket | null>(ticketId, column, sortOrder)'
    )
    expect(actionSource).toContain('useWorktreeStatusStore.getState().clearCompletedReviewSession')
    expect(actionSource).toContain('autoLaunchTicket(depTicket)')
    expect(actionSource).not.toContain('kanban.ticket.move')
  })

  it('removes the old kanban:ticket:move IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("invokeEnvelope('kanban:ticket:move'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:move'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:move'")
  })

  it('routes kanban ticket reordering through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      '      reorderTicket: async (ticketId: string, projectId: string, newSortOrder: number) => {'
    )
    const actionEnd = source.indexOf('      // ── toggleBoardView', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain('await kanbanApi.ticket.reorder(ticketId, newSortOrder)')
    expect(actionSource).toContain('const snapshot = prev.map((t) => ({ ...t }))')
    expect(actionSource).not.toContain('kanban.ticket.reorder')
  })

  it('removes the old kanban:ticket:reorder IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("invokeEnvelope('kanban:ticket:reorder'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:reorder'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:reorder'")
  })

  it('routes kanban simple-mode toggles through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      '      setSimpleMode: async (projectId: string, enabled: boolean) => {'
    )
    const actionEnd = source.indexOf('      // ── syncTicketWithSession', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain(
      'simpleModeByProject: { ...state.simpleModeByProject, [projectId]: enabled }'
    )
    expect(actionSource).toContain('await kanbanApi.simpleMode.toggle(projectId, enabled)')
    expect(actionSource).not.toContain('kanban.simpleMode.toggle')
  })

  it('removes the old kanban:simpleMode:toggle IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("invokeEnvelope('kanban:simpleMode:toggle'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:simpleMode:toggle'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:simpleMode:toggle'")
  })

  it('routes kanban ticket token accumulation through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf("              case 'session_completed': {")
    const actionEnd = source.indexOf("              case 'plan_ready': {", actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain('.addTokens<KanbanTicket | null>(ticket.id, event.tokenDelta)')
    expect(actionSource).toContain('total_tokens: updated.total_tokens')
    expect(actionSource).not.toContain('kanban.ticket.addTokens')
  })

  it('removes the old kanban:ticket:addTokens IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("invokeEnvelope('kanban:ticket:addTokens'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:addTokens'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:addTokens'")
  })

  it('routes kanban session ticket lookup through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf('      relinkTicketsForHandoff: async (')
    const actionEnd = source.indexOf('        for (const ticket of linkedTickets) {', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain(
      'const linkedTickets = await kanbanApi.ticket.getBySession<KanbanTicket>(oldSessionId)'
    )
    expect(actionSource).toContain('if (!linkedTickets || linkedTickets.length === 0) return')
    expect(actionSource).not.toContain('kanban.ticket.getBySession')
  })

  it('removes the old kanban:ticket:getBySession IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("invokeEnvelope('kanban:ticket:getBySession'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:getBySession'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:getBySession'")
  })

  it('routes kanban handoff relink updates through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf('      relinkTicketsForHandoff: async (')
    const actionEnd = source.indexOf('      // ── addDependency', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain(
      'await kanbanApi.ticket.update<KanbanTicket | null, KanbanTicketUpdate>(ticket.id, {'
    )
    expect(actionSource).toContain('current_session_id: newSessionId')
    expect(actionSource).toContain('goal_success_criteria: nextGoalSuccessCriteria')
    expect(actionSource).not.toContain('kanban.ticket.update')
  })

  it('routes kanban dependency loading through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf('      loadDependencies: async (projectId: string) => {')
    const actionEnd = source.indexOf('      // ── addDependency', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain('const deps = await kanbanApi.dependency.getForProject<{')
    expect(actionSource).toContain('existing.add(dep.blocker_id)')
    expect(actionSource).not.toContain('kanban.dependency.getForProject')
  })

  it('removes the old kanban:dependency:getForProject IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("invokeEnvelope('kanban:dependency:getForProject'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:dependency:getForProject'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:dependency:getForProject'")
  })

  it('routes kanban dependency creation through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      '      addDependency: async (dependentId: string, blockerId: string) => {'
    )
    const actionEnd = source.indexOf('      // ── removeDependency', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain(
      'const result = await kanbanApi.dependency.add(dependentId, blockerId)'
    )
    expect(actionSource).toContain('if (result.success) {')
    expect(actionSource).toContain('newSet.add(blockerId)')
    expect(actionSource).not.toContain('kanban.dependency.add')
  })

  it('removes the old kanban:dependency:add IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain('invokeEnvelope<{ success: boolean; error?: string }>(')
    expect(preloadSource).not.toContain("'kanban:dependency:add'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:dependency:add'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:dependency:add'")
  })

  it('routes kanban dependency removal through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useKanbanStore.ts'),
      'utf-8'
    )
    const actionStart = source.indexOf(
      '      removeDependency: async (dependentId: string, blockerId: string) => {'
    )
    const actionEnd = source.indexOf('      // ── enterDependencyMode', actionStart)
    const actionSource = source.slice(actionStart, actionEnd)

    expect(actionStart).toBeGreaterThan(-1)
    expect(actionEnd).toBeGreaterThan(actionStart)
    expect(actionSource).toContain('await kanbanApi.dependency.remove(dependentId, blockerId)')
    expect(actionSource).toContain('newSet.delete(blockerId)')
    expect(actionSource).toContain('newMap.delete(dependentId)')
    expect(actionSource).not.toContain('kanban.dependency.remove')
  })

  it('removes the old kanban:dependency:remove IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("invokeEnvelope<boolean>('kanban:dependency:remove'")
    expect(preloadSource).not.toContain("'kanban:dependency:remove'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:dependency:remove'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:dependency:remove'")
  })

  it('removes the old kanban:dependency:getBlockers IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("'kanban:dependency:getBlockers'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:dependency:getBlockers'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:dependency:getBlockers'")
  })

  it('removes the old kanban:dependency:getDependents IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("'kanban:dependency:getDependents'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:dependency:getDependents'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:dependency:getDependents'")
  })

  it('routes session first-time connection through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const initStart = source.indexOf('const initializeSession = async (): Promise<void> => {')
    const retryStart = source.indexOf('// Handle retry connection', initStart)
    const initSource = source.slice(initStart, retryStart)
    const connectStart = initSource.indexOf('// Create new OpenCode session')
    const connectSource = initSource.slice(connectStart)

    expect(initStart).toBeGreaterThan(-1)
    expect(retryStart).toBeGreaterThan(initStart)
    expect(connectStart).toBeGreaterThan(-1)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(connectSource).toContain(
      'const connectResult = unwrapEnvelope(await opencodeApi.connect(wtPath, sessionId))'
    )
    expect(connectSource).toContain('setOpencodeSessionId(connectResult.sessionId)')
    expect(connectSource).toContain(
      'useSessionStore.getState().setOpenCodeSessionId(sessionId, connectResult.sessionId)'
    )
    expect(connectSource).toContain('await sendPendingMessage(wtPath, connectResult.sessionId)')
    expect(connectSource).not.toContain('window.opencodeOps.connect(wtPath, sessionId)')
  })

  it('routes session initialization OpenCode id persistence through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const initStart = source.indexOf('const initializeSession = async (): Promise<void> => {')
    const retryStart = source.indexOf('// Handle retry connection', initStart)
    const initSource = source.slice(initStart, retryStart)
    const persistStart = initSource.indexOf('// Persist only for first-time session connections.')
    const fetchCommandsStart = initSource.indexOf('fetchCommands(wtPath)', persistStart)
    const persistSource = initSource.slice(persistStart, fetchCommandsStart)

    expect(initStart).toBeGreaterThan(-1)
    expect(retryStart).toBeGreaterThan(initStart)
    expect(persistStart).toBeGreaterThan(-1)
    expect(fetchCommandsStart).toBeGreaterThan(persistStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(persistSource).toContain('if (!existingOpcSessionId) {')
    expect(persistSource).toContain('await dbApi.session.update<DbSession>(sessionId, {')
    expect(persistSource).toContain('opencode_session_id: connectResult.sessionId')
    expect(persistSource).not.toContain('await db.session.update(sessionId, {')
  })

  it('routes session initialization availability through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const initStart = source.indexOf('const initializeSession = async (): Promise<void> => {')
    const retryStart = source.indexOf('// Handle retry connection', initStart)
    const initSource = source.slice(initStart, retryStart)
    const connectionSetupStart = initSource.indexOf('// 3. Continue with OpenCode connection setup')
    const connectStart = initSource.indexOf('// 4. Connect to OpenCode', connectionSetupStart)
    const connectionSetupSource = initSource.slice(connectionSetupStart, connectStart)

    expect(initStart).toBeGreaterThan(-1)
    expect(retryStart).toBeGreaterThan(initStart)
    expect(connectionSetupStart).toBeGreaterThan(-1)
    expect(connectStart).toBeGreaterThan(connectionSetupStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(connectionSetupSource).toContain('if (!wtPath)')
    expect(connectionSetupSource).toContain("console.warn('No worktree path for session")
    expect(connectionSetupSource).not.toContain('window.opencodeOps')
    expect(initSource).toContain(
      'await opencodeApi.reconnect(wtPath, existingOpcSessionId, sessionId)'
    )
    expect(initSource).toContain('await opencodeApi.connect(wtPath, sessionId)')
  })

  it('routes session pending initial prompt through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const pendingStart = source.indexOf(
      'const sendPendingMessage = async (path: string, opcId: string): Promise<void> => {'
    )
    const pendingEnd = source.indexOf('if (existingOpcSessionId) {', pendingStart)
    const pendingSource = source.slice(pendingStart, pendingEnd)

    expect(pendingStart).toBeGreaterThan(-1)
    expect(pendingEnd).toBeGreaterThan(pendingStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(pendingSource).toContain(
      'const pendingMsg = useSessionStore.getState().dequeuePendingMessage(sessionId)'
    )
    expect(pendingSource).toContain('const promptMessage = modePrefix + pendingMsg')
    expect(pendingSource).toContain('lastSentPromptRef.current = promptMessage')
    expect(pendingSource).toContain('const model = getModelForRequests()')
    expect(pendingSource).toContain("const parts: Array<{ type: 'text'; text: string }>")
    expect(pendingSource).toContain(
      'await opencodeApi.prompt(path, opcId, parts, model, codexPromptOptions)'
    )
    expect(pendingSource).toContain('restorePendingAfterFailure()')
    expect(pendingSource).not.toContain('window.opencodeOps.prompt')
  })

  it('routes session initialization reconnect through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const reconnectStart = source.indexOf('// Try to reconnect to existing session')
    const connectStart = source.indexOf('// Create new OpenCode session', reconnectStart)
    const reconnectSource = source.slice(reconnectStart, connectStart)

    expect(reconnectStart).toBeGreaterThan(-1)
    expect(connectStart).toBeGreaterThan(reconnectStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(reconnectSource).toContain(
      'await opencodeApi.reconnect(wtPath, existingOpcSessionId, sessionId)'
    )
    expect(reconnectSource).toContain('reconnectResult.revertMessageID')
    expect(reconnectSource).toContain('reconnectResult.sessionStatus')
    expect(reconnectSource).toContain('await sendPendingMessage(wtPath, existingOpcSessionId)')
    expect(reconnectSource).not.toContain('window.opencodeOps.reconnect')
  })

  it('routes session retry fallback connection through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const retryStart = source.indexOf('// Handle retry connection')
    const replyStart = source.indexOf('const handleQuestionReply = useCallback', retryStart)
    const retrySource = source.slice(retryStart, replyStart)

    expect(retryStart).toBeGreaterThan(-1)
    expect(replyStart).toBeGreaterThan(retryStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(retrySource).toContain(
      'const connectResult = unwrapEnvelope(await opencodeApi.connect(worktree.path, sessionId))'
    )
    expect(retrySource).toContain('activeOpcSessionId = connectResult.sessionId')
    expect(retrySource).toContain('setOpencodeSessionId(connectResult.sessionId)')
    expect(retrySource).toContain(
      'useSessionStore.getState().setOpenCodeSessionId(sessionId, connectResult.sessionId)'
    )
    expect(retrySource).toContain('await dbApi.session.update<DbSession>(sessionId, {')
    expect(retrySource).toContain('opencode_session_id: connectResult.sessionId')
    expect(retrySource).not.toContain('await db.session.update(sessionId, {')
    expect(retrySource).not.toContain('window.opencodeOps.connect(worktree.path, sessionId)')
  })

  it('routes session retry availability through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const retryStart = source.indexOf('// Handle retry connection')
    const replyStart = source.indexOf('const handleQuestionReply = useCallback', retryStart)
    const retrySource = source.slice(retryStart, replyStart)
    const existingSessionStart = retrySource.indexOf('if (existingOpcSessionId) {')
    const transcriptLoadStart = retrySource.indexOf('const transcriptResult = unwrapEnvelope(')

    expect(retryStart).toBeGreaterThan(-1)
    expect(replyStart).toBeGreaterThan(retryStart)
    expect(existingSessionStart).toBeGreaterThan(-1)
    expect(transcriptLoadStart).toBeGreaterThan(existingSessionStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(retrySource).toContain("throw new Error('Session not found')")
    expect(retrySource).toContain('if (!session.worktree_id)')
    expect(retrySource).toContain('if (!worktree)')
    expect(retrySource).toContain(
      'await opencodeApi.reconnect(worktree.path, existingOpcSessionId, sessionId)'
    )
    expect(retrySource).toContain('await opencodeApi.connect(worktree.path, sessionId)')
    expect(retrySource).toContain(
      'await opencodeApi.getMessages(worktree.path, activeOpcSessionId)'
    )
    expect(retrySource).not.toContain('window.opencodeOps')
  })

  it('routes session Codex refresh live transcript load through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const refreshStart = source.indexOf('const refreshMessagesFromOpenCode = useCallback')
    const durableFallbackStart = source.indexOf(
      'if (durableState.messages.length > 0)',
      refreshStart
    )
    const codexRefreshSource = source.slice(refreshStart, durableFallbackStart)

    expect(refreshStart).toBeGreaterThan(-1)
    expect(durableFallbackStart).toBeGreaterThan(refreshStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(codexRefreshSource).toContain(
      'await opencodeApi.getMessages(worktreePath, opencodeSessionId)'
    )
    expect(codexRefreshSource).not.toContain(
      'await window.opencodeOps.getMessages(worktreePath, opencodeSessionId)'
    )
  })

  it('routes session regular refresh transcript load through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const guardStart = source.indexOf('if (!worktreePath || !opencodeSessionId) return false')
    const durableRefreshStart = source.indexOf(
      'const refreshCodexMessagesFromDurableState = useCallback',
      guardStart
    )
    const regularRefreshSource = source.slice(guardStart, durableRefreshStart)

    expect(guardStart).toBeGreaterThan(-1)
    expect(durableRefreshStart).toBeGreaterThan(guardStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(regularRefreshSource).toContain(
      'await opencodeApi.getMessages(worktreePath, opencodeSessionId)'
    )
    expect(regularRefreshSource).not.toContain(
      'await window.opencodeOps.getMessages(worktreePath, opencodeSessionId)'
    )
  })

  it('routes session Codex streaming refresh transcript load through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const streamingRefreshStart = source.indexOf(
      'const refreshCodexStreamingMessages = useCallback'
    )
    const scheduleRefreshStart = source.indexOf(
      'const scheduleCodexStreamingRefresh = useCallback',
      streamingRefreshStart
    )
    const streamingRefreshSource = source.slice(streamingRefreshStart, scheduleRefreshStart)

    expect(streamingRefreshStart).toBeGreaterThan(-1)
    expect(scheduleRefreshStart).toBeGreaterThan(streamingRefreshStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(streamingRefreshSource).toContain(
      'await opencodeApi.getMessages(worktreePath, opencodeSessionId)'
    )
    expect(streamingRefreshSource).not.toContain(
      'await window.opencodeOps.getMessages(worktreePath, opencodeSessionId)'
    )
  })

  it('routes session refresh-from-thread event through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const eventStart = source.indexOf('const onRefreshFromFile = (event: Event): void => {')
    const cleanupStart = source.indexOf(
      "window.addEventListener('hive:refresh-codex-from-file'",
      eventStart
    )
    const refreshSource = source.slice(eventStart, cleanupStart)

    expect(eventStart).toBeGreaterThan(-1)
    expect(cleanupStart).toBeGreaterThan(eventStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(refreshSource).toContain(
      'await opencodeApi.refreshFromThread(worktreePath, opencodeSessionId)'
    )
    expect(refreshSource).not.toContain(
      'await window.opencodeOps.refreshFromThread(worktreePath, opencodeSessionId)'
    )
  })

  it('routes session queued follow-up prompt through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const idleFollowUpStart = source.indexOf('void handleSessionIdleFollowUp({')
    const idleFollowUpEnd = source.indexOf("} else if (status.type === 'retry')", idleFollowUpStart)
    const idleFollowUpSource = source.slice(idleFollowUpStart, idleFollowUpEnd)

    expect(idleFollowUpStart).toBeGreaterThan(-1)
    expect(idleFollowUpEnd).toBeGreaterThan(idleFollowUpStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(idleFollowUpSource).toContain('dispatchFollowUp: async (message) => {')
    expect(idleFollowUpSource).toContain('const wtPath = transcriptSourceRef.current.worktreePath')
    expect(idleFollowUpSource).toContain(
      'const opcSid = transcriptSourceRef.current.opencodeSessionId'
    )
    expect(idleFollowUpSource).toContain('await opencodeApi.prompt(')
    expect(idleFollowUpSource).toContain('wtPath')
    expect(idleFollowUpSource).toContain('opcSid')
    expect(idleFollowUpSource).toContain("[{ type: 'text', text: message }]")
    expect(idleFollowUpSource).toContain('getModelForRequests()')
    expect(idleFollowUpSource).not.toContain('window.opencodeOps.prompt')
  })

  it('routes session queued follow-up draft clear through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const queuedStart = source.indexOf('// If already streaming, this is a queued follow-up')
    const queuedBranchStart = source.indexOf('} else {', queuedStart)
    const queuedBranchEnd = source.indexOf('return', queuedBranchStart)
    const queuedBranchSource = source.slice(queuedBranchStart, queuedBranchEnd)

    expect(queuedStart).toBeGreaterThan(-1)
    expect(queuedBranchStart).toBeGreaterThan(queuedStart)
    expect(queuedBranchEnd).toBeGreaterThan(queuedBranchStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(queuedBranchSource).toContain('setQueuedMessages((prev) => [')
    expect(queuedBranchSource).toContain(
      'useSessionStore.getState().enqueueFollowUpMessage(sessionId, trimmedValue)'
    )
    expect(queuedBranchSource).toContain('setInputValue')
    expect(queuedBranchSource).toContain('fileMentions.clearMentions()')
    expect(queuedBranchSource).toContain(
      'if (draftTimerRef.current) clearTimeout(draftTimerRef.current)'
    )
    expect(queuedBranchSource).toContain('dbApi.session.updateDraft(sessionId, null)')
    expect(queuedBranchSource).not.toContain('db.session.updateDraft(sessionId, null)')
  })

  it('routes session /ask prompt through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const askStart = source.indexOf("if (commandName === 'ask') {")
    const askEnd = source.indexOf('// If already streaming, this is a queued follow-up', askStart)
    const askSource = source.slice(askStart, askEnd)

    expect(askStart).toBeGreaterThan(-1)
    expect(askEnd).toBeGreaterThan(askStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(askSource).toContain('const question = trimmedValue.slice(5).trim()')
    expect(askSource).toContain("const configuredAskModel = settings.getModelForMode('ask')")
    expect(askSource).toContain(
      'const selectedModel = askModel ?? fallbackModel ?? getModelForRequests()'
    )
    expect(askSource).toContain(
      'const prefixedQuestion = prAskContext + ASK_MODE_PREFIX + question'
    )
    expect(askSource).toContain(
      'const parts = buildMessageParts(attachments, prefixedQuestion, diffComments)'
    )
    expect(askSource).toContain('await opencodeApi.prompt(')
    expect(askSource).toContain('worktreePath')
    expect(askSource).toContain('opencodeSessionId')
    expect(askSource).toContain('parts')
    expect(askSource).toContain('selectedModel')
    expect(askSource).toContain('codexPromptOptions')
    expect(askSource).not.toContain('window.opencodeOps.prompt')
  })

  it('routes session /ask draft clear through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const askStart = source.indexOf("if (commandName === 'ask') {")
    const sendingStart = source.indexOf('// Set sending state', askStart)
    const draftClearSource = source.slice(askStart, sendingStart)

    expect(askStart).toBeGreaterThan(-1)
    expect(sendingStart).toBeGreaterThan(askStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(draftClearSource).toContain("toast.error('Please provide a question after /ask')")
    expect(draftClearSource).toContain("toast.error('OpenCode is not connected')")
    expect(draftClearSource).toContain('setSlashDismissed(false)')
    expect(draftClearSource).toContain('fileMentions.clearMentions()')
    expect(draftClearSource).toContain(
      'if (draftTimerRef.current) clearTimeout(draftTimerRef.current)'
    )
    expect(draftClearSource).toContain('dbApi.session.updateDraft(sessionId, null)')
    expect(draftClearSource).not.toContain('db.session.updateDraft(sessionId, null)')
  })

  it('routes session unknown slash-command prompt fallback through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const fallbackStart = source.indexOf('// Unknown command — send as regular prompt')
    const regularPromptStart = source.indexOf('// Regular prompt — existing code', fallbackStart)
    const fallbackSource = source.slice(fallbackStart, regularPromptStart)

    expect(fallbackStart).toBeGreaterThan(-1)
    expect(regularPromptStart).toBeGreaterThan(fallbackStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(fallbackSource).toContain('const promptMessage = prContext + modePrefix + trimmedValue')
    expect(fallbackSource).toContain('lastSentPromptRef.current = promptMessage')
    expect(fallbackSource).toContain(
      'const parts = buildMessageParts(attachments, promptMessage, diffComments)'
    )
    expect(fallbackSource).toContain('await opencodeApi.prompt(')
    expect(fallbackSource).toContain('worktreePath')
    expect(fallbackSource).toContain('opencodeSessionId')
    expect(fallbackSource).toContain('parts')
    expect(fallbackSource).toContain('requestModel')
    expect(fallbackSource).toContain('codexPromptOptions')
    expect(fallbackSource).not.toContain('window.opencodeOps.prompt')
  })

  it('routes session regular prompt dispatch through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const regularPromptStart = source.indexOf('// Regular prompt — existing code')
    const disconnectedStart = source.indexOf(
      '// No OpenCode connection - show placeholder',
      regularPromptStart
    )
    const regularPromptSource = source.slice(regularPromptStart, disconnectedStart)

    expect(regularPromptStart).toBeGreaterThan(-1)
    expect(disconnectedStart).toBeGreaterThan(regularPromptStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(regularPromptSource).toContain(
      'const promptMessage = prContext + modePrefix + trimmedValue'
    )
    expect(regularPromptSource).toContain('lastSentPromptRef.current = promptMessage')
    expect(regularPromptSource).toContain(
      'const parts = buildMessageParts(attachments, promptMessage, diffComments)'
    )
    expect(regularPromptSource).toContain('await opencodeApi.prompt(')
    expect(regularPromptSource).toContain('worktreePath')
    expect(regularPromptSource).toContain('opencodeSessionId')
    expect(regularPromptSource).toContain('parts')
    expect(regularPromptSource).toContain('requestModel')
    expect(regularPromptSource).toContain('codexPromptOptions')
    expect(regularPromptSource).not.toContain('window.opencodeOps.prompt')
  })

  it('routes session regular prompt draft clear through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const queuedStart = source.indexOf('// If already streaming, this is a queued follow-up')
    const queuedBranchStart = source.indexOf('} else {', queuedStart)
    const queuedBranchEnd = source.indexOf('return', queuedBranchStart)
    const regularCleanupStart = source.indexOf("setInputValue('')", queuedBranchEnd)
    const resetScrollStart = source.indexOf('resetAutoScrollState()', regularCleanupStart)
    const draftClearSource = source.slice(regularCleanupStart, resetScrollStart)

    expect(queuedStart).toBeGreaterThan(-1)
    expect(queuedBranchEnd).toBeGreaterThan(queuedBranchStart)
    expect(regularCleanupStart).toBeGreaterThan(queuedBranchEnd)
    expect(resetScrollStart).toBeGreaterThan(regularCleanupStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(draftClearSource).toContain('setInputValue')
    expect(draftClearSource).toContain('inputValueRef.current =')
    expect(draftClearSource).toContain('fileMentions.clearMentions()')
    expect(draftClearSource).toContain(
      'if (draftTimerRef.current) clearTimeout(draftTimerRef.current)'
    )
    expect(draftClearSource).toContain('dbApi.session.updateDraft(sessionId, null)')
    expect(draftClearSource).not.toContain('db.session.updateDraft(sessionId, null)')
  })

  it('routes session Enter-key plan rejection draft clear through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const enterStart = source.indexOf("if (e.key === 'Enter' && !e.shiftKey)")
    const sendStart = source.indexOf('handleSend()', enterStart)
    const enterPlanSource = source.slice(enterStart, sendStart)

    expect(enterStart).toBeGreaterThan(-1)
    expect(sendStart).toBeGreaterThan(enterStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(enterPlanSource).toContain('// When a plan is pending, sending text rejects the plan')
    expect(enterPlanSource).toContain(
      'const plan = useSessionStore.getState().pendingPlans.get(sessionId)'
    )
    expect(enterPlanSource).toContain('void handlePlanReject(inputValue.trim())')
    expect(enterPlanSource).toContain("setInputValue('')")
    expect(enterPlanSource).toContain('inputValueRef.current =')
    expect(enterPlanSource).toContain(
      'if (draftTimerRef.current) clearTimeout(draftTimerRef.current)'
    )
    expect(enterPlanSource).toContain('dbApi.session.updateDraft(sessionId, null)')
    expect(enterPlanSource).not.toContain('db.session.updateDraft(sessionId, null)')
  })

  it('routes session debounced draft save through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const debounceStart = source.indexOf('// Debounce draft persistence (3 seconds)')
    const callbackEnd = source.indexOf('}, 3000)', debounceStart)
    const debounceSource = source.slice(debounceStart, callbackEnd)

    expect(debounceStart).toBeGreaterThan(-1)
    expect(callbackEnd).toBeGreaterThan(debounceStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(debounceSource).toContain(
      'if (draftTimerRef.current) clearTimeout(draftTimerRef.current)'
    )
    expect(debounceSource).toContain('draftTimerRef.current = setTimeout(() => {')
    expect(debounceSource).toContain('dbApi.session.updateDraft(sessionId, value || null)')
    expect(debounceSource).not.toContain('db.session.updateDraft(sessionId, value || null)')
  })

  it('routes session send-button plan rejection draft clear through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const clickStart = source.indexOf('onClick={() => {')
    const pendingPlanStart = source.indexOf('if (pendingPlan && inputValue.trim())', clickStart)
    const sendStart = source.indexOf('void handleSend()', pendingPlanStart)
    const buttonPlanSource = source.slice(pendingPlanStart, sendStart)

    expect(clickStart).toBeGreaterThan(-1)
    expect(pendingPlanStart).toBeGreaterThan(clickStart)
    expect(sendStart).toBeGreaterThan(pendingPlanStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(buttonPlanSource).toContain('void handlePlanReject(inputValue.trim())')
    expect(buttonPlanSource).toContain("setInputValue('')")
    expect(buttonPlanSource).toContain('inputValueRef.current =')
    expect(buttonPlanSource).toContain(
      'if (draftTimerRef.current) clearTimeout(draftTimerRef.current)'
    )
    expect(buttonPlanSource).toContain('dbApi.session.updateDraft(sessionId, null)')
    expect(buttonPlanSource).not.toContain('db.session.updateDraft(sessionId, null)')
  })

  it('routes session tab refresh-from-file through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionTabs.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleRefreshFromFile = async (sessionId: string)')
    const nextHandlerStart = source.indexOf('const handleSessionTabClick =', handlerStart)
    const handlerSource = source.slice(handlerStart, nextHandlerStart)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(nextHandlerStart).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(handlerSource).toContain(
      'await opencodeApi.refreshFromThread(worktreePath, opencodeSessionId)'
    )
    expect(handlerSource).not.toContain('unwrapEnvelopeApi(() => window.opencodeOps)')
  })

  it('routes session tab Hive JSON import file picking through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionTabs.tsx'),
      'utf-8'
    )
    const importStart = source.indexOf('const result = await kanbanApi.board.openImportFile<{')
    const separatorStart = source.indexOf('<DropdownMenuSeparator />', importStart)
    const importSource = source.slice(importStart, separatorStart)

    expect(importStart).toBeGreaterThan(-1)
    expect(separatorStart).toBeGreaterThan(importStart)
    expect(source).toContain("import { kanbanApi } from '@/api/kanban-api'")
    expect(source).not.toContain('const kanban = unwrapEnvelopeApi(() => window.kanban)')
    expect(importSource).toContain('const result = await kanbanApi.board.openImportFile<')
    expect(importSource).toContain('setHiveImportTickets(result.tickets)')
    expect(importSource).toContain('setHiveImportDependencies(result.dependencies ?? [])')
    expect(importSource).toContain('setShowHiveImport(true)')
    expect(importSource).not.toContain('kanban.board.openImportFile')
  })

  it('removes the old kanban:board:openImportFile IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/kanban.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')
    const serverFunctionStart = serverSource.indexOf('openBoardImportFile: () =>')
    const serverFunctionEnd = serverSource.indexOf('  exportBoard:', serverFunctionStart)
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(preloadSource).not.toContain("'kanban:board:openImportFile'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:board:openImportFile'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:board:openImportFile")
    expect(kanbanHandlersSource).not.toContain('showOpenDialog')
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(serverFunctionSource).toContain('requestKanbanOpenBoardImportFileDialog()')
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('showOpenDialog')
  })

  it('routes session tab board export through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionTabs.tsx'),
      'utf-8'
    )
    const exportStart = source.indexOf('const result = await kanbanApi.board.export<{')
    const endStart = source.indexOf('Export Board', exportStart)
    const exportSource = source.slice(exportStart, endStart)

    expect(exportStart).toBeGreaterThan(-1)
    expect(endStart).toBeGreaterThan(exportStart)
    expect(source).toContain("import { kanbanApi } from '@/api/kanban-api'")
    expect(source).not.toContain("import { unwrapEnvelopeApi } from '@/lib/ipc-envelope'")
    expect(source).not.toContain('const kanban = unwrapEnvelopeApi(() => window.kanban)')
    expect(exportSource).toContain('const result = await kanbanApi.board.export<')
    expect(exportSource).toContain('>(project.id, project.name)')
    expect(exportSource).toContain('toast.success(`Exported ${result.ticketCount} tickets`)')
    expect(exportSource).not.toContain('kanban.board.export')
  })

  it('removes the old kanban:board:export IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/kanban.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')
    const serverFunctionStart = serverSource.indexOf('exportBoard: (projectId, projectName) =>')
    const serverFunctionEnd = serverSource.indexOf('  importBoardTickets:', serverFunctionStart)
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(preloadSource).not.toContain("'kanban:board:export'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:board:export'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:board:export")
    expect(kanbanHandlersSource).not.toContain('showSaveDialog')
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(serverFunctionSource).toContain('requestKanbanSaveBoardExportDialog(projectName)')
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('showSaveDialog')
  })

  it('routes recent worktree lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useRecentStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain(
      'const recentWorktrees = await dbApi.worktree.getRecentlyActive(cutoff)'
    )
    expect(source).not.toContain(
      'const recentWorktrees = await db.worktree.getRecentlyActive(cutoff)'
    )
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
  })

  it('routes Telegram handoff worktree lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useTelegramStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const worktree = await dbApi.worktree.get(payload.worktreeId)')
    expect(source).not.toContain('const worktree = await db.worktree.get(payload.worktreeId)')
  })

  it('routes Telegram handoff session lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useTelegramStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain(
      'const session = await dbApi.session.get<TelegramHandoffSession>(payload.sessionId)'
    )
    expect(source).not.toContain('const session = await db.session.get(payload.sessionId)')
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
  })

  it('routes Telegram handoff abort cleanup through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useTelegramStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(source).toContain(
      'await opencodeApi.abort(worktree.path, session.opencode_session_id).catch(() => {})'
    )
    expect(source).not.toContain(
      'await window.opencodeOps.abort(worktree.path, session.opencode_session_id).catch(() => {})'
    )
  })

  it('routes Telegram connection handoff abort cleanup through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useTelegramStore.ts'),
      'utf-8'
    )
    const connectionStart = source.indexOf('if (payload.connectionId) {')
    const worktreeStart = source.indexOf(
      'if (!session.project_id || !payload.worktreeId)',
      connectionStart
    )
    const connectionSource = source.slice(connectionStart, worktreeStart)

    expect(connectionStart).toBeGreaterThan(-1)
    expect(worktreeStart).toBeGreaterThan(connectionStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(connectionSource).toContain(
      'await opencodeApi.abort(connection.path, session.opencode_session_id).catch(() => {})'
    )
    expect(connectionSource).not.toContain('window.opencodeOps')
  })

  it('routes Telegram connection handoff lookup through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useTelegramStore.ts'),
      'utf-8'
    )
    const connectionStart = source.indexOf('if (payload.connectionId) {')
    const worktreeStart = source.indexOf(
      'if (!session.project_id || !payload.worktreeId)',
      connectionStart
    )
    const connectionSource = source.slice(connectionStart, worktreeStart)

    expect(connectionStart).toBeGreaterThan(-1)
    expect(worktreeStart).toBeGreaterThan(connectionStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(connectionSource).toContain(
      'const connectionResult = await connectionApi.get(payload.connectionId)'
    )
    expect(connectionSource).not.toContain('window.connectionOps.get')
  })

  it('routes OpenCode prompt-dispatch session lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useOpenCodeGlobalListener.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain(
      'const dbSession = await dbApi.session.get<PromptDispatchDbSession>(sessionId)'
    )
    expect(source).not.toContain('const dbSession = (await db.session.get(sessionId))')
    expect(source).not.toContain('await db.session.get(sessionId)')
  })

  it('routes OpenCode prompt-dispatch worktree lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useOpenCodeGlobalListener.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const dbWorktree = await dbApi.worktree.get(dbSession.worktree_id)')
    expect(source).not.toContain(
      'const dbWorktree = (await db.worktree.get(dbSession.worktree_id))'
    )
    expect(source).not.toContain('await db.worktree.get(dbSession.worktree_id)')
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
  })

  it('routes OpenCode prompt-dispatch connection lookup through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useOpenCodeGlobalListener.ts'),
      'utf-8'
    )
    const resolveStart = source.indexOf('async function resolvePromptDispatchContext(')
    const markCompletedStart = source.indexOf(
      'function markBackgroundSessionCompleted(sessionId: string)',
      resolveStart
    )
    const resolveSource = source.slice(resolveStart, markCompletedStart)

    expect(resolveStart).toBeGreaterThan(-1)
    expect(markCompletedStart).toBeGreaterThan(resolveStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(resolveSource).toContain(
      'const connectionResult = await connectionApi.get(dbSession.connection_id)'
    )
    expect(resolveSource).not.toContain('window.connectionOps?.get')
    expect(resolveSource).not.toContain('window.connectionOps.get')
  })

  it('routes OpenCode branch-renamed subscription through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useOpenCodeGlobalListener.ts'),
      'utf-8'
    )
    const effectStart = source.indexOf(
      'const unsubscribe = worktreeApi.onBranchRenamed((data) => {'
    )
    const effectEnd = source.indexOf('return unsubscribe', effectStart)
    const subscriptionSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(subscriptionSource).toContain('const { worktreeId, newBranch, worktreePath } = data')
    expect(subscriptionSource).toContain(
      'useWorktreeStore.getState().updateWorktreeBranch(worktreeId, newBranch)'
    )
    expect(subscriptionSource).not.toContain('window.worktreeOps')
  })

  it('removes the old worktree:branchRenamed IPC event bridge', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const worktreeEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/worktree-events.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.on('worktree:branchRenamed'")
    expect(preloadSource).not.toContain("ipcRenderer.removeListener('worktree:branchRenamed'")
    expect(worktreeEventsSource).not.toContain('webContents.send(WORKTREE_BRANCH_RENAMED_CHANNEL')
    expect(worktreeEventsSource).toContain(
      'publishWorktreeEvent(WORKTREE_BRANCH_RENAMED_CHANNEL, payload)'
    )
    expect(worktreeEventsSource).toContain('publishDesktopBackendEvent(channel, payload)')
  })

  it('removes the old empty worktreeOps preload global exposure', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const worktreeOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('worktreeOps'")
    expect(preloadSource).not.toContain('window.worktreeOps = worktreeOps')
    expect(preloadTypesSource).not.toContain('worktreeOps:')
    expect(preloadTypesSource).not.toContain('hasCommits: (projectPath: string)')
    expect(preloadTypesSource).not.toContain(
      'create: (params: { projectId: string; projectPath: string; projectName: string })'
    )
    expect(preloadTypesSource).not.toContain('onBranchRenamed: (')
    expect(preloadTypesSource).not.toContain('updateContext: (')
  })

  it('removes the old empty worktree IPC handler registrar after worktree RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')

    expect(fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/worktree-handlers.ts'))).toBe(
      false
    )
    expect(mainSource).not.toContain('registerWorktreeHandlers')
    expect(ipcIndexSource).not.toContain('registerWorktreeHandlers')
    expect(ipcIndexSource).not.toContain('./worktree-handlers')
  })

  it('routes OpenCode global stream subscription through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useOpenCodeGlobalListener.ts'),
      'utf-8'
    )
    const effectStart = source.indexOf('const unsubscribe = opencodeApi.onStream((event) => {')
    const effectEnd = source.indexOf('return unsubscribe', effectStart)
    const subscriptionSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(subscriptionSource).toContain('const sessionId = event.sessionId')
    expect(subscriptionSource).toContain("event.type === 'session.materialized'")
    expect(subscriptionSource).not.toContain('window.opencodeOps')
  })

  it('routes OpenCode idle-follow-up prompt dispatch through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useOpenCodeGlobalListener.ts'),
      'utf-8'
    )
    const followUpStart = source.indexOf('void handleSessionIdleFollowUp({')
    const dispatchStart = source.indexOf('dispatchFollowUp: async (message) => {', followUpStart)
    const dispatchEnd = source.indexOf('onDispatchFailure:', dispatchStart)
    const dispatchSource = source.slice(dispatchStart, dispatchEnd)

    expect(followUpStart).toBeGreaterThan(-1)
    expect(dispatchStart).toBeGreaterThan(followUpStart)
    expect(dispatchEnd).toBeGreaterThan(dispatchStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(dispatchSource).toContain(
      'const context = await resolvePromptDispatchContext(sessionId)'
    )
    expect(dispatchSource).toContain('if (!context) return false')
    expect(dispatchSource).toContain("context.opencodeSessionId.startsWith('pending::')")
    expect(dispatchSource).toContain(
      'await opencodeApi.prompt(context.worktreePath, context.opencodeSessionId, ['
    )
    expect(dispatchSource).toContain("{ type: 'text', text: message }")
    expect(dispatchSource).toContain('return result.success')
    expect(dispatchSource).not.toContain('window.opencodeOps?.prompt')
    expect(dispatchSource).not.toContain('window.opencodeOps.prompt')
  })

  it('routes OpenCode background permission auto-approve replies through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useOpenCodeGlobalListener.ts'),
      'utf-8'
    )
    const permissionAskedStart = source.indexOf("if (event.type === 'permission.asked')")
    const permissionRepliedStart = source.indexOf(
      "if (event.type === 'permission.replied')",
      permissionAskedStart
    )
    const permissionAskedSource = source.slice(permissionAskedStart, permissionRepliedStart)

    expect(permissionAskedStart).toBeGreaterThan(-1)
    expect(permissionRepliedStart).toBeGreaterThan(permissionAskedStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(permissionAskedSource).toContain('opencodeApi.permissionReply(')
    expect(permissionAskedSource).toContain("request.id, 'once', undefined")
    expect(permissionAskedSource).toContain(
      "console.warn('Auto-approve permissionReply (background) failed:', err)"
    )
    expect(permissionAskedSource).not.toContain('window.opencodeOps')
  })

  it('routes OpenCode background command approval auto-approve replies through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useOpenCodeGlobalListener.ts'),
      'utf-8'
    )
    const approvalStart = source.indexOf("if (event.type === 'command.approval_needed')")
    const approvalRepliedStart = source.indexOf(
      "if (event.type === 'command.approval_replied')",
      approvalStart
    )
    const approvalSource = source.slice(approvalStart, approvalRepliedStart)

    expect(approvalStart).toBeGreaterThan(-1)
    expect(approvalRepliedStart).toBeGreaterThan(approvalStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(approvalSource).toContain('opencodeApi.commandApprovalReply(')
    expect(approvalSource).toContain('request.id, true')
    expect(approvalSource).toContain(
      "console.warn('Auto-approve commandApprovalReply (background) failed:', err)"
    )
    expect(approvalSource).not.toContain('window.opencodeOps')
  })

  it('routes background session prompt connect through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/lib/backgroundSessionStart.ts'),
      'utf-8'
    )
    const start = source.indexOf('export async function startBackgroundSessionPrompt(')
    const dispatchStart = source.indexOf('const result = unwrapEnvelope(', start)
    const connectSource = source.slice(start, dispatchStart)

    expect(start).toBeGreaterThan(-1)
    expect(dispatchStart).toBeGreaterThan(start)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).not.toContain(
      "import { unwrapEnvelope, unwrapEnvelopeApi } from '@/lib/ipc-envelope'"
    )
    expect(source).not.toContain('const db = unwrapEnvelopeApi(() => window.db)')
    expect(connectSource).toContain(
      'const connectResult = unwrapEnvelope(await opencodeApi.connect(opts.worktreePath, opts.sessionId))'
    )
    expect(connectSource).toContain('!connectResult.success || !connectResult.sessionId')
    expect(connectSource).toContain(
      'useSessionStore.getState().setOpenCodeSessionId(opts.sessionId, connectResult.sessionId)'
    )
    expect(connectSource).toContain('await dbApi.session.update(opts.sessionId, {')
    expect(connectSource).toContain('opencode_session_id: connectResult.sessionId')
    expect(connectSource).not.toContain('await db.session.update(opts.sessionId, {')
    expect(connectSource).not.toContain('window.opencodeOps.connect')
  })

  it('routes background session prompt dispatch through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/lib/backgroundSessionStart.ts'),
      'utf-8'
    )
    const dispatchStart = source.indexOf('const result = unwrapEnvelope(')
    const endStart = source.indexOf('if (!result.success)', dispatchStart)
    const dispatchSource = source.slice(dispatchStart, endStart)

    expect(dispatchStart).toBeGreaterThan(-1)
    expect(endStart).toBeGreaterThan(dispatchStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(dispatchSource).toContain('await opencodeApi.prompt(')
    expect(dispatchSource).toContain('opts.worktreePath')
    expect(dispatchSource).toContain('connectResult.sessionId')
    expect(dispatchSource).toContain("[{ type: 'text', text: opts.prompt }]")
    expect(dispatchSource).toContain('model')
    expect(dispatchSource).not.toContain('window.opencodeOps.prompt')
  })

  it('routes auto-launch OpenCode connect through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/lib/auto-launch.ts'),
      'utf-8'
    )
    const connectStart = source.indexOf('// 8. Connect to OpenCode and send prompt')
    const promptStart = source.indexOf('// 9. Send prompt', connectStart)
    const connectSource = source.slice(connectStart, promptStart)

    expect(connectStart).toBeGreaterThan(-1)
    expect(promptStart).toBeGreaterThan(connectStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).not.toContain(
      "import { unwrapEnvelope, unwrapEnvelopeApi } from '@/lib/ipc-envelope'"
    )
    expect(source).not.toContain('const db = unwrapEnvelopeApi(() => window.db)')
    expect(connectSource).toContain(
      'const connectResult = unwrapEnvelope(await opencodeApi.connect(worktree.path, sessionId))'
    )
    expect(connectSource).toContain('!connectResult.success || !connectResult.sessionId')
    expect(connectSource).toContain(
      'useSessionStore.getState().setOpenCodeSessionId(sessionId, connectResult.sessionId)'
    )
    expect(connectSource).toContain(
      'await dbApi.session.update(sessionId, { opencode_session_id: connectResult.sessionId })'
    )
    expect(connectSource).not.toContain('await db.session.update(sessionId, {')
    expect(connectSource).not.toContain('window.opencodeOps.connect')
  })

  it('routes auto-launch Claude CLI terminal creation through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/lib/auto-launch.ts'),
      'utf-8'
    )
    const cliStart = source.indexOf("if (sessionAgentSdk === 'claude-code-cli') {")
    const openCodeStart = source.indexOf('// 8. Connect to OpenCode and send prompt', cliStart)
    const cliSource = source.slice(cliStart, openCodeStart)

    expect(cliStart).toBeGreaterThan(-1)
    expect(openCodeStart).toBeGreaterThan(cliStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(cliSource).toContain('bumpWorktreeLastMessage({ worktreeId })')
    expect(cliSource).toContain('await terminalApi.createClaudeCli(sessionId, {')
    expect(cliSource).toContain('pendingPrompt: outboundPrompt')
    expect(cliSource).toContain('unwrapEnvelope(')
    expect(cliSource).toContain('useSessionStore.getState().dequeuePendingMessage(sessionId)')
    expect(cliSource).not.toContain('window.terminalOps.createClaudeCli')
  })

  it('keeps auto-launch Claude CLI tests off direct legacy window API assertions', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/lib/auto-launch.claude-cli.test.ts'),
      'utf-8'
    )

    expect(source).toContain('setRendererRpcClient({ request, subscribe: vi.fn() })')
    expect(source).toContain("vi.mock('@/api/opencode-api'")
    expect(source).toContain("expect(request).toHaveBeenCalledWith('terminalOps.createClaudeCli'")
    expect(source).toContain("expect(request).toHaveBeenCalledWith('db.session.update'")
    expect(source).not.toContain('setupWindowApis')
    expect(source).not.toContain('legacyDb')
    expect(source).not.toContain("Object.defineProperty(window, 'terminalOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain('window.terminalOps.createClaudeCli')
    expect(source).not.toContain('window.opencodeOps.connect')
    expect(source).not.toContain('window.opencodeOps.prompt')
  })

  it('routes auto-launch OpenCode prompt dispatch through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/lib/auto-launch.ts'),
      'utf-8'
    )
    const promptStart = source.indexOf('// 9. Send prompt')
    const catchStart = source.indexOf('} catch (err) {', promptStart)
    const promptSource = source.slice(promptStart, catchStart)

    expect(promptStart).toBeGreaterThan(-1)
    expect(catchStart).toBeGreaterThan(promptStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(promptSource).toContain('await opencodeApi.prompt(')
    expect(promptSource).toContain('worktree.path')
    expect(promptSource).toContain('connectResult.sessionId')
    expect(promptSource).toContain("[{ type: 'text', text: outboundPrompt }]")
    expect(promptSource).toContain('effectiveModel')
    expect(promptSource).toContain('promptOptions')
    expect(promptSource).toContain('bumpWorktreeLastMessage({ worktreeId })')
    expect(promptSource).not.toContain('window.opencodeOps.prompt')
  })

  it('keeps worktree status store free of the window.db compatibility global', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useWorktreeStatusStore.ts'),
      'utf-8'
    )

    expect(source).not.toContain("import { unwrapEnvelopeApi } from '@/lib/ipc-envelope'")
    expect(source).not.toContain('const db = unwrapEnvelopeApi(() => window.db)')
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain(
      'dbApi.worktree.update(worktreeId, { last_message_at: next }).catch(() => {})'
    )
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('db.')
  })

  it('routes worktree picker connection-mode OpenCode connect through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/WorktreePickerModal.tsx'),
      'utf-8'
    )
    const connectionStart = source.indexOf('if (isConnectionMode && connectionId) {')
    const doneStart = source.indexOf('return // Done with connection path', connectionStart)
    const connectionSource = source.slice(connectionStart, doneStart)

    expect(connectionStart).toBeGreaterThan(-1)
    expect(doneStart).toBeGreaterThan(connectionStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(connectionSource).toContain(
      'const connectResult = unwrapEnvelope(await opencodeApi.connect(connectionPath, sessionId))'
    )
    expect(connectionSource).toContain('!connectResult.success || !connectResult.sessionId')
    expect(connectionSource).toContain(
      'useSessionStore.getState().setOpenCodeSessionId(sessionId, connectResult.sessionId)'
    )
    expect(connectionSource).toContain('await dbApi.session.update<Session>(sessionId, {')
    expect(connectionSource).toContain('opencode_session_id: connectResult.sessionId')
    expect(connectionSource).not.toContain(
      'await db.session.update(sessionId, { opencode_session_id: connectResult.sessionId })'
    )
    expect(connectionSource).toContain('bumpWorktreeLastMessage({ connectionId })')
    expect(connectionSource).toContain('connectionPath')
    expect(connectionSource).not.toContain('window.opencodeOps.connect')
  })

  it('routes worktree picker connection-mode Claude CLI terminal creation through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/WorktreePickerModal.tsx'),
      'utf-8'
    )
    const connectionStart = source.indexOf('if (isConnectionMode && connectionId) {')
    const cliStart = source.indexOf("if (sessionAgentSdk === 'claude-code-cli') {", connectionStart)
    const openCodeStart = source.indexOf('// Connect to opencode using connection path', cliStart)
    const cliSource = source.slice(cliStart, openCodeStart)

    expect(connectionStart).toBeGreaterThan(-1)
    expect(cliStart).toBeGreaterThan(connectionStart)
    expect(openCodeStart).toBeGreaterThan(cliStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(cliSource).toContain('bumpWorktreeLastMessage({ connectionId })')
    expect(cliSource).toContain('await terminalApi.createClaudeCli(sessionId, {')
    expect(cliSource).toContain('pendingPrompt: outboundPrompt')
    expect(cliSource).toContain('unwrapEnvelope(')
    expect(cliSource).toContain('useSessionStore.getState().dequeuePendingMessage(sessionId)')
    expect(cliSource).not.toContain('window.terminalOps.createClaudeCli')
  })

  it('routes worktree picker connection-mode OpenCode prompt through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/WorktreePickerModal.tsx'),
      'utf-8'
    )
    const connectionStart = source.indexOf('if (isConnectionMode && connectionId) {')
    const sendPromptStart = source.indexOf('// Send prompt', connectionStart)
    const doneStart = source.indexOf('return // Done with connection path', sendPromptStart)
    const promptSource = source.slice(sendPromptStart, doneStart)

    expect(connectionStart).toBeGreaterThan(-1)
    expect(sendPromptStart).toBeGreaterThan(connectionStart)
    expect(doneStart).toBeGreaterThan(sendPromptStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(promptSource).toContain('await opencodeApi.prompt(')
    expect(promptSource).toContain('connectionPath')
    expect(promptSource).toContain('connectResult.sessionId')
    expect(promptSource).toContain("[{ type: 'text', text: outboundPrompt }]")
    expect(promptSource).toContain('effectiveModel')
    expect(promptSource).toContain('promptOptions')
    expect(promptSource).toContain('bumpWorktreeLastMessage({ connectionId })')
    expect(promptSource).not.toContain('window.opencodeOps.prompt')
  })

  it('routes worktree picker worktree-mode OpenCode connect through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/WorktreePickerModal.tsx'),
      'utf-8'
    )
    const worktreeStart = source.indexOf('// ── Start the OpenCode session in the background')
    const promptStart = source.indexOf('// Send the prompt', worktreeStart)
    const worktreeSource = source.slice(worktreeStart, promptStart)

    expect(worktreeStart).toBeGreaterThan(-1)
    expect(promptStart).toBeGreaterThan(worktreeStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(worktreeSource).toContain(
      'const connectResult = unwrapEnvelope(await opencodeApi.connect(worktree.path, sessionId))'
    )
    expect(worktreeSource).toContain('!connectResult.success || !connectResult.sessionId')
    expect(worktreeSource).toContain(
      'useSessionStore.getState().setOpenCodeSessionId(sessionId, connectResult.sessionId)'
    )
    expect(worktreeSource).toContain('await dbApi.session.update<Session>(sessionId, {')
    expect(worktreeSource).toContain('opencode_session_id: connectResult.sessionId')
    expect(worktreeSource).not.toContain('await db.session.update(sessionId, {')
    // The store-based worktree lookup is hoisted above session creation so the
    // goal plan-file conversion can reuse it
    expect(source).toContain('const worktree = allWorktrees.find((w) => w.id === worktreeId)')
    expect(worktreeSource).not.toContain('window.opencodeOps.connect')
  })

  it('routes worktree picker worktree-mode Claude CLI terminal creation through terminalApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/WorktreePickerModal.tsx'),
      'utf-8'
    )
    const worktreeBranchStart = source.indexOf('// Close modal immediately')
    const cliStart = source.indexOf(
      "if (sessionAgentSdk === 'claude-code-cli') {",
      worktreeBranchStart
    )
    const openCodeStart = source.indexOf(
      '// ── Start the OpenCode session in the background',
      cliStart
    )
    const cliSource = source.slice(cliStart, openCodeStart)

    expect(worktreeBranchStart).toBeGreaterThan(-1)
    expect(cliStart).toBeGreaterThan(worktreeBranchStart)
    expect(openCodeStart).toBeGreaterThan(cliStart)
    expect(source).toContain("import { terminalApi } from '@/api/terminal-api'")
    expect(cliSource).toContain('bumpWorktreeLastMessage({ worktreeId })')
    expect(cliSource).toContain('await terminalApi.createClaudeCli(sessionId, {')
    expect(cliSource).toContain('pendingPrompt: outboundPrompt')
    expect(cliSource).toContain('unwrapEnvelope(')
    expect(cliSource).toContain('useSessionStore.getState().dequeuePendingMessage(sessionId)')
    expect(cliSource).not.toContain('window.terminalOps.createClaudeCli')
  })

  it('keeps WorktreePickerModal Claude CLI tests off direct legacy window API assertions', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/components/kanban/WorktreePickerModal.claude-cli.test.tsx'
      ),
      'utf-8'
    )

    expect(source).toContain('setRendererRpcClient({ request, subscribe: vi.fn() })')
    expect(source).toContain("expect(request).toHaveBeenCalledWith('terminalOps.createClaudeCli'")
    expect(source).not.toContain('setupWindowApis')
    expect(source).not.toContain('legacyApis')
    expect(source).not.toContain("Object.defineProperty(window, 'terminalOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'opencodeOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'gitOps'")
    expect(source).not.toContain('...window.gitOps')
    expect(source).not.toContain('window.gitOps')
    expect(source).not.toContain('window.terminalOps.createClaudeCli')
    expect(source).not.toContain('window.opencodeOps.connect')
    expect(source).not.toContain('window.opencodeOps.prompt')
  })

  it('routes worktree picker worktree-mode OpenCode prompt through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/WorktreePickerModal.tsx'),
      'utf-8'
    )
    const worktreeStart = source.indexOf('// ── Start the OpenCode session in the background')
    const sendPromptStart = source.indexOf('// Send the prompt', worktreeStart)
    const catchStart = source.indexOf('} catch {', sendPromptStart)
    const promptSource = source.slice(sendPromptStart, catchStart)

    expect(worktreeStart).toBeGreaterThan(-1)
    expect(sendPromptStart).toBeGreaterThan(worktreeStart)
    expect(catchStart).toBeGreaterThan(sendPromptStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(promptSource).toContain('await opencodeApi.prompt(')
    expect(promptSource).toContain('worktree.path')
    expect(promptSource).toContain('connectResult.sessionId')
    expect(promptSource).toContain("[{ type: 'text', text: outboundPrompt }]")
    expect(promptSource).toContain('effectiveModel')
    expect(promptSource).toContain('promptOptions')
    expect(promptSource).toContain('bumpWorktreeLastMessage({ worktreeId })')
    expect(promptSource).not.toContain('window.opencodeOps.prompt')
  })

  it('routes ticket run-script worktree fallback through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useTicketRunScript.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('dbApi.worktree')
    expect(source).toContain('.get(ticket.worktree_id)')
    expect(source).not.toContain('db.worktree')
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
  })

  it('routes worktree store active-project loads through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useWorktreeStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain(
      'const worktrees = await dbApi.worktree.getActiveByProject<Worktree>(projectId)'
    )
    expect(source).not.toContain(
      'const worktrees = await db.worktree.getActiveByProject(projectId)'
    )
  })

  it('routes worktree store touch updates through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useWorktreeStore.ts'),
      'utf-8'
    )
    const touchStart = source.indexOf('touchWorktree: async (id: string)')
    const syncStart = source.indexOf('syncWorktrees: async (projectId: string', touchStart)
    const touchSource = source.slice(touchStart, syncStart)

    expect(touchStart).toBeGreaterThan(-1)
    expect(syncStart).toBeGreaterThan(touchStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(touchSource).toContain('await dbApi.worktree.touch(id)')
    expect(touchSource).toContain('last_accessed_at: new Date().toISOString()')
    expect(touchSource).not.toContain('await db.worktree.touch(id)')
  })

  it('routes worktree sync through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useWorktreeStore.ts'),
      'utf-8'
    )
    const syncStart = source.indexOf('syncWorktrees: async (projectId: string')
    const getterStart = source.indexOf('  getWorktreesForProject:', syncStart)
    const syncSource = source.slice(syncStart, getterStart)

    expect(syncStart).toBeGreaterThan(-1)
    expect(getterStart).toBeGreaterThan(syncStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(syncSource).toContain('await worktreeApi.sync({ projectId, projectPath })')
    expect(syncSource).toContain('await get().loadWorktrees(projectId, { force: true })')
    expect(syncSource).toContain('lastSync.set(projectId, Date.now())')
    expect(syncSource).not.toContain('window.worktreeOps.sync')
    expect(syncSource).not.toContain('unwrapEnvelope(await window.worktreeOps.sync')
  })

  it('keeps WorktreeList sync-gating tests on API/RPC mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-13/session-5/worktree-list-sync-gating.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/worktree-api'")
    expect(source).toContain("request).toHaveBeenCalledWith('db.worktree.getActiveByProject'")
    expect(source).toContain('expect(worktreeApi.sync).toHaveBeenCalledWith')
    expect(source).not.toContain('window.worktreeOps')
    expect(source).not.toContain('window.db')
  })

  it('keeps selection propagation tests on API mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-13/session-3/selection-propagation.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain("vi.mock('@/api/worktree-api'")
    expect(source).toContain("vi.mock('@/api/project-api'")
    expect(source).toContain('vi.mocked(dbApi.worktree.touch).mockResolvedValue(true)')
    expect(source).not.toContain('window.projectOps')
    expect(source).not.toContain('window.worktreeOps')
    expect(source).not.toContain('window.db')
  })

  it('keeps refresh-project syncWorktrees tests on worktreeApi instead of window worktree globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-13/session-5/refresh-project.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/worktree-api'")
    expect(source).toContain('expect(worktreeApi.sync).toHaveBeenCalledWith')
    expect(source).toContain("method === 'db.worktree.getActiveByProject'")
    expect(source).not.toContain('window.worktreeOps')
    expect(source).not.toContain('window.projectOps')
  })

  it('routes worktree store session-title persistence through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useWorktreeStore.ts'),
      'utf-8'
    )
    const appendStart = source.indexOf('appendSessionTitle: (worktreeId: string, title: string)')
    const registrationStart = source.indexOf('// Register the worktree-clear callback', appendStart)
    const appendSource = source.slice(appendStart, registrationStart)

    expect(appendStart).toBeGreaterThan(-1)
    expect(registrationStart).toBeGreaterThan(appendStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
    expect(appendSource).toContain('void dbApi.worktree.appendSessionTitle(worktreeId, title)')
    expect(appendSource).not.toContain('db.worktree.appendSessionTitle')
  })

  it('keeps commit-message backend tests on dbApi mocks instead of window db globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-17/session-10/commit-message-backend.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('../../../src/renderer/src/api/db-api'")
    expect(source).toContain(
      'const mockAppendSessionTitle = vi.mocked(dbApi.worktree.appendSessionTitle)'
    )
    expect(source).toContain('await dbApi.worktree.appendSessionTitle(worktreeId, name)')
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('Object.defineProperty(window')
  })

  it('routes git store PR attachment persistence through dbApi and kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useGitStore.ts'),
      'utf-8'
    )
    const attachStart = source.indexOf(
      'attachPR: async (worktreeId: string, prNumber: number, prUrl: string)'
    )
    const detachStart = source.indexOf('detachPR: async (worktreeId: string)', attachStart)
    const attachSource = source.slice(attachStart, detachStart)

    expect(attachStart).toBeGreaterThan(-1)
    expect(detachStart).toBeGreaterThan(attachStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(attachSource).toContain(
      'const result = await dbApi.worktree.attachPR(worktreeId, prNumber, prUrl)'
    )
    expect(source).toContain("import { kanbanApi } from '@/api/kanban-api'")
    expect(attachSource).toContain('await kanbanApi.ticket.syncPR(worktreeId, prNumber, prUrl)')
    expect(attachSource).not.toContain('kanban.ticket.syncPR')
    expect(attachSource).not.toContain('db.worktree.attachPR')
  })

  it('removes the old kanban:ticket:syncPR IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("'kanban:ticket:syncPR'")
    expect(kanbanHandlersSource).not.toContain("'kanban:ticket:syncPR'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:syncPR'")
  })

  it('routes git store file status loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useGitStore.ts'),
      'utf-8'
    )
    const loadStart = source.indexOf(
      'loadFileStatuses: async (worktreePath: string, opts?: { force?: boolean }) => {'
    )
    const branchInfoStart = source.indexOf('// Load branch info for a worktree', loadStart)
    const loadSource = source.slice(loadStart, branchInfoStart)

    expect(loadStart).toBeGreaterThan(-1)
    expect(branchInfoStart).toBeGreaterThan(loadStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(loadSource).toContain('const result = await gitApi.getFileStatuses(worktreePath)')
    expect(loadSource).toContain("error: result.error || 'Failed to load file statuses'")
    expect(loadSource).toContain('const files = result.files!')
    expect(loadSource).toContain("files.some((f) => f.status === 'C')")
    expect(loadSource).toContain('lastFileStatusLoad.set(worktreePath, Date.now())')
    expect(loadSource).not.toContain('window.gitOps.getFileStatuses')
    expect(loadSource).not.toContain('unwrapEnvelope(await')
  })

  it('keeps merge conflict header button tests on gitApi mocks instead of gitOps globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-18/session-7/merge-conflict-button.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/git-api'")
    expect(source).toContain('gitApi: gitApiMocks')
    expect(source).toContain('gitApiMocks.getFileStatuses.mockResolvedValue')
    expect(source).not.toContain('window.gitOps')
    expect(source).not.toContain('mockGitOps')
  })

  it('keeps merge conflict sidebar tests on gitApi mocks instead of gitOps globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-19/session-3/merge-conflicts-sidebar.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/git-api'")
    expect(source).toContain('gitApi: gitApiMocks')
    expect(source).toContain('gitApiMocks.getFileStatuses.mockResolvedValue')
    expect(source).not.toContain('window.gitOps')
    expect(source).not.toContain('mockGitOps')
  })

  it('keeps file-tree git integration tests on explicit API mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-2/session-2/file-tree-git.test.ts'),
      'utf-8'
    )

    expect(source).toContain('interface GitApi')
    expect(source).toContain('interface ProjectApi')
    expect(source).toContain('const mockGitApi: GitApi')
    expect(source).toContain('const mockProjectApi: ProjectApi')
    expect(source).not.toContain('window.gitOps')
    expect(source).not.toContain('window.projectOps')
    expect(source).not.toContain('global.window')
    expect(source).not.toContain('mockGitOps')
    expect(source).not.toContain('mockProjectOps')
  })

  it('removes the old git:fileStatuses IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const gitFileHandlersSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:fileStatuses'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("git:fileStatuses"')
    expect(gitFileHandlersSource).not.toContain("defineHandler('git:fileStatuses'")
    expect(gitFileHandlersSource).not.toContain('defineHandler("git:fileStatuses"')
  })

  it('routes git store branch info loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useGitStore.ts'),
      'utf-8'
    )
    const loadStart = source.indexOf(
      'loadBranchInfo: async (worktreePath: string, opts?: { force?: boolean }) => {'
    )
    const getterStart = source.indexOf('// Get file statuses for a worktree', loadStart)
    const loadSource = source.slice(loadStart, getterStart)

    expect(loadStart).toBeGreaterThan(-1)
    expect(getterStart).toBeGreaterThan(loadStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(loadSource).toContain('const result = await gitApi.getBranchInfo(worktreePath)')
    expect(loadSource).toContain('if (!result.success || !result.branch) {')
    expect(loadSource).toContain('newMap.set(worktreePath, result.branch!)')
    expect(loadSource).toContain('lastBranchInfoLoad.set(worktreePath, Date.now())')
    expect(loadSource).toContain("console.error('Failed to load branch info:', error)")
    expect(loadSource).not.toContain('window.gitOps.getBranchInfo')
    expect(loadSource).not.toContain('unwrapEnvelope(await')
  })

  it('keeps git store metadata loading tests off direct legacy window API composition', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/__tests__/useGitStore.git-loads.test.ts'),
      'utf-8'
    )

    expect(source).toContain("setRendererRpcClient({ request, subscribe: vi.fn() })")
    expect(source).toContain("method === 'gitOps.getBranchInfo'")
    expect(source).toContain("method === 'db.worktree.attachPR'")
    expect(source).toContain("'kanban.ticket.clearPR'")
    expect(source).not.toContain('legacyGitOps')
    expect(source).not.toContain('...window.gitOps')
    expect(source).not.toContain('window.gitOps')
  })

  it('routes git store file staging through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useGitStore.ts'),
      'utf-8'
    )
    const stageStart = source.indexOf(
      'stageFile: async (worktreePath: string, relativePath: string)'
    )
    const unstageStart = source.indexOf(
      'unstageFile: async (worktreePath: string, relativePath: string)',
      stageStart
    )
    const stageSource = source.slice(stageStart, unstageStart)

    expect(stageStart).toBeGreaterThan(-1)
    expect(unstageStart).toBeGreaterThan(stageStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(stageSource).toContain(
      'const result = await gitApi.stageFile(worktreePath, relativePath)'
    )
    expect(stageSource).toContain('return result.success')
    expect(stageSource).toContain("console.error('Failed to stage file:', error)")
    expect(stageSource).toContain('return false')
    expect(stageSource).not.toContain('window.gitOps.stageFile')
    expect(stageSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes git store file unstaging through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useGitStore.ts'),
      'utf-8'
    )
    const unstageStart = source.indexOf(
      'unstageFile: async (worktreePath: string, relativePath: string)'
    )
    const stageAllStart = source.indexOf('stageAll: async (worktreePath: string)', unstageStart)
    const unstageSource = source.slice(unstageStart, stageAllStart)

    expect(unstageStart).toBeGreaterThan(-1)
    expect(stageAllStart).toBeGreaterThan(unstageStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(unstageSource).toContain(
      'const result = await gitApi.unstageFile(worktreePath, relativePath)'
    )
    expect(unstageSource).toContain('return result.success')
    expect(unstageSource).toContain("console.error('Failed to unstage file:', error)")
    expect(unstageSource).toContain('return false')
    expect(unstageSource).not.toContain('window.gitOps.unstageFile')
    expect(unstageSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes git store stage-all through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useGitStore.ts'),
      'utf-8'
    )
    const stageAllStart = source.indexOf('stageAll: async (worktreePath: string)')
    const unstageAllStart = source.indexOf(
      'unstageAll: async (worktreePath: string)',
      stageAllStart
    )
    const stageAllSource = source.slice(stageAllStart, unstageAllStart)

    expect(stageAllStart).toBeGreaterThan(-1)
    expect(unstageAllStart).toBeGreaterThan(stageAllStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(stageAllSource).toContain('const result = await gitApi.stageAll(worktreePath)')
    expect(stageAllSource).toContain('return result.success')
    expect(stageAllSource).toContain("console.error('Failed to stage all files:', error)")
    expect(stageAllSource).toContain('return false')
    expect(stageAllSource).not.toContain('window.gitOps.stageAll')
    expect(stageAllSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes git store unstage-all through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useGitStore.ts'),
      'utf-8'
    )
    const unstageAllStart = source.indexOf('unstageAll: async (worktreePath: string)')
    const discardStart = source.indexOf(
      'discardChanges: async (worktreePath: string, relativePath: string)',
      unstageAllStart
    )
    const unstageAllSource = source.slice(unstageAllStart, discardStart)

    expect(unstageAllStart).toBeGreaterThan(-1)
    expect(discardStart).toBeGreaterThan(unstageAllStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(unstageAllSource).toContain('const result = await gitApi.unstageAll(worktreePath)')
    expect(unstageAllSource).toContain('return result.success')
    expect(unstageAllSource).toContain("console.error('Failed to unstage all files:', error)")
    expect(unstageAllSource).toContain('return false')
    expect(unstageAllSource).not.toContain('window.gitOps.unstageAll')
    expect(unstageAllSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes git store discard-changes through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useGitStore.ts'),
      'utf-8'
    )
    const discardStart = source.indexOf(
      'discardChanges: async (worktreePath: string, relativePath: string)'
    )
    const gitignoreStart = source.indexOf(
      'addToGitignore: async (worktreePath: string, pattern: string)',
      discardStart
    )
    const discardSource = source.slice(discardStart, gitignoreStart)

    expect(discardStart).toBeGreaterThan(-1)
    expect(gitignoreStart).toBeGreaterThan(discardStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(discardSource).toContain('await gitApi.discardChanges(worktreePath, relativePath)')
    expect(discardSource).toContain('return true')
    expect(discardSource).toContain("console.error('Failed to discard changes:', error)")
    expect(discardSource).toContain('return false')
    expect(discardSource).not.toContain('window.gitOps.discardChanges')
    expect(discardSource).not.toContain('unwrapEnvelope(await')
  })

  it('removes the old git:discardChanges IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const gitFileHandlersSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:discardChanges'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("git:discardChanges"')
    expect(gitFileHandlersSource).not.toContain("defineHandler('git:discardChanges'")
    expect(gitFileHandlersSource).not.toContain('defineHandler("git:discardChanges"')
  })

  it('routes git store add-to-gitignore through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useGitStore.ts'),
      'utf-8'
    )
    const gitignoreStart = source.indexOf(
      'addToGitignore: async (worktreePath: string, pattern: string)'
    )
    const refreshStart = source.indexOf(
      'refreshStatuses: async (worktreePath: string)',
      gitignoreStart
    )
    const gitignoreSource = source.slice(gitignoreStart, refreshStart)

    expect(gitignoreStart).toBeGreaterThan(-1)
    expect(refreshStart).toBeGreaterThan(gitignoreStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(gitignoreSource).toContain(
      'const result = await gitApi.addToGitignore(worktreePath, pattern)'
    )
    expect(gitignoreSource).toContain('return result.success')
    expect(gitignoreSource).toContain("console.error('Failed to add to .gitignore:', error)")
    expect(gitignoreSource).toContain('return false')
    expect(gitignoreSource).not.toContain('window.gitOps.addToGitignore')
    expect(gitignoreSource).not.toContain('unwrapEnvelope(await')
  })

  it('removes the old git:addToGitignore IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const gitFileHandlersSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:addToGitignore'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("git:addToGitignore"')
    expect(gitFileHandlersSource).not.toContain("defineHandler('git:addToGitignore'")
    expect(gitFileHandlersSource).not.toContain('defineHandler("git:addToGitignore"')
  })

  it('routes git store remote info check through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useGitStore.ts'),
      'utf-8'
    )
    const remoteInfoStart = source.indexOf(
      'checkRemoteInfo: async (worktreeId: string, worktreePath: string)'
    )
    const attachPrStart = source.indexOf('setAttachedPR: (worktreeId: string', remoteInfoStart)
    const remoteInfoSource = source.slice(remoteInfoStart, attachPrStart)

    expect(remoteInfoStart).toBeGreaterThan(-1)
    expect(attachPrStart).toBeGreaterThan(remoteInfoStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(remoteInfoSource).toContain('const result = await gitApi.getRemoteUrl(worktreePath)')
    expect(remoteInfoSource).toContain('hasRemote: !!result.url')
    expect(remoteInfoSource).toContain("isGitHub: result.url?.includes('github.com') ?? false")
    expect(remoteInfoSource).toContain('url: result.url ?? null')
    expect(remoteInfoSource).toContain(
      'newRemoteInfo.set(worktreeId, { hasRemote: false, isGitHub: false, url: null })'
    )
    expect(remoteInfoSource).not.toContain('window.gitOps.getRemoteUrl')
    expect(remoteInfoSource).not.toContain('unwrapEnvelope(await')
  })

  it('keeps PR GitHub backend remote-info tests on gitApi mocks instead of gitOps globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-18/session-10/pr-github-backend.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/git-api'")
    expect(source).toContain('gitApi: gitApiMocks')
    expect(source).toContain('gitApiMocks.getRemoteUrl.mockResolvedValue')
    expect(source).not.toContain('window.gitOps')
    expect(source).not.toContain('...window.gitOps')
    expect(source).not.toContain("Object.defineProperty(window, 'gitOps'")
  })

  it('routes git store commit through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useGitStore.ts'),
      'utf-8'
    )
    const commitStart = source.indexOf('commit: async (worktreePath: string, message: string)')
    const pushStart = source.indexOf(
      'push: async (worktreePath: string, remote?: string, branch?: string, force?: boolean)',
      commitStart
    )
    const commitSource = source.slice(commitStart, pushStart)

    expect(commitStart).toBeGreaterThan(-1)
    expect(pushStart).toBeGreaterThan(commitStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(commitSource).toContain('const result = await gitApi.commit(worktreePath, message)')
    expect(commitSource).toContain('await get().refreshStatuses(worktreePath)')
    expect(commitSource).toContain(
      'get().setDefaultMergeBranch(worktree.project_id, branchInfo.name)'
    )
    expect(commitSource).toContain(
      'set((state) => ({ mergeSelectionVersion: state.mergeSelectionVersion + 1 }))'
    )
    expect(commitSource).toContain(
      "const errMessage = error instanceof Error ? error.message : 'Failed to commit'"
    )
    expect(commitSource).toContain('return { success: false, error: errMessage }')
    expect(commitSource).toContain('set({ isCommitting: false })')
    expect(commitSource).not.toContain('window.gitOps.commit')
    expect(commitSource).not.toContain('unwrapEnvelope(await')
  })

  it('keeps cross-worktree merge default tests on gitApi mocks instead of gitOps globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-19/session-4/cross-worktree-merge.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/git-api'")
    expect(source).toContain('gitApi: gitApiMocks')
    expect(source).toContain('gitApiMocks.commit.mockResolvedValue')
    expect(source).not.toContain('window.gitOps')
    expect(source).not.toContain("Object.defineProperty(window, 'gitOps'")
  })

  it('routes git store push through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useGitStore.ts'),
      'utf-8'
    )
    const pushStart = source.indexOf(
      'push: async (worktreePath: string, remote?: string, branch?: string, force?: boolean)'
    )
    const pullStart = source.indexOf(
      'pull: async (worktreePath: string, remote?: string, branch?: string, rebase?: boolean)',
      pushStart
    )
    const pushSource = source.slice(pushStart, pullStart)

    expect(pushStart).toBeGreaterThan(-1)
    expect(pullStart).toBeGreaterThan(pushStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(pushSource).toContain(
      'const result = await gitApi.push(worktreePath, remote, branch, force)'
    )
    expect(pushSource).toContain('await get().loadBranchInfo(worktreePath, { force: true })')
    expect(pushSource).toContain(
      "const errMessage = error instanceof Error ? error.message : 'Failed to push'"
    )
    expect(pushSource).toContain('return { success: false, error: errMessage }')
    expect(pushSource).toContain('set({ isPushing: false })')
    expect(pushSource).not.toContain('window.gitOps.push')
    expect(pushSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes git store pull through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useGitStore.ts'),
      'utf-8'
    )
    const pullStart = source.indexOf(
      'pull: async (worktreePath: string, remote?: string, branch?: string, rebase?: boolean)'
    )
    const storeEnd = source.indexOf('}))', pullStart)
    const pullSource = source.slice(pullStart, storeEnd)

    expect(pullStart).toBeGreaterThan(-1)
    expect(storeEnd).toBeGreaterThan(pullStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(pullSource).toContain(
      'const result = await gitApi.pull(worktreePath, remote, branch, rebase)'
    )
    expect(pullSource).toContain('await get().refreshStatuses(worktreePath)')
    expect(pullSource).toContain(
      "const errMessage = error instanceof Error ? error.message : 'Failed to pull'"
    )
    expect(pullSource).toContain('return { success: false, error: errMessage }')
    expect(pullSource).toContain('set({ isPulling: false })')
    expect(pullSource).not.toContain('window.gitOps.pull')
    expect(pullSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes git store PR detach persistence through dbApi and kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useGitStore.ts'),
      'utf-8'
    )
    const detachStart = source.indexOf('detachPR: async (worktreeId: string)')
    const targetBranchStart = source.indexOf(
      'setPrTargetBranch: (worktreeId: string, branch: string)',
      detachStart
    )
    const detachSource = source.slice(detachStart, targetBranchStart)

    expect(detachStart).toBeGreaterThan(-1)
    expect(targetBranchStart).toBeGreaterThan(detachStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain("import { kanbanApi } from '@/api/kanban-api'")
    expect(source).not.toContain('const db = unwrapEnvelopeApi(() => window.db)')
    expect(detachSource).toContain('const result = await dbApi.worktree.detachPR(worktreeId)')
    expect(detachSource).toContain('await kanbanApi.ticket.clearPR(worktreeId)')
    expect(detachSource).not.toContain('kanban.ticket.clearPR')
    expect(detachSource).not.toContain('db.worktree.detachPR')
  })

  it('removes the old kanban:ticket:clearPR IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("'kanban:ticket:clearPR'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:clearPR'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:clearPR'")
  })

  it('routes worktree archive abort cleanup through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useWorktreeStore.ts'),
      'utf-8'
    )
    const archiveStart = source.indexOf('archiveWorktree: async (')
    const unbranchStart = source.indexOf('unbranchWorktree: async (', archiveStart)
    const archiveSource = source.slice(archiveStart, unbranchStart)

    expect(archiveStart).toBeGreaterThan(-1)
    expect(unbranchStart).toBeGreaterThan(archiveStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(archiveSource).toContain(
      'unwrapEnvelope(await opencodeApi.abort(worktreePath, session.opencode_session_id))'
    )
    expect(archiveSource).not.toContain(
      'await window.opencodeOps.abort(worktreePath, session.opencode_session_id)'
    )
  })

  it('routes worktree archive delete through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useWorktreeStore.ts'),
      'utf-8'
    )
    const archiveStart = source.indexOf('archiveWorktree: async (')
    const unbranchStart = source.indexOf('unbranchWorktree: async (', archiveStart)
    const archiveSource = source.slice(archiveStart, unbranchStart)

    expect(archiveStart).toBeGreaterThan(-1)
    expect(unbranchStart).toBeGreaterThan(archiveStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(archiveSource).toContain('const result = await worktreeApi.delete({')
    expect(archiveSource).toContain('worktreeId,')
    expect(archiveSource).toContain('worktreePath,')
    expect(archiveSource).toContain('branchName,')
    expect(archiveSource).toContain('projectPath,')
    expect(archiveSource).toContain('archive: true')
    expect(archiveSource).toContain(
      "return { success: false, error: result.error || 'Failed to archive worktree' }"
    )
    expect(archiveSource).not.toContain('window.worktreeOps.delete')
    expect(archiveSource).not.toContain('unwrapEnvelope(await window.worktreeOps.delete')
  })

  it('routes worktree archive connection cleanup through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useWorktreeStore.ts'),
      'utf-8'
    )
    const archiveStart = source.indexOf('archiveWorktree: async (')
    const unbranchStart = source.indexOf('unbranchWorktree: async (', archiveStart)
    const archiveSource = source.slice(archiveStart, unbranchStart)

    expect(archiveStart).toBeGreaterThan(-1)
    expect(unbranchStart).toBeGreaterThan(archiveStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(archiveSource).toContain(
      'unwrapEnvelope(await connectionApi.removeWorktreeFromAll(worktreeId))'
    )
    expect(archiveSource).not.toContain('window.connectionOps.removeWorktreeFromAll')
  })

  it('routes worktree unbranch connection cleanup through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useWorktreeStore.ts'),
      'utf-8'
    )
    const unbranchStart = source.indexOf('unbranchWorktree: async (')
    const touchStart = source.indexOf('touchWorktree: async (id: string)', unbranchStart)
    const unbranchSource = source.slice(unbranchStart, touchStart)

    expect(unbranchStart).toBeGreaterThan(-1)
    expect(touchStart).toBeGreaterThan(unbranchStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(unbranchSource).toContain(
      'unwrapEnvelope(await connectionApi.removeWorktreeFromAll(worktreeId))'
    )
    expect(unbranchSource).not.toContain('window.connectionOps.removeWorktreeFromAll')
  })

  it('removes the old connection:removeWorktreeFromAll IPC adapter after connection cleanup RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const connectionHandlersSource = readOptionalSource('../../src/main/ipc/connection-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('connection:removeWorktreeFromAll'")
    expect(connectionHandlersSource).not.toContain(
      "defineHandler('connection:removeWorktreeFromAll'"
    )
    expect(connectionHandlersSource).not.toContain(
      "connectionFailed('connection:removeWorktreeFromAll'"
    )
  })

  it('routes worktree unbranch delete through worktreeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useWorktreeStore.ts'),
      'utf-8'
    )
    const unbranchStart = source.indexOf('unbranchWorktree: async (')
    const touchStart = source.indexOf('touchWorktree: async (id: string)', unbranchStart)
    const unbranchSource = source.slice(unbranchStart, touchStart)

    expect(unbranchStart).toBeGreaterThan(-1)
    expect(touchStart).toBeGreaterThan(unbranchStart)
    expect(source).toContain("import { worktreeApi } from '@/api/worktree-api'")
    expect(unbranchSource).toContain('const result = await worktreeApi.delete({')
    expect(unbranchSource).toContain('worktreeId,')
    expect(unbranchSource).toContain('worktreePath,')
    expect(unbranchSource).toContain('branchName,')
    expect(unbranchSource).toContain('projectPath,')
    expect(unbranchSource).toContain('archive: false')
    expect(unbranchSource).toContain(
      "return { success: false, error: result.error || 'Failed to unbranch worktree' }"
    )
    expect(unbranchSource).not.toContain('window.worktreeOps.delete')
    expect(unbranchSource).not.toContain('unwrapEnvelope(await window.worktreeOps.delete')
  })

  it('routes kanban column backward-drag abort cleanup through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanColumn.tsx'),
      'utf-8'
    )
    const backwardDragStart = source.indexOf('const handleConfirmBackwardDrag = useCallback')
    const dropIndicatorStart = source.indexOf('const dropIndicator = (', backwardDragStart)
    const backwardDragSource = source.slice(backwardDragStart, dropIndicatorStart)

    expect(backwardDragStart).toBeGreaterThan(-1)
    expect(dropIndicatorStart).toBeGreaterThan(backwardDragStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(backwardDragSource).toContain(
      'unwrapEnvelope(await opencodeApi.abort(worktree.path, session.opencode_session_id))'
    )
    expect(backwardDragSource).not.toContain(
      'await window.opencodeOps.abort(worktree.path, session.opencode_session_id)'
    )
  })

  it('routes KanbanColumn backward-drag session lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanColumn.tsx'),
      'utf-8'
    )
    const backwardDragStart = source.indexOf('const handleConfirmBackwardDrag = useCallback')
    const dropIndicatorStart = source.indexOf('const dropIndicator = (', backwardDragStart)
    const backwardDragSource = source.slice(backwardDragStart, dropIndicatorStart)

    expect(backwardDragStart).toBeGreaterThan(-1)
    expect(dropIndicatorStart).toBeGreaterThan(backwardDragStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(backwardDragSource).toContain(
      'const session = await dbApi.session.get<Session>(draggedTicket.current_session_id)'
    )
    expect(backwardDragSource).not.toContain('db.session.get(draggedTicket.current_session_id)')
  })

  it('routes KanbanColumn backward-drag worktree lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanColumn.tsx'),
      'utf-8'
    )
    const backwardDragStart = source.indexOf('const handleConfirmBackwardDrag = useCallback')
    const dropIndicatorStart = source.indexOf('const dropIndicator = (', backwardDragStart)
    const backwardDragSource = source.slice(backwardDragStart, dropIndicatorStart)

    expect(backwardDragStart).toBeGreaterThan(-1)
    expect(dropIndicatorStart).toBeGreaterThan(backwardDragStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(backwardDragSource).toContain(
      'const worktree = await dbApi.worktree.get<Worktree>(session.worktree_id)'
    )
    expect(backwardDragSource).not.toContain('db.worktree.get(session.worktree_id)')
  })

  it('routes KanbanColumn backward-drag session completion through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanColumn.tsx'),
      'utf-8'
    )
    const backwardDragStart = source.indexOf('const handleConfirmBackwardDrag = useCallback')
    const dropIndicatorStart = source.indexOf('const dropIndicator = (', backwardDragStart)
    const backwardDragSource = source.slice(backwardDragStart, dropIndicatorStart)

    expect(backwardDragStart).toBeGreaterThan(-1)
    expect(dropIndicatorStart).toBeGreaterThan(backwardDragStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(backwardDragSource).toContain(
      'await dbApi.session.update<Session>(draggedTicket.current_session_id, {'
    )
    expect(backwardDragSource).toContain("status: 'completed'")
    expect(backwardDragSource).toContain('completed_at: new Date().toISOString()')
    expect(backwardDragSource).not.toContain('db.session.update(draggedTicket.current_session_id')
  })

  it('routes KanbanColumn merge-on-done worktree lookups through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanColumn.tsx'),
      'utf-8'
    )
    const mergeStart = source.indexOf('// Merge-on-done: intercept drops to Done')
    const baseWorktreeStart = source.indexOf('// Verify an active base worktree exists', mergeStart)
    const mergeSource = source.slice(mergeStart, baseWorktreeStart)

    expect(mergeStart).toBeGreaterThan(-1)
    expect(baseWorktreeStart).toBeGreaterThan(mergeStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(mergeSource).toContain(
      'const worktree = await dbApi.worktree.get<Worktree>(draggedTicket.worktree_id)'
    )
    expect(mergeSource).toContain(
      'await dbApi.worktree.getActiveByProject<Worktree>(ticketProjectId)'
    )
    expect(mergeSource).toContain('const resolvedBaseBranch = worktree.base_branch')
    expect(mergeSource).not.toContain('db.worktree.get(draggedTicket.worktree_id)')
    expect(mergeSource).not.toContain('db.worktree.getActiveByProject(ticketProjectId)')
  })

  it('routes KanbanColumn merge-on-done dirty precheck through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanColumn.tsx'),
      'utf-8'
    )
    const precheckStart = source.indexOf(
      '// Pre-check: does the feature branch actually have work to merge?'
    )
    const branchStatHandlingStart = source.indexOf(
      'const commitsAhead = branchStatResult.success',
      precheckStart
    )
    const precheckSource = source.slice(precheckStart, branchStatHandlingStart)

    expect(precheckStart).toBeGreaterThan(-1)
    expect(branchStatHandlingStart).toBeGreaterThan(precheckStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(precheckSource).toContain('gitApi.hasUncommittedChanges(worktree.path)')
    expect(precheckSource).not.toContain('window.gitOps.hasUncommittedChanges')
  })

  it('routes KanbanColumn merge-on-done branch-stat precheck through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanColumn.tsx'),
      'utf-8'
    )
    const precheckStart = source.indexOf(
      '// Pre-check: does the feature branch actually have work to merge?'
    )
    const branchStatHandlingStart = source.indexOf(
      'const commitsAhead = branchStatResult.success',
      precheckStart
    )
    const precheckSource = source.slice(precheckStart, branchStatHandlingStart)

    expect(precheckStart).toBeGreaterThan(-1)
    expect(branchStatHandlingStart).toBeGreaterThan(precheckStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(precheckSource).toContain('gitApi.hasUncommittedChanges(worktree.path)')
    expect(precheckSource).toContain(
      'gitApi.branchDiffShortStat(worktree.path, resolvedBaseBranch)'
    )
    expect(precheckSource).not.toContain('window.gitOps')
    expect(precheckSource).not.toContain('unwrapEnvelope')
  })

  it('routes MergeOnDoneDialog feature worktree lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const initStart = source.indexOf('const init = async () => {')
    const activeWorktreesStart = source.indexOf('// Resolve base branch', initStart)
    const featureLookupSource = source.slice(initStart, activeWorktreesStart)

    expect(initStart).toBeGreaterThan(-1)
    expect(activeWorktreesStart).toBeGreaterThan(initStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('type MergeWorktree = {')
    expect(featureLookupSource).toContain(
      'const featureWorktree = await dbApi.worktree.get<MergeWorktree>(ticket.worktree_id)'
    )
    expect(featureLookupSource).not.toContain('db.worktree.get(ticket.worktree_id)')
  })

  it('routes MergeOnDoneDialog active worktree lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const resolveBaseStart = source.indexOf('// Resolve base branch')
    const findBaseStart = source.indexOf('// Find base worktree', resolveBaseStart)
    const resolveBaseSource = source.slice(resolveBaseStart, findBaseStart)

    expect(resolveBaseStart).toBeGreaterThan(-1)
    expect(findBaseStart).toBeGreaterThan(resolveBaseStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('type MergeWorktree = {')
    expect(resolveBaseSource).toContain(
      'const activeWorktrees = await dbApi.worktree.getActiveByProject<MergeWorktree>('
    )
    expect(resolveBaseSource).toContain('pending.projectId')
    expect(resolveBaseSource).toContain(
      'const defaultWt = activeWorktrees.find((w) => w.is_default)'
    )
    expect(resolveBaseSource).toContain(
      'const resolvedBaseBranch = featureWorktree.base_branch ?? defaultWt?.branch_name'
    )
    expect(resolveBaseSource).not.toContain('db.worktree.getActiveByProject(pending.projectId)')
  })

  it('routes MergeOnDoneDialog archive project lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const projectLookupStart = source.indexOf('// Get project path for archive step')
    const setResolvedStart = source.indexOf('setResolved({', projectLookupStart)
    const projectLookupSource = source.slice(projectLookupStart, setResolvedStart)

    expect(projectLookupStart).toBeGreaterThan(-1)
    expect(setResolvedStart).toBeGreaterThan(projectLookupStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('type MergeProject = {')
    expect(source).not.toContain('const db = unwrapEnvelopeApi(() => window.db)')
    expect(projectLookupSource).toContain(
      'const project = await dbApi.project.get<MergeProject>(featureWorktree.project_id)'
    )
    expect(projectLookupSource).toContain('if (cancelled) return')
    expect(projectLookupSource).not.toContain('db.project.get(featureWorktree.project_id)')
  })

  it('keeps merge-on-done dialog tests on API mocks instead of window globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/kanban/merge-on-done-dialog.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('../../src/renderer/src/api/db-api'")
    expect(source).toContain("vi.mock('../../src/renderer/src/api/git-api'")
    expect(source).toContain("vi.mock('../../src/renderer/src/api/kanban-api'")
    expect(source).toContain('vi.mocked(dbApi.worktree.get).mockResolvedValue(makeWorktree())')
    expect(source).toContain('vi.mocked(dbApi.project.get).mockResolvedValue(project)')
    expect(source).not.toContain('setRendererRpcClient')
    expect(source).not.toContain('Object.defineProperty(window')
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.gitOps')
    expect(source).not.toContain('window.kanban')
  })

  it('removes the old db:project:get IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:project:get'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:project:get'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:project:get'")
  })

  it('routes MergeOnDoneDialog feature commit stage-all through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const handleCommitStart = source.indexOf('const handleCommit = useCallback(async () => {')
    const baseCommitStart = source.indexOf(
      'const handleCommitBase = useCallback(async () => {',
      handleCommitStart
    )
    const handleCommitSource = source.slice(handleCommitStart, baseCommitStart)

    expect(handleCommitStart).toBeGreaterThan(-1)
    expect(baseCommitStart).toBeGreaterThan(handleCommitStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(handleCommitSource).toContain(
      'const stageResult = await gitApi.stageAll(resolved.featureWorktreePath)'
    )
    expect(handleCommitSource).toContain('toast.error(`Failed to stage: ${stageResult.error}`)')
    expect(handleCommitSource).not.toContain('window.gitOps.stageAll')
    expect(handleCommitSource).not.toContain('unwrapEnvelope(await window.gitOps.stageAll')
  })

  it('routes MergeOnDoneDialog feature commit through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const handleCommitStart = source.indexOf('const handleCommit = useCallback(async () => {')
    const baseCommitStart = source.indexOf(
      'const handleCommitBase = useCallback(async () => {',
      handleCommitStart
    )
    const handleCommitSource = source.slice(handleCommitStart, baseCommitStart)

    expect(handleCommitStart).toBeGreaterThan(-1)
    expect(baseCommitStart).toBeGreaterThan(handleCommitStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(handleCommitSource).toContain(
      'const commitResult = await gitApi.commit(resolved.featureWorktreePath, commitMessage.trim())'
    )
    expect(handleCommitSource).toContain('toast.error(`Failed to commit: ${commitResult.error}`)')
    expect(handleCommitSource).toContain("toast.success('Changes committed')")
    expect(handleCommitSource).not.toContain('window.gitOps.commit')
    expect(handleCommitSource).not.toContain('unwrapEnvelope(await window.gitOps.commit')
  })

  it('routes gitApi branchDiffShortStat through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitBranchDiffShortStatResult = {')
    expect(source).toContain('branchDiffShortStat: async (')
    expect(source).toContain(
      "getRendererRpcClient().request<GitBranchDiffShortStatResult>('gitOps.branchDiffShortStat'"
    )
    expect(source).toContain('baseBranch')
  })

  it('routes gitApi getDiff through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitDiffResult = {')
    expect(source).toContain('getDiff: async (')
    expect(source).toContain('filePath: string,')
    expect(source).toContain('contextLines?: number')
    expect(source).toContain("getRendererRpcClient().request<GitDiffResult>('gitOps.getDiff'")
    expect(source).toContain('staged')
    expect(source).toContain('isUntracked')
  })

  it('routes gitApi getBranchFileDiff through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitBranchFileDiffResult = {')
    expect(source).toContain('getBranchFileDiff: async (')
    expect(source).toContain(
      "getRendererRpcClient().request<GitBranchFileDiffResult>('gitOps.getBranchFileDiff'"
    )
    expect(source).toContain('branch,')
    expect(source).toContain('filePath')
  })

  it('routes gitApi status changes through the renderer event subscription', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('GIT_STATUS_CHANGED_CHANNEL')
    expect(source).toContain('GitStatusChangedEvent')
    expect(source).toContain('const isGitStatusChangedEvent = (')
    expect(source).toContain('onStatusChanged: (callback: (event: GitStatusChangedEvent) => void)')
    expect(source).toContain('getRendererRpcClient().subscribe(GIT_STATUS_CHANGED_CHANNEL')
    expect(source).toContain('if (isGitStatusChangedEvent(event.payload))')
  })

  it('removes the old git:statusChanged IPC event adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const gitEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/git-events.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.on('git:statusChanged'")
    expect(preloadSource).not.toContain('ipcRenderer.on("git:statusChanged"')
    expect(preloadSource).not.toContain("ipcRenderer.removeListener('git:statusChanged'")
    expect(preloadSource).not.toContain('ipcRenderer.removeListener("git:statusChanged"')
    expect(gitEventsSource).not.toContain('webContents.send(GIT_STATUS_CHANGED_CHANNEL')
    expect(gitEventsSource).not.toContain("webContents.send('git:statusChanged'")
    expect(gitEventsSource).not.toContain('webContents.send("git:statusChanged"')
  })

  it('routes gitApi branch changes through the renderer event subscription', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('GIT_BRANCH_CHANGED_CHANNEL')
    expect(source).toContain('GitBranchChangedEvent')
    expect(source).toContain('const isGitBranchChangedEvent = (')
    expect(source).toContain('onBranchChanged: (callback: (event: GitBranchChangedEvent) => void)')
    expect(source).toContain('getRendererRpcClient().subscribe(GIT_BRANCH_CHANGED_CHANNEL')
    expect(source).toContain('if (isGitBranchChangedEvent(event.payload))')
  })

  it('removes the old git:branchChanged IPC event adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const gitEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/git-events.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain("ipcRenderer.on('git:branchChanged'")
    expect(preloadSource).not.toContain('ipcRenderer.on("git:branchChanged"')
    expect(preloadSource).not.toContain("ipcRenderer.removeListener('git:branchChanged'")
    expect(preloadSource).not.toContain('ipcRenderer.removeListener("git:branchChanged"')
    expect(gitEventsSource).not.toContain('webContents.send(GIT_BRANCH_CHANGED_CHANNEL')
    expect(gitEventsSource).not.toContain("webContents.send('git:branchChanged'")
    expect(gitEventsSource).not.toContain('webContents.send("git:branchChanged"')
  })

  it('removes the old git watcher startup registration after watcher RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/server.ts'),
      'utf-8'
    )
    const backendManagerSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/desktop/backend-manager.ts'),
      'utf-8'
    )
    const gitOpsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/git-ops.ts'),
      'utf-8'
    )
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')
    const gitFileHandlersSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')
    const worktreeWatcherSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/worktree-watcher.ts'),
      'utf-8'
    )
    const branchWatcherSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/branch-watcher.ts'),
      'utf-8'
    )
    const gitEventsSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/main/services/git-events.ts'),
      'utf-8'
    )

    expect(gitFileHandlersSource).not.toContain('registerGitFileHandlers')
    expect(gitFileHandlersSource).not.toContain('initWorktreeWatcher')
    expect(gitFileHandlersSource).not.toContain('initBranchWatcher')
    expect(fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/git-file-handlers.ts'))).toBe(
      false
    )
    expect(worktreeWatcherSource).not.toContain('initWorktreeWatcher')
    expect(worktreeWatcherSource).not.toContain('BrowserWindow')
    expect(worktreeWatcherSource).not.toContain('mainWindow')
    expect(branchWatcherSource).not.toContain('initBranchWatcher')
    expect(branchWatcherSource).not.toContain('BrowserWindow')
    expect(branchWatcherSource).not.toContain('mainWindow')
    expect(gitEventsSource).toContain('emitGitStatusChanged = (payload: GitStatusChangedEvent)')
    expect(gitEventsSource).toContain('emitGitBranchChanged = (payload: GitBranchChangedEvent)')
    expect(gitEventsSource).toContain('setGitEventPublisher')
    expect(gitEventsSource).toContain("import('../desktop/backend-event-publisher')")
    expect(serverSource).toContain("from '../main/services/worktree-watcher'")
    expect(serverSource).toContain("from '../main/services/branch-watcher'")
    expect(serverSource).toContain("from '../main/services/git-events'")
    expect(serverSource).toContain('setGitEventPublisher((channel, payload)')
    expect(serverSource).toContain('await cleanupWorktreeWatchers()')
    expect(serverSource).toContain('await cleanupBranchWatchers()')
    expect(serverSource).toContain('setGitEventPublisher(null)')
    expect(mainSource).not.toContain("from './services/worktree-watcher'")
    expect(mainSource).not.toContain("from './services/branch-watcher'")
    expect(mainSource).not.toContain('registerGitFileHandlers')
    expect(mainSource).not.toContain('Registering GitFile handlers')
    expect(backendManagerSource).not.toContain('watchGitWorktree')
    expect(backendManagerSource).not.toContain('unwatchGitWorktree')
    expect(backendManagerSource).not.toContain('watchGitBranch')
    expect(backendManagerSource).not.toContain('unwatchGitBranch')
    expect(gitOpsSource).toContain("return import('../../../main/services/worktree-watcher')")
    expect(gitOpsSource).toContain("return import('../../../main/services/branch-watcher')")
    expect(gitOpsSource).toContain("if (command === 'watchGitWorktree') await watchWorktree(worktreePath)")
    expect(gitOpsSource).toContain("if (command === 'watchGitBranch') await watchBranch(worktreePath)")
    expect(gitOpsSource).not.toContain("makeDesktopCommandRequest(id, 'watchGitWorktree'")
    expect(gitOpsSource).not.toContain("makeDesktopCommandRequest(id, 'unwatchGitWorktree'")
    expect(gitOpsSource).not.toContain("makeDesktopCommandRequest(id, 'watchGitBranch'")
    expect(gitOpsSource).not.toContain("makeDesktopCommandRequest(id, 'unwatchGitBranch'")
    expect(ipcIndexSource).not.toContain('registerGitFileHandlers')
    expect(ipcIndexSource).not.toContain('git-file-handlers')
  })

  it('routes gitApi watchWorktree through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('watchWorktree: async (worktreePath: string)')
    expect(source).toContain("'gitOps.watchWorktree'")
    expect(source).toContain('worktreePath')
  })

  it('removes the old git:watchWorktree IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const gitFileHandlersSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:watchWorktree'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("git:watchWorktree"')
    expect(gitFileHandlersSource).not.toContain("defineHandler('git:watchWorktree'")
    expect(gitFileHandlersSource).not.toContain('defineHandler("git:watchWorktree"')
  })

  it('routes gitApi watchBranch through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('watchBranch: async (worktreePath: string)')
    expect(source).toContain("'gitOps.watchBranch'")
    expect(source).toContain('worktreePath')
  })

  it('removes the old git:watchBranch IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const gitFileHandlersSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:watchBranch'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("git:watchBranch"')
    expect(gitFileHandlersSource).not.toContain("defineHandler('git:watchBranch'")
    expect(gitFileHandlersSource).not.toContain('defineHandler("git:watchBranch"')
  })

  it('routes gitApi unwatchWorktree through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('unwatchWorktree: async (worktreePath: string)')
    expect(source).toContain("'gitOps.unwatchWorktree'")
    expect(source).toContain('worktreePath')
  })

  it('removes the old git:unwatchWorktree IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const gitFileHandlersSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:unwatchWorktree'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("git:unwatchWorktree"')
    expect(gitFileHandlersSource).not.toContain("defineHandler('git:unwatchWorktree'")
    expect(gitFileHandlersSource).not.toContain('defineHandler("git:unwatchWorktree"')
  })

  it('routes gitApi unwatchBranch through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('unwatchBranch: async (worktreePath: string)')
    expect(source).toContain("'gitOps.unwatchBranch'")
    expect(source).toContain('worktreePath')
  })

  it('removes the old git:unwatchBranch IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const gitFileHandlersSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:unwatchBranch'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("git:unwatchBranch"')
    expect(gitFileHandlersSource).not.toContain("defineHandler('git:unwatchBranch'")
    expect(gitFileHandlersSource).not.toContain('defineHandler("git:unwatchBranch"')
  })

  it('routes gitApi getBranchInfo through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitBranchInfoResult = {')
    expect(source).toContain('getBranchInfo: async (worktreePath: string)')
    expect(source).toContain(
      "getRendererRpcClient().request<GitBranchInfoResult>('gitOps.getBranchInfo'"
    )
    expect(source).toContain('worktreePath')
  })

  it('removes the old git:branchInfo IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const gitFileHandlersSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:branchInfo'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("git:branchInfo"')
    expect(gitFileHandlersSource).not.toContain("defineHandler('git:branchInfo'")
    expect(gitFileHandlersSource).not.toContain('defineHandler("git:branchInfo"')
  })

  it('routes gitApi listPRs through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('listPRs: async (projectPath: string)')
    expect(source).toContain(
      "getRendererRpcClient().request<GitListPullRequestsResult>('gitOps.listPRs'"
    )
    expect(source).toContain('projectPath')
  })

  it('removes the old git:listPRs IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const gitFileHandlersSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:listPRs'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("git:listPRs"')
    expect(gitFileHandlersSource).not.toContain("defineHandler('git:listPRs'")
    expect(gitFileHandlersSource).not.toContain('defineHandler("git:listPRs"')
  })

  it('routes gitApi getFileContent through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitFileContentResult = {')
    expect(source).toContain('getFileContent: async (')
    expect(source).toContain('worktreePath: string,')
    expect(source).toContain('filePath: string')
    expect(source).toContain(
      "getRendererRpcClient().request<GitFileContentResult>('gitOps.getFileContent'"
    )
    expect(source).toContain('worktreePath')
    expect(source).toContain('filePath')
  })

  it('keeps the plain-file backend contract test on gitApi RPC instead of legacy gitOps globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-18/session-8/plain-file-backend.test.ts'),
      'utf-8'
    )

    expect(source).toContain('gitApi.getFileContent')
    expect(source).toContain("request).toHaveBeenCalledWith('gitOps.getFileContent'")
    expect(source).not.toContain('window.gitOps')
    expect(source).not.toContain('git:getFileContent')
    expect(source).not.toContain('IPC')
  })

  it('keeps the plain-file frontend test on gitApi mocks instead of legacy gitOps globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-18/session-9/plain-file-frontend.test.tsx'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/git-api'")
    expect(source).toContain('gitApi: gitApiMocks')
    expect(source).not.toContain('window.gitOps')
    expect(source).not.toContain('...window.gitOps')
    expect(source).not.toContain("Object.defineProperty(window, 'gitOps'")
  })

  it('routes gitApi getFileContentBase64 through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitFileContentBase64Result = {')
    expect(source).toContain('getFileContentBase64: async (')
    expect(source).toContain('worktreePath: string,')
    expect(source).toContain('filePath: string')
    expect(source).toContain(
      "getRendererRpcClient().request<GitFileContentBase64Result>('gitOps.getFileContentBase64'"
    )
    expect(source).toContain('worktreePath')
    expect(source).toContain('filePath')
  })

  it('routes gitApi getBranchBaseContent through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitBranchBaseContentResult = {')
    expect(source).toContain('getBranchBaseContent: async (')
    expect(source).toContain('worktreePath: string,')
    expect(source).toContain('branch: string,')
    expect(source).toContain('filePath: string')
    expect(source).toContain(
      "getRendererRpcClient().request<GitBranchBaseContentResult>('gitOps.getBranchBaseContent'"
    )
    expect(source).toContain('worktreePath')
    expect(source).toContain('branch')
    expect(source).toContain('filePath')
  })

  it('routes gitApi getRefContent through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitRefContentResult = {')
    expect(source).toContain('getRefContent: async (')
    expect(source).toContain('worktreePath: string,')
    expect(source).toContain('ref: string,')
    expect(source).toContain('filePath: string')
    expect(source).toContain(
      "getRendererRpcClient().request<GitRefContentResult>('gitOps.getRefContent'"
    )
    expect(source).toContain('worktreePath')
    expect(source).toContain('ref')
    expect(source).toContain('filePath')
  })

  it('routes gitApi getRefContentBase64 through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitRefContentBase64Result = {')
    expect(source).toContain('getRefContentBase64: async (')
    expect(source).toContain('worktreePath: string,')
    expect(source).toContain('ref: string,')
    expect(source).toContain('filePath: string')
    expect(source).toContain(
      "getRendererRpcClient().request<GitRefContentBase64Result>('gitOps.getRefContentBase64'"
    )
    expect(source).toContain('worktreePath')
    expect(source).toContain('ref')
    expect(source).toContain('filePath')
  })

  it('routes gitApi getBranchBaseContentBase64 through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitBranchBaseContentBase64Result = {')
    expect(source).toContain('getBranchBaseContentBase64: async (')
    expect(source).toContain('worktreePath: string,')
    expect(source).toContain('branch: string,')
    expect(source).toContain('filePath: string')
    expect(source).toContain('getRendererRpcClient().request<GitBranchBaseContentBase64Result>(')
    expect(source).toContain("'gitOps.getBranchBaseContentBase64'")
    expect(source).toContain('worktreePath')
    expect(source).toContain('branch')
    expect(source).toContain('filePath')
  })

  it('routes gitApi hasUncommittedChanges through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('hasUncommittedChanges: async (worktreePath: string)')
    expect(source).toContain(
      "getRendererRpcClient().request<boolean>('gitOps.hasUncommittedChanges'"
    )
    expect(source).toContain('worktreePath')
  })

  it('routes gitApi isBranchMerged through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitBranchMergedResult = {')
    expect(source).toContain('isBranchMerged: async (')
    expect(source).toContain('worktreePath: string,')
    expect(source).toContain('branch: string')
    expect(source).toContain(
      "getRendererRpcClient().request<GitBranchMergedResult>('gitOps.isBranchMerged'"
    )
    expect(source).toContain('worktreePath')
    expect(source).toContain('branch')
  })

  it('routes gitApi getDiffStat through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitDiffStatResult = {')
    expect(source).toContain('getDiffStat: async (worktreePath: string)')
    expect(source).toContain(
      "getRendererRpcClient().request<GitDiffStatResult>('gitOps.getDiffStat'"
    )
    expect(source).toContain('worktreePath')
  })

  it('routes gitApi getBranchDiffFiles through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitBranchDiffFilesResult = {')
    expect(source).toContain('getBranchDiffFiles: async (')
    expect(source).toContain('worktreePath: string,')
    expect(source).toContain('branch: string')
    expect(source).toContain(
      "getRendererRpcClient().request<GitBranchDiffFilesResult>('gitOps.getBranchDiffFiles'"
    )
    expect(source).toContain('worktreePath')
    expect(source).toContain('branch')
  })

  it('routes gitApi listBranchesWithStatus through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitListBranchesWithStatusResult = {')
    expect(source).toContain('listBranchesWithStatus: async (projectPath: string)')
    expect(source).toContain('getRendererRpcClient().request<GitListBranchesWithStatusResult>(')
    expect(source).toContain("'gitOps.listBranchesWithStatus'")
    expect(source).toContain('projectPath')
  })

  it('routes gitApi getRangeDiff through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitRangeDiffResult = {')
    expect(source).toContain('getRangeDiff: async (worktreePath: string, baseBranch: string)')
    expect(source).toContain(
      "getRendererRpcClient().request<GitRangeDiffResult>('gitOps.getRangeDiff'"
    )
    expect(source).toContain('worktreePath')
    expect(source).toContain('baseBranch')
  })

  it('routes gitApi generatePRContent through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitGeneratePRContentResult = {')
    expect(source).toContain('type GitPRContentProvider =')
    expect(source).toContain('generatePRContent: async (')
    expect(source).toContain(
      "getRendererRpcClient().request<GitGeneratePRContentResult>('gitOps.generatePRContent'"
    )
    expect(source).toContain('worktreePath')
    expect(source).toContain('baseBranch')
    expect(source).toContain('provider')
  })

  it('routes gitApi createPR through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitCreatePRResult = {')
    expect(source).toContain('createPR: async (')
    expect(source).toContain("getRendererRpcClient().request<GitCreatePRResult>('gitOps.createPR'")
    expect(source).toContain('worktreePath')
    expect(source).toContain('baseBranch')
    expect(source).toContain('title')
    expect(source).toContain('body')
  })

  it('routes gitApi getPRState through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitPRStateResult = {')
    expect(source).toContain('getPRState: async (projectPath: string, prNumber: number)')
    expect(source).toContain("getRendererRpcClient().request<GitPRStateResult>('gitOps.getPRState'")
    expect(source).toContain('projectPath')
    expect(source).toContain('prNumber')
  })

  it('removes the old git:getPRState IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const gitFileHandlersSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:getPRState'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("git:getPRState"')
    expect(gitFileHandlersSource).not.toContain("defineHandler('git:getPRState'")
    expect(gitFileHandlersSource).not.toContain('defineHandler("git:getPRState"')
  })

  it('routes gitApi getPRReviewComments through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('type GitPRReviewCommentsResult = {')
    expect(source).toContain('getPRReviewComments: async (')
    expect(source).toContain(
      "getRendererRpcClient().request<GitPRReviewCommentsResult>('gitOps.getPRReviewComments'"
    )
    expect(source).toContain('projectPath')
    expect(source).toContain('prNumber')
  })

  it('removes the old git:getPRReviewComments IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const gitFileHandlersSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:getPRReviewComments'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("git:getPRReviewComments"')
    expect(gitFileHandlersSource).not.toContain("defineHandler('git:getPRReviewComments'")
    expect(gitFileHandlersSource).not.toContain('defineHandler("git:getPRReviewComments"')
  })

  it('routes gitApi openInEditor through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('openInEditor: async (filePath: string)')
    expect(source).toContain(
      "getRendererRpcClient().request<GitOperationResult>('gitOps.openInEditor'"
    )
    expect(source).toContain('filePath')
  })

  it('removes the old git:openInEditor IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const gitFileHandlersSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:openInEditor'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("git:openInEditor"')
    expect(gitFileHandlersSource).not.toContain("defineHandler('git:openInEditor'")
    expect(gitFileHandlersSource).not.toContain('defineHandler("git:openInEditor"')
  })

  it('routes gitApi showInFinder through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )
    const serverSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/server/rpc/domains/git-ops.ts'),
      'utf-8'
    )
    const serverFunctionStart = serverSource.indexOf('showInFinder: (filePath) =>')
    const serverFunctionEnd = serverSource.indexOf('    watchWorktree:', serverFunctionStart)
    const serverFunctionSource = serverSource.slice(serverFunctionStart, serverFunctionEnd)

    expect(source).toContain('showInFinder: async (filePath: string)')
    expect(source).toContain(
      "getRendererRpcClient().request<GitOperationResult>('gitOps.showInFinder'"
    )
    expect(source).toContain('filePath')
    expect(serverFunctionStart).toBeGreaterThan(-1)
    expect(serverFunctionEnd).toBeGreaterThan(serverFunctionStart)
    expect(serverFunctionSource).toContain('requestGitShowInFinderCommand(filePath)')
    expect(serverFunctionSource).not.toContain("import('electron')")
    expect(serverFunctionSource).not.toContain('shell.showItemInFolder')
  })

  it('removes the old git:showInFinder IPC invoke adapter', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const gitFileHandlersSource = readOptionalSource('../../src/main/ipc/git-file-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('git:showInFinder'")
    expect(preloadSource).not.toContain('ipcRenderer.invoke("git:showInFinder"')
    expect(gitFileHandlersSource).not.toContain("defineHandler('git:showInFinder'")
    expect(gitFileHandlersSource).not.toContain('defineHandler("git:showInFinder"')
  })

  it('routes gitApi needsPush through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('needsPush: async (worktreePath: string)')
    expect(source).toContain("getRendererRpcClient().request<boolean>('gitOps.needsPush'")
    expect(source).toContain('worktreePath')
  })

  it('routes gitApi prMerge through the renderer RPC client', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/api/git-api.ts'),
      'utf-8'
    )

    expect(source).toContain('prMerge: async (worktreePath: string, prNumber: number)')
    expect(source).toContain("getRendererRpcClient().request<GitOperationResult>('gitOps.prMerge'")
    expect(source).toContain('worktreePath')
    expect(source).toContain('prNumber')
  })

  it('keeps the legacy PR merge backend test on gitApi RPC instead of gitOps globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-20/session-3/pr-merge-ipc.test.ts'),
      'utf-8'
    )

    expect(source).toContain("await expect(gitApi.prMerge('/tmp/hive-feature', 123))")
    expect(source).toContain("request).toHaveBeenCalledWith('gitOps.prMerge'")
    expect(source).not.toContain('window.gitOps')
    expect(source).not.toContain('...window.gitOps')
    expect(source).not.toContain("Object.defineProperty(window, 'gitOps'")
  })

  it('routes MergeOnDoneDialog initial base dirty check through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const dirtyCheckStart = source.indexOf('// Check both worktrees for dirty state in parallel')
    const cancelledCheckStart = source.indexOf('if (cancelled) return', dirtyCheckStart)
    const dirtyCheckSource = source.slice(dirtyCheckStart, cancelledCheckStart)

    expect(dirtyCheckStart).toBeGreaterThan(-1)
    expect(cancelledCheckStart).toBeGreaterThan(dirtyCheckStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(dirtyCheckSource).toContain('gitApi.hasUncommittedChanges(baseWorktree.path)')
    expect(dirtyCheckSource).toContain(
      '.branchDiffShortStat(featureWorktree.path, resolvedBaseBranch)'
    )
    expect(dirtyCheckSource).not.toContain('window.gitOps.hasUncommittedChanges(baseWorktree.path)')
  })

  it('routes MergeOnDoneDialog initial feature dirty check through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const dirtyCheckStart = source.indexOf('// Check both worktrees for dirty state in parallel')
    const cancelledCheckStart = source.indexOf('if (cancelled) return', dirtyCheckStart)
    const dirtyCheckSource = source.slice(dirtyCheckStart, cancelledCheckStart)

    expect(dirtyCheckStart).toBeGreaterThan(-1)
    expect(cancelledCheckStart).toBeGreaterThan(dirtyCheckStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(dirtyCheckSource).toContain('gitApi.hasUncommittedChanges(baseWorktree.path)')
    expect(dirtyCheckSource).toContain('gitApi.hasUncommittedChanges(featureWorktree.path)')
    expect(dirtyCheckSource).toContain(
      '.branchDiffShortStat(featureWorktree.path, resolvedBaseBranch)'
    )
    expect(dirtyCheckSource).not.toContain(
      'window.gitOps.hasUncommittedChanges(featureWorktree.path)'
    )
  })

  it('routes MergeOnDoneDialog initial branch stat through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const dirtyCheckStart = source.indexOf('// Check both worktrees for dirty state in parallel')
    const cancelledCheckStart = source.indexOf('if (cancelled) return', dirtyCheckStart)
    const dirtyCheckSource = source.slice(dirtyCheckStart, cancelledCheckStart)

    expect(dirtyCheckStart).toBeGreaterThan(-1)
    expect(cancelledCheckStart).toBeGreaterThan(dirtyCheckStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(dirtyCheckSource).toContain('gitApi.hasUncommittedChanges(baseWorktree.path)')
    expect(dirtyCheckSource).toContain('gitApi.hasUncommittedChanges(featureWorktree.path)')
    expect(dirtyCheckSource).toContain(
      'gitApi.branchDiffShortStat(featureWorktree.path, resolvedBaseBranch)'
    )
    expect(dirtyCheckSource).not.toContain('window.gitOps')
    expect(dirtyCheckSource).not.toContain('unwrapEnvelope')
  })

  it('routes MergeOnDoneDialog feature diff stat through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const diffStatStart = source.indexOf(
      '// Get uncommitted diff stats for both worktrees if needed'
    )
    const statsStart = source.indexOf(
      'let uncommittedStats = { filesChanged: 0, insertions: 0, deletions: 0 }',
      diffStatStart
    )
    const diffStatSource = source.slice(diffStatStart, statsStart)

    expect(diffStatStart).toBeGreaterThan(-1)
    expect(statsStart).toBeGreaterThan(diffStatStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(diffStatSource).toContain('gitApi.getDiffStat(featureWorktree.path)')
    expect(diffStatSource).not.toContain('window.gitOps.getDiffStat(featureWorktree.path)')
  })

  it('routes MergeOnDoneDialog base diff stat through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const diffStatStart = source.indexOf(
      '// Get uncommitted diff stats for both worktrees if needed'
    )
    const statsStart = source.indexOf(
      'let uncommittedStats = { filesChanged: 0, insertions: 0, deletions: 0 }',
      diffStatStart
    )
    const diffStatSource = source.slice(diffStatStart, statsStart)

    expect(diffStatStart).toBeGreaterThan(-1)
    expect(statsStart).toBeGreaterThan(diffStatStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(diffStatSource).toContain('gitApi.getDiffStat(featureWorktree.path)')
    expect(diffStatSource).toContain('gitApi.getDiffStat(baseWorktree.path)')
    expect(diffStatSource).not.toContain('window.gitOps')
    expect(diffStatSource).not.toContain('unwrapEnvelope')
  })

  it('routes MergeOnDoneDialog remote check through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const handleMergeStart = source.indexOf('const handleMerge = useCallback(async () => {')
    const pullStart = source.indexOf('const pullResult = await gitApi.pull(', handleMergeStart)
    const remoteCheckSource = source.slice(handleMergeStart, pullStart)

    expect(handleMergeStart).toBeGreaterThan(-1)
    expect(pullStart).toBeGreaterThan(handleMergeStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(remoteCheckSource).toContain(
      'const remoteResult = await gitApi.getRemoteUrl(resolved.baseWorktreePath)'
    )
    expect(remoteCheckSource).toContain('if (remoteResult.url) {')
    expect(remoteCheckSource).not.toContain('window.gitOps.getRemoteUrl')
    expect(remoteCheckSource).not.toContain('unwrapEnvelope')
  })

  it('routes MergeOnDoneDialog pre-merge pull through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const handleMergeStart = source.indexOf('const handleMerge = useCallback(async () => {')
    const mergeStart = source.indexOf('// Merge feature into base', handleMergeStart)
    const pullSource = source.slice(handleMergeStart, mergeStart)

    expect(handleMergeStart).toBeGreaterThan(-1)
    expect(mergeStart).toBeGreaterThan(handleMergeStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(pullSource).toContain(
      'const remoteResult = await gitApi.getRemoteUrl(resolved.baseWorktreePath)'
    )
    expect(pullSource).toContain('const pullResult = await gitApi.pull(resolved.baseWorktreePath)')
    expect(pullSource).toContain(
      'toast.warning(`Pull failed on ${resolved.baseBranch} — continuing with local merge`)'
    )
    expect(pullSource).not.toContain('window.gitOps.pull')
    expect(pullSource).not.toContain('unwrapEnvelope')
  })

  it('routes MergeOnDoneDialog merge through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const mergeStart = source.indexOf('// Merge feature into base')
    const conflictStart = source.indexOf('if (!mergeResult.success) {', mergeStart)
    const mergeSource = source.slice(mergeStart, conflictStart)

    expect(mergeStart).toBeGreaterThan(-1)
    expect(conflictStart).toBeGreaterThan(mergeStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(mergeSource).toContain(
      'const mergeResult = await gitApi.merge(resolved.baseWorktreePath, resolved.featureBranch)'
    )
    expect(mergeSource).not.toContain('window.gitOps.merge')
    expect(mergeSource).not.toContain('unwrapEnvelope')
  })

  it('routes GitPushPull merge execution through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/git/GitPushPull.tsx'),
      'utf-8'
    )
    const mergeStart = source.indexOf('  const doMerge = useCallback(async () => {')
    const branchRefreshStart = source.indexOf(
      'const branchResult = await gitApi.listBranchesWithStatus(',
      mergeStart
    )
    const mergeSource = source.slice(mergeStart, branchRefreshStart)

    expect(mergeStart).toBeGreaterThan(-1)
    expect(branchRefreshStart).toBeGreaterThan(mergeStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(mergeSource).toContain(
      'const result = await gitApi.merge(worktreePath, mergeBranch.trim())'
    )
    expect(mergeSource).toContain('toast.success(`Merged ${mergeBranch} successfully`)')
    expect(mergeSource).toContain('await refreshStatuses(worktreePath)')
    expect(mergeSource).not.toContain('window.gitOps.merge')
    expect(mergeSource).not.toContain('unwrapEnvelope')
  })

  it('routes GitPushPull post-merge branch refresh through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/git/GitPushPull.tsx'),
      'utf-8'
    )
    const branchRefreshStart = source.indexOf(
      'const branchResult = await gitApi.listBranchesWithStatus('
    )
    const recheckStart = source.indexOf('setMergedCheckVersion((v) => v + 1)', branchRefreshStart)
    const branchRefreshSource = source.slice(branchRefreshStart, recheckStart)

    expect(branchRefreshStart).toBeGreaterThan(-1)
    expect(recheckStart).toBeGreaterThan(branchRefreshStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(branchRefreshSource).toContain(
      'const branchResult = await gitApi.listBranchesWithStatus(worktreePath)'
    )
    expect(branchRefreshSource).toContain('setBranches(branchResult.branches)')
    expect(branchRefreshSource).not.toContain('window.gitOps.listBranchesWithStatus')
    expect(branchRefreshSource).not.toContain('unwrapEnvelope')
  })

  it('routes GitPushPull dropdown branch loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/git/GitPushPull.tsx'),
      'utf-8'
    )
    const loadBranchesStart = source.indexOf('  // Load branches when dropdown opens')
    const focusInputStart = source.indexOf(
      '  // Focus the filter input when dropdown opens',
      loadBranchesStart
    )
    const loadBranchesSource = source.slice(loadBranchesStart, focusInputStart)

    expect(loadBranchesStart).toBeGreaterThan(-1)
    expect(focusInputStart).toBeGreaterThan(loadBranchesStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(loadBranchesSource).toContain('gitApi')
    expect(loadBranchesSource).toContain('.listBranchesWithStatus(worktreePath)')
    expect(loadBranchesSource).toContain('setBranches(result.branches)')
    expect(loadBranchesSource).toContain('setBranchesLoading(false)')
    expect(loadBranchesSource).not.toContain('window.gitOps.listBranchesWithStatus')
    expect(loadBranchesSource).not.toContain('unwrapEnvelope')
  })

  it('routes GitPushPull merge-branch merged check through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/git/GitPushPull.tsx'),
      'utf-8'
    )
    const mergedCheckStart = source.indexOf(
      '  // Check if the selected merge branch is already merged into HEAD'
    )
    const selectedBranchStart = source.indexOf(
      '  // Look up whether the selected merge branch is checked out in a worktree',
      mergedCheckStart
    )
    const mergedCheckSource = source.slice(mergedCheckStart, selectedBranchStart)

    expect(mergedCheckStart).toBeGreaterThan(-1)
    expect(selectedBranchStart).toBeGreaterThan(mergedCheckStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(mergedCheckSource).toContain('gitApi.isBranchMerged(worktreePath, mergeBranch)')
    expect(mergedCheckSource).toContain('setIsBranchMerged(result.isMerged)')
    expect(mergedCheckSource).toContain('mergedCheckVersion')
    expect(mergedCheckSource).not.toContain('window.gitOps.isBranchMerged')
    expect(mergedCheckSource).not.toContain('unwrapEnvelope')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('routes BranchDiffView branch list loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/file-tree/BranchDiffView.tsx'),
      'utf-8'
    )
    const loadBranchesStart = source.indexOf('  // Load branches')
    const loadDiffFilesStart = source.indexOf(
      '  // Load diff files for selected branch',
      loadBranchesStart
    )
    const loadBranchesSource = source.slice(loadBranchesStart, loadDiffFilesStart)

    expect(loadBranchesStart).toBeGreaterThan(-1)
    expect(loadDiffFilesStart).toBeGreaterThan(loadBranchesStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(loadBranchesSource).toContain(
      'const result = await gitApi.listBranchesWithStatus(worktreePath)'
    )
    expect(loadBranchesSource).toContain('setBranches(result.branches)')
    expect(loadBranchesSource).not.toContain('window.gitOps.listBranchesWithStatus')
    expect(loadBranchesSource).not.toContain('unwrapEnvelope')
  })

  it('routes BranchDiffView branch diff file loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/file-tree/BranchDiffView.tsx'),
      'utf-8'
    )
    const loadDiffFilesStart = source.indexOf('  // Load diff files for selected branch')
    const initialLoadStart = source.indexOf('  // Initial load of branches', loadDiffFilesStart)
    const loadDiffFilesSource = source.slice(loadDiffFilesStart, initialLoadStart)

    expect(loadDiffFilesStart).toBeGreaterThan(-1)
    expect(initialLoadStart).toBeGreaterThan(loadDiffFilesStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(loadDiffFilesSource).toContain(
      'const result = await gitApi.getBranchDiffFiles(worktreePath, selectedBranch)'
    )
    expect(loadDiffFilesSource).toContain('setFiles(result.files)')
    expect(loadDiffFilesSource).toContain(
      "setDiffError(result.error || 'Failed to load diff files')"
    )
    expect(loadDiffFilesSource).not.toContain('window.gitOps.getBranchDiffFiles')
    expect(loadDiffFilesSource).not.toContain('unwrapEnvelope')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('routes BranchDiffView status-change subscription through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/file-tree/BranchDiffView.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf(
      '  // Listen for git status changes to auto-refresh file list'
    )
    const closeDropdownStart = source.indexOf('  // Close dropdown on outside click', effectStart)
    const effectSource = source.slice(effectStart, closeDropdownStart)

    expect(effectStart).toBeGreaterThan(-1)
    expect(closeDropdownStart).toBeGreaterThan(effectStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(effectSource).toContain('if (!worktreePath || !selectedBranch) return')
    expect(effectSource).toContain('const cleanup = gitApi.onStatusChanged((event) => {')
    expect(effectSource).toContain('event.worktreePath === worktreePath')
    expect(effectSource).toContain('loadDiffFiles()')
    expect(effectSource).toContain('return cleanup')
    expect(effectSource).not.toContain('window.gitOps.onStatusChanged')
  })

  it('routes CreatePRModal remote branch loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/pr/CreatePRModal.tsx'),
      'utf-8'
    )
    const remoteBranchesStart = source.indexOf('    // Fetch remote branches')
    const uncommittedStart = source.indexOf(
      '    // Check for uncommitted changes',
      remoteBranchesStart
    )
    const remoteBranchesSource = source.slice(remoteBranchesStart, uncommittedStart)

    expect(remoteBranchesStart).toBeGreaterThan(-1)
    expect(uncommittedStart).toBeGreaterThan(remoteBranchesStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(remoteBranchesSource).toContain('gitApi')
    expect(remoteBranchesSource).toContain('.listBranchesWithStatus(worktreePath)')
    expect(remoteBranchesSource).toContain(
      'setRemoteBranches(result.branches.filter((b) => b.isRemote))'
    )
    expect(remoteBranchesSource).toContain('setLoadingBranches(false)')
    expect(remoteBranchesSource).not.toContain('window.gitOps.listBranchesWithStatus')
    expect(remoteBranchesSource).not.toContain('unwrapEnvelope')
  })

  it('routes CreatePRModal base-branch commit-count refresh through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/pr/CreatePRModal.tsx'),
      'utf-8'
    )
    const refreshStart = source.indexOf('  // ── Refresh commit count when base branch changes')
    const derivedStart = source.indexOf(
      '  // ── Derived: clean branch names for dropdown',
      refreshStart
    )
    const refreshSource = source.slice(refreshStart, derivedStart)

    expect(refreshStart).toBeGreaterThan(-1)
    expect(derivedStart).toBeGreaterThan(refreshStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(refreshSource).toContain('gitApi')
    expect(refreshSource).toContain('.getRangeDiff(worktreePath, baseBranch)')
    expect(refreshSource).toContain('setCommitCount(null)')
    expect(refreshSource).toContain('setCommitCount(rd.commitCount)')
    expect(refreshSource).not.toContain('window.gitOps.getRangeDiff')
    expect(refreshSource).not.toContain('unwrapEnvelope')
  })

  it('routes CreatePRModal post-commit commit-count refresh through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/pr/CreatePRModal.tsx'),
      'utf-8'
    )
    const postCommitStart = source.indexOf(
      '      // Refresh commit count and branch info after committing'
    )
    const branchInfoRefreshStart = source.indexOf(
      '      useGitStore.getState().loadBranchInfo(worktreePath)',
      postCommitStart
    )
    const postCommitSource = source.slice(postCommitStart, branchInfoRefreshStart)

    expect(postCommitStart).toBeGreaterThan(-1)
    expect(branchInfoRefreshStart).toBeGreaterThan(postCommitStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(postCommitSource).toContain('gitApi')
    expect(postCommitSource).toContain('.getRangeDiff(worktreePath, baseBranch)')
    expect(postCommitSource).toContain('setCommitCount(rd.commitCount)')
    expect(postCommitSource).not.toContain('window.gitOps.getRangeDiff')
    expect(postCommitSource).not.toContain('unwrapEnvelope')
  })

  it('routes CreatePRModal push preflight through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/pr/CreatePRModal.tsx'),
      'utf-8'
    )
    const preflightStart = source.indexOf('      // Step 1: Push if needed')
    const generateStart = source.indexOf(
      '      // Step 2: Generate content if needed',
      preflightStart
    )
    const preflightSource = source.slice(preflightStart, generateStart)

    expect(preflightStart).toBeGreaterThan(-1)
    expect(generateStart).toBeGreaterThan(preflightStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(preflightSource).toContain('gitApi')
    expect(preflightSource).toContain('willPush = await gitApi.needsPush(worktreePath)')
    expect(preflightSource).toContain('if (willPush)')
    expect(preflightSource).not.toContain('window.gitOps.needsPush')
    expect(preflightSource).not.toContain('unwrapEnvelope(await window.gitOps.needsPush')
  })

  it('routes CreatePRModal push execution through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/pr/CreatePRModal.tsx'),
      'utf-8'
    )
    const pushStart = source.indexOf('      if (willPush) {')
    const generateStart = source.indexOf('      // Step 2: Generate content if needed', pushStart)
    const pushSource = source.slice(pushStart, generateStart)

    expect(pushStart).toBeGreaterThan(-1)
    expect(generateStart).toBeGreaterThan(pushStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(pushSource).toContain('gitApi')
    expect(pushSource).toContain("update(notifId, { message: 'Pushing branch...' })")
    expect(pushSource).toContain('const pushResult = await gitApi.push(worktreePath)')
    expect(pushSource).toContain("throw new Error(pushResult.error ?? 'Push failed')")
    expect(pushSource).not.toContain('window.gitOps.push')
    expect(pushSource).not.toContain('unwrapEnvelope(await window.gitOps.push')
  })

  it('routes CreatePRModal PR content generation through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/pr/CreatePRModal.tsx'),
      'utf-8'
    )
    const generationStart = source.indexOf(
      "        update(notifId, { message: 'Generating PR content...' })"
    )
    const fallbackStart = source.indexOf(
      '        // Fallback if generation failed or returned empty',
      generationStart
    )
    const generationSource = source.slice(generationStart, fallbackStart)

    expect(generationStart).toBeGreaterThan(-1)
    expect(fallbackStart).toBeGreaterThan(generationStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(generationSource).toContain('gitApi')
    expect(generationSource).toContain(
      'const genResult = await gitApi.generatePRContent(worktreePath, targetBase, provider)'
    )
    expect(generationSource).toContain('if (genResult.success)')
    expect(generationSource).toContain('usedFallbackContent = true')
    expect(generationSource).not.toContain('window.gitOps.generatePRContent')
    expect(generationSource).not.toContain('unwrapEnvelope')
  })

  it('routes CreatePRModal PR creation through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/pr/CreatePRModal.tsx'),
      'utf-8'
    )
    const createStart = source.indexOf('      // Step 3: Create PR')
    const failureStart = source.indexOf('      if (!createResult.success)', createStart)
    const createSource = source.slice(createStart, failureStart)

    expect(createStart).toBeGreaterThan(-1)
    expect(failureStart).toBeGreaterThan(createStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(createSource).toContain('gitApi')
    expect(createSource).toContain("update(notifId, { message: 'Creating pull request...' })")
    expect(createSource).toContain(
      'const createResult = await gitApi.createPR(worktreePath, targetBase, finalTitle, finalBody)'
    )
    expect(createSource).not.toContain('window.gitOps.createPR')
    expect(createSource).not.toContain('unwrapEnvelope')
  })

  it('routes CreatePRModal duplicate PR title lookup through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/pr/CreatePRModal.tsx'),
      'utf-8'
    )
    const lookupStart = source.indexOf('            if (projectPath) {')
    const lookupEnd = source.indexOf('          } catch {', lookupStart)
    const lookupSource = source.slice(lookupStart, lookupEnd)

    expect(lookupStart).toBeGreaterThan(-1)
    expect(lookupEnd).toBeGreaterThan(lookupStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(lookupSource).toContain('gitApi')
    expect(lookupSource).toContain(
      'const state = await gitApi.getPRState(projectPath, existingNumber)'
    )
    expect(lookupSource).toContain('if (state.success) existingTitle = state.title')
    expect(lookupSource).not.toContain('window.gitOps.getPRState')
    expect(lookupSource).not.toContain('unwrapEnvelope')
  })

  it('routes PRNotificationStack merge action through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/pr/PRNotificationStack.tsx'),
      'utf-8'
    )
    const mergeStart = source.indexOf('  const handleMerge = useCallback(async () => {')
    const archiveStart = source.indexOf(
      '  const handleArchive = useCallback(async () => {',
      mergeStart
    )
    const mergeSource = source.slice(mergeStart, archiveStart)

    expect(mergeStart).toBeGreaterThan(-1)
    expect(archiveStart).toBeGreaterThan(mergeStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(mergeSource).toContain('gitApi')
    expect(mergeSource).toContain('const result = await gitApi.prMerge(worktreePath, prNumber)')
    expect(mergeSource).toContain('toast.error(`Merge failed: ${result.error}`)')
    expect(mergeSource).toContain("toast.error('Failed to merge PR')")
    expect(mergeSource).not.toContain('window.gitOps.prMerge')
    expect(mergeSource).not.toContain('unwrapEnvelope')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('routes useLifecycleActions PR merge through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useLifecycleActions.ts'),
      'utf-8'
    )
    const mergeStart = source.indexOf(
      '  const mergePR = useCallback(async (): Promise<boolean> => {'
    )
    const archiveStart = source.indexOf(
      '  const archiveWorktreeAction = useCallback(async (): Promise<boolean> => {',
      mergeStart
    )
    const mergeSource = source.slice(mergeStart, archiveStart)

    expect(mergeStart).toBeGreaterThan(-1)
    expect(archiveStart).toBeGreaterThan(mergeStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(mergeSource).toContain('gitApi')
    expect(mergeSource).toContain('const result = await gitApi.prMerge(worktree.path, pr.number)')
    expect(mergeSource).toContain("toast.success('PR merged successfully')")
    expect(mergeSource).toContain('toast.error(`Merge failed: ${result.error}`)')
    expect(mergeSource).toContain('setIsMergingPR(false)')
    expect(mergeSource).not.toContain('window.gitOps.prMerge')
    expect(mergeSource).not.toContain('unwrapEnvelope')
  })

  it('routes useLifecycleActions PR list loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useLifecycleActions.ts'),
      'utf-8'
    )
    const listStart = source.indexOf(
      '  const loadPRList = useCallback(async (): Promise<PRListItem[]> => {'
    )
    const stateStart = source.indexOf(
      '  const loadPRState = useCallback(async (): Promise<void> => {',
      listStart
    )
    const listSource = source.slice(listStart, stateStart)

    expect(listStart).toBeGreaterThan(-1)
    expect(stateStart).toBeGreaterThan(listStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(listSource).toContain('gitApi')
    expect(listSource).toContain('const res = await gitApi.listPRs(projectPath)')
    expect(listSource).toContain('const sorted = [...res.prs].sort((a, b) => {')
    expect(listSource).toContain("toast.error(res.error || 'Failed to load PRs')")
    expect(listSource).toContain("toast.error('Failed to load PRs')")
    expect(listSource).not.toContain('window.gitOps.listPRs')
    expect(listSource).not.toContain('unwrapEnvelope')
  })

  it('routes AttachPRPopover PR list loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/AttachPRPopover.tsx'),
      'utf-8'
    )
    const listStart = source.indexOf('  // ── Load PR list on open')
    const focusStart = source.indexOf('  // ── Auto-focus input when open', listStart)
    const listSource = source.slice(listStart, focusStart)

    expect(listStart).toBeGreaterThan(-1)
    expect(focusStart).toBeGreaterThan(listStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(listSource).toContain('gitApi')
    expect(listSource).toContain('.listPRs(projectPath)')
    expect(listSource).toContain('const sorted = [...result.prs].sort((a, b) => {')
    expect(listSource).toContain("setError(result.error ?? 'Failed to load PRs')")
    expect(listSource).toContain("setError('Failed to load PRs')")
    expect(listSource).toContain('setIsLoading(false)')
    expect(listSource).not.toContain('window.gitOps.listPRs')
    expect(listSource).not.toContain('unwrapEnvelope')
  })

  it('routes useLifecycleActions PR state loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/hooks/useLifecycleActions.ts'),
      'utf-8'
    )
    const stateStart = source.indexOf(
      '  const loadPRState = useCallback(async (): Promise<void> => {'
    )
    const setterStart = source.indexOf('  const setPrTargetBranchAction = useCallback(', stateStart)
    const stateSource = source.slice(stateStart, setterStart)

    expect(stateStart).toBeGreaterThan(-1)
    expect(setterStart).toBeGreaterThan(stateStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(stateSource).toContain('gitApi')
    expect(stateSource).toContain('const res = await gitApi.getPRState(projectPath, pr.number)')
    expect(stateSource).toContain('setPrLiveState({ state: res.state, title: res.title })')
    expect(stateSource).toContain('// non-critical')
    expect(stateSource).not.toContain('window.gitOps.getPRState')
    expect(stateSource).not.toContain('unwrapEnvelope')
  })

  it('routes AttachPRPopover numeric PR state lookup through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/AttachPRPopover.tsx'),
      'utf-8'
    )
    const lookupStart = source.indexOf('  // ── Debounced getPRState for numeric input')
    const filterStart = source.indexOf('  // ── Filtered PR list', lookupStart)
    const lookupSource = source.slice(lookupStart, filterStart)

    expect(lookupStart).toBeGreaterThan(-1)
    expect(filterStart).toBeGreaterThan(lookupStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(lookupSource).toContain('gitApi')
    expect(lookupSource).toContain('const result = await gitApi.getPRState(projectPath, prNumber)')
    expect(lookupSource).toContain(
      'setLookedUpPR({ number: prNumber, title: result.title, state: result.state })'
    )
    expect(lookupSource).toContain('setIsLookingUp(false)')
    expect(lookupSource).not.toContain('window.gitOps.getPRState')
    expect(lookupSource).not.toContain('unwrapEnvelope')
  })

  it('routes AttachPRPopover PR attach through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/AttachPRPopover.tsx'),
      'utf-8'
    )
    const attachStart = source.indexOf(
      '  const handleAttach = async (pr: { number: number; title: string }) => {'
    )
    const detachStart = source.indexOf('  const handleDetach = async () => {', attachStart)
    const attachSource = source.slice(attachStart, detachStart)

    expect(attachStart).toBeGreaterThan(-1)
    expect(detachStart).toBeGreaterThan(attachStart)
    expect(source).toContain("import { kanbanApi } from '@/api/kanban-api'")
    expect(attachSource).toContain(
      'useKanbanStore.getState().attachPRToTicket(ticket.id, ticket.project_id, pr.number, prUrl)'
    )
    expect(attachSource).toContain(
      'await kanbanApi.ticket.attachPR(ticket.id, ticket.project_id, pr.number, prUrl)'
    )
    expect(attachSource).toContain('toast.success(`PR #${pr.number} attached`)')
    expect(attachSource).toContain(
      'useKanbanStore.getState().detachPRFromTicket(ticket.id, ticket.project_id)'
    )
    expect(attachSource).not.toContain('kanban.ticket.attachPR')
  })

  it('removes the old kanban:ticket:attachPR IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("'kanban:ticket:attachPR'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:attachPR'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:attachPR'")
  })

  it('routes AttachPRPopover PR detach through kanbanApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/AttachPRPopover.tsx'),
      'utf-8'
    )
    const detachStart = source.indexOf('  const handleDetach = async () => {')
    const renderStart = source.indexOf('  // ── Render', detachStart)
    const detachSource = source.slice(detachStart, renderStart)

    expect(detachStart).toBeGreaterThan(-1)
    expect(renderStart).toBeGreaterThan(detachStart)
    expect(source).toContain("import { kanbanApi } from '@/api/kanban-api'")
    expect(source).not.toContain("import { unwrapEnvelopeApi } from '@/lib/ipc-envelope'")
    expect(source).not.toContain('const kanban = unwrapEnvelopeApi(() => window.kanban)')
    expect(detachSource).toContain(
      'useKanbanStore.getState().detachPRFromTicket(ticket.id, ticket.project_id)'
    )
    expect(detachSource).toContain('await kanbanApi.ticket.detachPR(ticket.id, ticket.project_id)')
    expect(detachSource).toContain("toast.success('PR detached')")
    expect(detachSource).toContain(
      '.attachPRToTicket(ticket.id, ticket.project_id, prev.number, prev.url)'
    )
    expect(detachSource).not.toContain('kanban.ticket.detachPR')
  })

  it('removes the old kanban:ticket:detachPR IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const kanbanHandlersSource = readOptionalSource('../../src/main/ipc/kanban-handlers.ts')

    expect(preloadSource).not.toContain("'kanban:ticket:detachPR'")
    expect(kanbanHandlersSource).not.toContain("defineHandler('kanban:ticket:detachPR'")
    expect(kanbanHandlersSource).not.toContain("tryKanban('kanban:ticket:detachPR'")
  })

  it('routes usePRReviewStore comment loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/usePRReviewStore.ts'),
      'utf-8'
    )
    const fetchStart = source.indexOf(
      '  fetchComments: async (worktreeId, projectPath, prNumber) => {'
    )
    const clearStart = source.indexOf('  clearComments: (worktreeId) => {', fetchStart)
    const fetchSource = source.slice(fetchStart, clearStart)

    expect(fetchStart).toBeGreaterThan(-1)
    expect(clearStart).toBeGreaterThan(fetchStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(fetchSource).toContain('gitApi')
    expect(fetchSource).toContain(
      'const result = await gitApi.getPRReviewComments(projectPath, prNumber)'
    )
    expect(fetchSource).toContain('comments.set(worktreeId, result.comments!)')
    expect(fetchSource).toContain(
      'if (result.baseBranch) baseBranch.set(worktreeId, result.baseBranch)'
    )
    expect(fetchSource).toContain(
      "error.set(worktreeId, result.error ?? 'Failed to fetch comments')"
    )
    expect(fetchSource).not.toContain('window.gitOps.getPRReviewComments')
    expect(fetchSource).not.toContain('unwrapEnvelope')
  })

  it('routes FileContextMenu open-in-editor through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/file-tree/FileContextMenu.tsx'),
      'utf-8'
    )
    const openStart = source.indexOf('  const handleOpenInEditor = useCallback(async () => {')
    const finderStart = source.indexOf(
      '  const handleOpenInFinder = useCallback(async () => {',
      openStart
    )
    const openSource = source.slice(openStart, finderStart)

    expect(openStart).toBeGreaterThan(-1)
    expect(finderStart).toBeGreaterThan(openStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(openSource).toContain('gitApi')
    expect(openSource).toContain('await gitApi.openInEditor(node.path)')
    expect(openSource).toContain('onClose?.()')
    expect(openSource).not.toContain('window.gitOps.openInEditor')
    expect(openSource).not.toContain('unwrapEnvelope')
  })

  it('routes FileContextMenu show-in-finder through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/file-tree/FileContextMenu.tsx'),
      'utf-8'
    )
    const finderStart = source.indexOf('  const handleOpenInFinder = useCallback(async () => {')
    const copyStart = source.indexOf(
      '  const handleCopyPath = useCallback(async () => {',
      finderStart
    )
    const finderSource = source.slice(finderStart, copyStart)

    expect(finderStart).toBeGreaterThan(-1)
    expect(copyStart).toBeGreaterThan(finderStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(finderSource).toContain('gitApi')
    expect(finderSource).toContain('await gitApi.showInFinder(node.path)')
    expect(finderSource).toContain('onClose?.()')
    expect(finderSource).not.toContain('window.gitOps.showInFinder')
    expect(finderSource).not.toContain('unwrapEnvelope')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('routes BranchPickerDialog branch loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/worktrees/BranchPickerDialog.tsx'),
      'utf-8'
    )
    const branchFetchStart = source.indexOf('  // Fetch branches when dialog opens')
    const prFetchStart = source.indexOf(
      '  // Fetch PRs when PRs tab is selected (lazy)',
      branchFetchStart
    )
    const branchFetchSource = source.slice(branchFetchStart, prFetchStart)

    expect(branchFetchStart).toBeGreaterThan(-1)
    expect(prFetchStart).toBeGreaterThan(branchFetchStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(branchFetchSource).toContain('gitApi')
    expect(branchFetchSource).toContain('.listBranchesWithStatus(projectPath)')
    expect(branchFetchSource).toContain('setBranches(result.branches)')
    expect(branchFetchSource).toContain("setError(result.error || 'Failed to load branches')")
    expect(branchFetchSource).toContain('setLoading(false)')
    expect(branchFetchSource).not.toContain('window.gitOps.listBranchesWithStatus')
    expect(branchFetchSource).not.toContain('unwrapEnvelope')
  })

  it('routes BranchPickerDialog PR list loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/worktrees/BranchPickerDialog.tsx'),
      'utf-8'
    )
    const prFetchStart = source.indexOf('  // Fetch PRs when PRs tab is selected (lazy)')
    const filterStart = source.indexOf('  // Filter and sort branches', prFetchStart)
    const prFetchSource = source.slice(prFetchStart, filterStart)

    expect(prFetchStart).toBeGreaterThan(-1)
    expect(filterStart).toBeGreaterThan(prFetchStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(prFetchSource).toContain("if (!open || activeTab !== 'prs') return")
    expect(prFetchSource).toContain('gitApi')
    expect(prFetchSource).toContain('.listPRs(projectPath)')
    expect(prFetchSource).toContain('setPrs(result.prs)')
    expect(prFetchSource).toContain("setPrsError(result.error || 'Failed to load pull requests')")
    expect(prFetchSource).toContain('setPrsLoading(false)')
    expect(prFetchSource).not.toContain('window.gitOps.listPRs')
    expect(prFetchSource).not.toContain('unwrapEnvelope')
    expect(source).not.toContain("import { unwrapEnvelope } from '@/lib/ipc-envelope'")
  })

  it('routes WorktreePickerModal lazy branch loading through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/WorktreePickerModal.tsx'),
      'utf-8'
    )
    const lazyBranchStart = source.indexOf('  // ── Lazy branch loading')
    const resetStart = source.indexOf('  // ── Reset state when modal opens', lazyBranchStart)
    const lazyBranchSource = source.slice(lazyBranchStart, resetStart)

    expect(lazyBranchStart).toBeGreaterThan(-1)
    expect(resetStart).toBeGreaterThan(lazyBranchStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(lazyBranchSource).toContain('gitApi')
    expect(lazyBranchSource).toContain('.listBranchesWithStatus(project.path)')
    expect(lazyBranchSource).toContain('setBranches(result.branches)')
    expect(lazyBranchSource).toContain('setSourceBranch(null)')
    expect(lazyBranchSource).toContain('setBranchesLoading(false)')
    expect(lazyBranchSource).not.toContain('window.gitOps.listBranchesWithStatus')
    expect(lazyBranchSource).not.toContain('unwrapEnvelope')
  })

  it('routes GitPushPull archive dirty check through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/git/GitPushPull.tsx'),
      'utf-8'
    )
    const archiveStart = source.indexOf('  const handleArchiveWorktree = useCallback(async () => {')
    const confirmStart = source.indexOf('  const handleArchiveConfirm = useCallback', archiveStart)
    const archiveSource = source.slice(archiveStart, confirmStart)

    expect(archiveStart).toBeGreaterThan(-1)
    expect(confirmStart).toBeGreaterThan(archiveStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(archiveSource).toContain(
      'const result = await gitApi.getDiffStat(selectedBranchInfo.worktreePath)'
    )
    expect(archiveSource).toContain('setArchiveConfirmFiles(result.files)')
    expect(archiveSource).toContain('setArchiveConfirmOpen(true)')
    expect(archiveSource).not.toContain('window.gitOps.getDiffStat')
    expect(archiveSource).not.toContain('unwrapEnvelope')
  })

  it('routes GitPushPull merge-confirm dirty check through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/git/GitPushPull.tsx'),
      'utf-8'
    )
    const mergeConfirmStart = source.indexOf('  const handleMerge = useCallback(async () => {')
    const doMergeStart = source.indexOf('    doMerge()', mergeConfirmStart)
    const mergeConfirmSource = source.slice(mergeConfirmStart, doMergeStart)

    expect(mergeConfirmStart).toBeGreaterThan(-1)
    expect(doMergeStart).toBeGreaterThan(mergeConfirmStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(mergeConfirmSource).toContain(
      'const result = await gitApi.getDiffStat(selectedBranchInfo.worktreePath)'
    )
    expect(mergeConfirmSource).toContain('setMergeConfirmFiles(result.files)')
    expect(mergeConfirmSource).toContain('setMergeConfirmOpen(true)')
    expect(mergeConfirmSource).not.toContain('window.gitOps.getDiffStat')
    expect(mergeConfirmSource).not.toContain('unwrapEnvelope')
  })

  it('routes CreatePRModal uncommitted preflight through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/pr/CreatePRModal.tsx'),
      'utf-8'
    )
    const preflightStart = source.indexOf(
      '// Check for uncommitted changes — show commit phase if any'
    )
    const effectEnd = source.indexOf(
      '  }, [open, worktreePath, prTargetBranch, loadFileStatuses, sessionTitles])',
      preflightStart
    )
    const preflightSource = source.slice(preflightStart, effectEnd)

    expect(preflightStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(preflightStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(preflightSource).toContain('gitApi.hasUncommittedChanges(worktreePath)')
    expect(preflightSource).toContain('loadFileStatuses(worktreePath)')
    expect(preflightSource).toContain("if (hasUncommitted) setPhase('commit')")
    expect(preflightSource).not.toContain('window.gitOps.hasUncommittedChanges')
    expect(preflightSource).not.toContain('unwrapEnvelope')
  })

  it('routes MergeOnDoneDialog feature post-commit branch diff through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const handleCommitStart = source.indexOf('const handleCommit = useCallback(async () => {')
    const baseCommitStart = source.indexOf(
      'const handleCommitBase = useCallback(async () => {',
      handleCommitStart
    )
    const handleCommitSource = source.slice(handleCommitStart, baseCommitStart)

    expect(handleCommitStart).toBeGreaterThan(-1)
    expect(baseCommitStart).toBeGreaterThan(handleCommitStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(handleCommitSource).toContain('const statResult = await gitApi.branchDiffShortStat(')
    expect(handleCommitSource).toContain('resolved.featureWorktreePath,')
    expect(handleCommitSource).toContain('resolved.baseBranch')
    expect(handleCommitSource).toContain(
      "toast.warning(`Cannot verify merge status: ${statResult.error ?? 'unknown error'}`)"
    )
    expect(handleCommitSource).toContain('if (statResult.commitsAhead > 0)')
    expect(handleCommitSource).not.toContain('window.gitOps.branchDiffShortStat')
    expect(handleCommitSource).not.toContain(
      'unwrapEnvelope(await window.gitOps.branchDiffShortStat'
    )
  })

  it('routes MergeOnDoneDialog base commit stage-all through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const baseCommitStart = source.indexOf('const handleCommitBase = useCallback(async () => {')
    const mergeStart = source.indexOf(
      'const handleMerge = useCallback(async () => {',
      baseCommitStart
    )
    const baseCommitSource = source.slice(baseCommitStart, mergeStart)

    expect(baseCommitStart).toBeGreaterThan(-1)
    expect(mergeStart).toBeGreaterThan(baseCommitStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(baseCommitSource).toContain(
      'const stageResult = await gitApi.stageAll(resolved.baseWorktreePath)'
    )
    expect(baseCommitSource).toContain(
      'toast.error(`Failed to stage on ${resolved.baseBranch}: ${stageResult.error}`)'
    )
    expect(baseCommitSource).not.toContain('window.gitOps.stageAll')
    expect(baseCommitSource).not.toContain('unwrapEnvelope(await window.gitOps.stageAll')
  })

  it('routes MergeOnDoneDialog base commit through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const baseCommitStart = source.indexOf('const handleCommitBase = useCallback(async () => {')
    const mergeStart = source.indexOf(
      'const handleMerge = useCallback(async () => {',
      baseCommitStart
    )
    const baseCommitSource = source.slice(baseCommitStart, mergeStart)

    expect(baseCommitStart).toBeGreaterThan(-1)
    expect(mergeStart).toBeGreaterThan(baseCommitStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(baseCommitSource).toContain('const commitResult = await gitApi.commit(')
    expect(baseCommitSource).toContain('resolved.baseWorktreePath,')
    expect(baseCommitSource).toContain('baseCommitMessage.trim()')
    expect(baseCommitSource).toContain(
      'toast.error(`Failed to commit on ${resolved.baseBranch}: ${commitResult.error}`)'
    )
    expect(baseCommitSource).toContain(
      'toast.success(`Changes committed on ${resolved.baseBranch}`)'
    )
    expect(baseCommitSource).not.toContain('window.gitOps.commit')
    expect(baseCommitSource).not.toContain('unwrapEnvelope(await window.gitOps.commit')
  })

  it('routes MergeOnDoneDialog base post-commit dirty check through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const baseCommitStart = source.indexOf('const handleCommitBase = useCallback(async () => {')
    const mergeStart = source.indexOf(
      'const handleMerge = useCallback(async () => {',
      baseCommitStart
    )
    const baseCommitSource = source.slice(baseCommitStart, mergeStart)

    expect(baseCommitStart).toBeGreaterThan(-1)
    expect(mergeStart).toBeGreaterThan(baseCommitStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(baseCommitSource).toContain(
      'const featureHasUncommitted = await gitApi.hasUncommittedChanges('
    )
    expect(baseCommitSource).toContain('resolved.featureWorktreePath')
    expect(baseCommitSource).toContain("setStep('commit')")
    expect(baseCommitSource).toContain('const statResult = await gitApi.branchDiffShortStat(')
    expect(baseCommitSource).not.toContain('window.gitOps.hasUncommittedChanges')
    expect(baseCommitSource).not.toContain(
      'unwrapEnvelope(await window.gitOps.hasUncommittedChanges'
    )
  })

  it('routes MergeOnDoneDialog base post-commit branch diff through gitApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/MergeOnDoneDialog.tsx'),
      'utf-8'
    )
    const baseCommitStart = source.indexOf('const handleCommitBase = useCallback(async () => {')
    const mergeStart = source.indexOf(
      'const handleMerge = useCallback(async () => {',
      baseCommitStart
    )
    const baseCommitSource = source.slice(baseCommitStart, mergeStart)

    expect(baseCommitStart).toBeGreaterThan(-1)
    expect(mergeStart).toBeGreaterThan(baseCommitStart)
    expect(source).toContain("import { gitApi } from '@/api/git-api'")
    expect(baseCommitSource).toContain(
      'const featureHasUncommitted = await gitApi.hasUncommittedChanges('
    )
    expect(baseCommitSource).toContain('const statResult = await gitApi.branchDiffShortStat(')
    expect(baseCommitSource).toContain('resolved.featureWorktreePath,')
    expect(baseCommitSource).toContain('resolved.baseBranch')
    expect(baseCommitSource).toContain(
      "toast.warning(`Cannot verify merge status: ${statResult.error ?? 'unknown error'}`)"
    )
    expect(baseCommitSource).toContain('if (statResult.commitsAhead > 0)')
    expect(baseCommitSource).not.toContain('window.gitOps.branchDiffShortStat')
    expect(baseCommitSource).not.toContain('unwrapEnvelope(await window.gitOps.branchDiffShortStat')
  })

  it('routes kanban ticket follow-up prompt dispatch through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const followupStart = source.indexOf('async function sendFollowupToSession(')
    const modalModeStart = source.indexOf('/** Determine what mode the modal should operate in */')
    const followupSource = source.slice(followupStart, modalModeStart)

    expect(followupStart).toBeGreaterThan(-1)
    expect(modalModeStart).toBeGreaterThan(followupStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(followupSource).toContain('const messageParts = opts.attachments?.length')
    expect(followupSource).toContain(
      'await opencodeApi.prompt(workingPath, session.opencode_session_id, messageParts, model)'
    )
    expect(followupSource).toContain('promptResult && !promptResult.success')
    expect(followupSource).not.toContain('window.opencodeOps.prompt')
  })

  it('routes kanban ticket follow-up reconnect through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const followupStart = source.indexOf('async function sendFollowupToSession(')
    const modalModeStart = source.indexOf('/** Determine what mode the modal should operate in */')
    const followupSource = source.slice(followupStart, modalModeStart)

    expect(followupStart).toBeGreaterThan(-1)
    expect(modalModeStart).toBeGreaterThan(followupStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(followupSource).toContain(
      'await opencodeApi.reconnect(workingPath, session.opencode_session_id, opts.sessionId)'
    )
    expect(followupSource).toContain('const messageParts = opts.attachments?.length')
    expect(followupSource).toContain(
      'await opencodeApi.prompt(workingPath, session.opencode_session_id, messageParts, model)'
    )
    expect(followupSource).not.toContain('window.opencodeOps.reconnect')
  })

  it('routes kanban ticket handoff abort cleanup through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const handoffStart = source.indexOf('const handleHandoff = useCallback')
    const superchargeStart = source.indexOf('const handleSupercharge = useCallback', handoffStart)
    const handoffSource = source.slice(handoffStart, superchargeStart)

    expect(handoffStart).toBeGreaterThan(-1)
    expect(superchargeStart).toBeGreaterThan(handoffStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(handoffSource).toContain(
      'unwrapEnvelope(await opencodeApi.abort(worktreePath, opcSessionId))'
    )
    expect(handoffSource).not.toContain(
      'unwrapEnvelope(await window.opencodeOps.abort(worktreePath, opcSessionId))'
    )
  })

  it('routes kanban ticket eager supercharge connect through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const superchargeStart = source.indexOf('const eagerSuperchargeStart = useCallback')
    const handoffStart = source.indexOf('const eagerHandoffStart = useCallback', superchargeStart)
    const superchargeSource = source.slice(superchargeStart, handoffStart)

    expect(superchargeStart).toBeGreaterThan(-1)
    expect(handoffStart).toBeGreaterThan(superchargeStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(superchargeSource).toContain(
      'const connectResult = unwrapEnvelope(await opencodeApi.connect(worktreePath, newSessionId))'
    )
    expect(superchargeSource).toContain('!connectResult.success || !connectResult.sessionId')
    expect(superchargeSource).toContain(
      'useSessionStore.getState().setOpenCodeSessionId(newSessionId, connectResult.sessionId)'
    )
    expect(superchargeSource).toContain('opencode_session_id: connectResult.sessionId')
    expect(superchargeSource).toContain('setPendingFollowUpMessages(newSessionId')
    expect(superchargeSource).toContain("'/using-superpowers'")
    expect(superchargeSource).not.toContain('window.opencodeOps.connect')
  })

  it('routes kanban ticket eager supercharge prompt through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const superchargeStart = source.indexOf('const eagerSuperchargeStart = useCallback')
    const handoffStart = source.indexOf('const eagerHandoffStart = useCallback', superchargeStart)
    const superchargeSource = source.slice(superchargeStart, handoffStart)

    expect(superchargeStart).toBeGreaterThan(-1)
    expect(handoffStart).toBeGreaterThan(superchargeStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(superchargeSource).toContain('setPendingFollowUpMessages(newSessionId')
    expect(superchargeSource).toContain('const model = resolveSessionModel(newSessionId)')
    expect(superchargeSource).toContain('await opencodeApi.prompt(')
    expect(superchargeSource).toContain('worktreePath')
    expect(superchargeSource).toContain('connectResult.sessionId')
    expect(superchargeSource).toContain("[{ type: 'text', text: '/using-superpowers' }]")
    expect(superchargeSource).toContain('model')
    expect(superchargeSource).not.toContain('window.opencodeOps.prompt')
  })

  it('routes kanban ticket eager handoff connect through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const handoffStart = source.indexOf('const eagerHandoffStart = useCallback')
    const superchargeHandlerStart = source.indexOf(
      'const handleSupercharge = useCallback',
      handoffStart
    )
    const handoffSource = source.slice(handoffStart, superchargeHandlerStart)

    expect(handoffStart).toBeGreaterThan(-1)
    expect(superchargeHandlerStart).toBeGreaterThan(handoffStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(handoffSource).toContain(
      'const connectResult = unwrapEnvelope(await opencodeApi.connect(workingPath, newSessionId))'
    )
    expect(handoffSource).toContain('!connectResult.success || !connectResult.sessionId')
    expect(handoffSource).toContain(
      'useSessionStore.getState().setOpenCodeSessionId(newSessionId, connectResult.sessionId)'
    )
    expect(handoffSource).toContain('opencode_session_id: connectResult.sessionId')
    expect(handoffSource).toContain('lastSendMode.set(newSessionId,')
    expect(handoffSource).toContain('bumpWorktreeLastMessage(bumpTarget)')
    expect(handoffSource).toContain('handoffPrompt')
    expect(handoffSource).not.toContain('window.opencodeOps.connect')
  })

  it('routes kanban ticket eager handoff prompt through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const handoffStart = source.indexOf('const eagerHandoffStart = useCallback')
    const superchargeHandlerStart = source.indexOf(
      'const handleSupercharge = useCallback',
      handoffStart
    )
    const handoffSource = source.slice(handoffStart, superchargeHandlerStart)

    expect(handoffStart).toBeGreaterThan(-1)
    expect(superchargeHandlerStart).toBeGreaterThan(handoffStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(handoffSource).toContain('messageSendTimes.set(newSessionId')
    expect(handoffSource).toContain("lastSendMode.set(newSessionId, 'build')")
    expect(handoffSource).toContain('const model = resolveSessionModel(newSessionId)')
    expect(handoffSource).toContain('await opencodeApi.prompt(')
    expect(handoffSource).toContain('workingPath')
    expect(handoffSource).toContain('connectResult.sessionId')
    expect(handoffSource).toContain("[{ type: 'text', text: handoffPrompt }]")
    expect(handoffSource).toContain('model')
    expect(handoffSource).not.toContain('window.opencodeOps.prompt')
  })

  it('routes kanban ticket supercharge abort cleanup through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const superchargeStart = source.indexOf('const handleSupercharge = useCallback')
    const localSuperchargeStart = source.indexOf(
      'const handleSuperchargeLocal = useCallback',
      superchargeStart
    )
    const superchargeSource = source.slice(superchargeStart, localSuperchargeStart)

    expect(superchargeStart).toBeGreaterThan(-1)
    expect(localSuperchargeStart).toBeGreaterThan(superchargeStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(superchargeSource).toContain(
      'unwrapEnvelope(await opencodeApi.abort(worktreePath, opcSessionId))'
    )
    expect(superchargeSource).not.toContain(
      'unwrapEnvelope(await window.opencodeOps.abort(worktreePath, opcSessionId))'
    )
  })

  it('routes kanban ticket local supercharge abort cleanup through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const localSuperchargeStart = source.indexOf('const handleSuperchargeLocal = useCallback')
    const renderStart = source.indexOf('return (', localSuperchargeStart)
    const localSuperchargeSource = source.slice(localSuperchargeStart, renderStart)

    expect(localSuperchargeStart).toBeGreaterThan(-1)
    expect(renderStart).toBeGreaterThan(localSuperchargeStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(localSuperchargeSource).toContain(
      'unwrapEnvelope(await opencodeApi.abort(worktreePath, opcSessionId))'
    )
    expect(localSuperchargeSource).not.toContain(
      'unwrapEnvelope(await window.opencodeOps.abort(worktreePath, opcSessionId))'
    )
  })

  it('routes kanban ticket plan approval through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const implementStart = source.indexOf('const handleImplement = useCallback')
    const approvalStart = source.indexOf(
      '// Claude Code sessions approve the real pending plan request.',
      implementStart
    )
    const successStart = source.indexOf("toast.success('Implementation started')", approvalStart)
    const implementSource = source.slice(implementStart, successStart)

    expect(implementStart).toBeGreaterThan(-1)
    expect(approvalStart).toBeGreaterThan(implementStart)
    expect(successStart).toBeGreaterThan(implementStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(implementSource).toContain(
      'await opencodeApi.planApprove(approvePath, sessionId, pendingBeforeAction.requestId)'
    )
    expect(implementSource).toContain('const pendingBeforeAction = pendingPlan')
    expect(implementSource).toContain('findWorktreePathById(sessionRecord.worktree_id)')
    expect(implementSource).not.toContain('window.opencodeOps.planApprove')
  })

  it('routes kanban ticket question replies through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const replyStart = source.indexOf('const handleReply = useCallback')
    const rejectStart = source.indexOf('const handleReject = useCallback', replyStart)
    const replySource = source.slice(replyStart, rejectStart)

    expect(replyStart).toBeGreaterThan(-1)
    expect(rejectStart).toBeGreaterThan(replyStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(replySource).toContain('await opencodeApi.questionReply(')
    expect(replySource).toContain('requestId')
    expect(replySource).toContain('answers')
    expect(replySource).toContain('questionPath || undefined')
    expect(replySource).toContain('findWorktreePathById(ticket.worktree_id)')
    expect(replySource).toContain('findSessionById(ticket.current_session_id)')
    expect(replySource).toContain('unwrapEnvelope(')
    expect(replySource).not.toContain('window.opencodeOps.questionReply')
  })

  it('routes kanban ticket question rejections through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const rejectStart = source.indexOf('const handleReject = useCallback')
    const modeContentEnd = source.indexOf('return (', rejectStart)
    const rejectSource = source.slice(rejectStart, modeContentEnd)

    expect(rejectStart).toBeGreaterThan(-1)
    expect(modeContentEnd).toBeGreaterThan(rejectStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(rejectSource).toContain('await opencodeApi.questionReject(')
    expect(rejectSource).toContain('requestId')
    expect(rejectSource).toContain('questionPath || undefined')
    expect(rejectSource).toContain('findWorktreePathById(ticket.worktree_id)')
    expect(rejectSource).toContain('findSessionById(ticket.current_session_id)')
    expect(rejectSource).toContain('unwrapEnvelope(')
    expect(rejectSource).not.toContain('window.opencodeOps.questionReject')
  })

  it('routes session plan-ready handoff abort cleanup through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const handoffStart = source.indexOf('const handlePlanReadyHandoff = useCallback')
    const copyPlanStart = source.indexOf(
      'const handlePlanReadyCopyPlan = useCallback',
      handoffStart
    )
    const handoffSource = source.slice(handoffStart, copyPlanStart)

    expect(handoffStart).toBeGreaterThan(-1)
    expect(copyPlanStart).toBeGreaterThan(handoffStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(handoffSource).toContain(
      'unwrapEnvelope(await opencodeApi.abort(worktreePath, opencodeSessionId))'
    )
    expect(handoffSource).not.toContain(
      'unwrapEnvelope(await window.opencodeOps.abort(worktreePath, opencodeSessionId))'
    )
  })

  it('routes session plan-ready superpowers abort cleanup through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const superpowersStart = source.indexOf('const handlePlanReadySuperpowers = useCallback')
    const localSuperpowersStart = source.indexOf(
      'const handlePlanReadySuperpowersLocal = useCallback',
      superpowersStart
    )
    const superpowersSource = source.slice(superpowersStart, localSuperpowersStart)

    expect(superpowersStart).toBeGreaterThan(-1)
    expect(localSuperpowersStart).toBeGreaterThan(superpowersStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(superpowersSource).toContain(
      'unwrapEnvelope(await opencodeApi.abort(worktreePath, opencodeSessionId))'
    )
    expect(superpowersSource).not.toContain(
      'unwrapEnvelope(await window.opencodeOps.abort(worktreePath, opencodeSessionId))'
    )
  })

  it('routes session plan-ready local superpowers abort cleanup through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const localSuperpowersStart = source.indexOf(
      'const handlePlanReadySuperpowersLocal = useCallback'
    )
    const saveAsTicketStart = source.indexOf(
      'const handlePlanReadySaveAsTicket = useCallback',
      localSuperpowersStart
    )
    const localSuperpowersSource = source.slice(localSuperpowersStart, saveAsTicketStart)

    expect(localSuperpowersStart).toBeGreaterThan(-1)
    expect(saveAsTicketStart).toBeGreaterThan(localSuperpowersStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(localSuperpowersSource).toContain(
      'unwrapEnvelope(await opencodeApi.abort(worktreePath, opencodeSessionId))'
    )
    expect(localSuperpowersSource).not.toContain(
      'unwrapEnvelope(await window.opencodeOps.abort(worktreePath, opencodeSessionId))'
    )
  })

  it('routes session manual streaming abort through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const abortStart = source.indexOf('const handleAbort = useCallback')
    const keyDownStart = source.indexOf('const handleKeyDown = useCallback', abortStart)
    const abortSource = source.slice(abortStart, keyDownStart)

    expect(abortStart).toBeGreaterThan(-1)
    expect(keyDownStart).toBeGreaterThan(abortStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(abortSource).toContain(
      'unwrapEnvelope(await opencodeApi.abort(worktreePath, opencodeSessionId))'
    )
    expect(abortSource).not.toContain(
      'unwrapEnvelope(await window.opencodeOps.abort(worktreePath, opencodeSessionId))'
    )
  })

  it('keeps abort streaming tests on opencodeApi mocks instead of window opencode globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-9/session-4/abort-streaming.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('../../../src/renderer/src/api/opencode-api'")
    expect(source).toContain('const mockAbort = vi.mocked(opencodeApi.abort)')
    expect(source).toContain('await opencodeApi.abort(worktreePath, opencodeSessionId)')
    expect(source).not.toContain('window.opencodeOps')
    expect(source).not.toContain('Object.defineProperty(window')
  })

  it('routes settings global selected model backend push through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSettingsStore.ts'),
      'utf-8'
    )
    const selectedModelStart = source.indexOf('setSelectedModel: async (')
    const selectedModelForSdkStart = source.indexOf(
      'setSelectedModelForSdk: async (',
      selectedModelStart
    )
    const selectedModelSource = source.slice(selectedModelStart, selectedModelForSdkStart)

    expect(selectedModelStart).toBeGreaterThan(-1)
    expect(selectedModelForSdkStart).toBeGreaterThan(selectedModelStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(selectedModelSource).toContain('unwrapEnvelope(await opencodeApi.setModel(model))')
    expect(selectedModelSource).not.toContain(
      'unwrapEnvelope(await window.opencodeOps.setModel(model))'
    )
  })

  it('routes settings per-SDK selected model backend push through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSettingsStore.ts'),
      'utf-8'
    )
    const selectedModelForSdkStart = source.indexOf('setSelectedModelForSdk: async (')
    const modeDefaultStart = source.indexOf(
      'setModeDefaultModel: async (',
      selectedModelForSdkStart
    )
    const selectedModelForSdkSource = source.slice(selectedModelForSdkStart, modeDefaultStart)

    expect(selectedModelForSdkStart).toBeGreaterThan(-1)
    expect(modeDefaultStart).toBeGreaterThan(selectedModelForSdkStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(selectedModelForSdkSource).toContain(
      'unwrapEnvelope(await opencodeApi.setModel(model ? { ...model, agentSdk } : null))'
    )
    expect(selectedModelForSdkSource).not.toContain(
      'unwrapEnvelope(await window.opencodeOps.setModel(model ? { ...model, agentSdk } : null))'
    )
  })

  it('routes session tab-switch model push through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const modelPushStart = source.indexOf('// Push per-session model to OpenCode on tab switch')
    const autoResizeStart = source.indexOf('// Auto-resize textarea', modelPushStart)
    const modelPushSource = source.slice(modelPushStart, autoResizeStart)

    expect(modelPushStart).toBeGreaterThan(-1)
    expect(autoResizeStart).toBeGreaterThan(modelPushStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(modelPushSource).toContain('opencodeApi.setModel(model).catch((error) => {')
    expect(modelPushSource).not.toContain('window.opencodeOps.setModel(model).catch((error) => {')
  })

  it('routes session initialization model hydration through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const hydrateStart = source.indexOf('if (session.model_provider_id && session.model_id) {')
    const worktreeStart = source.indexOf('let wtPath: string | null = null', hydrateStart)
    const hydrateSource = source.slice(hydrateStart, worktreeStart)

    expect(hydrateStart).toBeGreaterThan(-1)
    expect(worktreeStart).toBeGreaterThan(hydrateStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(hydrateSource).toContain('opencodeApi\n            .setModel({')
    expect(hydrateSource).toContain('providerID: session.model_provider_id')
    expect(hydrateSource).toContain('modelID: session.model_id')
    expect(hydrateSource).not.toContain('window.opencodeOps')
  })

  it('routes model selector model listing through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/ModelSelector.tsx'),
      'utf-8'
    )
    const loadStart = source.indexOf('async function loadModels(): Promise<void>')
    const buildSelectedStart = source.indexOf('const buildSelectedModel = useCallback', loadStart)
    const loadSource = source.slice(loadStart, buildSelectedStart)

    expect(loadStart).toBeGreaterThan(-1)
    expect(buildSelectedStart).toBeGreaterThan(loadStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(loadSource).toContain(
      'unwrapEnvelope(await opencodeApi.listModels({ agentSdk: listModelsSdk }))'
    )
    expect(loadSource).not.toContain('window.opencodeOps.listModels')
  })

  it('routes handoff model catalog loading through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/lib/handoffSelection.ts'),
      'utf-8'
    )
    const loadStart = source.indexOf('export async function loadHandoffModelCatalog')
    const nextExportStart = source.indexOf('export function resolveModelForSdkDefault', loadStart)
    const loadSource = source.slice(loadStart, nextExportStart)

    expect(loadStart).toBeGreaterThan(-1)
    expect(nextExportStart).toBeGreaterThan(loadStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(loadSource).toContain('.listModels({ agentSdk: listModelsSdk })')
    expect(loadSource).not.toContain('window.opencodeOps')
  })

  it('routes session context-limit model listing through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const fetchStart = source.indexOf('const fetchModelLimits = (): void => {')
    const fetchEnd = source.indexOf('// Fetch slash commands', fetchStart)
    const fetchSource = source.slice(fetchStart, fetchEnd)

    expect(fetchStart).toBeGreaterThan(-1)
    expect(fetchEnd).toBeGreaterThan(fetchStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(fetchSource).toContain('opencodeApi\n            .listModels()')
    expect(fetchSource).not.toContain('window.opencodeOps')
  })

  it('routes session runtime capabilities through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf(
      '// Fetch runtime capabilities when the opencode session changes'
    )
    const nextSectionStart = source.indexOf('// Prompt history navigation', effectStart)
    const effectSource = source.slice(effectStart, nextSectionStart)

    expect(effectStart).toBeGreaterThan(-1)
    expect(nextSectionStart).toBeGreaterThan(effectStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(effectSource).toContain('opencodeApi\n      .capabilities(opencodeSessionId)')
    expect(effectSource).toContain('setSessionCapabilities(result.capabilities)')
    expect(effectSource).not.toContain('window.opencodeOps')
  })

  it('routes session periodic permission polling through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const pollStart = source.indexOf(
      '// Periodic permission hydration while the session is actively streaming.'
    )
    const nextSectionStart = source.indexOf(
      '// Clean up rAF-based streaming and scroll guards on unmount',
      pollStart
    )
    const pollSource = source.slice(pollStart, nextSectionStart)

    expect(pollStart).toBeGreaterThan(-1)
    expect(nextSectionStart).toBeGreaterThan(pollStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(pollSource).toContain('opencodeApi\n        .permissionList(worktreePath)')
    expect(pollSource).toContain('usePermissionStore.getState().addPermission(targetSessionId, r)')
    expect(pollSource).not.toContain('window.opencodeOps')
  })

  it('routes session initialization permission hydration through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const hydrateStart = source.indexOf('// Hydrate any pending permission requests')
    const pendingStart = source.indexOf('// Send any pending initial message', hydrateStart)
    const hydrateSource = source.slice(hydrateStart, pendingStart)

    expect(hydrateStart).toBeGreaterThan(-1)
    expect(pendingStart).toBeGreaterThan(hydrateStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(hydrateSource).toContain('opencodeApi\n            .permissionList(path)')
    expect(hydrateSource).toContain(
      'usePermissionStore.getState().addPermission(targetSessionId, r)'
    )
    expect(hydrateSource).not.toContain('window.opencodeOps')
  })

  it('routes session slash-command listing through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const fetchStart = source.indexOf('// Fetch slash commands')
    const hydrateStart = source.indexOf('// Hydrate any pending permission requests', fetchStart)
    const fetchSource = source.slice(fetchStart, hydrateStart)

    expect(fetchStart).toBeGreaterThan(-1)
    expect(hydrateStart).toBeGreaterThan(fetchStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(fetchSource).toContain('opencodeApi\n            .commands(path, sessionId)')
    expect(fetchSource).toContain('setSlashCommands(result.commands)')
    expect(fetchSource).not.toContain('window.opencodeOps')
  })

  it('routes session commands-available refresh through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const eventStart = source.indexOf("if (event.type === 'session.commands_available')")
    const nextEventStart = source.indexOf("if (event.type === 'codex.goal.updated')", eventStart)
    const eventSource = source.slice(eventStart, nextEventStart)

    expect(eventStart).toBeGreaterThan(-1)
    expect(nextEventStart).toBeGreaterThan(eventStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(eventSource).toContain('opencodeApi')
    expect(eventSource).toContain('.commands(wtPath, sessionId)')
    expect(eventSource).toContain('setSlashCommands(result.commands)')
    expect(eventSource).not.toContain('window.opencodeOps')
  })

  it('routes ticket modal slash-command listing through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketModal.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('const [slashCommands, setSlashCommands]')
    const planContentStart = source.indexOf('const planContent =', effectStart)
    const effectSource = source.slice(effectStart, planContentStart)

    expect(effectStart).toBeGreaterThan(-1)
    expect(planContentStart).toBeGreaterThan(effectStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(effectSource).toContain('opencodeApi\n      .commands(worktreePath, opcSessionId)')
    expect(effectSource).toContain('setSlashCommands(result.commands)')
    expect(effectSource).not.toContain('window.opencodeOps')
  })

  it('routes session application-menu undo through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const menuStart = source.indexOf(
      '// Listen for undo/redo turn events from the application menu'
    )
    const redoStart = source.indexOf('const handleRedo = async (): Promise<void> => {', menuStart)
    const undoSource = source.slice(menuStart, redoStart)

    expect(menuStart).toBeGreaterThan(-1)
    expect(redoStart).toBeGreaterThan(menuStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(undoSource).toContain(
      'unwrapEnvelope(await opencodeApi.undo(worktreePath, opencodeSessionId))'
    )
    expect(undoSource).not.toContain('window.opencodeOps.undo')
  })

  it('routes session application-menu redo through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const redoStart = source.indexOf('const handleRedo = async (): Promise<void> => {')
    const eventStart = source.indexOf('const onUndo = (): void => {', redoStart)
    const redoSource = source.slice(redoStart, eventStart)

    expect(redoStart).toBeGreaterThan(-1)
    expect(eventStart).toBeGreaterThan(redoStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(redoSource).toContain(
      'unwrapEnvelope(await opencodeApi.redo(worktreePath, opencodeSessionId))'
    )
    expect(redoSource).not.toContain('window.opencodeOps.redo')
  })

  it('routes session slash-command undo through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const slashStart = source.indexOf("if (commandName === 'undo' || commandName === 'redo')")
    const redoStart = source.indexOf(
      'if (sessionCapabilities && !sessionCapabilities.supportsRedo)',
      slashStart
    )
    const undoSource = source.slice(slashStart, redoStart)

    expect(slashStart).toBeGreaterThan(-1)
    expect(redoStart).toBeGreaterThan(slashStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(undoSource).toContain(
      'unwrapEnvelope(await opencodeApi.undo(worktreePath, opencodeSessionId))'
    )
    expect(undoSource).not.toContain('window.opencodeOps.undo')
  })

  it('routes session slash-command draft clear through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const slashStart = source.indexOf("if (commandName === 'undo' || commandName === 'redo')")
    const tryStart = source.indexOf('try {', slashStart)
    const draftClearSource = source.slice(slashStart, tryStart)

    expect(slashStart).toBeGreaterThan(-1)
    expect(tryStart).toBeGreaterThan(slashStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(draftClearSource).toContain('setSlashDismissed(false)')
    expect(draftClearSource).toContain('savedDraftRef.current =')
    expect(draftClearSource).toContain(
      'if (draftTimerRef.current) clearTimeout(draftTimerRef.current)'
    )
    expect(draftClearSource).toContain('dbApi.session.updateDraft(sessionId, null)')
    expect(draftClearSource).not.toContain('db.session.updateDraft(sessionId, null)')
  })

  it('routes session slash-command redo through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const redoStart = source.indexOf(
      'if (sessionCapabilities && !sessionCapabilities.supportsRedo)'
    )
    const refreshStart = source.indexOf(
      'const refreshed = await refreshMessagesFromOpenCode()',
      redoStart
    )
    const redoSource = source.slice(redoStart, refreshStart)

    expect(redoStart).toBeGreaterThan(-1)
    expect(refreshStart).toBeGreaterThan(redoStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(redoSource).toContain(
      'unwrapEnvelope(await opencodeApi.redo(worktreePath, opencodeSessionId))'
    )
    expect(redoSource).not.toContain('window.opencodeOps.redo')
  })

  it('routes kanban ticket goal resume command through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/kanban/KanbanTicketCard.tsx'),
      'utf-8'
    )
    const resumeStart = source.indexOf('const handleResumeGoal = useCallback(async () => {')
    const markStart = source.indexOf('const handleMarkChange = useCallback(', resumeStart)
    const resumeSource = source.slice(resumeStart, markStart)

    expect(resumeStart).toBeGreaterThan(-1)
    expect(markStart).toBeGreaterThan(resumeStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(resumeSource).toContain(
      "await opencodeApi.command(worktree.path, ticket.current_session_id, 'goal', 'resume')"
    )
    expect(resumeSource).not.toContain('window.opencodeOps.command')
  })

  it('routes session slash-command execution through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const commandStart = source.indexOf('if (matchedCommand && !matchedCommand.builtIn) {')
    const fallbackStart = source.indexOf('// Unknown command', commandStart)
    const commandSource = source.slice(commandStart, fallbackStart)

    expect(commandStart).toBeGreaterThan(-1)
    expect(fallbackStart).toBeGreaterThan(commandStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(commandSource).toContain('await opencodeApi.command(')
    expect(commandSource).toContain('worktreePath')
    expect(commandSource).toContain('opencodeSessionId')
    expect(commandSource).toContain('commandName')
    expect(commandSource).toContain('commandArgs')
    expect(commandSource).toContain('requestModel')
    expect(commandSource).not.toContain('window.opencodeOps.command')
  })

  it('routes session message fork through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const forkStart = source.indexOf('setForkingMessageId(message.id)')
    const failureStart = source.indexOf(
      "throw new Error(forkResult.error || 'Failed to fork session')",
      forkStart
    )
    const forkSource = source.slice(forkStart, failureStart)

    expect(forkStart).toBeGreaterThan(-1)
    expect(failureStart).toBeGreaterThan(forkStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(forkSource).toContain(
      'await opencodeApi.fork(worktreePath, opencodeSessionId, cutoffMessage?.id)'
    )
    expect(forkSource).not.toContain('window.opencodeOps.fork')
  })

  it('routes session message fork source lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('const handleForkFromAssistantMessage = useCallback(')
    const targetWorktreeStart = source.indexOf('const targetWorktreeId =', handlerStart)
    const handlerSource = source.slice(handlerStart, targetWorktreeStart)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(targetWorktreeStart).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(handlerSource).toContain(
      'const sourceSession = sessionRecord ?? (await dbApi.session.get<DbSession>(sessionId))'
    )
    expect(handlerSource).not.toContain('db.session.get(sessionId)')
  })

  it('routes session message fork session creation through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const createStart = source.indexOf('const fallbackForkName =')
    const loadSessionsStart = source.indexOf(
      'await useSessionStore.getState().loadSessions(targetWorktreeId, sourceSession.project_id)',
      createStart
    )
    const createSource = source.slice(createStart, loadSessionsStart)

    expect(createStart).toBeGreaterThan(-1)
    expect(loadSessionsStart).toBeGreaterThan(createStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(createSource).toContain('const forkedSession = await dbApi.session.create<DbSession>({')
    expect(createSource).toContain('worktree_id: targetWorktreeId')
    expect(createSource).toContain('project_id: sourceSession.project_id')
    expect(createSource).toContain('opencode_session_id: forkResult.sessionId')
    expect(createSource).toContain('model_provider_id: sourceSession.model_provider_id')
    expect(createSource).toContain('model_id: sourceSession.model_id')
    expect(createSource).toContain('model_variant: sourceSession.model_variant')
    expect(createSource).not.toContain('db.session.create')
  })

  it('routes session queued-message steer through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const steerStart = source.indexOf(
      'const handleSteerMessage = useCallback(\n    async (messageId: string, content: string) => {'
    )
    const successStart = source.indexOf('if (result?.success) {', steerStart)
    const steerSource = source.slice(steerStart, successStart)

    expect(steerStart).toBeGreaterThan(-1)
    expect(successStart).toBeGreaterThan(steerStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(steerSource).toContain(
      'await opencodeApi.steer(worktreePath, opencodeSessionId, content)'
    )
    expect(steerSource).toContain('const result = unwrapEnvelope(')
    expect(steerSource).not.toContain('window.opencodeOps')
  })

  it('routes session plan approval through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const approveStart = source.indexOf(
      '// Claude Code sessions must resolve a real pending ExitPlanMode request.'
    )
    const modeStart = source.indexOf(
      "await useSessionStore.getState().setSessionMode(sessionId, 'build')",
      approveStart
    )
    const approveSource = source.slice(approveStart, modeStart)

    expect(approveStart).toBeGreaterThan(-1)
    expect(modeStart).toBeGreaterThan(approveStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(approveSource).toContain(
      'await opencodeApi.planApprove(worktreePath, sessionId, pendingBeforeAction.requestId)'
    )
    expect(approveSource).toContain('const result = unwrapEnvelope(')
    expect(approveSource).not.toContain('window.opencodeOps.planApprove')
  })

  it('routes session plan rejection through opencodeApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/sessions/SessionView.tsx'),
      'utf-8'
    )
    const rejectStart = source.indexOf(
      '// Reject first (unblocks the SDK with feedback), then clear frontend state'
    )
    const transitionStart = source.indexOf(
      '// Transition the ExitPlanMode tool card to "rejected" state with feedback',
      rejectStart
    )
    const rejectSource = source.slice(rejectStart, transitionStart)

    expect(rejectStart).toBeGreaterThan(-1)
    expect(transitionStart).toBeGreaterThan(rejectStart)
    expect(source).toContain("import { opencodeApi } from '@/api/opencode-api'")
    expect(rejectSource).toContain('await opencodeApi.planReject(')
    expect(rejectSource).toContain('worktreePath')
    expect(rejectSource).toContain('sessionId')
    expect(rejectSource).toContain('feedback')
    expect(rejectSource).toContain('pendingBeforeAction.requestId')
    expect(rejectSource).toContain('const result = unwrapEnvelope(')
    expect(rejectSource).not.toContain('window.opencodeOps.planReject')
  })

  it('routes project store loadProjects through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const projects = await dbApi.project.getAll<Project>()')
    expect(source).not.toContain('const projects = await db.project.getAll()')
  })

  it('removes the old db:project:getAll IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:project:getAll'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:project:getAll'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:project:getAll'")
  })

  it('routes project store duplicate path lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const addStart = source.indexOf('addProject: async (path: string)')
    const removeStart = source.indexOf('removeProject: async (id: string)', addStart)
    const addSource = source.slice(addStart, removeStart)

    expect(addStart).toBeGreaterThan(-1)
    expect(removeStart).toBeGreaterThan(addStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(addSource).toContain(
      'const existingProject = await dbApi.project.getByPath<Project>(path)'
    )
    expect(addSource).toContain('This project has already been added to Hive.')
    expect(addSource).not.toContain('db.project.getByPath(path)')
  })

  it('removes the old db:project:getByPath IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:project:getByPath'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:project:getByPath'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:project:getByPath'")
  })

  it('routes project store add-project validation through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const addStart = source.indexOf('addProject: async (path: string)')
    const removeStart = source.indexOf('removeProject: async (id: string)', addStart)
    const addSource = source.slice(addStart, removeStart)

    expect(addStart).toBeGreaterThan(-1)
    expect(removeStart).toBeGreaterThan(addStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(addSource).toContain('const validation = await projectApi.validateProject(path)')
    expect(addSource).toContain('if (!validation.success) {')
    expect(addSource).toContain('return { success: false, error: validation.error }')
    expect(addSource).not.toContain('window.projectOps.validateProject')
    expect(addSource).not.toContain('unwrapEnvelope(await')
  })

  it('routes project store project creation through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const addStart = source.indexOf('addProject: async (path: string)')
    const removeStart = source.indexOf('removeProject: async (id: string)', addStart)
    const addSource = source.slice(addStart, removeStart)

    expect(addStart).toBeGreaterThan(-1)
    expect(removeStart).toBeGreaterThan(addStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(addSource).toContain('const project = await dbApi.project.create<Project>({')
    expect(addSource).toContain('projects: [project, ...state.projects]')
    expect(addSource).not.toContain('db.project.create({')
  })

  it('removes the old db:project:create IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:project:create'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:project:create'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:project:create'")
  })

  it('routes project store add-project language persistence through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const addStart = source.indexOf('addProject: async (path: string)')
    const removeStart = source.indexOf('removeProject: async (id: string)', addStart)
    const addSource = source.slice(addStart, removeStart)

    expect(addStart).toBeGreaterThan(-1)
    expect(removeStart).toBeGreaterThan(addStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(addSource).toContain('await dbApi.project.update(project.id, { language })')
    expect(addSource).toContain('projects: state.projects.map((p) =>')
    expect(addSource).not.toContain('await db.project.update(project.id, { language })')
  })

  it('routes project store add-project language detection through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const addStart = source.indexOf('addProject: async (path: string)')
    const removeStart = source.indexOf('removeProject: async (id: string)', addStart)
    const addSource = source.slice(addStart, removeStart)
    const languageStart = addSource.indexOf('// Auto-detect language')
    const faviconStart = addSource.indexOf('// Auto-detect favicon', languageStart)
    const languageSource = addSource.slice(languageStart, faviconStart)

    expect(addStart).toBeGreaterThan(-1)
    expect(removeStart).toBeGreaterThan(addStart)
    expect(languageStart).toBeGreaterThan(-1)
    expect(faviconStart).toBeGreaterThan(languageStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(languageSource).toContain('.detectLanguage(validation.path!)')
    expect(languageSource).toContain('await dbApi.project.update(project.id, { language })')
    expect(languageSource).not.toContain('window.projectOps.detectLanguage')
    expect(languageSource).not.toContain('.then(unwrapEnvelope)')
  })

  it('routes project store add-project detected-icon persistence through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const addStart = source.indexOf('addProject: async (path: string)')
    const removeStart = source.indexOf('removeProject: async (id: string)', addStart)
    const addSource = source.slice(addStart, removeStart)

    expect(addStart).toBeGreaterThan(-1)
    expect(removeStart).toBeGreaterThan(addStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(addSource).toContain('await dbApi.project.update(project.id, {')
    expect(addSource).toContain("detected_icon: detectedIcon ?? 'none'")
    expect(addSource).not.toContain('await db.project.update(project.id, {')
  })

  it('routes project store add-project favicon detection through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const addStart = source.indexOf('addProject: async (path: string)')
    const removeStart = source.indexOf('removeProject: async (id: string)', addStart)
    const addSource = source.slice(addStart, removeStart)
    const faviconStart = addSource.indexOf('// Auto-detect favicon')
    const addToStateStart = addSource.indexOf('// Add to state', faviconStart)
    const faviconSource = addSource.slice(faviconStart, addToStateStart)

    expect(addStart).toBeGreaterThan(-1)
    expect(removeStart).toBeGreaterThan(addStart)
    expect(faviconStart).toBeGreaterThan(-1)
    expect(addToStateStart).toBeGreaterThan(faviconStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(faviconSource).toContain('.detectFavicon(validation.path!)')
    expect(faviconSource).toContain('await dbApi.project.update(project.id, {')
    expect(faviconSource).toContain("detected_icon: detectedIcon ?? 'none'")
    expect(faviconSource).not.toContain('window.projectOps.detectFavicon')
    expect(faviconSource).not.toContain('.then(unwrapEnvelope)')
  })

  it('routes project store startup detected-icon persistence through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const loadStart = source.indexOf('loadProjects: async () =>')
    const addStart = source.indexOf('addProject: async (path: string)', loadStart)
    const loadSource = source.slice(loadStart, addStart)

    expect(loadStart).toBeGreaterThan(-1)
    expect(addStart).toBeGreaterThan(loadStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(loadSource).toContain('await dbApi.project.update(project.id, {')
    expect(loadSource).toContain("detected_icon: detectedIcon ?? 'none'")
    expect(loadSource).not.toContain('await db.project.update(project.id, {')
  })

  it('routes project store startup favicon detection through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const loadStart = source.indexOf('loadProjects: async () =>')
    const addStart = source.indexOf('addProject: async (path: string)', loadStart)
    const loadSource = source.slice(loadStart, addStart)

    expect(loadStart).toBeGreaterThan(-1)
    expect(addStart).toBeGreaterThan(loadStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(loadSource).toContain(
      'const detectedIcon = await projectApi.detectFavicon(project.path)'
    )
    expect(loadSource).toContain("detected_icon: detectedIcon ?? 'none'")
    expect(loadSource).not.toContain('window.projectOps.detectFavicon(project.path)')
    expect(loadSource).not.toContain('unwrapEnvelope(await')
  })

  it('keeps project add store tests on RPC-backed projectApi paths instead of window project globals', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/stores/__tests__/useProjectStore.project-add.test.ts'
      ),
      'utf-8'
    )

    expect(source).toContain("request).toHaveBeenCalledWith('projectOps.detectLanguage'")
    expect(source).toContain("request).toHaveBeenCalledWith('projectOps.detectFavicon'")
    expect(source).toContain("request).toHaveBeenCalledWith('db.project.update'")
    expect(source).not.toContain('window.projectOps')
  })

  it('routes project store removal through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const removeStart = source.indexOf('removeProject: async (id: string)')
    const updateNameStart = source.indexOf('updateProjectName: async', removeStart)
    const removeSource = source.slice(removeStart, updateNameStart)

    expect(removeStart).toBeGreaterThan(-1)
    expect(updateNameStart).toBeGreaterThan(removeStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(removeSource).toContain('const success = await dbApi.project.delete(id)')
    expect(removeSource).toContain('projects: state.projects.filter((p) => p.id !== id)')
    expect(removeSource).not.toContain('db.project.delete(id)')
  })

  it('removes the old db:project:delete IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:project:delete'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:project:delete'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:project:delete'")
  })

  it('routes project store name updates through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const updateNameStart = source.indexOf('updateProjectName: async')
    const updateProjectStart = source.indexOf('updateProject: async (', updateNameStart)
    const updateNameSource = source.slice(updateNameStart, updateProjectStart)

    expect(updateNameStart).toBeGreaterThan(-1)
    expect(updateProjectStart).toBeGreaterThan(updateNameStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(updateNameSource).toContain(
      'const updatedProject = await dbApi.project.update(id, { name })'
    )
    expect(updateNameSource).toContain('editingProjectId: null')
    expect(updateNameSource).not.toContain('db.project.update(id, { name })')
  })

  it('routes project store generic updates through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const updateProjectStart = source.indexOf('updateProject: async (')
    const selectStart = source.indexOf('selectProject: (id: string | null', updateProjectStart)
    const updateProjectSource = source.slice(updateProjectStart, selectStart)

    expect(updateProjectStart).toBeGreaterThan(-1)
    expect(selectStart).toBeGreaterThan(updateProjectStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(updateProjectSource).toContain(
      'const updatedProject = await dbApi.project.update(id, data)'
    )
    expect(updateProjectSource).toContain('localUpdate.tags = tags ? JSON.stringify(tags) : null')
    expect(updateProjectSource).not.toContain('db.project.update(id, data)')
  })

  it('removes the old db:project:update IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:project:update'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:project:update'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:project:update'")
  })

  it('routes project store touch updates through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const touchStart = source.indexOf('touchProject: async (id: string)')
    const refreshStart = source.indexOf('refreshLanguage: async', touchStart)
    const touchSource = source.slice(touchStart, refreshStart)

    expect(touchStart).toBeGreaterThan(-1)
    expect(refreshStart).toBeGreaterThan(touchStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(touchSource).toContain('await dbApi.project.touch(id)')
    expect(touchSource).toContain('last_accessed_at: new Date().toISOString()')
    expect(touchSource).not.toContain('db.project.touch(id)')
  })

  it('removes the old db:project:touch IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:project:touch'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:project:touch'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:project:touch'")
  })

  it('routes project store refresh-language persistence through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const refreshStart = source.indexOf('refreshLanguage: async')
    const reorderStart = source.indexOf('reorderProjects: (fromIndex: number', refreshStart)
    const refreshSource = source.slice(refreshStart, reorderStart)

    expect(refreshStart).toBeGreaterThan(-1)
    expect(reorderStart).toBeGreaterThan(refreshStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(refreshSource).toContain('await dbApi.project.update(projectId, { language })')
    expect(refreshSource).toContain(
      'projects: state.projects.map((p) => (p.id === projectId ? { ...p, language } : p))'
    )
    expect(refreshSource).not.toContain('await db.project.update(projectId, { language })')
  })

  it('routes project store refresh-language detection through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const refreshStart = source.indexOf('refreshLanguage: async')
    const reorderStart = source.indexOf('reorderProjects: (fromIndex: number', refreshStart)
    const refreshSource = source.slice(refreshStart, reorderStart)

    expect(refreshStart).toBeGreaterThan(-1)
    expect(reorderStart).toBeGreaterThan(refreshStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(refreshSource).toContain('const language = await projectApi.detectLanguage(path)')
    expect(refreshSource).toContain('await dbApi.project.update(projectId, { language })')
    expect(refreshSource).not.toContain('window.projectOps.detectLanguage')
    expect(refreshSource).not.toContain('unwrapEnvelope(await window.projectOps.detectLanguage')
  })

  it('routes project store refresh detected-icon persistence through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const refreshStart = source.indexOf('refreshLanguage: async')
    const reorderStart = source.indexOf('reorderProjects: (fromIndex: number', refreshStart)
    const refreshSource = source.slice(refreshStart, reorderStart)

    expect(refreshStart).toBeGreaterThan(-1)
    expect(reorderStart).toBeGreaterThan(refreshStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(refreshSource).toContain('await dbApi.project.update(projectId, {')
    expect(refreshSource).toContain("detected_icon: detectedIcon ?? 'none'")
    expect(refreshSource).not.toContain('await db.project.update(projectId, {')
  })

  it('routes project store refresh favicon detection through projectApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const refreshStart = source.indexOf('refreshLanguage: async')
    const reorderStart = source.indexOf('reorderProjects: (fromIndex: number', refreshStart)
    const refreshSource = source.slice(refreshStart, reorderStart)
    const faviconStart = refreshSource.indexOf('// Also refresh favicon detection')
    const catchStart = refreshSource.indexOf('.catch(() => {})', faviconStart)
    const faviconSource = refreshSource.slice(faviconStart, catchStart)

    expect(refreshStart).toBeGreaterThan(-1)
    expect(reorderStart).toBeGreaterThan(refreshStart)
    expect(faviconStart).toBeGreaterThan(-1)
    expect(catchStart).toBeGreaterThan(faviconStart)
    expect(source).toContain("import { projectApi } from '@/api/project-api'")
    expect(faviconSource).toContain('.detectFavicon(path)')
    expect(faviconSource).toContain('await dbApi.project.update(projectId, {')
    expect(faviconSource).toContain("detected_icon: detectedIcon ?? 'none'")
    expect(faviconSource).not.toContain('window.projectOps.detectFavicon')
    expect(faviconSource).not.toContain('.then(unwrapEnvelope)')
  })

  it('keeps project update store tests on RPC-backed projectApi paths instead of window project globals', () => {
    const source = fs.readFileSync(
      path.resolve(
        __dirname,
        '../../src/renderer/src/stores/__tests__/useProjectStore.project-update.test.ts'
      ),
      'utf-8'
    )

    expect(source).toContain("request).toHaveBeenCalledWith('projectOps.detectLanguage'")
    expect(source).toContain("request).toHaveBeenCalledWith('projectOps.detectFavicon'")
    expect(source).toContain("request).toHaveBeenCalledWith('db.project.update'")
    expect(source).not.toContain('window.projectOps')
  })

  it('routes project store drag reorder persistence through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const reorderStart = source.indexOf('reorderProjects: (fromIndex: number')
    const sortStart = source.indexOf('sortProjectsByLastMessage: async', reorderStart)
    const reorderSource = source.slice(reorderStart, sortStart)

    expect(reorderStart).toBeGreaterThan(-1)
    expect(sortStart).toBeGreaterThan(reorderStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(reorderSource).toContain('const orderedIds = projects.map((p) => p.id)')
    expect(reorderSource).toContain('dbApi.project.reorder(orderedIds).catch(() => {')
    expect(reorderSource).not.toContain('db.project.reorder(orderedIds).catch(() => {')
  })

  it('keeps worktree drag reorder tests off stale window API fixtures', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-14/session-12/worktree-drag-reorder.test.ts'),
      'utf-8'
    )

    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'worktreeOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'projectOps'")
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.worktreeOps')
    expect(source).not.toContain('window.projectOps')
  })

  it('keeps quick-wins renderer tests off stale window API fixtures', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-7/session-1/quick-wins.test.ts'),
      'utf-8'
    )

    expect(source).not.toContain("Object.defineProperty(window, 'scriptOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'worktreeOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'projectOps'")
    expect(source).not.toContain('window.scriptOps')
    expect(source).not.toContain('window.worktreeOps')
    expect(source).not.toContain('window.projectOps')
  })

  it('keeps integration-polish renderer tests off stale window API fixtures', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-7/session-7/integration-polish.test.ts'),
      'utf-8'
    )

    expect(source).not.toContain("Object.defineProperty(window, 'scriptOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'worktreeOps'")
    expect(source).not.toContain("Object.defineProperty(window, 'projectOps'")
    expect(source).not.toContain('window.scriptOps')
    expect(source).not.toContain('window.worktreeOps')
    expect(source).not.toContain('window.projectOps')
  })

  it('keeps native notification tests on RPC-backed notification navigation', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-6/session-4/native-notifications.test.ts'),
      'utf-8'
    )

    expect(source).toContain('NOTIFICATION_NAVIGATE_CHANNEL')
    expect(source).toContain('setRendererRpcClient')
    expect(source).toContain('subscribe: mockSubscribe')
    expect(source).toContain("'db.project.touch'")
    expect(source).toContain("'db.worktree.touch'")
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
    expect(source).not.toContain("Object.defineProperty(window, 'systemOps'")
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('window.systemOps')
  })

  it('removes the old db:project:reorder IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:project:reorder'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:project:reorder'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:project:reorder'")
  })

  it('routes project store last-message sort lookup through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useProjectStore.ts'),
      'utf-8'
    )
    const sortStart = source.indexOf('sortProjectsByLastMessage: async')
    const settingsStart = source.indexOf('openProjectSettings: (projectId: string)', sortStart)
    const sortSource = source.slice(sortStart, settingsStart)

    expect(sortStart).toBeGreaterThan(-1)
    expect(settingsStart).toBeGreaterThan(sortStart)
    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).not.toContain(
      "import { unwrapEnvelope, unwrapEnvelopeApi } from '@/lib/ipc-envelope'"
    )
    expect(source).not.toContain('const db = unwrapEnvelopeApi(() => window.db)')
    expect(sortSource).toContain('const orderedIds = await dbApi.project.sortByLastMessage()')
    expect(sortSource).toContain('await dbApi.project.reorder(orderedIds)')
    expect(sortSource).not.toContain('await db.project.sortByLastMessage()')
    expect(sortSource).not.toContain('await db.project.reorder(orderedIds)')
  })

  it('removes the old db:project:sortByLastMessage IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:project:sortByLastMessage'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:project:sortByLastMessage'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:project:sortByLastMessage'")
  })

  it('removes the old db:worktree:create IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:create'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:create'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:create'")
  })

  it('removes the old db:worktree:get IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:get'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:get'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:get'")
  })

  it('removes the old db:worktree:getByProject IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:getByProject'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:getByProject'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:getByProject'")
  })

  it('removes the old db:worktree:getActiveByProject IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:getActiveByProject'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:getActiveByProject'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:getActiveByProject'")
  })

  it('removes the old db:worktree:getRecentlyActive IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:getRecentlyActive'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:getRecentlyActive'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:getRecentlyActive'")
  })

  it('removes the old db:worktree:update IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:update'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:update'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:update'")
  })

  it('removes the old db:worktree:delete IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:delete'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:delete'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:delete'")
  })

  it('removes the old db:worktree:archive IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:archive'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:archive'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:archive'")
  })

  it('removes the old db:worktree:touch IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:touch'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:touch'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:touch'")
  })

  it('removes the old db:worktree:updateModel IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:updateModel'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:updateModel'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:updateModel'")
  })

  it('keeps the worktree model backend test on dbApi RPC instead of window db globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-19/session-5/worktree-model-backend.test.ts'),
      'utf-8'
    )

    expect(source).toContain('await expect(dbApi.worktree.updateModel(params))')
    expect(source).toContain("request).toHaveBeenCalledWith('db.worktree.updateModel', params)")
    expect(source).not.toContain('window.db')
    expect(source).not.toContain("Object.defineProperty(window, 'db'")
  })

  it('removes the old db:worktree:appendSessionTitle IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:appendSessionTitle'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:appendSessionTitle'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:appendSessionTitle'")
  })

  it('removes the old db:worktree:addAttachment IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:addAttachment'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:addAttachment'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:addAttachment'")
  })

  it('removes the old db:worktree:removeAttachment IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:removeAttachment'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:removeAttachment'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:removeAttachment'")
  })

  it('removes the old db:worktree:attachPR IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:attachPR'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:attachPR'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:attachPR'")
  })

  it('removes the old db:worktree:detachPR IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:detachPR'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:detachPR'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:detachPR'")
  })

  it('removes the old db:worktree:setPinned IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:setPinned'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:setPinned'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:setPinned'")
  })

  it('removes the old db:worktree:getPinned IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:worktree:getPinned'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:worktree:getPinned'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:worktree:getPinned'")
  })

  it('removes the old db:session:create IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:session:create'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:session:create'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:session:create'")
  })

  it('removes the old db:session:get IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:session:get'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:session:get',")
    expect(databaseHandlersSource).not.toContain("tryDb('db:session:get'")
  })

  it('removes the old db:session:getByWorktree IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:session:getByWorktree'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:session:getByWorktree'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:session:getByWorktree'")
  })

  it('removes the old db:session:getByProject IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:session:getByProject'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:session:getByProject'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:session:getByProject'")
  })

  it('removes the old db:session:getActiveByWorktree IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:session:getActiveByWorktree'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:session:getActiveByWorktree'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:session:getActiveByWorktree'")
  })

  it('removes the old db:session:update IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:session:update'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:session:update'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:session:update'")
    expect(databaseHandlersSource).not.toContain('const sessionUpdateSchema = z.object')
  })

  it('removes the old db:session:delete IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:session:delete'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:session:delete'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:session:delete'")
  })

  it('removes the old db:session:getByConnection IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:session:getByConnection'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:session:getByConnection'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:session:getByConnection'")
  })

  it('removes the old db:session:getActiveByConnection IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:session:getActiveByConnection'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:session:getActiveByConnection'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:session:getActiveByConnection'")
  })

  it('removes the old db:session:setPinnedToBoard IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:session:setPinnedToBoard'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:session:setPinnedToBoard'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:session:setPinnedToBoard'")
  })

  it('removes the old db:session:getPinnedSessions IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:session:getPinnedSessions'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:session:getPinnedSessions'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:session:getPinnedSessions'")
  })

  it('removes the old db:session:search IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:session:search'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:session:search'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:session:search'")
  })

  it('removes the old db:session:getActiveBoardAssistant IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:session:getActiveBoardAssistant'")
    expect(databaseHandlersSource).not.toContain(
      "defineHandler('db:session:getActiveBoardAssistant'"
    )
    expect(databaseHandlersSource).not.toContain("tryDb('db:session:getActiveBoardAssistant'")
  })

  it('removes the old db:session:getDraft IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:session:getDraft'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:session:getDraft'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:session:getDraft'")
  })

  it('removes the old db:session:updateDraft IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:session:updateDraft'")
    expect(databaseHandlersSource).not.toContain("'db:session:updateDraft'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:session:updateDraft'")
  })

  it('removes the old db:sessionMessage:list IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:sessionMessage:list'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:sessionMessage:list'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:sessionMessage:list'")
  })

  it('removes the old db:sessionActivity:list IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:sessionActivity:list'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:sessionActivity:list'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:sessionActivity:list'")
  })

  it('removes the old db:space:list IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:space:list'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:space:list'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:space:list'")
  })

  it('removes the old db:space:create IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:space:create'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:space:create'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:space:create'")
  })

  it('removes the old db:space:update IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:space:update'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:space:update'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:space:update'")
  })

  it('removes the old db:space:delete IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:space:delete'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:space:delete'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:space:delete'")
  })

  it('removes the old db:space:assignProject IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:space:assignProject'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:space:assignProject'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:space:assignProject'")
  })

  it('removes the old db:space:removeProject IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:space:removeProject'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:space:removeProject'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:space:removeProject'")
  })

  it('removes the old db:space:getProjectIds IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:space:getProjectIds'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:space:getProjectIds'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:space:getProjectIds'")
  })

  it('removes the old db:space:getAllAssignments IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:space:getAllAssignments'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:space:getAllAssignments'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:space:getAllAssignments'")
  })

  it('removes the old db:space:reorder IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:space:reorder'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:space:reorder'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:space:reorder'")
  })

  it('removes the old db:diffComment:create IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:diffComment:create'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:diffComment:create'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:diffComment:create'")
  })

  it('removes the old db:diffComment:list IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:diffComment:list'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:diffComment:list'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:diffComment:list'")
  })

  it('removes the old db:diffComment:update IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:diffComment:update'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:diffComment:update'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:diffComment:update'")
  })

  it('removes the old db:diffComment:setOutdated IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:diffComment:setOutdated'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:diffComment:setOutdated'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:diffComment:setOutdated'")
  })

  it('removes the old db:diffComment:delete IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:diffComment:delete'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:diffComment:delete'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:diffComment:delete'")
  })

  it('removes the old db:diffComment:clearAll IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:diffComment:clearAll'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:diffComment:clearAll'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:diffComment:clearAll'")
  })

  it('removes the old db:schemaVersion IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:schemaVersion'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:schemaVersion'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:schemaVersion'")
  })

  it('removes the old db:tableExists IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:tableExists'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:tableExists'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:tableExists'")
  })

  it('removes the old db:getIndexes IPC adapter after RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const databaseHandlersSource = readOptionalSource('../../src/main/ipc/database-handlers.ts')

    expect(preloadSource).not.toContain("'db:getIndexes'")
    expect(databaseHandlersSource).not.toContain("defineHandler('db:getIndexes'")
    expect(databaseHandlersSource).not.toContain("tryDb('db:getIndexes'")
  })

  it('routes pinned store pinned-worktree loading through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/usePinnedStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('dbApi.worktree.getPinned<{ id: string; project_id: string }>()')
    expect(source).not.toContain('db.worktree.getPinned()')
  })

  it('routes pinned store pinned-connection loading through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/usePinnedStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(source).toContain('connectionApi.getPinned()')
    expect(source).not.toContain('window.connectionOps.getPinned()')
  })

  it('removes the old connection:getPinned IPC adapter after pinned connection loading RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const connectionHandlersSource = readOptionalSource('../../src/main/ipc/connection-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('connection:getPinned'")
    expect(connectionHandlersSource).not.toContain("defineHandler('connection:getPinned'")
    expect(connectionHandlersSource).not.toContain("connectionFailed('connection:getPinned'")
  })

  it('routes pinned store worktree pinning through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/usePinnedStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const result = await dbApi.worktree.setPinned(id, true)')
    expect(source).not.toContain('const result = await db.worktree.setPinned(id, true)')
  })

  it('routes pinned store worktree unpinning through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/usePinnedStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const result = await dbApi.worktree.setPinned(id, false)')
    expect(source).not.toContain('const result = await db.worktree.setPinned(id, false)')
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
  })

  it('routes pinned store connection pinning through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/usePinnedStore.ts'),
      'utf-8'
    )
    const pinStart = source.indexOf('pinConnection: async (id: string) => {')
    const unpinStart = source.indexOf('unpinConnection: async (id: string) => {', pinStart)
    const pinSource = source.slice(pinStart, unpinStart)

    expect(pinStart).toBeGreaterThan(-1)
    expect(unpinStart).toBeGreaterThan(pinStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(pinSource).toContain('const result = await connectionApi.setPinned(id, true)')
    expect(pinSource).toContain('next.add(id)')
    expect(pinSource).not.toContain('window.connectionOps.setPinned(id, true)')
  })

  it('routes pinned store connection unpinning through connectionApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/usePinnedStore.ts'),
      'utf-8'
    )
    const unpinStart = source.indexOf('unpinConnection: async (id: string) => {')
    const removeStart = source.indexOf('removeWorktree: (id: string) => {', unpinStart)
    const unpinSource = source.slice(unpinStart, removeStart)

    expect(unpinStart).toBeGreaterThan(-1)
    expect(removeStart).toBeGreaterThan(unpinStart)
    expect(source).toContain("import { connectionApi } from '@/api/connection-api'")
    expect(unpinSource).toContain('const result = await connectionApi.setPinned(id, false)')
    expect(unpinSource).toContain('next.delete(id)')
    expect(unpinSource).not.toContain('window.connectionOps.setPinned(id, false)')
  })

  it('removes the old connection:setPinned IPC adapter after pinned connection RPC migration', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const connectionHandlersSource = readOptionalSource('../../src/main/ipc/connection-handlers.ts')

    expect(preloadSource).not.toContain("ipcRenderer.invoke('connection:setPinned'")
    expect(connectionHandlersSource).not.toContain("defineHandler('connection:setPinned'")
    expect(connectionHandlersSource).not.toContain("'connection:setPinned'")
  })

  it('removes the old empty connectionOps preload global exposure', () => {
    const preloadSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.ts'),
      'utf-8'
    )
    const preloadTypesSource = fs.readFileSync(
      path.resolve(__dirname, '../../src/preload/index.d.ts'),
      'utf-8'
    )

    expect(preloadSource).not.toContain('const connectionOps =')
    expect(preloadSource).not.toContain("contextBridge.exposeInMainWorld('connectionOps'")
    expect(preloadSource).not.toContain('window.connectionOps = connectionOps')
    expect(preloadTypesSource).not.toContain('connectionOps:')
    expect(preloadTypesSource).not.toContain('getPinned: () => Promise<Envelope')
  })

  it('removes the old empty connection IPC handler registrar after connection RPC migration', () => {
    const mainSource = fs.readFileSync(path.resolve(__dirname, '../../src/main/index.ts'), 'utf-8')
    const ipcIndexSource = readOptionalSource('../../src/main/ipc/index.ts')

    expect(
      fs.existsSync(path.resolve(__dirname, '../../src/main/ipc/connection-handlers.ts'))
    ).toBe(false)
    expect(mainSource).not.toContain('registerConnectionHandlers')
    expect(ipcIndexSource).not.toContain('registerConnectionHandlers')
    expect(ipcIndexSource).not.toContain('./connection-handlers')
  })

  it('routes space store space list loading through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSpaceStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('dbApi.space.list()')
    expect(source).not.toContain('db.space.list()')
  })

  it('routes space store creation through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSpaceStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const space = await dbApi.space.create({')
    expect(source).not.toContain('const space = await db.space.create({')
  })

  it('routes space store updates through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSpaceStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const updated = await dbApi.space.update(id, data)')
    expect(source).not.toContain('const updated = await db.space.update(id, data)')
  })

  it('routes space store deletion through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSpaceStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('await dbApi.space.delete(id)')
    expect(source).not.toContain('await db.space.delete(id)')
  })

  it('routes space store project assignment through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSpaceStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('await dbApi.space.assignProject(projectId, spaceId)')
    expect(source).not.toContain('await db.space.assignProject(projectId, spaceId)')
  })

  it('routes space store project removal through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSpaceStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('await dbApi.space.removeProject(projectId, spaceId)')
    expect(source).not.toContain('await db.space.removeProject(projectId, spaceId)')
  })

  it('routes space store reordering through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSpaceStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('dbApi.space.reorder(orderedIds).catch(() => {')
    expect(source).not.toContain('db.space.reorder(orderedIds).catch(() => {')
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
  })

  it('routes space store assignment loading through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useSpaceStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('dbApi.space.getAllAssignments()')
    expect(source).not.toContain('db.space.getAllAssignments()')
  })

  it('keeps spaces schema store tests on dbApi mocks instead of window db globals', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../test/phase-17/session-14/spaces-schema-store.test.ts'),
      'utf-8'
    )

    expect(source).toContain("vi.mock('@/api/db-api'")
    expect(source).toContain('const mockSpaceDb = vi.mocked(dbApi.space)')
    expect(source).not.toContain('window.db')
    expect(source).not.toContain('Object.defineProperty(window')
    expect(source).not.toContain('Mock window.db.space')
  })

  it('routes diff comment list loading through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useDiffCommentStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const result = await dbApi.diffComment.list<DiffComment>(worktreeId)')
    expect(source).not.toContain('const result = await db.diffComment.list(worktreeId)')
  })

  it('routes diff comment creation through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useDiffCommentStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const created = await dbApi.diffComment.create<DiffComment>(data)')
    expect(source).not.toContain('const created = await db.diffComment.create(data)')
  })

  it('routes diff comment updates through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useDiffCommentStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain(
      'const updated = await dbApi.diffComment.update<DiffComment>(id, data)'
    )
    expect(source).not.toContain('const updated = await db.diffComment.update(id, data)')
  })

  it('routes diff comment deletion through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useDiffCommentStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('const deleted = await dbApi.diffComment.delete(id)')
    expect(source).not.toContain('const deleted = await db.diffComment.delete(id)')
  })

  it('routes diff comment clearing through dbApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/stores/useDiffCommentStore.ts'),
      'utf-8'
    )

    expect(source).toContain("import { dbApi } from '@/api/db-api'")
    expect(source).toContain('await dbApi.diffComment.clearAll(worktreeId)')
    expect(source).not.toContain('await db.diffComment.clearAll(worktreeId)')
    expect(source).not.toContain('unwrapEnvelopeApi(() => window.db)')
  })

  it('routes settings privacy enabled-state loading through analyticsApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/settings/SettingsPrivacy.tsx'),
      'utf-8'
    )
    const effectStart = source.indexOf('  useEffect(() => {')
    const effectEnd = source.indexOf('\n  const handleToggle = () => {', effectStart)
    const effectSource = source.slice(effectStart, effectEnd)

    expect(effectStart).toBeGreaterThan(-1)
    expect(effectEnd).toBeGreaterThan(effectStart)
    expect(source).toContain("import { analyticsApi } from '@/api/analytics-api'")
    expect(effectSource).toContain('analyticsApi')
    expect(effectSource).toContain('.isEnabled()')
    expect(effectSource).toContain('setEnabled(val)')
    expect(effectSource).toContain('setLoaded(true)')
    expect(effectSource).not.toContain('window.analyticsOps')
  })

  it('routes settings privacy analytics toggle through analyticsApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/settings/SettingsPrivacy.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('  const handleToggle = () => {')
    const handlerEnd = source.indexOf('\n  if (!loaded) return <div />', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { analyticsApi } from '@/api/analytics-api'")
    expect(handlerSource).toContain('const newValue = !enabled')
    expect(handlerSource).toContain('setEnabled(newValue)')
    expect(handlerSource).toContain("updateSetting('telemetryEnabled', newValue)")
    expect(handlerSource).toContain('analyticsApi.setEnabled(newValue)')
    expect(handlerSource).not.toContain('window.analyticsOps')
  })

  it('routes auto-selected onboarding analytics through analyticsApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/setup/AgentSetupGuard.tsx'),
      'utf-8'
    )
    const autoSelectedStart = source.indexOf('          // Exactly one found')
    const multiFoundStart = source.indexOf('        } else {', autoSelectedStart)
    const autoSelectedSource = source.slice(autoSelectedStart, multiFoundStart)

    expect(autoSelectedStart).toBeGreaterThan(-1)
    expect(multiFoundStart).toBeGreaterThan(autoSelectedStart)
    expect(source).toContain("import { analyticsApi } from '@/api/analytics-api'")
    expect(autoSelectedSource).toContain("analyticsApi.track('onboarding_completed', {")
    expect(autoSelectedSource).toContain('sdk: found[0]')
    expect(autoSelectedSource).toContain('auto_selected: true')
    expect(autoSelectedSource).not.toContain('window.analyticsOps.track')
  })

  it('routes manual-selected onboarding analytics through analyticsApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/setup/AgentSetupGuard.tsx'),
      'utf-8'
    )
    const manualSelectedStart = source.indexOf('        onSelect={(sdk) => {')
    const manualSelectedEnd = source.indexOf('          setStatus(', manualSelectedStart)
    const manualSelectedSource = source.slice(manualSelectedStart, manualSelectedEnd)

    expect(manualSelectedStart).toBeGreaterThan(-1)
    expect(manualSelectedEnd).toBeGreaterThan(manualSelectedStart)
    expect(source).toContain("import { analyticsApi } from '@/api/analytics-api'")
    expect(manualSelectedSource).toContain("analyticsApi.track('onboarding_completed', {")
    expect(manualSelectedSource).toContain('sdk,')
    expect(manualSelectedSource).toContain('auto_selected: false')
    expect(manualSelectedSource).not.toContain('window.analyticsOps.track')
  })

  it('routes settings advanced performance diagnostics toggle through perfDiagnosticsApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/settings/SettingsAdvanced.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('  const handlePerfDiagnosticsToggle = (): void => {')
    const handlerEnd = source.indexOf('\n  const handleCodexJsonlLoggingToggle', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { perfDiagnosticsApi } from '@/api/perf-diagnostics-api'")
    expect(handlerSource).toContain('const newValue = !perfDiagnosticsEnabled')
    expect(handlerSource).toContain("updateSetting('perfDiagnosticsEnabled', newValue)")
    expect(handlerSource).toContain('perfDiagnosticsApi.enable(newValue)')
    expect(handlerSource).toContain(
      "toast.success(newValue ? 'Performance diagnostics enabled' : 'Performance diagnostics disabled')"
    )
    expect(handlerSource).not.toContain('window.perfDiagnosticsOps')
  })

  it('routes settings advanced Codex JSONL logging toggle through codexDebugLoggerApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/settings/SettingsAdvanced.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('  const handleCodexJsonlLoggingToggle = (): void => {')
    const handlerEnd = source.indexOf('\n  const handleCodexJsonlResetToggle', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { codexDebugLoggerApi } from '@/api/codex-debug-logger-api'")
    expect(handlerSource).toContain('const newValue = !codexJsonlLoggingEnabled')
    expect(handlerSource).toContain("updateSetting('codexJsonlLoggingEnabled', newValue)")
    expect(handlerSource).toContain(
      'codexDebugLoggerApi.configure(newValue, codexJsonlResetPerSession)'
    )
    expect(handlerSource).toContain(
      "toast.success(newValue ? 'Codex JSONL logging enabled' : 'Codex JSONL logging disabled')"
    )
    expect(handlerSource).not.toContain('window.codexDebugLoggerOps')
  })

  it('routes settings advanced Codex JSONL reset toggle through codexDebugLoggerApi', () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, '../../src/renderer/src/components/settings/SettingsAdvanced.tsx'),
      'utf-8'
    )
    const handlerStart = source.indexOf('  const handleCodexJsonlResetToggle = (): void => {')
    const handlerEnd = source.indexOf('\n  const handleAdd = () => {', handlerStart)
    const handlerSource = source.slice(handlerStart, handlerEnd)

    expect(handlerStart).toBeGreaterThan(-1)
    expect(handlerEnd).toBeGreaterThan(handlerStart)
    expect(source).toContain("import { codexDebugLoggerApi } from '@/api/codex-debug-logger-api'")
    expect(handlerSource).toContain('const newValue = !codexJsonlResetPerSession')
    expect(handlerSource).toContain("updateSetting('codexJsonlResetPerSession', newValue)")
    expect(handlerSource).toContain(
      'codexDebugLoggerApi.configure(codexJsonlLoggingEnabled, newValue)'
    )
    expect(handlerSource).toContain(
      "toast.success(\n      newValue ? 'Log will reset each Codex session' : 'Log will append across sessions'\n    )"
    )
    expect(handlerSource).not.toContain('window.codexDebugLoggerOps')
  })
})
